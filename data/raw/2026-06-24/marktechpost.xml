<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	 xmlns:media="http://search.yahoo.com/mrss/" >

<channel>
	<title>MarkTechPost</title>
	<atom:link href="https://www.marktechpost.com/feed/" rel="self" type="application/rss+xml" />
	<link>https://www.marktechpost.com/</link>
	<description>An Artificial Intelligence News Platform</description>
	<lastBuildDate>Tue, 23 Jun 2026 19:37:35 +0000</lastBuildDate>
	<language>en-US</language>
	<sy:updatePeriod>
	hourly	</sy:updatePeriod>
	<sy:updateFrequency>
	1	</sy:updateFrequency>
	<generator>https://wordpress.org/?v=7.0</generator>

<image>
	<url>https://www.marktechpost.com/wp-content/uploads/2022/04/cropped-Favicon-512-x-512-1-1-32x32.png</url>
	<title>MarkTechPost</title>
	<link>https://www.marktechpost.com/</link>
	<width>32</width>
	<height>32</height>
</image> 
<site xmlns="com-wordpress:feed-additions:1">127842392</site>	<item>
		<title>Datalab Releases lift: A 9B Open-Weights Vision Model That Extracts Structured JSON From PDFs Using Schemas</title>
		<link>https://www.marktechpost.com/2026/06/23/datalab-releases-lift-a-9b-open-weights-vision-model-that-extracts-structured-json-from-pdfs-using-schemas/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Tue, 23 Jun 2026 19:35:33 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Computer Vision]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Generative AI]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[OCR]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Promote]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Sponsored]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80667</guid>

					<description><![CDATA[<p>Datalab released lift, a 9B open-weights vision model that turns PDFs and images into schema-matching JSON. It uses schema-constrained decoding for valid structure and trained abstention to return null instead of hallucinating absent fields, scoring 90.2% field accuracy on a 225-document benchmark.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/datalab-releases-lift-a-9b-open-weights-vision-model-that-extracts-structured-json-from-pdfs-using-schemas/">Datalab Releases lift: A 9B Open-Weights Vision Model That Extracts Structured JSON From PDFs Using Schemas</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph"><a href="https://pxllnk.co/euw154">Datalab</a> has released <strong><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">lift</a></strong>, a 9B open-weights vision model for structured extraction. You pass it a JSON schema, and it returns a JSON object that matches. The model reads PDFs and images directly, then decodes against your schema.</p>



<p class="wp-block-paragraph">This is Datalab&#8217;s first model built purely for extraction. The team already ships open-source OCR tools: <a href="https://github.com/datalab-to/chandra">chandra</a>, <a href="https://github.com/datalab-to/marker">marker</a>, and <a href="https://github.com/datalab-to/surya">surya</a>. <a href="https://pxllnk.co/t0x8a0r">lift </a>extends that work into schema-driven field extraction.</p>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/t0x8a0r">lift</a> scores <strong>90.2% field accuracy</strong> on Datalab&#8217;s 225-document benchmark. The research team reports it as the strongest small self-hostable model they tested. It runs at <a href="https://pxllnk.co/mf4o7kl">a median of <strong>9.5 seconds per document</strong></a>.</p>



<h2 id="h-what-is-datalab-lift" class="wp-block-heading"><strong>What is Datalab lift?</strong></h2>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/t0x8a0r">lift </a>is a 9B-parameter vision model for structured extraction. It accepts standard JSON Schema as input. It returns valid JSON of that shape as output.</p>



<p class="wp-block-paragraph">The model handles multi-page documents in a single pass. It can read values that span across pages. Whole documents go in at once, not page by page.</p>



<p class="wp-block-paragraph">Two inference modes ship with the package. Local inference runs through HuggingFace. Remote inference runs through a vLLM server, which Datalab recommends for production.</p>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">The code is Apache 2.0. The weights use a modified OpenRAIL-M license.</a></p>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/t0x8a0r">lift</a> enters a small but growing field of open extraction models. Some are purpose-built, like the NuExtract family. Others are general vision-language models pressed into extraction, like Qwen3.5-9B. It pairs a vision-language base with schema-constrained decoding and trained abstention. On Datalab&#8217;s benchmark, it leads that open group on field accuracy.</p>



<h2 id="h-schema-constrained-decoding-the-core-mechanism" class="wp-block-heading"><strong>Schema-Constrained Decoding: The Core Mechanism</strong></h2>



<p class="wp-block-paragraph">The main design choice is schema-constrained decoding. lift decodes its output directly against your schema. The result is always valid JSON of the correct shape.</p>



<p class="wp-block-paragraph">Here is what happens under the hood. <a href="https://pxllnk.co/t0x8a0r">lift</a> first turns your JSON Schema into a Pydantic model. It then normalizes that into a strict JSON Schema. The schema is passed to the vLLM server as a response_format constraint.</p>



<p class="wp-block-paragraph">During generation, the server compiles the schema into a grammar. At each step, the model assigns a probability to every possible next token. The grammar defines which tokens are valid continuations. Tokens that would break the schema are masked out. The model can only sample from what remains.</p>



<p class="wp-block-paragraph">This is why the output is always valid JSON of the right shape. The structure is enforced token by token, not checked afterward.</p>



<p class="wp-block-paragraph">There is a sharp limit to this guarantee. Constrained decoding governs structure and types, not meaning. A field typed as number will hold a number. Whether it holds the <em>correct</em> number is a separate question. The model can emit a valid value that is simply wrong. Validity is not correctness.</p>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">lift</a> also widens every field to allow null. Each scalar leaf in the compiled schema accepts its type or null. So the model can abstain on any field without breaking the structure. Abstention is both trained behavior and a property of the constraint.</p>



<p class="wp-block-paragraph">You write standard JSON Schema. Supported types include string, number, integer, boolean, arrays of those, arrays of objects, and nested objects. A field description guides the model when a name is ambiguous.</p>



<p class="wp-block-paragraph">This is also where a quiet failure mode lives. Some constructs cannot be compiled: enum, anyOf/oneOf, $ref, and additionalProperties. When <a href="https://pxllnk.co/t0x8a0r">lift</a> cannot compile your schema, it does not stop. It logs a warning and generates without the constraint. The structural guarantee is gone for that run, with no hard error. Output may then fail to match your schema at all.</p>



<p class="wp-block-paragraph">The practical rule is simple. Keep schemas inside the supported subset. Validate the returned JSON against your schema downstream. Do not assume valid output just because the call returned.</p>



<p class="wp-block-paragraph"><strong>Here is a simple invoice schema:</strong></p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">{
  "type": "object",
  "properties": {
    "invoice_number": {"type": "string", "description": "Invoice identifier"},
    "total": {"type": "number", "description": "Total amount due"},
    "line_items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "description": {"type": "string"},
          "amount": {"type": "number"}
        }
      }
    }
  },
  "required": ["invoice_number", "total"]
}</code></pre></div></div>



<h2 id="h-abstention-by-default" class="wp-block-heading"><strong>Abstention by Default</strong></h2>



<p class="wp-block-paragraph">Real extraction is hard for a non-obvious reason. Beyond reading fields that exist, the real challenge is not inventing fields that are absent.</p>



<p class="wp-block-paragraph">A model that hallucinates a tax ID is worse than one returning nothing. The error is silent and hard to catch downstream. lift is trained to leave genuinely missing fields null.</p>



<p class="wp-block-paragraph">Mark a field required only when it must appear. Fields absent from a document come back null. This gives you an extractor that can report a value is not present.</p>



<h2 id="h-benchmark" class="wp-block-heading"><strong>Benchmark</strong></h2>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/euw154">Datalab</a> evaluated <a href="https://pxllnk.co/t0x8a0r">lift </a>on a 225-document extraction benchmark. Documents ran 6 to 64 pages each, with roughly 11,000 scored fields. Adversarial cases were planted throughout the set.</p>



<p class="wp-block-paragraph">Those cases include cross-page values and exhaustive lists. They also include fields that must be left null and near-miss distractors. Multi-source aggregation was tested as well.</p>



<p class="wp-block-paragraph">Every model received the same rendered page images. Each extracted every document in a single pass. Scoring was a deterministic exact-match against ground truth, with numeric tolerance and normalized strings.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th><strong>Model</strong></th><th><strong>Size</strong></th><th><strong>Field accuracy</strong></th><th><strong>Full-document accuracy</strong></th><th><strong>Median latency*</strong></th><th><strong>Features</strong></th></tr></thead><tbody><tr><td>Datalab API</td><td>—</td><td>95.9%</td><td>44.4%</td><td>30.8s</td><td>Citations + Verification</td></tr><tr><td>Gemini Flash 3.5</td><td>—</td><td>91.3%</td><td>40.0%</td><td>28.1s</td><td></td></tr><tr><td><strong>lift</strong></td><td><strong>9B</strong></td><td><strong>90.2%</strong></td><td>20.9%</td><td><strong>9.5s</strong></td><td></td></tr><tr><td>Azure Content Understanding</td><td>—</td><td>83.4%</td><td>22.2%</td><td>73.7s</td><td>Citations</td></tr><tr><td>NuExtract3</td><td>4B</td><td>81.5%</td><td>8.4%</td><td>8.3s</td><td></td></tr><tr><td>Qwen3.5-9B</td><td>9B</td><td>76.32%</td><td>24.0%</td><td>16.8s</td><td></td></tr></tbody></table></figure>



<p class="wp-block-paragraph">* <em>Per document, 8 concurrent requests. Local models (<a href="https://pxllnk.co/t0x8a0r">lift</a>, Qwen3.5-9B, NuExtract3) were served with vLLM on a single GPU. Gemini, Datalab, and Azure ran via API. Latency varies with hardware and load; treat it as relative.</em></p>



<p class="wp-block-paragraph">Two details matter here. <strong>Field accuracy</strong> is the fraction of individual fields extracted correctly. <strong>Full-document accuracy</strong> is the fraction of documents where every field is correct.</p>



<p class="wp-block-paragraph">On field accuracy, lift leads the self-hostable models. It sits ahead of NuExtract3 and the Qwen3.5-9B base.&nbsp; It is also the fastest of the accurate models in the table.</p>



<p class="wp-block-paragraph">At 9.5s median, lift is roughly 3x faster than Gemini Flash 3.5. It stays within about a point of that model&#8217;s field accuracy. Full-document accuracy is a harder metric: every field must be correct. Here <a href="https://pxllnk.co/t0x8a0r">lift</a> scores 20.9%, ahead of only NuExtract3. The hosted APIs lead, at 44.4% and 40.0%.</p>



<p class="wp-block-paragraph">A note on reading these numbers. This is Datalab&#8217;s own benchmark, so treat it as a vendor result. Its adversarial design rewards models tuned to abstain, which <a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">lift</a> is. Full-document accuracy is low for every model, topping out at 44.4%. That reflects how hard single-pass extraction is on long documents. The numbers are also a snapshot; models change.</p>



<p class="wp-block-paragraph">This is the reality of single-pass, single-model extraction on hard documents. It tells you where <a href="https://pxllnk.co/t0x8a0r">lift</a> fits. It is excellent for field-level extraction that feeds a human-in-the-loop review or aggregate analytics. It is not yet a drop-in for zero-touch, every-field-must-be-perfect automation. For that last mile, <a href="https://pxllnk.co/euw154">Datalab&#8217;s</a> hosted API adds per-field verification, citations, and confidence scores on the same approach.</p>



<h2 id="h-a-practitioner-workflow-from-schema-to-reviewed-data" class="wp-block-heading"><strong>A Practitioner Workflow: From Schema to Reviewed Data</strong></h2>



<p class="wp-block-paragraph">Three use cases show the shape of the work. <strong>Invoice processing</strong>: define invoice_number, total, and line_items, and a missing tax_id returns null. <strong>Contract review</strong>: a two-page agreement carries a value across pages, which single-pass extraction stitches together. <strong>Document pipelines</strong>: an accounts-payable queue trusts that absent due dates return null, avoiding silent errors.</p>



<p class="wp-block-paragraph">Here is one of them as an end-to-end workflow. The goal is a clean, reviewed dataset, not raw model output.</p>



<p class="wp-block-paragraph"><strong>1. Define the schema.</strong> Add a description to any field whose name is not obvious. Mark only truly mandatory fields as required.</p>



<p class="wp-block-paragraph"><strong>2. Run extraction.</strong> Pass the schema and the file to lift. Use a dict, a file path, or a saved schema name.</p>



<p class="wp-block-paragraph"><strong>3. Branch on the result.</strong> A failed call or a null extraction goes to review. A missing required value also goes to review, since null is abstention, not an error.</p>



<p class="wp-block-paragraph"><strong>4. Validate before you trust.</strong> Check the returned JSON against your schema. This catches the silent fallback when a schema could not be compiled.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">from lift import extract

schema = {
    "type": "object",
    "properties": {
        "invoice_number": {"type": "string", "description": "Invoice identifier"},
        "total": {"type": "number", "description": "Total amount due"},
        "due_date": {"type": "string", "description": "Payment due date, ISO 8601"},
        "line_items": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "description": {"type": "string"},
                    "amount": {"type": "number"}
                }
            }
        }
    },
    "required": ["invoice_number", "total"]
}

result = extract("invoice.pdf", schema)

if result.error or result.extraction is None:
    queue_for_review("invoice.pdf", reason="extraction_failed")
else:
    data = result.extraction
    # A required field can still be null. That is abstention, not a crash.
    if data.get("total") is None:
        queue_for_review("invoice.pdf", reason="missing_total")
    else:
        save(data)</code></pre></div></div>



<p class="wp-block-paragraph">A few schema-design tips that pay off in practice:</p>



<ul class="wp-block-list">
<li>Write a description for ambiguous fields; it is your main lever on accuracy.</li>



<li>Keep schemas inside the supported subset, and validate output downstream.</li>



<li>Prefer flat, shallow schemas; deep nesting is harder to extract reliably.</li>



<li>Mark fields required sparingly, so genuine gaps can return null.</li>



<li>Use &#8211;page-range (CLI) or page_range (Python) to limit long PDFs.</li>



<li>Reuse one InferenceManager across calls to amortize model load.</li>
</ul>



<h2 id="h-self-host-vs-hosted-which-to-use" class="wp-block-heading"><strong>Self-Host vs. Hosted: Which to Use</strong></h2>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">lift</a> ships as open weights, and <a href="https://pxllnk.co/euw154">Datalab</a> runs a hosted API on the same approach. The choice is about constraints, not prestige.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th><strong>Choose</strong></th><th><strong>When</strong></th></tr></thead><tbody><tr><td><strong>Self-hosted lift</strong> (open weights)</td><td>Data residency or on-prem rules apply; you need cost control at high volume; you want latency control on your own GPUs; runs must work offline.</td></tr><tr><td><strong>Hosted Datalab API</strong></td><td>You need per-field verification, citations, and confidence scores; you want the highest accuracy; you would rather not manage infrastructure; volume is low or bursty.</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">One caveat for self-hosting. Commercial use needs a license under the modified OpenRAIL-M terms. It is free for research, personal use, and startups under $5M in funding or revenue, and not for use in competition with Datalab&#8217;s API.</p>



<h2 id="h-getting-started" class="wp-block-heading"><strong>Getting Started</strong></h2>



<p class="wp-block-paragraph">The fastest path is the CLI. lift-pdf requires Python 3.12 or newer.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">pip install lift-pdf

# Serve the model with vLLM (recommended)
lift_vllm

# Extract against a schema
lift_extract input.pdf ./output --schema schema.json</code></pre></div></div>



<p class="wp-block-paragraph">Each file produces two outputs. &lt;filename&gt;.json holds the extraction matching your schema. &lt;filename&gt;_metadata.json holds page count, token count, and error info for debugging.</p>



<p class="wp-block-paragraph">The Python API is equally small:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">from lift import extract

# schema: a dict, a path, an inline JSON string, or a library name
result = extract("document.pdf", "schema.json")
if result.extraction is not None:
    data = result.extraction  # dict matching the schema</code></pre></div></div>



<p class="wp-block-paragraph">Check result.extraction for the dict matching your schema. A null extraction signals a failure you can inspect. The HuggingFace backend uses &#8211;method hf and needs pip install lift-pdf[hf].</p>



<p class="wp-block-paragraph"><strong>Schema Studio</strong> ships as a Streamlit app. It lets you build, save, and test schemas against your own documents. Install it with pip install lift-pdf[app], then run lift_app.</p>



<p class="wp-block-paragraph">For production, lift_vllm launches a Docker container with batch size scaled to your GPU. Supported GPUs are: h100, a100-80, a100/a100-40, l40s, a10, l4, 4090, 3090, t4.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- lift interactive demo — paste into a WordPress "Custom HTML" block -->
<iframe
  title="lift schema extraction demo"
  style="width:100%;border:0;display:block;border-radius:10px;overflow:hidden"
  height="780"
  loading="lazy"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;lift schema extraction demo&lt;/title&gt;
&lt;style&gt;
  :root{
    --bg:#ffffff; --ink:#0a0a0a; --soft:#52525b; --line:#e4e4e7;
    --panel:#fafafa; --chip:#f4f4f5; --hi:#000000; --hiink:#ffffff;
    --null:#a1a1aa; --mono:'SFMono-Regular',ui-monospace,'JetBrains Mono',Menlo,Consolas,monospace;
    --sans:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Helvetica,Arial,sans-serif;
  }
  *{box-sizing:border-box}
  body{margin:0;background:var(--bg);color:var(--ink);font-family:var(--sans);
    -webkit-font-smoothing:antialiased;line-height:1.45;font-size:14px}
  .wrap{max-width:980px;margin:0 auto;padding:18px 18px 8px}
  .head{border:1px solid var(--line);border-bottom:2px solid var(--ink);
    border-radius:10px 10px 0 0;padding:14px 16px;background:var(--bg)}
  .kicker{font-family:var(--mono);font-size:11px;letter-spacing:.14em;
    text-transform:uppercase;color:var(--soft)}
  .title{font-size:19px;font-weight:700;margin:4px 0 2px;letter-spacing:-.01em}
  .sub{font-size:12.5px;color:var(--soft)}
  .grid{display:grid;grid-template-columns:1fr 1fr;gap:0;
    border:1px solid var(--line);border-top:none}
  .col{padding:14px 16px}
  .col + .col{border-left:1px solid var(--line)}
  .step{font-family:var(--mono);font-size:10.5px;letter-spacing:.12em;
    text-transform:uppercase;color:var(--soft);margin:0 0 9px;display:flex;
    align-items:center;gap:8px}
  .step .n{display:inline-flex;width:18px;height:18px;align-items:center;
    justify-content:center;border:1px solid var(--ink);border-radius:50%;
    font-size:10px;color:var(--ink)}
  .tabs{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:12px}
  .tab{font-family:var(--mono);font-size:11.5px;border:1px solid var(--line);
    background:var(--bg);color:var(--ink);padding:6px 10px;border-radius:6px;
    cursor:pointer;transition:.12s}
  .tab:hover{border-color:var(--ink)}
  .tab[aria-selected=&quot;true&quot;]{background:var(--hi);color:var(--hiink);border-color:var(--hi)}
  .doc{border:1px solid var(--line);background:var(--panel);border-radius:8px;
    padding:12px;font-size:11.5px;height:250px;overflow:auto}
  .doc .pg{font-family:var(--mono);font-size:9.5px;letter-spacing:.1em;
    color:var(--soft);text-transform:uppercase;border-bottom:1px dashed var(--line);
    padding-bottom:4px;margin:0 0 8px}
  .doc h4{margin:0 0 6px;font-size:13px}
  .doc table{width:100%;border-collapse:collapse;margin:6px 0;font-size:11px}
  .doc td{border-bottom:1px solid var(--line);padding:3px 4px}
  .doc .mut{color:var(--soft)}
  .doc .row{display:flex;justify-content:space-between;gap:8px;padding:2px 0}
  .fields{display:flex;flex-direction:column;gap:6px;height:208px;overflow:auto;
    padding-right:2px}
  .f{display:flex;align-items:flex-start;gap:9px;border:1px solid var(--line);
    border-radius:7px;padding:8px 10px;cursor:pointer;background:var(--bg);
    transition:.12s}
  .f:hover{border-color:var(--ink)}
  .f input{margin:2px 0 0;accent-color:#000;width:14px;height:14px;cursor:pointer}
  .f .meta{flex:1;min-width:0}
  .f .name{font-family:var(--mono);font-size:12px}
  .f .desc{font-size:11px;color:var(--soft);margin-top:1px}
  .f .tag{font-family:var(--mono);font-size:9px;letter-spacing:.08em;
    text-transform:uppercase;border:1px solid var(--line);border-radius:4px;
    padding:1px 5px;color:var(--soft);white-space:nowrap;margin-top:3px;
    display:inline-block}
  .f .tag.absent{border-color:var(--ink);color:var(--ink)}
  .req{font-family:var(--mono);font-size:9px;color:var(--soft)}
  .ctrls{display:flex;align-items:center;gap:10px;margin-top:12px}
  .btn{font-family:var(--mono);font-size:12px;letter-spacing:.03em;
    background:var(--hi);color:var(--hiink);border:1px solid var(--hi);
    padding:9px 16px;border-radius:7px;cursor:pointer;transition:.12s}
  .btn:hover{opacity:.85}
  .btn.ghost{background:var(--bg);color:var(--ink);border-color:var(--line)}
  .btn.ghost:hover{border-color:var(--ink)}
  .out{border:1px solid var(--line);border-top:none;border-radius:0 0 10px 10px;
    padding:14px 16px;background:var(--bg)}
  .outhead{display:flex;align-items:center;justify-content:space-between;
    gap:10px;margin-bottom:8px;flex-wrap:wrap}
  .outhead .lbl{font-family:var(--mono);font-size:10.5px;letter-spacing:.12em;
    text-transform:uppercase;color:var(--soft)}
  .stats{display:flex;gap:14px;font-family:var(--mono);font-size:11px;color:var(--soft)}
  .stats b{color:var(--ink);font-weight:600}
  pre{margin:0;background:var(--panel);border:1px solid var(--line);
    border-radius:8px;padding:12px 14px;font-family:var(--mono);font-size:12px;
    line-height:1.6;overflow:auto;max-height:250px}
  .k{color:var(--ink)}
  .s{color:#27272a}
  .n{color:#3f3f46}
  .nul{color:var(--null);font-style:italic}
  .badge{font-family:var(--mono);font-size:9px;letter-spacing:.06em;
    text-transform:uppercase;border-radius:4px;padding:1px 6px;margin-left:8px}
  .badge.found{background:var(--ink);color:#fff}
  .badge.absn{background:var(--bg);color:var(--ink);border:1px solid var(--ink)}
  .note{font-size:11.5px;color:var(--soft);margin-top:10px;line-height:1.5}
  .note code{font-family:var(--mono);background:var(--chip);padding:1px 5px;
    border-radius:4px;color:var(--ink)}
  .foot{display:flex;align-items:center;justify-content:space-between;
    gap:10px;flex-wrap:wrap;padding:12px 2px 4px;margin-top:6px;
    border-top:1px solid var(--line);color:var(--soft);font-size:11px}
  .foot .brand{font-family:var(--mono);font-size:11px;color:var(--ink);font-weight:600}
  .foot .brand span{color:var(--soft);font-weight:400}
  a{color:var(--ink)}
  @media (max-width:640px){
    .grid{grid-template-columns:1fr}
    .col + .col{border-left:none;border-top:1px solid var(--line)}
    .doc{height:220px}.fields{height:auto;max-height:260px}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot;&gt;
  &lt;div class=&quot;head&quot;&gt;
    &lt;div class=&quot;kicker&quot;&gt;Interactive demo · illustrative&lt;/div&gt;
    &lt;div class=&quot;title&quot;&gt;lift — schema &amp;rarr; structured JSON&lt;/div&gt;
    &lt;div class=&quot;sub&quot;&gt;Pick a document, choose the schema fields, and extract. lift returns valid JSON of your schema's shape, and leaves genuinely absent fields &lt;code style=&quot;font-family:var(--mono)&quot;&gt;null&lt;/code&gt;.&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;grid&quot;&gt;
    &lt;div class=&quot;col&quot;&gt;
      &lt;div class=&quot;step&quot;&gt;&lt;span class=&quot;n&quot;&gt;1&lt;/span&gt; Document&lt;/div&gt;
      &lt;div class=&quot;tabs&quot; id=&quot;tabs&quot; role=&quot;tablist&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;doc&quot; id=&quot;doc&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;col&quot;&gt;
      &lt;div class=&quot;step&quot;&gt;&lt;span class=&quot;n&quot;&gt;2&lt;/span&gt; Schema fields&lt;/div&gt;
      &lt;div class=&quot;fields&quot; id=&quot;fields&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;ctrls&quot;&gt;
        &lt;button class=&quot;btn&quot; id=&quot;run&quot;&gt;Extract&lt;/button&gt;
        &lt;button class=&quot;btn ghost&quot; id=&quot;all&quot;&gt;Toggle all&lt;/button&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;out&quot;&gt;
    &lt;div class=&quot;outhead&quot;&gt;
      &lt;span class=&quot;lbl&quot;&gt;Output · &amp;lt;filename&amp;gt;.json&lt;/span&gt;
      &lt;div class=&quot;stats&quot; id=&quot;stats&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;pre id=&quot;json&quot;&gt;&lt;span class=&quot;nul&quot;&gt;// Select fields and press Extract&lt;/span&gt;&lt;/pre&gt;
    &lt;div class=&quot;note&quot; id=&quot;note&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;foot&quot;&gt;
    &lt;div class=&quot;brand&quot;&gt;Marktechpost &lt;span&gt;· interactive embed&lt;/span&gt;&lt;/div&gt;
    &lt;div&gt;Illustrative demo of lift's schema-constrained extraction. Run the real 9B model via &lt;code style=&quot;font-family:var(--mono)&quot;&gt;pip install lift-pdf&lt;/code&gt;.&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
/* ---- Illustrative dataset: fields a real lift schema might define.
   &quot;absent&quot; fields are not present in the document, so lift returns null. ---- */
const DOCS = {
  invoice: {
    label: &quot;Invoice (1 page)&quot;,
    render: `&lt;div class=&quot;pg&quot;&gt;Page 1 / 1 — RPC Wireless&lt;/div&gt;
      &lt;h4&gt;INVOICE  #INV-20458&lt;/h4&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Bill to&lt;/span&gt;&lt;span&gt;Acme Logistics LLC&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Issue date&lt;/span&gt;&lt;span&gt;2024-09-13&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Due date&lt;/span&gt;&lt;span&gt;2024-10-13&lt;/span&gt;&lt;/div&gt;
      &lt;table&gt;
        &lt;tr&gt;&lt;td&gt;Mounting hardware&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;$120.00&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;Antenna unit ×2&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;$410.00&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;Install labor&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;$310.00&lt;/td&gt;&lt;/tr&gt;
      &lt;/table&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Total due&lt;/span&gt;&lt;span&gt;&lt;b&gt;$840.00&lt;/b&gt;&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;mut&quot; style=&quot;margin-top:8px&quot;&gt;No tax registration number printed on this invoice.&lt;/div&gt;`,
    fields: [
      {name:&quot;invoice_number&quot;, desc:&quot;Invoice identifier&quot;, type:&quot;string&quot;, req:true, val:&quot;INV-20458&quot;},
      {name:&quot;total&quot;, desc:&quot;Total amount due&quot;, type:&quot;number&quot;, req:true, val:840.00},
      {name:&quot;due_date&quot;, desc:&quot;Payment due date&quot;, type:&quot;string&quot;, req:false, val:&quot;2024-10-13&quot;},
      {name:&quot;tax_id&quot;, desc:&quot;Seller tax / VAT number&quot;, type:&quot;string&quot;, req:false, absent:true},
      {name:&quot;line_items&quot;, desc:&quot;Array of {description, amount}&quot;, type:&quot;array&quot;, req:false, val:[
        {description:&quot;Mounting hardware&quot;, amount:120.00},
        {description:&quot;Antenna unit ×2&quot;, amount:410.00},
        {description:&quot;Install labor&quot;, amount:310.00}
      ]}
    ]
  },
  contract: {
    label: &quot;Service agreement (2 pages)&quot;,
    render: `&lt;div class=&quot;pg&quot;&gt;Page 1 / 2 — Master Services Agreement&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Party A&lt;/span&gt;&lt;span&gt;Northwind Systems Inc.&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Party B&lt;/span&gt;&lt;span&gt;Brightline Data Co.&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Effective&lt;/span&gt;&lt;span&gt;2026-01-05&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;mut&quot; style=&quot;margin:8px 0&quot;&gt;3. TERM. The total contract value is defined in
        Schedule A, payable across the term set out below…&lt;/div&gt;
      &lt;div class=&quot;pg&quot; style=&quot;margin-top:10px&quot;&gt;Page 2 / 2 — Schedule A&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Schedule A value&lt;/span&gt;&lt;span&gt;$48,000 over 12 months&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Termination notice&lt;/span&gt;&lt;span&gt;30 days&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Governing law&lt;/span&gt;&lt;span&gt;State of Delaware&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;mut&quot; style=&quot;margin-top:8px&quot;&gt;No auto-renewal clause appears in this agreement.&lt;/div&gt;`,
    fields: [
      {name:&quot;party_a&quot;, desc:&quot;First contracting party&quot;, type:&quot;string&quot;, req:true, val:&quot;Northwind Systems Inc.&quot;},
      {name:&quot;party_b&quot;, desc:&quot;Second contracting party&quot;, type:&quot;string&quot;, req:true, val:&quot;Brightline Data Co.&quot;},
      {name:&quot;effective_date&quot;, desc:&quot;Agreement effective date&quot;, type:&quot;string&quot;, req:false, val:&quot;2026-01-05&quot;},
      {name:&quot;total_value&quot;, desc:&quot;Contract value (spans pages 1→2)&quot;, type:&quot;string&quot;, req:false, span:true, val:&quot;$48,000 over 12 months&quot;},
      {name:&quot;termination_notice_days&quot;, desc:&quot;Required notice period&quot;, type:&quot;integer&quot;, req:false, val:30},
      {name:&quot;governing_law&quot;, desc:&quot;Governing jurisdiction&quot;, type:&quot;string&quot;, req:false, val:&quot;State of Delaware&quot;},
      {name:&quot;auto_renewal&quot;, desc:&quot;Whether the contract auto-renews&quot;, type:&quot;boolean&quot;, req:false, absent:true}
    ]
  },
  lab: {
    label: &quot;Lab report (3 pages)&quot;,
    render: `&lt;div class=&quot;pg&quot;&gt;Page 1 / 3 — Patient summary&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Patient ID&lt;/span&gt;&lt;span&gt;PT-77310&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Collected&lt;/span&gt;&lt;span&gt;2026-03-02&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;pg&quot; style=&quot;margin-top:10px&quot;&gt;Page 2 / 3 — Panel results&lt;/div&gt;
      &lt;table&gt;
        &lt;tr&gt;&lt;td&gt;Hemoglobin&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;13.8 g/dL&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;White cell count&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;6.1 ×10⁹/L&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;Platelets&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;240 ×10⁹/L&lt;/td&gt;&lt;/tr&gt;
      &lt;/table&gt;
      &lt;div class=&quot;pg&quot; style=&quot;margin-top:10px&quot;&gt;Page 3 / 3 — Notes&lt;/div&gt;
      &lt;div class=&quot;mut&quot;&gt;Reviewing physician sign-off pending; not yet recorded.&lt;/div&gt;`,
    fields: [
      {name:&quot;patient_id&quot;, desc:&quot;Patient identifier&quot;, type:&quot;string&quot;, req:true, val:&quot;PT-77310&quot;},
      {name:&quot;collection_date&quot;, desc:&quot;Sample collection date&quot;, type:&quot;string&quot;, req:false, val:&quot;2026-03-02&quot;},
      {name:&quot;results&quot;, desc:&quot;Array of {analyte, value} across pages&quot;, type:&quot;array&quot;, req:false, val:[
        {analyte:&quot;Hemoglobin&quot;, value:&quot;13.8 g/dL&quot;},
        {analyte:&quot;White cell count&quot;, value:&quot;6.1 ×10⁹/L&quot;},
        {analyte:&quot;Platelets&quot;, value:&quot;240 ×10⁹/L&quot;}
      ]},
      {name:&quot;reviewing_physician&quot;, desc:&quot;Signing physician name&quot;, type:&quot;string&quot;, req:false, absent:true}
    ]
  }
};

let current = &quot;invoice&quot;;
const selected = {}; // doc -&gt; Set(fieldName)

function ensureSel(doc){
  if(!selected[doc]){
    selected[doc] = new Set(DOCS[doc].fields.map(f=&gt;f.name));
  }
  return selected[doc];
}

const tabsEl = document.getElementById('tabs');
const docEl  = document.getElementById('doc');
const fieldsEl = document.getElementById('fields');
const jsonEl = document.getElementById('json');
const statsEl = document.getElementById('stats');
const noteEl = document.getElementById('note');

function buildTabs(){
  tabsEl.innerHTML = &quot;&quot;;
  Object.keys(DOCS).forEach(k=&gt;{
    const b=document.createElement('button');
    b.className='tab'; b.textContent=DOCS[k].label; b.setAttribute('role','tab');
    b.setAttribute('aria-selected', k===current ? 'true':'false');
    b.onclick=()=&gt;{ current=k; render(); };
    tabsEl.appendChild(b);
  });
}

function buildFields(){
  const sel = ensureSel(current);
  fieldsEl.innerHTML=&quot;&quot;;
  DOCS[current].fields.forEach(f=&gt;{
    const row=document.createElement('label');
    row.className='f';
    const cb=document.createElement('input');
    cb.type='checkbox'; cb.checked=sel.has(f.name);
    cb.onchange=()=&gt;{ cb.checked ? sel.add(f.name) : sel.delete(f.name); };
    const meta=document.createElement('div'); meta.className='meta';
    let tag = f.absent ? `&lt;span class=&quot;tag absent&quot;&gt;not in document&lt;/span&gt;`
            : f.span  ? `&lt;span class=&quot;tag&quot;&gt;spans pages&lt;/span&gt;` : &quot;&quot;;
    meta.innerHTML = `&lt;div class=&quot;name&quot;&gt;${f.name}&lt;span class=&quot;req&quot;&gt; : ${f.type}${f.req?' · required':''}&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;desc&quot;&gt;${f.desc}&lt;/div&gt;${tag}`;
    row.appendChild(cb); row.appendChild(meta);
    fieldsEl.appendChild(row);
  });
}

function jval(v, indent){
  const pad = '  '.repeat(indent);
  const pad2 = '  '.repeat(indent+1);
  if(v===null) return `&lt;span class=&quot;nul&quot;&gt;null&lt;/span&gt;`;
  if(typeof v==='number') return `&lt;span class=&quot;n&quot;&gt;${v}&lt;/span&gt;`;
  if(typeof v==='boolean') return `&lt;span class=&quot;n&quot;&gt;${v}&lt;/span&gt;`;
  if(typeof v==='string') return `&lt;span class=&quot;s&quot;&gt;&quot;${v}&quot;&lt;/span&gt;`;
  if(Array.isArray(v)){
    if(v.length===0) return '[]';
    const items=v.map(it=&gt;pad2+jval(it,indent+1)).join(',\n');
    return `[\n${items}\n${pad}]`;
  }
  // object
  const keys=Object.keys(v);
  const body=keys.map(k=&gt;`${pad2}&lt;span class=&quot;k&quot;&gt;&quot;${k}&quot;&lt;/span&gt;: ${jval(v[k],indent+1)}`).join(',\n');
  return `{\n${body}\n${pad}}`;
}

function extract(){
  const sel = ensureSel(current);
  const fields = DOCS[current].fields.filter(f=&gt;sel.has(f.name));
  if(fields.length===0){
    jsonEl.innerHTML = `&lt;span class=&quot;nul&quot;&gt;// No fields selected — nothing to extract&lt;/span&gt;`;
    statsEl.innerHTML=&quot;&quot;; noteEl.innerHTML=&quot;&quot;; emitSize(); return;
  }
  // Build object honoring schema shape; absent -&gt; null (abstention)
  const obj={}; let found=0, nulls=0, spans=0;
  fields.forEach(f=&gt;{
    if(f.absent){ obj[f.name]=null; nulls++; }
    else { obj[f.name]=f.val; found++; if(f.span) spans++; }
  });
  const lines = Object.keys(obj).map(k=&gt;{
    const f = fields.find(x=&gt;x.name===k);
    const badge = f.absent ? `&lt;span class=&quot;badge absn&quot;&gt;null · abstained&lt;/span&gt;`
                           : `&lt;span class=&quot;badge found&quot;&gt;found&lt;/span&gt;`;
    return `  &lt;span class=&quot;k&quot;&gt;&quot;${k}&quot;&lt;/span&gt;: ${jval(obj[k],1)}${badge}`;
  });
  jsonEl.innerHTML = `{\n${lines.join(',\n')}\n}`;
  statsEl.innerHTML = `&lt;span&gt;&lt;b&gt;${found}&lt;/b&gt; found&lt;/span&gt;&lt;span&gt;&lt;b&gt;${nulls}&lt;/b&gt; null&lt;/span&gt;&lt;span&gt;&lt;b&gt;${fields.length}&lt;/b&gt; fields&lt;/span&gt;`;
  let n = `Output is valid JSON of exactly the shape you selected — that is schema-constrained decoding. `;
  if(nulls&gt;0) n += `Fields not present in the document came back &lt;code&gt;null&lt;/code&gt; instead of a guess (abstention). `;
  if(spans&gt;0) n += `&lt;code&gt;total_value&lt;/code&gt; is resolved from values that span two pages, read in a single pass.`;
  noteEl.innerHTML = n;
  emitSize();
}

function render(){
  buildTabs();
  docEl.innerHTML = DOCS[current].render;
  buildFields();
  jsonEl.innerHTML = `&lt;span class=&quot;nul&quot;&gt;// Press Extract to run the selected schema&lt;/span&gt;`;
  statsEl.innerHTML=&quot;&quot;; noteEl.innerHTML=&quot;&quot;;
  emitSize();
}

document.getElementById('run').onclick = extract;
document.getElementById('all').onclick = ()=&gt;{
  const sel=ensureSel(current);
  const all = DOCS[current].fields;
  const allOn = all.every(f=&gt;sel.has(f.name));
  all.forEach(f=&gt; allOn ? sel.delete(f.name) : sel.add(f.name));
  buildFields(); emitSize();
};

/* auto-resize: post component offsetHeight + 40 to parent */
function emitSize(){
  requestAnimationFrame(()=&gt;{
    const h = (document.querySelector('.wrap').offsetHeight || document.body.offsetHeight) + 40;
    parent.postMessage({type:'lift-demo-height', height:h}, '*');
  });
}
window.addEventListener('load', ()=&gt;{ render(); emitSize(); });
window.addEventListener('resize', emitSize);
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>



<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">lift</a> is Datalab&#8217;s 9B open-weights vision model that extracts schema-matching JSON from PDFs and images.</li>



<li>Schema-constrained decoding guarantees valid structure; trained abstention returns null instead of hallucinating absent fields.</li>



<li>The structural guarantee covers shape, not meaning, so validate output and review low-confidence fields.</li>



<li>It posts the highest field accuracy among self-hostable models tested (90.2%), at 9.5s median per document.</li>



<li>Full-document accuracy is 20.9% — ahead of only NuExtract3 — where the hosted APIs lead.</li>



<li>Code is Apache 2.0; weights are modified OpenRAIL-M (free for research, personal use, and startups under $5M in funding or revenue).</li>
</ul>



<h4 id="h-the-links-to-try-it" class="wp-block-heading"><strong>The links to try it:</strong></h4>



<p class="wp-block-paragraph"><strong><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">GitHub </a>· <a href="https://pxllnk.co/t0x8a0r" target="_blank" rel="noreferrer noopener">HuggingFace</a> · <a href="https://pxllnk.co/mf4o7kl" target="_blank" rel="noreferrer noopener">Playground</a> · <a href="https://pxllnk.co/euw154" target="_blank" rel="noreferrer noopener">Hosted API &amp; docs</a></strong></p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"><em><sub>Note:Thanks to the Datalab team for the thought leadership/ Resources for this article. Datalab team has supported this content/article for promotion.</sub></em></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/datalab-releases-lift-a-9b-open-weights-vision-model-that-extracts-structured-json-from-pdfs-using-schemas/">Datalab Releases lift: A 9B Open-Weights Vision Model That Extracts Structured JSON From PDFs Using Schemas</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80667</post-id>
	</item>
		<item>
		<title>How to Use NVIDIA Canary-1B-v2 for ASR, Translation, and Automatic SRT Subtitle Export in Python</title>
		<link>https://www.marktechpost.com/2026/06/23/how-to-use-nvidia-canary-1b-v2-for-asr-translation-and-automatic-srt-subtitle-export-in-python/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Tue, 23 Jun 2026 18:31:36 +0000</pubDate>
				<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Audio Language Model]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Tutorials]]></category>
		<category><![CDATA[Voice AI]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80664</guid>

					<description><![CDATA[<p>In this tutorial, we build a multilingual ASR and speech translation pipeline with NVIDIA Canary-1B-v2. We load the model on a GPU-enabled runtime, prepare audio into 16 kHz mono, and run English ASR. We then translate speech into French, German, Spanish, and Italian, and extract word and segment timestamps. We export translated subtitles as an SRT file, test long-form transcription, run batch processing, and benchmark inference speed.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/how-to-use-nvidia-canary-1b-v2-for-asr-translation-and-automatic-srt-subtitle-export-in-python/">How to Use NVIDIA Canary-1B-v2 for ASR, Translation, and Automatic SRT Subtitle Export in Python</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this <a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Voice%20AI/nvidia_canary_1b_v2_asr_translation_tutorial_marktechpost.py" target="_blank" rel="noreferrer noopener">tutorial</a>, we build a speech recognition and translation workflow using<a href="https://huggingface.co/nvidia/canary-1b-v2"> <strong>NVIDIA Canary-1B-v2</strong></a>. We begin by setting up the required audio, NeMo, NumPy, and SciPy dependencies, then load the Canary model on a GPU-enabled runtime for efficient inference. From there, we prepare audio into a clean 16 kHz mono format, perform English ASR, translate speech into multiple languages, generate word and segment timestamps, export translated subtitles as an SRT file, test long-form transcription, run batch processing, and benchmark inference speed. At the end, we have a complete multilingual ASR and speech translation pipeline that we can adapt for real audio files, subtitle generation, and large-scale transcription experiments.</p>



<h3 id="h-installing-nemo-audio-libraries-numpy-and-scipy-dependencies" class="wp-block-heading"><strong>Installing NeMo, Audio Libraries, NumPy, and SciPy Dependencies</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import os, subprocess, sys
SENTINEL = "/content/.canary_setup_done"
if not os.path.exists(SENTINEL):
   def sh(c):
       print("$", c); subprocess.run(c, shell=True, check=False)
   print(">>> PHASE 1: installing dependencies (one-time)...\n")
   sh("apt-get -qq update")
   sh("apt-get -qq install -y libsndfile1 ffmpeg > /dev/null")
   sh('pip install -q "nemo_toolkit[asr]"')
   sh("pip install -q librosa soundfile pydub")
   sh('pip install -q --force-reinstall --no-cache-dir "numpy>=2.2,&lt;2.4" "scipy>=1.15"')
   open(SENTINEL, "w").write("done")
   print("\n<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2705.png" alt="✅" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Setup complete. Restarting the runtime now.")
   print("   When it reconnects, RUN THIS CELL AGAIN to start the tutorial.")
   os.kill(os.getpid(), 9)
</code></pre></div></div>



<p class="wp-block-paragraph">We set up the environment for the NVIDIA Canary-1B-v2 tutorial. We install the required system packages, NeMo ASR toolkit, audio libraries, and compatible NumPy and SciPy versions. We then create a setup marker and restart the runtime so that the updated dependencies load cleanly before running the main tutorial.</p>



<h3 class="wp-block-heading"><strong>Loading NVIDIA Canary-1B-v2 and Checking GPU Availability</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import time, json, gc, math, urllib.request
import torch, numpy as np, soundfile as sf, librosa
print(">>> PHASE 2: running tutorial\n")
print("NumPy:", np.__version__, "| PyTorch:", torch.__version__)
print("CUDA available:", torch.cuda.is_available())
if torch.cuda.is_available():
   print("GPU:", torch.cuda.get_device_name(0),
         f"| VRAM: {torch.cuda.get_device_properties(0).total_memory/1e9:.1f} GB")
else:
   print("<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/26a0.png" alt="⚠" class="wp-smiley" style="height: 1em; max-height: 1em;" />  No GPU — will run on CPU (very slow). "
         "Set Runtime > Change runtime type > GPU.")
DEVICE = "cuda" if torch.cuda.is_available() else "cpu"
LANGS = {
   "bg":"Bulgarian","hr":"Croatian","cs":"Czech","da":"Danish","nl":"Dutch",
   "en":"English","et":"Estonian","fi":"Finnish","fr":"French","de":"German",
   "el":"Greek","hu":"Hungarian","it":"Italian","lv":"Latvian","lt":"Lithuanian",
   "mt":"Maltese","pl":"Polish","pt":"Portuguese","ro":"Romanian","sk":"Slovak",
   "sl":"Slovenian","es":"Spanish","sv":"Swedish","ru":"Russian","uk":"Ukrainian",
}
print(f"\nSupported languages ({len(LANGS)}):", ", ".join(LANGS.keys()))
from nemo.collections.asr.models import ASRModel
print("\nLoading nvidia/canary-1b-v2 ...")
t0 = time.time()
asr_model = ASRModel.from_pretrained(model_name="nvidia/canary-1b-v2").to(DEVICE).eval()
print(f"Model loaded in {time.time()-t0:.1f}s")
</code></pre></div></div>



<p class="wp-block-paragraph">We import the main libraries and check whether CUDA is available for GPU acceleration. We define the supported language dictionary to enable Canary to handle multilingual ASR and translation tasks. We then load the NVIDIA Canary-1B-v2 model from NeMo and move it to the available device for inference.</p>



<h3 class="wp-block-heading"><strong>Preparing 16 kHz Audio and Running English ASR with Translation</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">TARGET_SR = 16000
def prepare_audio(path_or_url, out_path=None):
   if str(path_or_url).startswith(("http://", "https://")):
       local = "/content/_dl_" + os.path.basename(path_or_url.split("?")[0])
       urllib.request.urlretrieve(path_or_url, local)
       path_or_url = local
   audio, _ = librosa.load(path_or_url, sr=TARGET_SR, mono=True)
   if out_path is None:
       base = os.path.splitext(os.path.basename(path_or_url))[0]
       out_path = f"/content/{base}_16k_mono.wav"
   sf.write(out_path, audio, TARGET_SR, subtype="PCM_16")
   dur = len(audio) / TARGET_SR
   print(f"Prepared: {out_path}  ({dur:.1f}s, 16kHz, mono)")
   return out_path, dur
SAMPLE_URL = "https://dldata-public.s3.us-east-2.amazonaws.com/2086-149220-0033.wav"
sample_wav, sample_dur = prepare_audio(SAMPLE_URL)
def transcribe(files, source_lang="en", target_lang="en", timestamps=False, batch_size=1):
   if isinstance(files, str):
       files = [files]
   return asr_model.transcribe(files, source_lang=source_lang, target_lang=target_lang,
                               timestamps=timestamps, batch_size=batch_size)
print("\n=== 1) BASIC ASR (English) ===")
res = transcribe(sample_wav, source_lang="en", target_lang="en")
print("Transcript:", res[0].text)
print("\n=== 2) TRANSLATION (EN audio -> X) ===")
for tgt in ["fr", "de", "es", "it"]:
   out = transcribe(sample_wav, source_lang="en", target_lang=tgt)
   print(f"  EN -> {LANGS[tgt]:&lt;10} ({tgt}): {out[0].text}")
</code></pre></div></div>



<p class="wp-block-paragraph">We create a reusable audio preparation function that downloads audio when needed and converts it into 16 kHz mono WAV format. We load the sample audio file and define a helper function for transcription and translation. We then run basic English ASR and translate the same English speech into French, German, Spanish, and Italian.</p>



<h3 class="wp-block-heading"><strong>Generating Word and Segment Timestamps and Exporting SRT Subtitles</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n=== 3) TIMESTAMPS (ASR) ===")
ts_out = transcribe(sample_wav, source_lang="en", target_lang="en", timestamps=True)
word_ts = ts_out[0].timestamp.get("word", [])
seg_ts  = ts_out[0].timestamp.get("segment", [])
print("Segments:")
for s in seg_ts:
   print(f"  [{s['start']:6.2f}s - {s['end']:6.2f}s]  {s['segment']}")
print("First 10 words:")
for w in word_ts[:10]:
   print(f"  [{w['start']:6.2f}s - {w['end']:6.2f}s]  {w['word']}")
def _srt_time(t):
   h=int(t//3600); m=int((t%3600)//60); s=int(t%60); ms=int(round((t-int(t))*1000))
   return f"{h:02d}:{m:02d}:{s:02d},{ms:03d}"
def segments_to_srt(segments, out_path="/content/output.srt"):
   lines=[]
   for i, seg in enumerate(segments, 1):
       lines += [str(i), f"{_srt_time(seg['start'])} --> {_srt_time(seg['end'])}",
                 seg["segment"].strip(), ""]
   open(out_path, "w", encoding="utf-8").write("\n".join(lines))
   print(f"Saved SRT: {out_path}")
   return out_path
print("\n=== 4) SRT EXPORT (translated French subtitles) ===")
fr_ts = transcribe(sample_wav, source_lang="en", target_lang="fr", timestamps=True)
segments_to_srt(fr_ts[0].timestamp["segment"], "/content/subtitles_fr.srt")
print(open("/content/subtitles_fr.srt").read())
</code></pre></div></div>



<p class="wp-block-paragraph">We enable timestamped transcription to extract both segment-level and word-level timing information. We print the transcript segments and the first few word timestamps to inspect how the model aligns text with audio. We also convert translated French segments into an SRT subtitle file and display the generated subtitles.</p>



<h3 class="wp-block-heading"><strong>Running Long-Form Transcription, Batch Processing, and Speed Benchmark</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n=== 5) LONG-FORM (sample tiled x6) ===")
long_audio, _ = librosa.load(sample_wav, sr=TARGET_SR, mono=True)
long_audio = np.tile(long_audio, 6)
sf.write("/content/long.wav", long_audio, TARGET_SR, subtype="PCM_16")
print(f"Long clip duration: {len(long_audio)/TARGET_SR:.1f}s")
long_out = transcribe("/content/long.wav", source_lang="en", target_lang="en", batch_size=1)
print("Long transcript (first 300 chars):", long_out[0].text[:300], "...")
print("\n=== 6) BATCH ===")
for name in ["clip_a", "clip_b"]:
   sf.write(f"/content/{name}.wav",
            librosa.load(sample_wav, sr=TARGET_SR, mono=True)[0], TARGET_SR, subtype="PCM_16")
batch = transcribe(["/content/clip_a.wav", "/content/clip_b.wav"],
                  source_lang="en", target_lang="en", batch_size=2)
for i, b in enumerate(batch):
   print(f"  file {i}: {b.text}")
print("\n=== 7) BENCHMARK ===")
t0 = time.time(); _ = transcribe(sample_wav, source_lang="en", target_lang="en")
elapsed = time.time()-t0
print(f"Audio: {sample_dur:.2f}s | Compute: {elapsed:.2f}s | RTFx ≈ {sample_dur/elapsed:.1f}x")
print("\n<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2705.png" alt="✅" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Done. Change source_lang/target_lang from the LANGS dict to try other languages.")
</code></pre></div></div>



<p class="wp-block-paragraph">We test long-form transcription by repeating the sample audio several times and passing the longer clip through the model. We also create two duplicate audio clips to demonstrate batch transcription with a batch size of two. Also, we benchmark the model by comparing audio duration with compute time and report the real-time factor speed.</p>



<h3 class="wp-block-heading"><strong>Conclusion</strong></h3>



<p class="wp-block-paragraph">In conclusion, we completed a practical end-to-end workflow for using NVIDIA Canary-1B-v2 as a multilingual ASR and speech translation system. We processed raw audio, generated accurate transcripts, translated speech into different target languages, extracted timestamps, created subtitle files, handled longer audio clips, and compared runtime performance through a simple benchmark. We now have a reusable Colab-ready pipeline that we can extend further with custom uploads, more languages, larger batches, and production-style audio processing.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Voice%20AI/nvidia_canary_1b_v2_asr_translation_tutorial_marktechpost.py" target="_blank" rel="noreferrer noopener">Full Codes with Notebook</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/how-to-use-nvidia-canary-1b-v2-for-asr-translation-and-automatic-srt-subtitle-export-in-python/">How to Use NVIDIA Canary-1B-v2 for ASR, Translation, and Automatic SRT Subtitle Export in Python</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		<enclosure url="https://dldata-public.s3.us-east-2.amazonaws.com/2086-149220-0033.wav" length="237964" type="audio/wav" />

		<post-id xmlns="com-wordpress:feed-additions:1">80664</post-id>
	</item>
		<item>
		<title>Prime Intellect Releases prime-rl 0.6.0 to Train Trillion-Parameter MoE Models on Agentic RL Workloads</title>
		<link>https://www.marktechpost.com/2026/06/23/prime-intellect-releases-prime-rl-0-6-0-to-train-trillion-parameter-moe-models-on-agentic-rl-workloads/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Tue, 23 Jun 2026 07:20:44 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80661</guid>

					<description><![CDATA[<p>Prime Intellect has released prime-rl 0.6.0, an open framework for asynchronous reinforcement learning on trillion-parameter Mixture-of-Experts models. It trained GLM-5 on SWE tasks at up to 131k sequence length, with sub-5-minute step times and 256 rollouts, on 28 H200 nodes. This breakdown covers the inference and training optimizations behind those numbers — FP8 inference, Wide Expert Parallelism, prefill/decode disaggregation, router replay, and 3-D parallelism (FSDP, EP, CP).</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/prime-intellect-releases-prime-rl-0-6-0-to-train-trillion-parameter-moe-models-on-agentic-rl-workloads/">Prime Intellect Releases prime-rl 0.6.0 to Train Trillion-Parameter MoE Models on Agentic RL Workloads</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Prime Intellect has released <a href="https://www.primeintellect.ai/blog/rl-at-1t-scale" target="_blank" rel="noreferrer noopener">prime-rl version 0.6.0</a>. The framework targets reinforcement learning on trillion-parameter Mixture-of-Experts (MoE) models. It focuses on heavy agentic workloads, like long-horizon software-engineering tasks.</p>



<p class="wp-block-paragraph">The research team trained GLM-5 on SWE tasks at up to 131k sequence length. Step times stayed under five minutes. The batch size was 256 rollouts. The run used only 28 H200 nodes.</p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li>prime-rl 0.6.0 trains trillion-parameter MoE models on agentic RL workloads.</li>



<li>GLM-5 trained on SWE at 131k sequence length, sub-5-minute steps, 28 H200 nodes.</li>



<li>Asynchronous RL disaggregates trainer and inference for independent optimization.</li>



<li>Inference uses FP8, Wide EP, P/D disaggregation, KV offloading, and router replay.</li>



<li>Training uses 3-D parallelism (FSDP, EP, CP) plus block-scaled FP8.</li>
</ul>



<h2 id="h-what-is-prime-rl-0-6-0" class="wp-block-heading"><strong>What is prime-rl 0.6.0?</strong></h2>



<p class="wp-block-paragraph">prime-rl is an open framework for asynchronous reinforcement learning. It post-trains large open-source models on agentic tasks. Version 0.6.0 extends this to trillion-parameter MoE scale.</p>



<p class="wp-block-paragraph">The example model in the announcement is <code>zai-org/GLM-5.1</code>. The optimizations also apply to other large MoE models. Examples include <code>moonshotai/Kimi-K2.7-Code</code> and <code>nvidia/NVIDIA-Nemotron-3-Ultra-550B-A55B-BF16</code>.</p>



<p class="wp-block-paragraph">A full GLM-5.1 run starts with one command on a Slurm cluster.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">uv run rl @ examples/glm5_llmd/rl.toml --output-dir /shared/outputs/glm5-llmd</code></pre></div></div>



<h2 id="h-role-of-asynchronous-rl" class="wp-block-heading"><strong>Role of asynchronous RL</strong></h2>



<p class="wp-block-paragraph">Agentic tasks have long-tail outliers. Some coding rollouts run for hours. Waiting for them before each policy update would idle GPUs.</p>



<p class="wp-block-paragraph">Asynchronous RL avoids this. The trainer and inference systems are disaggregated. They run and scale independently. The inference policy updates as soon as the optimizer step finishes.</p>



<p class="wp-block-paragraph">There is one synchronization point: the policy update. prime-rl pushes new weights as soon as they exist. Already-dispatched rollouts keep their active prefix cache. So a single rollout may mix tokens from several policy versions.</p>



<p class="wp-block-paragraph">New rollouts behave differently. They repopulate their own KV cache, even when prefixes match. A KV-cache salt forces this. Requests from too old a policy are dropped. The <code>max_off_policy_steps</code> value controls that threshold.</p>



<h2 id="h-inference-optimizations" class="wp-block-heading"><strong>Inference optimizations</strong></h2>



<p class="wp-block-paragraph">Inference is usually the throughput bottleneck in an RL system. prime-rl optimizes for throughput, while keeping latency bounded.</p>



<p class="wp-block-paragraph"><strong>FP8 inference</strong>: Lower precision speeds up prefill and decode. prime-rl uses FP8 with DeepEP and DeepGEMM kernels.</p>



<p class="wp-block-paragraph"><strong>Wide Expert Parallelism</strong>: Wide EP spreads experts across ≥32 GPUs. It pairs with a large data-parallel rank, for example 32. Each GPU holds separate experts and serves as an endpoint. Synchronization happens per-layer, through dispatch and combine operations.</p>



<p class="wp-block-paragraph"><strong>Prefill and Decode Disaggregation</strong>: Some model<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2194.png" alt="↔" class="wp-smiley" style="height: 1em; max-height: 1em;" />env pairs hit a 4:1 prefill:decode token ratio. Shared workers would inflate end-to-end latency. That reduces the benefits of PipelineRL. P/D disaggregation separates prefill and decode workers. Long tool outputs then stop throttling decode workers.</p>



<p class="wp-block-paragraph"><strong>KV cache management</strong>: High concurrency needs large KV cache space. prime-rl supports tiered offloading to CPU and disk. vLLM native offloading creates one pool per worker. Mooncake Store instead pools RAM and disk across all nodes centrally.</p>



<p class="wp-block-paragraph"><strong>Request routing</strong>: prime-rl ships a fork of vllm-router by default. It also supports the NVIDIA Dynamo router as a drop-in. Routers score workers using KV cache reuse, queue depth, and live load.</p>



<p class="wp-block-paragraph"><strong>Router replay (R3)</strong>: Trainer<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2194.png" alt="↔" class="wp-smiley" style="height: 1em; max-height: 1em;" />inference mismatch silently kills training. Router replay captures inference routing decisions. It replays them directly on the trainer. This cuts KL mismatch by roughly an order of magnitude. Routed experts have shape <code>[num_layers, top_k, seq_len]</code>. This payload can grow to hundreds of GB. At scale, the data rate reaches tens of Gbps. So prime-rl treats it as an opaque payload. Optimized PyTorch operations handle the processing.</p>



<h2 id="h-training-optimizations" class="wp-block-heading"><strong>Training optimizations</strong></h2>



<p class="wp-block-paragraph">The trainer builds on torchtitan, a PyTorch-native training codebase. It relies on 3-D parallelism: FSDP, CP, and EP. The GLM-5 case study uses all three.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Strategy</th><th>What it shards</th><th>Primary use</th><th>Key detail</th></tr></thead><tbody><tr><td><strong>FSDP (FSDP2)</strong></td><td>Parameters, gradients, optimizer states</td><td>Baseline memory amortization</td><td>Gathers weights on demand per layer via <code>fully_shard</code></td></tr><tr><td><strong>Expert Parallelism (EP)</strong></td><td>Experts within a layer</td><td>Shrinks active layer memory</td><td><code>all2all</code> dispatch/combine; torch-native or DeepEP</td></tr><tr><td><strong>Context Parallelism (CP)</strong></td><td>The sequence dimension</td><td>Long-context activation memory</td><td>Ulysses (default) or Ring Attention</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">EP exists because layers stay huge after FSDP. With 78 layers and 800B params in float32, one layer&#8217;s all-gather needs roughly 40GB. Overlapping one layer pushes that near 80GB. Setting EP=8 dispatches tokens instead of gathering full experts. torch-native all2all is slightly faster within one node. DeepEP wins when EP spans multiple nodes.</p>



<p class="wp-block-paragraph">CP matters at 131k+ sequence length. There, activations dominate memory, not parameters. GLM-5 uses DSA, which neither Ulysses nor Ring Attention parallelizes directly. So prime-rl ships a custom context-parallel implementation for it.</p>



<p class="wp-block-paragraph"><strong>FP8 training.</strong> prime-rl uses DeepGEMM block-scaled FP8, as proposed by DeepSeek V3. This rarely raises throughput, due to quantization overhead. Its real value is matching trainer and inference precision. That reduces KL mismatch and stabilizes training.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- prime-rl RL System Explorer | Marktechpost interactive embed | paste into a Custom HTML block -->
<div style="max-width:880px;margin:0 auto;">
<iframe id="primerl-explorer"
  title="prime-rl RL System Explorer"
  loading="lazy"
  style="width:100%;height:600px;border:0;overflow:hidden;display:block;"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;prime-rl RL System Explorer&lt;/title&gt;
&lt;style&gt;
  * { box-sizing: border-box; margin: 0; padding: 0; }
  :root{
    --ink:#0a0a0a; --paper:#ffffff; --mute:#6b6b6b; --line:#e2e2e2;
    --fill:#0a0a0a; --soft:#f4f4f4; --good:#0a0a0a;
  }
  html,body{ background:var(--paper); color:var(--ink);
    font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;
    -webkit-font-smoothing:antialiased; }
  .wrap{ max-width:860px; margin:0 auto; padding:26px 20px 18px; }
  .kicker{ font-size:11px; letter-spacing:2.4px; text-transform:uppercase; color:var(--mute); }
  h1{ font-size:23px; line-height:1.2; margin:6px 0 4px; font-weight:800; letter-spacing:-0.3px; }
  .sub{ font-size:13.5px; color:var(--mute); line-height:1.5; max-width:640px; }
  .grid{ display:grid; grid-template-columns:1.05fr 0.95fr; gap:22px; margin-top:22px; }
  .panel{ border:1px solid var(--line); border-radius:14px; padding:16px 16px 18px; background:var(--paper); }
  .panel h2{ font-size:12px; letter-spacing:1.6px; text-transform:uppercase; color:var(--mute); margin-bottom:14px; font-weight:700; }
  .opt{ display:flex; align-items:flex-start; gap:12px; padding:9px 0; border-top:1px solid var(--line); cursor:pointer; }
  .opt:first-of-type{ border-top:none; }
  .sw{ flex:0 0 38px; height:22px; border-radius:22px; background:#d9d9d9; position:relative; transition:background .18s ease; margin-top:1px; }
  .sw::after{ content:&quot;&quot;; position:absolute; top:2px; left:2px; width:18px; height:18px; border-radius:50%; background:var(--paper); transition:transform .18s ease; box-shadow:0 1px 2px rgba(0,0,0,.25); }
  .opt.on .sw{ background:var(--ink); }
  .opt.on .sw::after{ transform:translateX(16px); }
  .opt .meta{ flex:1; }
  .opt .name{ font-size:13.5px; font-weight:650; letter-spacing:-0.2px; }
  .opt .desc{ font-size:11.5px; color:var(--mute); line-height:1.45; margin-top:2px; }
  .metric{ margin-bottom:15px; }
  .metric:last-child{ margin-bottom:0; }
  .mtop{ display:flex; justify-content:space-between; align-items:baseline; margin-bottom:6px; }
  .mname{ font-size:12.5px; font-weight:650; }
  .mval{ font-size:13px; font-weight:800; font-variant-numeric:tabular-nums; }
  .mval small{ font-weight:600; color:var(--mute); font-size:11px; }
  .bar{ height:12px; border-radius:7px; background:var(--soft); overflow:hidden; border:1px solid var(--line); }
  .bar &gt; i{ display:block; height:100%; background:var(--fill);
    background-image:repeating-linear-gradient(45deg,rgba(255,255,255,.16) 0 6px,transparent 6px 12px);
    width:0%; transition:width .45s cubic-bezier(.2,.7,.2,1); }
  .hint{ font-size:11px; color:var(--mute); margin-top:5px; line-height:1.4; }
  .verdict{ margin-top:16px; border:1px solid var(--line); border-radius:12px; padding:12px 14px; background:var(--soft); }
  .verdict .vt{ font-size:11px; letter-spacing:1.4px; text-transform:uppercase; color:var(--mute); font-weight:700; }
  .verdict .vb{ font-size:13px; line-height:1.5; margin-top:4px; }
  .row{ display:flex; gap:8px; margin-top:14px; flex-wrap:wrap; }
  .btn{ font:inherit; font-size:11.5px; font-weight:650; letter-spacing:.2px; cursor:pointer;
    border:1px solid var(--ink); background:var(--paper); color:var(--ink); border-radius:9px; padding:7px 13px; }
  .btn.dark{ background:var(--ink); color:var(--paper); }
  .btn:active{ transform:translateY(1px); }
  .note{ font-size:10.5px; color:var(--mute); margin-top:16px; line-height:1.5; border-top:1px dashed var(--line); padding-top:10px; }
  .foot{ display:flex; justify-content:space-between; align-items:center; margin-top:14px; padding-top:12px; border-top:1px solid var(--line); flex-wrap:wrap; gap:6px; }
  .foot a{ color:var(--ink); font-weight:750; text-decoration:none; }
  .foot span{ font-size:10.5px; color:var(--mute); }
  @media (max-width:680px){ .grid{ grid-template-columns:1fr; gap:16px; } h1{ font-size:20px; } }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot; id=&quot;app&quot;&gt;
  &lt;div class=&quot;kicker&quot;&gt;Interactive · prime-rl 0.6.0&lt;/div&gt;
  &lt;h1&gt;RL System Explorer&lt;/h1&gt;
  &lt;p class=&quot;sub&quot;&gt;Toggle the optimizations from Prime Intellect&amp;#8217;s prime-rl deep dive. Watch how they shift inference throughput, training step time, and trainer&amp;#8596;inference KL mismatch. Values are illustrative, for intuition only.&lt;/p&gt;

  &lt;div class=&quot;grid&quot;&gt;
    &lt;div class=&quot;panel&quot;&gt;
      &lt;h2&gt;Optimizations&lt;/h2&gt;
      &lt;div class=&quot;opt on&quot; data-k=&quot;fp8inf&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;FP8 inference&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Low-precision prefill/decode with DeepEP + DeepGEMM kernels. Lifts throughput.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt on&quot; data-k=&quot;wideep&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;Wide Expert Parallelism&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Experts spread across &amp;#8805;32 GPUs with a large DP rank. Maximizes throughput.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt on&quot; data-k=&quot;pd&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;P/D disaggregation&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Separate prefill and decode workers. Keeps decode latency predictable.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt&quot; data-k=&quot;mooncake&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;KV offloading (Mooncake)&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Centralized RAM/disk pool across nodes. Raises concurrency.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt on&quot; data-k=&quot;async&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;Async weight updates&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Push new weights as soon as the optimizer step finishes. Cuts idle GPU time.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt&quot; data-k=&quot;r3&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;Router replay (R3)&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Replay inference routing on the trainer. Cuts KL mismatch ~10&amp;#215;.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt&quot; data-k=&quot;fp8train&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;FP8 training&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Block-scaled FP8 (DeepSeek V3). Matches trainer/inference precision.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;
        &lt;button class=&quot;btn dark&quot; id=&quot;all&quot;&gt;Enable all&lt;/button&gt;
        &lt;button class=&quot;btn&quot; id=&quot;none&quot;&gt;Naive baseline&lt;/button&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;panel&quot;&gt;
      &lt;h2&gt;Estimated System Behavior&lt;/h2&gt;

      &lt;div class=&quot;metric&quot;&gt;
        &lt;div class=&quot;mtop&quot;&gt;&lt;div class=&quot;mname&quot;&gt;Inference throughput&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;tpV&quot;&gt;1.0&lt;small&gt;&amp;#215;&lt;/small&gt;&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;bar&quot;&gt;&lt;i id=&quot;tpB&quot;&gt;&lt;/i&gt;&lt;/div&gt;
        &lt;div class=&quot;hint&quot;&gt;Higher is better. Throughput is the usual RL bottleneck.&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;metric&quot;&gt;
        &lt;div class=&quot;mtop&quot;&gt;&lt;div class=&quot;mname&quot;&gt;Training step time&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;stV&quot;&gt;12.0&lt;small&gt;min&lt;/small&gt;&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;bar&quot;&gt;&lt;i id=&quot;stB&quot;&gt;&lt;/i&gt;&lt;/div&gt;
        &lt;div class=&quot;hint&quot;&gt;Lower is better. The GLM-5 target was sub-5-minute steps.&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;metric&quot;&gt;
        &lt;div class=&quot;mtop&quot;&gt;&lt;div class=&quot;mname&quot;&gt;Trainer&amp;#8596;inference KL mismatch&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;klV&quot;&gt;High&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;bar&quot;&gt;&lt;i id=&quot;klB&quot;&gt;&lt;/i&gt;&lt;/div&gt;
        &lt;div class=&quot;hint&quot;&gt;Lower is better. Drift silently destabilizes RL training.&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;verdict&quot;&gt;
        &lt;div class=&quot;vt&quot;&gt;Read-out&lt;/div&gt;
        &lt;div class=&quot;vb&quot; id=&quot;vsay&quot;&gt;Toggle options to see the trade-offs.&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;p class=&quot;note&quot;&gt;&lt;strong&gt;Illustrative model.&lt;/strong&gt; Bars use simple weights to convey direction and trade-offs, not benchmark figures. Source: Prime Intellect, &amp;#8220;RL at 1T Scale: prime-rl Performance Deep Dive&amp;#8221; (Jun 21, 2026).&lt;/p&gt;

  &lt;div class=&quot;foot&quot;&gt;
    &lt;a href=&quot;https://www.marktechpost.com&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Marktechpost&lt;/a&gt;
    &lt;span&gt;Interactive explainer · black &amp;amp; white&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
  var state = { fp8inf:true, wideep:true, pd:true, mooncake:false, async:true, r3:false, fp8train:false };

  function compute(){
    // Throughput multiplier (baseline 1.0x)
    var tp = 1.0;
    if(state.fp8inf)  tp *= 1.55;
    if(state.wideep)  tp *= 1.45;
    if(state.pd)      tp *= 1.30;
    if(state.mooncake)tp *= 1.20;
    // Step time in minutes (baseline 12.0)
    var st = 12.0;
    if(state.async)   st -= 3.4;
    if(state.pd)      st -= 2.6;
    if(state.wideep)  st -= 1.8;
    if(state.fp8inf)  st -= 1.6;
    if(state.mooncake)st -= 0.9;
    if(st &lt; 3.6) st = 3.6;
    // KL mismatch score 0..100 (lower better), baseline 92
    var kl = 92;
    if(state.r3)       kl -= 46;
    if(state.fp8train) kl -= 28;
    if(state.async)    kl += 6; // async adds some off-policy drift
    if(kl &lt; 6) kl = 6;
    if(kl &gt; 100) kl = 100;
    return { tp:tp, st:st, kl:kl };
  }

  function klLabel(k){
    if(k &lt;= 22) return &quot;Low&quot;;
    if(k &lt;= 55) return &quot;Moderate&quot;;
    return &quot;High&quot;;
  }

  function verdict(c){
    var msgs = [];
    if(c.st &lt;= 5)  msgs.push(&quot;Step time is in the sub-5-minute target zone.&quot;);
    else if(c.st &gt;= 9) msgs.push(&quot;Step time is high; GPUs likely idle on long rollouts.&quot;);
    if(c.tp &gt;= 3.0) msgs.push(&quot;Inference throughput is well above the naive baseline.&quot;);
    else if(c.tp &lt;= 1.2) msgs.push(&quot;Throughput is near baseline; inference is the bottleneck.&quot;);
    if(c.kl &lt;= 22) msgs.push(&quot;Low KL mismatch means stable, on-policy training.&quot;);
    else if(c.kl &gt;= 70) msgs.push(&quot;High KL mismatch risks silent training instability.&quot;);
    if(!msgs.length) msgs.push(&quot;A balanced setup with room to push throughput or stability.&quot;);
    return msgs.slice(0,2).join(&quot; &quot;);
  }

  function render(){
    var c = compute();
    document.getElementById(&quot;tpV&quot;).innerHTML = c.tp.toFixed(2) + &quot;&lt;small&gt;&amp;#215;&lt;/small&gt;&quot;;
    document.getElementById(&quot;tpB&quot;).style.width = Math.min(100, (c.tp/4.0)*100).toFixed(1) + &quot;%&quot;;
    document.getElementById(&quot;stV&quot;).innerHTML = c.st.toFixed(1) + &quot;&lt;small&gt;min&lt;/small&gt;&quot;;
    // invert: shorter step =&gt; fuller bar
    document.getElementById(&quot;stB&quot;).style.width = Math.max(6, ((12.0 - c.st)/(12.0-3.6))*100).toFixed(1) + &quot;%&quot;;
    document.getElementById(&quot;klV&quot;).textContent = klLabel(c.kl);
    // invert: lower KL =&gt; fuller (better) bar
    document.getElementById(&quot;klB&quot;).style.width = Math.max(6, (100 - c.kl)).toFixed(1) + &quot;%&quot;;
    document.getElementById(&quot;vsay&quot;).textContent = verdict(c);
    sizeUp();
  }

  document.querySelectorAll(&quot;.opt&quot;).forEach(function(el){
    el.addEventListener(&quot;click&quot;, function(){
      var k = el.getAttribute(&quot;data-k&quot;);
      state[k] = !state[k];
      el.classList.toggle(&quot;on&quot;, state[k]);
      render();
    });
  });
  document.getElementById(&quot;all&quot;).addEventListener(&quot;click&quot;, function(){
    Object.keys(state).forEach(function(k){ state[k]=true; });
    document.querySelectorAll(&quot;.opt&quot;).forEach(function(el){ el.classList.add(&quot;on&quot;); });
    render();
  });
  document.getElementById(&quot;none&quot;).addEventListener(&quot;click&quot;, function(){
    Object.keys(state).forEach(function(k){ state[k]=false; });
    document.querySelectorAll(&quot;.opt&quot;).forEach(function(el){ el.classList.remove(&quot;on&quot;); });
    render();
  });

  // Auto-resize for WordPress iframe embed (offsetHeight + buffer)
  function sizeUp(){
    try{
      var h = document.getElementById(&quot;app&quot;).offsetHeight + 40;
      parent.postMessage({ type:&quot;primerl-explorer-height&quot;, height:h }, &quot;*&quot;);
    }catch(e){}
  }
  window.addEventListener(&quot;load&quot;, function(){ render(); setTimeout(sizeUp, 60); });
  window.addEventListener(&quot;resize&quot;, sizeUp);
  render();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
</div>
<script>
(function(){
  window.addEventListener("message", function(e){
    if(e && e.data && e.data.type === "primerl-explorer-height"){
      var f = document.getElementById("primerl-explorer");
      if(f && e.data.height){ f.style.height = e.data.height + "px"; }
    }
  });
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use cases with examples</strong></h2>



<ul class="wp-block-list">
<li><strong>Long-horizon SWE agents</strong>: Train a model on real repository issues. Rollouts can span 100s of turns and tool calls. P/D disaggregation keeps decode latency predictable here.</li>



<li><strong>1T-scale post-training on fewer nodes</strong>: The GLM-5 run fit on 28 H200 nodes. Wide EP and KV offloading raise concurrency and throughput.</li>



<li><strong>Stable agentic RL at scale</strong>: Router replay and FP8 training both reduce trainer<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2194.png" alt="↔" class="wp-smiley" style="height: 1em; max-height: 1em;" />inference KL mismatch. Lower mismatch means steadier training.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://www.primeintellect.ai/blog/rl-at-1t-scale" target="_blank" rel="noreferrer noopener">Technical details</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/prime-intellect-releases-prime-rl-0-6-0-to-train-trillion-parameter-moe-models-on-agentic-rl-workloads/">Prime Intellect Releases prime-rl 0.6.0 to Train Trillion-Parameter MoE Models on Agentic RL Workloads</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80661</post-id>
	</item>
		<item>
		<title>GLM-5.2 OpenAI-Compatible API: A Hands-On Guide to Reasoning Effort, Function Calling, and Long-Context Retrieval</title>
		<link>https://www.marktechpost.com/2026/06/22/glm-5-2-openai-compatible-api-a-hands-on-guide-to-reasoning-effort-function-calling-and-long-context-retrieval/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Tue, 23 Jun 2026 06:35:05 +0000</pubDate>
				<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80658</guid>

					<description><![CDATA[<p>We build a practical GLM-5.2 workflow using its hosted, OpenAI-compatible API instead of running the model locally. We set up multiple providers, load the API key securely, and create a reusable chat wrapper. We then test thinking-effort control, streamed reasoning, function calling, a tool-using agent, structured JSON output, and long-context retrieval. We close with token and cost accounting so every demo stays measurable.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/22/glm-5-2-openai-compatible-api-a-hands-on-guide-to-reasoning-effort-function-calling-and-long-context-retrieval/">GLM-5.2 OpenAI-Compatible API: A Hands-On Guide to Reasoning Effort, Function Calling, and Long-Context Retrieval</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this <a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/LLM%20Projects/glm_5_2_api_workflow_tutorial_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">tutorial</a>, we work with<a href="https://huggingface.co/zai-org/GLM-5.2"> <strong>GLM-5.2</strong></a> and use its hosted, OpenAI-compatible API instead of running the full model locally. We begin by setting up multiple provider options, securely loading the API key, and creating a reusable chat wrapper that supports normal chat, thinking mode, streaming, tool calling, and token tracking. Then we move beyond a simple chatbot example and test the model in more practical situations, including reasoning-effort control, streamed reasoning and answers, function calling, a small tool-using agent, structured JSON output, long-context retrieval, and cost estimation. </p>



<h3 id="h-setting-up-the-glm-5-2-openai-compatible-client-and-reusable-chat-wrapper" class="wp-block-heading"><strong>Setting Up the GLM-5.2 OpenAI-Compatible Client and Reusable Chat Wrapper</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import sys, subprocess
subprocess.run([sys.executable, "-m", "pip", "install", "-q", "-U", "openai"], check=False)
import os, re, json, time, getpass
from openai import OpenAI
PROVIDERS = {
   "zai":         {"base_url": "https://api.z.ai/api/paas/v4/",   "model": "glm-5.2",        "env": "ZAI_API_KEY"},
   "openrouter":  {"base_url": "https://openrouter.ai/api/v1",    "model": "z-ai/glm-5.2",   "env": "OPENROUTER_API_KEY"},
   "together":    {"base_url": "https://api.together.xyz/v1",     "model": "zai-org/GLM-5.2","env": "TOGETHER_API_KEY"},
   "requesty":    {"base_url": "https://router.requesty.ai/v1",   "model": "zai/glm-5.2",    "env": "REQUESTY_API_KEY"},
   "huggingface": {"base_url": "https://router.huggingface.co/v1","model": "zai-org/GLM-5.2","env": "HF_TOKEN"},
}
PROVIDER = "zai"
CFG   = PROVIDERS[PROVIDER]
MODEL = CFG["model"]
def load_api_key(env_name):
   try:
       from google.colab import userdata
       v = userdata.get(env_name)
       if v: return v
   except Exception:
       pass
   if os.environ.get(env_name):
       return os.environ[env_name]
   return getpass.getpass(f"Enter your {env_name}: ")
client = OpenAI(api_key=load_api_key(CFG["env"]), base_url=CFG["base_url"])
PRICE_IN_PER_M, PRICE_OUT_PER_M = 1.40, 4.40
_USAGE = {"in": 0, "out": 0, "calls": 0}
def _track(usage):
   if usage:
       _USAGE["in"]    += getattr(usage, "prompt_tokens", 0) or 0
       _USAGE["out"]   += getattr(usage, "completion_tokens", 0) or 0
       _USAGE["calls"] += 1
def get_reasoning(obj):
   """Pull GLM's hidden reasoning trace from a message/delta (a provider-extra field)."""
   val = getattr(obj, "reasoning_content", None)
   if val: return val
   extra = getattr(obj, "model_extra", None) or {}
   if extra.get("reasoning_content"): return extra["reasoning_content"]
   try:    return obj.to_dict().get("reasoning_content")
   except Exception: return None
def chat(messages, effort=None, thinking=True, tools=None, tool_choice="auto",
        stream=False, max_tokens=2048, temperature=1.0, tool_stream=False):
   """
   effort:   None | "high" | "max"   (GLM-5.2 thinking-effort level; max is the model default)
   thinking: True -> deep thinking on; False -> off (fast, cheap, low-latency)
   GLM-specific params go through extra_body so any OpenAI client works.
   """
   extra = {"thinking": {"type": "enabled" if thinking else "disabled"}}
   if effort and thinking: extra["reasoning_effort"] = effort
   if tool_stream:         extra["tool_stream"] = True
   kwargs = dict(model=MODEL, messages=messages, max_tokens=max_tokens,
                 temperature=temperature, stream=stream, extra_body=extra)
   if tools:
       kwargs.update(tools=tools, tool_choice=tool_choice)
   if stream:
       kwargs["stream_options"] = {"include_usage": True}
   return client.chat.completions.create(**kwargs)
</code></pre></div></div>



<p class="wp-block-paragraph">We set up the complete foundation for using GLM-5.2 through an OpenAI-compatible API. We define multiple provider options, load the API key securely, create the OpenAI client, and set up token-cost tracking for the entire notebook. We also build a reusable chat wrapper so that every subsequent demo can use thinking mode, reasoning effort, streaming, tool calling, and provider-specific parameters cleanly.</p>



<h3 class="wp-block-heading"><strong>Basic Chat, Thinking-Effort Control, and Streamed Reasoning with GLM-5.2</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def demo_basic():
   print("\n=== 1. BASIC CHAT / SANITY CHECK =========================")
   resp = chat([{"role": "system", "content": "You are a concise technical assistant."},
                {"role": "user",   "content": "In one sentence, what is GLM-5.2 best at?"}],
               thinking=False, max_tokens=200)
   _track(resp.usage)
   print(resp.choices[0].message.content.strip())
def demo_effort():
   print("\n=== 2. THINKING-EFFORT CONTROL (off / high / max) ========")
   problem = ("Train A leaves city A at 9:00 going 60 km/h toward city B. "
              "Train B leaves B (420 km away) at 9:30 going 90 km/h toward A. "
              "At what clock time do they meet? Show the key steps briefly.")
   for label, kw in [("thinking OFF", dict(thinking=False)),
                     ("effort=high",  dict(thinking=True, effort="high")),
                     ("effort=max",   dict(thinking=True, effort="max"))]:
       t0 = time.time()
       resp = chat([{"role": "user", "content": problem}], max_tokens=2000, **kw)
       dt = time.time() - t0
       _track(resp.usage)
       msg, u = resp.choices[0].message, resp.usage
       print(f"\n--- {label} | {dt:0.1f}s | out_tokens={getattr(u,'completion_tokens',0)} ---")
       r = get_reasoning(msg)
       if r:
           print("  [reasoning, first 220 chars]: " + " ".join(r.split())[:220] + " ...")
       print("  <noscript><div><i>Please view this post in your web browser to complete the quiz.</i></div></noscript>: " + " ".join((msg.content or '').split())[:350])
def demo_streaming():
   print("\n=== 3. STREAMING: reasoning channel vs answer channel ====")
   stream = chat([{"role": "user", "content":
                   "Explain why the sky is blue, then give a one-line TL;DR."}],
                 thinking=True, effort="high", stream=True, max_tokens=1200)
   saw_r = saw_a = False
   usage = None
   for chunk in stream:
       if getattr(chunk, "usage", None): usage = chunk.usage
       if not chunk.choices: continue
       delta = chunk.choices[0].delta
       r = get_reasoning(delta)
       if r:
           if not saw_r: print("\n[thinking] ", end="", flush=True); saw_r = True
           print(r, end="", flush=True)
       if getattr(delta, "content", None):
           if not saw_a: print("\n\n ", end="", flush=True); saw_a = True
           print(delta.content, end="", flush=True)
   print()
   _track(usage)
</code></pre></div></div>



<p class="wp-block-paragraph">We start testing GLM-5.2 with basic chat, reasoning-effort control, and streaming output. We first run a simple sanity check, then compare the same problem across thinking-off, high-effort, and max-effort modes to observe changes in latency and output tokens. We also stream the model response so we can view the reasoning channel and the final answer separately as the response is being generated.</p>



<h3 class="wp-block-heading"><strong>Function Calling and a Multi-Step Tool-Using GLM-5.2 Agent</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def tool_calculator(expression: str):
   if not re.fullmatch(r"[0-9+\-*/(). %]+", expression or ""):
       return {"error": "unsupported characters"}
   try:    return {"result": eval(expression, {"__builtins__": {}}, {})}
   except Exception as e: return {"error": str(e)}
_CITY_POP = {"tokyo": 37_400_068, "delhi": 32_900_000, "shanghai": 28_500_000,
            "sao paulo": 22_400_000, "mexico city": 21_800_000}
def tool_city_population(city: str):
   return {"city": city, "population": _CITY_POP.get((city or "").strip().lower())}
TOOLS = [
   {"type": "function", "function": {
       "name": "calculator", "description": "Evaluate basic arithmetic like '37400068/21800000'.",
       "parameters": {"type": "object", "properties": {"expression": {"type": "string"}},
                      "required": ["expression"]}}},
   {"type": "function", "function": {
       "name": "city_population", "description": "Look up the metro population of a city.",
       "parameters": {"type": "object", "properties": {"city": {"type": "string"}},
                      "required": ["city"]}}},
]
TOOL_IMPLS = {"calculator": tool_calculator, "city_population": tool_city_population}
def run_tool_loop(messages, max_rounds=6, effort="max"):
   """Full loop: model -> tool_calls -> execute -> feed results back -> repeat."""
   for _ in range(max_rounds):
       resp = chat(messages, tools=TOOLS, thinking=True, effort=effort,
                   max_tokens=1500, temperature=0.3)
       _track(resp.usage)
       m = resp.choices[0].message
       if not getattr(m, "tool_calls", None):
           return m.content
       messages.append({
           "role": "assistant", "content": m.content or "",
           "tool_calls": [{"id": tc.id, "type": "function",
                           "function": {"name": tc.function.name,
                                        "arguments": tc.function.arguments}}
                          for tc in m.tool_calls]})
       for tc in m.tool_calls:
           try:    args = json.loads(tc.function.arguments or "{}")
           except json.JSONDecodeError: args = {}
           result = TOOL_IMPLS.get(tc.function.name, lambda **k: {"error": "unknown"})(**args)
           print(f"   ↳ {tc.function.name}({args}) -> {result}")
           messages.append({"role": "tool", "tool_call_id": tc.id,
                            "content": json.dumps(result)})
   return "(stopped: max tool rounds reached)"
def demo_tools():
   print("\n=== 4. FUNCTION / TOOL CALLING ===========================")
   q = ("How many times larger is Tokyo's metro population than Mexico City's? "
        "Use the tools, then answer with the ratio to one decimal place.")
   print("Final:", " ".join((run_tool_loop([{"role": "user", "content": q}]) or "").split()))
def demo_agent():
   print("\n=== 5. MINI MULTI-STEP AGENT (tools + max effort) ========")
   task = ("Rank Tokyo, Delhi, and Shanghai by metro population (largest first), "
           "then compute the combined population of the top two and report it. "
           "Use the tools for every lookup and sum; never guess numbers.")
   ans = run_tool_loop([{"role": "system", "content": "You are a careful analyst."},
                        {"role": "user",   "content": task}])
   print("Final:", " ".join((ans or "").split()))
</code></pre></div></div>



<p class="wp-block-paragraph">We connect GLM-5.2 to external tools and build a small tool-using workflow. We define a calculator and a city-population lookup tool, register them in an OpenAI-style tool schema, and create a loop in which the model requests tool calls and receives tool results. We then use this setup for a direct function-calling task and a small multi-step agent that looks up populations, ranks cities, and performs calculations without guessing.</p>



<h3 class="wp-block-heading"><strong>Structured JSON Output and Long-Context Retrieval with GLM-5.2</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def tool_calculator(expression: str):
   if not re.fullmatch(r"[0-9+\-*/(). %]+", expression or ""):
       return {"error": "unsupported characters"}
   try:    return {"result": eval(expression, {"__builtins__": {}}, {})}
   except Exception as e: return {"error": str(e)}
_CITY_POP = {"tokyo": 37_400_068, "delhi": 32_900_000, "shanghai": 28_500_000,
            "sao paulo": 22_400_000, "mexico city": 21_800_000}
def tool_city_population(city: str):
   return {"city": city, "population": _CITY_POP.get((city or "").strip().lower())}
TOOLS = [
   {"type": "function", "function": {
       "name": "calculator", "description": "Evaluate basic arithmetic like '37400068/21800000'.",
       "parameters": {"type": "object", "properties": {"expression": {"type": "string"}},
                      "required": ["expression"]}}},
   {"type": "function", "function": {
       "name": "city_population", "description": "Look up the metro population of a city.",
       "parameters": {"type": "object", "properties": {"city": {"type": "string"}},
                      "required": ["city"]}}},
]
TOOL_IMPLS = {"calculator": tool_calculator, "city_population": tool_city_population}
def run_tool_loop(messages, max_rounds=6, effort="max"):
   """Full loop: model -> tool_calls -> execute -> feed results back -> repeat."""
   for _ in range(max_rounds):
       resp = chat(messages, tools=TOOLS, thinking=True, effort=effort,
                   max_tokens=1500, temperature=0.3)
       _track(resp.usage)
       m = resp.choices[0].message
       if not getattr(m, "tool_calls", None):
           return m.content
       messages.append({
           "role": "assistant", "content": m.content or "",
           "tool_calls": [{"id": tc.id, "type": "function",
                           "function": {"name": tc.function.name,
                                        "arguments": tc.function.arguments}}
                          for tc in m.tool_calls]})
       for tc in m.tool_calls:
           try:    args = json.loads(tc.function.arguments or "{}")
           except json.JSONDecodeError: args = {}
           result = TOOL_IMPLS.get(tc.function.name, lambda **k: {"error": "unknown"})(**args)
           print(f"   ↳ {tc.function.name}({args}) -> {result}")
           messages.append({"role": "tool", "tool_call_id": tc.id,
                            "content": json.dumps(result)})
   return "(stopped: max tool rounds reached)"
def demo_tools():
   print("\n=== 4. FUNCTION / TOOL CALLING ===========================")
   q = ("How many times larger is Tokyo's metro population than Mexico City's? "
        "Use the tools, then answer with the ratio to one decimal place.")
   print("Final:", " ".join((run_tool_loop([{"role": "user", "content": q}]) or "").split()))
def demo_agent():
   print("\n=== 5. MINI MULTI-STEP AGENT (tools + max effort) ========")
   task = ("Rank Tokyo, Delhi, and Shanghai by metro population (largest first), "
           "then compute the combined population of the top two and report it. "
           "Use the tools for every lookup and sum; never guess numbers.")
   ans = run_tool_loop([{"role": "system", "content": "You are a careful analyst."},
                        {"role": "user",   "content": task}])
   print("Final:", " ".join((ans or "").split()))
</code></pre></div></div>



<p class="wp-block-paragraph">We focus on reliable, structured output and long-context retrieval. We create a JSON extraction helper, ask the model to return a strict JSON object, and retry once if the first response is not valid JSON. We also build a synthetic long document with a hidden “needle” and send it to GLM-5.2 to check whether the model retrieves the exact launch code from the provided context.</p>



<h3 class="wp-block-heading"><strong>Running All Demos with GLM-5.2 Token and Cost Accounting</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def cost_summary():
   print("\n=== 8. TOKEN + COST ACCOUNTING ===========================")
   cost = _USAGE["in"]/1e6*PRICE_IN_PER_M + _USAGE["out"]/1e6*PRICE_OUT_PER_M
   print(f"  calls: {_USAGE['calls']} | input: {_USAGE['in']:,} tok | output: {_USAGE['out']:,} tok")
   print(f"  estimated spend @ ${PRICE_IN_PER_M}/{PRICE_OUT_PER_M} per 1M: ${cost:0.4f}")
DEMOS = [demo_basic, demo_effort, demo_streaming, demo_tools,
        demo_agent, demo_structured, demo_long_context]
print(f"Provider={PROVIDER}   model={MODEL}")
for fn in DEMOS:
   try:    fn()
   except Exception as e:
       print(f"  [skipped {fn.__name__}: {type(e).__name__}: {e}]")
cost_summary()
print("\nDone. Tweak PROVIDER / effort / max_tokens and re-run any demo function.")
</code></pre></div></div>



<p class="wp-block-paragraph">We finish the tutorial by collecting usage information and running all demos from top to bottom. We calculate the estimated cost from total input and output tokens, then print a compact summary of calls, token counts, and spend. We also use a driver loop so that a single failed demo does not halt the entire notebook, making the tutorial easier to run, debug, and reuse.</p>



<h3 class="wp-block-heading"><strong>Conclusion</strong></h3>



<p class="wp-block-paragraph">In conclusion, we have a practical and reusable workflow for using GLM-5.2 in Python applications. We learned how to control its reasoning behavior, compare different thinking modes, connect it with tools, validate structured outputs, test long-context inputs, and monitor token usage with estimated cost. It provides us a strong starting point for building more advanced systems such as research assistants, document analysis tools, coding agents, long-context retrieval workflows, or API-based reasoning pipelines. We finished with a setup that is lightweight enough for Colab but still close to how we would build with GLM-5.2 in a real project.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/LLM%20Projects/glm_5_2_api_workflow_tutorial_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">Full Codes here</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/22/glm-5-2-openai-compatible-api-a-hands-on-guide-to-reasoning-effort-function-calling-and-long-context-retrieval/">GLM-5.2 OpenAI-Compatible API: A Hands-On Guide to Reasoning Effort, Function Calling, and Long-Context Retrieval</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80658</post-id>
	</item>
		<item>
		<title>xAI Launches /goal in Grok Build, Adding Long-Running Autonomous Execution With Built-In Verification for Multi-Step Coding Tasks</title>
		<link>https://www.marktechpost.com/2026/06/22/xai-launches-goal-in-grok-build-adding-long-running-autonomous-execution-with-built-in-verification-for-multi-step-coding-tasks/</link>
		
		<dc:creator><![CDATA[Michal Sutter]]></dc:creator>
		<pubDate>Mon, 22 Jun 2026 20:34:48 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Tech News]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80655</guid>

					<description><![CDATA[<p>xAI introduced /goal in Grok Build, a mode for long-running, autonomous task execution. You hand off one objective, and the agent plans an approach, executes a progress checklist, and verifies the result until the goal completes.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/22/xai-launches-goal-in-grok-build-adding-long-running-autonomous-execution-with-built-in-verification-for-multi-step-coding-tasks/">xAI Launches /goal in Grok Build, Adding Long-Running Autonomous Execution With Built-In Verification for Multi-Step Coding Tasks</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">xAI shipped a new mode <a href="https://x.ai/news/introducing-goal" target="_blank" rel="noreferrer noopener">called <code>/goal</code> inside Grok Build</a>, its terminal coding agent. The feature targets long-running, autonomous task execution. You hand the agent a larger implementation task, then step back. </p>



<p class="wp-block-paragraph">Most coding sessions require back-and-forth execution and verification. You prompt, the agent acts, and you verify each step. <code>/goal</code> changes that loop. The agent keeps working until a task is completed and verified. Verification can mean reviewing code, inspecting webpages, or executing scripts.</p>



<p class="wp-block-paragraph">The mode packages that autonomy behind one command and a visible progress checklist.</p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li><code>/goal</code> runs long, autonomous tasks inside Grok Build&#8217;s terminal agent.</li>



<li>It plans an approach, builds a checklist, executes, and verifies until complete.</li>



<li>Verification can review code, inspect webpages, or execute scripts.</li>



<li><code>status</code>, <code>pause</code>, <code>resume</code>, and <code>clear</code> steer the live run.</li>



<li>Access needs a SuperGrok or X Premium Plus subscription.</li>
</ul>



<h2 id="h-what-is-goal" class="wp-block-heading"><strong>What is <code>/goal</code></strong></h2>



<p class="wp-block-paragraph"><code>/goal</code> is a mode within Grok Build, not a separate product. Grok Build is xAI&#8217;s coding agent and CLI for software engineering. A CLI is a command-line tool that runs in your terminal. It works against your local codebase, reading files and running commands.</p>



<p class="wp-block-paragraph">Grok Build already ships several agent features. It has a plan mode that blocks edits until you approve a plan. It delegates larger work to specialized subagents that run in parallel. It reads your <code>AGENTS.md</code>, plugins, hooks, skills, and MCP servers out of the box. MCP is a protocol that connects agents to external tools and data.</p>



<p class="wp-block-paragraph">Access requires a SuperGrok or X Premium Plus subscription. You install the CLI with one command:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">curl -fsSL https://x.ai/cli/install.sh | bash</code></pre></div></div>



<p class="wp-block-paragraph">You then sign in with your account and invoke <code>/goal</code>. The mode is built for handoff. You describe one objective, and the agent owns the multi-step work.</p>



<h2 id="h-why-long-running-execution-matters" class="wp-block-heading"><strong>Why Long-Running Execution Matters</strong></h2>



<p class="wp-block-paragraph">An agent is given a goal, not just a single prompt. It plans steps, executes them, observes results, and replans when reality diverges. That is the observe–plan–act loop. The trade-off is observability: long runs produce many actions you must monitor. <code>/goal</code> answers that with a progress checklist and steering commands.</p>



<h2 id="h-how-goal-works" class="wp-block-heading"><strong>How <code>/goal</code> Works</strong></h2>



<p class="wp-block-paragraph">You set a goal in a single line. You type it inside the Grok Build session, not your system shell. For example:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">/goal Migrate the auth module to the new API</code></pre></div></div>



<p class="wp-block-paragraph">Grok Build plans an approach for that objective. It breaks the work into a progress checklist. It then starts executing the checklist items one by one.</p>



<p class="wp-block-paragraph">You are not locked out during the run. You can keep sharing additional instructions as it works. When the goal is finished, the panel flips to <strong>Complete</strong>. Every checklist item then shows as checked.</p>



<h2 id="h-the-verification-step" class="wp-block-heading"><strong>The Verification Step</strong></h2>



<p class="wp-block-paragraph">The detail worth noting is verification. <code>/goal</code> does not stop at writing code. It continues until the task is completed and verified.</p>



<p class="wp-block-paragraph">Verification, per xAI, can take three forms. The agent may review the code it produced. It may inspect webpages to confirm behavior. It may execute scripts to test the result.</p>



<p class="wp-block-paragraph">This matters for autonomous runs. An agent that only edits files may report success before the change works. Building verification into the run means the agent tests its own output before it finishes.</p>



<h2 id="h-interactive-demo" class="wp-block-heading"><strong>Interactive Demo</strong></h2>



<!-- Marktechpost embed: paste into a WordPress Custom HTML block -->
<iframe id="mtp-goal-iframe"
  style="width:100%;border:0;display:block;height:600px;overflow:hidden"
  loading="lazy"
  title="Grok Build /goal interactive"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;Grok Build /goal — Interactive Simulation&lt;/title&gt;
&lt;style&gt;
  :root{
    --bg:#0a0a0a; --panel:#101010; --ink:#f5f5f5; --mut:#9a9a9a;
    --dim:#6a6a6a; --line:#2a2a2a; --line2:#3a3a3a; --hi:#ffffff;
    --ok:#e8e8e8; --chip:#161616;
    --mono:&quot;SF Mono&quot;,ui-monospace,&quot;JetBrains Mono&quot;,&quot;Cascadia Code&quot;,Menlo,Consolas,monospace;
    --sans:ui-sans-serif,system-ui,-apple-system,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;
  }
  *{box-sizing:border-box}
  html,body{margin:0;padding:0;background:transparent}
  #mtp-goal-demo{
    background:var(--bg);color:var(--ink);font-family:var(--sans);
    border:1px solid var(--line2);max-width:880px;margin:0 auto;
    -webkit-font-smoothing:antialiased;
  }
  #mtp-goal-demo .hd{
    display:flex;align-items:center;gap:10px;padding:14px 16px;
    border-bottom:1px solid var(--line);background:var(--panel);
  }
  #mtp-goal-demo .dots{display:flex;gap:6px}
  #mtp-goal-demo .dot{width:10px;height:10px;border:1px solid var(--line2);border-radius:50%}
  #mtp-goal-demo .ttl{font-family:var(--mono);font-size:13px;letter-spacing:.02em;color:var(--ink)}
  #mtp-goal-demo .ttl b{color:var(--hi)}
  #mtp-goal-demo .tag{
    margin-left:auto;font-family:var(--mono);font-size:10px;text-transform:uppercase;
    letter-spacing:.14em;color:var(--dim);border:1px solid var(--line2);padding:4px 8px;
  }
  #mtp-goal-demo .body{padding:16px}
  #mtp-goal-demo .lead{font-size:13px;color:var(--mut);margin:0 0 14px;line-height:1.5}

  /* input row */
  #mtp-goal-demo .promptline{
    display:flex;align-items:center;border:1px solid var(--line2);
    background:#0d0d0d;font-family:var(--mono);
  }
  #mtp-goal-demo .caret{padding:0 10px;color:var(--hi);font-size:14px;user-select:none}
  #mtp-goal-demo .gi{
    flex:1;background:transparent;border:0;color:var(--ink);font-family:var(--mono);
    font-size:14px;padding:13px 6px 13px 0;outline:none;min-width:0;
  }
  #mtp-goal-demo .gi::placeholder{color:var(--dim)}
  #mtp-goal-demo .run{
    border:0;border-left:1px solid var(--line2);background:var(--hi);color:#000;
    font-family:var(--mono);font-size:13px;font-weight:700;padding:0 18px;cursor:pointer;
    letter-spacing:.02em;align-self:stretch;
  }
  #mtp-goal-demo .run:disabled{background:var(--line2);color:var(--dim);cursor:not-allowed}
  #mtp-goal-demo .run:hover:not(:disabled){background:#d8d8d8}

  /* preset chips */
  #mtp-goal-demo .chips{display:flex;flex-wrap:wrap;gap:7px;margin:11px 0 0}
  #mtp-goal-demo .chip{
    font-family:var(--mono);font-size:11px;color:var(--mut);background:var(--chip);
    border:1px solid var(--line2);padding:6px 9px;cursor:pointer;line-height:1.2;
  }
  #mtp-goal-demo .chip:hover{color:var(--hi);border-color:var(--mut)}

  /* layout grid */
  #mtp-goal-demo .grid{display:grid;grid-template-columns:1.15fr .85fr;gap:14px;margin-top:16px}

  /* console */
  #mtp-goal-demo .panel{border:1px solid var(--line);background:#0d0d0d}
  #mtp-goal-demo .panel .ph{
    display:flex;align-items:center;justify-content:space-between;
    padding:9px 12px;border-bottom:1px solid var(--line);
    font-family:var(--mono);font-size:11px;text-transform:uppercase;letter-spacing:.14em;color:var(--dim)
  }
  #mtp-goal-demo .console{height:248px;overflow:auto;padding:11px 12px;font-family:var(--mono);font-size:12.5px;line-height:1.65}
  #mtp-goal-demo .ln{white-space:pre-wrap;word-break:break-word;color:var(--mut);margin:0 0 2px;display:flex;gap:8px}
  #mtp-goal-demo .ln .pre{color:var(--dim);flex:none}
  #mtp-goal-demo .ln.sys .pre{color:var(--hi)}
  #mtp-goal-demo .ln.sys{color:var(--ink)}
  #mtp-goal-demo .ln.ok .pre{color:var(--hi)}
  #mtp-goal-demo .ln.you{color:var(--hi)}
  #mtp-goal-demo .ln.you .pre{color:var(--hi)}
  #mtp-goal-demo .blink{display:inline-block;width:8px;height:15px;background:var(--hi);animation:bk 1s steps(1) infinite;vertical-align:-2px}
  @keyframes bk{50%{opacity:0}}

  /* checklist */
  #mtp-goal-demo .checks{height:248px;overflow:auto;padding:8px 12px}
  #mtp-goal-demo .item{display:flex;gap:10px;align-items:flex-start;padding:8px 0;border-bottom:1px solid #1b1b1b}
  #mtp-goal-demo .item:last-child{border-bottom:0}
  #mtp-goal-demo .box{
    flex:none;width:16px;height:16px;border:1px solid var(--line2);margin-top:1px;
    display:flex;align-items:center;justify-content:center;font-family:var(--mono);font-size:11px;color:#000;background:transparent
  }
  #mtp-goal-demo .item.run .box{border-color:var(--hi);animation:pulse 1s ease-in-out infinite}
  #mtp-goal-demo .item.done .box{background:var(--hi);border-color:var(--hi)}
  @keyframes pulse{50%{background:#222}}
  #mtp-goal-demo .itx{font-family:var(--mono);font-size:12.5px;color:var(--mut);line-height:1.45}
  #mtp-goal-demo .item.run .itx{color:var(--ink)}
  #mtp-goal-demo .item.done .itx{color:var(--dim);text-decoration:line-through;text-decoration-color:var(--line2)}
  #mtp-goal-demo .sub{display:block;font-size:10.5px;color:var(--dim);text-transform:uppercase;letter-spacing:.1em;margin-top:3px}
  #mtp-goal-demo .empty{color:var(--dim);font-family:var(--mono);font-size:12px;padding:14px 2px;line-height:1.6}

  /* status bar */
  #mtp-goal-demo .status{
    display:flex;align-items:center;gap:14px;margin-top:14px;padding:12px 14px;
    border:1px solid var(--line2);background:var(--panel)
  }
  #mtp-goal-demo .state{font-family:var(--mono);font-size:12px;letter-spacing:.12em;text-transform:uppercase;color:var(--mut);flex:none}
  #mtp-goal-demo .state b{color:var(--hi)}
  #mtp-goal-demo .bar{flex:1;height:6px;background:#1c1c1c;border:1px solid var(--line);position:relative;overflow:hidden}
  #mtp-goal-demo .fill{position:absolute;inset:0 100% 0 0;background:var(--hi);transition:right .35s ease}
  #mtp-goal-demo .pct{font-family:var(--mono);font-size:12px;color:var(--ink);flex:none;width:42px;text-align:right}
  #mtp-goal-demo .status.complete{border-color:var(--hi)}
  #mtp-goal-demo .status.complete .state b{color:var(--hi)}

  /* controls */
  #mtp-goal-demo .ctrls{display:flex;flex-wrap:wrap;gap:8px;margin-top:12px}
  #mtp-goal-demo .ctrl{
    font-family:var(--mono);font-size:12px;color:var(--ink);background:#0d0d0d;
    border:1px solid var(--line2);padding:9px 14px;cursor:pointer;letter-spacing:.02em
  }
  #mtp-goal-demo .ctrl:hover:not(:disabled){border-color:var(--hi);color:var(--hi)}
  #mtp-goal-demo .ctrl:disabled{opacity:.4;cursor:not-allowed}
  #mtp-goal-demo .ctrl span{color:var(--dim)}

  /* footer */
  #mtp-goal-demo .ft{
    display:flex;align-items:center;gap:10px;flex-wrap:wrap;
    padding:12px 16px;border-top:1px solid var(--line);background:var(--panel);
    font-family:var(--mono);font-size:11px;color:var(--dim)
  }
  #mtp-goal-demo .brand{display:flex;align-items:center;gap:8px;color:var(--mut)}
  #mtp-goal-demo .mark{width:11px;height:11px;background:var(--hi);display:inline-block}
  #mtp-goal-demo .brand b{color:var(--ink);font-weight:700}
  #mtp-goal-demo .ft .note{margin-left:auto;color:var(--dim)}

  @media (max-width:640px){
    #mtp-goal-demo .grid{grid-template-columns:1fr}
    #mtp-goal-demo .console,#mtp-goal-demo .checks{height:200px}
    #mtp-goal-demo .ttl{font-size:12px}
    #mtp-goal-demo .ft .note{margin-left:0;width:100%}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;mtp-goal-demo&quot;&gt;
  &lt;div class=&quot;hd&quot;&gt;
    &lt;div class=&quot;dots&quot;&gt;&lt;span class=&quot;dot&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot&quot;&gt;&lt;/span&gt;&lt;/div&gt;
    &lt;div class=&quot;ttl&quot;&gt;grok build &lt;b&gt;/goal&lt;/b&gt;&lt;/div&gt;
    &lt;div class=&quot;tag&quot;&gt;Illustrative simulation&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;body&quot;&gt;
    &lt;p class=&quot;lead&quot;&gt;Hand the agent one objective. It plans an approach, builds a progress checklist, executes, and verifies until the goal is complete. This is a self-contained demo of the documented flow — it does not connect to xAI or run real code.&lt;/p&gt;

    &lt;div class=&quot;promptline&quot;&gt;
      &lt;span class=&quot;caret&quot;&gt;$&lt;/span&gt;
      &lt;input class=&quot;gi&quot; id=&quot;gi&quot; autocomplete=&quot;off&quot; spellcheck=&quot;false&quot;
             placeholder=&quot;/goal Migrate the auth module to the new API&quot;&gt;
      &lt;button class=&quot;run&quot; id=&quot;run&quot;&gt;/goal&lt;/button&gt;
    &lt;/div&gt;

    &lt;div class=&quot;chips&quot; id=&quot;chips&quot;&gt;&lt;/div&gt;

    &lt;div class=&quot;grid&quot;&gt;
      &lt;div class=&quot;panel&quot;&gt;
        &lt;div class=&quot;ph&quot;&gt;&lt;span&gt;Console&lt;/span&gt;&lt;span id=&quot;phstate&quot;&gt;idle&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;console&quot; id=&quot;console&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;panel&quot;&gt;
        &lt;div class=&quot;ph&quot;&gt;&lt;span&gt;Progress checklist&lt;/span&gt;&lt;span id=&quot;phcount&quot;&gt;0/0&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;checks&quot; id=&quot;checks&quot;&gt;
          &lt;div class=&quot;empty&quot;&gt;No active goal.&lt;br&gt;Type a goal above or pick an example, then run.&lt;/div&gt;
        &lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;status&quot; id=&quot;status&quot;&gt;
      &lt;div class=&quot;state&quot;&gt;State &lt;b id=&quot;state&quot;&gt;IDLE&lt;/b&gt;&lt;/div&gt;
      &lt;div class=&quot;bar&quot;&gt;&lt;div class=&quot;fill&quot; id=&quot;fill&quot;&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;pct&quot; id=&quot;pct&quot;&gt;0%&lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;ctrls&quot;&gt;
      &lt;button class=&quot;ctrl&quot; id=&quot;c-status&quot;&gt;/goal status &lt;span&gt;· live panel&lt;/span&gt;&lt;/button&gt;
      &lt;button class=&quot;ctrl&quot; id=&quot;c-pause&quot; disabled&gt;/goal pause &lt;span&gt;· keep goal&lt;/span&gt;&lt;/button&gt;
      &lt;button class=&quot;ctrl&quot; id=&quot;c-resume&quot; disabled&gt;/goal resume &lt;span&gt;· pick back up&lt;/span&gt;&lt;/button&gt;
      &lt;button class=&quot;ctrl&quot; id=&quot;c-clear&quot; disabled&gt;/goal clear &lt;span&gt;· drop goal&lt;/span&gt;&lt;/button&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span class=&quot;brand&quot;&gt;&lt;span class=&quot;mark&quot;&gt;&lt;/span&gt;&lt;b&gt;Marktechpost&lt;/b&gt;&lt;/span&gt;
    &lt;span&gt;· interactive explainer&lt;/span&gt;
    &lt;span class=&quot;note&quot;&gt;Behavior modeled on xAI's /goal announcement (Jun 22, 2026)&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var root=document.getElementById('mtp-goal-demo');
  var gi=document.getElementById('gi'), runBtn=document.getElementById('run');
  var con=document.getElementById('console'), checks=document.getElementById('checks');
  var stateEl=document.getElementById('state'), fill=document.getElementById('fill');
  var pct=document.getElementById('pct'), statusBox=document.getElementById('status');
  var phstate=document.getElementById('phstate'), phcount=document.getElementById('phcount');
  var cStatus=document.getElementById('c-status'), cPause=document.getElementById('c-pause');
  var cResume=document.getElementById('c-resume'), cClear=document.getElementById('c-clear');

  var presets=[
    &quot;Migrate the auth module to the new API&quot;,
    &quot;Refactor the billing service and run its test suite&quot;,
    &quot;Add a /users/:id endpoint with validation and tests&quot;,
    &quot;Upgrade the date library and fix the build&quot;
  ];
  var chips=document.getElementById('chips');
  presets.forEach(function(p){
    var b=document.createElement('button'); b.className='chip'; b.textContent=p;
    b.onclick=function(){ gi.value=&quot;/goal &quot;+p; gi.focus(); };
    chips.appendChild(b);
  });

  // verification actions cycle, taken from the documented forms
  var verify=[&quot;reviewing code&quot;,&quot;inspecting webpages&quot;,&quot;executing scripts&quot;];

  var timer=null, items=[], idx=0, paused=false, running=false;

  function emit(text, cls){
    var d=document.createElement('div'); d.className='ln '+(cls||'');
    var pre=document.createElement('span'); pre.className='pre';
    pre.textContent = cls==='you' ? '$' : cls==='ok' ? '✓' : '›';
    var t=document.createElement('span'); t.textContent=text;
    d.appendChild(pre); d.appendChild(t); con.appendChild(d);
    con.scrollTop=con.scrollHeight;
    return d;
  }
  function setState(s){ stateEl.textContent=s; phstate.textContent=s.toLowerCase(); }
  function setProgress(){
    var done=items.filter(function(i){return i.done;}).length;
    var p = items.length ? Math.round(done/items.length*100) : 0;
    fill.style.right=(100-p)+'%'; pct.textContent=p+'%';
    phcount.textContent=done+'/'+items.length;
  }
  function buildChecklist(goal){
    var target = goal.replace(/^migrate the\s*/i,'').replace(/\.$/,'');
    return [
      {t:&quot;Analyze repository structure and locate targets&quot;, s:&quot;planning&quot;},
      {t:&quot;Draft an execution plan for: &quot;+goal, s:&quot;planning&quot;},
      {t:&quot;Apply changes across affected files&quot;, s:&quot;editing&quot;},
      {t:&quot;Review the generated code for errors&quot;, s:&quot;reviewing code&quot;},
      {t:&quot;Inspect affected pages and endpoints&quot;, s:&quot;inspecting webpages&quot;},
      {t:&quot;Execute verification scripts and tests&quot;, s:&quot;executing scripts&quot;},
      {t:&quot;Confirm task completed and verified&quot;, s:&quot;verifying&quot;}
    ];
  }
  function renderChecklist(){
    checks.innerHTML='';
    items.forEach(function(it,i){
      var row=document.createElement('div'); row.className='item'+(it.done?' done':'')+(i===idx&amp;&amp;running&amp;&amp;!it.done?' run':'');
      var box=document.createElement('div'); box.className='box'; box.textContent=it.done?'✓':'';
      var tx=document.createElement('div'); tx.className='itx';
      tx.appendChild(document.createTextNode(it.t));
      var sub=document.createElement('span'); sub.className='sub'; sub.textContent=it.s;
      tx.appendChild(sub);
      row.appendChild(box); row.appendChild(tx); checks.appendChild(row);
    });
    setProgress();
  }

  function startGoal(){
    var raw=gi.value.trim();
    if(!raw) return;
    var goal = raw.replace(/^\/goal\s+/i,'').trim();
    if(!goal) return;

    // reset
    if(timer){clearInterval(timer);timer=null;}
    con.innerHTML=''; idx=0; paused=false; running=true;
    statusBox.classList.remove('complete');
    items=buildChecklist(goal);

    emit(&quot;/goal &quot;+goal, 'you');
    runBtn.disabled=true; gi.disabled=true;
    cPause.disabled=false; cResume.disabled=true; cClear.disabled=false;

    setState('PLANNING');
    emit(&quot;Planning an approach for the objective.&quot;, 'sys');
    var ph=emit(&quot;Breaking the work into a progress checklist&quot;, 'sys');
    var b=document.createElement('span'); b.className='blink'; ph.appendChild(b);

    renderChecklist();

    setTimeout(function(){
      ph.removeChild(b);
      emit(&quot;Checklist ready — &quot;+items.length+&quot; steps. Starting execution.&quot;, 'ok');
      setState('RUNNING');
      runLoop();
    }, 950);
  }

  function runLoop(){
    if(timer){clearInterval(timer);}
    timer=setInterval(function(){
      if(paused) return;
      if(idx&gt;=items.length){ finish(); return; }
      var it=items[idx];
      renderChecklist();
      // log the step action with a verification verb where relevant
      var v = it.s;
      emit(stepVerb(idx)+&quot; — &quot;+v, idx&lt;2 ? 'sys' : 'sys');
      it.done=true; idx++;
      renderChecklist();
      if(idx&gt;=items.length){ finish(); }
    }, 1100);
  }
  function stepVerb(i){
    var verbs=[&quot;Scanning files&quot;,&quot;Planning&quot;,&quot;Editing files&quot;,&quot;Reviewing output&quot;,&quot;Inspecting pages&quot;,&quot;Running scripts&quot;,&quot;Finalizing&quot;];
    return verbs[i]||&quot;Working&quot;;
  }
  function finish(){
    if(timer){clearInterval(timer);timer=null;}
    running=false;
    setState('COMPLETE');
    statusBox.classList.add('complete');
    emit(&quot;Goal complete. All checklist items verified.&quot;, 'ok');
    renderChecklist();
    runBtn.disabled=false; gi.disabled=false;
    cPause.disabled=true; cResume.disabled=true; cClear.disabled=false;
    resize();
  }

  // controls
  cPause.onclick=function(){
    if(!running) return;
    paused=true; setState('PAUSED');
    emit(&quot;Work paused. The goal is kept.&quot;, 'sys');
    cPause.disabled=true; cResume.disabled=false;
  };
  cResume.onclick=function(){
    if(!running||!paused) return;
    paused=false; setState('RUNNING');
    emit(&quot;Resuming from the last checklist item.&quot;, 'sys');
    cPause.disabled=false; cResume.disabled=true;
  };
  cClear.onclick=function(){
    if(timer){clearInterval(timer);timer=null;}
    running=false; paused=false; items=[]; idx=0;
    con.innerHTML=''; setState('IDLE'); statusBox.classList.remove('complete');
    checks.innerHTML='&lt;div class=&quot;empty&quot;&gt;No active goal.&lt;br&gt;Type a goal above or pick an example, then run.&lt;/div&gt;';
    setProgress();
    emit(&quot;Goal dropped. Ready for a new objective.&quot;, 'sys');
    runBtn.disabled=false; gi.disabled=false;
    cPause.disabled=true; cResume.disabled=true; cClear.disabled=true;
    gi.focus();
  };
  cStatus.onclick=function(){
    var done=items.length?items.filter(function(i){return i.done;}).length:0;
    emit(&quot;/goal status — &quot;+stateEl.textContent+&quot; · &quot;+done+&quot;/&quot;+items.length+&quot; steps complete&quot;, 'you');
    statusBox.scrollIntoView({behavior:'smooth',block:'nearest'});
  };

  runBtn.onclick=startGoal;
  gi.addEventListener('keydown',function(e){ if(e.key==='Enter'){ startGoal(); }});

  // auto-resize for WordPress srcdoc iframe (component height + ~40px)
  function resize(){
    try{
      var h = root.offsetHeight + 40;
      parent.postMessage({type:'mtp-goal-resize', height:h}, '*');
    }catch(e){}
  }
  var ro = window.ResizeObserver ? new ResizeObserver(resize) : null;
  if(ro) ro.observe(root);
  window.addEventListener('load',resize);
  setInterval(resize, 1200);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
<script>
(function(){
  window.addEventListener('message', function(e){
    var d = e.data || {};
    if(d && d.type === 'mtp-goal-resize' && d.height){
      var f = document.getElementById('mtp-goal-iframe');
      if(f){ f.style.height = d.height + 'px'; }
    }
  });
})();
</script>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use Cases With Examples</strong></h2>



<p class="wp-block-paragraph">Module migration is the main example. <code>/goal Migrate the auth module to the new API</code> covers a multi-file change. The task is long, mechanical, and testable.</p>



<p class="wp-block-paragraph"><strong>Other practical handoffs fit the same shape:</strong></p>



<ul class="wp-block-list">
<li>Refactor a service, then run its test suite to confirm a green build.</li>



<li>Add an endpoint, then inspect the rendered page for runtime errors.</li>



<li>Upgrade a dependency, execute the build, and fix the breakages.</li>



<li>Port a config across files, then run a script to validate it.</li>
</ul>



<p class="wp-block-paragraph">Each task suits an autonomous goal better than a quick one-line edit. The payoff comes when the work spans many steps you would otherwise supervise.</p>



<h2 id="h-steering-a-long-running-goal" class="wp-block-heading"><strong>Steering a Long-Running Goal</strong></h2>



<p class="wp-block-paragraph"><code>/goal</code> adds commands to monitor and steer the run. You type them inside the Grok Build session:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">/goal status     # see the live progress panel
/goal pause      # stop work, keep the goal
/goal resume     # pick back up
/goal clear      # drop the goal entirely</code></pre></div></div>



<p class="wp-block-paragraph"><code>status</code> surfaces the live progress panel. <code>pause</code> halts work but keeps the goal intact. <code>resume</code> continues from where it stopped. <code>clear</code> drops the goal entirely and starts fresh.</p>



<p class="wp-block-paragraph">These controls are important for unattended runs. You can pause work or drop the goal at any point. That helps when an agent edits files and runs scripts on its own.</p>



<h2 id="h-how-goal-compares" class="wp-block-heading"><strong>How <code>/goal</code> Compares</strong></h2>



<p class="wp-block-paragraph"><code>/goal</code> sits among terminal coding agents that already run multi-step work. What it adds is an explicit, steerable goal object. The named lifecycle commands and the built-in verification step are the new parts.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Dimension</th><th>Grok Build <code>/goal</code></th><th>Claude Code</th><th>OpenAI Codex CLI</th><th>Cursor Agent Mode</th></tr></thead><tbody><tr><td>Handoff model</td><td>One-line goal object</td><td>Conversational + task list</td><td>Prompt with approval mode</td><td>Chat instruction in IDE</td></tr><tr><td>Progress view</td><td>Auto checklist + live panel</td><td>Internal to-do list</td><td>Step-by-step log</td><td>Inline edit stream</td></tr><tr><td>Steering controls</td><td><code>status</code> / <code>pause</code> / <code>resume</code> / <code>clear</code></td><td>Interrupt and reprompt</td><td>Approve / reject steps</td><td>Accept / reject edits</td></tr><tr><td>Verification</td><td>Runs until completed and verified</td><td>Runs tests when instructed</td><td>Runs tests when instructed</td><td>Runs tests when instructed</td></tr><tr><td>Where it runs</td><td>Terminal (TUI, headless, ACP)</td><td>Terminal</td><td>Terminal</td><td>IDE-integrated</td></tr><tr><td>Access</td><td>SuperGrok / X Premium Plus</td><td>Subscription or API</td><td>Subscription or API</td><td>Subscription</td></tr></tbody></table></figure>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li><code>/goal</code> runs long, autonomous tasks inside Grok Build&#8217;s terminal agent.</li>



<li>It plans an approach, builds a checklist, executes, and verifies until complete.</li>



<li>Verification can review code, inspect webpages, or execute scripts.</li>



<li><code>status</code>, <code>pause</code>, <code>resume</code>, and <code>clear</code> steer the live run.</li>



<li>Access needs a SuperGrok or X Premium Plus subscription.</li>
</ul>



<h2 id="h-community-reactions" class="wp-block-heading"><strong>Community Reactions</strong></h2>



<p class="wp-block-paragraph"></p>



<!-- Marktechpost embed: paste into a WordPress Custom HTML block -->
<iframe id="mtp-reactions-iframe"
  style="width:100%;border:0;display:block;height:600px;overflow:hidden"
  loading="lazy"
  title="Grok Build /goal interactive"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;Community Reactions — Grok Build /goal&lt;/title&gt;
&lt;style&gt;
  :root{
    --bg:#0a0a0a; --panel:#101010; --card:#0d0d0d; --ink:#f5f5f5; --mut:#9a9a9a;
    --dim:#6a6a6a; --line:#2a2a2a; --line2:#3a3a3a; --hi:#ffffff; --chip:#161616;
    --mono:&quot;SF Mono&quot;,ui-monospace,&quot;JetBrains Mono&quot;,&quot;Cascadia Code&quot;,Menlo,Consolas,monospace;
    --sans:ui-sans-serif,system-ui,-apple-system,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;
  }
  *{box-sizing:border-box}
  html,body{margin:0;padding:0;background:transparent}
  #mtp-reactions{
    background:var(--bg);color:var(--ink);font-family:var(--sans);
    border:1px solid var(--line2);max-width:880px;margin:0 auto;-webkit-font-smoothing:antialiased;
  }
  #mtp-reactions .hd{padding:16px;border-bottom:1px solid var(--line);background:var(--panel)}
  #mtp-reactions .hd h3{margin:0;font-size:16px;letter-spacing:.01em;color:var(--hi);font-weight:700}
  #mtp-reactions .hd .sub{margin:5px 0 0;font-family:var(--mono);font-size:11px;color:var(--dim);letter-spacing:.04em}
  #mtp-reactions .banner{
    margin:14px 16px 0;padding:9px 12px;border:1px dashed var(--line2);background:#0d0d0d;
    font-family:var(--mono);font-size:11px;color:var(--mut);line-height:1.55
  }
  #mtp-reactions .banner b{color:var(--ink)}
  #mtp-reactions .tabs{display:flex;flex-wrap:wrap;gap:7px;padding:14px 16px 0}
  #mtp-reactions .tab{
    font-family:var(--mono);font-size:11px;color:var(--mut);background:var(--chip);
    border:1px solid var(--line2);padding:7px 12px;cursor:pointer;letter-spacing:.04em
  }
  #mtp-reactions .tab.on{background:var(--hi);color:#000;border-color:var(--hi);font-weight:700}
  #mtp-reactions .tab:hover:not(.on){color:var(--hi);border-color:var(--mut)}

  #mtp-reactions .grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;padding:14px 16px 4px}
  #mtp-reactions .card{
    border:1px solid var(--line);background:var(--card);padding:14px;display:flex;flex-direction:column;gap:9px
  }
  #mtp-reactions .crow{display:flex;align-items:center;gap:9px}
  #mtp-reactions .badge{
    font-family:var(--mono);font-size:9.5px;text-transform:uppercase;letter-spacing:.12em;
    border:1px solid var(--line2);color:var(--ink);padding:3px 7px;flex:none
  }
  #mtp-reactions .src{font-family:var(--mono);font-size:12px;color:var(--mut)}
  #mtp-reactions .src b{color:var(--ink)}
  #mtp-reactions .txt{font-size:13.5px;line-height:1.55;color:var(--ink);margin:0}
  #mtp-reactions .meta{
    margin-top:auto;display:flex;align-items:center;gap:8px;
    font-family:var(--mono);font-size:10.5px;color:var(--dim);letter-spacing:.06em;
    padding-top:7px;border-top:1px solid #1b1b1b
  }
  #mtp-reactions .theme{text-transform:uppercase}
  #mtp-reactions .lnk{margin-left:auto;color:var(--dim);text-decoration:none;border-bottom:1px dotted var(--line2)}
  #mtp-reactions .lnk:hover{color:var(--hi);border-color:var(--hi)}

  #mtp-reactions .ft{
    display:flex;align-items:center;gap:10px;flex-wrap:wrap;padding:12px 16px;
    border-top:1px solid var(--line);background:var(--panel);
    font-family:var(--mono);font-size:11px;color:var(--dim)
  }
  #mtp-reactions .brand{display:flex;align-items:center;gap:8px;color:var(--mut)}
  #mtp-reactions .mark{width:11px;height:11px;background:var(--hi);display:inline-block}
  #mtp-reactions .brand b{color:var(--ink)}
  #mtp-reactions .ft .note{margin-left:auto;color:var(--dim)}

  @media (max-width:640px){
    #mtp-reactions .grid{grid-template-columns:1fr}
    #mtp-reactions .ft .note{margin-left:0;width:100%}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;mtp-reactions&quot;&gt;
  &lt;div class=&quot;hd&quot;&gt;
    &lt;h3&gt;Community reactions: Grok Build &lt;code&gt;/goal&lt;/code&gt;&lt;/h3&gt;
    &lt;p class=&quot;sub&quot;&gt;Early developer sentiment across X · Reddit · Hacker News&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;banner&quot;&gt;
    &lt;b&gt;Editor's note:&lt;/b&gt; /goal launched June 22, 2026. The cards below are &lt;b&gt;representative discussion themes&lt;/b&gt;, paraphrased from how practitioners typically frame a launch like this — not quotes attributed to specific accounts. Replace each card with a verified post and link as real reactions appear.
  &lt;/div&gt;

  &lt;div class=&quot;tabs&quot; id=&quot;tabs&quot;&gt;
    &lt;button class=&quot;tab on&quot; data-f=&quot;all&quot;&gt;All&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-f=&quot;x&quot;&gt;X / Twitter&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-f=&quot;reddit&quot;&gt;Reddit&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-f=&quot;hn&quot;&gt;Hacker News&lt;/button&gt;
  &lt;/div&gt;

  &lt;div class=&quot;grid&quot; id=&quot;grid&quot;&gt;&lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span class=&quot;brand&quot;&gt;&lt;span class=&quot;mark&quot;&gt;&lt;/span&gt;&lt;b&gt;Marktechpost&lt;/b&gt;&lt;/span&gt;
    &lt;span&gt;· community pulse&lt;/span&gt;
    &lt;span class=&quot;note&quot;&gt;Representative themes · verify before publishing&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var data=[
    {p:'x', src:'Developer reaction', theme:'verification loop',
     t:'The interesting part is not autonomy, it is that /goal keeps going until the task is verified, not just written.'},
    {p:'x', src:'Engineer reaction', theme:'checklist UX',
     t:'A goal that auto-breaks into a checklist and flips to Complete is a clean mental model for long tasks.'},
    {p:'reddit', src:'r/programming theme', theme:'pause / resume',
     t:'Pause and resume on a running goal is the feature I want most. A kill switch for unattended agents matters.'},
    {p:'hn', src:'Hacker News thread', theme:'vs Claude Code / Codex',
     t:'Every lab now ships a long-running coding mode. The differentiator here is the named goal lifecycle, not raw capability.'},
    {p:'reddit', src:'r/grok theme', theme:'access gating',
     t:'Looks useful, but it is locked behind SuperGrok or X Premium Plus. Hard to evaluate without a paid plan.'},
    {p:'hn', src:'Hacker News comment', theme:'blast radius',
     t:'Autonomous file edits plus script execution is powerful and risky. I would scope it to a worktree before trusting it.'},
    {p:'x', src:'Practitioner reaction', theme:'real workflow fit',
     t:'Module migrations and dependency upgrades are exactly the long, mechanical jobs you want to hand off and verify.'},
    {p:'reddit', src:'r/devtools theme', theme:'steering mid-run',
     t:'Being able to keep sending instructions while the goal runs is the detail that makes this feel practical.'}
  ];

  var grid=document.getElementById('grid'), tabs=document.getElementById('tabs');
  var root=document.getElementById('mtp-reactions');
  var labels={x:'X', reddit:'Reddit', hn:'HN'};

  function render(f){
    grid.innerHTML='';
    data.filter(function(d){return f==='all'||d.p===f;}).forEach(function(d){
      var c=document.createElement('div'); c.className='card';
      var row=document.createElement('div'); row.className='crow';
      var b=document.createElement('span'); b.className='badge'; b.textContent=labels[d.p];
      var s=document.createElement('span'); s.className='src'; s.innerHTML='&lt;b&gt;'+d.src+'&lt;/b&gt;';
      row.appendChild(b); row.appendChild(s);
      var t=document.createElement('p'); t.className='txt'; t.textContent='“'+d.t+'”';
      var m=document.createElement('div'); m.className='meta';
      var th=document.createElement('span'); th.className='theme'; th.textContent=d.theme;
      var a=document.createElement('a'); a.className='lnk'; a.href='#'; a.textContent='add source link';
      a.onclick=function(e){e.preventDefault();};
      m.appendChild(th); m.appendChild(a);
      c.appendChild(row); c.appendChild(t); c.appendChild(m);
      grid.appendChild(c);
    });
    resize();
  }
  Array.prototype.forEach.call(tabs.children,function(btn){
    btn.onclick=function(){
      Array.prototype.forEach.call(tabs.children,function(b){b.classList.remove('on');});
      btn.classList.add('on'); render(btn.getAttribute('data-f'));
    };
  });
  render('all');

  function resize(){
    try{ parent.postMessage({type:'mtp-reactions-resize', height: root.offsetHeight+40}, '*'); }catch(e){}
  }
  var ro = window.ResizeObserver ? new ResizeObserver(resize) : null;
  if(ro) ro.observe(root);
  window.addEventListener('load',resize);
  setInterval(resize,1200);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
<script>
(function(){
  window.addEventListener('message', function(e){
    var d = e.data || {};
    if(d && d.type === 'mtp-reactions-resize' && d.height){
      var f = document.getElementById('mtp-reactions-iframe');
      if(f){ f.style.height = d.height + 'px'; }
    }
  });
})();
</script>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://x.ai/news/introducing-goal" target="_blank" rel="noreferrer noopener">Technical details</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>



<p class="wp-block-paragraph"></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/22/xai-launches-goal-in-grok-build-adding-long-running-autonomous-execution-with-built-in-verification-for-multi-step-coding-tasks/">xAI Launches /goal in Grok Build, Adding Long-Running Autonomous Execution With Built-In Verification for Multi-Step Coding Tasks</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80655</post-id>
	</item>
		<item>
		<title>Sakana AI Launches Sakana Fugu: An Orchestration Model That Routes Tasks Across a Swappable Pool of Frontier LLMs</title>
		<link>https://www.marktechpost.com/2026/06/22/sakana-ai-launches-sakana-fugu-an-orchestration-model-that-routes-tasks-across-a-swappable-pool-of-frontier-llms/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Mon, 22 Jun 2026 18:42:59 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Agents]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Generative AI]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80652</guid>

					<description><![CDATA[<p>Fugu and Fugu Ultra route tasks across a swappable model pool, leading most coding, reasoning, and agentic benchmarks.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/22/sakana-ai-launches-sakana-fugu-an-orchestration-model-that-routes-tasks-across-a-swappable-pool-of-frontier-llms/">Sakana AI Launches Sakana Fugu: An Orchestration Model That Routes Tasks Across a Swappable Pool of Frontier LLMs</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Today, Sakana AI launched <strong>Sakana Fugu</strong>. It is a multi-agent orchestration system that behaves like one model. You send a request to a single endpoint. Fugu decides how to handle it internally. It solves a task directly when that is enough. It also assembles and coordinates a team of expert models when needed. The complexity of a multi-agent system never reaches your code.</p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li>Fugu delivers a multi-agent system behind one OpenAI-compatible API.</li>



<li>Fugu Ultra leads most published coding and reasoning benchmarks.</li>



<li>The orchestrator beats the individual models it coordinates.</li>



<li>Opt-out and provider routing target compliance and single-vendor risk.</li>



<li>Routing is proprietary, so per-query model selection stays hidden.</li>
</ul>



<h2 id="h-what-is-sakana-fugu" class="wp-block-heading"><strong>What is Sakana Fugu</strong></h2>



<p class="wp-block-paragraph">Fugu is itself a language model. It is trained to call other LLMs in an agent pool. That pool includes instances of itself, called recursively. Fugu manages model selection, delegation, verification, and synthesis internally.</p>



<p class="wp-block-paragraph">Instead of hard-coded roles or workflows, Fugu learns how to coordinate. It decides when to delegate and how agents should communicate. It then combines their work into one answer. From the outside, you call a single model. Inside, a coordinated system of experts does the work.</p>



<p class="wp-block-paragraph">Sakana AI frames this as a hedge against single-vendor dependency. If one provider restricts access, Fugu routes around the disruption. The research team cites recent export controls on Anthropic&#8217;s Fable and Mythos models as motivation. Over time, newer models can be folded into the pool.</p>



<h2 id="h-fugu-and-fugu-ultra-two-models-one-api" class="wp-block-heading"><strong>Fugu and Fugu Ultra: Two Models, One API</strong></h2>



<p class="wp-block-paragraph"><strong>Fugu ships in two variants, both behind one OpenAI-compatible API:</strong></p>



<ul class="wp-block-list">
<li><strong>Fugu</strong> balances strong performance with low latency. It is a default for everyday coding, code review, and chatbots. It also fits tools like Codex. You can opt specific agents out of its pool. That helps teams meet data, privacy, and compliance requirements.</li>



<li><strong>Fugu Ultra</strong> is tuned for maximum answer quality on hard, multi-step problems. It coordinates a deeper pool of expert agents. Its pool is fixed, so opt-out is not available. The current model ID is <code>fugu-ultra-20260615</code>.</li>
</ul>



<h2 id="h-the-research-behind-the-orchestrator" class="wp-block-heading"><strong>The Research Behind the Orchestrator</strong></h2>



<p class="wp-block-paragraph">Fugu builds on two ICLR 2026 papers <a href="https://sakana.ai/trinity/">Trinity</a> and the <a href="https://sakana.ai/learning-to-orchestrate/">Conductor</a> on learned orchestration.</p>



<p class="wp-block-paragraph"><strong>TRINITY</strong> uses a lightweight evolved coordinator across several turns. It assigns Thinker, Worker, or Verifier roles to delegate work adaptively. <strong>Conductor</strong> is trained with reinforcement learning. It discovers natural-language coordination strategies and focused prompts for diverse LLM pools.</p>



<p class="wp-block-paragraph">Together, they show systems can learn to assemble and route agents per task. That replaces hand-designed workflows.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- Sakana Fugu Orchestration Simulator | Marktechpost interactive embed -->
<!-- Paste into a WordPress "Custom HTML" block only. -->
<iframe
  id="fugu-sim-frame"
  title="Sakana Fugu Orchestration Simulator"
  loading="lazy"
  style="width:100%; max-width:920px; display:block; margin:0 auto; border:0; height:600px; overflow:hidden;"
  srcdoc="&lt;!DOCTYPE html&gt;&lt;html lang=&quot;en&quot;&gt;&lt;head&gt;&lt;meta charset=&quot;utf-8&quot;&gt;&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;&lt;link rel=&quot;preconnect&quot; href=&quot;https://fonts.googleapis.com&quot;&gt;&lt;link href=&quot;https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;600;700&amp;display=swap&quot; rel=&quot;stylesheet&quot;&gt;&lt;style&gt;html,body{margin:0;padding:0;background:#fff}body{padding:2px}&lt;/style&gt;&lt;/head&gt;&lt;body&gt;&lt;div id=&quot;fugu-sim-root&quot;&gt;
  &lt;style&gt;
    #fugu-sim-root *{box-sizing:border-box;margin:0;padding:0}
    #fugu-sim-root{
      --bg:#ffffff;--ink:#0a0a0a;--mut:#6b6b6b;--line:#d6d6d6;
      --fill:#0a0a0a;--soft:#f4f4f4;--soft2:#ececec;
      font-family:&quot;IBM Plex Mono&quot;,ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;
      background:var(--bg);color:var(--ink);
      border:1px solid var(--ink);max-width:920px;margin:0 auto;
      -webkit-font-smoothing:antialiased;line-height:1.5;
    }
    #fugu-sim-root .hd{border-bottom:1px solid var(--ink);padding:18px 20px;display:flex;justify-content:space-between;align-items:flex-start;gap:12px;flex-wrap:wrap}
    #fugu-sim-root .hd h2{font-size:17px;letter-spacing:.04em;font-weight:700}
    #fugu-sim-root .hd .tag{font-size:10px;letter-spacing:.14em;text-transform:uppercase;border:1px solid var(--ink);padding:4px 8px;white-space:nowrap}
    #fugu-sim-root .hd p{font-size:11.5px;color:var(--mut);margin-top:6px;max-width:560px}
    #fugu-sim-root .panel{padding:18px 20px;border-bottom:1px solid var(--line)}
    #fugu-sim-root .lbl{font-size:10px;letter-spacing:.16em;text-transform:uppercase;color:var(--mut);margin-bottom:10px;display:block}
    #fugu-sim-root .row{display:flex;gap:10px;flex-wrap:wrap;align-items:center}
    #fugu-sim-root select{
      font-family:inherit;font-size:13px;background:var(--bg);color:var(--ink);
      border:1px solid var(--ink);padding:9px 12px;width:100%;cursor:pointer;border-radius:0;
    }
    #fugu-sim-root .seg{display:inline-flex;border:1px solid var(--ink)}
    #fugu-sim-root .seg button{font-family:inherit;font-size:12px;background:var(--bg);color:var(--ink);border:0;padding:9px 16px;cursor:pointer;letter-spacing:.02em}
    #fugu-sim-root .seg button+button{border-left:1px solid var(--ink)}
    #fugu-sim-root .seg button.on{background:var(--fill);color:#fff}
    #fugu-sim-root .chips{display:flex;gap:8px;flex-wrap:wrap}
    #fugu-sim-root .chip{
      font-family:inherit;font-size:12px;border:1px solid var(--ink);background:var(--bg);color:var(--ink);
      padding:8px 12px;cursor:pointer;display:flex;align-items:center;gap:8px;user-select:none;border-radius:0;
    }
    #fugu-sim-root .chip .box{width:11px;height:11px;border:1px solid var(--ink);display:inline-block;position:relative}
    #fugu-sim-root .chip.on .box{background:var(--fill)}
    #fugu-sim-root .chip.on .box:after{content:&quot;&quot;;position:absolute;inset:2px;border:1px solid #fff;border-top:0;border-right:0;transform:rotate(-45deg);top:1px;left:1px;width:6px;height:3px}
    #fugu-sim-root .chip.off{color:var(--mut);text-decoration:line-through;border-style:dashed;opacity:.6;cursor:not-allowed}
    #fugu-sim-root .chip.locked{cursor:not-allowed;opacity:.85}
    #fugu-sim-root .hint{font-size:10.5px;color:var(--mut);margin-top:10px}
    #fugu-sim-root .actions{display:flex;gap:10px;flex-wrap:wrap;align-items:center}
    #fugu-sim-root button.act{
      font-family:inherit;font-size:13px;letter-spacing:.03em;padding:11px 20px;cursor:pointer;border-radius:0;
      background:var(--fill);color:#fff;border:1px solid var(--ink);
    }
    #fugu-sim-root button.act.ghost{background:var(--bg);color:var(--ink)}
    #fugu-sim-root button.act:disabled{opacity:.4;cursor:not-allowed}
    #fugu-sim-root .grid{display:grid;grid-template-columns:1fr 1fr;gap:0}
    #fugu-sim-root .col{padding:18px 20px}
    #fugu-sim-root .col+.col{border-left:1px solid var(--line)}
    #fugu-sim-root .endpoint{border:1px dashed var(--ink);padding:10px 12px;font-size:12px;background:var(--soft);display:flex;align-items:center;gap:8px;flex-wrap:wrap}
    #fugu-sim-root .dot{width:8px;height:8px;border-radius:50%;background:var(--ink);display:inline-block;flex:none}
    #fugu-sim-root .log{font-size:12px;min-height:230px;max-height:300px;overflow:auto}
    #fugu-sim-root .step{display:flex;gap:10px;padding:7px 0;border-bottom:1px dotted var(--line);opacity:0;transform:translateY(4px);animation:fin .3s forwards}
    @keyframes fin{to{opacity:1;transform:none}}
    #fugu-sim-root .step .n{font-size:10px;border:1px solid var(--ink);width:20px;height:20px;display:flex;align-items:center;justify-content:center;flex:none;margin-top:1px}
    #fugu-sim-root .step .t{flex:1}
    #fugu-sim-root .step .t b{font-weight:700}
    #fugu-sim-root .role{display:inline-block;font-size:9.5px;letter-spacing:.1em;text-transform:uppercase;border:1px solid var(--ink);padding:1px 6px;margin-right:6px}
    #fugu-sim-root .role.v{background:var(--fill);color:#fff}
    #fugu-sim-root .agentline{display:flex;align-items:center;gap:8px;padding:8px 0;border-bottom:1px dotted var(--line);font-size:12px}
    #fugu-sim-root .bar{height:8px;background:var(--soft2);flex:1;position:relative;overflow:hidden}
    #fugu-sim-root .bar i{position:absolute;left:0;top:0;bottom:0;width:0;background:var(--fill);transition:width .5s ease}
    #fugu-sim-root .bar.hatch i{background-image:repeating-linear-gradient(45deg,var(--ink),var(--ink) 2px,transparent 2px,transparent 5px)}
    #fugu-sim-root .answer{border:1px solid var(--ink);padding:14px;font-size:12.5px;background:var(--soft);min-height:60px}
    #fugu-sim-root .answer .muted{color:var(--mut)}
    #fugu-sim-root .metric{display:flex;justify-content:space-between;font-size:11.5px;padding:5px 0;border-bottom:1px dotted var(--line)}
    #fugu-sim-root .metric b{font-weight:700}
    #fugu-sim-root .ft{padding:12px 20px;border-top:1px solid var(--ink);display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap;font-size:10.5px;color:var(--mut)}
    #fugu-sim-root .ft b{color:var(--ink);letter-spacing:.04em}
    #fugu-sim-root .disc{font-size:10px;color:var(--mut);padding:0 20px 16px;line-height:1.6}
    #fugu-sim-root .blink{animation:bk 1s steps(1) infinite}
    @keyframes bk{50%{opacity:0}}
    @media(max-width:640px){
      #fugu-sim-root .grid{grid-template-columns:1fr}
      #fugu-sim-root .col+.col{border-left:0;border-top:1px solid var(--line)}
      #fugu-sim-root .hd h2{font-size:15px}
    }
  &lt;/style&gt;

  &lt;div class=&quot;hd&quot;&gt;
    &lt;div&gt;
      &lt;h2&gt;Sakana Fugu &amp;mdash; Orchestration Simulator&lt;/h2&gt;
      &lt;p&gt;An illustrative walkthrough of how Fugu routes a request: decide, assign Thinker / Worker / Verifier roles, coordinate an agent pool, then synthesize one answer.&lt;/p&gt;
    &lt;/div&gt;
    &lt;span class=&quot;tag&quot;&gt;Illustrative&lt;/span&gt;
  &lt;/div&gt;

  &lt;div class=&quot;panel&quot;&gt;
    &lt;span class=&quot;lbl&quot;&gt;1 &amp;middot; Task type&lt;/span&gt;
    &lt;div class=&quot;row&quot;&gt;
      &lt;select id=&quot;task&quot;&gt;
        &lt;option value=&quot;code&quot;&gt;Coding &amp;amp; code review&lt;/option&gt;
        &lt;option value=&quot;reason&quot;&gt;Reasoning / math&lt;/option&gt;
        &lt;option value=&quot;research&quot;&gt;AutoResearch (automated ML)&lt;/option&gt;
        &lt;option value=&quot;security&quot;&gt;Security assessment&lt;/option&gt;
        &lt;option value=&quot;repro&quot;&gt;Paper reproduction&lt;/option&gt;
        &lt;option value=&quot;longctx&quot;&gt;Long-context analysis&lt;/option&gt;
      &lt;/select&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;panel&quot;&gt;
    &lt;span class=&quot;lbl&quot;&gt;2 &amp;middot; Model&lt;/span&gt;
    &lt;div class=&quot;row&quot;&gt;
      &lt;div class=&quot;seg&quot; id=&quot;modelSeg&quot;&gt;
        &lt;button data-m=&quot;fugu&quot; class=&quot;on&quot;&gt;Fugu&lt;/button&gt;
        &lt;button data-m=&quot;ultra&quot;&gt;Fugu Ultra&lt;/button&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;hint&quot; id=&quot;modelHint&quot;&gt;Fugu balances quality and latency. You can opt agents out of its pool for data, privacy, or compliance needs.&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;panel&quot;&gt;
    &lt;span class=&quot;lbl&quot;&gt;3 &amp;middot; Agent pool&lt;/span&gt;
    &lt;div class=&quot;chips&quot; id=&quot;pool&quot;&gt;
      &lt;div class=&quot;chip on&quot; data-a=&quot;opus&quot;&gt;&lt;span class=&quot;box&quot;&gt;&lt;/span&gt;Opus 4.8&lt;/div&gt;
      &lt;div class=&quot;chip on&quot; data-a=&quot;gemini&quot;&gt;&lt;span class=&quot;box&quot;&gt;&lt;/span&gt;Gemini 3.1 Pro&lt;/div&gt;
      &lt;div class=&quot;chip on&quot; data-a=&quot;gpt&quot;&gt;&lt;span class=&quot;box&quot;&gt;&lt;/span&gt;GPT 5.5&lt;/div&gt;
      &lt;div class=&quot;chip on locked&quot; data-a=&quot;self&quot;&gt;&lt;span class=&quot;box&quot;&gt;&lt;/span&gt;Fugu (recursive self&amp;#8209;call)&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;hint&quot; id=&quot;poolHint&quot;&gt;Toggle a provider off to opt it out. Fugu Ultra uses the full fixed pool.&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;panel&quot;&gt;
    &lt;span class=&quot;lbl&quot;&gt;4 &amp;middot; Resilience event&lt;/span&gt;
    &lt;div class=&quot;actions&quot;&gt;
      &lt;button class=&quot;act ghost&quot; id=&quot;restrictBtn&quot;&gt;Simulate a provider restriction&lt;/button&gt;
      &lt;span class=&quot;hint&quot; id=&quot;restrictState&quot;&gt;No active restriction. The pool is intact.&lt;/span&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;panel&quot; style=&quot;border-bottom:1px solid var(--ink)&quot;&gt;
    &lt;div class=&quot;endpoint&quot;&gt;
      &lt;span class=&quot;dot&quot;&gt;&lt;/span&gt;
      &lt;span&gt;POST /v1/chat/completions&lt;/span&gt;
      &lt;span style=&quot;color:var(--mut)&quot;&gt;&amp;nbsp;model =&lt;/span&gt;
      &lt;b id=&quot;modelId&quot;&gt;fugu&lt;/b&gt;
      &lt;span style=&quot;color:var(--mut)&quot;&gt;&amp;middot; OpenAI&amp;#8209;compatible &amp;middot; single endpoint&lt;/span&gt;
    &lt;/div&gt;
    &lt;div class=&quot;actions&quot; style=&quot;margin-top:14px&quot;&gt;
      &lt;button class=&quot;act&quot; id=&quot;runBtn&quot;&gt;Run orchestration&lt;/button&gt;
      &lt;button class=&quot;act ghost&quot; id=&quot;resetBtn&quot;&gt;Reset&lt;/button&gt;
      &lt;span class=&quot;hint&quot; id=&quot;status&quot;&gt;Ready.&lt;/span&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;grid&quot;&gt;
    &lt;div class=&quot;col&quot;&gt;
      &lt;span class=&quot;lbl&quot;&gt;Orchestration trace&lt;/span&gt;
      &lt;div class=&quot;log&quot; id=&quot;log&quot;&gt;
        &lt;div style=&quot;color:var(--mut);font-size:12px;padding:14px 0&quot;&gt;Press &lt;b&gt;Run orchestration&lt;/b&gt; to watch the routing steps.&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;col&quot;&gt;
      &lt;span class=&quot;lbl&quot;&gt;Agent activity&lt;/span&gt;
      &lt;div id=&quot;agents&quot; style=&quot;margin-bottom:16px&quot;&gt;&lt;/div&gt;
      &lt;span class=&quot;lbl&quot;&gt;Synthesized answer&lt;/span&gt;
      &lt;div class=&quot;answer&quot; id=&quot;answer&quot;&gt;&lt;span class=&quot;muted&quot;&gt;Awaiting run&amp;hellip;&lt;/span&gt;&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;disc&quot;&gt;
    This widget is an educational illustration of Fugu&amp;rsquo;s documented mechanics &amp;mdash; a single OpenAI&amp;#8209;compatible endpoint, a swappable agent pool, opt&amp;#8209;out controls, role assignment (Thinker / Worker / Verifier), and routing around a restricted provider. It does not call any live API. Real model selection and coordination are proprietary and not exposed by Sakana AI. Benchmark figures referenced in the article come from Sakana AI&amp;rsquo;s published materials.
  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;&lt;b&gt;Marktechpost&lt;/b&gt; &amp;middot; Sakana Fugu interactive explainer&lt;/span&gt;
    &lt;span&gt;Source: sakana.ai/fugu&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var root = document.getElementById('fugu-sim-root');
  var Q = function(s){return root.querySelector(s);};
  var QA = function(s){return Array.prototype.slice.call(root.querySelectorAll(s));};

  var state = {model:'fugu', restricted:null, busy:false};

  var AGENTS = {
    opus:{name:'Opus 4.8'}, gemini:{name:'Gemini 3.1 Pro'},
    gpt:{name:'GPT 5.5'}, self:{name:'Fugu (self-call)'}
  };

  var TASKS = {
    code:{plan:'Solve directly with one expert, then verify the diff.',
      answer:'Code review complete. 22 issues surfaced across modules, ranked by severity, each with a suggested fix and a retest note.',
      metrics:[['Issues surfaced','22'],['Roles used','Worker + Verifier'],['Mode','Single-agent + check']]},
    reason:{plan:'Assemble a team. Multiple solvers, then majority verification.',
      answer:'Final answer agreed by the Verifier after cross-checking three independent solution paths.',
      metrics:[['Solution paths','3'],['Roles used','Thinker x2 + Verifier'],['Mode','Multi-agent']]},
    research:{plan:'Open-ended loop: propose, run, read failures, revise, repeat.',
      answer:'Best validation BPB 0.9774 after iterative edits to batch size, depth, learning rate, and optimizer settings.',
      metrics:[['Iterations','123'],['Roles used','Thinker + Worker + Verifier'],['Mode','Long-running agentic']]},
    security:{plan:'Scoped end-to-end pass. Recon, checks, review, report.',
      answer:'End-to-end assessment delivered: recon, XSS/SQLi checks, auth review, and a clean report with evidence and retest steps.',
      metrics:[['Stages','4'],['Roles used','Worker + Verifier'],['Mode','Scoped agentic']]},
    repro:{plan:'Read, implement, train, evaluate, then analyze the gap.',
      answer:'Method reproduced and evaluated. Remaining gap to reported numbers identified and explained.',
      metrics:[['Phases','5'],['Roles used','Thinker + Worker + Verifier'],['Mode','Long-running agentic']]},
    longctx:{plan:'Chunk, retrieve, reason across the full context, then synthesize.',
      answer:'Cross-document synthesis produced, with connections drawn across sources that single-pass reading would miss.',
      metrics:[['Mode','Long-context'],['Roles used','Worker + Verifier'],['Mode2','Retrieve + reason']]}
  };

  function activePool(){
    return QA('#pool .chip').filter(function(c){
      return c.classList.contains('on') &amp;&amp; !c.classList.contains('off');
    }).map(function(c){return c.getAttribute('data-a');});
  }

  function renderModel(){
    var ultra = state.model==='ultra';
    Q('#modelId').textContent = ultra ? 'fugu-ultra-20260615' : 'fugu';
    Q('#modelHint').textContent = ultra
      ? 'Fugu Ultra maximizes answer quality on hard, multi-step problems. It coordinates a deeper, fixed pool.'
      : 'Fugu balances quality and latency. You can opt agents out of its pool for data, privacy, or compliance needs.';
    Q('#poolHint').textContent = ultra
      ? 'Fugu Ultra relies on the full agent pool, so opt-out is disabled.'
      : 'Toggle a provider off to opt it out. Fugu Ultra uses the full fixed pool.';
    QA('#pool .chip').forEach(function(c){
      var a = c.getAttribute('data-a');
      if(a==='self') return;
      if(ultra){ c.classList.add('on'); c.classList.remove('off'); c.classList.add('locked'); }
      else { c.classList.remove('locked'); }
      // re-apply restriction visual
      if(state.restricted===a){ c.classList.add('off'); c.classList.remove('on'); }
    });
  }

  // model toggle
  QA('#modelSeg button').forEach(function(b){
    b.addEventListener('click', function(){
      if(state.busy) return;
      QA('#modelSeg button').forEach(function(x){x.classList.remove('on');});
      b.classList.add('on');
      state.model = b.getAttribute('data-m');
      renderModel();
    });
  });

  // pool toggles
  QA('#pool .chip').forEach(function(c){
    c.addEventListener('click', function(){
      if(state.busy) return;
      var a = c.getAttribute('data-a');
      if(a==='self') return;                         // recursive self-call always present
      if(state.model==='ultra') return;              // fixed pool
      if(state.restricted===a) return;               // restricted stays off
      c.classList.toggle('on');
      c.classList.toggle('off', !c.classList.contains('on'));
    });
  });

  // restriction
  Q('#restrictBtn').addEventListener('click', function(){
    if(state.busy) return;
    if(state.restricted){
      var prev = state.restricted; state.restricted = null;
      var pc = root.querySelector('#pool .chip[data-a=&quot;'+prev+'&quot;]');
      pc.classList.remove('off'); pc.classList.add('on');
      Q('#restrictBtn').textContent = 'Simulate a provider restriction';
      Q('#restrictState').textContent = 'No active restriction. The pool is intact.';
    } else {
      var pool = activePool().filter(function(a){return a!=='self';});
      var pick = pool.length&gt;1 ? pool[0] : 'gpt';
      state.restricted = pick;
      var c = root.querySelector('#pool .chip[data-a=&quot;'+pick+'&quot;]');
      c.classList.add('off'); c.classList.remove('on');
      Q('#restrictBtn').textContent = 'Lift the restriction';
      Q('#restrictState').textContent = AGENTS[pick].name + ' is unavailable. Fugu will route around it.';
    }
  });

  function reset(){
    Q('#log').innerHTML = '&lt;div style=&quot;color:var(--mut);font-size:12px;padding:14px 0&quot;&gt;Press &lt;b&gt;Run orchestration&lt;/b&gt; to watch the routing steps.&lt;/div&gt;';
    Q('#agents').innerHTML = '';
    Q('#answer').innerHTML = '&lt;span class=&quot;muted&quot;&gt;Awaiting run&amp;hellip;&lt;/span&gt;';
    Q('#status').textContent = 'Ready.';
  }
  Q('#resetBtn').addEventListener('click', function(){ if(!state.busy) reset(); });

  function addStep(n, role, html){
    var d = document.createElement('div');
    d.className = 'step';
    var r = role ? '&lt;span class=&quot;role'+(role==='Verifier'?' v':'')+'&quot;&gt;'+role+'&lt;/span&gt;' : '';
    d.innerHTML = '&lt;div class=&quot;n&quot;&gt;'+n+'&lt;/div&gt;&lt;div class=&quot;t&quot;&gt;'+r+html+'&lt;/div&gt;';
    Q('#log').appendChild(d);
    Q('#log').scrollTop = Q('#log').scrollHeight;
  }

  function agentBars(used){
    var box = Q('#agents'); box.innerHTML='';
    used.forEach(function(u){
      var line = document.createElement('div'); line.className='agentline';
      line.innerHTML = '&lt;span style=&quot;width:120px&quot;&gt;'+AGENTS[u.id].name+'&lt;/span&gt;'+
                       '&lt;span class=&quot;bar'+(u.id==='self'?' hatch':'')+'&quot;&gt;&lt;i&gt;&lt;/i&gt;&lt;/span&gt;'+
                       '&lt;span style=&quot;width:64px;text-align:right;color:var(--mut)&quot;&gt;'+u.role+'&lt;/span&gt;';
      box.appendChild(line);
      var bar = line.querySelector('i');
      setTimeout(function(){ bar.style.width = (40+Math.random()*55)+'%'; }, 120);
    });
  }

  function run(){
    if(state.busy) return;
    state.busy = true;
    Q('#runBtn').disabled = true; Q('#resetBtn').disabled = true;
    Q('#log').innerHTML=''; Q('#agents').innerHTML=''; 
    Q('#answer').innerHTML='&lt;span class=&quot;muted&quot;&gt;Orchestrating&lt;span class=&quot;blink&quot;&gt;_&lt;/span&gt;&lt;/span&gt;';
    Q('#status').textContent = 'Running\u2026';

    var task = Q('#task').value;
    var ultra = state.model==='ultra';
    var pool = activePool();
    var workers = pool.filter(function(a){return a!=='self';});
    if(workers.length===0) workers=['self'];
    var T = TASKS[task];

    // choose mode: ultra or research/repro/reason =&gt; multi-agent team
    var team = ultra || task==='research' || task==='repro' || task==='reason';

    var steps = [];
    steps.push({d:300, role:null, html:'Request received at a &lt;b&gt;single endpoint&lt;/b&gt;. No client-side multi-agent code.'});
    steps.push({d:600, role:null, html:'Decide: '+(team?'this task is multi-step &amp;mdash; &lt;b&gt;assemble a team&lt;/b&gt;.':'one strong model is enough &amp;mdash; &lt;b&gt;solve directly&lt;/b&gt;, then verify.')});

    var used = [];
    if(team){
      var thinker = workers[0];
      var worker = workers[1] || workers[0];
      var verifier = workers[2] || workers[0];
      steps.push({d:700, role:'Thinker', html:'&lt;b&gt;'+AGENTS[thinker].name+'&lt;/b&gt; drafts a plan and decomposes the task.'});
      steps.push({d:700, role:'Worker', html:'&lt;b&gt;'+AGENTS[worker].name+'&lt;/b&gt; executes sub-tasks and produces candidate output.'});
      if(pool.indexOf('self')&gt;-1){
        steps.push({d:600, role:'Worker', html:'Fugu issues a &lt;b&gt;recursive self-call&lt;/b&gt; on a hard sub-problem.'});
      }
      steps.push({d:700, role:'Verifier', html:'&lt;b&gt;'+AGENTS[verifier].name+'&lt;/b&gt; checks the result and flags gaps to revise.'});
      used = [{id:thinker,role:'Thinker'},{id:worker,role:'Worker'},{id:verifier,role:'Verifier'}];
      if(pool.indexOf('self')&gt;-1) used.push({id:'self',role:'Worker'});
    } else {
      var solo = workers[0];
      steps.push({d:700, role:'Worker', html:'&lt;b&gt;'+AGENTS[solo].name+'&lt;/b&gt; handles the task in one pass.'});
      steps.push({d:700, role:'Verifier', html:'&lt;b&gt;'+AGENTS[(workers[1]||solo)].name+'&lt;/b&gt; verifies the diff before returning.'});
      used = [{id:solo,role:'Worker'},{id:(workers[1]||solo),role:'Verifier'}];
    }

    if(state.restricted){
      steps.splice(2,0,{d:650, role:null, html:'&lt;b&gt;'+AGENTS[state.restricted].name+'&lt;/b&gt; is restricted. Routing around it &amp;mdash; no integration change.'});
    }

    steps.push({d:700, role:null, html:'Synthesize the agents&amp;rsquo; work into &lt;b&gt;one reliable answer&lt;/b&gt;.'});

    // dedupe used by id keeping first role
    var seen={}; used = used.filter(function(u){ if(seen[u.id])return false; seen[u.id]=1; return true; });

    var i=0, n=0;
    function next(){
      if(i&gt;=steps.length){ finish(used, T); return; }
      var s = steps[i++]; n++;
      setTimeout(function(){
        addStep(n, s.role, s.html);
        if(n===Math.min(4,steps.length-1)) agentBars(used);
        next();
      }, s.d);
    }
    next();
  }

  function finish(used, T){
    setTimeout(function(){
      QA('#agents .bar i').forEach(function(b){ b.style.width='100%'; });
      var mhtml = T.metrics.map(function(m){
        return '&lt;div class=&quot;metric&quot;&gt;&lt;span style=&quot;color:var(--mut)&quot;&gt;'+m[0]+'&lt;/span&gt;&lt;b&gt;'+m[1]+'&lt;/b&gt;&lt;/div&gt;';
      }).join('');
      Q('#answer').innerHTML = '&lt;div style=&quot;margin-bottom:10px&quot;&gt;'+T.answer+'&lt;/div&gt;'+mhtml;
      Q('#status').textContent = 'Done. Routing details are proprietary and not exposed in production.';
      state.busy=false; Q('#runBtn').disabled=false; Q('#resetBtn').disabled=false;
      sz();
    }, 500);
  }

  Q('#runBtn').addEventListener('click', run);
  Q('#task').addEventListener('change', function(){ if(!state.busy) reset(); });

  renderModel();

  // ---- auto-resize for WordPress iframe embed ----
  function sz(){
    var h = root.offsetHeight + 40;
    if(window.parent &amp;&amp; window.parent!==window){
      window.parent.postMessage({type:'fugu-sim-height', height:h}, '*');
    }
  }
  window.addEventListener('load', sz);
  window.addEventListener('resize', sz);
  setTimeout(sz, 300);
  // observe DOM changes that affect height
  if(window.MutationObserver){
    new MutationObserver(sz).observe(root, {childList:true, subtree:true, attributes:true});
  }
})();
&lt;/script&gt;
&lt;/body&gt;&lt;/html&gt;">
</iframe>
<script>
(function(){
  window.addEventListener("message", function(e){
    if (e && e.data && e.data.type === "fugu-sim-height") {
      var f = document.getElementById("fugu-sim-frame");
      if (f && e.data.height) { f.style.height = e.data.height + "px"; }
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<h2 id="h-benchmark" class="wp-block-heading"><strong>Benchmark</strong></h2>



<p class="wp-block-paragraph">Sakana AI compares Fugu against the foundation models it orchestrates. Baselines use provider-reported scores. SWE Bench Pro uses the mini-swe-agent as scaffolding. </p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Benchmark</th><th>Fugu</th><th>Fugu Ultra</th><th>Opus 4.8</th><th>Gemini 3.1 Pro</th><th>GPT 5.5</th></tr></thead><tbody><tr><td>SWE Bench Pro*</td><td>59.0</td><td><strong>73.7</strong></td><td>69.2</td><td>54.2</td><td>58.6</td></tr><tr><td>TerminalBench 2.1</td><td>80.2</td><td><strong>82.1</strong></td><td>74.6</td><td>70.3</td><td>78.2</td></tr><tr><td>LiveCodeBench</td><td>92.9</td><td><strong>93.2</strong></td><td>87.8</td><td>88.5</td><td>85.3</td></tr><tr><td>LiveCodeBench Pro</td><td>87.8</td><td><strong>90.8</strong></td><td>84.8</td><td>82.9</td><td>88.4</td></tr><tr><td>Humanity&#8217;s Last Exam</td><td>47.2</td><td><strong>50.0</strong></td><td>49.8</td><td>44.4</td><td>41.4</td></tr><tr><td>CharXiv Reasoning</td><td>85.1</td><td><strong>86.6</strong></td><td>84.2</td><td>83.3</td><td>84.1</td></tr><tr><td>GPQA-D</td><td><strong>95.5</strong></td><td><strong>95.5</strong></td><td>92.0</td><td>94.3</td><td>93.6</td></tr><tr><td>SciCode</td><td><strong>60.1</strong></td><td>58.7</td><td>53.5</td><td>58.9</td><td>56.1</td></tr><tr><td>τ³ Banking</td><td><strong>21.7</strong></td><td>20.6</td><td>20.6</td><td>8.4</td><td>20.6</td></tr><tr><td>Long Context Reasoning</td><td><strong>74.7</strong></td><td>73.3</td><td>67.7</td><td>72.7</td><td>74.3</td></tr><tr><td>MRCRv2</td><td>86.6</td><td>93.6</td><td>87.9</td><td>84.9</td><td><strong>94.8</strong></td></tr></tbody></table></figure>



<p class="wp-block-paragraph">The orchestrator posts the top score on 10 of 11 rows. Fugu Ultra tops the four coding benchmarks, CharXiv Reasoning, and Humanity&#8217;s Last Exam. It ties regular Fugu on GPQA-D. Regular Fugu leads SciCode, τ³ Banking, and Long Context Reasoning. GPT 5.5 wins MRCRv2, the only baseline win here.</p>



<p class="wp-block-paragraph">Its Fugu models stand shoulder-to-shoulder with Anthropic&#8217;s Fable 5 and Mythos Preview. Those two are not in Fugu&#8217;s pool, since they are not publicly accessible.</p>



<h2 id="h-use-cases" class="wp-block-heading"><strong>Use Cases</strong></h2>



<p class="wp-block-paragraph">Sakana AI ran a beta with close to 500 early users. The published examples favor long, multi-step tasks.</p>



<ul class="wp-block-list">
<li><strong>AutoResearch</strong>: An agent improved a small GPT&#8217;s training recipe autonomously. It ran 123 experiments over roughly 14 hours on one H100 GPU. Fugu Ultra reached the best mean validation BPB of 0.9774, with a best single run of 0.9748.</li>



<li><strong>Rubik&#8217;s Cube solver</strong>: Each model wrote a pure-Python solver, no libraries allowed. Fugu Ultra solved all 300 held-out cubes, averaging 19.72 moves. One baseline matched it closely at 19.76 moves. Two others crashed and solved none.</li>



<li><strong>Classical Japanese kana reading order</strong>: On a 1610 letter, Fugu Ultra scored NED 0.80. The nearest baseline reached only 0.24.</li>



<li><strong>Blindfold chess</strong>: Fugu played four games from memory, with no board shown. It beat three frontier models and a 2100-Elo Stockfish engine.</li>



<li><strong>Online trading</strong>: On one 50-week window, Fugu Ultra returned +19.43% on average across five runs. The other frontier models stayed below +15%. Sakana AI notes past performance does not guarantee future results.</li>
</ul>



<h2 id="h-a-minimal-api-example" class="wp-block-heading"><strong>A Minimal API Example</strong></h2>



<p class="wp-block-paragraph">Fugu uses an OpenAI-compatible API, so no SDK migration is required. Point an existing client at your console-provided endpoint.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">from openai import OpenAI

# Endpoint and key come from your Sakana console (console.sakana.ai).
client = OpenAI(
    base_url="https://&lt;your-fugu-endpoint>/v1",  # from console.sakana.ai
    api_key="YOUR_SAKANA_API_KEY",
)

resp = client.chat.completions.create(
    model="fugu-ultra-20260615",           # or "fugu"
    messages=[
        {"role": "user",
         "content": "Reproduce the method in this paper and report the gap."},
    ],
)

print(resp.choices[0].message.content)</code></pre></div></div>



<p class="wp-block-paragraph">Token usage and cost are reported per request. So you can monitor spend in real time.</p>



<h2 id="h-community-reactions" class="wp-block-heading"><strong>Community Reactions</strong></h2>



<div id="fugu-sent-root">
  <style>
    #fugu-sent-root *{box-sizing:border-box;margin:0;padding:0}
    #fugu-sent-root{
      --bg:#fff;--ink:#0a0a0a;--mut:#6b6b6b;--line:#dcdcdc;--soft:#f5f5f5;--soft2:#ebebeb;
      font-family:"IBM Plex Mono",ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;
      background:var(--bg);color:var(--ink);border:1px solid var(--ink);
      max-width:920px;margin:0 auto;-webkit-font-smoothing:antialiased;line-height:1.5;
    }
    #fugu-sent-root .hd{border-bottom:1px solid var(--ink);padding:18px 20px;display:flex;justify-content:space-between;align-items:flex-start;gap:12px;flex-wrap:wrap}
    #fugu-sent-root .hd h2{font-size:17px;letter-spacing:.03em;font-weight:700}
    #fugu-sent-root .hd p{font-size:11.5px;color:var(--mut);margin-top:6px;max-width:560px}
    #fugu-sent-root .tag{font-size:10px;letter-spacing:.12em;text-transform:uppercase;border:1px solid var(--ink);padding:4px 8px;white-space:nowrap}
    #fugu-sent-root .panel{padding:18px 20px;border-bottom:1px solid var(--line)}
    #fugu-sent-root .lbl{font-size:10px;letter-spacing:.16em;text-transform:uppercase;color:var(--mut);margin-bottom:10px;display:block}
    /* overview bar */
    #fugu-sent-root .obar{display:flex;height:32px;border:1px solid var(--ink);overflow:hidden}
    #fugu-sent-root .seg{display:flex;align-items:center;justify-content:center;white-space:nowrap;border-right:1px solid var(--ink)}
    #fugu-sent-root .seg:last-child{border-right:0}
    #fugu-sent-root .seg.sup{background:#0a0a0a}
    #fugu-sent-root .seg.ske{background:repeating-linear-gradient(45deg,#0a0a0a,#0a0a0a 1px,#fff 1px,#fff 6px)}
    #fugu-sent-root .seg.cri{background:#fff}
    #fugu-sent-root .seg .t{font-size:10.5px;font-weight:700;background:#fff;color:#0a0a0a;border:1px solid #0a0a0a;padding:1px 7px;line-height:1.4}
    #fugu-sent-root .legend{display:flex;gap:18px;flex-wrap:wrap;margin-top:12px;font-size:11px;color:var(--mut)}
    #fugu-sent-root .legend span{display:inline-flex;align-items:center;gap:7px}
    #fugu-sent-root .sw{width:14px;height:14px;border:1px solid var(--ink);display:inline-block}
    #fugu-sent-root .sw.sup{background:#0a0a0a}
    #fugu-sent-root .sw.ske{background:repeating-linear-gradient(45deg,#0a0a0a,#0a0a0a 1px,#fff 1px,#fff 6px)}
    #fugu-sent-root .sw.cri{background:#fff}
    #fugu-sent-root .summary{font-size:12.5px;margin-top:14px;border-left:3px solid var(--ink);padding-left:12px}
    /* filters */
    #fugu-sent-root .filters{display:flex;gap:0;flex-wrap:wrap;border:1px solid var(--ink);width:max-content;max-width:100%}
    #fugu-sent-root .filters button{font-family:inherit;font-size:12px;background:var(--bg);color:var(--ink);border:0;padding:9px 15px;cursor:pointer;letter-spacing:.02em;border-right:1px solid var(--ink)}
    #fugu-sent-root .filters button:last-child{border-right:0}
    #fugu-sent-root .filters button.on{background:#0a0a0a;color:#fff}
    /* cards */
    #fugu-sent-root .cards{padding:8px 20px 18px;display:grid;grid-template-columns:1fr 1fr;gap:12px}
    #fugu-sent-root .card{border:1px solid var(--ink);padding:13px 14px;display:flex;flex-direction:column;gap:9px;background:var(--bg)}
    #fugu-sent-root .card .top{display:flex;justify-content:space-between;align-items:center;gap:8px}
    #fugu-sent-root .who{display:flex;align-items:center;gap:8px;min-width:0}
    #fugu-sent-root .plat{font-size:9px;letter-spacing:.08em;border:1px solid var(--ink);padding:2px 5px;font-weight:700;flex:none}
    #fugu-sent-root .plat.x{background:#0a0a0a;color:#fff}
    #fugu-sent-root .handle{font-size:12px;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
    #fugu-sent-root .chip{font-size:9px;letter-spacing:.08em;text-transform:uppercase;border:1px solid var(--ink);padding:2px 7px;flex:none}
    #fugu-sent-root .chip.sup{background:#0a0a0a;color:#fff}
    #fugu-sent-root .chip.ske{background:var(--soft2)}
    #fugu-sent-root .chip.cri{background:#fff;border-style:dashed}
    #fugu-sent-root .card .body{font-size:12.5px;line-height:1.5}
    #fugu-sent-root .card .q{font-style:italic}
    #fugu-sent-root .card .foot{display:flex;justify-content:space-between;align-items:center;gap:8px;margin-top:auto;padding-top:4px;border-top:1px dotted var(--line)}
    #fugu-sent-root .theme{font-size:10px;color:var(--mut);letter-spacing:.04em}
    #fugu-sent-root a.src{font-size:11px;color:var(--ink);text-decoration:none;border-bottom:1px solid var(--ink);white-space:nowrap;font-weight:700}
    #fugu-sent-root a.src:hover{background:#0a0a0a;color:#fff;border-bottom-color:#0a0a0a;padding:0 3px}
    #fugu-sent-root .affil{font-size:9px;color:var(--mut)}
    /* press row */
    #fugu-sent-root .press{display:flex;gap:10px;flex-wrap:wrap}
    #fugu-sent-root .press a{font-size:11.5px;color:var(--ink);text-decoration:none;border:1px solid var(--ink);padding:7px 11px;display:inline-flex;gap:6px;align-items:center}
    #fugu-sent-root .press a:hover{background:#0a0a0a;color:#fff}
    #fugu-sent-root .note{font-size:10px;color:var(--mut);line-height:1.6;padding:0 20px 16px}
    #fugu-sent-root .ft{padding:12px 20px;border-top:1px solid var(--ink);display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap;font-size:10.5px;color:var(--mut)}
    #fugu-sent-root .ft b{color:var(--ink);letter-spacing:.04em}
    @media(max-width:640px){
      #fugu-sent-root .cards{grid-template-columns:1fr}
      #fugu-sent-root .hd h2{font-size:15px}
      #fugu-sent-root .seg{font-size:10px}
    }
  </style>

  <div class="hd">
    <div>
      <h2>Sakana Fugu &mdash; Early Community Sentiment</h2>
      <p>A manual review of public reaction on X and Hacker News, with links to every source. Captured June 22, 2026.</p>
    </div>
    <span class="tag">12 posts reviewed</span>
  </div>

  <div class="panel">
    <span class="lbl">Sentiment split (n = 12)</span>
    <div class="obar">
      <div class="seg sup" style="flex:3"><span class="t">Supportive 3</span></div>
      <div class="seg ske" style="flex:6"><span class="t">Skeptical 6</span></div>
      <div class="seg cri" style="flex:3"><span class="t">Critical 3</span></div>
    </div>
    <div class="legend">
      <span><i class="sw sup"></i>Supportive</span>
      <span><i class="sw ske"></i>Skeptical</span>
      <span><i class="sw cri"></i>Critical</span>
    </div>
    <div class="summary">
      Early reaction skews skeptical. The &ldquo;is this just a router or wrapper?&rdquo; question dominates. The clearest supportive voices are Sakana&#8209;affiliated.
    </div>
  </div>

  <div class="panel">
    <div class="filters" id="filters">
      <button data-f="all" class="on">All</button>
      <button data-f="sup">Supportive</button>
      <button data-f="ske">Skeptical</button>
      <button data-f="cri">Critical</button>
    </div>
  </div>

  <div class="cards" id="cards"></div>

  <div class="panel">
    <span class="lbl">Press &amp; analysis</span>
    <div class="press">
      <a href="https://news.ycombinator.com/item?id=48624782" target="_blank" rel="noopener noreferrer">Hacker News thread &middot; 50 pts &nearr;</a>
      <a href="https://venturebeat.com/orchestration/no-claude-fable-5-no-problem-sakana-achieves-frontier-performance-with-new-fugu-multi-model-auto-synthesis-system" target="_blank" rel="noopener noreferrer">VentureBeat report &nearr;</a>
      <a href="https://clankercloud.ai/blog/sakana-fugu-release-model-orchestration-clanker-cloud" target="_blank" rel="noopener noreferrer">Clanker Cloud analysis &nearr;</a>
    </div>
  </div>

  <div class="note">
    Method: sentiment was assigned by hand from a small sample of public posts on June 22, 2026. This is not a statistical survey, and the split can shift as more reactions arrive. Two of the three supportive posts are from Sakana AI or its CEO. Quotes are shortened; follow each link for full context. The Reddit quote is as reported by VentureBeat.
  </div>

  <div class="ft">
    <span><b>Marktechpost</b> &middot; Sakana Fugu sentiment tracker</span>
    <span>Sources: X &middot; Hacker News &middot; VentureBeat</span>
  </div>
</div>

<script>
(function(){
  var root = document.getElementById('fugu-sent-root');
  var DATA = [
    // SUPPORTIVE
    {s:'sup', plat:'X', handle:'@SakanaAILabs', affil:'Sakana AI (official)',
     body:'Launch announcement. Positions Fugu Ultra to match Fable and Mythos, without export-control risk.',
     theme:'Announcement', url:'https://x.com/SakanaAILabs/status/2068861630327443966'},
    {s:'sup', plat:'X', handle:'@hardmaru', affil:'David Ha, Sakana CEO',
     body:'<span class="q">&ldquo;Orchestration Models are the next frontier, beyond bigger models.&rdquo;</span> Frames it as a hedge against single-vendor risk.',
     theme:'Vision', url:'https://x.com/hardmaru/status/2068884466056225025'},
    {s:'sup', plat:'Blog', handle:'Clanker Cloud', affil:'Independent analysis',
     body:'Calls Fugu a productized orchestration layer and a healthy debate &mdash; but wants real observability into which agents ran.',
     theme:'Analysis', url:'https://clankercloud.ai/blog/sakana-fugu-release-model-orchestration-clanker-cloud'},
    // SKEPTICAL
    {s:'ske', plat:'HN', handle:'ed_mercer', affil:'Hacker News',
     body:'<span class="q">&ldquo;So basically... openrouter?&rdquo;</span>',
     theme:'Router framing', url:'https://news.ycombinator.com/item?id=48625104'},
    {s:'ske', plat:'HN', handle:'embedding-shape', affil:'Hacker News',
     body:'Asks how one API is not just swapping one single-vendor dependency for another.',
     theme:'Sovereignty', url:'https://news.ycombinator.com/item?id=48625312'},
    {s:'ske', plat:'HN', handle:'bprasanna', affil:'Hacker News',
     body:'<span class="q">&ldquo;Isn&rsquo;t this what perplexity is?&rdquo;</span>',
     theme:'Router framing', url:'https://news.ycombinator.com/item?id=48625401'},
    {s:'ske', plat:'HN', handle:'stygiansonic', affil:'Hacker News',
     body:'Reads it as a coordinator, not just fusion &mdash; an agent-of-agents, with token usage rising accordingly.',
     theme:'Architecture', url:'https://news.ycombinator.com/item?id=48625273'},
    {s:'ske', plat:'HN', handle:'alasano', affil:'Hacker News',
     body:'Sees Fugu Ultra building a dynamic multi-model mini-plan, more than OpenRouter Fusion.',
     theme:'Architecture', url:'https://news.ycombinator.com/item?id=48625361'},
    {s:'ske', plat:'Reddit', handle:'GreedyWorking1499', affil:'Reddit (via VentureBeat)',
     body:'<span class="q">&ldquo;A highly advanced router/wrapper&rdquo;</span> &mdash; not a fundamental leap like Mythos or Fable, until proven otherwise.',
     theme:'Router framing', url:'https://venturebeat.com/orchestration/no-claude-fable-5-no-problem-sakana-achieves-frontier-performance-with-new-fugu-multi-model-auto-synthesis-system'},
    // CRITICAL
    {s:'cri', plat:'X', handle:'@eliebakouch', affil:'Prime Intellect',
     body:'<span class="q">&ldquo;This is not &lsquo;AI sovereignty&rsquo;.&rdquo;</span> Calls regular Fugu a router and flags opaque &ldquo;Model A/B/C&rdquo; baselines.',
     theme:'Sovereignty / transparency', url:'https://x.com/eliebakouch/status/2068939729811468503'},
    {s:'cri', plat:'X', handle:'@teortaxesTex', affil:'Independent',
     body:'Withholds excitement pending cost analysis. An orchestrator spending many frontier tokens may not beat best-of-n.',
     theme:'Cost', url:'https://x.com/teortaxesTex/status/2068986775796687229'},
    {s:'cri', plat:'HN', handle:'adamnemecek', affil:'Hacker News',
     body:'<span class="q">&ldquo;Seems kinda underwhelming considering they raised like $400M.&rdquo;</span>',
     theme:'Expectations', url:'https://news.ycombinator.com/item?id=48625429'}
  ];

  var labelS = {sup:'Supportive', ske:'Skeptical', cri:'Critical'};

  function render(filter){
    var box = root.querySelector('#cards');
    box.innerHTML = '';
    DATA.filter(function(d){ return filter==='all' || d.s===filter; }).forEach(function(d){
      var platCls = d.plat==='X' ? 'plat x' : 'plat';
      var card = document.createElement('div');
      card.className = 'card';
      card.innerHTML =
        '<div class="top">'+
          '<div class="who"><span class="'+platCls+'">'+d.plat+'</span>'+
          '<span class="handle">'+d.handle+'</span></div>'+
          '<span class="chip '+d.s+'">'+labelS[d.s]+'</span>'+
        '</div>'+
        '<div class="affil">'+d.affil+'</div>'+
        '<div class="body">'+d.body+'</div>'+
        '<div class="foot"><span class="theme">'+d.theme+'</span>'+
        '<a class="src" href="'+d.url+'" target="_blank" rel="noopener noreferrer">View source &nearr;</a></div>';
      box.appendChild(card);
    });
    sz();
  }

  root.querySelectorAll('#filters button').forEach(function(b){
    b.addEventListener('click', function(){
      root.querySelectorAll('#filters button').forEach(function(x){x.classList.remove('on');});
      b.classList.add('on');
      render(b.getAttribute('data-f'));
    });
  });

  render('all');

  // auto-resize for WordPress iframe embed
  function sz(){
    var h = root.offsetHeight + 40;
    if(window.parent && window.parent!==window){
      window.parent.postMessage({type:'fugu-sent-height', height:h}, '*');
    }
  }
  window.addEventListener('load', sz);
  window.addEventListener('resize', sz);
  setTimeout(sz, 300);
  if(window.MutationObserver){
    new MutationObserver(sz).observe(root, {childList:true, subtree:true, attributes:true});
  }
})();
</script>




<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph">Check out the <strong><a href="https://sakana.ai/fugu/" target="_blank" rel="noreferrer noopener">Product page</a></strong> and <strong><a href="https://sakana.ai/fugu-release/" target="_blank" rel="noreferrer noopener">Technical details</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/22/sakana-ai-launches-sakana-fugu-an-orchestration-model-that-routes-tasks-across-a-swappable-pool-of-frontier-llms/">Sakana AI Launches Sakana Fugu: An Orchestration Model That Routes Tasks Across a Swappable Pool of Frontier LLMs</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80652</post-id>
	</item>
		<item>
		<title>MoonMath AI Open-Sources a HIP Attention Kernel for AMD MI300X That Beats AITER v3 on Every Shape and Rounding Mode</title>
		<link>https://www.marktechpost.com/2026/06/22/moonmath-ai-open-sources-a-hip-attention-kernel-for-amd-mi300x-that-beats-aiter-v3-on-every-shape-and-rounding-mode/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Mon, 22 Jun 2026 07:13:49 +0000</pubDate>
				<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80648</guid>

					<description><![CDATA[<p>The HIP kernel uses one-instruction asm wrappers and an eight-wave pipeline to outperform AMD's AITER v3 on MI300X.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/22/moonmath-ai-open-sources-a-hip-attention-kernel-for-amd-mi300x-that-beats-aiter-v3-on-every-shape-and-rounding-mode/">MoonMath AI Open-Sources a HIP Attention Kernel for AMD MI300X That Beats AITER v3 on Every Shape and Rounding Mode</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">MoonMath AI team has released a bf16 forward attention kernel for AMD&#8217;s MI300X GPU. It is written in HIP, not hand-written assembly. The code is open-source under the MIT license. The MoonMath.ai team reports it beats AITER v3, AMD&#8217;s own optimized kernel, on every tested shape. Bare-metal access came from HotAisle, an AMD cloud provider.</p>



<p class="wp-block-paragraph">Attention is the fused <code>softmax(QKᵀ/√d)·V</code> operation inside every transformer. The MI300X is AMD&#8217;s CDNA3 data-center GPU, with the ISA target (gfx942). This kernel runs on that hardware only.</p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li>MoonMath.ai open-sources a bf16 forward attention kernel for AMD MI300X, written in HIP, not assembly (MIT).</li>



<li>It beats AMD&#8217;s AITER v3 on every shape and rounding mode — geomean 1.18×/1.15×/1.08×, up to 1.26×.</li>



<li>The core trick: one-instruction asm wrappers let you pick the opcode while the compiler allocates registers.</li>



<li>Most of the speedup is memory placement — K in LDS, V hot in L1, Q and accumulators in registers.</li>



<li>A real SGLang PR used it to speed up Wan2.1 video diffusion by 1.23×, with no quality regression.</li>
</ul>



<h2 id="h-understanding-kernel" class="wp-block-heading"><strong>Understanding Kernel</strong></h2>



<p class="wp-block-paragraph">A kernel is a small program that runs directly on the GPU&#8217;s many cores to perform one specific computation—here, the attention math—as fast as the hardware allows. The kernel computes forward attention in bf16 on MI300X only. It takes inputs in either BSHD or BHSD layout, with no transpose. Head dimension is fixed at 128. It supports any sequence length, including cross-attention.</p>



<p class="wp-block-paragraph">There are real limits. There is no causal mask, no GQA, and no varlen batching. Outputs are bf16, and it runs on gfx942 hardware exclusively.</p>



<p class="wp-block-paragraph">Numerics are tightly controlled. All three rounding modes match AITER&#8217;s per-mode rounding rule. Every finite output sits within 1 bf16 ULP of AITER. NaN and Inf handling is bit-identical, and results are deterministic.</p>



<h2 id="h-the-core-trick-one-instruction-asm-wrappers" class="wp-block-heading"><strong>The Core Trick: One-Instruction asm Wrappers</strong></h2>



<p class="wp-block-paragraph">The core technique avoids a familiar dilemma. Compiler intrinsics keep code tidy but let the compiler reorder or rename operands. Raw inline assembly gives control but forces manual register and address management.</p>



<p class="wp-block-paragraph">MoonMath wraps exactly one instruction in a <code>__device__ __forceinline__</code> function. Extended asm constraints describe the operands. The research team picks the opcode. The compiler still allocates registers and tracks data flow.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">// in/out tied to the SAME VGPR → no accumulator rename, no v_mov copy.
__device__ __forceinline__ void asm_mfma(bf16x4_t a, bf16x4_t b, fp32x4_t&amp; c) {
    asm volatile("v_mfma_f32_16x16x16_bf16 %0, %1, %2, %0"
                 : "+v"(c) : "v"(a), "v"(b));
}</code></pre></div></div>



<p class="wp-block-paragraph">The <code>"+v"(c)</code> constraint ties the accumulator input and output to the same VGPR. No copy instruction is emitted. This keeps the kernel close to ordinary HIP. It still steers the machine one instruction at a time.</p>



<h2 id="h-the-architecture-eight-waves-two-groups-two-barriers" class="wp-block-heading"><strong>The Architecture: Eight Waves, Two Groups, Two Barriers</strong></h2>



<p class="wp-block-paragraph">A CDNA3 compute unit has four SIMD units. The textbook block is four waves. MoonMath instead runs eight waves per block, in two groups of four.</p>



<p class="wp-block-paragraph">The two groups run the same <code>Q*K</code>, softmax, <code>O += P*V</code> sequence. They are offset by a phase. While one group saturates the matrix core, the other runs softmax and issues loads. Then they swap, so the matrix core never idles.</p>



<p class="wp-block-paragraph">There are two <code>s_barrier</code>s per iteration. One sits at the phase handoff. One sits at the iteration boundary. Per-counter waits handle the rest of the synchronization.</p>



<p class="wp-block-paragraph">This echoes FlashAttention-3&#8217;s matmul and softmax alternation. It does not copy FA3&#8217;s producer and consumer warp split. On CDNA3, every memory move is already asynchronous, so a dedicated producer wave is unnecessary.</p>



<h2 id="h-where-data-lives-and-why-16-16-16" class="wp-block-heading"><strong>Where Data Lives, and Why 16×16×16</strong></h2>



<p class="wp-block-paragraph">Most of the speedup comes from memory placement. <code>K</code> streams from HBM into LDS, double-buffered, shared by all eight waves. <code>V</code> stays hot in L1, read on every PV matmul. <code>Q</code> and accumulators live in registers.</p>



<p class="wp-block-paragraph">The research team picked the 16×16×16 MFMA over 32×32×8. Both shapes have identical throughput. The smaller tile accumulates into 4 fp32 elements per lane, against 16. Lower accumulator pressure leaves room for deeper prefetch and a third <code>Q</code> tile.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Decision</th><th>Choice</th><th>Reason</th></tr></thead><tbody><tr><td>Waves per block</td><td>8 (two groups of 4)</td><td>Plan the pipeline directly; share one K copy</td></tr><tr><td>MFMA shape</td><td>16×16×16 bf16</td><td>Same throughput, lower VGPR pressure, better power efficiency</td></tr><tr><td>K placement</td><td>LDS, double-buffered, 32 KiB</td><td>Shared by all 8 waves, swapped per iteration</td></tr><tr><td>V placement</td><td>L1, resident, prefetched</td><td>Reread across PV, kept hot deliberately</td></tr><tr><td>Q + accumulators</td><td>VGPRs</td><td>Read every iteration, never reloaded</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">Two later wins close the gap. A third <code>Q</code> tile (3Q) raises data reuse per loaded <code>K</code> and <code>V</code> tile. A Flash-Decoding-style tail KV split rescues the stranded fractional round across MI300X&#8217;s 304 CUs. These wins cascade. Moving <code>V</code> to L1 freed the LDS that the third <code>Q</code> tile then fills.</p>



<h2 id="h-benchmark" class="wp-block-heading"><strong>Benchmark</strong></h2>



<p class="wp-block-paragraph">Tests ran on MI300X in bf16, head dimension 128. Each shape was measured at three rounding modes. RTNE rounds to nearest even. RTNA rounds to nearest, ties away from zero. RTZ truncates toward zero.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Shape (B, H, S, D)</th><th>Round</th><th>Ours (ms)</th><th>AITER v3 (ms)</th><th>vs AITER</th><th>vs MAX</th></tr></thead><tbody><tr><td>(2, 24, 8192, 128)</td><td>RTNE</td><td>3.083</td><td>3.792</td><td>1.23×</td><td>1.37×</td></tr><tr><td>(2, 24, 16384, 128)</td><td>RTNE</td><td>11.670</td><td>14.691</td><td>1.26×</td><td>1.54×</td></tr><tr><td>(4, 16, 16384, 128)</td><td>RTZ</td><td>15.055</td><td>16.183</td><td>1.07×</td><td>1.47×</td></tr><tr><td>(2, 24, 32768, 128)</td><td>RTNA</td><td>44.440</td><td>52.363</td><td>1.18×</td><td>1.57×</td></tr><tr><td>(1, 16, 131072, 128)</td><td>RTNE</td><td>232.517</td><td>269.278</td><td>1.16×</td><td>1.46×</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">Geomeans across the sweep favor MoonMath. Versus AITER, it scores 1.18× (RTNE), 1.15× (RTNA), and 1.08× (RTZ). Versus Modular MAX, geomeans run 1.44× to 1.49×, and per-shape speedups reach 1.59×.</p>



<p class="wp-block-paragraph">RTZ is AITER&#8217;s own fastest mode and the tightest race. The (4, 16, 16384) RTZ shape moved from 0.95× to 1.07×. The tail KV split is what closed that final gap.</p>



<p class="wp-block-paragraph"></p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- MoonMath CDNA3 Attention — interactive demo (Marktechpost). Paste into a WordPress Custom HTML block. -->
<iframe id="mm-cdna3-frame" title="MoonMath CDNA3 attention — interactive demo" loading="lazy" height="600" style="width:100%;height:600px;border:0;display:block;overflow:hidden" srcdoc="&lt;!DOCTYPE html&gt;&lt;html lang=&quot;en&quot;&gt;&lt;head&gt;&lt;meta charset=&quot;utf-8&quot;&gt;&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width,initial-scale=1&quot;&gt;&lt;style&gt;html,body{margin:0;padding:0;background:#fff}&lt;/style&gt;&lt;/head&gt;&lt;body&gt;&lt;div id=&quot;mm-cdna3-demo&quot;&gt;
&lt;style&gt;
#mm-cdna3-demo *{box-sizing:border-box;margin:0;padding:0}
#mm-cdna3-demo{
  --ink:#000;--paper:#fff;--g1:#f2f2f2;--g2:#e2e2e2;--g3:#c7c7c7;--g4:#8f8f8f;--g5:#5a5a5a;
  font-family:'IBM Plex Mono',ui-monospace,'SF Mono',Menlo,Consolas,monospace;
  color:var(--ink);background:var(--paper);
  border:2px solid var(--ink);max-width:920px;margin:0 auto;
  -webkit-font-smoothing:antialiased;line-height:1.5;
}
#mm-cdna3-demo .hd{border-bottom:2px solid var(--ink);padding:22px 24px 18px}
#mm-cdna3-demo .eyebrow{font-size:11px;letter-spacing:.22em;text-transform:uppercase;color:var(--g5)}
#mm-cdna3-demo h1{font-size:23px;line-height:1.18;font-weight:700;margin:8px 0 6px;letter-spacing:-.01em}
#mm-cdna3-demo .sub{font-size:13px;color:var(--g5);max-width:640px}
#mm-cdna3-demo .tabs{display:flex;border-bottom:2px solid var(--ink);flex-wrap:wrap}
#mm-cdna3-demo .tab{flex:1;min-width:120px;padding:13px 10px;background:var(--paper);border:0;border-right:1px solid var(--g3);
  font-family:inherit;font-size:12px;letter-spacing:.12em;text-transform:uppercase;cursor:pointer;color:var(--g5);font-weight:600}
#mm-cdna3-demo .tab:last-child{border-right:0}
#mm-cdna3-demo .tab[aria-selected=&quot;true&quot;]{background:var(--ink);color:var(--paper)}
#mm-cdna3-demo .tab:focus-visible{outline:3px solid var(--ink);outline-offset:-3px}
#mm-cdna3-demo .panel{padding:24px;display:none}
#mm-cdna3-demo .panel.on{display:block}
#mm-cdna3-demo .panel-lead{font-size:13px;color:var(--g5);margin-bottom:18px;max-width:680px}

/* controls */
#mm-cdna3-demo .ctl{display:flex;gap:18px;flex-wrap:wrap;align-items:flex-end;margin-bottom:22px}
#mm-cdna3-demo .field label{display:block;font-size:10px;letter-spacing:.16em;text-transform:uppercase;color:var(--g5);margin-bottom:6px}
#mm-cdna3-demo select{font-family:inherit;font-size:13px;padding:9px 12px;border:2px solid var(--ink);background:var(--paper);color:var(--ink);min-width:210px;cursor:pointer}
#mm-cdna3-demo .seg{display:inline-flex;border:2px solid var(--ink)}
#mm-cdna3-demo .seg button{font-family:inherit;font-size:12px;font-weight:600;padding:9px 15px;background:var(--paper);border:0;border-right:2px solid var(--ink);cursor:pointer;color:var(--ink);letter-spacing:.04em}
#mm-cdna3-demo .seg button:last-child{border-right:0}
#mm-cdna3-demo .seg button[aria-pressed=&quot;true&quot;]{background:var(--ink);color:var(--paper)}
#mm-cdna3-demo .seg button:focus-visible{outline:3px solid var(--ink);outline-offset:2px}

/* bar chart */
#mm-cdna3-demo .chart{border:1px solid var(--g3);padding:20px 20px 8px}
#mm-cdna3-demo .row{display:grid;grid-template-columns:108px 1fr 86px;align-items:center;gap:14px;margin-bottom:18px}
#mm-cdna3-demo .row .name{font-size:12px;font-weight:600}
#mm-cdna3-demo .row .name small{display:block;font-weight:400;color:var(--g4);font-size:10px;letter-spacing:.04em}
#mm-cdna3-demo .track{height:30px;background:var(--g1);position:relative;border:1px solid var(--g3)}
#mm-cdna3-demo .fill{height:100%;transition:width .55s cubic-bezier(.22,1,.36,1);position:relative}
#mm-cdna3-demo .fill.ours{background:var(--ink)}
#mm-cdna3-demo .fill.aiter{background:repeating-linear-gradient(45deg,var(--g4) 0 6px,var(--g3) 6px 12px)}
#mm-cdna3-demo .fill.max{background:repeating-linear-gradient(45deg,var(--g3) 0 6px,var(--g1) 6px 12px)}
#mm-cdna3-demo .ms{position:absolute;right:8px;top:50%;transform:translateY(-50%);font-size:11px;font-weight:700;color:var(--paper);mix-blend-mode:difference}
#mm-cdna3-demo .row .spd{font-size:13px;font-weight:700;text-align:right}
#mm-cdna3-demo .row .spd small{display:block;font-weight:400;color:var(--g4);font-size:9px;letter-spacing:.1em;text-transform:uppercase}
#mm-cdna3-demo .legend{display:flex;gap:20px;flex-wrap:wrap;font-size:11px;color:var(--g5);margin-top:6px}
#mm-cdna3-demo .legend i{display:inline-block;width:22px;height:11px;margin-right:7px;vertical-align:-1px;border:1px solid var(--g3)}
#mm-cdna3-demo .li-ours{background:var(--ink)}
#mm-cdna3-demo .li-aiter{background:repeating-linear-gradient(45deg,var(--g4) 0 5px,var(--g3) 5px 10px)}
#mm-cdna3-demo .li-max{background:repeating-linear-gradient(45deg,var(--g3) 0 5px,var(--g1) 5px 10px)}
#mm-cdna3-demo .note{font-size:11px;color:var(--g5);margin-top:16px;border-top:1px solid var(--g2);padding-top:12px}

/* pipeline */
#mm-cdna3-demo .pipewrap{border:1px solid var(--g3);padding:18px}
#mm-cdna3-demo .grouprow{display:grid;grid-template-columns:74px 1fr;align-items:center;gap:12px;margin-bottom:12px}
#mm-cdna3-demo .glabel{font-size:11px;font-weight:700;letter-spacing:.08em}
#mm-cdna3-demo .glabel small{display:block;font-weight:400;color:var(--g4);font-size:9px}
#mm-cdna3-demo .lane{display:flex;gap:4px}
#mm-cdna3-demo .slot{flex:1;height:46px;border:1px solid var(--g3);display:flex;flex-direction:column;align-items:center;justify-content:center;font-size:10px;text-align:center;line-height:1.2;background:var(--paper);transition:background .15s,color .15s;position:relative}
#mm-cdna3-demo .slot b{font-size:11px;letter-spacing:.02em}
#mm-cdna3-demo .slot.mc{background:var(--ink);color:var(--paper)}
#mm-cdna3-demo .slot.mem{background:var(--g2)}
#mm-cdna3-demo .slot.active{box-shadow:inset 0 0 0 3px var(--ink)}
#mm-cdna3-demo .slot.dim,#mm-cdna3-demo .pipe .seg2.dim{opacity:.32}
#mm-cdna3-demo .slot.role{font-size:9px}
#mm-cdna3-demo .slot.role .tag{font-size:8px;letter-spacing:.14em;text-transform:uppercase;color:var(--g4);margin-top:3px}
#mm-cdna3-demo .slot.mc.role .tag{color:var(--g3)}
#mm-cdna3-demo .barbar{display:grid;grid-template-columns:74px 1fr;gap:12px;margin:14px 0 4px}
#mm-cdna3-demo .barbar .tl{display:flex;gap:4px}
#mm-cdna3-demo .barbar .ph{flex:1;text-align:center;font-size:10px;color:var(--g5);letter-spacing:.1em;text-transform:uppercase;padding:4px 0;border:1px dashed var(--g3)}
#mm-cdna3-demo .barbar .ph.cur{border:1px solid var(--ink);color:var(--ink);font-weight:700}
#mm-cdna3-demo .barrier{font-size:10px;color:var(--g5);text-align:center;margin-top:4px;letter-spacing:.04em}
#mm-cdna3-demo .transport{display:grid;grid-template-columns:74px 1fr;gap:12px;margin-top:10px}
#mm-cdna3-demo .pipe{display:flex;gap:4px}
#mm-cdna3-demo .pipe .seg2{flex:1;height:22px;border:1px solid var(--g3);font-size:9px;display:flex;align-items:center;justify-content:center;color:var(--g5);background:var(--g1)}
#mm-cdna3-demo .pipe .seg2.flow{background:var(--ink);color:var(--paper)}
#mm-cdna3-demo .pctl{display:flex;gap:10px;align-items:center;margin-top:18px;flex-wrap:wrap}
#mm-cdna3-demo .btn{font-family:inherit;font-size:12px;font-weight:600;padding:9px 16px;background:var(--paper);border:2px solid var(--ink);color:var(--ink);cursor:pointer;letter-spacing:.06em}
#mm-cdna3-demo .btn.primary{background:var(--ink);color:var(--paper)}
#mm-cdna3-demo .btn:focus-visible{outline:3px solid var(--ink);outline-offset:2px}
#mm-cdna3-demo .speed{font-size:11px;color:var(--g5);display:flex;align-items:center;gap:8px;margin-left:auto}
#mm-cdna3-demo .speed input{accent-color:#000}
#mm-cdna3-demo .iter{font-size:11px;color:var(--g5);letter-spacing:.08em}

/* memory map */
#mm-cdna3-demo .mem-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:0;border:1px solid var(--g3)}
#mm-cdna3-demo .cell{border-right:1px solid var(--g3);padding:0}
#mm-cdna3-demo .cell:last-child{border-right:0}
#mm-cdna3-demo .cell h4{font-size:11px;letter-spacing:.1em;text-transform:uppercase;padding:11px 12px;border-bottom:1px solid var(--g3);background:var(--g1)}
#mm-cdna3-demo .cell .cap{font-size:9px;color:var(--g4);padding:0 12px 10px;border-bottom:1px solid var(--g2)}
#mm-cdna3-demo .chip{display:block;width:100%;text-align:left;font-family:inherit;font-size:11px;padding:11px 12px;background:var(--paper);border:0;border-bottom:1px solid var(--g2);cursor:pointer;color:var(--ink)}
#mm-cdna3-demo .chip:hover{background:var(--g1)}
#mm-cdna3-demo .chip[aria-pressed=&quot;true&quot;]{background:var(--ink);color:var(--paper)}
#mm-cdna3-demo .chip b{letter-spacing:.02em}
#mm-cdna3-demo .chip span{display:block;font-size:9px;color:var(--g4);margin-top:2px}
#mm-cdna3-demo .chip[aria-pressed=&quot;true&quot;] span{color:var(--g3)}
#mm-cdna3-demo .detail{margin-top:18px;border:2px solid var(--ink);padding:16px 18px;min-height:96px}
#mm-cdna3-demo .detail .dt{font-size:13px;font-weight:700;margin-bottom:6px;letter-spacing:.02em}
#mm-cdna3-demo .detail .db{font-size:12px;color:var(--g5);line-height:1.6}
#mm-cdna3-demo .ft{border-top:2px solid var(--ink);padding:12px 24px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px;font-size:11px;color:var(--g5)}
#mm-cdna3-demo .ft b{color:var(--ink);letter-spacing:.04em}
#mm-cdna3-demo .ft a{color:var(--ink);text-decoration:underline}

@media(max-width:640px){
  #mm-cdna3-demo h1{font-size:19px}
  #mm-cdna3-demo .row{grid-template-columns:78px 1fr 62px;gap:9px}
  #mm-cdna3-demo .mem-grid{grid-template-columns:repeat(2,1fr)}
  #mm-cdna3-demo .cell:nth-child(2){border-right:0}
  #mm-cdna3-demo .cell:nth-child(1),#mm-cdna3-demo .cell:nth-child(2){border-bottom:1px solid var(--g3)}
  #mm-cdna3-demo .grouprow,#mm-cdna3-demo .barbar,#mm-cdna3-demo .transport{grid-template-columns:1fr}
  #mm-cdna3-demo .glabel{margin-bottom:4px}
  #mm-cdna3-demo .slot b{font-size:10px}
  #mm-cdna3-demo select{min-width:160px}
}
&lt;/style&gt;

&lt;div class=&quot;hd&quot;&gt;
  &lt;div class=&quot;eyebrow&quot;&gt;MoonMath.ai · CDNA3 Attention · MI300X (gfx942)&lt;/div&gt;
  &lt;h1&gt;bf16 Forward Attention in HIP, Beating AITER v3&lt;/h1&gt;
  &lt;p class=&quot;sub&quot;&gt;An interactive walkthrough of the kernel: explore the benchmark sweep, watch the eight-wave two-group pipeline overlap compute with memory, and see where K, V, Q, and accumulators live. All numbers are from MoonMath's published results.&lt;/p&gt;
&lt;/div&gt;

&lt;div class=&quot;tabs&quot; role=&quot;tablist&quot;&gt;
  &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;true&quot; data-tab=&quot;bench&quot;&gt;Benchmark&lt;/button&gt;
  &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-tab=&quot;pipe&quot;&gt;Pipeline&lt;/button&gt;
  &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-tab=&quot;mem&quot;&gt;Memory Map&lt;/button&gt;
&lt;/div&gt;

&lt;!-- BENCHMARK --&gt;
&lt;div class=&quot;panel on&quot; data-panel=&quot;bench&quot;&gt;
  &lt;p class=&quot;panel-lead&quot;&gt;Pick an input shape (B, H, S, D) and a bf16 rounding mode. Bars show median kernel time; shorter is faster. Speedups are &lt;code&gt;other_ms / ours_ms&lt;/code&gt;.&lt;/p&gt;
  &lt;div class=&quot;ctl&quot;&gt;
    &lt;div class=&quot;field&quot;&gt;
      &lt;label for=&quot;mm-shape&quot;&gt;Input shape (B, H, S, D)&lt;/label&gt;
      &lt;select id=&quot;mm-shape&quot;&gt;&lt;/select&gt;
    &lt;/div&gt;
    &lt;div class=&quot;field&quot;&gt;
      &lt;label&gt;Rounding mode&lt;/label&gt;
      &lt;div class=&quot;seg&quot; id=&quot;mm-round&quot; role=&quot;group&quot; aria-label=&quot;Rounding mode&quot;&gt;
        &lt;button data-r=&quot;rtne&quot; aria-pressed=&quot;true&quot;&gt;RTNE&lt;/button&gt;
        &lt;button data-r=&quot;rtna&quot; aria-pressed=&quot;false&quot;&gt;RTNA&lt;/button&gt;
        &lt;button data-r=&quot;rtz&quot; aria-pressed=&quot;false&quot;&gt;RTZ&lt;/button&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;chart&quot; id=&quot;mm-chart&quot;&gt;
    &lt;div class=&quot;row&quot;&gt;
      &lt;div class=&quot;name&quot;&gt;MoonMath&lt;small&gt;HIP kernel&lt;/small&gt;&lt;/div&gt;
      &lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill ours&quot; id=&quot;f-ours&quot;&gt;&lt;span class=&quot;ms&quot; id=&quot;m-ours&quot;&gt;&lt;/span&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;spd&quot;&gt;baseline&lt;small&gt;this kernel&lt;/small&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;row&quot;&gt;
      &lt;div class=&quot;name&quot;&gt;AITER v3&lt;small&gt;AMD, ASM&lt;/small&gt;&lt;/div&gt;
      &lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill aiter&quot; id=&quot;f-aiter&quot;&gt;&lt;span class=&quot;ms&quot; id=&quot;m-aiter&quot;&gt;&lt;/span&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;spd&quot; id=&quot;s-aiter&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;row&quot;&gt;
      &lt;div class=&quot;name&quot;&gt;Modular MAX&lt;small&gt;RTNE internally&lt;/small&gt;&lt;/div&gt;
      &lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill max&quot; id=&quot;f-max&quot;&gt;&lt;span class=&quot;ms&quot; id=&quot;m-max&quot;&gt;&lt;/span&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;spd&quot; id=&quot;s-max&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;legend&quot;&gt;
      &lt;span&gt;&lt;i class=&quot;li-ours&quot;&gt;&lt;/i&gt;MoonMath (ours)&lt;/span&gt;
      &lt;span&gt;&lt;i class=&quot;li-aiter&quot;&gt;&lt;/i&gt;AITER v3&lt;/span&gt;
      &lt;span&gt;&lt;i class=&quot;li-max&quot;&gt;&lt;/i&gt;Modular MAX&lt;/span&gt;
    &lt;/div&gt;
  &lt;/div&gt;
  &lt;p class=&quot;note&quot; id=&quot;mm-bnote&quot;&gt;&lt;/p&gt;
&lt;/div&gt;

&lt;!-- PIPELINE --&gt;
&lt;div class=&quot;panel&quot; data-panel=&quot;pipe&quot;&gt;
  &lt;p class=&quot;panel-lead&quot;&gt;Eight waves run as two groups of four. Each K-block iteration has two phases. While one group saturates the matrix core, the other does softmax and issues the next loads. Two &lt;code&gt;s_barrier&lt;/code&gt;s bound each iteration.&lt;/p&gt;
  &lt;div class=&quot;pipewrap&quot;&gt;
    &lt;div class=&quot;barbar&quot;&gt;
      &lt;div&gt;&lt;/div&gt;
      &lt;div class=&quot;tl&quot;&gt;&lt;div class=&quot;ph&quot; id=&quot;ph1&quot;&gt;Phase 1&lt;/div&gt;&lt;div class=&quot;ph&quot; id=&quot;ph2&quot;&gt;Phase 2&lt;/div&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;grouprow&quot;&gt;
      &lt;div class=&quot;glabel&quot;&gt;Group A&lt;small&gt;4 waves&lt;/small&gt;&lt;/div&gt;
      &lt;div class=&quot;lane&quot;&gt;&lt;div class=&quot;slot&quot; id=&quot;a0&quot;&gt;&lt;/div&gt;&lt;div class=&quot;slot&quot; id=&quot;a1&quot;&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;grouprow&quot;&gt;
      &lt;div class=&quot;glabel&quot;&gt;Group B&lt;small&gt;4 waves&lt;/small&gt;&lt;/div&gt;
      &lt;div class=&quot;lane&quot;&gt;&lt;div class=&quot;slot&quot; id=&quot;b0&quot;&gt;&lt;/div&gt;&lt;div class=&quot;slot&quot; id=&quot;b1&quot;&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;transport&quot;&gt;
      &lt;div class=&quot;glabel&quot; style=&quot;align-self:center&quot;&gt;Memory&lt;small&gt;async&lt;/small&gt;&lt;/div&gt;
      &lt;div class=&quot;pipe&quot;&gt;
        &lt;div class=&quot;seg2&quot; id=&quot;t0&quot;&gt;K: HBM→LDS&lt;/div&gt;
        &lt;div class=&quot;seg2&quot; id=&quot;t1&quot;&gt;V prefetch→L1&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;barrier&quot; id=&quot;brk&quot;&gt;— s_barrier at phase handoff · s_barrier + lds_barrier at iteration boundary —&lt;/div&gt;
  &lt;/div&gt;
  &lt;div class=&quot;pctl&quot;&gt;
    &lt;button class=&quot;btn primary&quot; id=&quot;mm-play&quot;&gt;► Play&lt;/button&gt;
    &lt;button class=&quot;btn&quot; id=&quot;mm-step&quot;&gt;Step ›&lt;/button&gt;
    &lt;span class=&quot;iter&quot; id=&quot;mm-iter&quot;&gt;iteration N · phase 1&lt;/span&gt;
    &lt;span class=&quot;speed&quot;&gt;slow&lt;input type=&quot;range&quot; id=&quot;mm-spd&quot; min=&quot;350&quot; max=&quot;1600&quot; value=&quot;950&quot; step=&quot;50&quot;&gt;fast&lt;/span&gt;
  &lt;/div&gt;
  &lt;p class=&quot;note&quot;&gt;The matrix core (black slots) never idles: as Group A finishes its PV/QK MFMAs, Group B has already taken over the matrix core while A moves to softmax and prefetch. This mirrors FlashAttention-3's matmul/softmax alternation, but without a dedicated producer warp — every load here is already asynchronous on CDNA3.&lt;/p&gt;
&lt;/div&gt;

&lt;!-- MEMORY --&gt;
&lt;div class=&quot;panel&quot; data-panel=&quot;mem&quot;&gt;
  &lt;p class=&quot;panel-lead&quot;&gt;The kernel is one long argument about which level of the MI300X memory hierarchy holds what. Tap any item to see why it lives there. Per-CU capacities are shown under each level.&lt;/p&gt;
  &lt;div class=&quot;mem-grid&quot;&gt;
    &lt;div class=&quot;cell&quot;&gt;
      &lt;h4&gt;VGPRs&lt;/h4&gt;&lt;div class=&quot;cap&quot;&gt;512 KiB register file / CU&lt;/div&gt;
      &lt;button class=&quot;chip&quot; data-k=&quot;q&quot; aria-pressed=&quot;true&quot;&gt;&lt;b&gt;Q tile&lt;/b&gt;&lt;span&gt;persistent&lt;/span&gt;&lt;/button&gt;
      &lt;button class=&quot;chip&quot; data-k=&quot;acc&quot;&gt;&lt;b&gt;scores · O&lt;/b&gt;&lt;span&gt;fp32 accumulators&lt;/span&gt;&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;cell&quot;&gt;
      &lt;h4&gt;LDS&lt;/h4&gt;&lt;div class=&quot;cap&quot;&gt;64 KiB shared / CU&lt;/div&gt;
      &lt;button class=&quot;chip&quot; data-k=&quot;k&quot;&gt;&lt;b&gt;K tile&lt;/b&gt;&lt;span&gt;double-buffered, 32 KiB&lt;/span&gt;&lt;/button&gt;
      &lt;button class=&quot;chip&quot; data-k=&quot;q3&quot;&gt;&lt;b&gt;3rd Q tile&lt;/b&gt;&lt;span&gt;32 KiB, streamed&lt;/span&gt;&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;cell&quot;&gt;
      &lt;h4&gt;L1&lt;/h4&gt;&lt;div class=&quot;cap&quot;&gt;32 KiB vector cache / CU&lt;/div&gt;
      &lt;button class=&quot;chip&quot; data-k=&quot;v&quot;&gt;&lt;b&gt;V_t tile&lt;/b&gt;&lt;span&gt;resident, prefetched&lt;/span&gt;&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;cell&quot;&gt;
      &lt;h4&gt;HBM / L2&lt;/h4&gt;&lt;div class=&quot;cap&quot;&gt;32 MiB L2 · 256 MiB Infinity&lt;/div&gt;
      &lt;button class=&quot;chip&quot; data-k=&quot;src&quot;&gt;&lt;b&gt;K / V source&lt;/b&gt;&lt;span&gt;streamed in&lt;/span&gt;&lt;/button&gt;
    &lt;/div&gt;
  &lt;/div&gt;
  &lt;div class=&quot;detail&quot; id=&quot;mm-detail&quot;&gt;
    &lt;div class=&quot;dt&quot;&gt;&lt;/div&gt;&lt;div class=&quot;db&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;div class=&quot;ft&quot;&gt;
  &lt;span&gt;Source: &lt;a href=&quot;https://moonmath.ai/cdna3attention/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;moonmath.ai/cdna3attention&lt;/a&gt; · MIT-licensed kernel&lt;/span&gt;
  &lt;span&gt;&lt;b&gt;Marktechpost&lt;/b&gt; · interactive explainer&lt;/span&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var root=document.getElementById('mm-cdna3-demo');
  function $(s){return root.querySelector(s);}
  function $all(s){return Array.prototype.slice.call(root.querySelectorAll(s));}

  /* ---- data: verbatim from MoonMath published table ---- */
  var DATA=[
    {s:&quot;(2, 24, 8192, 128)&quot;,  rtne:[3.083,3.792,4.237],  rtna:[3.022,3.605,4.237],  rtz:[2.983,3.303,4.237]},
    {s:&quot;(2, 24, 16384, 128)&quot;, rtne:[11.670,14.691,17.923],rtna:[11.479,13.801,17.923],rtz:[11.385,12.629,17.923]},
    {s:&quot;(1, 32, 16384, 128)&quot;, rtne:[8.013,9.031,11.030],  rtna:[7.828,8.656,11.030],  rtz:[7.731,7.989,11.030]},
    {s:&quot;(4, 16, 16384, 128)&quot;, rtne:[15.591,18.337,22.061],rtna:[15.331,17.567,22.061],rtz:[15.055,16.183,22.061]},
    {s:&quot;(1, 64, 16384, 128)&quot;, rtne:[15.528,18.333,22.763],rtna:[15.239,17.535,22.763],rtz:[15.040,16.161,22.763]},
    {s:&quot;(2, 24, 32768, 128)&quot;, rtne:[46.002,54.794,69.947],rtna:[44.440,52.363,69.947],rtz:[44.075,48.549,69.947]},
    {s:&quot;(2, 16, 65536, 128)&quot;, rtne:[117.612,136.301,171.273],rtna:[115.550,130.278,171.273],rtz:[114.665,121.668,171.273]},
    {s:&quot;(2, 8, 86016, 128)&quot;,  rtne:[101.071,118.939,141.319],rtna:[100.165,114.515,141.319],rtz:[99.397,106.513,141.319]},
    {s:&quot;(1, 16, 131072, 128)&quot;,rtne:[232.517,269.278,339.322],rtna:[228.475,258.092,339.322],rtz:[226.152,239.587,339.322]}
  ];
  var round='rtne', shapeIdx=0;
  var sel=$('#mm-shape');
  DATA.forEach(function(d,i){var o=document.createElement('option');o.value=i;o.textContent=d.s;sel.appendChild(o);});

  function fmt(x){return x.toFixed(x&lt;10?3:(x&lt;100?2:1));}
  function drawBench(){
    var d=DATA[shapeIdx], row=d[round];
    var ours=row[0],aiter=row[1],mx=row[2];
    var maxv=Math.max(ours,aiter,mx);
    $('#f-ours').style.width=(ours/maxv*100)+'%';
    $('#f-aiter').style.width=(aiter/maxv*100)+'%';
    $('#f-max').style.width=(mx/maxv*100)+'%';
    $('#m-ours').textContent=fmt(ours)+' ms';
    $('#m-aiter').textContent=fmt(aiter)+' ms';
    $('#m-max').textContent=fmt(mx)+' ms';
    $('#s-aiter').innerHTML=(aiter/ours).toFixed(2)+'×&lt;small&gt;vs AITER&lt;/small&gt;';
    $('#s-max').innerHTML=(mx/ours).toFixed(2)+'×&lt;small&gt;vs MAX&lt;/small&gt;';
    var rn={rtne:'rounds to nearest even',rtna:&quot;rounds to nearest, ties away (AITER's default)&quot;,rtz:&quot;truncates toward zero (AITER's fastest mode)&quot;};
    $('#mm-bnote').textContent='Shape '+d.s+' · '+round.toUpperCase()+' '+rn[round]+'. MoonMath is '+(aiter/ours).toFixed(2)+'× faster than AITER v3 and '+(mx/ours).toFixed(2)+'× faster than Modular MAX on this configuration.';
  }
  sel.addEventListener('change',function(){shapeIdx=+this.value;drawBench();});
  $all('#mm-round button').forEach(function(b){
    b.addEventListener('click',function(){
      $all('#mm-round button').forEach(function(x){x.setAttribute('aria-pressed','false');});
      b.setAttribute('aria-pressed','true');round=b.getAttribute('data-r');drawBench();
    });
  });

  /* ---- tabs ---- */
  $all('.tab').forEach(function(t){
    t.addEventListener('click',function(){
      $all('.tab').forEach(function(x){x.setAttribute('aria-selected','false');});
      t.setAttribute('aria-selected','true');
      var id=t.getAttribute('data-tab');
      $all('.panel').forEach(function(p){p.classList.toggle('on',p.getAttribute('data-panel')===id);});
      reportHeight();
    });
  });

  /* ---- pipeline animation ---- */
  // Fixed roles per phase; the highlight (current phase column) moves.
  // Phase 1: A on matrix core (PV/QK); B on memory+softmax (K load).
  // Phase 2: roles swap — B on matrix core; A on memory+softmax (V prefetch).
  // So the matrix-core &quot;baton&quot; passes A -&gt; B across the two phase columns.
  var phase=0, iter='N', playing=false, timer=null, spd=950;
  // [phase1 cell, phase2 cell] for each lane — content is fixed
  var CELLS={
    a:[{c:'mc',t:'PV · QK',tag:'matrix core'},{c:'mem',t:'softmax · V→L1',tag:'memory'}],
    b:[{c:'mem',t:'K→LDS · softmax',tag:'memory'},{c:'mc',t:'PV · QK',tag:'matrix core'}],
    t:[{t:'K: HBM→LDS'},{t:'V prefetch→L1'}]
  };
  function setSlot(id,cell){var el=$('#'+id);el.className='slot role '+cell.c;el.innerHTML='&lt;b&gt;'+cell.t+'&lt;/b&gt;&lt;span class=&quot;tag&quot;&gt;'+cell.tag+'&lt;/span&gt;';}
  function paintStatic(){
    setSlot('a0',CELLS.a[0]);setSlot('a1',CELLS.a[1]);
    setSlot('b0',CELLS.b[0]);setSlot('b1',CELLS.b[1]);
    $('#t0').innerHTML=CELLS.t[0].t;$('#t1').innerHTML=CELLS.t[1].t;
  }
  function paintPipe(){
    // phase 0 -&gt; col0 active (a0,b0,t0); phase 1 -&gt; col1 active (a1,b1,t1)
    var act=phase, dim=phase^1;
    [['a0','a1'],['b0','b1']].forEach(function(pair){
      $('#'+pair[act]).classList.remove('dim');$('#'+pair[act]).classList.add('active');
      $('#'+pair[dim]).classList.add('dim');$('#'+pair[dim]).classList.remove('active');
    });
    ['t0','t1'].forEach(function(id,i){$('#'+id).className='seg2'+(i===act?' flow':' dim');});
    $('#ph1').className='ph'+(phase===0?' cur':'');
    $('#ph2').className='ph'+(phase===1?' cur':'');
    $('#mm-iter').textContent='iteration '+iter+' · phase '+(phase+1)+(phase===0?' — Group A on matrix core':' — Group B on matrix core');
  }
  function adv(){
    phase++;
    if(phase&gt;1){phase=0;iter=(iter==='N')?'N+1':(iter==='N+1'?'N+2':'N');}
    paintPipe();
  }
  $('#mm-step').addEventListener('click',function(){stop();adv();});
  function play(){playing=true;$('#mm-play').innerHTML='❚❚ Pause';$('#mm-play').classList.remove('primary');
    timer=setInterval(adv,spd);}
  function stop(){playing=false;$('#mm-play').innerHTML='► Play';$('#mm-play').classList.add('primary');
    if(timer){clearInterval(timer);timer=null;}}
  $('#mm-play').addEventListener('click',function(){playing?stop():play();});
  $('#mm-spd').addEventListener('input',function(){spd=1950-(+this.value);if(playing){stop();play();}});
  paintStatic();paintPipe();

  /* ---- memory map ---- */
  var MEM={
    q:{t:'Q tile — VGPRs, persistent',b:'The Q tile is read every iteration and never reloaded, so it stays resident in the vector register file. Two of three Q tiles per wave stay register-resident and hot.'},
    acc:{t:'scores · O — fp32 accumulators in VGPRs',b:'Matrix-core outputs (the score matrix and the running output) never leave registers until the final store. The 16×16×16 MFMA accumulates into just 4 fp32 elements per lane, keeping accumulator pressure low.'},
    k:{t:'K tile — LDS, double-buffered, 32 KiB',b:'One copy of K is shared by all eight waves and swapped per iteration via a double buffer. K streams from HBM straight into LDS by direct DMA, never passing through a VGPR. An XOR swizzle breaks bank conflicts with zero padding.'},
    q3:{t:'3rd Q tile — LDS, 32 KiB, streamed',b:'Moving V to L1 freed 32 KiB of LDS. The kernel spends it on a third Q tile (48 q-rows per wave). It is parked in LDS and streamed through a ping-pong buffer during the QK matmul, raising K/V reuse.'},
    v:{t:'V_t tile — L1, resident',b:'The pre-transposed V tile is kept hot in L1 and reread on every PV matmul. L1 is not addressable, so residency is engineered by prefetching the next iteration\u2019s lines into a throwaway register — the data lands in L1 as a side effect.'},
    src:{t:'K / V source — HBM, staged via L2',b:'A head-first chiplet swizzle maps all of a (batch, head)\u2019s Q blocks onto a single XCD, so its K and V stay resident in that XCD\u2019s slice of L2 instead of thrashing across all eight.'}
  };
  function showMem(k){
    $all('.chip').forEach(function(c){c.setAttribute('aria-pressed', c.getAttribute('data-k')===k ? 'true':'false');});
    $('#mm-detail .dt').textContent=MEM[k].t;
    $('#mm-detail .db').textContent=MEM[k].b;
    reportHeight();
  }
  $all('.chip').forEach(function(c){c.addEventListener('click',function(){showMem(c.getAttribute('data-k'));});});

  /* ---- init ---- */
  drawBench();showMem('q');

  /* ---- auto-resize for WordPress embed ---- */
  function reportHeight(){
    var h=root.offsetHeight+40;
    if(window.parent){window.parent.postMessage({type:'mm-cdna3-height',height:h},'*');}
  }
  window.addEventListener('load',reportHeight);
  window.addEventListener('resize',reportHeight);
  setTimeout(reportHeight,300);setTimeout(reportHeight,900);
})();
&lt;/script&gt;
&lt;/div&gt;&lt;/body&gt;&lt;/html&gt;"></iframe>
<script>
(function(){
  window.addEventListener("message",function(e){
    if(e&&e.data&&e.data.type==="mm-cdna3-height"){
      var f=document.getElementById("mm-cdna3-frame");
      if(f&&e.data.height){f.style.height=e.data.height+"px";}
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<h2 id="h-use-cases" class="wp-block-heading"><strong>Use Cases</strong></h2>



<p class="wp-block-paragraph">The kernel installs with pip and exposes a small API. It launches on the caller&#8217;s stream, so it overlaps inside larger pipelines.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import torch
import moonmath_attention as ma

# PyTorch's ROCm build uses the "cuda" device string on AMD GPUs
q = torch.randn(2, 8192, 24, 128, dtype=torch.bfloat16, device="cuda")
k = torch.randn(2, 8192, 24, 128, dtype=torch.bfloat16, device="cuda")
v = torch.randn(2, 8192, 24, 128, dtype=torch.bfloat16, device="cuda")

out     = ma.forward(q, k, v, layout="bshd")
out_rtz = ma.forward(q, k, v, layout="bshd", round_mode="rtz")</code></pre></div></div>



<p class="wp-block-paragraph">One concrete use case is video diffusion. The team added LiteAttention support and sent a PR to SGLang diffusion. On Wan2.1-T2V-1.3B-Diffusers, they switched attention from AITER to <code>liteattention_rocm</code>. End-to-end generation improved by 1.23× on MI300X, with no visible quality regression.</p>



<p class="wp-block-paragraph">The BSHD layout suits diffusion tensors directly. Cross-attention works with any KV length and no padding.</p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>The kernel is bf16 forward attention for MI300X, written in HIP under MIT.</li>



<li>It beats AITER v3 on every shape and rounding mode, geomean 1.18×/1.15×/1.08×.</li>



<li>One-instruction asm wrappers give opcode control while the compiler allocates registers.</li>



<li>Memory placement drove most of the gain: K in LDS, V hot in L1, Q in registers.</li>



<li>A real SGLang PR sped up Wan2.1 video diffusion by 1.23× with no quality regression.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://moonmath.ai/cdna3attention/" target="_blank" rel="noreferrer noopener">Technical details</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/22/moonmath-ai-open-sources-a-hip-attention-kernel-for-amd-mi300x-that-beats-aiter-v3-on-every-shape-and-rounding-mode/">MoonMath AI Open-Sources a HIP Attention Kernel for AMD MI300X That Beats AITER v3 on Every Shape and Rounding Mode</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80648</post-id>
	</item>
		<item>
		<title>How to Design Python-First Interactive Dashboards with Prefab Reactive UI Components and Static HTML Export</title>
		<link>https://www.marktechpost.com/2026/06/21/how-to-design-python-first-interactive-dashboards-with-prefab-reactive-ui-components-and-static-html-export/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Mon, 22 Jun 2026 00:13:29 +0000</pubDate>
				<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Data Science]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80645</guid>

					<description><![CDATA[<p>In this tutorial, we build a Prefab application that creates interactive dashboards entirely in Python. We design an operations dashboard with reactive state, charts, tables, filters, forms, tabs, and metrics. We generate synthetic pipeline monitoring data and connect it to live UI controls. We then export the app as static HTML and preview it directly inside Google Colab.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/21/how-to-design-python-first-interactive-dashboards-with-prefab-reactive-ui-components-and-static-html-export/">How to Design Python-First Interactive Dashboards with Prefab Reactive UI Components and Static HTML Export</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this <a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Data%20Analysis/prefab_reactive_dashboard_tutorial_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">tutorial</a>, we build a<a href="https://github.com/PrefectHQ/prefab"> <strong>Prefab</strong></a> application that demonstrates how to create interactive dashboards entirely in Python. We use Prefab’s component-based Python interface to design a polished operations dashboard with reactive state, charts, tables, filters, forms, tabs, alerts, metrics, and client-side actions. We generate realistic pipeline monitoring data, connect it to live UI controls, and export the final app as a static HTML dashboard that we can preview directly inside Google Colab. Through this workflow, we learn how Prefab lets us move from Python data logic to a modern React-powered user interface without having to write frontend code manually.</p>



<h2 id="h-installing-prefab-in-colab" class="wp-block-heading"><strong>Installing Prefab in Colab</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import os
import sys
import base64
import subprocess
from pathlib import Path
from IPython.display import HTML, display, FileLink
PREFAB_VERSION = "0.20.2"
APP_PATH = Path("/content/prefab_advanced_tutorial_app.py")
HTML_PATH = Path("/content/prefab_advanced_dashboard.html")
subprocess.check_call([
   sys.executable,
   "-m",
   "pip",
   "install",
   "-q",
   f"prefab-ui=={PREFAB_VERSION}",
])
APP_CODE = ""
</code></pre></div></div>



<p class="wp-block-paragraph">We set up the Colab environment by importing the required Python utilities and defining the Prefab version, app path, and HTML export path. We install the pinned prefab-ui package so that the tutorial runs consistently without version-related issues. We also initialize an empty APP_CODE string, which we use to build the complete Prefab application step by step.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">APP_CODE += r'''</code></pre></div></div>



<h2 id="h-generating-synthetic-operations-data" class="wp-block-heading"><strong>Generating Synthetic Operations Data</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import random
from collections import Counter, defaultdict
from datetime import date, timedelta
from prefab_ui.actions import AppendState, OpenLink, PopState, SetState, ShowToast, ToggleState
from prefab_ui.app import PrefabApp
from prefab_ui.components import (
   Alert, AlertDescription, AlertTitle, Badge, Button, Card, CardContent,
   CardDescription, CardFooter, CardHeader, CardTitle, Code, Column,
   DataTable, DataTableColumn, Form, Grid, H2, Input, Markdown, Mermaid,
   Metric, Muted, Progress, Ring, Row, Slider, Small, Switch, Tab, Tabs,
   Text
)
from prefab_ui.components.charts import (
   BarChart, ChartSeries, LineChart, PieChart, RadarChart, ScatterChart,
   Sparkline
)
from prefab_ui.components.control_flow import Else, ForEach, If
from prefab_ui.rx import EVENT, STATE
random.seed(42)
TODAY = date.today()
DATES = [TODAY - timedelta(days=29 - i) for i in range(30)]
REGIONS = ["All", "APAC", "EMEA", "NA", "LATAM"]
PIPELINES = [
   "Customer 360 ETL",
   "Invoice OCR",
   "LLM Triage",
   "Risk Scoring",
   "Forecast Sync",
   "Warehouse Load",
]
OWNERS = ["Data Platform", "AI Apps", "Revenue Ops", "Risk Engineering"]
STATES = ["Completed", "Completed", "Completed", "Completed", "Late", "Failed"]
PRIORITIES = ["P0", "P1", "P2", "P3"]
runs = []
daily_region_rows = []
for d in DATES:
   for region in REGIONS[1:]:
       region_bias = {
           "APAC": 0.96,
           "EMEA": 0.94,
           "NA": 0.97,
           "LATAM": 0.91,
       }[region]
       volume = random.randint(32, 78)
       failures = 0
       late = 0
       total_cost = 0.0
       total_latency = 0.0
       total_revenue = 0.0
       for i in range(volume):
           pipeline = random.choice(PIPELINES)
           owner = random.choice(OWNERS)
           state = random.choices(
               STATES,
               weights=[
                   region_bias * 10,
                   6,
                   4,
                   3,
                   1.2,
                   max(0.2, (1 - region_bias) * 16),
               ],
               k=1,
           )[0]
           duration = max(
               12,
               int(
                   random.gauss(95, 35)
                   + (20 if state == "Late" else 0)
                   + (45 if state == "Failed" else 0)
               ),
           )
           cost = round(max(0.09, random.lognormvariate(-1.15, 0.55) + duration / 1800), 2)
           revenue = round(random.uniform(1.2, 8.5) * (1.3 if state == "Completed" else 0.6), 2)
           priority = random.choices(PRIORITIES, weights=[1, 3, 7, 10], k=1)[0]
           if state == "Failed":
               failures += 1
           if state == "Late":
               late += 1
           total_cost += cost
           total_latency += duration
           total_revenue += revenue
           if d >= TODAY - timedelta(days=10) and (state in {"Failed", "Late"} or random.random() &lt; 0.05):
               runs.append({
                   "run_id": f"{d.strftime('%m%d')}-{region[:2]}-{len(runs)+1:04d}",
                   "date": d.strftime("%Y-%m-%d"),
                   "pipeline": pipeline,
                   "owner": owner,
                   "region": region,
                   "state": state,
                   "priority": priority,
                   "duration_s": duration,
                   "cost_usd": cost,
                   "revenue_k": revenue,
                   "sla_gap": round(max(0, duration - 120) / 60, 1),
               })
       daily_region_rows.append({
           "date": d.strftime("%b %d"),
           "region": region,
           "runs": volume,
           "failures": failures,
           "late": late,
           "success_rate": round(100 * (volume - failures - late * 0.35) / volume, 1),
           "avg_latency": round(total_latency / volume, 1),
           "cost_usd": round(total_cost, 2),
           "revenue_k": round(total_revenue, 1),
       })
runs = sorted(
   runs,
   key=lambda r: (r["priority"], r["state"] != "Failed", -r["duration_s"])
)[:80]
def aggregate_daily(rows):
   by_date = defaultdict(lambda: {
       "date": "",
       "runs": 0,
       "failures": 0,
       "late": 0,
       "cost_usd": 0.0,
       "revenue_k": 0.0,
       "latency_weighted": 0.0,
   })
   for r in rows:
       bucket = by_date[r["date"]]
       bucket["date"] = r["date"]
       bucket["runs"] += r["runs"]
       bucket["failures"] += r["failures"]
       bucket["late"] += r["late"]
       bucket["cost_usd"] += r["cost_usd"]
       bucket["revenue_k"] += r["revenue_k"]
       bucket["latency_weighted"] += r["avg_latency"] * r["runs"]
   out = []
   for d in [x.strftime("%b %d") for x in DATES]:
       b = by_date[d]
       if b["runs"]:
           b["success_rate"] = round(100 * (b["runs"] - b["failures"] - b["late"] * 0.35) / b["runs"], 1)
           b["avg_latency"] = round(b["latency_weighted"] / b["runs"], 1)
           b["cost_usd"] = round(b["cost_usd"], 2)
           b["revenue_k"] = round(b["revenue_k"], 1)
           del b["latency_weighted"]
           out.append(dict(b))
   return out
def aggregate_regions(rows):
   by_region = defaultdict(lambda: {
       "region": "",
       "runs": 0,
       "failures": 0,
       "late": 0,
       "cost_usd": 0.0,
       "revenue_k": 0.0,
       "latency_weighted": 0.0,
   })
   for r in rows:
       b = by_region[r["region"]]
       b["region"] = r["region"]
       b["runs"] += r["runs"]
       b["failures"] += r["failures"]
       b["late"] += r["late"]
       b["cost_usd"] += r["cost_usd"]
       b["revenue_k"] += r["revenue_k"]
       b["latency_weighted"] += r["avg_latency"] * r["runs"]
   out = []
   for region in REGIONS[1:]:
       b = by_region[region]
       b["success_rate"] = round(100 * (b["runs"] - b["failures"] - b["late"] * 0.35) / b["runs"], 1)
       b["avg_latency"] = round(b["latency_weighted"] / b["runs"], 1)
       b["cost_usd"] = round(b["cost_usd"], 2)
       b["revenue_k"] = round(b["revenue_k"], 1)
       b["roi"] = round(b["revenue_k"] / max(1, b["cost_usd"]), 1)
       del b["latency_weighted"]
       out.append(dict(b))
   return out
def make_status_rows(table_rows):
   counts = Counter(r["state"] for r in table_rows)
   return [{"state": k, "count": v} for k, v in counts.items()]
def make_pipeline_rows(table_rows):
   counts = Counter(r["pipeline"] for r in table_rows)
   return [{"pipeline": k, "count": v} for k, v in counts.most_common()]
def make_kpis(region, daily_rows, table_rows):
   runs_count = sum(r["runs"] for r in daily_rows)
   failures = sum(r["failures"] for r in daily_rows)
   late = sum(r["late"] for r in daily_rows)
   cost = sum(r["cost_usd"] for r in daily_rows)
   revenue = sum(r["revenue_k"] for r in daily_rows)
   return {
       "region": region,
       "runs": runs_count,
       "success_rate": round(100 * (runs_count - failures - late * 0.35) / max(1, runs_count), 1),
       "avg_latency": round(sum(r["avg_latency"] * r["runs"] for r in daily_rows) / max(1, runs_count), 1),
       "cost_usd": round(cost, 2),
       "revenue_k": round(revenue, 1),
       "roi": round(revenue / max(1, cost), 1),
       "open_issues": len(table_rows),
       "p0p1": sum(1 for r in table_rows if r["priority"] in {"P0", "P1"}),
       "failure_rate": round(100 * failures / max(1, runs_count), 2),
       "spark": [r["success_rate"] for r in daily_rows[-14:]],
   }
DAILY_BY_REGION = {"All": aggregate_daily(daily_region_rows)}
REGION_ROWS = aggregate_regions(daily_region_rows)
for region in REGIONS[1:]:
   DAILY_BY_REGION[region] = [r for r in daily_region_rows if r["region"] == region]
RUNS_BY_REGION = {
   region: [r for r in runs if region == "All" or r["region"] == region]
   for region in REGIONS
}
STATUS_BY_REGION = {
   region: make_status_rows(RUNS_BY_REGION[region])
   for region in REGIONS
}
PIPELINE_BY_REGION = {
   region: make_pipeline_rows(RUNS_BY_REGION[region])
   for region in REGIONS
}
KPI_BY_REGION = {
   region: make_kpis(region, DAILY_BY_REGION[region], RUNS_BY_REGION[region])
   for region in REGIONS
}
WATCHLIST = sorted(
   runs,
   key=lambda r: (r["priority"], r["state"] != "Failed", -r["sla_gap"])
)[:8]
SCATTER_ROWS = [
   {
       "region": r["region"],
       "success_rate": r["success_rate"],
       "cost_usd": r["cost_usd"],
       "avg_latency": r["avg_latency"],
   }
   for r in REGION_ROWS
]
RADAR_ROWS = [
   {"metric": "Success", **{r["region"]: r["success_rate"] for r in REGION_ROWS}},
   {"metric": "ROI", **{r["region"]: min(100, r["roi"] * 8) for r in REGION_ROWS}},
   {"metric": "Latency", **{r["region"]: max(0, 100 - r["avg_latency"] / 2) for r in REGION_ROWS}},
   {"metric": "Cost", **{r["region"]: max(0, 100 - r["cost_usd"] / 20) for r in REGION_ROWS}},
]
REGION_ACTIONS = {
   region: [
       SetState("selected_region", region),
       SetState("line_rows", DAILY_BY_REGION[region]),
       SetState("table_rows", RUNS_BY_REGION[region]),
       SetState("status_rows", STATUS_BY_REGION[region]),
       SetState("pipeline_rows", PIPELINE_BY_REGION[region]),
       SetState("region_kpis", KPI_BY_REGION[region]),
       SetState("selected_run", None),
       ShowToast(f"Region set to {region}", variant="info", duration=1800),
   ]
   for region in REGIONS
}
'''
</code></pre></div></div>



<p class="wp-block-paragraph">We add the main imports for Prefab components, actions, charts, control flow, and reactive state handling. We generate realistic synthetic operations data across regions, pipelines, owners, states, priorities, costs, latency, and revenue. We also create aggregation functions and prepare datasets that power the dashboard’s charts, metrics, filters, tables, and watchlist.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">APP_CODE += r'''</code></pre></div></div>



<h2 id="h-building-the-overview-tab" class="wp-block-heading"><strong>Building the Overview Tab</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">initial_state = {
   "selected_region": "All",
   "line_rows": DAILY_BY_REGION["All"],
   "table_rows": RUNS_BY_REGION["All"],
   "status_rows": STATUS_BY_REGION["All"],
   "pipeline_rows": PIPELINE_BY_REGION["All"],
   "region_kpis": KPI_BY_REGION["All"],
   "selected_run": None,
   "slo_target": 94,
   "operator_name": "Colab Builder",
   "notes": [{"note": "Click a run row to inspect it, then add triage notes here."}],
   "watchlist": WATCHLIST,
   "compact": False,
   "dark_mode": False,
}
with PrefabApp(
   title="Prefab Advanced Colab Tutorial",
   state=initial_state,
   css_class="max-w-7xl mx-auto p-6",
) as app:
   with Column(gap=6):
       with Row(justify="between", align="center", gap=4):
           with Column(gap=1):
               H2("Prefab Advanced Operations Dashboard")
               Muted(
                   "A complete Colab-ready tutorial: Python DSL, reactive state, "
                   "charts, tables, forms, conditionals, actions, Mermaid, and static export."
               )
           with Row(gap=2, align="center"):
               Badge(f"Region: {STATE.selected_region}", variant="info")
               Badge(f"Operator: {STATE.operator_name}", variant="secondary")
               Button(
                   "Docs",
                   variant="outline",
                   onClick=OpenLink("https://prefab.prefect.io/docs/welcome"),
               )
       with Alert(variant="info", icon="sparkles"):
           AlertTitle("What this app demonstrates")
           AlertDescription(
               "Everything below is composed in Python. The exported HTML runs client-side "
               "with no backend, while actions update Prefab state instantly."
           )
       with Card():
           with CardHeader():
               CardTitle("Interactive controls")
               CardDescription(
                   "Use state actions to swap datasets, adjust SLO targets, and personalize "
                   "the UI without JavaScript."
               )
           with CardContent():
               with Grid(columns={"sm": 1, "md": 2, "lg": 4}, gap=4):
                   with Column(gap=2):
                       Small("Region quick filters")
                       with Row(gap=2):
                           for region in REGIONS:
                               Button(
                                   region,
                                   size="sm",
                                   variant="secondary" if region != "All" else "default",
                                   onClick=REGION_ACTIONS[region],
                               )
                   with Column(gap=2):
                       Small("SLO target")
                       Slider(
                           value=STATE.slo_target,
                           min=80,
                           max=99,
                           step=0.5,
                           onChange=SetState("slo_target", EVENT),
                           gradient=True,
                       )
                       Muted(f"Target is {STATE.slo_target}% successful runs")
                   with Column(gap=2):
                       Small("Operator name")
                       Input(
                           value=STATE.operator_name,
                           placeholder="Your name",
                           on_change=SetState("operator_name", EVENT),
                       )
                   with Column(gap=2):
                       Small("Client-side toggles")
                       Switch(
                           value=STATE.compact,
                           label="Compact review mode",
                           onChange=ToggleState("compact"),
                       )
                       Switch(
                           value=STATE.dark_mode,
                           label="Dark-mode flag",
                           onChange=ToggleState("dark_mode"),
                       )
       with Tabs(value="overview"):
           with Tab("Overview", value="overview"):
               with Column(gap=5):
                   with Grid(columns={"sm": 1, "md": 2, "lg": 4}, gap=4):
                       Metric(
                           label="Runs",
                           value=STATE.region_kpis.runs,
                           description=f"Selected slice: {STATE.selected_region}",
                       )
                       Metric(
                           label="Success rate",
                           value=f"{STATE.region_kpis.success_rate}%",
                           delta=f"Target {STATE.slo_target}%",
                           trend="up",
                           trendSentiment="positive",
                       )
                       Metric(
                           label="Avg latency",
                           value=f"{STATE.region_kpis.avg_latency}s",
                           description="Weighted 30-day average",
                       )
                       Metric(
                           label="ROI index",
                           value=STATE.region_kpis.roi,
                           description="Revenue thousands per USD cost",
                       )
                   with Grid(columns={"sm": 1, "lg": 3}, gap=4):
                       with Card(css_class="lg:col-span-2"):
                           with CardHeader():
                               CardTitle("Reliability trend")
                               CardDescription(
                                   "Line chart bound to a reactive state key. "
                                   "Region buttons replace the whole data list."
                               )
                           with CardContent():
                               LineChart(
                                   data=STATE.line_rows,
                                   xAxis="date",
                                   series=[
                                       ChartSeries(dataKey="success_rate", label="Success %"),
                                       ChartSeries(dataKey="avg_latency", label="Avg latency"),
                                   ],
                                   height=330,
                                   curve="smooth",
                                   showDots=False,
                                   showLegend=True,
                               )
                       with Card():
                           with CardHeader():
                               CardTitle("SLO health")
                               CardDescription(
                                   "Progress and ring components read live KPI and target state."
                               )
                           with CardContent():
                               with Column(gap=4, align="center"):
                                   Ring(
                                       value=STATE.region_kpis.success_rate,
                                       target=STATE.slo_target,
                                       label=f"{STATE.region_kpis.success_rate}%",
                                       size="lg",
                                       gradient=True,
                                   )
                                   Progress(
                                       value=STATE.region_kpis.success_rate,
                                       target=STATE.slo_target,
                                       max=100,
                                       gradient=True,
                                   )
                                   Sparkline(
                                       data=STATE.region_kpis.spark,
                                       height=56,
                                       curve="smooth",
                                       fill=True,
                                   )
                                   with If(STATE.region_kpis.success_rate >= STATE.slo_target):
                                       Badge("Meeting target", variant="success")
                                   with Else():
                                       Badge("Below target", variant="warning")
                   with Grid(columns={"sm": 1, "lg": 2}, gap=4):
                       with Card():
                           with CardHeader():
                               CardTitle("Open issue status mix")
                           with CardContent():
                               PieChart(
                                   data=STATE.status_rows,
                                   dataKey="count",
                                   nameKey="state",
                                   innerRadius=55,
                                   showLegend=True,
                                   height=310,
                               )
                       with Card():
                           with CardHeader():
                               CardTitle("Affected pipelines")
                           with CardContent():
                               BarChart(
                                   data=STATE.pipeline_rows,
                                   xAxis="pipeline",
                                   series=[ChartSeries(dataKey="count", label="Open issues")],
                                   height=310,
                                   showLegend=False,
                               )
'''
</code></pre></div></div>



<p class="wp-block-paragraph">We define the initial application state and start building the Prefab dashboard layout. We create the dashboard header, region filters, SLO slider, operator input, toggles, and the main overview tab. We connect reactive state to metrics, line charts, progress indicators, rings, sparklines, pie charts, and bar charts so the interface updates dynamically.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">APP_CODE += r'''</code></pre></div></div>



<h2 id="h-run-explorer-and-diagnostics" class="wp-block-heading"><strong>Run Explorer and Diagnostics</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">           with Tab("Run Explorer", value="runs"):
               with Column(gap=4):
                   with Card():
                       with CardHeader():
                           CardTitle("Searchable sortable run table")
                           CardDescription(
                               "Click any row. Prefab stores the row object in state and "
                               "conditionally renders the detail panel."
                           )
                       with CardContent():
                           DataTable(
                               columns=[
                                   DataTableColumn(key="run_id", header="Run ID", sortable=True),
                                   DataTableColumn(key="date", header="Date", sortable=True),
                                   DataTableColumn(key="pipeline", header="Pipeline", sortable=True),
                                   DataTableColumn(key="owner", header="Owner", sortable=True),
                                   DataTableColumn(key="region", header="Region", sortable=True),
                                   DataTableColumn(key="state", header="State", sortable=True),
                                   DataTableColumn(key="priority", header="Priority", sortable=True),
                                   DataTableColumn(
                                       key="duration_s",
                                       header="Duration",
                                       sortable=True,
                                       align="right",
                                   ),
                                   DataTableColumn(
                                       key="cost_usd",
                                       header="Cost",
                                       sortable=True,
                                       align="right",
                                   ),
                               ],
                               rows=STATE.table_rows,
                               search=True,
                               paginated=True,
                               pageSize=12,
                               onRowClick=SetState("selected_run", EVENT),
                           )
                   with If(STATE.selected_run):
                       with Card():
                           with CardHeader():
                               CardTitle(f"Selected run: {STATE.selected_run.run_id}")
                               CardDescription(
                                   f"{STATE.selected_run.pipeline} in {STATE.selected_run.region}"
                               )
                           with CardContent():
                               with Grid(columns={"sm": 1, "md": 4}, gap=3):
                                   Metric(label="State", value=STATE.selected_run.state)
                                   Metric(label="Priority", value=STATE.selected_run.priority)
                                   Metric(
                                       label="Duration",
                                       value=f"{STATE.selected_run.duration_s}s",
                                   )
                                   Metric(
                                       label="SLA gap",
                                       value=f"{STATE.selected_run.sla_gap} min",
                                   )
                           with CardFooter():
                               Button(
                                   "Clear selection",
                                   variant="outline",
                                   onClick=SetState("selected_run", None),
                               )
           with Tab("Diagnostics", value="diagnostics"):
               with Grid(columns={"sm": 1, "lg": 2}, gap=4):
                   with Card():
                       with CardHeader():
                           CardTitle("Regional reliability map")
                           CardDescription(
                               "Scatter chart compares cost, latency, and success by region."
                           )
                       with CardContent():
                           ScatterChart(
                               data=SCATTER_ROWS,
                               xAxis="cost_usd",
                               yAxis="success_rate",
                               zAxis="avg_latency",
                               series=[ChartSeries(dataKey="region", label="Region")],
                               height=320,
                           )
                   with Card():
                       with CardHeader():
                           CardTitle("Balanced score radar")
                           CardDescription(
                               "Scores are normalized for visual comparison across regions."
                           )
                       with CardContent():
                           RadarChart(
                               data=RADAR_ROWS,
                               axisKey="metric",
                               series=[
                                   ChartSeries(dataKey="APAC"),
                                   ChartSeries(dataKey="EMEA"),
                                   ChartSeries(dataKey="NA"),
                                   ChartSeries(dataKey="LATAM"),
                               ],
                               height=320,
                           )
                   with Card(css_class="lg:col-span-2"):
                       with CardHeader():
                           CardTitle("P0/P1 watchlist")
                           CardDescription(
                               "ForEach repeats a mini-card for each high-priority item."
                           )
                       with CardContent():
                           with Grid(columns={"sm": 1, "md": 2, "lg": 4}, gap=3):
                               with ForEach("watchlist") as item:
                                   with Card():
                                       with CardHeader():
                                           with Row(justify="between", align="center"):
                                               CardTitle(item.run_id)
                                               Badge(item.priority, variant="destructive")
                                           CardDescription(item.pipeline)
                                       with CardContent():
                                           Text(
                                               f"{item.region} · {item.state} · "
                                               f"gap {item.sla_gap} min"
                                           )
'''
</code></pre></div></div>



<p class="wp-block-paragraph">We add the run explorer and diagnostics sections of the application. We build a searchable and sortable data table where we can click a row and inspect detailed run information using conditional rendering. We also add diagnostic visualizations such as scatter charts, radar charts, and a high-priority watchlist rendered through repeated Prefab cards.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">APP_CODE += r'''</code></pre></div></div>



<h2 id="h-exporting-static-html-dashboard" class="wp-block-heading"><strong>Exporting Static HTML Dashboard</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">          with Tab("Triage Notes", value="notes"):
               with Grid(columns={"sm": 1, "lg": 2}, gap=4):
                   with Card():
                       with CardHeader():
                           CardTitle("Add client-side notes")
                           CardDescription(
                               "The form appends submitted values to a state list. "
                               "No backend is required."
                           )
                       with CardContent():
                           with Form(
                               on_submit=[
                                   AppendState("notes", EVENT),
                                   ShowToast("Note added", variant="success"),
                               ]
                           ):
                               Input(
                                   name="note",
                                   placeholder="Example: Escalate APAC LLM Triage failures",
                                   required=True,
                               )
                               Button("Add note", buttonType="submit")
                       with CardFooter():
                           Button(
                               "Remove latest note",
                               variant="outline",
                               onClick=PopState("notes", -1),
                           )
                   with Card():
                       with CardHeader():
                           CardTitle("Notes in state")
                           CardDescription("ForEach renders each submitted note.")
                       with CardContent():
                           with Column(gap=2):
                               with ForEach("notes") as note:
                                   with Alert(variant="success", icon="check"):
                                       AlertDescription(note.note)
           with Tab("Architecture", value="architecture"):
               with Column(gap=4):
                   Markdown("""
### How this tutorial maps to real Prefab work
1. Build data in Python.
2. Compose UI components with `with` blocks.
3. Bind data and labels to reactive state using `STATE` / `Rx`.
4. Trigger `SetState`, `AppendState`, `ToggleState`, and `ShowToast` from UI events.
5. Export to HTML for static sharing, or serve locally with `prefab serve`.
""")
                   Mermaid("""
flowchart LR
   A[Python data and business logic] --> B[Prefab component tree]
   B --> C[JSON wire protocol]
   C --> D[Bundled React renderer]
   D --> E[Interactive dashboard]
   E --> F[Client-side state actions]
   F --> B
""")
                   Code(
                       """prefab export /content/prefab_advanced_tutorial_app.py -o /content/prefab_advanced_dashboard.html
prefab serve /content/prefab_advanced_tutorial_app.py --reload""",
                       language="bash",
                   )
'''
APP_PATH.write_text(APP_CODE)
print("Verifying Prefab installation...")
subprocess.check_call([sys.executable, "-m", "prefab_ui.cli", "version"])
print("\nExporting Prefab app to static HTML...")
subprocess.check_call([
   sys.executable,
   "-m",
   "prefab_ui.cli",
   "export",
   str(APP_PATH),
   "-o",
   str(HTML_PATH),
])
print(f"\nPython app written to: {APP_PATH}")
print(f"Static dashboard written to: {HTML_PATH}")
print("\nDownload links:")
display(FileLink(str(APP_PATH)))
display(FileLink(str(HTML_PATH)))
html_bytes = HTML_PATH.read_bytes()
encoded = base64.b64encode(html_bytes).decode("utf-8")
display(HTML(f"""
&lt;div style="font-family: system-ui, -apple-system, Segoe UI, sans-serif; margin: 12px 0;">
 &lt;h3 style="margin-bottom: 4px;">Prefab Advanced Dashboard Preview&lt;/h3>
 &lt;p style="margin-top: 0; color: #555;">
   Use the tabs, region buttons, SLO slider, table row clicks, and triage-note form inside the embedded app.
 &lt;/p>
&lt;/div>
&lt;iframe
 src="data:text/html;base64,{encoded}"
 width="100%"
 height="950"
 style="border: 1px solid #ddd; border-radius: 12px;"
>&lt;/iframe>
"""))
</code></pre></div></div>



<p class="wp-block-paragraph">We complete the dashboard by adding triage notes, architecture documentation, a Mermaid flowchart, and command examples. We write the accumulated Prefab code into a Python file, verify the installation, export the app as static HTML, and generate download links. We then embed the exported dashboard inside Colab using an iframe so we can interact with the finished application directly in the notebook.</p>



<h2 class="wp-block-heading"><strong>Conclusion</strong></h2>



<p class="wp-block-paragraph">In conclusion, we successfully created a complete Prefab-powered dashboard that combines Python data generation, reactive state management, interactive visual components, and static HTML export into a single practical workflow. We worked with filters, metrics, charts, data tables, conditionals, forms, notes, and architecture diagrams to understand how Prefab can support real-world dashboards and internal tools. By running the project in Google Colab, we kept the setup simple while still building an advanced application with a production-style UI.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Data%20Analysis/prefab_reactive_dashboard_tutorial_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">Full Codes with Notebook</a></strong><a href="https://github.com/NVlabs/SpatialClaw" target="_blank" rel="noreferrer noopener">.</a><strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/21/how-to-design-python-first-interactive-dashboards-with-prefab-reactive-ui-components-and-static-html-export/">How to Design Python-First Interactive Dashboards with Prefab Reactive UI Components and Static HTML Export</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80645</post-id>
	</item>
		<item>
		<title>The 7 Types of Agent Memory: A Technical Guide for AI Engineers</title>
		<link>https://www.marktechpost.com/2026/06/21/the-7-types-of-agent-memory-a-technical-guide-for-ai-engineers/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Sun, 21 Jun 2026 23:12:31 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Agents]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Uncategorized]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80639</guid>

					<description><![CDATA[<p>LLMs are stateless by default. Agent memory fixes that. This guide breaks down all 7 types — working, semantic, episodic, procedural, retrieval, parametric, and prospective. It covers what each stores, where it lives, and when to build it. Includes a comparison table and working Python code.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/21/the-7-types-of-agent-memory-a-technical-guide-for-ai-engineers/">The 7 Types of Agent Memory: A Technical Guide for AI Engineers</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Large language models are stateless by default. Each API call starts fresh. The model forgets your last message once the response returns. That is fine for a single question. It breaks the moment you build an agent.</p>



<p class="wp-block-paragraph">Agents plan, call tools, and run across many steps. They need to remember. Memory is the infrastructure that fixes this. It turns a stateless model into a system that retains context. That system can learn from experience and act over time.</p>



<h2 id="h-what-is-agent-memory" class="wp-block-heading"><strong>What is Agent Memory</strong></h2>



<p class="wp-block-paragraph">Memory is any mechanism that carries information across a model&#8217;s reasoning. Some of it lives inside the context window. Some of it lives outside, in databases or model weights. Each type stores a different class of information for a different duration.</p>



<p class="wp-block-paragraph">Memory varies by form and by time. Form means parametric, stored in weights, or non-parametric, stored as text. Time means short-term or long-term. The seven types below map onto those two axes.</p>



<h2 id="h-the-seven-types-of-agent-memory" class="wp-block-heading"><strong>The Seven Types of Agent Memory</strong></h2>



<p class="wp-block-paragraph"><strong>1. In-Context / Working Memory (Short-Term)</strong>: This is everything the model can currently see inside its context window. It includes the system prompt, recent messages, tool outputs, and reasoning steps. Think of it as RAM. It is fast and essential, but temporary and size-limited. Every other memory type competes for space here.</p>



<p class="wp-block-paragraph"><strong>2. Semantic Memory (Long-Term)</strong>: This is a persistent store of facts, preferences, and domain knowledge. It holds entries like &#8220;the user prefers Python over JavaScript.&#8221; The knowledge is decoupled from when it was learned. It is the agent&#8217;s organized encyclopedia about a user or topic.</p>



<p class="wp-block-paragraph"><strong>3. Episodic Memory (Long-Term)</strong>: This logs specific past events, full conversations, and task runs. It records what worked and what failed. The agent uses it to learn from experience. Systems like Reflexion and ExpeL write verbal post-mortems and store conclusions for future runs.</p>



<p class="wp-block-paragraph"><strong>4. Procedural Memory (Long-Term)</strong>: This is the agent&#8217;s knowledge of how to do things. It covers skills, tool usage patterns, workflows, and behavioral rules. A support agent handling its hundredth password reset does not re-reason the workflow. It executes a learned procedure instead.</p>



<p class="wp-block-paragraph"><strong>5. External / Retrieval Memory (Short-Term + Long-Term)</strong>: This is knowledge stored outside the model in a vector database. It is pulled into context at inference time using similarity search. This is RAG applied to agent history or documents. Retrieval quality becomes the bottleneck fast.</p>



<p class="wp-block-paragraph"><strong>6. Parametric Memory (Long-Term)</strong>: This is knowledge baked directly into the model&#8217;s weights during training. It holds language, reasoning patterns, and general world knowledge. The model does not look anything up. It generates from learned associations. The tradeoff is that this memory is frozen at training time.</p>



<p class="wp-block-paragraph"><strong>7. Prospective Memory (Short-Term + Long-Term)</strong>: This is the agent&#8217;s ability to remember future intentions and scheduled goals. It tracks things the agent planned but has not yet executed. It is critical for long-horizon and multi-step planning agents. Without it, an agent forgets its own commitments.</p>



<h2 id="h-side-by-side-how-the-seven-compare" class="wp-block-heading"><strong>Side-by-Side: How the Seven Compare</strong></h2>



<p class="wp-block-paragraph">The table below maps each type to its timescale, location, and typical implementation.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Memory type</th><th>Timescale</th><th>Where it lives</th><th>What it stores</th><th>Common implementation</th></tr></thead><tbody><tr><td>Working / In-context</td><td>Short-term</td><td>Context window</td><td>Prompt, messages, tool outputs</td><td>Native to the LLM</td></tr><tr><td>Semantic</td><td>Long-term</td><td>External store</td><td>Facts, preferences, domain knowledge</td><td>Vector DB or profile schema</td></tr><tr><td>Episodic</td><td>Long-term</td><td>External store</td><td>Past events, task runs, outcomes</td><td>Vector DB plus event logs</td></tr><tr><td>Procedural</td><td>Long-term</td><td>Prompt or weights</td><td>Skills, workflows, behavioral rules</td><td>System prompt or fine-tune</td></tr><tr><td>Retrieval / External</td><td>Both</td><td>Vector database</td><td>Documents, history chunks</td><td>RAG pipeline</td></tr><tr><td>Parametric</td><td>Long-term</td><td>Model weights</td><td>World knowledge, language, reasoning</td><td>Pre-training or fine-tuning</td></tr><tr><td>Prospective</td><td>Both</td><td>State store</td><td>Future intentions, scheduled goals</td><td>Task queue or scheduler</td></tr></tbody></table></figure>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- Marktechpost :: 7 Types of Agent Memory (interactive). Paste into a Custom HTML block. -->
<iframe id="mtp-mem-7K9-frame" title="The 7 Types of Agent Memory"
  style="width:100%;border:0;display:block;height:620px;overflow:hidden"
  scrolling="no" loading="lazy"
  srcdoc="&lt;!DOCTYPE html&gt;&lt;html&gt;&lt;head&gt;&lt;meta charset=&quot;utf-8&quot;&gt;&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width,initial-scale=1&quot;&gt;&lt;style&gt;html,body{margin:0;padding:0;background:#fff}&lt;/style&gt;&lt;/head&gt;&lt;body&gt;&lt;div id=&quot;mtp-mem-7K9&quot;&gt;
&lt;style&gt;
#mtp-mem-7K9 *{margin:0;padding:0;box-sizing:border-box}
#mtp-mem-7K9{
  --ink:#0a0a0a; --paper:#ffffff; --line:#0a0a0a; --soft:#777;
  --fill:#f2f2f2; --fill2:#e6e6e6; --hot:#0a0a0a;
  font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Helvetica,Arial,sans-serif;
  color:var(--ink); background:var(--paper);
  border:2px solid var(--line); max-width:880px; margin:0 auto;
  -webkit-font-smoothing:antialiased;
}
#mtp-mem-7K9 .mono{font-family:&quot;SF Mono&quot;,ui-monospace,&quot;Roboto Mono&quot;,Menlo,Consolas,monospace}
#mtp-mem-7K9 .hd{
  border-bottom:2px solid var(--line); padding:20px 22px 18px;
}
#mtp-mem-7K9 .kick{
  font-size:11px; letter-spacing:.18em; text-transform:uppercase;
  color:var(--soft); font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; margin-bottom:8px;
}
#mtp-mem-7K9 .ttl{font-size:25px; font-weight:800; line-height:1.05; letter-spacing:-.01em}
#mtp-mem-7K9 .sub{font-size:13.5px; color:#333; margin-top:9px; line-height:1.5; max-width:620px}

/* tabs */
#mtp-mem-7K9 .tabs{display:flex; border-bottom:2px solid var(--line)}
#mtp-mem-7K9 .tab{
  flex:1; padding:12px 8px; background:var(--paper); border:none; cursor:pointer;
  font-size:12px; font-weight:700; letter-spacing:.04em; text-transform:uppercase;
  color:var(--soft); font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace;
  border-right:2px solid var(--line); transition:background .12s,color .12s;
}
#mtp-mem-7K9 .tab:last-child{border-right:none}
#mtp-mem-7K9 .tab[aria-selected=&quot;true&quot;]{background:var(--ink); color:var(--paper)}
#mtp-mem-7K9 .tab:hover:not([aria-selected=&quot;true&quot;]){background:var(--fill)}

/* panes */
#mtp-mem-7K9 .pane{display:none; padding:22px}
#mtp-mem-7K9 .pane.on{display:block}

/* EXPLORE: stack + detail */
#mtp-mem-7K9 .grid{display:grid; grid-template-columns:1fr 1.1fr; gap:18px}
#mtp-mem-7K9 .stack{display:flex; flex-direction:column; gap:0; border:2px solid var(--line)}
#mtp-mem-7K9 .row{
  display:flex; align-items:center; gap:11px; padding:11px 13px; cursor:pointer;
  border-bottom:1px solid var(--line); background:var(--paper); text-align:left; width:100%;
  font-family:inherit; transition:background .1s;
}
#mtp-mem-7K9 .stack .row:last-child{border-bottom:none}
#mtp-mem-7K9 .row:hover{background:var(--fill)}
#mtp-mem-7K9 .row[aria-selected=&quot;true&quot;]{background:var(--ink); color:var(--paper)}
#mtp-mem-7K9 .row[aria-selected=&quot;true&quot;] .num{border-color:var(--paper); color:var(--paper)}
#mtp-mem-7K9 .row[aria-selected=&quot;true&quot;] .tag{border-color:var(--paper); color:var(--paper)}
#mtp-mem-7K9 .num{
  width:24px; height:24px; flex:0 0 24px; border:1.5px solid var(--ink); border-radius:50%;
  display:grid; place-items:center; font-size:12px; font-weight:800;
  font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace;
}
#mtp-mem-7K9 .rname{font-size:13.5px; font-weight:700; line-height:1.15; flex:1}
#mtp-mem-7K9 .tag{
  font-size:9.5px; letter-spacing:.05em; text-transform:uppercase; padding:2px 6px;
  border:1px solid var(--soft); color:var(--soft); border-radius:2px;
  font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; white-space:nowrap;
}

#mtp-mem-7K9 .detail{border:2px solid var(--line); padding:18px; min-height:300px; display:flex; flex-direction:column}
#mtp-mem-7K9 .dnum{
  font-size:11px; letter-spacing:.15em; color:var(--soft);
  font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; text-transform:uppercase; margin-bottom:6px;
}
#mtp-mem-7K9 .dname{font-size:20px; font-weight:800; line-height:1.1; margin-bottom:4px}
#mtp-mem-7K9 .dtime{
  display:inline-block; font-size:10px; letter-spacing:.06em; text-transform:uppercase;
  font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; border:1.5px solid var(--ink);
  padding:3px 8px; border-radius:2px; margin-bottom:14px;
}
#mtp-mem-7K9 .ddesc{font-size:13.5px; line-height:1.55; color:#222; margin-bottom:16px}
#mtp-mem-7K9 .meta{display:flex; flex-direction:column; gap:10px; margin-top:auto}
#mtp-mem-7K9 .mline{display:flex; gap:10px; font-size:12.5px; line-height:1.4; border-top:1px dashed #ccc; padding-top:10px}
#mtp-mem-7K9 .mk{
  flex:0 0 92px; font-size:10px; letter-spacing:.07em; text-transform:uppercase; color:var(--soft);
  font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; padding-top:1px;
}
#mtp-mem-7K9 .mv{flex:1; color:#111}
#mtp-mem-7K9 .mv code{background:var(--fill); padding:1px 5px; border:1px solid #ddd; font-size:11.5px}

/* SIMULATE */
#mtp-mem-7K9 .sim-intro{font-size:13px; color:#333; line-height:1.5; margin-bottom:14px}
#mtp-mem-7K9 .scen{display:flex; flex-wrap:wrap; gap:8px; margin-bottom:18px}
#mtp-mem-7K9 .schip{
  border:1.5px solid var(--ink); background:var(--paper); cursor:pointer;
  padding:8px 12px; font-size:12px; font-weight:600; font-family:inherit; border-radius:2px;
  transition:background .1s,color .1s;
}
#mtp-mem-7K9 .schip:hover{background:var(--fill)}
#mtp-mem-7K9 .schip[aria-selected=&quot;true&quot;]{background:var(--ink); color:var(--paper)}

#mtp-mem-7K9 .simbody{display:grid; grid-template-columns:1fr 1fr; gap:16px}
#mtp-mem-7K9 .simquery{
  border:2px solid var(--line); padding:14px; font-size:13px; line-height:1.5; background:var(--fill);
  grid-column:1 / -1;
}
#mtp-mem-7K9 .simquery .qlab{
  font-size:10px; letter-spacing:.12em; text-transform:uppercase; color:var(--soft);
  font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; display:block; margin-bottom:5px;
}
#mtp-mem-7K9 .simquery b{font-weight:700}
#mtp-mem-7K9 .barwrap{border:2px solid var(--line); padding:14px}
#mtp-mem-7K9 .barwrap h4{
  font-size:10px; letter-spacing:.1em; text-transform:uppercase; color:var(--soft);
  font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; margin-bottom:12px; font-weight:700;
}
#mtp-mem-7K9 .bar{display:flex; align-items:center; gap:9px; margin-bottom:8px}
#mtp-mem-7K9 .bar .bn{
  width:18px; height:18px; flex:0 0 18px; border:1.5px solid #bbb; border-radius:50%;
  font-size:10px; font-weight:800; display:grid; place-items:center; color:#bbb;
  font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; transition:all .25s;
}
#mtp-mem-7K9 .bar .bl{font-size:11.5px; color:#bbb; font-weight:600; flex:1; transition:color .25s}
#mtp-mem-7K9 .bar .bfill{
  width:0; height:7px; background:var(--ink); transition:width .5s ease; flex:0 0 auto; max-width:120px;
}
#mtp-mem-7K9 .bar.lit .bn{border-color:var(--ink); background:var(--ink); color:var(--paper)}
#mtp-mem-7K9 .bar.lit .bl{color:var(--ink)}
#mtp-mem-7K9 .bar.lit .bfill{width:120px}

#mtp-mem-7K9 .readout{border:2px solid var(--line); padding:14px; min-height:120px}
#mtp-mem-7K9 .readout h4{
  font-size:10px; letter-spacing:.1em; text-transform:uppercase; color:var(--soft);
  font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; margin-bottom:10px; font-weight:700;
}
#mtp-mem-7K9 .step{font-size:12.5px; line-height:1.5; padding:6px 0; border-bottom:1px dashed #ddd; opacity:0; transform:translateY(4px); transition:opacity .3s,transform .3s}
#mtp-mem-7K9 .step:last-child{border-bottom:none}
#mtp-mem-7K9 .step.show{opacity:1; transform:none}
#mtp-mem-7K9 .step b{font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; font-size:11px; background:var(--ink); color:var(--paper); padding:1px 5px; margin-right:5px}

#mtp-mem-7K9 .replay{
  margin-top:14px; border:1.5px solid var(--ink); background:var(--paper); cursor:pointer;
  padding:9px 16px; font-size:11px; font-weight:700; letter-spacing:.06em; text-transform:uppercase;
  font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; border-radius:2px; transition:background .1s,color .1s;
}
#mtp-mem-7K9 .replay:hover{background:var(--ink); color:var(--paper)}

#mtp-mem-7K9 .ft{
  border-top:2px solid var(--line); padding:13px 22px; display:flex; justify-content:space-between;
  align-items:center; gap:10px; flex-wrap:wrap;
}
#mtp-mem-7K9 .ft .brand{font-size:11px; font-weight:700; letter-spacing:.02em}
#mtp-mem-7K9 .ft .brand span{font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace; color:var(--soft); font-weight:400}
#mtp-mem-7K9 .ft .hint{font-size:10px; color:var(--soft); font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,monospace}

@media (max-width:640px){
  #mtp-mem-7K9 .grid{grid-template-columns:1fr}
  #mtp-mem-7K9 .simbody{grid-template-columns:1fr}
  #mtp-mem-7K9 .ttl{font-size:21px}
  #mtp-mem-7K9 .tab{font-size:10px; padding:11px 4px}
  #mtp-mem-7K9 .pane{padding:16px}
  #mtp-mem-7K9 .hd{padding:16px}
}
&lt;/style&gt;

&lt;div class=&quot;hd&quot;&gt;
  &lt;div class=&quot;kick&quot;&gt;Interactive · Agent Architecture&lt;/div&gt;
  &lt;div class=&quot;ttl&quot;&gt;The 7 Types of Agent Memory&lt;/div&gt;
  &lt;div class=&quot;sub&quot;&gt;Click any layer to see what it stores and where it lives. Then run a scenario to watch which memory types activate.&lt;/div&gt;
&lt;/div&gt;

&lt;div class=&quot;tabs&quot; role=&quot;tablist&quot;&gt;
  &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;true&quot; data-tab=&quot;explore&quot;&gt;Explore the 7 types&lt;/button&gt;
  &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-tab=&quot;simulate&quot;&gt;Run a scenario&lt;/button&gt;
&lt;/div&gt;

&lt;!-- EXPLORE --&gt;
&lt;div class=&quot;pane on&quot; data-pane=&quot;explore&quot;&gt;
  &lt;div class=&quot;grid&quot;&gt;
    &lt;div class=&quot;stack&quot; id=&quot;stk-7K9&quot; role=&quot;listbox&quot; aria-label=&quot;Memory types&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;detail&quot; id=&quot;det-7K9&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;!-- SIMULATE --&gt;
&lt;div class=&quot;pane&quot; data-pane=&quot;simulate&quot;&gt;
  &lt;div class=&quot;sim-intro&quot;&gt;Pick what the agent is asked to do. The memory stack lights up in the order each layer is used.&lt;/div&gt;
  &lt;div class=&quot;scen&quot; id=&quot;scen-7K9&quot; role=&quot;tablist&quot; aria-label=&quot;Scenarios&quot;&gt;&lt;/div&gt;
  &lt;div class=&quot;simbody&quot;&gt;
    &lt;div class=&quot;simquery&quot; id=&quot;sq-7K9&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;barwrap&quot;&gt;
      &lt;h4&gt;Memory stack&lt;/h4&gt;
      &lt;div id=&quot;bars-7K9&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;readout&quot;&gt;
      &lt;h4&gt;Execution trace&lt;/h4&gt;
      &lt;div id=&quot;steps-7K9&quot;&gt;&lt;/div&gt;
      &lt;button class=&quot;replay&quot; id=&quot;replay-7K9&quot;&gt;↻ Replay&lt;/button&gt;
    &lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;div class=&quot;ft&quot;&gt;
  &lt;div class=&quot;brand&quot;&gt;Marktechpost &lt;span&gt;/ agent memory explainer&lt;/span&gt;&lt;/div&gt;
  &lt;div class=&quot;hint&quot;&gt;7 layers · click to explore&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var root=document.getElementById('mtp-mem-7K9');
  if(!root||root.dataset.init)return; root.dataset.init='1';

  var MEM=[
    {n:1,name:&quot;In-Context / Working Memory&quot;,time:&quot;Short-term&quot;,
     desc:&quot;Everything the model can currently see inside its context window: the system prompt, recent messages, tool outputs, and reasoning steps.&quot;,
     where:&quot;Context window&quot;,stores:&quot;Prompt, messages, tool outputs&quot;,impl:&quot;Native to the LLM&quot;},
    {n:2,name:&quot;Semantic Memory&quot;,time:&quot;Long-term&quot;,
     desc:&quot;A persistent store of facts, preferences, and domain knowledge about a user or topic, decoupled from when it was learned.&quot;,
     where:&quot;External store&quot;,stores:&quot;Facts, preferences&quot;,impl:&quot;Vector DB / profile&quot;},
    {n:3,name:&quot;Episodic Memory&quot;,time:&quot;Long-term&quot;,
     desc:&quot;A log of specific past events, full conversations, and task runs — what worked and what failed — so the agent learns from experience.&quot;,
     where:&quot;External store&quot;,stores:&quot;Past events, task runs&quot;,impl:&quot;Vector DB + event logs&quot;},
    {n:4,name:&quot;Procedural Memory&quot;,time:&quot;Long-term&quot;,
     desc:&quot;The agent's knowledge of how to do things: its skills, tool usage patterns, workflows, and behavioral rules.&quot;,
     where:&quot;Prompt / weights&quot;,stores:&quot;Skills, workflows, rules&quot;,impl:&quot;System prompt / fine-tune&quot;},
    {n:5,name:&quot;External / Retrieval Memory&quot;,time:&quot;Short + Long-term&quot;,
     desc:&quot;Knowledge stored outside the model in a vector database, pulled into context at inference time based on similarity search.&quot;,
     where:&quot;Vector database&quot;,stores:&quot;Documents, history chunks&quot;,impl:&quot;RAG pipeline&quot;},
    {n:6,name:&quot;Parametric Memory&quot;,time:&quot;Long-term&quot;,
     desc:&quot;Knowledge baked directly into the model's weights during training: language, reasoning patterns, and general world knowledge.&quot;,
     where:&quot;Model weights&quot;,stores:&quot;World knowledge, language&quot;,impl:&quot;Pre-training / fine-tune&quot;},
    {n:7,name:&quot;Prospective Memory&quot;,time:&quot;Short + Long-term&quot;,
     desc:&quot;The agent's ability to remember future intentions and scheduled goals — things it planned but has not yet executed. Critical for long-horizon planning.&quot;,
     where:&quot;State store&quot;,stores:&quot;Future intentions, goals&quot;,impl:&quot;Task queue / scheduler&quot;}
  ];

  // ---- EXPLORE ----
  var stk=root.querySelector('#stk-7K9'), det=root.querySelector('#det-7K9'), sel=0;
  MEM.forEach(function(m,i){
    var b=document.createElement('button');
    b.className='row'; b.setAttribute('role','option'); b.setAttribute('aria-selected', i===0?'true':'false');
    b.innerHTML='&lt;span class=&quot;num&quot;&gt;'+m.n+'&lt;/span&gt;&lt;span class=&quot;rname&quot;&gt;'+m.name+'&lt;/span&gt;&lt;span class=&quot;tag&quot;&gt;'+m.time+'&lt;/span&gt;';
    b.addEventListener('click',function(){ sel=i; paint(); });
    stk.appendChild(b);
  });
  function paint(){
    var rows=stk.querySelectorAll('.row');
    rows.forEach(function(r,i){ r.setAttribute('aria-selected', i===sel?'true':'false'); });
    var m=MEM[sel];
    det.innerHTML=
      '&lt;div class=&quot;dnum&quot;&gt;Type '+m.n+' / 7&lt;/div&gt;'+
      '&lt;div class=&quot;dname&quot;&gt;'+m.name+'&lt;/div&gt;'+
      '&lt;div class=&quot;dtime&quot;&gt;'+m.time+'&lt;/div&gt;'+
      '&lt;div class=&quot;ddesc&quot;&gt;'+m.desc+'&lt;/div&gt;'+
      '&lt;div class=&quot;meta&quot;&gt;'+
        '&lt;div class=&quot;mline&quot;&gt;&lt;span class=&quot;mk&quot;&gt;Lives in&lt;/span&gt;&lt;span class=&quot;mv&quot;&gt;'+m.where+'&lt;/span&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;mline&quot;&gt;&lt;span class=&quot;mk&quot;&gt;Stores&lt;/span&gt;&lt;span class=&quot;mv&quot;&gt;'+m.stores+'&lt;/span&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;mline&quot;&gt;&lt;span class=&quot;mk&quot;&gt;Build with&lt;/span&gt;&lt;span class=&quot;mv&quot;&gt;&lt;code&gt;'+m.impl+'&lt;/code&gt;&lt;/span&gt;&lt;/div&gt;'+
      '&lt;/div&gt;';
  }
  paint();

  // ---- SIMULATE ----
  var SCEN=[
    {label:&quot;Suggest a recipe&quot;, q:&quot;User: &lt;b&gt;\u201cSuggest a dinner recipe for me.\u201d&lt;/b&gt;&quot;,
     trace:[
      [6,&quot;Parametric supplies cooking knowledge and language.&quot;],
      [2,&quot;Semantic recalls the saved preference: vegetarian.&quot;],
      [1,&quot;Working assembles the facts into the prompt.&quot;]
     ]},
    {label:&quot;Remember me next time&quot;, q:&quot;User: &lt;b&gt;\u201cI\u2019m allergic to gluten \u2014 remember that.\u201d&lt;/b&gt;&quot;,
     trace:[
      [1,&quot;Working holds the new statement in context.&quot;],
      [2,&quot;Semantic writes the fact for future sessions.&quot;]
     ]},
    {label:&quot;Book a trip&quot;, q:&quot;User: &lt;b&gt;\u201cBook me a flight to Tokyo next month.\u201d&lt;/b&gt;&quot;,
     trace:[
      [4,&quot;Procedural runs the flow: search, compare, reserve, confirm.&quot;],
      [5,&quot;Retrieval pulls live fares from the vector store.&quot;],
      [1,&quot;Working tracks the booking state across steps.&quot;]
     ]},
    {label:&quot;Improve from past runs&quot;, q:&quot;Agent: &lt;b&gt;Prepare a market analysis report.&lt;/b&gt;&quot;,
     trace:[
      [3,&quot;Episodic recalls which sources were reliable before.&quot;],
      [4,&quot;Procedural drives section order: sizing, landscape, risk.&quot;],
      [1,&quot;Working composes the draft in context.&quot;]
     ]},
    {label:&quot;Long-horizon project&quot;, q:&quot;Agent: &lt;b&gt;Manage a week-long launch plan.&lt;/b&gt;&quot;,
     trace:[
      [7,&quot;Prospective stores the intent: send Friday report.&quot;],
      [3,&quot;Episodic logs each completed step and outcome.&quot;],
      [1,&quot;Working surfaces today\u2019s task into context.&quot;]
     ]}
  ];
  var scenWrap=root.querySelector('#scen-7K9'), sq=root.querySelector('#sq-7K9'),
      bars=root.querySelector('#bars-7K9'), steps=root.querySelector('#steps-7K9'),
      replay=root.querySelector('#replay-7K9'), curScen=0, timers=[];

  SCEN.forEach(function(s,i){
    var c=document.createElement('button');
    c.className='schip'; c.setAttribute('role','tab'); c.setAttribute('aria-selected', i===0?'true':'false');
    c.textContent=s.label;
    c.addEventListener('click',function(){ curScen=i; selScen(); run(); });
    scenWrap.appendChild(c);
  });
  // build bar rows once
  MEM.forEach(function(m){
    var d=document.createElement('div'); d.className='bar'; d.dataset.n=m.n;
    d.innerHTML='&lt;span class=&quot;bn&quot;&gt;'+m.n+'&lt;/span&gt;&lt;span class=&quot;bl&quot;&gt;'+m.name.replace(' Memory','').replace(' / Retrieval','/Retrieval')+'&lt;/span&gt;&lt;span class=&quot;bfill&quot;&gt;&lt;/span&gt;';
    bars.appendChild(d);
  });
  function clearTimers(){ timers.forEach(clearTimeout); timers=[]; }
  function selScen(){
    scenWrap.querySelectorAll('.schip').forEach(function(c,i){ c.setAttribute('aria-selected', i===curScen?'true':'false'); });
  }
  function resetBars(){
    bars.querySelectorAll('.bar').forEach(function(b){ b.classList.remove('lit'); });
  }
  function run(){
    clearTimers();
    var s=SCEN[curScen];
    sq.innerHTML='&lt;span class=&quot;qlab&quot;&gt;Scenario&lt;/span&gt;'+s.q;
    resetBars(); steps.innerHTML='';
    s.trace.forEach(function(t,idx){
      var n=t[0], txt=t[1];
      // step element
      var st=document.createElement('div'); st.className='step';
      st.innerHTML='&lt;b&gt;'+n+'&lt;/b&gt;'+txt; steps.appendChild(st);
      timers.push(setTimeout(function(){
        var bar=bars.querySelector('.bar[data-n=&quot;'+n+'&quot;]');
        if(bar) bar.classList.add('lit');
        st.classList.add('show');
      }, 350*(idx+1)));
    });
  }
  replay.addEventListener('click',run);
  selScen(); run();

  // ---- TABS ----
  root.querySelectorAll('.tab').forEach(function(t){
    t.addEventListener('click',function(){
      root.querySelectorAll('.tab').forEach(function(x){ x.setAttribute('aria-selected','false'); });
      t.setAttribute('aria-selected','true');
      var name=t.dataset.tab;
      root.querySelectorAll('.pane').forEach(function(p){ p.classList.toggle('on', p.dataset.pane===name); });
      if(name==='simulate'){ run(); }
    });
  });
})();
&lt;/script&gt;

&lt;script&gt;
(function(){
  var EL=document.getElementById('mtp-mem-7K9');
  function ping(){
    if(!EL)return;
    var h=EL.offsetHeight+40;
    parent.postMessage({mtpMem7K9:true, height:h}, '*');
  }
  window.addEventListener('load', function(){ ping(); setTimeout(ping,300); setTimeout(ping,900); });
  if(window.ResizeObserver &amp;&amp; EL){ new ResizeObserver(ping).observe(EL); }
  EL &amp;&amp; EL.addEventListener('click', function(){ setTimeout(ping,60); setTimeout(ping,650); });
})();
&lt;/script&gt;
&lt;/body&gt;&lt;/html&gt;"></iframe>
<script>
(function(){
  var f=document.getElementById("mtp-mem-7K9-frame");
  window.addEventListener("message",function(e){
    if(e&&e.data&&e.data.mtpMem7K9&&e.data.height){ f.style.height=e.data.height+"px"; }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"></p>



<h2 id="h-use-cases-which-memory-solves-which-problem" class="wp-block-heading"><strong>Use Cases: Which Memory Solves Which Problem</strong></h2>



<p class="wp-block-paragraph">Each type answers a concrete product need. Map the need to the memory.</p>



<ul class="wp-block-list">
<li>A coding assistant inside one session uses working memory. It tracks the open files and recent edits in context. Close the session and that state is gone.</li>



<li>A personal assistant that remembers you needs semantic memory. It stores &#8220;allergic to gluten&#8221; and recalls it next week. The fact survives across sessions.</li>



<li>A research agent that improves over time needs episodic memory. It recalls that risk sections landed well last month. It repeats what worked and avoids what failed.</li>



<li>A travel-booking agent needs procedural memory. It knows the flow: search flights, compare, reserve, confirm. The sequence is a learned skill, not a fresh plan.</li>



<li>A documentation chatbot needs retrieval memory. It embeds the docs and pulls relevant chunks per query. The answer stays grounded in retrieved text.</li>



<li>A long-horizon agent managing a week-long project needs prospective memory. It remembers to send the Friday report. The intention persists until execution.</li>
</ul>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<h2 id="h-a-combined-example-all-seven-in-one-agent" class="wp-block-heading"><strong>A Combined Example: All Seven in One Agent</strong></h2>



<p class="wp-block-paragraph">Consider an autonomous market-analysis agent. One task exercises every memory type at once.</p>



<p class="wp-block-paragraph">Parametric memory supplies the base reasoning and language. Retrieval memory pulls current market data from a vector store. Semantic memory provides the user&#8217;s preferred report format. Episodic memory recalls which sources proved reliable before. Procedural memory drives the section order: sizing, then landscape, then risk. Prospective memory schedules the follow-up draft for next week. Working memory assembles all of it into the active context.</p>



<p class="wp-block-paragraph">Remove any one layer and the agent gets weaker. Each handles a job the others cannot.</p>



<h2 id="h-implementation-a-minimal-memory-stack" class="wp-block-heading"><strong>Implementation: A Minimal Memory Stack</strong></h2>



<p class="wp-block-paragraph">Here is a stripped-down sketch in Python. It shows working, semantic, episodic, and procedural memory as separate stores.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">from datetime import datetime

# Semantic memory: durable facts about the user
semantic_memory = {"diet": "vegetarian", "language_pref": "Python"}

# Episodic memory: a log of past events and outcomes
episodic_memory = [
    {"timestamp": datetime.now(),
     "event": "recipe_request",
     "result": "user liked a 20-minute meal"},
]

# Procedural memory: skills the agent can execute
def suggest_recipe(diet):
    return f"a quick {diet} recipe"

procedural_memory = {"suggest_recipe": suggest_recipe}

# Working memory: assembled fresh for each inference call
def build_context(query):
    diet = semantic_memory["diet"]
    last = episodic_memory[-1]["result"]
    skill = procedural_memory["suggest_recipe"]
    return (
        f"Query: {query}\n"
        f"Semantic: user is {diet}\n"
        f"Episodic: last time, {last}\n"
        f"Procedural: returning {skill(diet)}"
    )

print(build_context("suggest dinner"))</code></pre></div></div>



<p class="wp-block-paragraph">In production, the long-term stores move to a vector database. The pattern stays the same. Write to long-term memory, retrieve into working memory, then reason.</p>



<h2 id="h-how-to-layer-them-a-practical-build-order" class="wp-block-heading"><strong>How to Layer Them: A Practical Build Order</strong></h2>



<p class="wp-block-paragraph">Do not build all seven at once. Add memory only when a real need justifies the complexity.</p>



<ul class="wp-block-list">
<li>Start with working memory. It ships with the model. Most simple agents need nothing more.</li>



<li>Add semantic memory when users expect the agent to remember them across sessions. This is the first long-term layer most products require.</li>



<li>Layer in episodic, procedural, and prospective memory later. Add them only when your agent must plan ahead, learn from failure, and adapt over time.</li>



<li>Parametric and retrieval memory are often already present. Parametric memory is the base model itself. Retrieval memory arrives the moment you add RAG.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"><strong>Sources:</strong> CoALA framework (Princeton, arXiv:2309.02427); &#8220;Memory in the Age of AI Agents&#8221; survey (arXiv:2512.13564); &#8220;From Human Memory to AI Memory&#8221; survey (arXiv:2504.15965); LangChain LangMem, MongoDB, Redis, and Neo4j agent-memory documentation; original concept notes on the seven memory types.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/21/the-7-types-of-agent-memory-a-technical-guide-for-ai-engineers/">The 7 Types of Agent Memory: A Technical Guide for AI Engineers</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80639</post-id>
	</item>
		<item>
		<title>Crawlee for Python: Build a Web Crawling Pipeline with Robots Handling, Link Graphs, and RAG Chunk Export</title>
		<link>https://www.marktechpost.com/2026/06/20/crawlee-for-python-build-a-web-crawling-pipeline-with-robots-handling-link-graphs-and-rag-chunk-export/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Sun, 21 Jun 2026 06:52:10 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80636</guid>

					<description><![CDATA[<p>In this tutorial, we build a complete Crawlee for Python workflow from setup to AI-ready output. We generate a local demo website, then crawl it with BeautifulSoupCrawler, ParselCrawler, and PlaywrightCrawler. We extract titles, metadata, product fields, and JavaScript-rendered cards, and capture full-page screenshots. We then normalize the data, build a link graph, and export JSON, CSV, and RAG-ready JSONL chunks.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/20/crawlee-for-python-build-a-web-crawling-pipeline-with-robots-handling-link-graphs-and-rag-chunk-export/">Crawlee for Python: Build a Web Crawling Pipeline with Robots Handling, Link Graphs, and RAG Chunk Export</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this tutorial, we build a full<a href="https://github.com/apify/crawlee-python"> <strong>Crawlee-for-Python</strong></a> workflow that covers environment setup, local website generation, static crawling, dynamic crawling, structured extraction, and downstream data processing. We begin by configuring a compatible Crawlee runtime with pinned Pydantic support, Playwright browser installation, persistent storage directories, and Colab-safe execution handling. We then generate a realistic local demo website containing product pages, documentation pages, blog content, internal links, robots.txt rules, JSON-LD metadata, and JavaScript-rendered catalog items. Using BeautifulSoupCrawler, we perform fast recursive HTML crawling and extract page titles, metadata, text previews, outgoing links, product attributes, documentation headings, code blocks, and blog tags. With ParselCrawler, we run precise CSS- and XPath-based extraction on product detail pages. With PlaywrightCrawler, we render JavaScript content in a headless Chromium browser, wait for dynamic DOM elements to appear, extract client-side data, and capture full-page screenshots.</p>



<h2 id="h-setting-up-the-crawlee-python-runtime-and-helpers" class="wp-block-heading"><strong>Setting Up the Crawlee Python Runtime and Helpers</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import os
import sys
import re
import csv
import json
import time
import math
import shutil
import socket
import hashlib
import asyncio
import textwrap
import subprocess
import threading
from pathlib import Path
from functools import partial
from http.server import ThreadingHTTPServer, SimpleHTTPRequestHandler
from importlib.metadata import version, PackageNotFoundError
SETUP_SENTINEL = "/content/.crawlee_python_tutorial_setup_done_v2"
def sh(command, check=True, quiet=False):
   print(f"\n$ {command}")
   result = subprocess.run(
       command,
       shell=True,
       text=True,
       stdout=subprocess.PIPE,
       stderr=subprocess.STDOUT,
   )
   if not quiet and result.stdout:
       print(result.stdout[-5000:])
   if check and result.returncode != 0:
       raise RuntimeError(f"Command failed with exit code {result.returncode}: {command}")
   return result.returncode == 0
def package_version(package_name):
   try:
       return version(package_name)
   except PackageNotFoundError:
       return None
def is_good_pydantic_version(v):
   if not v:
       return False
   m = re.match(r"^(\d+)\.(\d+)", v)
   if not m:
       return False
   major, minor = int(m.group(1)), int(m.group(2))
   return major == 2 and minor == 11
current_crawlee = package_version("crawlee")
current_pydantic = package_version("pydantic")
needs_setup = (
   not os.path.exists(SETUP_SENTINEL)
   or current_crawlee is None
   or not is_good_pydantic_version(current_pydantic)
)
if needs_setup:
   print("PHASE 1: Installing compatible Crawlee + Pydantic + Playwright dependencies.")
   print("After this finishes, Colab will restart automatically. Then run this same cell again.")
   sh(f'{sys.executable} -m pip uninstall -y crawlee pydantic pydantic-core', check=False)
   sh(
       f'{sys.executable} -m pip install -q -U '
       f'"pydantic>=2.11,&lt;2.12" '
       f'"crawlee[all]" '
       f'pandas matplotlib networkx nest_asyncio beautifulsoup4 parsel'
   )
   sh(f'{sys.executable} -m playwright install --with-deps chromium', check=False)
   Path(SETUP_SENTINEL).write_text("done", encoding="utf-8")
   print("\nInstalled versions:")
   sh(f'{sys.executable} -m pip show crawlee pydantic pydantic-core', check=False)
   try:
       import google.colab
       print("\nRestarting Colab runtime now. After it reconnects, run this same cell again.")
       os.kill(os.getpid(), 9)
   except Exception:
       raise SystemExit("Setup complete. Restart the runtime/kernel manually, then run this cell again.")
print("PHASE 2: Dependencies are ready. Running the Crawlee tutorial.")
import pandas as pd
import matplotlib.pyplot as plt
import networkx as nx
import nest_asyncio
nest_asyncio.apply()
TUTORIAL_ROOT = Path("/content/crawlee_python_advanced_tutorial")
SITE_DIR = TUTORIAL_ROOT / "demo_site"
OUTPUT_DIR = TUTORIAL_ROOT / "outputs"
STORAGE_DIR = TUTORIAL_ROOT / "crawlee_storage"
SCREENSHOT_DIR = OUTPUT_DIR / "screenshots"
for path in [SITE_DIR, OUTPUT_DIR, STORAGE_DIR]:
   if path.exists():
       shutil.rmtree(path)
for path in [SITE_DIR, OUTPUT_DIR, STORAGE_DIR, SCREENSHOT_DIR]:
   path.mkdir(parents=True, exist_ok=True)
os.environ["CRAWLEE_STORAGE_DIR"] = str(STORAGE_DIR)
os.environ["CRAWLEE_LOG_LEVEL"] = "INFO"
os.environ["CRAWLEE_PURGE_ON_START"] = "true"
from crawlee import Glob, ConcurrencySettings
from crawlee.crawlers import (
   BeautifulSoupCrawler,
   BeautifulSoupCrawlingContext,
   ParselCrawler,
   ParselCrawlingContext,
   PlaywrightCrawler,
   PlaywrightCrawlingContext,
)
try:
   import crawlee
   print("Crawlee version:", crawlee.__version__)
except Exception:
   print("Crawlee imported successfully.")
print("Pydantic version:", package_version("pydantic"))
def safe_slug(value):
   value = re.sub(r"[^a-zA-Z0-9]+", "-", str(value)).strip("-").lower()
   return value or "item"
def money_to_float(value):
   if value is None:
       return None
   cleaned = re.sub(r"[^0-9.]", "", str(value))
   return float(cleaned) if cleaned else None
def normalize_text(value, max_len=None):
   value = re.sub(r"\s+", " ", value or "").strip()
   return value[:max_len] if max_len else value
def write_file(path, content):
   path = Path(path)
   path.parent.mkdir(parents=True, exist_ok=True)
   path.write_text(textwrap.dedent(content).strip() + "\n", encoding="utf-8")
</code></pre></div></div>



<p class="wp-block-paragraph">We begin by preparing the complete Colab runtime for the Crawlee tutorial. We install compatible versions of Crawlee, Pydantic, Playwright, and the required analysis libraries, and handle the automatic restart required after setup. We then configure storage folders, environment variables, crawler imports, and helper functions to ensure the rest of the workflow runs smoothly.</p>



<h2 class="wp-block-heading"><strong>Generating the Demo Website and Product Catalog</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">PRODUCTS = [
   {
       "sku": "CRW-101",
       "name": "Crawler Reliability Kit",
       "category": "automation",
       "price": 149.0,
       "rating": 4.8,
       "stock": 18,
       "features": ["retry policy", "queue replay", "structured logs"],
       "related": ["CRW-202", "CRW-303"],
   },
   {
       "sku": "CRW-202",
       "name": "Playwright Rendering Pack",
       "category": "browser",
       "price": 249.0,
       "rating": 4.7,
       "stock": 9,
       "features": ["headless chromium", "screenshots", "dynamic DOM extraction"],
       "related": ["CRW-101", "CRW-404"],
   },
   {
       "sku": "CRW-303",
       "name": "RAG Extraction Bundle",
       "category": "ai-data",
       "price": 199.0,
       "rating": 4.9,
       "stock": 13,
       "features": ["clean text chunks", "metadata capture", "JSONL export"],
       "related": ["CRW-101", "CRW-505"],
   },
   {
       "sku": "CRW-404",
       "name": "Anti-Fragile Session Toolkit",
       "category": "resilience",
       "price": 299.0,
       "rating": 4.6,
       "stock": 5,
       "features": ["session rotation", "state recovery", "graceful failures"],
       "related": ["CRW-202", "CRW-505"],
   },
   {
       "sku": "CRW-505",
       "name": "Data Export Control Plane",
       "category": "storage",
       "price": 179.0,
       "rating": 4.5,
       "stock": 21,
       "features": ["datasets", "key-value store", "CSV and JSON export"],
       "related": ["CRW-303", "CRW-404"],
   },
]
def layout(title, body, extra_head="", extra_script=""):
   css = """
   &lt;style>
     body {
       font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
       margin: 0;
       background: #f7f7fb;
       color: #1f2430;
     }
     header {
       background: #202638;
       color: white;
       padding: 28px 40px;
     }
     nav a {
       color: #dbe7ff;
       margin-right: 18px;
       text-decoration: none;
       font-weight: 600;
     }
     main {
       max-width: 1050px;
       margin: 0 auto;
       padding: 32px;
     }
     .grid {
       display: grid;
       grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
       gap: 18px;
     }
     .card, article, .panel {
       background: white;
       border: 1px solid #e5e7ef;
       border-radius: 16px;
       padding: 20px;
       box-shadow: 0 8px 25px rgba(20, 30, 60, 0.05);
     }
     .price {
       font-size: 1.3rem;
       font-weight: 800;
     }
     .tag {
       display: inline-block;
       background: #edf2ff;
       border: 1px solid #d6e0ff;
       border-radius: 999px;
       padding: 4px 10px;
       margin: 3px;
       font-size: 0.82rem;
     }
     .stock-low {
       color: #b42318;
       font-weight: 700;
     }
     .stock-ok {
       color: #067647;
       font-weight: 700;
     }
     code, pre {
       background: #111827;
       color: #d1fae5;
       border-radius: 10px;
     }
     pre {
       padding: 16px;
       overflow-x: auto;
     }
     footer {
       padding: 30px 40px;
       color: #606779;
     }
   &lt;/style>
   """
   return f"""
   &lt;!doctype html>
   &lt;html lang="en">
     &lt;head>
       &lt;meta charset="utf-8">
       &lt;meta name="viewport" content="width=device-width, initial-scale=1">
       &lt;meta name="description" content="{title} page for a Crawlee Python tutorial demo website.">
       &lt;title>{title}&lt;/title>
       {css}
       {extra_head}
     &lt;/head>
     &lt;body>
       &lt;header>
         &lt;h1>{title}&lt;/h1>
         &lt;nav>
           &lt;a href="/index.html">Home&lt;/a>
           &lt;a href="/products/product-crw-101.html">Products&lt;/a>
           &lt;a href="/docs/getting-started.html">Docs&lt;/a>
           &lt;a href="/blog/crawling-at-scale.html">Blog&lt;/a>
           &lt;a href="/dynamic.html">Dynamic JS Page&lt;/a>
           &lt;a href="/admin/hidden.html">Admin&lt;/a>
         &lt;/nav>
       &lt;/header>
       &lt;main>{body}&lt;/main>
       &lt;footer>Local demo website generated for Crawlee Python advanced tutorial.&lt;/footer>
       {extra_script}
     &lt;/body>
   &lt;/html>
   """
def build_demo_site():
   write_file(
       SITE_DIR / "robots.txt",
       """
       User-agent: *
       Disallow: /admin/
       Allow: /
       """,
   )
   product_cards = []
   for product in PRODUCTS:
       product_cards.append(
           f"""
           &lt;div class="card product-teaser" data-sku="{product['sku']}" data-category="{product['category']}">
             &lt;h2>&lt;a href="/products/product-{safe_slug(product['sku'])}.html">{product['name']}&lt;/a>&lt;/h2>
             &lt;p>{product['category']} crawler module with rating {product['rating']}.&lt;/p>
             &lt;p class="price" data-price="{product['price']}">${product['price']:.2f}&lt;/p>
             &lt;p class="{'stock-low' if product['stock'] &lt; 10 else 'stock-ok'}">Stock: {product['stock']}&lt;/p>
           &lt;/div>
           """
       )
   write_file(
       SITE_DIR / "index.html",
       layout(
           "Crawlee Demo Commerce + Docs Hub",
           f"""
           &lt;section class="panel">
             &lt;h2>Why this site exists&lt;/h2>
             &lt;p>
               This local website gives us predictable pages for testing Crawlee without scraping a third-party website.
               We include static HTML pages, documentation pages, product detail pages, a blog article, robots.txt,
               and a JavaScript-rendered page.
             &lt;/p>
           &lt;/section>
           &lt;h2>Featured crawler modules&lt;/h2>
           &lt;section class="grid">
             {''.join(product_cards)}
           &lt;/section>
           &lt;section class="panel">
             &lt;h2>Internal links for recursive crawling&lt;/h2>
             &lt;ul>
               &lt;li>&lt;a href="/docs/getting-started.html">Getting started guide&lt;/a>&lt;/li>
               &lt;li>&lt;a href="/docs/advanced-routing.html">Advanced routing guide&lt;/a>&lt;/li>
               &lt;li>&lt;a href="/blog/crawling-at-scale.html">Crawling at scale article&lt;/a>&lt;/li>
               &lt;li>&lt;a href="/dynamic.html">JavaScript-rendered catalog&lt;/a>&lt;/li>
               &lt;li>&lt;a href="/admin/hidden.html">Admin page blocked by robots and crawler filters&lt;/a>&lt;/li>
             &lt;/ul>
           &lt;/section>
           """,
       ),
   )
   for product in PRODUCTS:
       related_links = "\n".join(
           f'&lt;li>&lt;a class="related-link" href="/products/product-{safe_slug(sku)}.html">{sku}&lt;/a>&lt;/li>'
           for sku in product["related"]
       )
       feature_list = "\n".join(f"&lt;li>{feature}&lt;/li>" for feature in product["features"])
       json_ld = json.dumps(
           {
               "@context": "https://schema.org",
               "@type": "Product",
               "sku": product["sku"],
               "name": product["name"],
               "category": product["category"],
               "offers": {
                   "@type": "Offer",
                   "price": product["price"],
                   "priceCurrency": "USD",
               },
               "aggregateRating": {
                   "@type": "AggregateRating",
                   "ratingValue": product["rating"],
               },
           },
           indent=2,
       )
       write_file(
           SITE_DIR / "products" / f"product-{safe_slug(product['sku'])}.html",
           layout(
               f"{product['name']} | Product Detail",
               f"""
               &lt;article class="product"
                        data-sku="{product['sku']}"
                        data-category="{product['category']}"
                        data-rating="{product['rating']}"
                        data-stock="{product['stock']}">
                 &lt;h2 class="product-title">{product['name']}&lt;/h2>
                 &lt;p class="sku">SKU: &lt;strong>{product['sku']}&lt;/strong>&lt;/p>
                 &lt;p class="category">Category: &lt;strong>{product['category']}&lt;/strong>&lt;/p>
                 &lt;p class="price" data-price="{product['price']}">${product['price']:.2f}&lt;/p>
                 &lt;p class="rating">Rating: {product['rating']} / 5&lt;/p>
                 &lt;p class="{'stock-low' if product['stock'] &lt; 10 else 'stock-ok'}">Stock: {product['stock']}&lt;/p>
                 &lt;h3>Features&lt;/h3>
                 &lt;ul class="features">{feature_list}&lt;/ul>
                 &lt;h3>Related modules&lt;/h3>
                 &lt;ul>{related_links}&lt;/ul>
               &lt;/article>
               &lt;script type="application/ld+json">{json_ld}&lt;/script>
               """,
           ),
       )
</code></pre></div></div>



<p class="wp-block-paragraph">We create a realistic product catalog that becomes the structured data source for our demo website. We define reusable HTML layout logic, styling, navigation, and page templates to make the local website look and behave like a small commercial and documentation portal. We then generate the homepage and product detail pages, including prices, ratings, stock levels, product features, related links, and JSON-LD metadata.</p>



<h2 class="wp-block-heading"><strong>Adding Docs, Blog, Dynamic, and Admin Pages</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">   write_file(
       SITE_DIR / "docs" / "getting-started.html",
       layout(
           "Getting Started with Reliable Crawlers",
           """
           &lt;article class="doc" data-doc-id="getting-started">
             &lt;h2>HTTP-first crawling strategy&lt;/h2>
             &lt;p>
               We start with HTTP crawlers because they are lightweight and efficient.
               Browser crawling is reserved for pages that need JavaScript rendering.
             &lt;/p>
             &lt;h2>Core extraction fields&lt;/h2>
             &lt;p>
               Each crawler extracts URL, title, page type, text summary, outgoing links, and page-specific metadata.
             &lt;/p>
             &lt;pre>&lt;code>crawler = BeautifulSoupCrawler(max_requests_per_crawl=20)&lt;/code>&lt;/pre>
             &lt;p>&lt;a href="/docs/advanced-routing.html">Next: advanced routing&lt;/a>&lt;/p>
           &lt;/article>
           """,
       ),
   )
   write_file(
       SITE_DIR / "docs" / "advanced-routing.html",
       layout(
           "Advanced Routing and Storage",
           """
           &lt;article class="doc" data-doc-id="advanced-routing">
             &lt;h2>Queue filtering&lt;/h2>
             &lt;p>
               We filter links to keep the crawl focused on the same local domain and skip admin pages.
             &lt;/p>
             &lt;h2>Storage design&lt;/h2>
             &lt;p>
               Structured rows go to datasets. Binary screenshots and snapshots go to a key-value store.
             &lt;/p>
             &lt;pre>&lt;code>await context.enqueue_links(include=[Glob("https://example.com/**")])&lt;/code>&lt;/pre>
             &lt;p>&lt;a href="/blog/crawling-at-scale.html">Read the scaling article&lt;/a>&lt;/p>
           &lt;/article>
           """,
       ),
   )
   write_file(
       SITE_DIR / "blog" / "crawling-at-scale.html",
       layout(
           "Crawling at Scale",
           """
           &lt;article class="blog-post" data-author="demo-team" data-reading-time="7">
             &lt;h2>Scaling crawler jobs without losing reliability&lt;/h2>
             &lt;p>
               Production crawlers need controlled concurrency, retry behavior, stable request queues,
               structured exports, and monitoring-ready output.
             &lt;/p>
             &lt;p>
               For AI data workflows, we also normalize text, preserve source URLs, create chunks,
               and record extraction provenance.
             &lt;/p>
             &lt;span class="tag">queues&lt;/span>
             &lt;span class="tag">datasets&lt;/span>
             &lt;span class="tag">rag&lt;/span>
             &lt;span class="tag">playwright&lt;/span>
           &lt;/article>
           """,
       ),
   )
   dynamic_items = json.dumps(
       [
           {
               "sku": "JS-900",
               "name": "Dynamic Inventory Scanner",
               "price": 329.0,
               "stock": 4,
               "desc": "Rendered only after JavaScript executes.",
           },
           {
               "sku": "JS-901",
               "name": "Client-Side Review Miner",
               "price": 279.0,
               "stock": 11,
               "desc": "Created by browser-side DOM manipulation.",
           },
           {
               "sku": "JS-902",
               "name": "Async Catalog Watcher",
               "price": 389.0,
               "stock": 7,
               "desc": "Useful for testing PlaywrightCrawler extraction.",
           },
       ],
       indent=2,
   )
   dynamic_script = f"""
   &lt;script>
     const dynamicItems = {dynamic_items};
     function renderItems() {{
       const root = document.querySelector("#dynamic-products");
       root.innerHTML = "";
       for (const item of dynamicItems) {{
         const card = document.createElement("div");
         card.className = "card js-card";
         card.dataset.sku = item.sku;
         card.dataset.price = item.price;
         card.dataset.stock = item.stock;
         card.innerHTML = `
           &lt;h3>${{item.name}}&lt;/h3>
           &lt;p class="desc">${{item.desc}}&lt;/p>
           &lt;p class="price">$${{item.price.toFixed(2)}}&lt;/p>
           &lt;p class="${{item.stock &lt; 8 ? "stock-low" : "stock-ok"}}">Stock: ${{item.stock}}&lt;/p>
         `;
         root.appendChild(card);
       }}
       document.querySelector("#render-status").textContent =
         "Rendered " + dynamicItems.length + " JavaScript items.";
     }}
     setTimeout(renderItems, 600);
   &lt;/script>
   """
   write_file(
       SITE_DIR / "dynamic.html",
       layout(
           "JavaScript Rendered Catalog",
           """
           &lt;section class="panel">
             &lt;h2>Dynamic content test&lt;/h2>
             &lt;p>
               A plain HTTP crawler can download this page, but it will not see the cards below until JavaScript runs.
               PlaywrightCrawler opens a real browser and extracts the rendered DOM.
             &lt;/p>
             &lt;p id="render-status">Waiting for JavaScript rendering...&lt;/p>
           &lt;/section>
           &lt;section id="dynamic-products" class="grid">&lt;/section>
           """,
           extra_script=dynamic_script,
       ),
   )
   write_file(
       SITE_DIR / "admin" / "hidden.html",
       layout(
           "Hidden Admin Page",
           """
           &lt;article class="panel">
             &lt;h2>This page should be skipped&lt;/h2>
             &lt;p>
               The crawler excludes this admin path to demonstrate control over the rawl scope 
             &lt;/p>
           &lt;/article>
           """,
       ),
   )
build_demo_site()
print(f"Demo site generated at: {SITE_DIR}")
class QuietHandler(SimpleHTTPRequestHandler):
   def log_message(self, format, *args):
       pass
def start_local_server(directory):
   probe = socket.socket()
   probe.bind(("127.0.0.1", 0))
   port = probe.getsockname()[1]
   probe.close()
   handler = partial(QuietHandler, directory=str(directory))
   httpd = ThreadingHTTPServer(("127.0.0.1", port), handler)
   thread = threading.Thread(target=httpd.serve_forever, daemon=True)
   thread.start()
   base_url = f"http://127.0.0.1:{port}"
   time.sleep(0.5)
   return httpd, base_url
def extract_json_ld(soup):
   blocks = []
   for script in soup.select('script[type="application/ld+json"]'):
       raw = script.string or script.get_text()
       if not raw:
           continue
       try:
           blocks.append(json.loads(raw))
       except Exception:
           blocks.append({"raw": raw})
   return blocks
def write_json(path, rows):
   path = Path(path)
   path.write_text(json.dumps(rows, ensure_ascii=False, indent=2), encoding="utf-8")
def write_csv(path, rows):
   path = Path(path)
   if not rows:
       path.write_text("", encoding="utf-8")
       return
   flattened = []
   for row in rows:
       flat = {}
       for key, value in row.items():
           if isinstance(value, (list, dict)):
               flat[key] = json.dumps(value, ensure_ascii=False)
           else:
               flat[key] = value
       flattened.append(flat)
   fieldnames = sorted({key for row in flattened for key in row.keys()})
   with path.open("w", newline="", encoding="utf-8") as f:
       writer = csv.DictWriter(f, fieldnames=fieldnames)
       writer.writeheader()
       writer.writerows(flattened)
</code></pre></div></div>



<p class="wp-block-paragraph">We expand the demo website by adding documentation pages, a blog article, a JavaScript-rendered catalog page, and an admin page intended to be excluded from crawling. We use these pages to test different crawling scenarios, including static HTML extraction, documentation parsing, blog metadata extraction, dynamic browser rendering, and crawl filtering. We also start a local HTTP server and define utilities to extract JSON-LD content and export crawl results to JSON and CSV.</p>



<h2 class="wp-block-heading"><strong>Static Crawling with BeautifulSoupCrawler and ParselCrawler</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">async def run_beautifulsoup_crawl(base_url):
   print("\n=== 1) BeautifulSoupCrawler: fast recursive HTTP crawl ===")
   rows = []
   crawler = BeautifulSoupCrawler(
       parser="html.parser",
       max_requests_per_crawl=30,
       max_request_retries=1,
       respect_robots_txt_file=True,
       concurrency_settings=ConcurrencySettings(
           desired_concurrency=4,
           max_concurrency=6,
       ),
   )
   @crawler.router.default_handler
   async def request_handler(context: BeautifulSoupCrawlingContext) -> None:
       soup = context.soup
       url = context.request.url
       title = normalize_text(soup.title.get_text(" ", strip=True) if soup.title else "")
       meta_description = ""
       meta_tag = soup.find("meta", attrs={"name": "description"})
       if meta_tag:
           meta_description = normalize_text(meta_tag.get("content", ""))
       out_links = []
       for a in soup.select("a[href]"):
           href = a.get("href")
           label = normalize_text(a.get_text(" ", strip=True), 120)
           out_links.append({"href": href, "label": label})
       page_text = normalize_text(soup.get_text(" ", strip=True), 1000)
       if "/products/" in url:
           page_type = "product"
       elif "/docs/" in url:
           page_type = "documentation"
       elif "/blog/" in url:
           page_type = "blog"
       elif "/dynamic" in url:
           page_type = "dynamic-shell"
       else:
           page_type = "index"
       row = {
           "source": "beautifulsoup-http",
           "url": url,
           "title": title,
           "page_type": page_type,
           "meta_description": meta_description,
           "text_preview": page_text,
           "out_links": out_links,
           "json_ld": extract_json_ld(soup),
           "extracted_at_unix": time.time(),
       }
       if page_type == "product":
           article = soup.select_one("article.product")
           if article:
               price_node = soup.select_one(".price")
               row["product"] = {
                   "sku": article.get("data-sku"),
                   "category": article.get("data-category"),
                   "name": normalize_text(
                       soup.select_one(".product-title").get_text(" ", strip=True)
                       if soup.select_one(".product-title")
                       else ""
                   ),
                   "price": money_to_float(price_node.get("data-price") if price_node else None),
                   "rating": float(article.get("data-rating")) if article.get("data-rating") else None,
                   "stock": int(article.get("data-stock")) if article.get("data-stock") else None,
                   "features": [
                       normalize_text(li.get_text(" ", strip=True))
                       for li in soup.select(".features li")
                   ],
               }
       if page_type == "documentation":
           row["doc"] = {
               "headings": [
                   normalize_text(h.get_text(" ", strip=True))
                   for h in soup.select("h2, h3")
               ],
               "code_blocks": [
                   normalize_text(code.get_text(" ", strip=True))
                   for code in soup.select("pre code")
               ],
           }
       if page_type == "blog":
           row["blog"] = {
               "author": soup.select_one(".blog-post").get("data-author") if soup.select_one(".blog-post") else None,
               "reading_time": soup.select_one(".blog-post").get("data-reading-time") if soup.select_one(".blog-post") else None,
               "tags": [
                   normalize_text(tag.get_text(" ", strip=True))
                   for tag in soup.select(".tag")
               ],
           }
       rows.append(row)
       await context.push_data(row)
       await context.enqueue_links(
           include=[Glob(f"{base_url}/**")],
           exclude=[
               Glob(f"{base_url}/admin/**"),
               Glob(f"{base_url}/dynamic.html"),
           ],
       )
   await crawler.run([f"{base_url}/index.html"])
   write_json(OUTPUT_DIR / "beautifulsoup_crawl.json", rows)
   write_csv(OUTPUT_DIR / "beautifulsoup_crawl.csv", rows)
   print(f"BeautifulSoup rows extracted: {len(rows)}")
   return rows
async def run_parsel_precision_crawl(base_url):
   print("\n=== 2) ParselCrawler: precise CSS/XPath extraction from product pages ===")
   rows = []
   product_urls = [
       f"{base_url}/products/product-{safe_slug(product['sku'])}.html"
       for product in PRODUCTS
   ]
   crawler = ParselCrawler(
       max_requests_per_crawl=len(product_urls),
       max_request_retries=1,
       concurrency_settings=ConcurrencySettings(
           desired_concurrency=5,
           max_concurrency=8,
       ),
   )
   @crawler.router.default_handler
   async def request_handler(context: ParselCrawlingContext) -> None:
       selector = context.selector
       title = selector.css("title::text").get()
       sku = selector.css("article.product::attr(data-sku)").get()
       category = selector.css("article.product::attr(data-category)").get()
       rating = selector.css("article.product::attr(data-rating)").get()
       stock = selector.css("article.product::attr(data-stock)").get()
       name = selector.css(".product-title::text").get()
       price = selector.css(".price::attr(data-price)").get()
       features = [
           normalize_text(feature)
           for feature in selector.css(".features li::text").getall()
       ]
       row = {
           "source": "parsel-precision",
           "url": context.request.url,
           "title": normalize_text(title),
           "sku": sku,
           "name": normalize_text(name),
           "category": category,
           "price": money_to_float(price),
           "rating": float(rating) if rating else None,
           "stock": int(stock) if stock else None,
           "features": features,
           "xpath_title": normalize_text(selector.xpath("//title/text()").get()),
       }
       rows.append(row)
       await context.push_data(row)
   await crawler.run(product_urls)
   write_json(OUTPUT_DIR / "parsel_products.json", rows)
   write_csv(OUTPUT_DIR / "parsel_products.csv", rows)
   print(f"Parsel product rows extracted: {len(rows)}")
   return rows
</code></pre></div></div>



<p class="wp-block-paragraph">We implement the static crawling part of the workflow using BeautifulSoupCrawler and ParselCrawler. With BeautifulSoupCrawler, we recursively crawl the local website and extract page titles, metadata, text previews, outgoing links, product details, documentation headings, code blocks, and blog tags. With ParselCrawler, we perform more targeted CSS and XPath extraction from product pages to collect clean product-level fields, including SKU, category, price, rating, stock, and features.</p>



<h2 class="wp-block-heading"><strong>Dynamic Rendering with PlaywrightCrawler and Link Graphs</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">async def run_playwright_dynamic_crawl(base_url):
   print("\n=== 3) PlaywrightCrawler: browser-rendered JavaScript crawl ===")
   rows = []
   crawler = PlaywrightCrawler(
       max_requests_per_crawl=2,
       max_request_retries=1,
       headless=True,
       browser_type="chromium",
       browser_launch_options={
           "args": ["--no-sandbox", "--disable-dev-shm-usage"],
       },
       goto_options={
           "wait_until": "domcontentloaded",
       },
       concurrency_settings=ConcurrencySettings(
           desired_concurrency=1,
           max_concurrency=2,
       ),
   )
   @crawler.router.default_handler
   async def request_handler(context: PlaywrightCrawlingContext) -> None:
       await context.page.wait_for_selector(".js-card", timeout=10000)
       cards = await context.page.locator(".js-card").evaluate_all(
           """
           (cards) => cards.map((card) => {
             const h3 = card.querySelector("h3");
             const desc = card.querySelector(".desc");
             const price = card.querySelector(".price");
             return {
               sku: card.dataset.sku,
               name: h3 ? h3.textContent.trim() : null,
               description: desc ? desc.textContent.trim() : null,
               price_text: price ? price.textContent.trim() : null,
               price: Number(card.dataset.price),
               stock: Number(card.dataset.stock),
               rendered_text: card.innerText.trim()
             };
           })
           """
       )
       screenshot_bytes = await context.page.screenshot(full_page=True)
       screenshot_path = SCREENSHOT_DIR / "dynamic_catalog_full_page.png"
       screenshot_path.write_bytes(screenshot_bytes)
       try:
           kvs = await context.get_key_value_store()
           await kvs.set_value(
               key="dynamic-catalog-full-page",
               value=screenshot_bytes,
               content_type="image/png",
           )
       except Exception as exc:
           print("Key-value store screenshot save skipped:", repr(exc))
       for card in cards:
           row = {
               **card,
               "source": "playwright-rendered-js",
               "url": context.request.url,
               "screenshot_path": str(screenshot_path),
               "extracted_at_unix": time.time(),
           }
           rows.append(row)
       await context.push_data(rows)
   try:
       await crawler.run([f"{base_url}/dynamic.html"])
   except Exception as exc:
       print("Playwright section failed gracefully.")
       print("Reason:", repr(exc))
   write_json(OUTPUT_DIR / "playwright_dynamic.json", rows)
   write_csv(OUTPUT_DIR / "playwright_dynamic.csv", rows)
   print(f"Playwright dynamic rows extracted: {len(rows)}")
   return rows
def flatten_products(rows):
   products = []
   for row in rows:
       if row.get("page_type") == "product" and isinstance(row.get("product"), dict):
           product = row["product"]
           products.append(
               {
                   "source": row.get("source"),
                   "url": row.get("url"),
                   "sku": product.get("sku"),
                   "name": product.get("name"),
                   "category": product.get("category"),
                   "price": product.get("price"),
                   "rating": product.get("rating"),
                   "stock": product.get("stock"),
                   "features": "; ".join(product.get("features", [])),
               }
           )
       elif row.get("source") == "parsel-precision":
           products.append(
               {
                   "source": row.get("source"),
                   "url": row.get("url"),
                   "sku": row.get("sku"),
                   "name": row.get("name"),
                   "category": row.get("category"),
                   "price": row.get("price"),
                   "rating": row.get("rating"),
                   "stock": row.get("stock"),
                   "features": "; ".join(row.get("features", [])),
               }
           )
       elif row.get("source") == "playwright-rendered-js":
           products.append(
               {
                   "source": row.get("source"),
                   "url": row.get("url"),
                   "sku": row.get("sku"),
                   "name": row.get("name"),
                   "category": "dynamic-js",
                   "price": row.get("price") or money_to_float(row.get("price_text")),
                   "rating": None,
                   "stock": row.get("stock"),
                   "features": row.get("description"),
               }
           )
   return products
def absolute_url(base_url, href):
   if not href:
       return None
   if href.startswith("http://") or href.startswith("https://"):
       return href
   if href.startswith("/"):
       return base_url + href
   return base_url + "/" + href
def build_link_graph(base_url, rows):
   graph = nx.DiGraph()
   for row in rows:
       src = row.get("url")
       if not src:
           continue
       graph.add_node(
           src,
           title=row.get("title", ""),
           page_type=row.get("page_type", ""),
       )
       for link in row.get("out_links", []) or []:
           dst = absolute_url(base_url, link.get("href"))
           if not dst:
               continue
           if "/admin/" in dst:
               continue
           graph.add_node(dst)
           graph.add_edge(src, dst, label=link.get("label", ""))
   return graph
</code></pre></div></div>



<p class="wp-block-paragraph">We handle dynamic content using PlaywrightCrawler, which opens the JavaScript-rendered page in a headless Chromium browser. We wait for client-side product cards to appear, extract their rendered fields, capture a full-page screenshot, and save the browser-based results for later analysis. We then define helper functions to normalize product records and build a directed link graph from the internal links discovered during crawling.</p>



<h2 class="wp-block-heading"><strong>Building AI-Ready Outputs and Running the Pipeline</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def make_rag_chunks(rows, max_chars=700):
   chunks = []
   for row in rows:
       text = (
           row.get("text_preview")
           or row.get("rendered_text")
           or row.get("description")
           or ""
       )
       text = normalize_text(text)
       if not text:
           continue
       sentences = re.split(r"(?&lt;=[.!?])\s+", text)
       current = ""
       for sentence in sentences:
           if len(current) + len(sentence) + 1 &lt;= max_chars:
               current = (current + " " + sentence).strip()
           else:
               if current:
                   chunks.append(
                       {
                           "chunk_id": hashlib.sha1(
                               (row.get("url", "") + current).encode()
                           ).hexdigest()[:12],
                           "url": row.get("url"),
                           "source": row.get("source"),
                           "page_type": row.get("page_type"),
                           "title": row.get("title") or row.get("name"),
                           "text": current,
                       }
                   )
               current = sentence
       if current:
           chunks.append(
               {
                   "chunk_id": hashlib.sha1(
                       (row.get("url", "") + current).encode()
                   ).hexdigest()[:12],
                   "url": row.get("url"),
                   "source": row.get("source"),
                   "page_type": row.get("page_type"),
                   "title": row.get("title") or row.get("name"),
                   "text": current,
               }
           )
   return chunks
def analyze_outputs(base_url, bs4_rows, parsel_rows, playwright_rows):
   all_rows = bs4_rows + parsel_rows + playwright_rows
   products = flatten_products(all_rows)
   crawl_df = pd.DataFrame(all_rows)
   product_df = pd.DataFrame(products)
   if not product_df.empty:
       product_df["price"] = pd.to_numeric(product_df["price"], errors="coerce")
       product_df["stock"] = pd.to_numeric(product_df["stock"], errors="coerce")
       product_df["rating"] = pd.to_numeric(product_df["rating"], errors="coerce")
       product_df["inventory_value"] = product_df["price"] * product_df["stock"]
   graph = build_link_graph(base_url, bs4_rows)
   graph_path = OUTPUT_DIR / "site_link_graph.graphml"
   if graph.number_of_nodes() > 0:
       nx.write_graphml(graph, graph_path)
   chunks = make_rag_chunks(all_rows)
   rag_path = OUTPUT_DIR / "rag_chunks.jsonl"
   with rag_path.open("w", encoding="utf-8") as f:
       for chunk in chunks:
           f.write(json.dumps(chunk, ensure_ascii=False) + "\n")
   crawl_json_path = OUTPUT_DIR / "combined_crawl_results.json"
   crawl_json_path.write_text(
       json.dumps(all_rows, ensure_ascii=False, indent=2),
       encoding="utf-8",
   )
   product_csv_path = OUTPUT_DIR / "normalized_product_catalog.csv"
   if not product_df.empty:
       product_df.to_csv(product_csv_path, index=False)
   price_plot_path = OUTPUT_DIR / "product_price_chart.png"
   if not product_df.empty and product_df["price"].notna().any():
       plot_df = product_df.dropna(subset=["price"]).copy()
       plot_df["label"] = plot_df["sku"].fillna("unknown") + "\n" + plot_df["source"].fillna("")
       ax = plot_df.plot(
           kind="bar",
           x="label",
           y="price",
           legend=False,
           figsize=(11, 5),
           title="Extracted Product Prices by Source",
       )
       ax.set_xlabel("Product / extraction source")
       ax.set_ylabel("Price")
       plt.xticks(rotation=35, ha="right")
       plt.tight_layout()
       plt.savefig(price_plot_path, dpi=160)
       plt.show()
   graph_stats = {
       "nodes": graph.number_of_nodes(),
       "edges": graph.number_of_edges(),
       "weakly_connected_components": (
           nx.number_weakly_connected_components(graph)
           if graph.number_of_nodes()
           else 0
       ),
   }
   if graph.number_of_nodes() > 0:
       in_degrees = dict(graph.in_degree())
       out_degrees = dict(graph.out_degree())
       graph_stats["top_in_degree"] = sorted(
           in_degrees.items(),
           key=lambda x: x[1],
           reverse=True,
       )[:5]
       graph_stats["top_out_degree"] = sorted(
           out_degrees.items(),
           key=lambda x: x[1],
           reverse=True,
       )[:5]
   summary = {
       "base_url": base_url,
       "rows_total": len(all_rows),
       "beautifulsoup_rows": len(bs4_rows),
       "parsel_rows": len(parsel_rows),
       "playwright_rows": len(playwright_rows),
       "products_total": len(product_df),
       "rag_chunks_total": len(chunks),
       "graph": graph_stats,
       "outputs": {
           "beautifulsoup_json": str(OUTPUT_DIR / "beautifulsoup_crawl.json"),
           "beautifulsoup_csv": str(OUTPUT_DIR / "beautifulsoup_crawl.csv"),
           "parsel_json": str(OUTPUT_DIR / "parsel_products.json"),
           "parsel_csv": str(OUTPUT_DIR / "parsel_products.csv"),
           "playwright_json": str(OUTPUT_DIR / "playwright_dynamic.json"),
           "playwright_csv": str(OUTPUT_DIR / "playwright_dynamic.csv"),
           "combined_json": str(crawl_json_path),
           "product_csv": str(product_csv_path) if product_csv_path.exists() else None,
           "rag_jsonl": str(rag_path),
           "graphml": str(graph_path) if graph_path.exists() else None,
           "price_plot": str(price_plot_path) if price_plot_path.exists() else None,
           "screenshots_dir": str(SCREENSHOT_DIR),
       },
   }
   summary_path = OUTPUT_DIR / "run_summary.md"
   summary_path.write_text(
       "# Crawlee Python Advanced Tutorial Run Summary\n\n"
       f"- Local demo site: `{base_url}`\n"
       f"- Total extracted rows: `{summary['rows_total']}`\n"
       f"- BeautifulSoup rows: `{summary['beautifulsoup_rows']}`\n"
       f"- Parsel rows: `{summary['parsel_rows']}`\n"
       f"- Playwright rows: `{summary['playwright_rows']}`\n"
       f"- Normalized products: `{summary['products_total']}`\n"
       f"- RAG chunks: `{summary['rag_chunks_total']}`\n"
       f"- Link graph nodes: `{graph_stats['nodes']}`\n"
       f"- Link graph edges: `{graph_stats['edges']}`\n\n"
       "## Output files\n\n"
       + "\n".join(f"- `{k}`: `{v}`" for k, v in summary["outputs"].items())
       + "\n",
       encoding="utf-8",
   )
   print("\n=== 4) Analysis summary ===")
   print(json.dumps(summary, indent=2, ensure_ascii=False))
   try:
       from IPython.display import display, Markdown, Image as IPImage
       display(Markdown("## Crawlee crawl preview"))
       if not crawl_df.empty:
           preview_cols = [
               col for col in ["source", "page_type", "title", "url"]
               if col in crawl_df.columns
           ]
           display(crawl_df[preview_cols].head(12))
       display(Markdown("## Normalized product catalog"))
       if not product_df.empty:
           display(product_df.head(20))
       if price_plot_path.exists():
           display(Markdown("## Product price chart"))
           display(IPImage(filename=str(price_plot_path)))
       screenshot_path = SCREENSHOT_DIR / "dynamic_catalog_full_page.png"
       if screenshot_path.exists():
           display(Markdown("## Playwright screenshot of JavaScript-rendered page"))
           display(IPImage(filename=str(screenshot_path)))
       display(Markdown(f"## Output directory\n`{OUTPUT_DIR}`"))
   except Exception as exc:
       print("Notebook display skipped:", repr(exc))
   return summary
async def main():
   httpd, base_url = start_local_server(SITE_DIR)
   print(f"\nLocal demo website is running at: {base_url}/index.html")
   try:
       bs4_rows = await run_beautifulsoup_crawl(base_url)
       parsel_rows = await run_parsel_precision_crawl(base_url)
       playwright_rows = await run_playwright_dynamic_crawl(base_url)
       summary = analyze_outputs(base_url, bs4_rows, parsel_rows, playwright_rows)
       return summary
   finally:
       httpd.shutdown()
       print("\nLocal demo server shut down.")
loop = asyncio.get_event_loop()
summary = loop.run_until_complete(main())
print("\nTutorial complete.")
print(f"All outputs are in: {OUTPUT_DIR}")
print("Key files:")
for file_path in sorted(OUTPUT_DIR.rglob("*")):
   if file_path.is_file():
       print(" -", file_path)
</code></pre></div></div>



<p class="wp-block-paragraph">We process the extracted crawl data into analysis-ready and AI-ready outputs. We create RAG-style JSONL chunks, combine all crawl results, build a normalized product catalog, generate a GraphML link graph, and visualize product prices with Matplotlib. Finally, we run the full pipeline end-to-end, display previews in the notebook, save all generated artifacts, and print the final output file paths.</p>



<h2 class="wp-block-heading"><strong>Conclusion</strong></h2>



<p class="wp-block-paragraph">In conclusion, we have a complete Crawlee-based pipeline for crawling and data engineering that converts a small website into structured, reusable datasets. We used crawl scoping, robots.txt handling, concurrency settings, link enqueuing, browser rendering, key-value storage, and dataset exports to simulate patterns used in production web crawling systems. We normalized the extracted product data, saved the crawl outputs as JSON and CSV, created GraphML link graphs with NetworkX, generated JSONL chunks for retrieval-augmented generation workflows, and visualized the extracted product prices with Matplotlib.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Agentic%20Workflows/crawlee_python_static_dynamic_web_crawling_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">Full Codes here</a></strong><a href="https://github.com/NVlabs/SpatialClaw" target="_blank" rel="noreferrer noopener">.</a><strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/20/crawlee-for-python-build-a-web-crawling-pipeline-with-robots-handling-link-graphs-and-rag-chunk-export/">Crawlee for Python: Build a Web Crawling Pipeline with Robots Handling, Link Graphs, and RAG Chunk Export</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80636</post-id>
	</item>
	</channel>
</rss>
