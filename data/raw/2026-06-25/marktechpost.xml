<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	 xmlns:media="http://search.yahoo.com/mrss/" >

<channel>
	<title>MarkTechPost</title>
	<atom:link href="https://www.marktechpost.com/feed/" rel="self" type="application/rss+xml" />
	<link>https://www.marktechpost.com/</link>
	<description>An Artificial Intelligence News Platform</description>
	<lastBuildDate>Wed, 24 Jun 2026 20:00:24 +0000</lastBuildDate>
	<language>en-US</language>
	<sy:updatePeriod>
	hourly	</sy:updatePeriod>
	<sy:updateFrequency>
	1	</sy:updateFrequency>
	<generator>https://wordpress.org/?v=7.0</generator>

<image>
	<url>https://www.marktechpost.com/wp-content/uploads/2022/04/cropped-Favicon-512-x-512-1-1-32x32.png</url>
	<title>MarkTechPost</title>
	<link>https://www.marktechpost.com/</link>
	<width>32</width>
	<height>32</height>
</image> 
<site xmlns="com-wordpress:feed-additions:1">127842392</site>	<item>
		<title>Gradium Launches stt-translate and s2s-translate, Real-Time Speech Translation Models Beating gpt-realtime-translate on Accuracy and Latency</title>
		<link>https://www.marktechpost.com/2026/06/24/gradium-launches-stt-translate-and-s2s-translate-real-time-speech-translation-models-beating-gpt-realtime-translate-on-accuracy-and-latency/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Wed, 24 Jun 2026 20:00:21 +0000</pubDate>
				<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Audio Language Model]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Voice AI]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80696</guid>

					<description><![CDATA[<p>Gradium released two real-time speech translation models, stt-translate and s2s-translate, covering English, French, German, Spanish, and Portuguese across 20 language pairs. The models collapse the standard three-model cascade into two, pairing single-pass transcription-and-translation with a Gradium TTS stage over one duplex WebSocket. Gradium reports a better accuracy-latency tradeoff than gpt-realtime-translate and gemini-3.5-live-translate, plus output voice selection and cloning.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/gradium-launches-stt-translate-and-s2s-translate-real-time-speech-translation-models-beating-gpt-realtime-translate-on-accuracy-and-latency/">Gradium Launches stt-translate and s2s-translate, Real-Time Speech Translation Models Beating gpt-realtime-translate on Accuracy and Latency</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Gradium today released two real-time speech translation models: <code>stt-translate</code> and <code>s2s-translate</code>. Both run across five languages and stream results live in the browser.</p>



<p class="wp-block-paragraph">Gradium claims a better accuracy-latency tradeoff than <code>gpt-realtime-translate</code> and <code>gemini-3.5-live-translate</code>. It also adds output voice control, including cloning, that <code>gpt-realtime-translate</code> lacks.</p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li>Gradium launched two real-time speech translation models: <code>stt-translate</code> (speech → text) and <code>s2s-translate</code> (speech → speech).</li>



<li>They cover five languages (EN, FR, DE, ES, PT) and 20 pairs, collapsing the usual 3-model cascade into 2.</li>



<li>Accuracy leads <code>gemini-3.5-live-translate</code> on BLEU and MetricX, and beats <code>gpt-realtime-translate</code> on BLEU (comparable on MetricX).</li>



<li>Latency averages 3.0s — ahead of <code>gpt-realtime-translate</code> (3.6s), just behind <code>gemini-3.5-live-translate</code> (2.9s).</li>



<li>Unlike <code>gpt-realtime-translate</code>, you pick the output voice or clone your own, all over one duplex WebSocket.</li>
</ul>



<h2 id="h-stt-translate" class="wp-block-heading"><strong>stt-translate</strong></h2>



<p class="wp-block-paragraph"><code>stt-translate</code> takes speech in one language and returns text in another. It supports English (<code>EN</code>), French (<code>FR</code>), German (<code>DE</code>), Spanish (<code>ES</code>), and Portuguese (<code>PT</code>).</p>



<p class="wp-block-paragraph">Any source maps to any target across that set. That is 20 language pairs in total, in every direction.</p>



<p class="wp-block-paragraph">The key design choice is collapsing two steps into one. Transcription and translation happen in a single pass, inside the speech model. There is no intermediate transcript to wait on and no handoff between systems.</p>



<p class="wp-block-paragraph">According to Gradium: the approach draws on the <a href="https://arxiv.org/abs/2602.11072">Hibiki-Zero framework</a>. The model optimizes low latency and high accuracy jointly through Reinforcement Learning. This means fewer moving parts in the pipeline.</p>



<h2 id="h-s2s-translate" class="wp-block-heading"><strong>s2s-translate</strong></h2>



<p class="wp-block-paragraph"><code>s2s-translate</code> turns spoken audio in one language into spoken audio in another, end to end. It builds on <code>stt-translate</code> and pairs it with a Gradium TTS model in one service.</p>



<p class="wp-block-paragraph">You stream audio in over a WebSocket. You receive both the synthesized output audio and the translated transcript as they are produced.</p>



<p class="wp-block-paragraph">That removes integration work. You do not wire STT and TTS together yourself or manage two connections. The server runs the pipeline and streams results back.</p>



<p class="wp-block-paragraph">Input audio is PCM at 24 kHz, 16-bit signed mono. Output audio is PCM at 48 kHz, 16-bit signed mono. WAV, Opus, mu-law, and A-law are also supported.</p>



<h2 id="h-how-gradium-measures-quality-bleu-and-metricx" class="wp-block-heading"><strong>How Gradium Measures Quality: BLEU and MetricX</strong></h2>



<p class="wp-block-paragraph"><strong>Translation quality is not one number, so Gradium reports two complementary metrics:</strong></p>



<p class="wp-block-paragraph"><strong>BLEU</strong> (Bilingual Evaluation Understudy) is the long-standing machine translation standard (Papineni et al.). It measures n-gram overlap between model output and human reference translations. It runs from 0 to 100, where higher is better.</p>



<p class="wp-block-paragraph">BLEU is fast, reproducible, and comparable across systems. Its limit is that it rewards surface word matching. A correct translation using different wording can be penalized.</p>



<p class="wp-block-paragraph"><strong>MetricX</strong> is a learned, neural quality metric developed by Google (Juraska et al.). It predicts how a human would rate a translation. It is an error score, so lower is better, and it tracks human judgment more closely than BLEU.</p>



<p class="wp-block-paragraph">The two catch different failures. BLEU checks lexical fidelity; MetricX checks semantic adequacy.</p>



<h2 id="h-benchmark" class="wp-block-heading"><strong>Benchmark</strong></h2>



<p class="wp-block-paragraph">Gradium benchmarks on a proprietary dataset of conversational speech. The data reflects everyday topics like work, travel, and weather, rather than scripted text.</p>



<p class="wp-block-paragraph">Against <code>gemini-3.5-live-translate</code>, Gradium leads on both BLEU and MetricX. Against <code>gpt-realtime-translate</code>, Gradium leads on BLEU and is comparable on MetricX.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Capability</th><th>Gradium</th><th><code>gpt-realtime-translate</code></th><th><code>gemini-3.5-live-translate</code></th></tr></thead><tbody><tr><td>Average latency (all pairs)</td><td>3.0s</td><td>3.6s</td><td>2.9s</td></tr><tr><td>BLEU (higher is better)</td><td>Leads both</td><td>Lower than Gradium</td><td>Lower than Gradium</td></tr><tr><td>MetricX (lower error is better)</td><td>Comparable to GPT; leads Gemini</td><td>Comparable to Gradium</td><td>Higher error than Gradium</td></tr><tr><td>Choose output voice</td><td>Yes (catalogue)</td><td>No</td><td>Not stated</td></tr><tr><td>Clone your own voice</td><td>Yes</td><td>No</td><td>Not stated</td></tr><tr><td>Languages</td><td>5 languages, 20 pairs</td><td>Not stated</td><td>Not stated</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">Accuracy (BLEU and MetricX) is measured on <code>stt-translate</code>&#8216;s translation; latency is for the full <code>s2s-translate</code> pipeline. Read it as a tradeoff, not a clean sweep. Gemini is fractionally faster; Gradium is more accurate and adds voice control.</p>



<h2 id="h-why-two-models-beat-three" class="wp-block-heading"><strong>Why Two Models Beat Three</strong></h2>



<p class="wp-block-paragraph">The standard speech-to-speech stack uses three models: Speech-To-Text, then Text-To-Text translation, then Text-To-Speech. Each stage is a separate inference call. Each adds processing time and a handoff.</p>



<p class="wp-block-paragraph">Gradium uses two. <code>stt-translate</code> performs transcription and translation in a single pass. The dedicated Text-To-Text stage disappears entirely.</p>



<p class="wp-block-paragraph">That removes one full model from the critical path, along with its latency and handoff. The end-to-end path is shorter than a three-model cascade at equivalent quality.</p>



<p class="wp-block-paragraph">The numbers back the design. <code>s2s-translate</code> averages 3.0s across all language pairs. That beats <code>gpt-realtime-translate</code> at 3.6s and sits near <code>gemini-3.5-live-translate</code> at 2.9s.</p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use Cases With Examples</strong></h2>



<ul class="wp-block-list">
<li><strong>Live dubbing and localization</strong>: Clone a presenter&#8217;s voice once. Translate a French keynote into Spanish that still sounds like the original speaker.</li>



<li><strong>Multilingual voice agents</strong>: Route a support call through <code>s2s-translate</code>. An English agent hears a German caller in English, and replies stream back in German.</li>



<li><strong>Real-time meetings</strong>: Pipe microphone audio in over the WebSocket. Each participant receives translated speech and transcript in their own language.</li>



<li><strong>Accessibility and captioning</strong>: Use <code>stt-translate</code> alone when you only need text. Render live translated captions without generating audio.</li>
</ul>



<h2 id="h-translate-in-a-few-lines-of-code" class="wp-block-heading"><strong>Translate in a Few Lines of Code</strong></h2>



<p class="wp-block-paragraph">The Python SDK streams audio through the Speech-To-Speech endpoint and returns translated audio plus transcript.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import asyncio
import numpy as np
from gradium import client as gradium_client

grc = gradium_client.GradiumClient()  # reads GRADIUM_API_KEY from the environment

setup = {
    "model_name": "s2s-translate",
    "input_format": "pcm_24000",        # 24 kHz, 16-bit signed mono input
    "output_format": "pcm_48000",       # 48 kHz, 16-bit signed mono output
    "voice_id": "cLONiZ4hQ8VpQ4Sz",     # must be a voice in the target language
    "stt_model_name": "stt-translate",
    "tts_model_name": "default",
    "target_language": "en",
}

# Raw 24 kHz, 16-bit mono PCM bytes (from a file, buffer, or microphone).
with open("input_24k_mono.pcm", "rb") as f:
    pcm = f.read()

async def main() -> np.ndarray:
    audio_out: list[bytes] = []
    async with grc.s2s_realtime(wait_for_ready_on_start=True, **setup) as s2s:
        async def send_loop():
            for i in range(0, len(pcm), 1920):       # 1920 bytes = 40 ms at 24 kHz
                await s2s.send_audio(pcm[i : i + 1920])
            await s2s.send_eos()                     # signal end of input

        async def recv_loop():
            async for msg in s2s:
                if msg["type"] == "audio":
                    audio_out.append(msg["audio"])           # translated speech (bytes)
                elif msg["type"] == "text":
                    print(msg["text"], end=" ", flush=True)  # translated transcript
                elif msg["type"] == "end_of_stream":
                    break

        async with asyncio.TaskGroup() as tg:
            tg.create_task(send_loop())
            tg.create_task(recv_loop())

    return np.frombuffer(b"".join(audio_out), dtype=np.int16)  # 48 kHz mono PCM

translated_pcm = asyncio.run(main())</code></pre></div></div>



<p class="wp-block-paragraph">The SDK exposes three ways to drive S2S. Use <code>s2s_realtime</code> for live sources, <code>s2s_stream</code> for finite iterables, and <code>s2s</code> for buffered files. All three talk to <code>wss://api.gradium.ai/api/speech/s2s</code>.</p>



<h2 id="h-strengths-and-weaknesses" class="wp-block-heading"><strong>Strengths and Weaknesses</strong></h2>



<h4 id="h-strengths" class="wp-block-heading"><strong>Strengths</strong></h4>



<ul class="wp-block-list">
<li>Single-pass <code>stt-translate</code> removes one model from the latency path</li>



<li>Leads <code>gemini-3.5-live-translate</code> on both BLEU and MetricX</li>



<li>Output voice choice and cloning, which <code>gpt-realtime-translate</code> lacks</li>



<li>One duplex WebSocket replaces a hand-wired STT-plus-TTS pipeline</li>
</ul>



<h4 id="h-weaknesses" class="wp-block-heading"><strong>Weaknesses</strong></h4>



<ul class="wp-block-list">
<li>Five languages at launch, with 20 pairs only across that set</li>



<li><code>gemini-3.5-live-translate</code> is fractionally lower latency at 2.9s</li>



<li>MetricX is only comparable to, not ahead of, <code>gpt-realtime-translate</code></li>



<li>Benchmarks use a proprietary dataset, so external replication is limited</li>
</ul>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- ===== Gradium Translate Interactive Demo — paste into a WordPress "Custom HTML" block ===== -->
<iframe id="gradium-translate-embed"
        title="Gradium Translate Interactive Demo"
        style="width:100%;border:0;display:block;height:600px;overflow:hidden;"
        loading="lazy"
        sandbox="allow-scripts allow-same-origin allow-popups"
        srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;UTF-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;Gradium Translate — Interactive Demo&lt;/title&gt;
&lt;style&gt;
  /* ---- scoped reset ---- */
  #gtx-root *{box-sizing:border-box;margin:0;padding:0}
  #gtx-root{
    --bg:#000000; --panel:#0e0e0e; --panel2:#171717; --line:#2b2b2b;
    --txt:#ffffff; --mut:#9a9a9a; --acc:#ffffff; --acc2:#ffffff;
    --good:#ffffff; --warn:#cfcfcf; --bad:#6f6f6f;
    font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;
    background:var(--bg); color:var(--txt); padding:22px; line-height:1.5;
    border:1px solid var(--line); border-radius:16px; max-width:860px; margin:0 auto;
  }
  #gtx-root .gtx-head{display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin-bottom:4px}
  #gtx-root .gtx-logo{width:34px;height:34px;border-radius:9px;
    background:linear-gradient(135deg,var(--acc),var(--acc2));display:grid;place-items:center;
    font-weight:800;font-size:18px;color:#0a0c12;flex:none}
  #gtx-root h1{font-size:19px;font-weight:700;letter-spacing:-.2px}
  #gtx-root .gtx-sub{color:var(--mut);font-size:13px;margin:6px 0 18px}
  #gtx-root .gtx-pill{font-size:11px;color:var(--acc2);border:1px solid var(--line);
    background:var(--panel);padding:3px 9px;border-radius:999px;font-weight:600}

  /* ---- tabs ---- */
  #gtx-root .gtx-tabs{display:flex;gap:6px;background:var(--panel);border:1px solid var(--line);
    border-radius:11px;padding:5px;margin-bottom:18px}
  #gtx-root .gtx-tab{flex:1;border:0;background:transparent;color:var(--mut);font-weight:600;
    font-size:13px;padding:9px 6px;border-radius:8px;cursor:pointer;transition:.18s}
  #gtx-root .gtx-tab[aria-selected=&quot;true&quot;]{background:var(--panel2);color:var(--txt);
    box-shadow:0 0 0 1px var(--line)}
  #gtx-root .gtx-tab:hover{color:var(--txt)}
  #gtx-root .gtx-view{display:none}
  #gtx-root .gtx-view.gtx-on{display:block;animation:gtxf .25s ease}
  @keyframes gtxf{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}

  /* ---- controls ---- */
  #gtx-root .gtx-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}
  #gtx-root label{display:block;font-size:11px;text-transform:uppercase;letter-spacing:.6px;
    color:var(--mut);margin-bottom:6px;font-weight:700}
  #gtx-root select,#gtx-root .gtx-seg{width:100%;background:var(--panel);color:var(--txt);
    border:1px solid var(--line);border-radius:9px;padding:10px 11px;font-size:14px;font-family:inherit}
  #gtx-root select:focus{outline:none;border-color:var(--acc)}
  #gtx-root .gtx-field{margin-bottom:14px}

  /* ---- pipeline cards ---- */
  #gtx-root .gtx-io{display:grid;grid-template-columns:1fr;gap:12px;margin-top:6px}
  #gtx-root .gtx-card{background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:14px 16px}
  #gtx-root .gtx-card .gtx-clab{display:flex;justify-content:space-between;align-items:center;
    font-size:11px;color:var(--mut);text-transform:uppercase;letter-spacing:.6px;font-weight:700;margin-bottom:7px}
  #gtx-root .gtx-srctext{font-size:16px;color:var(--mut)}
  #gtx-root .gtx-outtext{font-size:18px;font-weight:600;min-height:27px}
  #gtx-root .gtx-cur{border-right:2px solid var(--acc2);animation:gtxblink 1s steps(1) infinite}
  @keyframes gtxblink{50%{border-color:transparent}}

  #gtx-root .gtx-go{margin-top:14px;display:flex;gap:10px;flex-wrap:wrap}
  #gtx-root button.gtx-btn{border:0;border-radius:10px;padding:12px 20px;font-weight:700;font-size:14px;
    cursor:pointer;font-family:inherit;transition:.15s}
  #gtx-root .gtx-primary{background:linear-gradient(135deg,var(--acc),var(--acc2));color:#0a0c12}
  #gtx-root .gtx-primary:hover{filter:brightness(1.08)}
  #gtx-root .gtx-primary:disabled{opacity:.5;cursor:not-allowed;filter:none}
  #gtx-root .gtx-ghost{background:var(--panel2);color:var(--txt);box-shadow:0 0 0 1px var(--line)}

  /* ---- latency meter ---- */
  #gtx-root .gtx-lat{margin-top:16px;background:var(--panel);border:1px solid var(--line);
    border-radius:12px;padding:14px 16px}
  #gtx-root .gtx-latrow{display:flex;align-items:center;gap:10px;margin:9px 0;font-size:13px}
  #gtx-root .gtx-latname{width:170px;flex:none;color:var(--mut);font-weight:600}
  #gtx-root .gtx-bartrack{flex:1;height:10px;background:#050505;border:1px solid #1f1f1f;border-radius:999px;overflow:hidden}
  #gtx-root .gtx-barfill{height:100%;width:0;border-radius:999px;transition:width .9s cubic-bezier(.4,0,.2,1)}
  #gtx-root .gtx-latval{width:46px;flex:none;text-align:right;font-variant-numeric:tabular-nums;font-weight:700}
  #gtx-root .gtx-note{font-size:12px;color:var(--mut);margin-top:10px}

  /* ---- benchmark grid ---- */
  #gtx-root table{width:100%;border-collapse:collapse;margin-top:10px;font-size:13px}
  #gtx-root th,#gtx-root td{padding:11px 10px;text-align:left;border-bottom:1px solid var(--line)}
  #gtx-root th{font-size:11px;text-transform:uppercase;letter-spacing:.5px;color:var(--mut)}
  #gtx-root td:first-child{color:var(--mut);font-weight:600}
  #gtx-root .gtx-tag{display:inline-block;padding:3px 9px;border-radius:999px;font-size:11px;font-weight:700}
  #gtx-root .t-lead{background:#ffffff;color:#000}
  #gtx-root .t-comp{background:transparent;border:1px solid #ffffff;color:#fff}
  #gtx-root .t-trail{background:#1c1c1c;color:#8a8a8a;border:1px solid #2b2b2b}
  #gtx-root .t-na{background:var(--panel2);color:var(--mut);border:1px dashed #333}

  /* ---- architecture ---- */
  #gtx-root .gtx-archtoggle{display:flex;gap:6px;background:var(--panel);border:1px solid var(--line);
    border-radius:10px;padding:5px;margin-bottom:14px;max-width:340px}
  #gtx-root .gtx-archtoggle button{flex:1;border:0;background:transparent;color:var(--mut);
    font-weight:700;font-size:12px;padding:8px;border-radius:7px;cursor:pointer}
  #gtx-root .gtx-archtoggle button.on{background:var(--panel2);color:var(--txt);box-shadow:0 0 0 1px var(--line)}
  #gtx-root .gtx-flow{display:flex;align-items:center;gap:8px;flex-wrap:wrap;justify-content:center;
    background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:20px 14px}
  #gtx-root .gtx-stage{background:var(--panel2);border:1px solid var(--line);border-radius:10px;
    padding:12px 14px;text-align:center;min-width:96px}
  #gtx-root .gtx-stage b{display:block;font-size:13px}
  #gtx-root .gtx-stage span{font-size:11px;color:var(--mut)}
  #gtx-root .gtx-stage.acc{border-color:var(--acc);box-shadow:0 0 0 1px var(--acc)}
  #gtx-root .gtx-stage.drop{opacity:.32;border-style:dashed;text-decoration:line-through}
  #gtx-root .gtx-arrow{color:var(--acc2);font-size:18px;font-weight:700}
  #gtx-root .gtx-archnote{font-size:13px;color:var(--mut);margin-top:14px;text-align:center}

  #gtx-root .gtx-foot{margin-top:20px;padding-top:14px;border-top:1px solid var(--line);
    display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px;
    font-size:12px;color:var(--mut)}
  #gtx-root .gtx-foot b{color:var(--acc2)}
  #gtx-root a{color:#fff;text-decoration:underline;text-underline-offset:2px}

  @media(max-width:560px){
    #gtx-root{padding:16px}
    #gtx-root .gtx-grid{grid-template-columns:1fr}
    #gtx-root .gtx-latname{width:120px;font-size:12px}
    #gtx-root table{font-size:12px}
    #gtx-root th,#gtx-root td{padding:9px 6px}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;gtx-root&quot;&gt;

  &lt;div class=&quot;gtx-head&quot;&gt;
    &lt;div class=&quot;gtx-logo&quot;&gt;G&lt;/div&gt;
    &lt;h1&gt;Gradium Translate — Interactive Demo&lt;/h1&gt;
    &lt;span class=&quot;gtx-pill&quot;&gt;stt-translate · s2s-translate&lt;/span&gt;
  &lt;/div&gt;
  &lt;p class=&quot;gtx-sub&quot;&gt;Pick a phrase and a target language, then hear it spoken back. Benchmark figures are Gradium's published numbers.&lt;/p&gt;

  &lt;div class=&quot;gtx-tabs&quot; role=&quot;tablist&quot;&gt;
    &lt;button class=&quot;gtx-tab&quot; role=&quot;tab&quot; aria-selected=&quot;true&quot; data-v=&quot;try&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/25b6.png" alt="▶" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Try it&lt;/button&gt;
    &lt;button class=&quot;gtx-tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-v=&quot;bench&quot;&gt;Benchmarks&lt;/button&gt;
    &lt;button class=&quot;gtx-tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-v=&quot;arch&quot;&gt;Architecture&lt;/button&gt;
  &lt;/div&gt;

  &lt;!-- ============ TRY IT ============ --&gt;
  &lt;section class=&quot;gtx-view gtx-on&quot; data-view=&quot;try&quot;&gt;
    &lt;div class=&quot;gtx-grid&quot;&gt;
      &lt;div class=&quot;gtx-field&quot;&gt;
        &lt;label&gt;Source language&lt;/label&gt;
        &lt;select id=&quot;gtx-src&quot;&gt;&lt;/select&gt;
      &lt;/div&gt;
      &lt;div class=&quot;gtx-field&quot;&gt;
        &lt;label&gt;Target language&lt;/label&gt;
        &lt;select id=&quot;gtx-tgt&quot;&gt;&lt;/select&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;gtx-grid&quot;&gt;
      &lt;div class=&quot;gtx-field&quot;&gt;
        &lt;label&gt;Phrase to translate&lt;/label&gt;
        &lt;select id=&quot;gtx-phrase&quot;&gt;&lt;/select&gt;
      &lt;/div&gt;
      &lt;div class=&quot;gtx-field&quot;&gt;
        &lt;label&gt;Output voice&lt;/label&gt;
        &lt;select id=&quot;gtx-voice&quot;&gt;&lt;/select&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;gtx-io&quot;&gt;
      &lt;div class=&quot;gtx-card&quot;&gt;
        &lt;div class=&quot;gtx-clab&quot;&gt;&lt;span id=&quot;gtx-srclang&quot;&gt;Source&lt;/span&gt;&lt;span&gt;input speech&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;gtx-srctext&quot; id=&quot;gtx-srctext&quot;&gt;—&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;gtx-card&quot;&gt;
        &lt;div class=&quot;gtx-clab&quot;&gt;&lt;span id=&quot;gtx-tgtlang&quot;&gt;Target&lt;/span&gt;&lt;span&gt;translated output&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;gtx-outtext&quot; id=&quot;gtx-outtext&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;gtx-go&quot;&gt;
      &lt;button class=&quot;gtx-btn gtx-primary&quot; id=&quot;gtx-run&quot;&gt;Translate &amp;amp; speak&lt;/button&gt;
      &lt;button class=&quot;gtx-btn gtx-ghost&quot; id=&quot;gtx-clear&quot;&gt;Clear&lt;/button&gt;
    &lt;/div&gt;

    &lt;div class=&quot;gtx-lat&quot;&gt;
      &lt;div class=&quot;gtx-latrow&quot;&gt;
        &lt;span class=&quot;gtx-latname&quot;&gt;Gradium s2s-translate&lt;/span&gt;
        &lt;span class=&quot;gtx-bartrack&quot;&gt;&lt;span class=&quot;gtx-barfill&quot; id=&quot;gb-grad&quot; style=&quot;background:linear-gradient(90deg,var(--acc),var(--acc2))&quot;&gt;&lt;/span&gt;&lt;/span&gt;
        &lt;span class=&quot;gtx-latval&quot;&gt;3.0s&lt;/span&gt;
      &lt;/div&gt;
      &lt;div class=&quot;gtx-latrow&quot;&gt;
        &lt;span class=&quot;gtx-latname&quot;&gt;gemini-3.5-live-translate&lt;/span&gt;
        &lt;span class=&quot;gtx-bartrack&quot;&gt;&lt;span class=&quot;gtx-barfill&quot; id=&quot;gb-gem&quot; style=&quot;background:#aaaaaa&quot;&gt;&lt;/span&gt;&lt;/span&gt;
        &lt;span class=&quot;gtx-latval&quot;&gt;2.9s&lt;/span&gt;
      &lt;/div&gt;
      &lt;div class=&quot;gtx-latrow&quot;&gt;
        &lt;span class=&quot;gtx-latname&quot;&gt;gpt-realtime-translate&lt;/span&gt;
        &lt;span class=&quot;gtx-bartrack&quot;&gt;&lt;span class=&quot;gtx-barfill&quot; id=&quot;gb-gpt&quot; style=&quot;background:#555555&quot;&gt;&lt;/span&gt;&lt;/span&gt;
        &lt;span class=&quot;gtx-latval&quot;&gt;3.6s&lt;/span&gt;
      &lt;/div&gt;
      &lt;div class=&quot;gtx-note&quot; id=&quot;gtx-runnote&quot;&gt;Average end-to-end latency over all language pairs (lower is better).&lt;/div&gt;
    &lt;/div&gt;
  &lt;/section&gt;

  &lt;!-- ============ BENCHMARKS ============ --&gt;
  &lt;section class=&quot;gtx-view&quot; data-view=&quot;bench&quot;&gt;
    &lt;table&gt;
      &lt;thead&gt;
        &lt;tr&gt;&lt;th&gt;Metric&lt;/th&gt;&lt;th&gt;Gradium&lt;/th&gt;&lt;th&gt;gpt-realtime-translate&lt;/th&gt;&lt;th&gt;gemini-3.5-live-translate&lt;/th&gt;&lt;/tr&gt;
      &lt;/thead&gt;
      &lt;tbody&gt;
        &lt;tr&gt;
          &lt;td&gt;Avg latency (all pairs)&lt;/td&gt;
          &lt;td&gt;&lt;b&gt;3.0s&lt;/b&gt;&lt;/td&gt;&lt;td&gt;3.6s&lt;/td&gt;&lt;td&gt;2.9s&lt;/td&gt;
        &lt;/tr&gt;
        &lt;tr&gt;
          &lt;td&gt;BLEU (higher better)&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-lead&quot;&gt;Leads&lt;/span&gt;&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-trail&quot;&gt;Lower&lt;/span&gt;&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-trail&quot;&gt;Lower&lt;/span&gt;&lt;/td&gt;
        &lt;/tr&gt;
        &lt;tr&gt;
          &lt;td&gt;MetricX (lower error better)&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-lead&quot;&gt;Leads / comp.&lt;/span&gt;&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-comp&quot;&gt;Comparable&lt;/span&gt;&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-trail&quot;&gt;Higher error&lt;/span&gt;&lt;/td&gt;
        &lt;/tr&gt;
        &lt;tr&gt;
          &lt;td&gt;Choose output voice&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-lead&quot;&gt;Yes&lt;/span&gt;&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-trail&quot;&gt;No&lt;/span&gt;&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-na&quot;&gt;Not stated&lt;/span&gt;&lt;/td&gt;
        &lt;/tr&gt;
        &lt;tr&gt;
          &lt;td&gt;Clone your voice&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-lead&quot;&gt;Yes&lt;/span&gt;&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-trail&quot;&gt;No&lt;/span&gt;&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-na&quot;&gt;Not stated&lt;/span&gt;&lt;/td&gt;
        &lt;/tr&gt;
        &lt;tr&gt;
          &lt;td&gt;Languages&lt;/td&gt;
          &lt;td&gt;&lt;b&gt;5 · 20 pairs&lt;/b&gt;&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-na&quot;&gt;Not stated&lt;/span&gt;&lt;/td&gt;
          &lt;td&gt;&lt;span class=&quot;gtx-tag t-na&quot;&gt;Not stated&lt;/span&gt;&lt;/td&gt;
        &lt;/tr&gt;
      &lt;/tbody&gt;
    &lt;/table&gt;
    &lt;p class=&quot;gtx-note&quot;&gt;Accuracy claims: vs gemini-3.5-live-translate, Gradium leads BLEU and MetricX. vs gpt-realtime-translate, Gradium leads BLEU and is comparable on MetricX. Source: Gradium launch benchmark on a proprietary conversational-speech dataset.&lt;/p&gt;
  &lt;/section&gt;

  &lt;!-- ============ ARCHITECTURE ============ --&gt;
  &lt;section class=&quot;gtx-view&quot; data-view=&quot;arch&quot;&gt;
    &lt;div class=&quot;gtx-archtoggle&quot;&gt;
      &lt;button class=&quot;on&quot; data-arch=&quot;grad&quot;&gt;Gradium (2 models)&lt;/button&gt;
      &lt;button data-arch=&quot;cascade&quot;&gt;Standard cascade (3 models)&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;gtx-flow&quot; id=&quot;gtx-flow&quot;&gt;&lt;/div&gt;
    &lt;p class=&quot;gtx-archnote&quot; id=&quot;gtx-archnote&quot;&gt;&lt;/p&gt;
  &lt;/section&gt;

  &lt;div class=&quot;gtx-foot&quot;&gt;
    &lt;span&gt;Illustrative demo · speech via your browser · numbers from &lt;b&gt;Gradium&lt;/b&gt;&lt;/span&gt;
    &lt;span&gt;&lt;a href=&quot;https://gradium.ai/translate&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;gradium.ai/translate <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2197.png" alt="↗" class="wp-smiley" style="height: 1em; max-height: 1em;" />&lt;/a&gt;&lt;/span&gt;
  &lt;/div&gt;

&lt;/div&gt;

&lt;script&gt;
(function(){
  var root=document.getElementById('gtx-root');

  /* ---- data ---- */
  var LANGS=[
    {c:'EN',name:'English',bcp:'en-US',flag:'<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f1ec-1f1e7.png" alt="🇬🇧" class="wp-smiley" style="height: 1em; max-height: 1em;" />'},
    {c:'FR',name:'French',bcp:'fr-FR',flag:'<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f1eb-1f1f7.png" alt="🇫🇷" class="wp-smiley" style="height: 1em; max-height: 1em;" />'},
    {c:'ES',name:'Spanish',bcp:'es-ES',flag:'<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f1ea-1f1f8.png" alt="🇪🇸" class="wp-smiley" style="height: 1em; max-height: 1em;" />'},
    {c:'DE',name:'German',bcp:'de-DE',flag:'<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f1e9-1f1ea.png" alt="🇩🇪" class="wp-smiley" style="height: 1em; max-height: 1em;" />'},
    {c:'PT',name:'Portuguese',bcp:'pt-BR',flag:'<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f1e7-1f1f7.png" alt="🇧🇷" class="wp-smiley" style="height: 1em; max-height: 1em;" />'}
  ];
  var PHRASES=[
    {EN:&quot;Good morning, how are you today?&quot;,FR:&quot;Bonjour, comment allez-vous aujourd'hui ?&quot;,ES:&quot;Buenos días, ¿cómo estás hoy?&quot;,DE:&quot;Guten Morgen, wie geht es dir heute?&quot;,PT:&quot;Bom dia, como você está hoje?&quot;},
    {EN:&quot;Where is the nearest train station?&quot;,FR:&quot;Où se trouve la gare la plus proche ?&quot;,ES:&quot;¿Dónde está la estación de tren más cercana?&quot;,DE:&quot;Wo ist der nächste Bahnhof?&quot;,PT:&quot;Onde fica a estação de trem mais próxima?&quot;},
    {EN:&quot;I would like to book a table for two.&quot;,FR:&quot;Je voudrais réserver une table pour deux.&quot;,ES:&quot;Me gustaría reservar una mesa para dos.&quot;,DE:&quot;Ich möchte einen Tisch für zwei reservieren.&quot;,PT:&quot;Eu gostaria de reservar uma mesa para dois.&quot;},
    {EN:&quot;The weather is beautiful today.&quot;,FR:&quot;Il fait très beau aujourd'hui.&quot;,ES:&quot;Hoy hace un tiempo precioso.&quot;,DE:&quot;Das Wetter ist heute wunderschön.&quot;,PT:&quot;O tempo está lindo hoje.&quot;},
    {EN:&quot;Thank you very much for your help.&quot;,FR:&quot;Merci beaucoup pour votre aide.&quot;,ES:&quot;Muchas gracias por tu ayuda.&quot;,DE:&quot;Vielen Dank für Ihre Hilfe.&quot;,PT:&quot;Muito obrigado pela sua ajuda.&quot;}
  ];

  var $=function(s){return root.querySelector(s)};
  var srcSel=$('#gtx-src'),tgtSel=$('#gtx-tgt'),phSel=$('#gtx-phrase'),vSel=$('#gtx-voice');

  LANGS.forEach(function(l){
    srcSel.add(new Option(l.flag+' '+l.name,l.c));
    tgtSel.add(new Option(l.flag+' '+l.name,l.c));
  });
  srcSel.value='EN'; tgtSel.value='FR';
  PHRASES.forEach(function(p,i){ phSel.add(new Option(p.EN,i)); });

  function lang(c){return LANGS.filter(function(l){return l.c===c})[0];}

  /* ---- voices ---- */
  function loadVoices(){
    var tgt=tgtSel.value, bcp=lang(tgt).bcp, pre=bcp.split('-')[0];
    vSel.innerHTML='';
    var vs=(window.speechSynthesis?speechSynthesis.getVoices():[])||[];
    var match=vs.filter(function(v){return v.lang&amp;&amp;v.lang.toLowerCase().indexOf(pre)===0;});
    if(match.length){
      match.forEach(function(v){ vSel.add(new Option(v.name.replace(/Microsoft|Google/gi,'').trim()+' ('+v.lang+')',v.name)); });
      vSel.disabled=false;
    } else {
      vSel.add(new Option('System default voice',''));
      vSel.disabled=true;
    }
  }
  if(window.speechSynthesis){ speechSynthesis.onvoiceschanged=loadVoices; }
  loadVoices();

  /* ---- render source/labels ---- */
  function refresh(){
    var p=PHRASES[+phSel.value], s=srcSel.value, t=tgtSel.value;
    $('#gtx-srclang').textContent=lang(s).flag+' '+lang(s).name;
    $('#gtx-tgtlang').textContent=lang(t).flag+' '+lang(t).name;
    $('#gtx-srctext').textContent=p[s];
    loadVoices();
  }
  srcSel.onchange=refresh; tgtSel.onchange=refresh; phSel.onchange=refresh;
  refresh();

  /* ---- run translation (type + speak) ---- */
  var running=false;
  function setBars(on){
    $('#gb-grad').style.width=on?'83%':'0';
    $('#gb-gem').style.width=on?'81%':'0';
    $('#gb-gpt').style.width=on?'100%':'0';
  }
  $('#gtx-run').onclick=function(){
    if(running) return;
    var p=PHRASES[+phSel.value], t=tgtSel.value, out=p[t], bcp=lang(t).bcp;
    var box=$('#gtx-outtext'); box.innerHTML='';
    setBars(false);
    if(window.speechSynthesis) speechSynthesis.cancel();
    running=true; $('#gtx-run').disabled=true;
    $('#gtx-runnote').textContent='Translating in a single pass (stt-translate), then synthesizing voice…';

    var i=0;
    var timer=setInterval(function(){
      box.textContent=out.slice(0,i);
      var c=document.createElement('span'); c.className='gtx-cur'; box.appendChild(c);
      i++;
      if(i&gt;out.length){
        clearInterval(timer);
        box.textContent=out;
        setBars(true);
        speak(out,bcp);
        $('#gtx-runnote').textContent='Average end-to-end latency over all language pairs (lower is better).';
        running=false; $('#gtx-run').disabled=false;
      }
    },26);
  };
  function speak(text,bcp){
    if(!window.speechSynthesis){return;}
    var u=new SpeechSynthesisUtterance(text); u.lang=bcp; u.rate=.96;
    var want=vSel.value, vs=speechSynthesis.getVoices();
    var v=vs.filter(function(x){return x.name===want;})[0]
        ||vs.filter(function(x){return x.lang&amp;&amp;x.lang.toLowerCase().indexOf(bcp.split('-')[0])===0;})[0];
    if(v) u.voice=v;
    speechSynthesis.speak(u);
  }
  $('#gtx-clear').onclick=function(){
    $('#gtx-outtext').innerHTML=''; setBars(false);
    if(window.speechSynthesis) speechSynthesis.cancel();
  };

  /* ---- tabs ---- */
  root.querySelectorAll('.gtx-tab').forEach(function(tb){
    tb.onclick=function(){
      root.querySelectorAll('.gtx-tab').forEach(function(x){x.setAttribute('aria-selected','false');});
      tb.setAttribute('aria-selected','true');
      root.querySelectorAll('.gtx-view').forEach(function(v){v.classList.remove('gtx-on');});
      $('[data-view=&quot;'+tb.dataset.v+'&quot;]').classList.add('gtx-on');
      report();
    };
  });

  /* ---- architecture ---- */
  var FLOWS={
    grad:[['<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f399.png" alt="🎙" class="wp-smiley" style="height: 1em; max-height: 1em;" />','Input speech',''],['stt-translate','transcribe + translate','acc'],['TTS','synthesize voice','']],
    cascade:[['<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f399.png" alt="🎙" class="wp-smiley" style="height: 1em; max-height: 1em;" />','Input speech',''],['STT','transcribe',''],['T2T','translate','drop'],['TTS','synthesize','']]
  };
  function drawArch(k){
    var flow=$('#gtx-flow'); flow.innerHTML='';
    FLOWS[k].forEach(function(st,idx){
      if(idx&gt;0){var a=document.createElement('span');a.className='gtx-arrow';a.textContent='→';flow.appendChild(a);}
      var d=document.createElement('div'); d.className='gtx-stage'+(st[2]?' '+st[2]:'');
      d.innerHTML='&lt;b&gt;'+st[0]+'&lt;/b&gt;&lt;span&gt;'+st[1]+'&lt;/span&gt;'; flow.appendChild(d);
    });
    $('#gtx-archnote').textContent = k==='grad'
      ? 'Two models. stt-translate fuses transcription and translation, removing the separate Text-To-Text stage and its handoff.'
      : 'Three models. Each stage is a separate inference call with its own latency and a handoff the next stage waits on.';
    report();
  }
  root.querySelectorAll('.gtx-archtoggle button').forEach(function(b){
    b.onclick=function(){
      root.querySelectorAll('.gtx-archtoggle button').forEach(function(x){x.classList.remove('on');});
      b.classList.add('on'); drawArch(b.dataset.arch);
    };
  });
  drawArch('grad');

  /* ---- height reporting for WordPress iframe (offsetHeight + 40, never scrollHeight) ---- */
  function report(){
    var h=root.offsetHeight+40;
    parent.postMessage({type:'gtx-height',height:h},'*');
  }
  window.addEventListener('load',report);
  setTimeout(report,120);
  if(window.ResizeObserver){ new ResizeObserver(report).observe(root); }
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
<script>
(function(){
  var f=document.getElementById("gradium-translate-embed");
  window.addEventListener("message",function(e){
    if(e&&e.data&&e.data.type==="gtx-height"&&typeof e.data.height==="number"){
      f.style.height=e.data.height+"px";
    }
  });
})();
</script>
<!-- ===== end Gradium Translate embed ===== -->




<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">You can test real-time translation in the browser at <a href="https://gradium.ai/translate" target="_blank" rel="noreferrer noopener"><strong>gradium.ai/translate</strong></a>, with integration details in the <a href="https://docs.gradium.ai/guides/speech-to-speech-overview" target="_blank" rel="noreferrer noopener"><strong>API docs</strong></a>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/gradium-launches-stt-translate-and-s2s-translate-real-time-speech-translation-models-beating-gpt-realtime-translate-on-accuracy-and-latency/">Gradium Launches stt-translate and s2s-translate, Real-Time Speech Translation Models Beating gpt-realtime-translate on Accuracy and Latency</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80696</post-id>
	</item>
		<item>
		<title>How to Design an OpenHarness Style Agent Runtime with Tools, Memory, Permissions, Skills, and Multi-Agent Coordination</title>
		<link>https://www.marktechpost.com/2026/06/24/how-to-design-an-openharness-style-agent-runtime-with-tools-memory-permissions-skills-and-multi-agent-coordination/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Wed, 24 Jun 2026 19:08:36 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80693</guid>

					<description><![CDATA[<p>In this tutorial, we build an OpenHarness style agent harness from scratch to see how a practical agent system works. We recreate the core building blocks: tool use, typed tool schemas, permissions, lifecycle hooks, memory, skills, context compaction, retry logic, cost tracking, and multi-agent coordination. We expose the full control flow instead of treating the framework as a black box. We keep everything runnable so we can experiment without API keys or extra infrastructure.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/how-to-design-an-openharness-style-agent-runtime-with-tools-memory-permissions-skills-and-multi-agent-coordination/">How to Design an OpenHarness Style Agent Runtime with Tools, Memory, Permissions, Skills, and Multi-Agent Coordination</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this <a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Agentic%20AI%20Codes/openharness_agent_runtime_from_scratch_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">tutorial</a>, we build<a href="https://github.com/HKUDS/OpenHarness"> <strong>OpenHarness</strong></a> from scratch to better understand how a practical agent harness works. We recreate the major building blocks that make an agent system useful, including tool use, typed tool schemas, permissions, lifecycle hooks, memory, skills, context compaction, retry logic, cost tracking, and multi-agent coordination. Instead of treating an agent framework as a black box, we expose the full control flow and watch how the harness receives a user task, lets the model decide the next action, validates and executes tool calls, returns observations, and continues the loop until the task is complete. We also keep the implementation runnable so we can experiment with the architecture without needing API keys or complex infrastructure.</p>



<h3 id="h-setting-up-the-openharness-core" class="wp-block-heading"><strong>Setting Up the OpenHarness Core</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">from __future__ import annotations
import asyncio
import contextlib
import dataclasses
import fnmatch
import io
import json
import os
import re
import tempfile
import textwrap
import time
import traceback
import types
import typing
import urllib.error
import urllib.request
from dataclasses import dataclass, field
from enum import Enum
MISSING = dataclasses.MISSING
UnionType = getattr(types, "UnionType", None)
def run_async(coro):
   """Run a coroutine to completion from sync code, even inside a live loop."""
   try:
       loop = asyncio.get_running_loop()
   except RuntimeError:
       loop = None
   if loop is not None and loop.is_running():
       try:
           import nest_asyncio
           nest_asyncio.apply()
           return loop.run_until_complete(coro)
       except Exception:
           import threading
           box: dict = {}
           def _runner():
               new_loop = asyncio.new_event_loop()
               try:
                   box["value"] = new_loop.run_until_complete(coro)
               finally:
                   new_loop.close()
           t = threading.Thread(target=_runner)
           t.start()
           t.join()
           return box["value"]
   return asyncio.run(coro)
BANNER = "═" * 78
def banner(title: str) -> None:
   print("\n" + BANNER)
   print(f"  {title}")
   print(BANNER)
def explain(title: str, body: str) -> None:
   banner(title)
   print(textwrap.fill(textwrap.dedent(body).strip(), width=78))
   print("-" * 78)
def short(text: str, n: int = 240) -> str:
   text = " ".join(str(text).split())
   return text if len(text) &lt;= n else text[: n - 1] + "…"
@dataclass
class Usage:
   input_tokens: int = 0
   output_tokens: int = 0
   def __add__(self, other: "Usage") -> "Usage":
       return Usage(self.input_tokens + other.input_tokens,
                    self.output_tokens + other.output_tokens)
@dataclass
class ToolCall:
   id: str
   name: str
   arguments: dict
@dataclass
class AssistantTurn:
   """One turn produced by the model: some text + zero or more tool calls."""
   text: str = ""
   tool_calls: list = field(default_factory=list)
   stop_reason: str = "end_turn"
   usage: Usage = field(default_factory=Usage)
@dataclass
class Message:
   """A single message in the running conversation transcript."""
   role: str
   content: str = ""
   tool_calls: list = field(default_factory=list)
   tool_call_id: str = ""
   name: str = ""
def count_tokens(text: str) -> int:
   """Cheap, provider-agnostic token estimate (~4 chars/token)."""
   if not text:
       return 0
   return max(1, round(len(text) / 4))
PRICE_BOOK = {
   "mock-sonnet":     (3.00, 15.00),
   "claude-sonnet-4": (3.00, 15.00),
   "gpt-4.1":         (2.00,  8.00),
   "default":         (1.00,  3.00),
}
class CostMeter:
   """Accumulates token usage and converts it to an estimated dollar cost."""
   def __init__(self, model: str):
       self.model = model
       self.total = Usage()
       self.calls = 0
   def add(self, usage: Usage) -> None:
       self.total = self.total + usage
       self.calls += 1
   @property
   def dollars(self) -> float:
       pin, pout = PRICE_BOOK.get(self.model, PRICE_BOOK["default"])
       return (self.total.input_tokens / 1e6) * pin + \
              (self.total.output_tokens / 1e6) * pout
   def summary(self) -> str:
       return (f"{self.calls} model call(s) | "
               f"in={self.total.input_tokens} out={self.total.output_tokens} tok | "
               f"~${self.dollars:.5f} ({self.model})")
def fld(description: str = "", default=MISSING, default_factory=MISSING):
   """Declare a tool-input field with a description (and optional default)."""
   md = {"description": description}
   if default_factory is not MISSING:
       return field(default_factory=default_factory, metadata=md)
   if default is not MISSING:
       return field(default=default, metadata=md)
   return field(metadata=md)
def _is_optional(t) -> bool:
   origin = typing.get_origin(t)
   if origin is typing.Union or (UnionType is not None and origin is UnionType):
       return type(None) in typing.get_args(t)
   return False
def _py_to_json_type(t) -> dict:
   origin = typing.get_origin(t)
   if origin is typing.Union or (UnionType is not None and origin is UnionType):
       args = [a for a in typing.get_args(t) if a is not type(None)]
       return _py_to_json_type(args[0]) if args else {"type": "string"}
   if t is str:
       return {"type": "string"}
   if t is bool:
       return {"type": "boolean"}
   if t is int:
       return {"type": "integer"}
   if t is float:
       return {"type": "number"}
   if origin is list or t is list:
       args = typing.get_args(t)
       item = _py_to_json_type(args[0]) if args else {"type": "string"}
       return {"type": "array", "items": item}
   if origin is dict or t is dict:
       return {"type": "object"}
   return {"type": "string"}
def build_json_schema(model_cls) -> dict:
   """Turn a dataclass input model into a JSON Schema (object with properties)."""
   hints = typing.get_type_hints(model_cls)
   props, required = {}, []
   for f in dataclasses.fields(model_cls):
       t = hints.get(f.name, str)
       js = dict(_py_to_json_type(t))
       desc = f.metadata.get("description", "")
       if desc:
           js["description"] = desc
       props[f.name] = js
       has_default = (f.default is not MISSING) or (f.default_factory is not MISSING)
       if not has_default and not _is_optional(t):
           required.append(f.name)
   schema = {"type": "object", "properties": props}
   if required:
       schema["required"] = required
   return schema
def _coerce(v, t):
   origin = typing.get_origin(t)
   if origin is typing.Union or (UnionType is not None and origin is UnionType):
       if v is None:
           return None
       args = [a for a in typing.get_args(t) if a is not type(None)]
       return _coerce(v, args[0]) if args else v
   if t is str:
       return v if isinstance(v, str) else str(v)
   if t is bool:
       if isinstance(v, bool):
           return v
       if isinstance(v, str):
           return v.strip().lower() in ("1", "true", "yes", "y", "on")
       return bool(v)
   if t is int:
       return int(v)
   if t is float:
       return float(v)
   if origin is list or t is list:
       args = typing.get_args(t)
       it = args[0] if args else str
       if not isinstance(v, list):
           v = [v]
       return [_coerce(x, it) for x in v]
   if origin is dict or t is dict:
       return dict(v) if v else {}
   return v
def instantiate(model_cls, raw: dict):
   """Validate + coerce raw JSON args into a typed input instance."""
   hints = typing.get_type_hints(model_cls)
   raw = raw or {}
   kwargs = {}
   for f in dataclasses.fields(model_cls):
       t = hints.get(f.name, str)
       if f.name in raw and raw[f.name] is not None:
           try:
               kwargs[f.name] = _coerce(raw[f.name], t)
           except (TypeError, ValueError) as e:
               raise ValueError(f"Bad value for '{f.name}': {e}")
       elif f.default is not MISSING:
           kwargs[f.name] = f.default
       elif f.default_factory is not MISSING:
           kwargs[f.name] = f.default_factory()
       elif _is_optional(t):
           kwargs[f.name] = None
       else:
           raise ValueError(f"Missing required argument '{f.name}'")
   return model_cls(**kwargs)
class PermissionKind(Enum):
   """How dangerous a tool is — drives the default permission policy."""
   READ = "read"
   WRITE = "write"
   EXECUTE = "execute"
   META = "meta"
@dataclass
class ToolResult:
   output: str
   is_error: bool = False
   metadata: dict = field(default_factory=dict)
class ToolContext:
   """Everything a tool may need at runtime (services + shared state)."""
   def __init__(self, **services):
       self.__dict__.update(services)
class BaseTool:
   """Base class for all tools. Subclasses set name/description/InputModel/kind
   and implement `execute`. Schema + validation are handled here."""
   name: str = "base"
   description: str = ""
   InputModel = None
   kind: PermissionKind = PermissionKind.READ
   def schema(self) -> dict:
       return {
           "name": self.name,
           "description": self.description,
           "kind": self.kind.value,
           "input_schema": (build_json_schema(self.InputModel)
                            if self.InputModel else
                            {"type": "object", "properties": {}}),
       }
   async def run(self, raw_args: dict, ctx: ToolContext) -> ToolResult:
       args = instantiate(self.InputModel, raw_args) if self.InputModel else None
       return await self.execute(args, ctx)
   async def execute(self, args, ctx: ToolContext) -> ToolResult:
       raise NotImplementedError
class ToolRegistry:
   def __init__(self):
       self._tools: dict = {}
   def register(self, tool: BaseTool) -> "ToolRegistry":
       self._tools[tool.name] = tool
       return self
   def get(self, name: str) -> BaseTool | None:
       return self._tools.get(name)
   def schemas(self) -> list:
       return [t.schema() for t in self._tools.values()]
   def names(self) -> list:
       return list(self._tools)
class VirtualFS:
   """In-memory filesystem. Keeps the tutorial safe &amp; deterministic in Colab."""
   def __init__(self):
       self.files: dict = {}
   @staticmethod
   def norm(path: str) -> str:
       return path.lstrip("./").strip()
   def write(self, path: str, content: str) -> None:
       self.files[self.norm(path)] = content
   def read(self, path: str) -> str:
       return self.files[self.norm(path)]
   def exists(self, path: str) -> bool:
       return self.norm(path) in self.files
   def list(self, pattern: str = "*") -> list:
       return sorted(p for p in self.files if fnmatch.fnmatch(p, pattern))
   def tree(self) -> str:
       if not self.files:
           return "(empty)"
       return "\n".join(f"  {p}  ({len(c)} bytes)"
                        for p, c in sorted(self.files.items()))
class PermissionMode(Enum):
   DEFAULT = "default"
   AUTO = "auto"
   PLAN = "plan"
@dataclass
class PermissionDecision:
   action: str
   reason: str = ""
SENSITIVE_PATTERNS = [
   "/etc/*", "*/.ssh/*", "*.pem", "*id_rsa*", "*/.aws/*",
   "*credentials*", "*.env", "*/secrets/*",
]
class PermissionChecker:
   def __init__(self, mode: PermissionMode = PermissionMode.DEFAULT,
                path_rules: list | None = None,
                denied_commands: list | None = None):
       self.mode = mode
       self.path_rules = path_rules or []
       self.denied_commands = denied_commands or []
   def _check_path(self, path: str) -> PermissionDecision | None:
       for pat in SENSITIVE_PATTERNS:
           if fnmatch.fnmatch(path, pat):
               return PermissionDecision("deny", f"sensitive path '{path}' ({pat})")
       for rule in self.path_rules:
           if fnmatch.fnmatch(path, rule["pattern"]):
               if rule.get("allow", True):
                   return PermissionDecision("allow", f"path rule allows '{rule['pattern']}'")
               return PermissionDecision("deny", f"path rule blocks '{rule['pattern']}'")
       return None
   def _check_command(self, command: str) -> PermissionDecision | None:
       for pat in self.denied_commands:
           if re.search(pat, command):
               return PermissionDecision("deny", f"denied command matched /{pat}/")
       return None
   def check(self, tool: BaseTool, args: dict) -> PermissionDecision:
       if "path" in args and tool.kind in (PermissionKind.WRITE, PermissionKind.EXECUTE):
           d = self._check_path(str(args["path"]))
           if d:
               return d
       if "command" in args:
           d = self._check_command(str(args["command"]))
           if d:
               return d
       if self.mode is PermissionMode.AUTO:
           return PermissionDecision("allow", "auto mode")
       if self.mode is PermissionMode.PLAN:
           if tool.kind in (PermissionKind.WRITE, PermissionKind.EXECUTE):
               return PermissionDecision("deny", "plan mode blocks writes/executes")
           return PermissionDecision("allow", "plan mode allows reads")
       if tool.kind in (PermissionKind.READ, PermissionKind.META):
           return PermissionDecision("allow", "safe tool")
       return PermissionDecision("ask", f"{tool.kind.value} requires approval")
async def auto_approve(tool, args, reason) -> bool:
   print(f"        <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f510.png" alt="🔐" class="wp-smiley" style="height: 1em; max-height: 1em;" /> approval needed: {tool.name} ({reason}) -> [auto-approved]")
   return True
async def interactive_approve(tool, args, reason) -> bool:
   ans = input(f"        <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f510.png" alt="🔐" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Allow {tool.name}({short(json.dumps(args), 80)})? [y/N] ")
   return ans.strip().lower().startswith("y")
@dataclass
class HookOutcome:
   blocked: bool = False
   reason: str = ""
   arguments: dict | None = None
class HookManager:
   """Lifecycle events around every tool call (like PreToolUse/PostToolUse)."""
   def __init__(self):
       self.pre: list = []
       self.post: list = []
   def add_pre(self, fn):
       self.pre.append(fn); return self
   def add_post(self, fn):
       self.post.append(fn); return self
   def run_pre(self, call: ToolCall, tool: BaseTool, ctx: ToolContext) -> HookOutcome:
       args = dict(call.arguments)
       for fn in self.pre:
           out = fn(call, tool, ctx)
           if out is None:
               continue
           if out.blocked:
               return out
           if out.arguments is not None:
               args = out.arguments
       return HookOutcome(arguments=args)
   def run_post(self, call, tool, result: ToolResult, ctx) -> ToolResult:
       for fn in self.post:
           new = fn(call, tool, result, ctx)
           if new is not None:
               result = new
       return result
</code></pre></div></div>



<p class="wp-block-paragraph">We begin by establishing the foundation for the OpenHarness-style tutorial, including imports, async execution support, helper functions, and core data models. We define messages, tool calls, usage tracking, token counting, cost estimation, permission modes, hooks, and the virtual filesystem that keeps execution safe. We use this snippet to establish the basic architecture on which all subsequent tools, agent loops, and demos depend.</p>



<h3 id="h-building-the-tool-layer" class="wp-block-heading"><strong>Building the Tool Layer</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">@dataclass
class WriteFileInput:
   path: str = fld("File path to write")
   content: str = fld("Full file content")
class WriteFileTool(BaseTool):
   name = "write_file"
   description = "Create or overwrite a file with the given content."
   InputModel = WriteFileInput
   kind = PermissionKind.WRITE
   async def execute(self, args: WriteFileInput, ctx) -> ToolResult:
       ctx.vfs.write(args.path, args.content)
       return ToolResult(f"Wrote {len(args.content)} bytes to {args.path}")
@dataclass
class ReadFileInput:
   path: str = fld("File path to read")
class ReadFileTool(BaseTool):
   name = "read_file"
   description = "Read the full contents of a file."
   InputModel = ReadFileInput
   kind = PermissionKind.READ
   async def execute(self, args: ReadFileInput, ctx) -> ToolResult:
       if not ctx.vfs.exists(args.path):
           return ToolResult(f"No such file: {args.path}", is_error=True)
       return ToolResult(ctx.vfs.read(args.path))
@dataclass
class EditInput:
   path: str = fld("File to edit")
   old: str = fld("Exact substring to replace")
   new: str = fld("Replacement text")
class EditTool(BaseTool):
   name = "edit"
   description = "Replace the first occurrence of `old` with `new` in a file."
   InputModel = EditInput
   kind = PermissionKind.WRITE
   async def execute(self, args: EditInput, ctx) -> ToolResult:
       if not ctx.vfs.exists(args.path):
           return ToolResult(f"No such file: {args.path}", is_error=True)
       text = ctx.vfs.read(args.path)
       if args.old not in text:
           return ToolResult(f"`old` not found in {args.path}", is_error=True)
       ctx.vfs.write(args.path, text.replace(args.old, args.new, 1))
       return ToolResult(f"Edited {args.path}: replaced 1 occurrence.")
@dataclass
class ListFilesInput:
   pattern: str = fld("Glob pattern", default="*")
class ListFilesTool(BaseTool):
   name = "list_files"
   description = "List files matching a glob pattern."
   InputModel = ListFilesInput
   kind = PermissionKind.READ
   async def execute(self, args: ListFilesInput, ctx) -> ToolResult:
       files = ctx.vfs.list(args.pattern)
       return ToolResult("\n".join(files) if files else "(no matches)")
@dataclass
class GrepInput:
   pattern: str = fld("Regex to search for")
   path_glob: str = fld("Which files to search", default="*")
class GrepTool(BaseTool):
   name = "grep"
   description = "Search file contents with a regular expression."
   InputModel = GrepInput
   kind = PermissionKind.READ
   async def execute(self, args: GrepInput, ctx) -> ToolResult:
       rx = re.compile(args.pattern)
       hits = []
       for p in ctx.vfs.list(args.path_glob):
           for i, line in enumerate(ctx.vfs.read(p).splitlines(), 1):
               if rx.search(line):
                   hits.append(f"{p}:{i}: {line.strip()}")
       return ToolResult("\n".join(hits) if hits else "(no matches)")
@dataclass
class RunPythonInput:
   files: list = fld("VFS files to exec in order in one namespace",
                     default_factory=list)
   code: str = fld("Extra Python code to run after the files", default="")
class RunPythonTool(BaseTool):
   name = "run_python"
   description = ("Execute Python from the virtual filesystem (and/or an inline "
                  "snippet) and capture stdout. Used to run tests.")
   InputModel = RunPythonInput
   kind = PermissionKind.EXECUTE
   async def execute(self, args: RunPythonInput, ctx) -> ToolResult:
       source_parts = []
       for p in args.files:
           if not ctx.vfs.exists(p):
               return ToolResult(f"No such file: {p}", is_error=True)
           source_parts.append(ctx.vfs.read(p))
       if args.code:
           source_parts.append(args.code)
       source = "\n\n".join(source_parts)
       buf = io.StringIO()
       sandbox_globals = {"__name__": "__main__", "__builtins__": __builtins__}
       try:
           with contextlib.redirect_stdout(buf):
               exec(compile(source, "&lt;agent_code>", "exec"), sandbox_globals)
       except Exception as e:
           frames = [f for f in traceback.extract_tb(e.__traceback__)
                     if f.filename == "&lt;agent_code>"]
           loc = ""
           if frames:
               last = frames[-1]
               src_lines = source.splitlines()
               text = (src_lines[last.lineno - 1].strip()
                       if 0 &lt; last.lineno &lt;= len(src_lines) else "")
               loc = f"  (line {last.lineno}: {text})" if text else f"  (line {last.lineno})"
           out = buf.getvalue()
           msg = f"{type(e).__name__}: {e}{loc}"
           return ToolResult(f"{out}\n{msg}".strip(), is_error=True)
       return ToolResult(buf.getvalue().strip() or "(ran with no output)")
@dataclass
class ShellInput:
   command: str = fld("Shell command to run")
class ShellTool(BaseTool):
   name = "shell"
   description = "Run a shell command (simulated over the virtual filesystem)."
   InputModel = ShellInput
   kind = PermissionKind.EXECUTE
   async def execute(self, args: ShellInput, ctx) -> ToolResult:
       cmd = args.command.strip()
       if cmd.startswith("ls"):
           return ToolResult("\n".join(ctx.vfs.list("*")) or "(empty)")
       if cmd.startswith("cat "):
           p = cmd[4:].strip()
           if ctx.vfs.exists(p):
               return ToolResult(ctx.vfs.read(p))
           return ToolResult(f"cat: {p}: No such file", is_error=True)
       if cmd.startswith("echo "):
           return ToolResult(cmd[5:])
       return ToolResult(f"(simulated) `{cmd}` executed.")
_FAKE_WEB = {
   "vector database":
       "Vector databases (FAISS, Milvus, pgvector) index embeddings for "
       "approximate nearest-neighbour search powering RAG.",
   "agent harness":
       "An agent harness is the infrastructure around an LLM: tools, memory, "
       "permissions, and a loop that turns model output into real actions.",
   "exponential backoff":
       "Exponential backoff retries failed calls after 2^n growing delays to "
       "avoid hammering a struggling service.",
}
@dataclass
class WebSearchInput:
   query: str = fld("Search query")
class WebSearchTool(BaseTool):
   name = "web_search"
   description = "Search the web for up-to-date information. (Mocked in tutorial.)"
   InputModel = WebSearchInput
   kind = PermissionKind.READ
   async def execute(self, args: WebSearchInput, ctx) -> ToolResult:
       await asyncio.sleep(0.05)
       q = args.query.lower()
       for key, val in _FAKE_WEB.items():
           if key in q:
               return ToolResult(f"[mock result] {val}")
       return ToolResult(f"[mock result] No canned answer for '{args.query}'. "
                         f"(Wire a real search API here.)")
@dataclass
class SkillInput:
   name: str = fld("Name of the skill to load")
class SkillTool(BaseTool):
   name = "skill"
   description = ("Load an on-demand skill (markdown playbook) into context. "
                  "Only load a skill right before you need its guidance.")
   InputModel = SkillInput
   kind = PermissionKind.META
   async def execute(self, args: SkillInput, ctx) -> ToolResult:
       body = ctx.skills.load(args.name)
       if body is None:
           return ToolResult(f"Unknown skill: {args.name}", is_error=True)
       return ToolResult(f"Loaded skill '{args.name}':\n\n{body}")
@dataclass
class RememberInput:
   note: str = fld("A durable fact/preference to remember across sessions")
class RememberTool(BaseTool):
   name = "remember"
   description = "Persist a durable fact to long-term memory (MEMORY.md)."
   InputModel = RememberInput
   kind = PermissionKind.META
   async def execute(self, args: RememberInput, ctx) -> ToolResult:
       ctx.memory.append(args.note)
       return ToolResult(f"Remembered: {args.note}")
@dataclass
class AskUserInput:
   question: str = fld("A question to ask the human")
class AskUserTool(BaseTool):
   name = "ask_user"
   description = "Ask the human a clarifying question."
   InputModel = AskUserInput
   kind = PermissionKind.META
   async def execute(self, args: AskUserInput, ctx) -> ToolResult:
       canned = getattr(ctx, "canned_answers", {})
       ans = canned.get(args.question, "(no answer configured)")
       return ToolResult(f"User answered: {ans}")
@dataclass
class SpawnAgentInput:
   role: str = fld("Which agent profile to spawn (e.g. 'researcher')")
   task: str = fld("The task to delegate to that agent")
class SpawnAgentTool(BaseTool):
   name = "spawn_agent"
   description = ("Delegate a subtask to a specialized subagent and get its "
                  "final result. Multiple spawns in one turn run in parallel.")
   InputModel = SpawnAgentInput
   kind = PermissionKind.META
   async def execute(self, args: SpawnAgentInput, ctx) -> ToolResult:
       result = await ctx.spawn(args.role, args.task)
       return ToolResult(f"[{args.role}] {result}")
@dataclass
class Skill:
   name: str
   description: str
   body: str
class SkillLibrary:
   """Holds skills. The *summary* (name + description) is injected into the
   system prompt; the *body* is only pulled in when the model loads it."""
   def __init__(self):
       self._skills: dict = {}
       self.loaded: list = []
   @staticmethod
   def parse_markdown(md: str) -> Skill:
       """Parse a skill .md with YAML-ish frontmatter (name/description)."""
       name, desc, body = "unnamed", "", md
       m = re.match(r"^---\s*\n(.*?)\n---\s*\n(.*)$", md, re.S)
       if m:
           front, body = m.group(1), m.group(2)
           for line in front.splitlines():
               if ":" in line:
                   k, v = line.split(":", 1)
                   if k.strip() == "name":
                       name = v.strip()
                   elif k.strip() == "description":
                       desc = v.strip()
       return Skill(name=name, description=desc, body=body.strip())
   def add_markdown(self, md: str) -> "SkillLibrary":
       s = self.parse_markdown(md)
       self._skills[s.name] = s
       return self
   def summary(self) -> str:
       if not self._skills:
           return "(none)"
       return "\n".join(f"- {s.name}: {s.description}"
                        for s in self._skills.values())
   def load(self, name: str) -> str | None:
       s = self._skills.get(name)
       if s is None:
           return None
       if name not in self.loaded:
           self.loaded.append(name)
       return s.body
class MemoryStore:
   def __init__(self, path: str):
       self.path = path
   def read(self) -> str:
       if os.path.exists(self.path):
           with open(self.path, "r", encoding="utf-8") as fh:
               return fh.read().strip()
       return ""
   def append(self, note: str) -> None:
       with open(self.path, "a", encoding="utf-8") as fh:
           fh.write(f"- {note}\n")
   def reset(self) -> None:
       if os.path.exists(self.path):
           os.remove(self.path)
</code></pre></div></div>



<p class="wp-block-paragraph">We build the practical tool layer that allows the harness to read files, write files, edit content, list files, search text, run Python code, simulate shell commands, search mock web data, load skills, remember notes, ask the user, and spawn subagents. We define each tool with typed inputs, descriptions, permissions, and executable behavior so the agent can interact with its environment in a structured way. We also add the skill library and persistent memory store, which help the agent load knowledge on demand and preserve useful information across sessions.</p>



<h3 id="h-defining-the-model-brain-layer" class="wp-block-heading"><strong>Defining the Model Brain Layer</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">class TransientLLMError(Exception):
   """A retryable error (timeout / 5xx / rate-limit)."""
class LLMBrain:
   """Interface: given the transcript + tool schemas, produce the next turn."""
   model = "abstract"
   async def stream(self, messages: list, tools: list, on_event=None) -> AssistantTurn:
       raise NotImplementedError
def Say(text: str) -> dict:
   return {"final": True, "text": text, "calls": []}
def Use(thought: str, calls: list) -> dict:
   return {"final": False, "text": thought, "calls": calls}
def last_tool_results(messages: list) -> list:
   """The tool results produced since the last assistant turn."""
   out = []
   for m in reversed(messages):
       if m.role == "tool":
           out.append({"name": m.name, "content": m.content})
       elif m.role == "assistant":
           break
   return list(reversed(out))
class ScriptedBrain(LLMBrain):
   model = "mock-sonnet"
   def __init__(self, script: list, name: str = "mock-sonnet"):
       self.script = script
       self.i = 0
       self.model = name
   async def stream(self, messages, tools, on_event=None) -> AssistantTurn:
       if self.i >= len(self.script):
           return AssistantTurn(text="(script exhausted)", stop_reason="end_turn")
       step = self.script[self.i]
       self.i += 1
       action = step(messages) if callable(step) else step
       text = action["text"]
       if on_event and text:
           on_event("text", text)
       calls = []
       for j, (tname, targs) in enumerate(action["calls"]):
           tc = ToolCall(id=f"call_{self.i}_{j}", name=tname, arguments=targs)
           calls.append(tc)
           if on_event:
               on_event("tool_call", tc)
       in_tok = sum(count_tokens(m.content) for m in messages) + 200
       out_tok = count_tokens(text) + sum(count_tokens(json.dumps(c.arguments))
                                          for c in calls)
       return AssistantTurn(
           text=text,
           tool_calls=calls,
           stop_reason="tool_use" if calls else "end_turn",
           usage=Usage(in_tok, out_tok),
       )
class FlakyBrain(LLMBrain):
   """Wraps a brain and fails the first `fail_times` calls with a transient
   error, so we can watch the engine retry with exponential backoff."""
   def __init__(self, inner: LLMBrain, fail_times: int = 1):
       self.inner = inner
       self.failures_left = fail_times
       self.model = inner.model
   async def stream(self, messages, tools, on_event=None) -> AssistantTurn:
       if self.failures_left > 0:
           self.failures_left -= 1
           raise TransientLLMError("simulated 503 from provider")
       return await self.inner.stream(messages, tools, on_event)
class RetryingBrain(LLMBrain):
   def __init__(self, inner: LLMBrain, retries: int = 4, base_delay: float = 0.05):
       self.inner = inner
       self.retries = retries
       self.base_delay = base_delay
       self.model = inner.model
   async def stream(self, messages, tools, on_event=None) -> AssistantTurn:
       attempt = 0
       while True:
           try:
               return await self.inner.stream(messages, tools, on_event)
           except TransientLLMError as e:
               if attempt >= self.retries:
                   raise
               delay = self.base_delay * (2 ** attempt)
               print(f"        <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/26a0.png" alt="⚠" class="wp-smiley" style="height: 1em; max-height: 1em;" />  transient error ({e}); "
                     f"retry {attempt + 1}/{self.retries} in {delay:.2f}s")
               await asyncio.sleep(delay)
               attempt += 1
class RealLLMBrain(LLMBrain):
   def __init__(self, api_format: str, model: str, api_key: str,
                base_url: str, system: str = ""):
       self.api_format = api_format
       self.model = model
       self.api_key = api_key
       self.base_url = base_url.rstrip("/")
       self.system = system
   def _post(self, url: str, headers: dict, payload: dict) -> dict:
       data = json.dumps(payload).encode()
       req = urllib.request.Request(url, data=data, headers=headers, method="POST")
       try:
           with urllib.request.urlopen(req, timeout=60) as resp:
               return json.loads(resp.read().decode())
       except urllib.error.HTTPError as e:
           if e.code in (429, 500, 502, 503, 504):
               raise TransientLLMError(f"HTTP {e.code}")
           raise
       except urllib.error.URLError as e:
           raise TransientLLMError(str(e))
   def _tools_anthropic(self, tools: list) -> list:
       return [{"name": t["name"], "description": t["description"],
                "input_schema": t["input_schema"]} for t in tools]
   def _tools_openai(self, tools: list) -> list:
       return [{"type": "function", "function": {
           "name": t["name"], "description": t["description"],
           "parameters": t["input_schema"]}} for t in tools]
   def _msgs_anthropic(self, messages: list) -> list:
       out = []
       for m in messages:
           if m.role == "user":
               out.append({"role": "user", "content": m.content})
           elif m.role == "assistant":
               blocks = []
               if m.content:
                   blocks.append({"type": "text", "text": m.content})
               for tc in m.tool_calls:
                   blocks.append({"type": "tool_use", "id": tc.id,
                                  "name": tc.name, "input": tc.arguments})
               out.append({"role": "assistant", "content": blocks or m.content})
           elif m.role == "tool":
               out.append({"role": "user", "content": [
                   {"type": "tool_result", "tool_use_id": m.tool_call_id,
                    "content": m.content}]})
       return out
   def _msgs_openai(self, messages: list) -> list:
       out = []
       if self.system:
           out.append({"role": "system", "content": self.system})
       for m in messages:
           if m.role == "user":
               out.append({"role": "user", "content": m.content})
           elif m.role == "assistant":
               msg = {"role": "assistant", "content": m.content or None}
               if m.tool_calls:
                   msg["tool_calls"] = [{
                       "id": tc.id, "type": "function",
                       "function": {"name": tc.name,
                                    "arguments": json.dumps(tc.arguments)}}
                       for tc in m.tool_calls]
               out.append(msg)
           elif m.role == "tool":
               out.append({"role": "tool", "tool_call_id": m.tool_call_id,
                           "content": m.content})
       return out
   async def stream(self, messages, tools, on_event=None) -> AssistantTurn:
       loop = asyncio.get_event_loop()
       if self.api_format == "anthropic":
           payload = {"model": self.model, "max_tokens": 1024,
                      "system": self.system,
                      "messages": self._msgs_anthropic(messages),
                      "tools": self._tools_anthropic(tools)}
           headers = {"x-api-key": self.api_key,
                      "anthropic-version": "2023-06-01",
                      "content-type": "application/json"}
           url = f"{self.base_url}/v1/messages"
           data = await loop.run_in_executor(None, self._post, url, headers, payload)
           text, calls = "", []
           for block in data.get("content", []):
               if block.get("type") == "text":
                   text += block["text"]
               elif block.get("type") == "tool_use":
                   calls.append(ToolCall(block["id"], block["name"],
                                         block.get("input", {})))
           u = data.get("usage", {})
           usage = Usage(u.get("input_tokens", 0), u.get("output_tokens", 0))
           stop = "tool_use" if calls else "end_turn"
       else:
           payload = {"model": self.model,
                      "messages": self._msgs_openai(messages),
                      "tools": self._tools_openai(tools)}
           headers = {"Authorization": f"Bearer {self.api_key}",
                      "Content-Type": "application/json"}
           url = f"{self.base_url}/chat/completions"
           data = await loop.run_in_executor(None, self._post, url, headers, payload)
           choice = data["choices"][0]["message"]
           text = choice.get("content") or ""
           calls = []
           for tc in (choice.get("tool_calls") or []):
               fn = tc["function"]
               try:
                   cargs = json.loads(fn.get("arguments") or "{}")
               except json.JSONDecodeError:
                   cargs = {}
               calls.append(ToolCall(tc["id"], fn["name"], cargs))
           u = data.get("usage", {})
           usage = Usage(u.get("prompt_tokens", 0), u.get("completion_tokens", 0))
           stop = "tool_use" if calls else "end_turn"
       if on_event and text:
           on_event("text", text)
       for c in calls:
           if on_event:
               on_event("tool_call", c)
       return AssistantTurn(text=text, tool_calls=calls,
                            stop_reason=stop, usage=usage)
def make_real_brain(system: str = "") -> LLMBrain | None:
   """Build a real brain from env vars, or return None to fall back to mock."""
   if os.environ.get("USE_REAL_LLM", "0") not in ("1", "true", "True"):
       return None
   if os.environ.get("ANTHROPIC_API_KEY"):
       return RealLLMBrain(
           api_format="anthropic",
           model=os.environ.get("MODEL", "claude-sonnet-4-6"),
           api_key=os.environ["ANTHROPIC_API_KEY"],
           base_url=os.environ.get("ANTHROPIC_BASE_URL", "https://api.anthropic.com"),
           system=system)
   if os.environ.get("OPENAI_API_KEY"):
       return RealLLMBrain(
           api_format="openai",
           model=os.environ.get("MODEL", "gpt-4.1"),
           api_key=os.environ["OPENAI_API_KEY"],
           base_url=os.environ.get("OPENAI_BASE_URL", "https://api.openai.com/v1"),
           system=system)
   return None
</code></pre></div></div>



<p class="wp-block-paragraph">We define the model brain layer that decides what the agent does next in the loop. We create a scripted mock brain for deterministic execution, a flaky brain to simulate provider errors, a retrying wrapper with exponential backoff, and a real provider brain for Anthropic- or OpenAI-compatible APIs. We use this snippet to show that the harness remains the same while the intelligence layer can switch between mock execution and real LLM calls.</p>



<h2 class="wp-block-heading"><strong>Assembling the System Prompt and the QueryEngine Agent Loop</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def assemble_system_prompt(*, base: str, project_context: str, memory: str,
                          skills_summary: str, tool_names: list) -> str:
   """Mirror OpenHarness prompt assembly: base + CLAUDE.md + MEMORY.md +
   on-demand skill list + available tools."""
   parts = [base.strip()]
   if project_context:
       parts.append(f"## Project context (CLAUDE.md)\n{project_context.strip()}")
   if memory:
       parts.append(f"## Long-term memory (MEMORY.md)\n{memory.strip()}")
   if skills_summary and skills_summary != "(none)":
       parts.append("## Available skills (load on demand with the `skill` tool)\n"
                    + skills_summary)
   parts.append("## Available tools\n" + ", ".join(tool_names))
   return "\n\n".join(parts)
def estimate_messages_tokens(messages: list) -> int:
   return sum(count_tokens(m.content) +
              sum(count_tokens(json.dumps(c.arguments)) for c in m.tool_calls)
              for m in messages)
def maybe_compact(messages: list, *, max_tokens: int, keep_last: int = 4,
                 verbose: bool = True) -> list:
   """Auto-compaction: when the transcript grows past `max_tokens`, summarize
   the older middle into one note while preserving the original task and the
   most recent turns. Real OpenHarness asks the model to write the summary and
   also preserves task state + channel logs; we use a heuristic here."""
   tok = estimate_messages_tokens(messages)
   if tok &lt;= max_tokens or len(messages) &lt;= keep_last + 1:
       return messages
   first = messages[0]
   tail = messages[-keep_last:]
   middle = messages[1:-keep_last]
   facts = []
   for m in middle:
       if m.role == "tool" and not m.content.startswith("ERROR"):
           facts.append(f"{m.name}: {short(m.content, 80)}")
       elif m.role == "assistant" and m.content:
           facts.append(f"assistant: {short(m.content, 80)}")
   summary = Message(
       role="system",
       content=("[Auto-compacted context] Earlier "
                f"{len(middle)} messages summarized. Key results:\n  - "
                + "\n  - ".join(facts[-8:])),
   )
   compacted = [first, summary] + tail
   if verbose:
       print(f"        <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f5dc.png" alt="🗜" class="wp-smiley" style="height: 1em; max-height: 1em;" />  auto-compaction: {len(messages)} msgs "
             f"(~{tok} tok) -> {len(compacted)} msgs "
             f"(~{estimate_messages_tokens(compacted)} tok)")
   return compacted
def console_printer(event: str, data) -> None:
   if event == "text" and data:
       print(f"  <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f916.png" alt="🤖" class="wp-smiley" style="height: 1em; max-height: 1em;" /> {short(data, 400)}")
   elif event == "tool_call":
       print(f"     ↳ call {data.name}({short(json.dumps(data.arguments), 120)})")
   elif event == "tool_result":
       flag = "✗" if data.get("is_error") else "✓"
       print(f"       {flag} {short(data['output'], 200)}")
class QueryEngine:
   def __init__(self, *, brain: LLMBrain, registry: ToolRegistry,
                ctx: ToolContext, perms: PermissionChecker, hooks: HookManager,
                system_prompt: str, cost: CostMeter | None = None,
                approve=auto_approve, max_turns: int = 12,
                compact_at_tokens: int | None = None):
       self.brain = brain
       self.registry = registry
       self.ctx = ctx
       self.perms = perms
       self.hooks = hooks
       self.system_prompt = system_prompt
       self.cost = cost or CostMeter(getattr(brain, "model", "default"))
       self.approve = approve
       self.max_turns = max_turns
       self.compact_at_tokens = compact_at_tokens
   async def execute_tool(self, call: ToolCall, on_event=None) -> ToolResult:
       tool = self.registry.get(call.name)
       if tool is None:
           return ToolResult(f"Unknown tool: {call.name}", is_error=True)
       decision = self.perms.check(tool, call.arguments)
       if decision.action == "deny":
           r = ToolResult(f"DENIED by permissions: {decision.reason}", is_error=True)
           if on_event:
               on_event("tool_result", {"output": r.output, "is_error": True})
           return r
       if decision.action == "ask":
           ok = await self.approve(tool, call.arguments, decision.reason)
           if not ok:
               r = ToolResult("DENIED by user.", is_error=True)
               if on_event:
                   on_event("tool_result", {"output": r.output, "is_error": True})
               return r
       pre = self.hooks.run_pre(call, tool, self.ctx)
       if pre.blocked:
           r = ToolResult(f"BLOCKED by hook: {pre.reason}", is_error=True)
           if on_event:
               on_event("tool_result", {"output": r.output, "is_error": True})
           return r
       args = pre.arguments
       try:
           result = await tool.run(args, self.ctx)
       except Exception as e:
           result = ToolResult(f"Tool raised: {e}", is_error=True)
       result = self.hooks.run_post(call, tool, result, self.ctx)
       if on_event:
           on_event("tool_result", {"output": result.output,
                                    "is_error": result.is_error})
       return result
   async def run(self, task: str, on_event=console_printer) -> str:
       messages: list = [Message(role="user", content=task)]
       for turn in range(self.max_turns):
           if self.compact_at_tokens:
               messages = maybe_compact(messages, max_tokens=self.compact_at_tokens)
           assistant = await self.brain.stream(messages, self.registry.schemas(),
                                               on_event)
           self.cost.add(assistant.usage)
           if assistant.stop_reason != "tool_use" or not assistant.tool_calls:
               return assistant.text
           messages.append(Message(role="assistant", content=assistant.text,
                                   tool_calls=assistant.tool_calls))
           results = await asyncio.gather(
               *[self.execute_tool(tc, on_event) for tc in assistant.tool_calls]
           )
           for tc, res in zip(assistant.tool_calls, results):
               content = res.output
               if res.is_error and not content.startswith(("DENIED", "BLOCKED")):
                   content = "ERROR: " + content
               elif res.is_error:
                   content = "ERROR: " + content
               messages.append(Message(role="tool", name=tc.name,
                                       tool_call_id=tc.id, content=content))
       return "(stopped: reached max_turns)"
DEFAULT_TOOLS = [
   WriteFileTool, ReadFileTool, EditTool, ListFilesTool, GrepTool,
   RunPythonTool, ShellTool, WebSearchTool, SkillTool, RememberTool,
   AskUserTool, SpawnAgentTool,
]
def build_registry(tool_classes=None) -> ToolRegistry:
   reg = ToolRegistry()
   for cls in (tool_classes or DEFAULT_TOOLS):
       reg.register(cls())
   return reg
BASE_SYSTEM = (
   "You are an autonomous coding/research agent running inside OpenHarness. "
   "Use tools to take real actions. Think step by step, verify your work by "
   "running it, and only stop when the task is truly complete."
)
</code></pre></div></div>



<p class="wp-block-paragraph">We assemble the system prompt, estimate transcript size, compact long conversations, print streaming events, and define the main QueryEngine. We make the engine responsible for asking the brain what to do, checking permissions, running hooks, executing tools, collecting results, and looping until the task is finished. We also register the default tools and create the base system instruction that guides the agent toward verified and tool-based work.</p>



<h3 class="wp-block-heading"><strong>Running the Agent Loop, Permission Governance, and On-Demand Skills Demos</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">async def demo_agent_loop():
   explain(
       "DEMO 1 — The Agent Loop (write code → run → see it fail → fix → pass)",
       """This is the centerpiece. A scripted 'brain' drives the real harness:
       each turn it emits tool calls, the engine runs them through permissions +
       hooks, feeds results back, and the brain REACTS. It writes a factorial
       module with an off-by-one bug, writes a test, runs it, sees the failure
       in the tool result, fixes the bug, re-runs to green, and saves a note to
       memory. We also wrap the brain in retry/backoff and a one-time transient
       failure so you can watch recovery — and we track token cost throughout.""")
   buggy = (
       "def factorial(n):\n"
       "    result = 1\n"
       "    for i in range(1, n):\n"
       "        result *= i\n"
       "    return result\n"
   )
   test_code = (
       'assert factorial(0) == 1, "0! should be 1"\n'
       'assert factorial(5) == 120, "5! should be 120 but got " + str(factorial(5))\n'
       'print("All tests passed: 0!=1 and 5!=120")\n'
   )
   def reactive_fix(messages):
       outs = last_tool_results(messages)
       failed = any("ERROR" in o["content"] or "Assert" in o["content"]
                    for o in outs)
       if failed:
           return Use("The test failed — classic off-by-one in the loop range. "
                      "range(1, n) stops at n-1, so I'll make it range(1, n + 1).",
                      [("edit", {"path": "mathutils.py",
                                 "old": "range(1, n)",
                                 "new": "range(1, n + 1)"})])
       return Use("Tests already pass; re-running to confirm.",
                  [("run_python", {"files": ["mathutils.py", "test_math.py"]})])
   script = [
       Use("First I'll create the factorial module.",
           [("write_file", {"path": "mathutils.py", "content": buggy})]),
       Use("Now a test that pins down the expected behavior.",
           [("write_file", {"path": "test_math.py", "content": test_code})]),
       Use("Let me run the test to see if it works.",
           [("run_python", {"files": ["mathutils.py", "test_math.py"]})]),
       reactive_fix,
       Use("Re-running the test after the fix.",
           [("run_python", {"files": ["mathutils.py", "test_math.py"]})]),
       Use("I'll remember the lesson for next time.",
           [("remember",
             {"note": "factorial(n): loop must be range(1, n+1); "
                      "range(1, n) gives n!/n (off-by-one)."})]),
       lambda m: Say("Done <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2705.png" alt="✅" class="wp-smiley" style="height: 1em; max-height: 1em;" />  Created mathutils.py with factorial(), wrote a "
                     "test, caught an off-by-one bug (range(1, n) → "
                     "range(1, n + 1)), fixed it, and the test now passes. "
                     "Saved the lesson to memory."),
   ]
   vfs = VirtualFS()
   memory = MemoryStore(os.path.join(tempfile.gettempdir(), "oh_demo1_mem.md"))
   memory.reset()
   skills = SkillLibrary()
   registry = build_registry()
   ctx = ToolContext(vfs=vfs, memory=memory, skills=skills)
   hooks = HookManager()
   hooks.add_pre(lambda call, tool, c:
                 print(f"        <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1fa9d.png" alt="🪝" class="wp-smiley" style="height: 1em; max-height: 1em;" /> pre-hook saw {tool.name}") or None)
   brain = RetryingBrain(FlakyBrain(ScriptedBrain(script), fail_times=1))
   cost = CostMeter("mock-sonnet")
   engine = QueryEngine(
       brain=brain, registry=registry, ctx=ctx,
       perms=PermissionChecker(PermissionMode.DEFAULT),
       hooks=hooks,
       system_prompt=assemble_system_prompt(
           base=BASE_SYSTEM, project_context="", memory=memory.read(),
           skills_summary=skills.summary(), tool_names=registry.names()),
       cost=cost, approve=auto_approve, max_turns=10,
   )
   print("\n[running the agent loop]\n")
   final = await engine.run("Implement factorial() with a test; make tests pass.")
   print(f"\n  FINAL ANSWER:\n  {final}")
   print(f"\n  Virtual filesystem now contains:\n{vfs.tree()}")
   print(f"\n  Final mathutils.py:\n" +
         textwrap.indent(vfs.read("mathutils.py"), "    "))
   print(f"\n  <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f4b0.png" alt="💰" class="wp-smiley" style="height: 1em; max-height: 1em;" /> cost: {cost.summary()}")
   print("\n  TAKEAWAY: the model only decides WHAT to do; the harness handles "
         "HOW — permissions, hooks, execution, retries, results, and cost.")
async def demo_permissions():
   explain(
       "DEMO 2 — Governance: permission modes, path rules, denied commands",
       """The harness is the safety boundary. The same write_file tool behaves
       differently under default/auto/plan modes; sensitive paths are always
       denied; path rules and denied-command patterns add fine-grained control;
       and PreToolUse hooks can veto a call outright.""")
   write = WriteFileTool()
   shell = ShellTool()
   print("\n  (a) Same write under each mode:")
   for mode in (PermissionMode.DEFAULT, PermissionMode.AUTO, PermissionMode.PLAN):
       d = PermissionChecker(mode).check(write, {"path": "notes.txt", "content": "x"})
       print(f"      {mode.value:8s} -> {d.action.upper():5s}  ({d.reason})")
   print("\n  (b) Built-in sensitive-path protection (any mode):")
   for p in ["app/main.py", ".env", "~/.ssh/id_rsa", "secrets/key.txt"]:
       d = PermissionChecker(PermissionMode.AUTO).check(write, {"path": p, "content": "x"})
       print(f"      write {p:18s} -> {d.action.upper():5s}  ({d.reason})")
   print("\n  (c) Custom path_rules + denied_commands:")
   pc = PermissionChecker(
       PermissionMode.AUTO,
       path_rules=[{"pattern": "build/*", "allow": False}],
       denied_commands=[r"rm\s+-rf\s+/", r"DROP\s+TABLE"])
   print("      write build/x ->",
         pc.check(write, {"path": "build/x", "content": "x"}).action.upper())
   print("      shell 'rm -rf /' ->",
         pc.check(shell, {"command": "rm -rf /"}).action.upper())
   print("      shell 'ls -la' ->",
         pc.check(shell, {"command": "ls -la"}).action.upper())
   print("\n  (d) A PreToolUse security hook that vetoes writing credentials:")
   def security_hook(call, tool, ctx):
       if tool.name == "write_file" and "password" in call.arguments.get("content", "").lower():
           return HookOutcome(blocked=True, reason="content looks like a secret")
       return None
   hooks = HookManager().add_pre(security_hook)
   ctx = ToolContext(vfs=VirtualFS())
   engine = QueryEngine(brain=ScriptedBrain([]), registry=build_registry(),
                        ctx=ctx, perms=PermissionChecker(PermissionMode.AUTO),
                        hooks=hooks, system_prompt="", approve=auto_approve)
   r = await engine.execute_tool(
       ToolCall("c1", "write_file",
                {"path": "config.txt", "content": "password=hunter2"}),
       on_event=console_printer)
   print(f"      result -> {r.output}")
   print("\n  TAKEAWAY: permissions + hooks form layered defense around every "
         "single tool call, independent of how clever the model is.")
async def demo_skills():
   explain(
       "DEMO 3 — Skills: on-demand knowledge loading",
       """Skills are markdown playbooks. Only their name+description sit in the
       system prompt (cheap); the full body is pulled in ONLY when the model
       decides it needs it, via the `skill` tool. This keeps context small while
       giving the agent deep, swappable expertise. Compatible in spirit with
       anthropics/skills (drop a .md file in and it just works).""")
   skills = SkillLibrary()
   skills.add_markdown(textwrap.dedent("""\
       ---
       name: commit
       description: Create clean, conventional git commits
       ---
       # Commit skill
       2. Group related changes; write a Conventional Commit subject
          (feat:, fix:, docs:, refactor:, test:).
       3. Keep the subject &lt;= 50 chars; explain *why* in the body.
       """))
   skills.add_markdown(textwrap.dedent("""\
       ---
       name: review
       description: Review code for bugs, security, and clarity
       ---
       # Review skill
       Check: correctness, edge cases, input validation, error handling,
       secrets in code, naming, and test coverage. Be specific and kind.
       """))
   registry = build_registry()
   ctx = ToolContext(vfs=VirtualFS(), skills=skills,
                     memory=MemoryStore(os.path.join(tempfile.gettempdir(),
                                                     "oh_d3.md")))
   sysprompt = assemble_system_prompt(
       base=BASE_SYSTEM, project_context="", memory="",
       skills_summary=skills.summary(), tool_names=registry.names())
   print("  System prompt's skills section (names+descriptions only):")
   print(textwrap.indent(skills.summary(), "      "))
   print(f"\n  Loaded skill bodies so far: {skills.loaded}  (none yet!)")
   script = [
       Use("This is a commit task — I'll load the commit skill first.",
           [("skill", {"name": "commit"})]),
       lambda m: Say("Following the commit playbook I just loaded, I'd stage the "
                     "related changes and write: 'fix: correct factorial "
                     "off-by-one in range bound'."),
   ]
   engine = QueryEngine(brain=ScriptedBrain(script), registry=registry, ctx=ctx,
                        perms=PermissionChecker(PermissionMode.AUTO),
                        hooks=HookManager(), system_prompt=sysprompt)
   print("\n[running]\n")
   final = await engine.run("Help me commit my changes properly.")
   print(f"\n  FINAL: {final}")
   print(f"\n  Loaded skill bodies now: {skills.loaded}  (loaded on demand)")
   print("\n  TAKEAWAY: knowledge is paged in just-in-time, not crammed into "
         "every prompt.")
</code></pre></div></div>



<p class="wp-block-paragraph">We implement the first three demos to show the harness in action. We first run a complete agent loop where the system writes buggy factorial code, tests it, detects the failure, fixes the bug, reruns the test, and saves the lesson to memory. We then explore permission governance and on-demand skills to see how safety controls and reusable playbooks improve the agent workflow.</p>



<h3 class="wp-block-heading"><strong>Adding Memory, Context Compaction, and Multi-Agent Coordination</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">async def demo_memory():
   explain(
       "DEMO 4 — Memory: persistent MEMORY.md across sessions",
       """Long-term memory survives between runs by persisting to MEMORY.md. In
       session 1 the agent records a user preference; in a brand-new session 2
       (fresh engine, fresh transcript) that memory is injected into the system
       prompt, so the agent already 'knows' the user.""")
   mem_path = os.path.join(tempfile.gettempdir(), "oh_demo4_MEMORY.md")
   memory = MemoryStore(mem_path)
   memory.reset()
   registry = build_registry()
   print("  ── Session 1 ──")
   ctx1 = ToolContext(vfs=VirtualFS(), memory=memory, skills=SkillLibrary())
   s1 = [
       Use("I'll remember the user's stated preferences.",
           [("remember", {"note": "User prefers metric units and concise answers."})]),
       lambda m: Say("Noted your preferences for next time."),
   ]
   eng1 = QueryEngine(brain=ScriptedBrain(s1), registry=registry, ctx=ctx1,
                      perms=PermissionChecker(PermissionMode.AUTO),
                      hooks=HookManager(),
                      system_prompt=assemble_system_prompt(
                          base=BASE_SYSTEM, project_context="",
                          memory=memory.read(),
                          skills_summary="(none)", tool_names=registry.names()))
   await eng1.run("Remember that I like metric units and short answers.")
   print(f"  MEMORY.md is now:\n{textwrap.indent(memory.read(), '      ')}")
   print("\n  ── Session 2 (new session, memory reloaded from disk) ──")
   memory2 = MemoryStore(mem_path)
   ctx2 = ToolContext(vfs=VirtualFS(), memory=memory2, skills=SkillLibrary())
   sysprompt2 = assemble_system_prompt(
       base=BASE_SYSTEM, project_context="", memory=memory2.read(),
       skills_summary="(none)", tool_names=registry.names())
   print("  The new system prompt already contains:")
   print(textwrap.indent("## Long-term memory (MEMORY.md)\n" + memory2.read(),
                         "      "))
   s2 = [lambda m: Say("Since you prefer metric and brevity: it's about 5 km. <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f642.png" alt="🙂" class="wp-smiley" style="height: 1em; max-height: 1em;" />")]
   eng2 = QueryEngine(brain=ScriptedBrain(s2), registry=registry, ctx=ctx2,
                      perms=PermissionChecker(PermissionMode.AUTO),
                      hooks=HookManager(), system_prompt=sysprompt2)
   final = await eng2.run("How far is a 5000 meter run, roughly?")
   print(f"\n  FINAL: {final}")
   print("\n  TAKEAWAY: state that should outlive a conversation goes to memory, "
         "then is re-injected at the start of future sessions.")
async def demo_compaction():
   explain(
       "DEMO 5 — Context auto-compaction (multi-day sessions without overflow)",
       """As a session grows, the transcript can blow past the context window.
       Auto-compaction summarizes the older middle of the conversation into a
       compact note while preserving the original task and the most recent
       turns — so long-running agents keep going. (We force a tiny threshold to
       trigger it; real OpenHarness asks the model to write the summary.)""")
   msgs = [Message(role="user", content="Build and verify a data pipeline.")]
   for i in range(8):
       msgs.append(Message(role="assistant", content=f"Step {i}: doing work...",
                           tool_calls=[ToolCall(f"c{i}", "shell",
                                                {"command": f"process chunk {i}"})]))
       msgs.append(Message(role="tool", name="shell", tool_call_id=f"c{i}",
                           content=f"chunk {i} processed: 1000 rows ok " * 4))
   before = estimate_messages_tokens(msgs)
   print(f"  Before: {len(msgs)} messages, ~{before} tokens")
   compacted = maybe_compact(msgs, max_tokens=300, keep_last=4)
   after = estimate_messages_tokens(compacted)
   print(f"  After:  {len(compacted)} messages, ~{after} tokens "
         f"({100 * (before - after) // before}% smaller)")
   print("\n  The injected summary message:")
   print(textwrap.indent(compacted[1].content, "      "))
   print("\n  TAKEAWAY: the harness manages the context window so the agent can "
         "run far longer than a single window allows.")
async def demo_multi_agent():
   explain(
       "DEMO 6 — Swarm coordination: spawning parallel subagents",
       """A lead agent decomposes a task and delegates to specialized subagents.
       Each subagent is its OWN harness (own loop, own brain, own tools). Two
       researchers run IN PARALLEL (issued in the same turn → asyncio.gather),
       then a writer synthesizes their findings. The team registry tracks who
       did what.""")
   def researcher_profile():
       reg = build_registry([WebSearchTool])
       script = [
           Use("Researching via web search.",
               [("web_search", {"query": "PLACEHOLDER"})]),
           lambda m: Say("Summary: " +
                         short(last_tool_results(m)[0]["content"], 160)),
       ]
       return ScriptedBrain(script), reg
   def writer_profile():
       reg = build_registry([WriteFileTool])
       script = [lambda m: Say("Synthesized brief combining both research notes "
                               "into a coherent paragraph.")]
       return ScriptedBrain(script), reg
   profiles = {"researcher": researcher_profile, "writer": writer_profile}
   vfs = VirtualFS()
   memory = MemoryStore(os.path.join(tempfile.gettempdir(), "oh_d6.md"))
   skills = SkillLibrary()
   team: list = []
   def make_spawn():
       async def spawn(role: str, task: str) -> str:
           factory = profiles.get(role)
           if not factory:
               return f"(no such role: {role})"
           child_brain, child_reg = factory()
           if role == "researcher" and child_brain.script:
               child_brain.script[0] = Use(f"Researching: {task}",
                                           [("web_search", {"query": task})])
           child_ctx = ToolContext(vfs=vfs, memory=memory, skills=skills,
                                   spawn=spawn)
           child_engine = QueryEngine(
               brain=child_brain, registry=child_reg, ctx=child_ctx,
               perms=PermissionChecker(PermissionMode.AUTO),
               hooks=HookManager(), system_prompt="(subagent)",
               approve=auto_approve, max_turns=6)
           print(f"        <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f9d1-200d-1f527.png" alt="🧑‍🔧" class="wp-smiley" style="height: 1em; max-height: 1em;" /> spawned [{role}] for: {short(task, 60)}")
           result = await child_engine.run(task, on_event=None)
           team.append({"role": role, "task": task, "result": result})
           return result
       return spawn
   ctx = ToolContext(vfs=vfs, memory=memory, skills=skills, spawn=make_spawn())
   registry = build_registry()
   lead_script = [
       Use("I'll split this: research vector databases AND agent harnesses in "
           "parallel, then have a writer combine the findings.",
           [("spawn_agent", {"role": "researcher",
                             "task": "vector database for RAG"}),
            ("spawn_agent", {"role": "researcher",
                             "task": "agent harness design"})]),
       Use("Both research notes are in — delegating synthesis to the writer.",
           [("spawn_agent", {"role": "writer",
                             "task": "combine the two research notes"})]),
       lambda m: Say("Coordination complete: 2 researchers (parallel) + 1 "
                     "writer produced a combined brief."),
   ]
   engine = QueryEngine(brain=ScriptedBrain(lead_script), registry=registry,
                        ctx=ctx, perms=PermissionChecker(PermissionMode.AUTO),
                        hooks=HookManager(), system_prompt="(lead agent)",
                        max_turns=8)
   print("\n[running the lead agent]\n")
   t0 = time.time()
   final = await engine.run("Produce a short brief on building RAG agents.")
   dt = time.time() - t0
   print(f"\n  FINAL: {final}")
   print(f"\n  Team registry ({len(team)} subagent runs, total {dt:.3f}s):")
   for entry in team:
       print(f"    - [{entry['role']}] {short(entry['task'], 40)} -> "
             f"{short(entry['result'], 80)}")
   print("\n  TAKEAWAY: the same loop nests — a 'tool' can be an entire agent, "
         "enabling parallel teams and delegation.")
async def demo_real_provider():
   explain(
       "DEMO 7 — Swap in a REAL model (Anthropic / OpenAI-compatible)",
       """Everything above ran on a deterministic mock brain — zero keys, zero
       cost. Going live changes exactly ONE thing: the brain. The engine, tools,
       permissions, hooks, skills, memory, and coordinator are untouched. This
       is the whole point of a harness: the model is pluggable.""")
   print(textwrap.dedent("""\
         To run the SAME harness on a real model, set environment variables and
         re-run (works with any OpenAI- or Anthropic-compatible endpoint that
         OpenHarness supports: Claude, GPT, Kimi, GLM, DeepSeek, Qwen, Groq,
         Ollama, OpenRouter, ...):
             import os
             os.environ["USE_REAL_LLM"]    = "1"
             # --- Anthropic-style ---
             os.environ["ANTHROPIC_API_KEY"] = "sk-ant-..."
             os.environ["MODEL"]             = "claude-sonnet-4-6"
             # --- or OpenAI-style (incl. local Ollama) ---
             # os.environ["OPENAI_API_KEY"]  = "sk-..."
             # os.environ["OPENAI_BASE_URL"] = "http://localhost:11434/v1"
             # os.environ["MODEL"]           = "llama-3.3-70b"
         Then build the engine with the real brain instead of the mock:
             brain = make_real_brain(system=system_prompt) or ScriptedBrain([...])
             engine = QueryEngine(brain=brain, registry=registry, ctx=ctx, ...)
             await engine.run("Refactor utils.py and add tests.")
   """))
   sysprompt = assemble_system_prompt(
       base=BASE_SYSTEM, project_context="", memory="",
       skills_summary="(none)", tool_names=build_registry().names())
   real = make_real_brain(system=sysprompt)
   if real is None:
       print("  [USE_REAL_LLM not set → staying on the mock brain. "
             "Set the env vars above and re-run to go live.]")
       return
   print(f"  [LIVE] Using real provider: {real.api_format} / {real.model}\n")
   vfs = VirtualFS()
   ctx = ToolContext(vfs=vfs, memory=MemoryStore(
       os.path.join(tempfile.gettempdir(), "oh_real.md")),
       skills=SkillLibrary(), canned_answers={})
   engine = QueryEngine(
       brain=RetryingBrain(real), registry=build_registry(), ctx=ctx,
       perms=PermissionChecker(PermissionMode.AUTO), hooks=HookManager(),
       system_prompt=sysprompt, cost=CostMeter(real.model), max_turns=12)
   final = await engine.run(
       "Create greet.py with a function greet(name) that returns "
       "'Hello, &lt;name>!', then write and run a quick test to prove it works.")
   print(f"\n  FINAL: {final}")
   print(f"\n  Files:\n{vfs.tree()}")
   print(f"\n  <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f4b0.png" alt="💰" class="wp-smiley" style="height: 1em; max-height: 1em;" /> {engine.cost.summary()}")
async def main():
   banner("OpenHarness From Scratch — guided walkthrough")
   print(textwrap.dedent("""
       We will build up the harness one subsystem at a time:
         1. The agent loop  (tools, run/verify/fix, retries, cost)
         2. Permissions     (modes, sensitive paths, rules, hook veto)
         3. Skills          (on-demand knowledge)
         4. Memory          (persistent MEMORY.md across sessions)
         5. Compaction      (surviving long sessions)
         6. Multi-agent     (parallel subagent delegation)
         7. Real provider   (one-line swap to a live model)
       Architecture (what each piece is responsible for):
           User prompt
                │
                ▼
           QueryEngine ──► LLM brain (mock or real)   "WHAT to do"
                │  ▲            │ tool_use
                │  └────────────┘
                ▼
           For each tool call:  Permission ─► PreHook ─► Execute ─► PostHook
                                     │           │          │          │
                                 deny/ask     veto/edit   sandbox    redact
                │
                ▼
           Tool result ──► back into the transcript ──► loop
   """).rstrip())
   await demo_agent_loop()
   await demo_permissions()
   await demo_skills()
   await demo_memory()
   await demo_compaction()
   await demo_multi_agent()
   await demo_real_provider()
   banner("All demos complete <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f389.png" alt="🎉" class="wp-smiley" style="height: 1em; max-height: 1em;" />")
   print(textwrap.dedent("""\
       You just built the core of an agent harness:
         • a streaming tool-call loop with retries &amp; cost tracking
         • type-validated, self-describing tools
         • layered governance (permission modes + lifecycle hooks)
         • on-demand skills and persistent memory
         • context auto-compaction
         • nested multi-agent coordination
         • a one-line swap to a real LLM provider
       To go deeper, study the real project: https://github.com/HKUDS/OpenHarness
       (43+ tools, plugin ecosystem, MCP client, React/Ink TUI, the `oh` CLI,
       and the `ohmo` personal agent). "The model is the agent; the code is the
       harness."
   """))
run_async(main())
</code></pre></div></div>



<p class="wp-block-paragraph">We complete the tutorial with memory, context compaction, multi-agent coordination, real-provider switching, and the final guided walkthrough. We demonstrate how memory persists across sessions, how old context gets summarized, and how a lead agent delegates work to parallel subagents before synthesizing results. We finish by running all demos through the main function, yielding a complete, runnable view of an OpenHarness-style agent system.</p>



<h3 class="wp-block-heading"><strong>Conclusion</strong></h3>



<p class="wp-block-paragraph">In conclusion, we have a complete hands-on understanding of how an agent harnesses language-model reasoning with real, controlled actions. We saw how each subsystem contributes to reliability: tools make the agent capable, permissions keep execution safe, hooks add governance, memory preserves useful preferences, skills load knowledge only when needed, and compaction helps longer sessions stay manageable. We also explored how the same harness can support deterministic mock brains, real LLM providers, and nested subagents that work together on delegated tasks.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Agentic%20AI%20Codes/openharness_agent_runtime_from_scratch_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">Full Codes here</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/how-to-design-an-openharness-style-agent-runtime-with-tools-memory-permissions-skills-and-multi-agent-coordination/">How to Design an OpenHarness Style Agent Runtime with Tools, Memory, Permissions, Skills, and Multi-Agent Coordination</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80693</post-id>
	</item>
		<item>
		<title>Using Graphify and NetworkX to Map Python Codebase Structure with God Nodes, Communities, and Architecture Visualizations</title>
		<link>https://www.marktechpost.com/2026/06/24/using-graphify-and-networkx-to-map-python-codebase-structure-with-god-nodes-communities-and-architecture-visualizations/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Wed, 24 Jun 2026 09:36:59 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80690</guid>

					<description><![CDATA[<p>In this tutorial, we build a fully offline Graphify pipeline that turns a multi-module Python application into a knowledge graph. We install Graphify, generate a connected sample app, and extract the graph locally using tree-sitter, with no API key or LLM backend. We load graph.json into NetworkX and analyze file types, relationship types, centrality scores, community detection, and shortest paths. We then create static and interactive visualizations to see how modules, classes, functions, and database objects connect.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/using-graphify-and-networkx-to-map-python-codebase-structure-with-god-nodes-communities-and-architecture-visualizations/">Using Graphify and NetworkX to Map Python Codebase Structure with God Nodes, Communities, and Architecture Visualizations</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this <a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/AI%20Agents%20Codes/graphify_offline_code_knowledge_graph_tutorial_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">tutorial</a>, we build a fully offline<a href="https://github.com/safishamsi/graphify"> <strong>Graphify</strong></a> workflow that turns a realistic multi-module Python application into a knowledge graph. We start by installing Graphify and supporting graph libraries, then generate a small but connected sample application with configuration, database, authentication, service, API, cache, model, and SQL layers. We extract the graph locally using Graphify’s tree-sitter-based analysis, so we do not need an API key or any LLM backend. After loading the generated graph.json into NetworkX, we analyze the codebase&#8217;s structure using file types, relationship types, centrality scores, community detection, and shortest paths among important symbols. Also, we create both static and interactive visualizations, making it easier to understand how modules, classes, functions, and database objects connect across the project.</p>



<h2 id="h-installing-graphify-and-networkx" class="wp-block-heading"><strong>Installing Graphify and NetworkX</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import subprocess, sys
def pip(*pkgs):
   subprocess.run([sys.executable, "-m", "pip", "install", "-q", *pkgs], check=False)
pip("graphifyy[sql]", "pyvis", "networkx", "matplotlib")
import os, json, glob, textwrap, warnings
import networkx as nx
import matplotlib.pyplot as plt
warnings.filterwarnings("ignore")
</code></pre></div></div>



<p class="wp-block-paragraph">We install Graphify along with the graph analysis and visualization libraries needed for the tutorial. We import the required Python modules, including NetworkX for graph processing and Matplotlib for static plotting. We also suppress unnecessary warnings so the notebook output stays clean and focused.</p>



<h2 class="wp-block-heading"><strong>Building the Sample Codebase</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">ROOT = "sample_app"
os.makedirs(ROOT, exist_ok=True)
FILES = {
"config.py": '''
# Central settings object — used everywhere (expect this to be a "god node").
class Settings:
   def __init__(self):
       self.db_dsn = "postgresql://localhost/app"
       self.jwt_secret = "change-me"
       self.rate_limit = 100
settings = Settings()
''',
"database.py": '''
from config import settings
class DatabasePool:
   """Connection pool. WHY: reuse sockets instead of reconnecting per query."""
   def __init__(self, dsn):
       self.dsn = dsn
       self._conns = []
   def acquire(self):
       return {"dsn": self.dsn}
pool = DatabasePool(settings.db_dsn)
def get_connection():
   return pool.acquire()
''',
"models.py": '''
class User:
   def __init__(self, user_id, email):
       self.user_id = user_id
       self.email = email
class Session:
   def __init__(self, user, token):
       self.user = user
       self.token = token
''',
"cache.py": '''
from config import settings
class RateLimiter:
   # NOTE: naive in-memory limiter; swap for Redis in prod.
   def __init__(self, limit):
       self.limit = limit
       self.hits = {}
   def allow(self, key):
       self.hits[key] = self.hits.get(key, 0) + 1
       return self.hits[key] &lt;= self.limit
limiter = RateLimiter(settings.rate_limit)
''',
"auth.py": '''
from config import settings
from database import get_connection
from models import User, Session
def hash_password(raw):
   return f"hashed::{raw}"
def verify_password(raw, hashed):
   return hash_password(raw) == hashed
class AuthService:
   def __init__(self):
       self.secret = settings.jwt_secret
   def login(self, email, password):
       conn = get_connection()
       user = User(user_id=1, email=email)
       return Session(user=user, token=self.secret + email)
''',
"services.py": '''
from database import get_connection
from models import User
from auth import AuthService
class UserService:
   def __init__(self):
       self.auth = AuthService()
   def register(self, email, password):
       conn = get_connection()
       return User(user_id=2, email=email)
   def authenticate(self, email, password):
       return self.auth.login(email, password)
''',
"api.py": '''
from cache import limiter
from services import UserService
from auth import verify_password
svc = UserService()
def signup_route(email, password):
   if not limiter.allow(email):
       return {"error": "rate limited"}
   return svc.register(email, password)
def login_route(email, password):
   if not limiter.allow(email):
       return {"error": "rate limited"}
   return svc.authenticate(email, password)
''',
"main.py": '''
from api import signup_route, login_route
from database import pool
def run():
   signup_route("a@x.com", "pw")
   return login_route("a@x.com", "pw")
if __name__ == "__main__":
   run()
''',
"schema.sql": '''
CREATE TABLE users (
   user_id  SERIAL PRIMARY KEY,
   email    TEXT UNIQUE NOT NULL
);
CREATE TABLE sessions (
   token    TEXT PRIMARY KEY,
   user_id  INTEGER NOT NULL REFERENCES users(user_id)
);
CREATE VIEW active_sessions AS
SELECT s.token, u.email
FROM sessions s JOIN users u ON s.user_id = u.user_id;
''',
}
for name, body in FILES.items():
   with open(os.path.join(ROOT, name), "w") as f:
       f.write(textwrap.dedent(body).lstrip())
print(f"Wrote {len(FILES)} files to ./{ROOT}/")
</code></pre></div></div>



<p class="wp-block-paragraph">We create a realistic sample application with multiple Python modules and one SQL schema file. We design the files to include meaningful cross-module relationships, such as imports, function calls, service dependencies, authentication logic, database access, and rate limiting. We then write all these files to a local sample_app directory, giving Graphify a complete mini-codebase to analyze.</p>



<h2 class="wp-block-heading"><strong>Extracting the Knowledge Graph</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">res = subprocess.run(
   [sys.executable, "-m", "graphify", "extract", ROOT, "--no-cluster"],
   capture_output=True, text=True
)
print(res.stdout[-1500:] or res.stderr[-1500:])
graph_paths = glob.glob("**/graph.json", recursive=True)
assert graph_paths, "graph.json not found — check the extract output above."
GRAPH_JSON = sorted(graph_paths, key=os.path.getmtime)[-1]
print("Graph file:", GRAPH_JSON)
def load_graphify(path):
   data = json.load(open(path))
   ekey = "links" if "links" in data else ("edges" if "edges" in data else None)
   G = nx.DiGraph() if data.get("directed") else nx.Graph()
   for n in data.get("nodes", []):
       nid = n.get("id")
       G.add_node(nid, **{k: v for k, v in n.items() if k != "id"})
   for e in data.get(ekey or "links", []):
       G.add_edge(e.get("source"), e.get("target"),
                  **{k: v for k, v in e.items() if k not in ("source", "target")})
   G.graph.update(data.get("graph", {}))
   return G
G = load_graphify(GRAPH_JSON)
UG = G.to_undirected()
print(f"\nGraph: {G.number_of_nodes()} nodes, {G.number_of_edges()} edges")
def label(n):
   return G.nodes[n].get("label", str(n))
</code></pre></div></div>



<p class="wp-block-paragraph">We run Graphify locally on the generated application and extract the project knowledge graph without using any API key or LLM backend. We locate the generated graph.json file and load it into NetworkX using a version-proof node-link loader. We then convert the graph into an undirected form for easier structural analysis and define a helper function to display readable node labels.</p>



<h2 class="wp-block-heading"><strong>Analyzing Centrality and Communities</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">from collections import Counter
ftypes  = Counter(d.get("file_type", "?") for _, d in G.nodes(data=True))
rels    = Counter(d.get("relation", "?")  for *_ , d in G.edges(data=True))
conf    = Counter(d.get("confidence", "?") for *_ , d in G.edges(data=True))
print("\nNodes by file_type :", dict(ftypes))
print("Edges by relation  :", dict(rels))
print("Edges by confidence:", dict(conf))
deg = nx.degree_centrality(UG)
btw = nx.betweenness_centrality(UG)
print("\nTop 'god nodes' by degree centrality:")
for n, c in sorted(deg.items(), key=lambda x: -x[1])[:8]:
   print(f"  {label(n):&lt;22} deg={c:.3f}  betweenness={btw.get(n,0):.3f}")
try:
   communities = nx.community.louvain_communities(UG, seed=42)
except Exception:
   communities = list(nx.community.greedy_modularity_communities(UG))
node_comm = {n: i for i, com in enumerate(communities) for n in com}
print(f"\nDetected {len(communities)} communities:")
for i, com in enumerate(communities):
   members = ", ".join(sorted(label(n) for n in com))[:90]
   print(f"  Community {i}: {members}")
def find(substr):
   for n in G.nodes:
       if substr.lower() in label(n).lower():
           return n
   return None
a, b = find("api"), find("DatabasePool")
if a and b and nx.has_path(UG, a, b):
   path = nx.shortest_path(UG, a, b)
   print(f"\nPath {label(a)} -> {label(b)}:")
   print("   " + "  →  ".join(label(p) for p in path))
</code></pre></div></div>



<p class="wp-block-paragraph">We analyze the extracted graph by summarizing node types, edge relationships, and confidence levels. We compute degree centrality and betweenness centrality to identify important “god nodes” that connect many parts of the application. We also detect communities in the graph and trace a shortest path between key components to understand how parts of the codebase are connected.</p>



<h2 class="wp-block-heading"><strong>Visualizing the Code Graph</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">plt.figure(figsize=(13, 9))
pos = nx.spring_layout(UG, k=0.7, seed=42)
nx.draw_networkx_edges(UG, pos, alpha=0.25)
nx.draw_networkx_nodes(
   UG, pos,
   node_color=[node_comm.get(n, 0) for n in UG.nodes],
   node_size=[300 + 4000 * deg.get(n, 0) for n in UG.nodes],
   cmap=plt.cm.tab20, alpha=0.9,
)
top = {n for n, _ in sorted(deg.items(), key=lambda x: -x[1])[:14]}
nx.draw_networkx_labels(UG, pos, {n: label(n) for n in top}, font_size=8)
plt.title("Graphify knowledge graph — size=centrality, color=community")
plt.axis("off"); plt.tight_layout()
plt.savefig("graph_static.png", dpi=130); plt.show()
try:
   from pyvis.network import Network
   net = Network(height="650px", width="100%", bgcolor="#111", font_color="white",
                 notebook=True, cdn_resources="in_line", directed=G.is_directed())
   palette = ["#e6194B","#3cb44b","#4363d8","#f58231","#911eb4",
              "#42d4f4","#f032e6","#bfef45","#fabed4","#469990"]
   for n, d in G.nodes(data=True):
       c = node_comm.get(n, 0)
       net.add_node(n, label=label(n), title=f"{d.get('file_type','?')} · {d.get('source_file','')}",
                    color=palette[c % len(palette)], size=12 + 60 * deg.get(n, 0))
   for s, t, d in G.edges(data=True):
       net.add_edge(s, t, title=d.get("relation", ""))
   net.save_graph("graph_interactive.html")
   print("\nSaved interactive graph -> graph_interactive.html")
   from IPython.display import HTML, display
   display(HTML(open("graph_interactive.html").read()))
except Exception as e:
   print("Interactive viz skipped:", e)
for cmd in (
   ["query", "what connects auth to the database?", "--graph", GRAPH_JSON],
   ["path",  "AuthService", "DatabasePool", "--graph", GRAPH_JSON],
   ["explain", "RateLimiter", "--graph", GRAPH_JSON],
):
   print("\n$ graphify " + " ".join(cmd))
   r = subprocess.run([sys.executable, "-m", "graphify", *cmd],
                      capture_output=True, text=True)
   print((r.stdout or r.stderr)[:1200])
print("\nDone. Artifacts: graph_static.png, graph_interactive.html,",
     "and graphify-out/ (graph.json, GRAPH_REPORT.md).")
</code></pre></div></div>



<p class="wp-block-paragraph">We visualize the knowledge graph using both static and interactive methods. We first create a Matplotlib graph where node size represents centrality and node color represents community membership. We then build an interactive Pyvis visualization and run Graphify’s CLI commands to query the graph, find paths, and explain selected symbols.</p>



<h2 class="wp-block-heading"><strong>Conclusion</strong></h2>



<p class="wp-block-paragraph">In conclusion, we have a complete local pipeline for converting source code into a useful knowledge graph and studying it with graph analytics. We saw how Graphify extracts meaningful relationships from a Python and SQL codebase, and we use NetworkX to identify central “god nodes,” detect communities, and trace paths between components such as authentication and database logic. We also generated visual outputs that help us inspect the architecture from both a high-level and interactive perspective. This workflow gives us a pathway to reason about code structure, dependency flow, architectural hotspots, and cross-file connections without relying on external APIs, making it useful for codebase exploration, documentation, refactoring, and software architecture analysis.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/AI%20Agents%20Codes/graphify_offline_code_knowledge_graph_tutorial_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">Full Codes here</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/using-graphify-and-networkx-to-map-python-codebase-structure-with-god-nodes-communities-and-architecture-visualizations/">Using Graphify and NetworkX to Map Python Codebase Structure with God Nodes, Communities, and Architecture Visualizations</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80690</post-id>
	</item>
		<item>
		<title>Nous Research Adds /learn to Hermes Agent&#8217;s Skills System, Capturing Workflows as Slash Commands Without Hand-Writing SKILL.md</title>
		<link>https://www.marktechpost.com/2026/06/24/nous-research-adds-learn-to-hermes-agents-skills-system-capturing-workflows-as-slash-commands-without-hand-writing-skill-md/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Wed, 24 Jun 2026 09:21:49 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Agents]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80687</guid>

					<description><![CDATA[<p>Nous Research has added /learn to the Hermes Agent Skills System. The command authors a standards-compliant SKILL.md from a local directory, a doc URL, a past conversation, or pasted notes. The live agent sources the material with its own tools, then writes the skill — no hand-writing and no separate ingestion engine. Here is how it works, where it fits, and what to review before trusting the output.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/nous-research-adds-learn-to-hermes-agents-skills-system-capturing-workflows-as-slash-commands-without-hand-writing-skill-md/">Nous Research Adds /learn to Hermes Agent&#8217;s Skills System, Capturing Workflows as Slash Commands Without Hand-Writing SKILL.md</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Nous Research has expanded the Skills System inside Hermes Agent, its open-source self-improving agent. The new addition is <a href="https://hermes-agent.nousresearch.com/docs/user-guide/features/skills#learning-a-skill-from-sources-learn"><code>/learn</code>, a command that writes a reusable skill for you</a>. Point it at a document page, a local SDK, a past conversation, or pasted notes. The live agent gathers the material, then authors a <code>SKILL.md</code> on your behalf. </p>



<figure class="wp-block-embed is-type-rich is-provider-x wp-block-embed-x"><div class="wp-block-embed__wrapper">
<div class="embed-x"><blockquote class="twitter-tweet" data-width="550" data-dnt="true"><p lang="en" dir="ltr">Hermes Agent can now /learn from anything: feed it directories of any source material (code, API docs, manuals, PDFs, configs) and it distills a verifiable reusable skill <a href="https://t.co/oRznwCRF3E">pic.twitter.com/oRznwCRF3E</a></p>&mdash; Nous Research (@NousResearch) <a href="https://x.com/NousResearch/status/2069526242236182697?ref_src=twsrc%5Etfw">June 23, 2026</a></blockquote><script async src="https://platform.x.com/widgets.js" charset="utf-8"></script></div>
</div></figure>



<h2 id="h-hermes-skills-system" class="wp-block-heading"><strong>Hermes Skills System</strong></h2>



<p class="wp-block-paragraph">Skills are on-demand knowledge documents the agent loads when needed. Each one is a folder containing a <code>SKILL.md</code> file with instructions. They follow a <strong>progressive disclosure</strong> pattern to keep token usage low. The format is compatible with the <a href="https://agentskills.io/specification">agentskills.io</a> open standard.</p>



<p class="wp-block-paragraph">All skills live in <code>~/.hermes/skills/</code>, the single source of truth. On a fresh install, bundled skills are copied from the repo. Hub-installed and agent-created skills land there too. Every installed skill becomes a slash command automatically. Running <code>/plan</code> or <code>/axolotl</code> loads that skill&#8217;s instructions into the turn.</p>



<p class="wp-block-paragraph">Think of a skill as a reference document the agent reads only when relevant. Memory, by contrast, holds small durable facts that should always stay in context.</p>



<h2 id="h-how-learn-works" class="wp-block-heading"><strong>How /learn Works</strong></h2>



<p class="wp-block-paragraph"><code>/learn</code> removes the hand-writing step. You describe a source, and the agent does the sourcing with tools it already has. It reads local directories with <code>read_file</code> and <code>search_files</code>. It fetches online docs with <code>web_extract</code>. It can also capture a workflow you just walked it through.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php"># A local SDK or doc directory
/learn the REST client in ~/projects/acme-sdk, focus on auth + pagination

# An online doc page
/learn https://docs.example.com/api/quickstart

# The workflow you just completed in this conversation
/learn how I just deployed the staging server

# Pasted notes or a described procedure
/learn filing an expense: open the portal, New > Expense, attach receipt, submit</code></pre></div></div>



<p class="wp-block-paragraph">The agent then authors a skill that follows the house authoring standards. That means a description under 60 characters, the standard section order, and Hermes-tool framing. It does not invent commands that do not exist.</p>



<p class="wp-block-paragraph">There is no separate ingestion engine. <code>/learn</code> builds a standards-guided prompt and hands it to the agent as a normal turn. So it works the same in the CLI, the messaging gateway, the TUI, and the dashboard. It also works on any terminal backend, whether local, Docker, or remote. The dashboard adds a <strong>Learn a skill</strong> button with a directory field, a URL field, and a text box.</p>



<p class="wp-block-paragraph">The agent saves the result with the <code>skill_manage</code> tool. If you have the write-approval gate on, that approval step still applies.</p>



<h2 id="h-why-skills-stay-cheap" class="wp-block-heading"><strong>Why Skills Stay Cheap</strong></h2>



<p class="wp-block-paragraph">Skills load in three levels, so the agent pays only for what it uses.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th>Level</th><th>Call</th><th>Returns</th><th>Approx. cost</th></tr></thead><tbody><tr><td>0</td><td><code>skills_list()</code></td><td>Names, descriptions, categories</td><td>~3k tokens</td></tr><tr><td>1</td><td><code>skill_view(name)</code></td><td>Full content plus metadata</td><td>Varies</td></tr><tr><td>2</td><td><code>skill_view(name, path)</code></td><td>A specific reference file</td><td>Varies</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">The agent sees a compact index at all times. It loads full skill content only when a task needs it. This keeps a large skill library from flooding the context window.</p>



<h2 id="h-four-ways-to-create-a-skill" class="wp-block-heading"><strong>Four Ways to Create a Skill</strong></h2>



<p class="wp-block-paragraph"><code>/learn</code> is one path among several. The right choice depends on who authors the skill and where the source lives.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th>Method</th><th>Who authors</th><th>Source input</th><th>Review gate</th><th>Best for</th></tr></thead><tbody><tr><td>Hand-write <code>SKILL.md</code></td><td>You</td><td>Your own knowledge</td><td>None</td><td>Full control over wording</td></tr><tr><td><code>/learn</code></td><td>The live agent</td><td>Dir, URL, conversation, notes</td><td><code>skill_manage</code> gate</td><td>Turning existing material into a skill fast</td></tr><tr><td><code>skill_manage</code> (auto)</td><td>The agent itself</td><td>A workflow it just solved</td><td><code>write_approval</code> gate</td><td>Capturing procedural memory after hard tasks</td></tr><tr><td>Skills Hub install</td><td>A third party</td><td>Registry or GitHub repo</td><td>Security scanner</td><td>Reusing community or vendor skills</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">Agent-created skills are the agent&#8217;s procedural memory. The agent may save an approach after a complex task of five or more tool calls. It also saves when it hit a dead end and found the working path. By default, <code>write_approval</code> is false, so the agent writes freely. Set it to true to stage every write for review under <code>~/.hermes/pending/skills/</code>.</p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use Cases With Examples</strong></h2>



<ul class="wp-block-list">
<li><strong>Onboarding an internal API</strong>: Run <code>/learn</code> on your private docs URL. The agent produces a skill covering auth, pagination, and common calls. New teammates then invoke it as a slash command.</li>



<li><strong>Capturing a deploy runbook</strong>: Walk the agent through one staging deploy. Then run <code>/learn how I just deployed the staging server</code>. The procedure becomes repeatable across the CLI and chat platforms.</li>



<li><strong>Grouping a recurring task</strong>: Use a skill bundle to load several skills at once. One slash command then pulls in review, test, and PR skills together.</li>
</ul>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php"># ~/.hermes/skill-bundles/backend-dev.yaml
name: backend-dev
description: Backend feature work — review, test, PR workflow.
skills:
  - github-code-review
  - test-driven-development
  - github-pr-workflow
instruction: |
  Always start by writing failing tests, then implement.</code></pre></div></div>



<h2 id="h-a-look-at-the-skill-md-format" class="wp-block-heading"><strong>A Look at the SKILL.md Format</strong></h2>



<p class="wp-block-paragraph">A skill is mostly a markdown file with YAML frontmatter. The body follows a fixed section order. <code>/learn</code> targets this exact shape so output stays consistent.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">---
name: my-skill
description: Brief description of what this skill does
version: 1.0.0
platforms: [macos, linux]     # Optional — restrict to specific OS
metadata:
  hermes:
    tags: [python, automation]
    category: devops
---

# Skill Title

## When to Use
Trigger conditions for this skill.

## Procedure
1. Step one
2. Step two

## Pitfalls
- Known failure modes and fixes

## Verification
How to confirm it worked.</code></pre></div></div>



<p class="wp-block-paragraph">The <code>platforms</code> field can hide a skill on incompatible operating systems. Conditional fields can also show a skill only when certain toolsets are present or absent.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- Hermes /learn Skill Builder — interactive demo. Paste into a WordPress Custom HTML block. -->
<div style="max-width:880px;margin:0 auto;">
  <iframe id="hl-learn-frame" title="Hermes /learn Skill Builder demo"
    style="width:100%;border:0;height:600px;overflow:hidden;display:block;"
    loading="lazy" scrolling="no"
    srcdoc="&lt;!DOCTYPE html&gt;&lt;html lang=&quot;en&quot;&gt;&lt;head&gt;&lt;meta charset=&quot;utf-8&quot;&gt;&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width,initial-scale=1&quot;&gt;&lt;link rel=&quot;preconnect&quot; href=&quot;https://fonts.googleapis.com&quot;&gt;&lt;link href=&quot;https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&amp;family=JetBrains+Mono:wght@400;600&amp;display=swap&quot; rel=&quot;stylesheet&quot;&gt;&lt;style&gt;html,body{margin:0;padding:0;background:transparent}&lt;/style&gt;&lt;/head&gt;&lt;body&gt;&lt;div id=&quot;hl-learn-demo&quot;&gt;
  &lt;style&gt;
    #hl-learn-demo *{box-sizing:border-box;margin:0;padding:0}
    #hl-learn-demo{
      --bg:#0a0c08;--panel:#111310;--panel2:#161913;--line:#26291f;
      --green:#76B900;--green-dim:#5c8f00;--txt:#e9ece4;--muted:#8a8f80;--red:#ff5d5d;
      font-family:'Inter',-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Arial,sans-serif;
      background:var(--bg);color:var(--txt);border:1px solid var(--line);border-radius:14px;
      overflow:hidden;max-width:880px;margin:0 auto;line-height:1.55;
    }
    #hl-learn-demo .hd{padding:18px 22px;border-bottom:1px solid var(--line);background:linear-gradient(180deg,#10130d,#0a0c08)}
    #hl-learn-demo .hd h2{font-size:18px;font-weight:700;letter-spacing:-.2px;display:flex;align-items:center;gap:10px}
    #hl-learn-demo .dot{width:9px;height:9px;border-radius:50%;background:var(--green);box-shadow:0 0 10px var(--green)}
    #hl-learn-demo .hd p{color:var(--muted);font-size:12.5px;margin-top:6px}
    #hl-learn-demo .cmd{font-family:'JetBrains Mono',ui-monospace,Menlo,Consolas,monospace}
    #hl-learn-demo .body{padding:20px 22px}
    #hl-learn-demo .tabs{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:18px}
    #hl-learn-demo .tab{flex:1 1 auto;min-width:120px;background:var(--panel);border:1px solid var(--line);color:var(--muted);
      padding:11px 12px;border-radius:9px;cursor:pointer;font-size:13px;font-weight:600;text-align:center;transition:.15s}
    #hl-learn-demo .tab:hover{border-color:var(--green-dim);color:var(--txt)}
    #hl-learn-demo .tab.on{background:rgba(118,185,0,.12);border-color:var(--green);color:var(--green)}
    #hl-learn-demo label{display:block;font-size:11.5px;text-transform:uppercase;letter-spacing:.6px;color:var(--muted);margin:14px 0 6px}
    #hl-learn-demo input,#hl-learn-demo textarea{width:100%;background:var(--panel);border:1px solid var(--line);color:var(--txt);
      border-radius:9px;padding:11px 13px;font-size:13.5px;font-family:inherit;outline:none;transition:.15s}
    #hl-learn-demo input:focus,#hl-learn-demo textarea:focus{border-color:var(--green)}
    #hl-learn-demo textarea{resize:vertical;min-height:70px}
    #hl-learn-demo .row{display:flex;gap:14px;flex-wrap:wrap}
    #hl-learn-demo .row&gt;div{flex:1 1 200px}
    #hl-learn-demo .ct{font-size:11px;text-align:right;margin-top:4px;color:var(--muted)}
    #hl-learn-demo .ct.over{color:var(--red)}
    #hl-learn-demo .cmdprev{margin-top:16px;background:#06070a;border:1px solid var(--line);border-radius:9px;padding:12px 14px;
      font-family:'JetBrains Mono',ui-monospace,monospace;font-size:12.5px;color:var(--green);word-break:break-all}
    #hl-learn-demo .cmdprev .pl{color:#5c6657}
    #hl-learn-demo .run{margin-top:16px;width:100%;background:var(--green);color:#0a0c08;border:none;border-radius:9px;
      padding:13px;font-size:14px;font-weight:700;cursor:pointer;transition:.15s;letter-spacing:.2px}
    #hl-learn-demo .run:hover{background:#84cf00;box-shadow:0 0 18px rgba(118,185,0,.4)}
    #hl-learn-demo .run:disabled{opacity:.5;cursor:not-allowed;box-shadow:none}
    #hl-learn-demo .out{margin-top:20px;display:none}
    #hl-learn-demo .out.show{display:block}
    #hl-learn-demo .stage{background:#06070a;border:1px solid var(--line);border-radius:9px;padding:14px 16px;
      font-family:'JetBrains Mono',ui-monospace,monospace;font-size:12.5px;color:var(--muted);margin-bottom:16px}
    #hl-learn-demo .stage .ln{opacity:0;transform:translateY(4px);animation:hlfade .35s forwards}
    #hl-learn-demo .stage .ok{color:var(--green)}
    #hl-learn-demo .stage .tool{color:#cfe39a}
    @keyframes hlfade{to{opacity:1;transform:translateY(0)}}
    #hl-learn-demo .pd{display:flex;gap:8px;margin-bottom:16px}
    #hl-learn-demo .pd .lv{flex:1;background:var(--panel);border:1px solid var(--line);border-radius:8px;padding:9px 10px}
    #hl-learn-demo .pd .lv b{display:block;color:var(--green);font-size:12px}
    #hl-learn-demo .pd .lv span{font-size:10.5px;color:var(--muted)}
    #hl-learn-demo .skillwrap{position:relative}
    #hl-learn-demo .skillwrap .cap{display:flex;justify-content:space-between;align-items:center;margin-bottom:6px}
    #hl-learn-demo .skillwrap .cap b{font-size:11.5px;text-transform:uppercase;letter-spacing:.6px;color:var(--muted)}
    #hl-learn-demo .copy{background:var(--panel2);border:1px solid var(--line);color:var(--green);font-size:11px;
      padding:5px 11px;border-radius:7px;cursor:pointer;font-weight:600}
    #hl-learn-demo .copy:hover{border-color:var(--green)}
    #hl-learn-demo pre{background:#06070a;border:1px solid var(--line);border-radius:9px;padding:16px;overflow-x:auto;
      font-family:'JetBrains Mono',ui-monospace,monospace;font-size:12.5px;line-height:1.65;color:#dfe6d3;white-space:pre}
    #hl-learn-demo .fm{color:#7d8470}
    #hl-learn-demo .key{color:var(--green)}
    #hl-learn-demo .h1{color:#fff;font-weight:700}
    #hl-learn-demo .h2{color:#a9d65a;font-weight:700}
    #hl-learn-demo .note{font-size:11px;color:var(--muted);margin-top:14px;border-top:1px solid var(--line);padding-top:12px}
    #hl-learn-demo .ft{padding:12px 22px;border-top:1px solid var(--line);font-size:11px;color:var(--muted);
      display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:6px}
    #hl-learn-demo .ft b{color:var(--green)}
    @media(max-width:560px){#hl-learn-demo .pd{flex-direction:column}#hl-learn-demo .tab{min-width:0}}
  &lt;/style&gt;

  &lt;div class=&quot;hd&quot;&gt;
    &lt;h2&gt;&lt;span class=&quot;dot&quot;&gt;&lt;/span&gt;Hermes &lt;span class=&quot;cmd&quot; style=&quot;color:var(--green)&quot;&gt;/learn&lt;/span&gt; — Skill Builder&lt;/h2&gt;
    &lt;p&gt;Pick a source, describe the skill, and generate a standards-compliant &lt;span class=&quot;cmd&quot;&gt;SKILL.md&lt;/span&gt;. Illustrative simulator — runs fully in your browser, no API call.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;body&quot;&gt;
    &lt;div class=&quot;tabs&quot; id=&quot;hl-tabs&quot;&gt;
      &lt;div class=&quot;tab on&quot; data-src=&quot;dir&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f4c1.png" alt="📁" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Local directory&lt;/div&gt;
      &lt;div class=&quot;tab&quot; data-src=&quot;url&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f517.png" alt="🔗" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Doc URL&lt;/div&gt;
      &lt;div class=&quot;tab&quot; data-src=&quot;conv&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f4ac.png" alt="💬" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Conversation&lt;/div&gt;
      &lt;div class=&quot;tab&quot; data-src=&quot;notes&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f4dd.png" alt="📝" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Pasted notes&lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;row&quot;&gt;
      &lt;div&gt;
        &lt;label&gt;Skill name (slug)&lt;/label&gt;
        &lt;input id=&quot;hl-name&quot; value=&quot;acme-api&quot; maxlength=&quot;40&quot; placeholder=&quot;lowercase-with-hyphens&quot;&gt;
      &lt;/div&gt;
      &lt;div&gt;
        &lt;label&gt;Category&lt;/label&gt;
        &lt;input id=&quot;hl-cat&quot; value=&quot;devops&quot; maxlength=&quot;24&quot; placeholder=&quot;e.g. devops&quot;&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;label&gt;Description (house standard: ≤ 60 characters)&lt;/label&gt;
    &lt;input id=&quot;hl-desc&quot; value=&quot;Acme REST API: auth, pagination, common calls&quot; maxlength=&quot;90&quot;&gt;
    &lt;div class=&quot;ct&quot; id=&quot;hl-ct&quot;&gt;45 / 60&lt;/div&gt;

    &lt;label id=&quot;hl-srclabel&quot;&gt;Directory path&lt;/label&gt;
    &lt;input id=&quot;hl-src&quot; value=&quot;~/projects/acme-sdk&quot;&gt;

    &lt;label id=&quot;hl-foclabel&quot;&gt;Focus / instruction (optional)&lt;/label&gt;
    &lt;input id=&quot;hl-focus&quot; value=&quot;focus on auth + pagination&quot;&gt;

    &lt;div class=&quot;cmdprev cmd&quot; id=&quot;hl-cmdprev&quot;&gt;&lt;/div&gt;

    &lt;button class=&quot;run&quot; id=&quot;hl-run&quot;&gt;Run /learn ▸&lt;/button&gt;

    &lt;div class=&quot;out&quot; id=&quot;hl-out&quot;&gt;
      &lt;div class=&quot;pd&quot;&gt;
        &lt;div class=&quot;lv&quot;&gt;&lt;b&gt;Level 0&lt;/b&gt;&lt;span&gt;skills_list() · ~3k tokens&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;lv&quot;&gt;&lt;b&gt;Level 1&lt;/b&gt;&lt;span&gt;skill_view(name) · full&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;lv&quot;&gt;&lt;b&gt;Level 2&lt;/b&gt;&lt;span&gt;skill_view(name, path)&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;stage&quot; id=&quot;hl-stage&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;skillwrap&quot;&gt;
        &lt;div class=&quot;cap&quot;&gt;&lt;b&gt;Generated ~/.hermes/skills/&amp;lt;category&amp;gt;/&amp;lt;name&amp;gt;/SKILL.md&lt;/b&gt;
          &lt;button class=&quot;copy&quot; id=&quot;hl-copy&quot;&gt;Copy&lt;/button&gt;&lt;/div&gt;
        &lt;pre id=&quot;hl-skill&quot;&gt;&lt;/pre&gt;
      &lt;/div&gt;
      &lt;div class=&quot;note&quot;&gt;Saved via the &lt;span class=&quot;cmd&quot;&gt;skill_manage&lt;/span&gt; tool. With &lt;span class=&quot;cmd&quot;&gt;skills.write_approval: true&lt;/span&gt;, this write is staged under &lt;span class=&quot;cmd&quot;&gt;~/.hermes/pending/skills/&lt;/span&gt; for review.&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;Progressive disclosure · agentskills.io standard&lt;/span&gt;
    &lt;span&gt;Demo by &lt;b&gt;Marktechpost&lt;/b&gt;&lt;/span&gt;
  &lt;/div&gt;

  &lt;script&gt;
  (function(){
    var root=document.getElementById('hl-learn-demo');
    var src='dir';
    var tabs=root.querySelectorAll('.tab');
    var srcLabel=root.querySelector('#hl-srclabel');
    var srcIn=root.querySelector('#hl-src');
    var focLabel=root.querySelector('#hl-foclabel');
    var focIn=root.querySelector('#hl-focus');
    var nameIn=root.querySelector('#hl-name');
    var catIn=root.querySelector('#hl-cat');
    var descIn=root.querySelector('#hl-desc');
    var ct=root.querySelector('#hl-ct');
    var cmdPrev=root.querySelector('#hl-cmdprev');
    var runBtn=root.querySelector('#hl-run');
    var out=root.querySelector('#hl-out');
    var stage=root.querySelector('#hl-stage');
    var skillPre=root.querySelector('#hl-skill');
    var copyBtn=root.querySelector('#hl-copy');

    var presets={
      dir:{label:'Directory path',foc:'Focus / instruction (optional)',ph:'~/projects/acme-sdk',
           val:'~/projects/acme-sdk',focv:'focus on auth + pagination',tool:'read_file + search_files',
           verb:'Reading directory'},
      url:{label:'Doc URL',foc:'Focus / instruction (optional)',ph:'https://docs.example.com/api/quickstart',
           val:'https://docs.example.com/api/quickstart',focv:'',tool:'web_extract',verb:'Fetching page'},
      conv:{label:'What you just did',foc:'Extra detail (optional)',ph:'how I just deployed the staging server',
           val:'how I just deployed the staging server',focv:'',tool:'conversation history',verb:'Reviewing this conversation'},
      notes:{label:'Pasted procedure',foc:'Extra detail (optional)',ph:'open the portal, New &gt; Expense, attach receipt, submit',
           val:'open the portal, New &gt; Expense, attach receipt, submit',focv:'',tool:'pasted text',verb:'Parsing pasted notes'}
    };

    function slug(s){return (s||'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')||'my-skill';}
    function esc(s){return (s||'').replace(/&amp;/g,'&amp;amp;').replace(/&lt;/g,'&amp;lt;').replace(/&gt;/g,'&amp;gt;');}

    function updateCt(){
      var n=descIn.value.length;
      ct.textContent=n+' / 60';
      ct.className='ct'+(n&gt;60?' over':'');
    }

    function updateCmd(){
      var p=presets[src];
      var arg=srcIn.value.trim();
      var foc=focIn.value.trim();
      var tail=foc?(', '+foc):'';
      if(src==='dir') tail = foc?(' '+foc):'';
      var body;
      if(src==='dir') body=arg+(foc?(', '+foc):'');
      else if(src==='url') body=arg;
      else body=arg+(foc?(' — '+foc):'');
      cmdPrev.innerHTML='&lt;span class=&quot;pl&quot;&gt;$ &lt;/span&gt;/learn '+esc(body);
    }

    function setTab(s){
      src=s;
      tabs.forEach(function(t){t.classList.toggle('on',t.dataset.src===s);});
      var p=presets[s];
      srcLabel.textContent=p.label;
      focLabel.textContent=p.foc;
      srcIn.value=p.val;srcIn.placeholder=p.ph;
      focIn.value=p.focv;
      updateCmd();
    }

    tabs.forEach(function(t){t.addEventListener('click',function(){setTab(t.dataset.src);});});
    [srcIn,focIn].forEach(function(el){el.addEventListener('input',updateCmd);});
    descIn.addEventListener('input',updateCt);

    function buildSkill(){
      var name=slug(nameIn.value);
      var cat=slug(catIn.value)||'general';
      var desc=descIn.value.trim().slice(0,60);
      var p=presets[src];
      var arg=srcIn.value.trim();
      var foc=focIn.value.trim();
      var tagBase=name.split('-').slice(0,2);
      var tags='['+tagBase.join(', ')+']';

      var whenLine, procLines, verifyLine, sourceNote;
      if(src==='dir'){
        whenLine='Use when working with the code in '+arg+'.';
        sourceNote='Authored from local source: '+arg+(foc?(' ('+foc+').'):'.');
        procLines=['1. Inspect the relevant module before calling it.',
                   '2. Apply the documented auth and request pattern.',
                   '3. Handle pagination and errors as the source specifies.'];
        verifyLine='Run a sample call and confirm a valid response.';
      } else if(src==='url'){
        whenLine='Use when integrating the API documented at '+arg+'.';
        sourceNote='Authored from doc page: '+arg+'.';
        procLines=['1. Read the quickstart steps in order.',
                   '2. Set credentials and the base URL.',
                   '3. Make the first request and inspect the payload.'];
        verifyLine='Confirm the response matches the documented schema.';
      } else if(src==='conv'){
        whenLine='Use to repeat this procedure: '+arg+'.';
        sourceNote='Captured from a completed workflow in conversation.';
        procLines=['1. Reproduce the steps in the order performed.',
                   '2. Substitute environment-specific values where noted.',
                   '3. Stop and report if any step fails.'];
        verifyLine='Confirm the end state matches the original run.';
      } else {
        whenLine='Use to follow this procedure: '+arg+'.';
        sourceNote='Authored from pasted notes.';
        procLines=['1. '+arg.split(',')[0].trim()+'.',
                   '2. Continue through each listed step in order.',
                   '3. Confirm completion before closing the task.'];
        verifyLine='Confirm the final step completed successfully.';
      }

      var L='\n';
      var s='';
      s+='&lt;span class=&quot;fm&quot;&gt;---&lt;/span&gt;'+L;
      s+='&lt;span class=&quot;key&quot;&gt;name:&lt;/span&gt; '+esc(name)+L;
      s+='&lt;span class=&quot;key&quot;&gt;description:&lt;/span&gt; '+esc(desc)+L;
      s+='&lt;span class=&quot;key&quot;&gt;version:&lt;/span&gt; 1.0.0'+L;
      s+='&lt;span class=&quot;key&quot;&gt;metadata:&lt;/span&gt;'+L;
      s+='  &lt;span class=&quot;key&quot;&gt;hermes:&lt;/span&gt;'+L;
      s+='    &lt;span class=&quot;key&quot;&gt;tags:&lt;/span&gt; '+esc(tags)+L;
      s+='    &lt;span class=&quot;key&quot;&gt;category:&lt;/span&gt; '+esc(cat)+L;
      s+='&lt;span class=&quot;fm&quot;&gt;---&lt;/span&gt;'+L+L;
      s+='&lt;span class=&quot;h1&quot;&gt;# '+esc(name.replace(/-/g,' ').replace(/\b\w/g,function(c){return c.toUpperCase();}))+'&lt;/span&gt;'+L+L;
      s+='&lt;span class=&quot;h2&quot;&gt;## When to Use&lt;/span&gt;'+L+esc(whenLine)+L+L;
      s+='&lt;span class=&quot;h2&quot;&gt;## Procedure&lt;/span&gt;'+L+procLines.map(esc).join(L)+L+L;
      s+='&lt;span class=&quot;h2&quot;&gt;## Pitfalls&lt;/span&gt;'+L+'- Do not invent calls the source does not define.'+L+'- Re-check the source if behavior changes.'+L+L;
      s+='&lt;span class=&quot;h2&quot;&gt;## Verification&lt;/span&gt;'+L+esc(verifyLine)+L+L;
      s+='&lt;span class=&quot;fm&quot;&gt;&amp;lt;!-- '+esc(sourceNote)+' --&amp;gt;&lt;/span&gt;';
      return {html:s, name:name, cat:cat};
    }

    function plainSkill(name,cat,html){
      var tmp=html.replace(/&lt;[^&gt;]+&gt;/g,'');
      var d=document.createElement('textarea');d.innerHTML=tmp;return d.value;
    }

    function typeStage(lines,done){
      stage.innerHTML='';
      lines.forEach(function(t,i){
        var d=document.createElement('div');
        d.className='ln';d.style.animationDelay=(i*0.32)+'s';
        d.innerHTML=t;stage.appendChild(d);
      });
      setTimeout(done, lines.length*320+150);
    }

    function run(){
      var p=presets[src];
      runBtn.disabled=true;runBtn.textContent='Running…';
      out.classList.add('show');
      var built=buildSkill();
      var lines=[
        '&lt;span class=&quot;tool&quot;&gt;→ '+p.verb+'&lt;/span&gt; via &lt;span class=&quot;ok&quot;&gt;'+p.tool+'&lt;/span&gt;',
        '→ Building standards-guided prompt (≤60-char description, fixed section order)',
        '→ Agent authoring &lt;span class=&quot;ok&quot;&gt;SKILL.md&lt;/span&gt; as a normal turn',
        '&lt;span class=&quot;ok&quot;&gt;✓ skill_manage(create)&lt;/span&gt; → ~/.hermes/skills/'+built.cat+'/'+built.name+'/',
        '&lt;span class=&quot;ok&quot;&gt;✓ Slash command ready:&lt;/span&gt; /'+built.name
      ];
      typeStage(lines,function(){
        skillPre.innerHTML=built.html;
        runBtn.disabled=false;runBtn.textContent='Run /learn ▸';
        report();
      });
      report();
    }

    copyBtn.addEventListener('click',function(){
      var txt=skillPre.innerText;
      if(navigator.clipboard){navigator.clipboard.writeText(txt);}
      copyBtn.textContent='Copied ✓';
      setTimeout(function(){copyBtn.textContent='Copy';},1400);
    });

    runBtn.addEventListener('click',run);

    // auto-resize for WordPress iframe embedding
    function report(){
      var h=document.body.offsetHeight+40;
      if(window.parent){window.parent.postMessage({hlLearnHeight:h},'*');}
    }
    window.addEventListener('load',report);
    window.addEventListener('resize',report);
    new MutationObserver(report).observe(root,{subtree:true,childList:true,attributes:true});

    // init
    updateCt();setTab('dir');
    setTimeout(report,200);
  })();
  &lt;/script&gt;
&lt;/div&gt;
&lt;/body&gt;&lt;/html&gt;"></iframe>
</div>
<script>
(function(){
  window.addEventListener("message", function(e){
    if (e && e.data && e.data.hlLearnHeight){
      var f = document.getElementById("hl-learn-frame");
      if (f) f.style.height = e.data.hlLearnHeight + "px";
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>Hermes Agent&#8217;s new <code>/learn</code> command authors a reusable skill from a directory, URL, conversation, or pasted notes — no hand-writing needed.</li>



<li>The live agent sources material with its own tools (<code>read_file</code>, <code>search_files</code>, <code>web_extract</code>), then writes a standards-compliant <code>SKILL.md</code>.</li>



<li>There is no separate ingestion engine, so <code>/learn</code> works the same across the CLI, TUI, messaging gateway, and dashboard.</li>



<li>Progressive disclosure keeps skills cheap: a ~3k-token index loads first, and full content loads only when a task needs it.</li>



<li>Skills save via <code>skill_manage</code>, so the <code>write_approval</code> gate can stage every write for review before it lands.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<h3 id="h-sources" class="wp-block-heading"><strong>Sources</strong></h3>



<ul class="wp-block-list">
<li><a href="https://hermes-agent.nousresearch.com/docs/user-guide/features/skills">Skills System — Hermes Agent documentation</a></li>



<li><a href="https://hermes-agent.nousresearch.com/docs/user-guide/features/skills#learning-a-skill-from-sources-learn">Learning a skill from sources (/learn)</a></li>



<li><a href="https://hermes-agent.nousresearch.com/docs/">Hermes Agent documentation home</a></li>



<li><a href="https://github.com/NousResearch/hermes-agent">NousResearch/hermes-agent — GitHub</a></li>



<li><a href="https://x.com/NousResearch/status/2069526242236182697">Nous Research on X</a></li>
</ul>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/nous-research-adds-learn-to-hermes-agents-skills-system-capturing-workflows-as-slash-commands-without-hand-writing-skill-md/">Nous Research Adds /learn to Hermes Agent&#8217;s Skills System, Capturing Workflows as Slash Commands Without Hand-Writing SKILL.md</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80687</post-id>
	</item>
		<item>
		<title>16 Best Generative AI Coding Tools in 2026 Compared: Features, and Best Fit</title>
		<link>https://www.marktechpost.com/2026/06/24/top-generative-ai-coding-tools-of-2026/</link>
					<comments>https://www.marktechpost.com/2026/06/24/top-generative-ai-coding-tools-of-2026/#respond</comments>
		
		<dc:creator><![CDATA[Michal Sutter]]></dc:creator>
		<pubDate>Wed, 24 Jun 2026 08:12:27 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[AI Startups]]></category>
		<category><![CDATA[AI Tool]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Generative AI]]></category>
		<category><![CDATA[List]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[No Code AI]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Top]]></category>
		<category><![CDATA[Uncategorized]]></category>
		<category><![CDATA[Vibe Coding]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=32919</guid>

					<description><![CDATA[<p>Generative AI has reshaped how software gets built. What began as line-by-line autocomplete now spans full application generation, multi-agent build pipelines, and natural-language interfaces to entire codebases. Large language models trained on code can read context, follow intent, and produce working frontends, backends, and infrastructure with little manual setup. For early-level AI engineers, software engineers, [&#8230;]</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/top-generative-ai-coding-tools-of-2026/">16 Best Generative AI Coding Tools in 2026 Compared: Features, and Best Fit</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Generative AI has reshaped how software gets built. What began as line-by-line autocomplete now spans full application generation, multi-agent build pipelines, and natural-language interfaces to entire codebases. Large language models trained on code can read context, follow intent, and produce working frontends, backends, and infrastructure with little manual setup.</p>



<p class="wp-block-paragraph">For early-level AI engineers, software engineers, and data scientists, the practical question is no longer whether these tools help, but which ones fit a given task. Some accelerate writing and reviewing code inside an existing workflow. Others remove the editor entirely and build deployable products from a prompt.</p>



<p class="wp-block-paragraph"><strong>Here are the top generative AI tools in code generation and coding to know in 2026</strong>:</p>



<h2 id="h-1-atoms" class="wp-block-heading"><strong>1. <a href="https://tinyurl.com/ms4xwdj4?via=marktechpost">Atoms</a></strong>*</h2>



<p class="wp-block-paragraph"><a href="https://tinyurl.com/ms4xwdj4?via=marktechpost">Atoms</a>* (10% discount coupon: <strong>MARKTECHPOST10</strong>) is an AI platform that turns natural-language descriptions into fully deployable applications. It marks a clear step beyond standalone code generators by pairing an AI Engineer with a broader agent team covering deep research, architecture, product management, SEO, ads, and data analysis.</p>



<p class="wp-block-paragraph">Users describe what they want to build in plain language. Atoms then generates the frontend, backend, integrations, and hosting configuration automatically. Every app ships with authentication, a database, and Stripe payments through Atoms Cloud, so most standard web patterns work without separate infrastructure. The platform supports popular AI models like GPT and Gemini without manual key setup, and you can export the code or sync to GitHub at any time.</p>



<p class="wp-block-paragraph">A distinctive feature, Race Mode, runs multiple models or agent teams in parallel on the same prompt, letting you compare approaches and keep the best result. Whether you are prototyping a new SaaS product or building an internal business tool, Atoms shows how far generative AI in software development has come. It is free to start, and the code <strong>MARKTECHPOST10</strong> gives 10% off.</p>



<h2 id="h-2-github-copilot" class="wp-block-heading"><strong>2. <a href="https://github.com/features/copilot">GitHub Copilot</a></strong></h2>



<p class="wp-block-paragraph">GitHub Copilot remains one of the most widely used AI coding assistants. Built by GitHub and OpenAI, it suggests code as you type inside editors like VS Code, Visual Studio, and JetBrains IDEs. It turns natural-language prompts and comments into working code across dozens of languages, and now includes agent and chat modes for multi-file edits, test generation, and pull-request workflows.</p>



<h2 id="h-3-tabnine" class="wp-block-heading"><strong>3. <a href="https://www.tabnine.com/">Tabnine</a></strong></h2>



<p class="wp-block-paragraph">Tabnine is an AI code-completion tool that predicts and suggests the next lines of code from context and syntax. It supports many languages, including JavaScript, Python, TypeScript, Rust, Go, and Bash, and integrates with VS Code, IntelliJ, Sublime, and other editors. Tabnine emphasizes privacy and lets teams run models on their own code, which appeals to organizations with strict data requirements.</p>



<h2 id="h-4-replit" class="wp-block-heading"><strong>4. <a href="https://replit.com/">Replit</a></strong></h2>



<p class="wp-block-paragraph">Replit is a cloud-based IDE for writing, testing, and deploying code from the browser. It supports many languages, including Python, JavaScript, Ruby, and C++, and ships with templates and starter projects. Its AI agent can build and modify full applications from natural-language instructions, then deploy them, making Replit a strong choice for fast prototyping and learning without local setup.</p>



<h2 id="h-5-warp" class="wp-block-heading"><strong>5. <a href="https://www.warp.dev/">Warp</a></strong></h2>



<p class="wp-block-paragraph">Warp upgrades the terminal into a modern platform for engineering workflows. It makes the command line more natural and collaborative for individuals and teams. Its AI features translate natural language into executable shell commands and can run agentic, multi-step tasks directly in the terminal, helping engineers move faster through setup, debugging, and operations work.</p>



<h2 id="h-6-hugging-face" class="wp-block-heading"><strong>6. <a href="https://huggingface.co/">Hugging Face</a></strong></h2>



<p class="wp-block-paragraph">Hugging Face is a platform offering open models, datasets, and tools for machine learning, including code generation. Developers can browse and run a large library of open code models for autocompletion, explanation, and refactoring, and integrate them into their own applications through the Hub and inference tooling. It is a core resource for engineers building on or experimenting with open-source code models.</p>



<h2 id="h-7-codacy" class="wp-block-heading"><strong>7. <a href="https://www.codacy.com/">Codacy</a></strong></h2>



<p class="wp-block-paragraph">Codacy is a code-quality platform that uses automated analysis to find issues and enforce standards. It gives developers immediate feedback on style, security, and maintainability across many languages. Codacy integrates with GitHub, Slack, Jira, and similar tools, and its AI-assisted checks help teams catch problems early and keep large codebases consistent.</p>



<h2 id="h-8-metabob" class="wp-block-heading"><strong>8. <a href="https://metabob.com/">Metabob</a></strong></h2>



<p class="wp-block-paragraph">Metabob is an AI-based code analysis tool that detects and helps resolve hidden issues before code is merged. Built on graph neural networks, it understands code logic and context across large codebases and flags problems like race conditions, memory leaks, and unhandled edge cases. It runs alongside generative AI coding tools to catch errors that language models routinely miss, and integrates with VS Code, GitHub, and CI/CD pipelines.</p>



<h2 id="h-9-aixcoder" class="wp-block-heading"><strong>9. <a href="https://www.aixcoder.com/">aiXcoder</a></strong></h2>



<p class="wp-block-paragraph">aiXcoder is an AI-powered coding assistant that helps developers write code faster and more accurately. It offers method-level code generation and smart, context-aware completion using natural-language processing and machine learning. Built on its own open-source code model, aiXcoder can run locally for private, offline use, and supports popular languages and IDEs, which appeals to teams with strict security requirements.</p>



<h2 id="h-10-bloop" class="wp-block-heading"><strong>10. <a href="https://bloop.ai/">Bloop</a></strong></h2>



<p class="wp-block-paragraph">Bloop began as an in-IDE code search engine that answered natural-language questions about a codebase. The team has since shifted toward AI-agent infrastructure, building tools to plan, orchestrate, and review the work of autonomous coding agents as the industry moves from instant autocomplete to longer-running tasks. Its open-source roots in fast, semantic code search remain a useful reference for understanding code at scale.</p>



<h2 id="h-11-mintlify" class="wp-block-heading"><strong>11. <a href="https://mintlify.com/">Mintlify</a></strong></h2>



<p class="wp-block-paragraph">Mintlify (formerly Mintify) is an AI-native documentation platform for developers and API companies. It generates and maintains documentation that stays in sync with code through a docs-as-code workflow, with smart search, interactive API playgrounds, and a built-in AI assistant. It now powers documentation for major developer platforms and is increasingly used to make codebases and APIs readable to both engineers and AI agents.</p>



<h2 id="h-12-locofy" class="wp-block-heading"><strong>12. <a href="https://www.locofy.ai/">Locofy</a></strong></h2>



<p class="wp-block-paragraph">Locofy converts designs into production-ready frontend code for web and mobile apps. Users can turn Figma and Penpot files into React, React Native, HTML/CSS, Vue, Angular, Next.js, Flutter, and more. By automating the design-to-code handoff with AI, Locofy helps teams ship interfaces faster while keeping output close to clean, editable components.</p>



<h2 id="h-13-anima" class="wp-block-heading"><strong>13. <a href="https://www.animaapp.com/">Anima</a></strong></h2>



<p class="wp-block-paragraph">Anima is a design-to-code platform that turns Figma designs, prompts, or images into working frontend code and functional apps. It integrates closely with design tools and can generate responsive React, Vue, or HTML/CSS, plus interactive prototypes. Anima narrows the gap between design and engineering, and now offers AI features to detect data needs, set up backends, and deploy with one click.</p>



<h2 id="h-14-dhiwise" class="wp-block-heading"><strong>14. <a href="https://www.dhiwise.com/">DhiWise</a></strong></h2>



<p class="wp-block-paragraph">DhiWise is a programming platform that transforms designs and prompts into developer-friendly code for mobile and web apps. It automates parts of the application lifecycle and produces readable, modular, and reusable output, with support for setting up actions, navigation, and API integration. DhiWise targets developers who want to accelerate delivery without giving up structured, maintainable code they can extend.</p>



<h2 id="h-15-durable" class="wp-block-heading"><strong>15. <a href="https://durable.co/">Durable</a></strong></h2>



<p class="wp-block-paragraph">Durable is an AI website builder that generates a complete site, including images and copy, in seconds. It can infer a business type from a short description, then produce a tailored, mobile-responsive website with no coding required. Durable bundles hosting, a CRM, invoicing, and AI marketing tools, making it suited to small businesses and solo founders who need a functional web presence fast.</p>



<h2 id="h-16-the-com" class="wp-block-heading"><strong>16. <a href="https://the.com/">The.com</a></strong></h2>



<p class="wp-block-paragraph">The.com is a platform for generating and managing websites and web pages at scale. Businesses use it to publish large volumes of pages programmatically, making it a strong fit for programmatic SEO and content operations. It is aimed at teams that treat web pages as a high-volume, data-driven asset rather than one-off builds, with fast load times and a spreadsheet-style publishing workflow.</p>



<h2 id="h-the-shift-to-come" class="wp-block-heading"><strong>The shift to come</strong></h2>



<p class="wp-block-paragraph">The clearest trend in 2026 is consolidation of the workflow. Earlier tools focused on a single slice of development: completing a function, searching a repo, or converting a design. The newer generation, led by agent-based platforms like Atoms, compresses research, full-stack build, deployment, and even growth into one place.</p>



<p class="wp-block-paragraph">That does not make the focused tools obsolete. Assistants like Copilot and Tabnine still earn their place inside professional codebases, and quality tools like Codacy and Metabob remain essential where reliability matters. The practical approach is to match the tool to the job: agent platforms for going from idea to live product, assistants for daily engineering, and analysis tools for keeping output safe and maintainable.</p>



<p class="wp-block-paragraph">For engineers evaluating where to start, the low-risk move is to try a focused assistant inside your existing editor, then test an agent platform on a contained project. The gap between describing software and shipping it keeps narrowing, and the tools above are where that change is happening fastest.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"><em>*we do make a small Affiliate commission by adding an affiliate url.</em></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/top-generative-ai-coding-tools-of-2026/">16 Best Generative AI Coding Tools in 2026 Compared: Features, and Best Fit</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
					<wfw:commentRss>https://www.marktechpost.com/2026/06/24/top-generative-ai-coding-tools-of-2026/feed/</wfw:commentRss>
			<slash:comments>0</slash:comments>
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">32919</post-id>
	</item>
		<item>
		<title>DFlash Speculative Decoding Drafts Whole Token Blocks in Parallel for Up to 15x Higher Throughput on NVIDIA Blackwell</title>
		<link>https://www.marktechpost.com/2026/06/24/dflash-speculative-decoding-drafts-whole-token-blocks-in-parallel-for-up-to-15x-higher-throughput-on-nvidia-blackwell/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Wed, 24 Jun 2026 07:21:10 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80675</guid>

					<description><![CDATA[<p>UC San Diego's DFlash replaces autoregressive drafting with a lightweight block diffusion model for speculative decoding. It drafts whole token blocks in a single forward pass and conditions on target hidden features through KV injection. The paper reports up to 6.08x lossless speedup on Qwen3-8B, while NVIDIA reports up to 15x throughput on Blackwell at fixed interactivity. DFlash ships 20 checkpoints and supports SGLang, vLLM, and TensorRT-LLM.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/dflash-speculative-decoding-drafts-whole-token-blocks-in-parallel-for-up-to-15x-higher-throughput-on-nvidia-blackwell/">DFlash Speculative Decoding Drafts Whole Token Blocks in Parallel for Up to 15x Higher Throughput on NVIDIA Blackwell</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Autoregressive large language models generate text one token at a time. Each token waits for the one before it. This serial loop leaves modern GPUs underused and keeps inference slow. The cost grows worse with long Chain-of-Thought reasoning models. Their lengthy outputs make latency the dominant part of generation.</p>



<p class="wp-block-paragraph">Speculative decoding is the standard fix. A small draft model proposes future tokens. The large target model verifies those tokens in parallel. Accepted tokens are kept, so the output stays lossless. But most methods, including the state-of-the-art EAGLE-3, still draft autoregressively. That serial drafting caps real-world speedups near 2–3×.</p>



<p class="wp-block-paragraph"><strong>DFlash</strong>, introduced by research team from UC San Diego team (z-lab), takes a different route. It is a lightweight block diffusion model built for drafting. Instead of drafting tokens one at a time, it proposes a whole block in a single forward pass. The target model then verifies that block in parallel.</p>



<p class="wp-block-paragraph">The research team reports over 6× lossless acceleration across a range of models and tasks. It reaches up to 2.5× higher speedup than EAGLE-3. On NVIDIA Blackwell, NVIDIA engineering team reports up to 15× higher throughput for gpt-oss-120b. That figure holds at the same user interactivity target.</p>


<div class="wp-block-image">
<figure class="aligncenter size-full is-resized"><img fetchpriority="high" decoding="async" width="1708" height="922" data-attachment-id="80677" data-permalink="https://www.marktechpost.com/2026/06/24/dflash-speculative-decoding-drafts-whole-token-blocks-in-parallel-for-up-to-15x-higher-throughput-on-nvidia-blackwell/screenshot-2026-06-24-at-12-00-06-am-2/" data-orig-file="https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1.png" data-orig-size="1708,922" data-comments-opened="0" data-image-meta="{&quot;aperture&quot;:&quot;0&quot;,&quot;credit&quot;:&quot;&quot;,&quot;camera&quot;:&quot;&quot;,&quot;caption&quot;:&quot;&quot;,&quot;created_timestamp&quot;:&quot;0&quot;,&quot;copyright&quot;:&quot;&quot;,&quot;focal_length&quot;:&quot;0&quot;,&quot;iso&quot;:&quot;0&quot;,&quot;shutter_speed&quot;:&quot;0&quot;,&quot;title&quot;:&quot;&quot;,&quot;orientation&quot;:&quot;0&quot;,&quot;alt&quot;:&quot;&quot;}" data-image-title="Screenshot 2026-06-24 at 12.00.06 AM" data-image-description="" data-image-caption="" data-large-file="https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-1024x553.png" src="https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1.png" alt="" class="wp-image-80677" style="aspect-ratio:1.8525083340108952;width:792px;height:auto" srcset="https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1.png 1708w, https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-300x162.png 300w, https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-1024x553.png 1024w, https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-768x415.png 768w, https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-1536x829.png 1536w, https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-778x420.png 778w, https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-150x81.png 150w, https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-696x376.png 696w, https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-1068x577.png 1068w" sizes="(max-width: 1708px) 100vw, 1708px" /><figcaption class="wp-element-caption">https://developer.nvidia.com/blog/boost-inference-performance-up-to-15x-on-nvidia-blackwell-using-dflash-speculative-decoding/</figcaption></figure>
</div>


<h2 id="h-what-block-diffusion-drafting-changes" class="wp-block-heading"><strong>What block diffusion drafting changes</strong></h2>



<p class="wp-block-paragraph">Block diffusion models denoise a block of masked tokens at once. They blend parallel generation with autoregressive block structure. DFlash applies this idea only to the drafting stage. Verification stays with the trusted autoregressive target model.</p>



<p class="wp-block-paragraph">This split matters for quality. Standalone diffusion LLMs often trail autoregressive models on accuracy. They also need many denoising steps, which slows their raw inference speed. DFlash sidesteps both problems. The draft only needs to be good enough to be accepted. The target&#8217;s parallel verification guarantees the final output distribution.</p>



<p class="wp-block-paragraph">A second benefit is drafting cost. An autoregressive drafter&#8217;s cost grows linearly with the number of speculative tokens. A diffusion drafter generates all tokens in one parallel pass. So drafting latency stays largely flat as the block grows. This frees DFlash to use deeper, more expressive draft models without adding latency.</p>



<p class="wp-block-paragraph">This separates DFlash from earlier diffusion-drafter work. Methods like DiffuSpec and SpecDiff-2 used massive 7B drafters, capping speedups near 3–4×. DFlash instead uses a small five-layer drafter (eight layers for Qwen3-Coder).</p>



<h2 id="h-the-target-knows-best-insight" class="wp-block-heading"><strong>The &#8220;target knows best&#8221; insight</strong></h2>



<p class="wp-block-paragraph">DFlash&#8217;s core idea is simple: the target knows best. Large autoregressive models&#8217; hidden features encode information about multiple future tokens. DFlash extracts hidden states from several target layers. It fuses them into one compact target context feature. This feature then conditions the draft model.</p>



<p class="wp-block-paragraph">DFlash injects this feature differently than EAGLE-3. EAGLE-3 fuses target features into the draft&#8217;s input embeddings only. As draft depth grows, that signal gets diluted. DFlash instead injects the feature into the Key and Value projections of every draft layer. The projected features sit in the draft&#8217;s KV cache and persist across drafting iterations.</p>



<p class="wp-block-paragraph">This KV injection lets acceptance length scale with draft depth. A five-layer DFlash drafter generating 16 tokens beats EAGLE-3 generating 8 tokens. It is both lower-latency and higher-acceptance in the paper&#8217;s tests. The draft model effectively becomes a diffusion adapter on top of the target.</p>



<h2 id="h-two-speedup-numbers-measured-differently" class="wp-block-heading"><strong>Two speedup numbers, measured differently</strong></h2>



<p class="wp-block-paragraph">The DFlash research&#8217;s 6× is single-stream lossless acceleration. On Qwen3-8B with greedy decoding (Transformers backend), DFlash averages a 4.86× speedup. EAGLE-3 averages 1.76× at tree size 16 and 2.02× at tree size 60. DFlash peaks at 6.08× on MATH-500 (τ = 7.87) and averages τ = 6.49 across tasks.</p>



<p class="wp-block-paragraph">NVIDIA&#8217;s 15× is throughput at a fixed interactivity target. It applies to gpt-oss-120b on eight NVIDIA Blackwell GPUs in a DGX B300 system, using TensorRT-LLM. At the 500–600 tokens/sec per-user range, DFlash serves more than 15× the throughput of autoregressive decoding. That is about 1.5× more than EAGLE-3 at the same point.</p>



<p class="wp-block-paragraph">The table below shows the paper&#8217;s per-task speedups on Qwen3-8B at temperature 0 (Transformers backend).</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Task (Qwen3-8B, temp=0)</th><th>Baseline</th><th>EAGLE-3 (16)</th><th>DFlash (16)</th><th>DFlash τ</th></tr></thead><tbody><tr><td>GSM8K</td><td>1.00×</td><td>1.94×</td><td><strong>5.15×</strong></td><td>6.54</td></tr><tr><td>MATH-500</td><td>1.00×</td><td>1.81×</td><td><strong>6.08×</strong></td><td>7.87</td></tr><tr><td>AIME25</td><td>1.00×</td><td>1.79×</td><td><strong>5.62×</strong></td><td>7.08</td></tr><tr><td>HumanEval</td><td>1.00×</td><td>1.89×</td><td><strong>5.14×</strong></td><td>6.50</td></tr><tr><td>MBPP</td><td>1.00×</td><td>1.69×</td><td><strong>4.65×</strong></td><td>5.95</td></tr><tr><td>LiveCodeBench</td><td>1.00×</td><td>1.57×</td><td><strong>5.51×</strong></td><td>7.27</td></tr><tr><td>MT-Bench</td><td>1.00×</td><td>1.63×</td><td><strong>2.75×</strong></td><td>4.24</td></tr><tr><td><strong>Average</strong></td><td>1.00×</td><td>1.76×</td><td><strong>4.86×</strong></td><td>6.49</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">A separate NVIDIA Speed-Bench comparison measures interactivity speedups at matched concurrency. On gpt-oss-120b, DFlash averages 2.3× versus EAGLE-3&#8217;s 1.7×. On Llama 3.1 8B Instruct, DFlash averages 2.8× versus EAGLE-3&#8217;s 2.2×.</p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use cases with examples</strong></h2>



<p class="wp-block-paragraph">DFlash targets latency-sensitive serving where token-by-token generation hurts. <strong>Three patterns fit well</strong>:</p>



<ul class="wp-block-list">
<li><strong>Coding agents</strong>: Code generation needs fast, interactive responses. On Gemma 4 31B with vLLM, NVIDIA reports up to 5.8× on Math500 at concurrency 1. HumanEval reaches 5.6×. Faster drafts mean shorter wait times inside agent loops.</li>



<li><strong>Reasoning models</strong>: Long Chain-of-Thought traces dominate generation time. With thinking mode enabled, DFlash holds roughly 4.5× under greedy decoding on Qwen3-4B and Qwen3-8B. Under sampling, it holds about 3.9×. This cuts the cost of long reasoning outputs.</li>



<li><strong>Serving and throughput</strong>: DFlash also raises serving throughput. On SGLang with a B200 GPU, it reaches up to 5.1× on Qwen3-8B (Math500, concurrency 1). Gains taper as concurrency rises but stay positive, so serving cost still drops.</li>
</ul>



<h2 id="h-running-dflash" class="wp-block-heading"><strong>Running DFlash</strong></h2>



<p class="wp-block-paragraph">DFlash ships with checkpoints and framework support, so adoption needs little code. On vLLM, you swap an EAGLE-3 config for a DFlash one. No application refactoring is required.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">vllm serve Qwen/Qwen3.5-27B \
  --speculative-config '{"method": "dflash", "model": "z-lab/Qwen3.5-27B-DFlash", "num_speculative_tokens": 15}' \
  --attention-backend flash_attn \
  --max-num-batched-tokens 32768</code></pre></div></div>



<p class="wp-block-paragraph">The Transformers backend supports Qwen3 and LLaMA-3.1 models. It exposes a <code>spec_generate</code> call that pairs a draft model with a target model.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">from transformers import AutoModel, AutoModelForCausalLM, AutoTokenizer

draft = AutoModel.from_pretrained(
    "z-lab/Qwen3-8B-DFlash-b16", trust_remote_code=True,
    dtype="auto", device_map="cuda:0").eval()
target = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen3-8B", dtype="auto", device_map="cuda:0").eval()
tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen3-8B")

messages = [{"role": "user", "content": "How many positive whole-number divisors does 196 have?"}]
input_ids = tokenizer.apply_chat_template(
    messages, return_tensors="pt", add_generation_prompt=True,
    enable_thinking=False).to(draft.device)

output = draft.spec_generate(
    input_ids=input_ids, max_new_tokens=2048, temperature=0.0,
    target=target, stop_token_ids=[tokenizer.eos_token_id])
print(tokenizer.decode(output[0], skip_special_tokens=False))</code></pre></div></div>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>DFlash drafts an entire token block in one forward pass, not one token at a time.</li>



<li>It injects target hidden features into every draft layer&#8217;s KV cache, scaling acceptance length with depth.</li>



<li>Research Paper&#8217;s metrics: up to 6.08× lossless speedup on Qwen3-8B; NVIDIA test: up to 15× throughput on Blackwell at fixed interactivity.</li>



<li>A lightweight five-layer drafter replaces the 7B drafters that capped earlier diffusion methods near 3–4×.</li>
</ul>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- DFlash interactive demo - paste into a WordPress "Custom HTML" block -->
<div id="dflash-embed-wrap" style="max-width:100%;margin:0 auto;">
<iframe id="dflash-frame"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;UTF-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;
&lt;title&gt;DFlash Speculative Decoding Demo&lt;/title&gt;
&lt;style&gt;
  :root{
    --bg:#0a0c08; --panel:#11140d; --panel2:#161a10;
    --green:#76B900; --green-dim:#4d7a00; --green-glow:rgba(118,185,0,.35);
    --text:#e8eee0; --muted:#8a9580; --line:#26301a; --red:#d14b3a; --amber:#e0a500;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  body{background:var(--bg);color:var(--text);
    font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;
    padding:18px;line-height:1.5}
  .wrap{max-width:980px;margin:0 auto}
  .head{border-left:4px solid var(--green);padding-left:14px;margin-bottom:18px}
  .head h1{font-size:22px;font-weight:700;letter-spacing:-.02em}
  .head h1 span{color:var(--green)}
  .head p{color:var(--muted);font-size:13.5px;margin-top:4px}

  .controls{background:var(--panel);border:1px solid var(--line);border-radius:12px;
    padding:16px 18px;display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-bottom:16px}
  .ctrl label{display:block;font-size:12px;color:var(--muted);text-transform:uppercase;
    letter-spacing:.05em;margin-bottom:8px}
  .ctrl .val{color:var(--green);font-weight:700}
  input[type=range]{-webkit-appearance:none;width:100%;height:5px;border-radius:5px;
    background:var(--line);outline:none}
  input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;width:17px;height:17px;
    border-radius:50%;background:var(--green);cursor:pointer;box-shadow:0 0 8px var(--green-glow)}
  input[type=range]::-moz-range-thumb{width:17px;height:17px;border-radius:50%;
    background:var(--green);cursor:pointer;border:none}

  .btns{display:flex;gap:10px;margin-bottom:18px}
  button{font-family:inherit;font-size:14px;font-weight:600;border-radius:9px;
    padding:10px 22px;cursor:pointer;border:1px solid var(--line);transition:.15s}
  .run{background:var(--green);color:#0a0c08;border-color:var(--green)}
  .run:hover{box-shadow:0 0 16px var(--green-glow)}
  .run:disabled{opacity:.5;cursor:not-allowed;box-shadow:none}
  .reset{background:transparent;color:var(--text)}
  .reset:hover{border-color:var(--green-dim)}

  .lanes{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-bottom:16px}
  .lane{background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:14px}
  .lane.dflash{border-color:var(--green-dim);box-shadow:0 0 0 1px var(--green-glow) inset}
  .lane h3{font-size:14px;margin-bottom:2px}
  .lane .tag{font-size:11px;color:var(--muted);margin-bottom:12px}
  .lane.dflash h3 span{color:var(--green)}

  .grid{display:flex;flex-wrap:wrap;gap:4px;min-height:120px;align-content:flex-start}
  .tok{width:22px;height:22px;border-radius:4px;background:var(--panel2);
    border:1px solid var(--line);transition:.12s;position:relative}
  .tok.draft{background:#2a3416;border-color:var(--amber)}
  .tok.accept{background:var(--green-dim);border-color:var(--green);
    box-shadow:0 0 6px var(--green-glow)}
  .tok.reject{background:#3a1512;border-color:var(--red)}
  .tok.bonus{background:var(--green);border-color:var(--green)}

  .lanestats{display:flex;gap:14px;margin-top:12px;padding-top:12px;border-top:1px solid var(--line)}
  .lanestats div{flex:1}
  .lanestats .k{font-size:10.5px;color:var(--muted);text-transform:uppercase;letter-spacing:.04em}
  .lanestats .v{font-size:18px;font-weight:700;margin-top:2px}
  .lane.dflash .v{color:var(--green)}

  .verdict{background:var(--panel);border:1px solid var(--line);border-radius:12px;
    padding:16px 18px;display:flex;align-items:center;justify-content:space-between;gap:16px}
  .verdict .big{font-size:30px;font-weight:800;color:var(--green);
    text-shadow:0 0 18px var(--green-glow)}
  .verdict .lbl{font-size:12px;color:var(--muted);text-transform:uppercase;letter-spacing:.05em}
  .verdict .desc{font-size:12.5px;color:var(--muted);max-width:480px;text-align:right}

  .legend{display:flex;flex-wrap:wrap;gap:14px;margin:14px 2px 0;font-size:11.5px;color:var(--muted)}
  .legend span{display:inline-flex;align-items:center;gap:6px}
  .sw{width:12px;height:12px;border-radius:3px;display:inline-block}
  .note{font-size:11px;color:var(--muted);margin-top:14px;line-height:1.5;
    border-top:1px solid var(--line);padding-top:12px}
  .foot{margin-top:14px;font-size:11px;color:var(--muted);text-align:center}
  .foot b{color:var(--green)}
  @media(max-width:680px){
    .controls{grid-template-columns:1fr}
    .lanes{grid-template-columns:1fr}
    .verdict{flex-direction:column;align-items:flex-start}
    .verdict .desc{text-align:left}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot;&gt;
  &lt;div class=&quot;head&quot;&gt;
    &lt;h1&gt;&lt;span&gt;DFlash&lt;/span&gt; vs Autoregressive Drafting&lt;/h1&gt;
    &lt;p&gt;Watch how block-diffusion drafting decodes a 40-token answer in fewer target passes.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;controls&quot;&gt;
    &lt;div class=&quot;ctrl&quot;&gt;
      &lt;label&gt;Block size &lt;span class=&quot;val&quot; id=&quot;bsVal&quot;&gt;16&lt;/span&gt;&lt;/label&gt;
      &lt;input type=&quot;range&quot; id=&quot;bs&quot; min=&quot;4&quot; max=&quot;16&quot; step=&quot;2&quot; value=&quot;16&quot;&gt;
    &lt;/div&gt;
    &lt;div class=&quot;ctrl&quot;&gt;
      &lt;label&gt;Avg accepted / block (&amp;tau;) &lt;span class=&quot;val&quot; id=&quot;accVal&quot;&gt;7&lt;/span&gt;&lt;/label&gt;
      &lt;input type=&quot;range&quot; id=&quot;acc&quot; min=&quot;2&quot; max=&quot;12&quot; step=&quot;1&quot; value=&quot;7&quot;&gt;
    &lt;/div&gt;
    &lt;div class=&quot;ctrl&quot;&gt;
      &lt;label&gt;Draft cost vs verify &lt;span class=&quot;val&quot; id=&quot;drVal&quot;&gt;0.2&amp;times;&lt;/span&gt;&lt;/label&gt;
      &lt;input type=&quot;range&quot; id=&quot;dr&quot; min=&quot;0&quot; max=&quot;60&quot; step=&quot;5&quot; value=&quot;20&quot;&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;btns&quot;&gt;
    &lt;button class=&quot;run&quot; id=&quot;runBtn&quot;&gt;&amp;#9654; Run decode&lt;/button&gt;
    &lt;button class=&quot;reset&quot; id=&quot;resetBtn&quot;&gt;Reset&lt;/button&gt;
  &lt;/div&gt;

  &lt;div class=&quot;lanes&quot;&gt;
    &lt;div class=&quot;lane ar&quot;&gt;
      &lt;h3&gt;Autoregressive (EAGLE-3 style)&lt;/h3&gt;
      &lt;div class=&quot;tag&quot;&gt;One token verified per target pass &amp;mdash; serial&lt;/div&gt;
      &lt;div class=&quot;grid&quot; id=&quot;arGrid&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;lanestats&quot;&gt;
        &lt;div&gt;&lt;div class=&quot;k&quot;&gt;Target passes&lt;/div&gt;&lt;div class=&quot;v&quot; id=&quot;arSteps&quot;&gt;0&lt;/div&gt;&lt;/div&gt;
        &lt;div&gt;&lt;div class=&quot;k&quot;&gt;Tokens&lt;/div&gt;&lt;div class=&quot;v&quot; id=&quot;arTok&quot;&gt;0&lt;/div&gt;&lt;/div&gt;
        &lt;div&gt;&lt;div class=&quot;k&quot;&gt;Time (units)&lt;/div&gt;&lt;div class=&quot;v&quot; id=&quot;arTime&quot;&gt;0&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;lane dflash&quot;&gt;
      &lt;h3&gt;&lt;span&gt;DFlash&lt;/span&gt; (block diffusion)&lt;/h3&gt;
      &lt;div class=&quot;tag&quot;&gt;A block drafted in one pass, verified in parallel&lt;/div&gt;
      &lt;div class=&quot;grid&quot; id=&quot;dfGrid&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;lanestats&quot;&gt;
        &lt;div&gt;&lt;div class=&quot;k&quot;&gt;Target passes&lt;/div&gt;&lt;div class=&quot;v&quot; id=&quot;dfSteps&quot;&gt;0&lt;/div&gt;&lt;/div&gt;
        &lt;div&gt;&lt;div class=&quot;k&quot;&gt;Tokens&lt;/div&gt;&lt;div class=&quot;v&quot; id=&quot;dfTok&quot;&gt;0&lt;/div&gt;&lt;/div&gt;
        &lt;div&gt;&lt;div class=&quot;k&quot;&gt;Time (units)&lt;/div&gt;&lt;div class=&quot;v&quot; id=&quot;dfTime&quot;&gt;0&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;verdict&quot;&gt;
    &lt;div&gt;
      &lt;div class=&quot;lbl&quot;&gt;Effective speedup&lt;/div&gt;
      &lt;div class=&quot;big&quot; id=&quot;speedup&quot;&gt;&amp;mdash;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;desc&quot; id=&quot;verdictDesc&quot;&gt;
      Press &lt;b&gt;Run decode&lt;/b&gt;. DFlash drafts a whole block per pass, so it needs far fewer
      sequential target verifications to reach the same answer.
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;legend&quot;&gt;
    &lt;span&gt;&lt;i class=&quot;sw&quot; style=&quot;background:#161a10;border:1px solid #26301a&quot;&gt;&lt;/i&gt; pending&lt;/span&gt;
    &lt;span&gt;&lt;i class=&quot;sw&quot; style=&quot;background:#2a3416;border:1px solid #e0a500&quot;&gt;&lt;/i&gt; drafted block&lt;/span&gt;
    &lt;span&gt;&lt;i class=&quot;sw&quot; style=&quot;background:#4d7a00;border:1px solid #76B900&quot;&gt;&lt;/i&gt; accepted&lt;/span&gt;
    &lt;span&gt;&lt;i class=&quot;sw&quot; style=&quot;background:#3a1512;border:1px solid #d14b3a&quot;&gt;&lt;/i&gt; rejected&lt;/span&gt;
    &lt;span&gt;&lt;i class=&quot;sw&quot; style=&quot;background:#76B900&quot;&gt;&lt;/i&gt; bonus token&lt;/span&gt;
  &lt;/div&gt;

  &lt;div class=&quot;note&quot;&gt;
    &lt;b&gt;Illustrative simulation.&lt;/b&gt; Token counts and speedup are computed from the controls above,
    not from a live model. They model the core mechanism: autoregressive drafting costs one target
    pass per token, while DFlash drafts a block in a single pass and the target verifies it in parallel,
    accepting a run of tokens (&amp;tau;) plus one bonus token. Real speedups depend on the model,
    task, hardware, and concurrency. The DFlash paper reports up to 6.08&amp;times; lossless single-stream
    speedup on Qwen3-8B; NVIDIA reports up to 15&amp;times; throughput on Blackwell at a fixed interactivity target.
  &lt;/div&gt;

  &lt;div class=&quot;foot&quot;&gt;Built on the &lt;b&gt;DFlash&lt;/b&gt; paper (UC San Diego, z-lab) &amp;middot; Marktechpost&lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var TOTAL = 40;
  var bs = document.getElementById('bs'), acc = document.getElementById('acc'), dr = document.getElementById('dr');
  var bsVal = document.getElementById('bsVal'), accVal = document.getElementById('accVal'), drVal = document.getElementById('drVal');
  var arGrid = document.getElementById('arGrid'), dfGrid = document.getElementById('dfGrid');
  var arSteps = document.getElementById('arSteps'), arTok = document.getElementById('arTok'), arTime = document.getElementById('arTime');
  var dfSteps = document.getElementById('dfSteps'), dfTok = document.getElementById('dfTok'), dfTime = document.getElementById('dfTime');
  var speedup = document.getElementById('speedup'), verdictDesc = document.getElementById('verdictDesc');
  var runBtn = document.getElementById('runBtn'), resetBtn = document.getElementById('resetBtn');
  var running = false, timers = [];

  function clamp(v,a,b){return Math.max(a,Math.min(b,v));}
  function syncLabels(){
    bsVal.textContent = bs.value;
    var maxAcc = Math.min(parseInt(bs.value), 12);
    if(parseInt(acc.value) &gt; maxAcc){ acc.value = maxAcc; }
    acc.max = maxAcc;
    accVal.textContent = acc.value;
    drVal.innerHTML = (parseInt(dr.value)/100).toFixed(2) + '&amp;times;';
  }
  [bs,acc,dr].forEach(function(el){ el.addEventListener('input', function(){ syncLabels(); reset(); }); });

  function buildGrid(grid){
    grid.innerHTML='';
    var cells=[];
    for(var i=0;i&lt;TOTAL;i++){
      var d=document.createElement('div'); d.className='tok'; grid.appendChild(d); cells.push(d);
    }
    return cells;
  }
  var arCells = buildGrid(arGrid), dfCells = buildGrid(dfGrid);

  function reset(){
    running=false; runBtn.disabled=false;
    timers.forEach(clearTimeout); timers=[];
    arCells = buildGrid(arGrid); dfCells = buildGrid(dfGrid);
    arSteps.textContent='0'; arTok.textContent='0'; arTime.textContent='0';
    dfSteps.textContent='0'; dfTok.textContent='0'; dfTime.textContent='0';
    speedup.innerHTML='&amp;mdash;';
    verdictDesc.innerHTML='Press &lt;b&gt;Run decode&lt;/b&gt;. DFlash drafts a whole block per pass, so it needs far fewer sequential target verifications to reach the same answer.';
  }
  resetBtn.addEventListener('click', reset);

  function run(){
    if(running) return;
    reset(); running=true; runBtn.disabled=true;
    var blockSize=parseInt(bs.value), tau=parseInt(acc.value), draftRatio=parseInt(dr.value)/100;

    // ---- timing model (units of one target verify pass) ----
    var arPasses = TOTAL;                  // one token per pass
    var arTimeUnits = arPasses * 1;        // verify = 1 unit each
    var perBlock = tau + 1;                // accepted run + 1 bonus token
    var dfPasses = Math.ceil(TOTAL / perBlock);
    var dfTimeUnits = dfPasses * (draftRatio + 1); // draft + verify per cycle
    var spd = arTimeUnits / dfTimeUnits;

    // ---- animate autoregressive: one cell at a time ----
    var i=0;
    function arStep(){
      if(i&gt;=TOTAL) return;
      arCells[i].classList.add('accept');
      i++;
      arSteps.textContent=i; arTok.textContent=i; arTime.textContent=i;
      timers.push(setTimeout(arStep, 70));
    }
    arStep();

    // ---- animate DFlash: block by block ----
    var pos=0, pass=0;
    function dfBlock(){
      if(pos&gt;=TOTAL){ finish(); return; }
      pass++;
      var blockEnd=Math.min(pos+blockSize, TOTAL);
      // 1) show drafted block (amber); clear any prior reject state at the frontier
      for(var j=pos;j&lt;blockEnd;j++){
        dfCells[j].classList.remove('reject','accept','bonus');
        dfCells[j].classList.add('draft');
      }
      dfSteps.textContent=pass;
      timers.push(setTimeout(function(){
        // 2) verify: accept tau tokens + 1 bonus, reject the rest of the block
        var acceptEnd=Math.min(pos+perBlock, blockEnd);
        for(var j=pos;j&lt;blockEnd;j++){
          dfCells[j].classList.remove('draft');
          if(j&lt;acceptEnd-1){ dfCells[j].classList.add('accept'); }
          else if(j===acceptEnd-1){ dfCells[j].classList.add('bonus'); }
          else { dfCells[j].classList.add('reject'); }
        }
        var produced=Math.min(acceptEnd, TOTAL);
        dfTok.textContent=produced;
        dfTime.textContent=(pass*(draftRatio+1)).toFixed(1);
        pos=acceptEnd;
        timers.push(setTimeout(dfBlock, 360));
      }, 320));
    }
    dfBlock();

    function finish(){
      running=false; runBtn.disabled=false;
      arTime.textContent=arTimeUnits.toFixed(0);
      dfTime.textContent=dfTimeUnits.toFixed(1);
      speedup.textContent=spd.toFixed(2)+'\u00d7';
      verdictDesc.innerHTML='DFlash reached &lt;b&gt;'+TOTAL+' tokens&lt;/b&gt; in &lt;b&gt;'+dfPasses+
        ' target passes&lt;/b&gt; vs &lt;b&gt;'+arPasses+'&lt;/b&gt; for autoregressive. Fewer sequential '+
        'verifications &amp;rarr; &lt;b&gt;'+spd.toFixed(2)+'&amp;times;&lt;/b&gt; faster at &amp;tau;='+tau+', block size '+blockSize+'.';
    }
  }
  runBtn.addEventListener('click', run);

  syncLabels();
})();
&lt;/script&gt;

&lt;script&gt;
(function(){
  function report(){
    var el=document.querySelector('.wrap');
    var h=(el?el.offsetHeight:document.body.offsetHeight)+40;
    parent.postMessage({dflashHeight:h},'*');
  }
  window.addEventListener('load',function(){report();setTimeout(report,300);});
  window.addEventListener('resize',report);
  // report after user interactions/animation settle
  document.addEventListener('click',function(){setTimeout(report,200);setTimeout(report,5600);});
  document.addEventListener('input',function(){setTimeout(report,150);});
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"
  style="width:100%;height:600px;border:0;display:block;overflow:hidden;background:#0a0c08;border-radius:12px;"
  loading="lazy" title="DFlash Speculative Decoding Demo"></iframe>
</div>
<script>
(function(){
  window.addEventListener('message',function(e){
    if(e.data && e.data.dflashHeight){
      var f=document.getElementById('dflash-frame');
      if(f){ f.style.height=e.data.dflashHeight+'px'; }
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <em><a href="https://z-lab.ai/projects/dflash/"><strong>Project page</strong></a>, <a href="https://arxiv.org/abs/2602.06036"><strong>Paper (arXiv 2602.06036)</strong></a>, <strong><a href="https://github.com/z-lab/dflash">GitHub</a>,</strong> <strong><a href="https://huggingface.co/collections/z-lab/dflash">Hugging Face checkpoints</a> </strong>and <a href="https://developer.nvidia.com/blog/boost-inference-performance-up-to-15x-on-nvidia-blackwell-using-dflash-speculative-decoding/"><strong>NVIDIA blog</strong></a>.</em><strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/24/dflash-speculative-decoding-drafts-whole-token-blocks-in-parallel-for-up-to-15x-higher-throughput-on-nvidia-blackwell/">DFlash Speculative Decoding Drafts Whole Token Blocks in Parallel for Up to 15x Higher Throughput on NVIDIA Blackwell</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80675</post-id>
		<media:thumbnail url="https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-150x150.png" />
		<media:content url="https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1.png" medium="image">
			<media:title type="html">Screenshot 2026-06-24 at 12.00.06 AM</media:title>
			<media:thumbnail url="https://www.marktechpost.com/wp-content/uploads/2026/06/Screenshot-2026-06-24-at-12.00.06-AM-1-150x150.png" />
		</media:content>
	</item>
		<item>
		<title>Mistral OCR 4 Brings Citation-Ready Structured Output to RAG, Agentic, and Enterprise Search Pipelines</title>
		<link>https://www.marktechpost.com/2026/06/23/mistral-ocr-4/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Tue, 23 Jun 2026 23:43:43 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[OCR]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80672</guid>

					<description><![CDATA[<p>Mistral AI released OCR 4 on June 23, 2026, moving from clean text extraction to structured document output. Each block returns a bounding box, a typed classification, and per-page and per-word confidence scores. The model supports 170 languages, runs in a single self-hosted container, and feeds citation-ready inputs into RAG, agentic, and enterprise search pipelines through one API endpoint.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/mistral-ocr-4/">Mistral OCR 4 Brings Citation-Ready Structured Output to RAG, Agentic, and Enterprise Search Pipelines</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Today, Mistral AI released <strong>OCR 4</strong>, its latest document-understanding model. This new release adds bounding boxes, block classification, and inline confidence scores alongside extracted text. It supports <strong>170 languages across 10 language groups</strong> and runs in a single container for fully self-hosted deployments. OCR 4 also serves as an ingestion component for enterprise search, RAG, and domain-specific retrieval pipelines. </p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li>OCR 4 returns bounding boxes, typed-block labels, and per-word confidence scores, not just text.</li>



<li>It supports 170 languages across 10 groups, with gains on rare and low-resource languages.</li>



<li>Independent annotators preferred OCR 4 over every system tested, averaging 72% win rates.</li>



<li>Pricing is $4 per 1,000 pages, dropping to $2 with the Batch-API discount.</li>



<li>One endpoint serves both raw extraction and schema-driven Document AI output.</li>
</ul>



<h2 id="h-mistral-ocr-4" class="wp-block-heading"><strong>Mistral OCR 4</strong></h2>



<p class="wp-block-paragraph">Mistral OCR 4 extracts and structures content from a wide range of documents. Previous generations focused on converting a page into clean text and tables. OCR 4 instead returns a structured representation of the whole document.</p>



<p class="wp-block-paragraph">Each block is localized with a bounding box and classified by type. Block types include titles, tables, equations, signatures, and more. Inline confidence scores are generated per-page and per-word.</p>



<p class="wp-block-paragraph">Downstream systems therefore learn more than what a document says. They also learn where each element sits, what role it plays, and how confident the model is. That extra context matters for citations, redactions, and human-in-the-loop verification.</p>



<p class="wp-block-paragraph">OCR 4 accepts common enterprise formats, including PDF, DOC, PPT, and OpenDocument. The model is compact enough to deploy in a single container. Self-managed deployment is available to enterprise customers for data residency and compliance.</p>



<h2 id="h-benchmark" class="wp-block-heading"><strong>Benchmark</strong></h2>



<p class="wp-block-paragraph">Mistral compared OCR 4 against AI-native OCR models, frontier general-purpose models, enterprise document services, and Mistral OCR 3.</p>



<p class="wp-block-paragraph">A number of independent annotators preferred OCR 4 over every leading system tested. Win rates averaged 72% across the comparison set. The evaluation used 600+ documents across 12+ languages, sourced from third-party vendors. Annotators ranked each competitor&#8217;s output against OCR 4&#8217;s, document by document.</p>



<p class="wp-block-paragraph">On automated benchmarks, OCR 4 scored <strong>85.20</strong> on the public OlmOCRBench. It scored <strong>93.07</strong> on OmniDocBench and <strong>.98</strong> on Mistral&#8217;s internal Crawl Multilingual evaluation.</p>



<p class="wp-block-paragraph">Two customer data points add context. Rogo reported equivalent accuracy at roughly 8x lower cost and 17x lower latency versus leading agentic parsers. Anaqua measured roughly 4x faster per page than its incumbent provider.</p>



<h2 id="h-segmentation-not-just-text" class="wp-block-heading"><strong>Segmentation, Not Just Text</strong></h2>



<p class="wp-block-paragraph">Bounding boxes were Mistral&#8217;s most-requested capability. They localize text for in-context highlighting and reliable data pipelines.</p>



<p class="wp-block-paragraph">Block types and confidence scores serve different jobs. They drive source-grounded citations, redactions, and human-in-the-loop verification. This structure supports several downstream workloads.</p>



<p class="wp-block-paragraph">Clean, classified blocks become better retrieval units for RAG. Agents gain structural primitives to act on documents, not just read them. Connectors receive consistent, typed output for ingestion and indexing.</p>



<p class="wp-block-paragraph">OCR 4 is also an ingestion component of <strong>Mistral Search Toolkit</strong>, now in public preview. Search Toolkit is Mistral&#8217;s open-source, composable search framework. Its structured output supplies citation-ready inputs to retrieval and evaluation workflows.</p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use Cases With Examples</strong></h2>



<p class="wp-block-paragraph">OCR 4 supports both high-volume pipelines and interactive document workflows.</p>



<ul class="wp-block-list">
<li><strong>Document parsing and extraction</strong>: Turn a multilingual contract into clean, structured markdown for indexing.</li>



<li><strong>Retrieval-Augmented Generation (RAG)</strong>: Feed classified blocks into Search Toolkit for source-grounded answers with citations.</li>



<li><strong>Agentic workflows</strong>: Give an invoice-processing agent typed fields and bounding boxes to fill forms automatically.</li>



<li><strong>Confidence-gated pipelines</strong>: Route low-confidence regions to human verifiers, and auto-approve the rest.</li>



<li><strong>Enterprise search</strong>: Use OCR 4 as a data-source component for ingestion and entity extraction across an archive.</li>
</ul>



<p class="wp-block-paragraph">Early users apply OCR 4 to turn invoices into structured fields and digitize company archives. Others extract clean text from technical reports or power enterprise search.</p>



<p class="wp-block-paragraph"><strong>A note on scope from Mistral official release</strong>: OCR 4 is a document-understanding model, not a decision-maker. It is not intended for medical diagnosis, legal judgment, or high-stakes financial decisions. It is also unsuited to safety-critical systems, real-time processing, or non-document inputs like raw audio or video.</p>



<h2 id="h-comparison-pure-extraction-vs-document-ai" class="wp-block-heading"><strong>Comparison: Pure Extraction vs Document AI</strong></h2>



<p class="wp-block-paragraph">OCR 4 ships behind a single API endpoint. Every request runs the same model. It always returns extracted content, bounding boxes, block types, confidence scores, and markdown. What varies is how much you layer on top.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th>Capability</th><th>Pure Extraction Mode</th><th>Document AI Mode (same endpoint)</th></tr></thead><tbody><tr><td>Output</td><td>Markdown, bboxes, block types, confidence</td><td>Structured JSON in a schema you define</td></tr><tr><td>How it works</td><td>Raw OCR response</td><td>OCR output fed to <code>mistral-small-2603</code></td></tr><tr><td>Image annotation</td><td>Not applied</td><td>Per-image vision-language call on schema</td></tr><tr><td>Custom prompt</td><td>No</td><td>Yes, guides interpretation or summary</td></tr><tr><td>Best for</td><td>Pipelines, agents, batch ingestion</td><td>Business users, pilots, no parsing logic</td></tr><tr><td>Price</td><td>$4 / 1,000 pages ($2 batch)</td><td>$5 / 1,000 pages</td></tr><tr><td>Self-hosting</td><td>Available for enterprise</td><td>Available for enterprise</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">The decision rule is simple. Need raw extracted content? Use OCR 4 as-is. Need the output reshaped into a schema or annotated with domain fields? Add the Document AI parameters to the same call.</p>



<h2 id="h-working-with-the-api" class="wp-block-heading"><strong>Working With the API</strong></h2>



<p class="wp-block-paragraph">Basic extraction takes a document URL and returns structured pages. Set <code>include_blocks=True</code> to get the typed blocks and bounding boxes.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import os
from mistralai.client import Mistral

client = Mistral(api_key=os.environ["MISTRAL_API_KEY"])

ocr_response = client.ocr.process(
    model="mistral-ocr-latest",
    document={
        "type": "document_url",
        "document_url": "https://arxiv.org/pdf/2201.04234"
    },
    include_blocks=True,                  # typed blocks + bounding boxes
    table_format="html",                  # None (inline), "markdown", or "html"
    include_image_base64=True
)</code></pre></div></div>



<p class="wp-block-paragraph">The response is a JSON object with a <code>pages</code> array. Each page carries <code>markdown</code>, <code>images</code>, <code>tables</code>, <code>hyperlinks</code>, <code>dimensions</code>, and <code>confidence_scores</code>. To gate a human-review pipeline, request per-word confidence.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">ocr_response = client.ocr.process(
    model="mistral-ocr-latest",
    document={"type": "document_url",
              "document_url": "https://arxiv.org/pdf/2201.04234"},
    confidence_scores_granularity="word"   # or "page" for aggregates
)</code></pre></div></div>



<p class="wp-block-paragraph">The <code>"word"</code> setting adds a <code>word_confidence_scores</code> array per page and per table entry. For high-volume jobs, Mistral recommends the Batch Inference service, which halves the per-page cost.</p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<h2 id="h-try-it-interactive-output-explorer" class="wp-block-heading"><strong>Try It: Interactive Output Explorer</strong></h2>



<p class="wp-block-paragraph">The embed below visualizes OCR 4&#8217;s structured output. Switch between sample documents, toggle bounding boxes and block types, and turn on the confidence heatmap. The Markdown and JSON tabs show the two output shapes side by side. The sample data is illustrative, not a live API call.</p>



<p class="wp-block-paragraph"></p>



<!-- Mistral OCR 4 — Live Output Explorer | paste into a WordPress "Custom HTML" block -->
<iframe id="mocr4-frame"
  style="width:100%;border:0;display:block;min-height:600px;overflow:hidden;"
  scrolling="no"
  srcdoc="&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;div id=&quot;m4&quot;&gt;
  &lt;div class=&quot;m4-wrap&quot;&gt;
    &lt;header class=&quot;m4-head&quot;&gt;
      &lt;div class=&quot;m4-flame&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;m4-htxt&quot;&gt;
        &lt;h2&gt;Mistral OCR 4 — Live Output Explorer&lt;/h2&gt;
        &lt;p&gt;Run an illustrative OCR pass over a real document. Watch bounding boxes, block types &amp;amp; confidence scores appear, then stream into structured output. Sample visualization — not a live API call.&lt;/p&gt;
      &lt;/div&gt;
    &lt;/header&gt;

    &lt;div class=&quot;m4-bar&quot;&gt;
      &lt;div class=&quot;m4-seg&quot; id=&quot;m4-docs&quot;&gt;
        &lt;button class=&quot;m4-doc active&quot; data-doc=&quot;invoice&quot;&gt;Invoice (EN)&lt;/button&gt;
        &lt;button class=&quot;m4-doc&quot; data-doc=&quot;paper&quot;&gt;Paper (EN)&lt;/button&gt;
        &lt;button class=&quot;m4-doc&quot; data-doc=&quot;form&quot;&gt;Form (Hindi)&lt;/button&gt;
      &lt;/div&gt;
      &lt;button class=&quot;m4-run&quot; id=&quot;m4-run&quot;&gt;▸ Run OCR 4&lt;/button&gt;
    &lt;/div&gt;

    &lt;div class=&quot;m4-bar2&quot;&gt;
      &lt;label class=&quot;m4-tg&quot;&gt;&lt;input type=&quot;checkbox&quot; id=&quot;tg-box&quot; checked&gt; Bounding boxes&lt;/label&gt;
      &lt;label class=&quot;m4-tg&quot;&gt;&lt;input type=&quot;checkbox&quot; id=&quot;tg-type&quot; checked&gt; Block types&lt;/label&gt;
      &lt;label class=&quot;m4-tg&quot;&gt;&lt;input type=&quot;checkbox&quot; id=&quot;tg-conf&quot;&gt; Confidence heatmap&lt;/label&gt;
    &lt;/div&gt;

    &lt;div class=&quot;m4-stage&quot;&gt;
      &lt;div class=&quot;m4-docwrap&quot;&gt;
        &lt;div class=&quot;m4-page&quot; id=&quot;m4-page&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;m4-overlay&quot; id=&quot;m4-overlay&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;m4-scan&quot; id=&quot;m4-scan&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;m4-hint&quot; id=&quot;m4-hint&quot;&gt;Press &lt;b&gt;Run OCR 4&lt;/b&gt; to extract this document →&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;m4-side&quot;&gt;
        &lt;div class=&quot;m4-tabs&quot;&gt;
          &lt;button class=&quot;m4-tab active&quot; data-view=&quot;md&quot;&gt;Markdown&lt;/button&gt;
          &lt;button class=&quot;m4-tab&quot; data-view=&quot;json&quot;&gt;JSON&lt;/button&gt;
        &lt;/div&gt;
        &lt;pre class=&quot;m4-out&quot; id=&quot;m4-out&quot;&gt;&lt;span class=&quot;m4-ph&quot;&gt;// extracted output appears here after the run&lt;/span&gt;&lt;/pre&gt;
        &lt;div class=&quot;m4-meta&quot; id=&quot;m4-meta&quot;&gt;
          &lt;span&gt;model &lt;b&gt;mistral-ocr-4-0&lt;/b&gt;&lt;/span&gt;
          &lt;span&gt;blocks &lt;b id=&quot;m4-nb&quot;&gt;—&lt;/b&gt;&lt;/span&gt;
          &lt;span&gt;avg conf &lt;b id=&quot;m4-ac&quot;&gt;—&lt;/b&gt;&lt;/span&gt;
          &lt;span&gt;min conf &lt;b id=&quot;m4-mc&quot;&gt;—&lt;/b&gt;&lt;/span&gt;
        &lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;m4-legend&quot; id=&quot;m4-legend&quot;&gt;&lt;/div&gt;

    &lt;footer class=&quot;m4-foot&quot;&gt;
      &lt;span&gt;Structured output per Mistral OCR 4 (June 2026): per-block bbox · typed classification · per-page &amp;amp; per-word confidence.&lt;/span&gt;
      &lt;span class=&quot;m4-brand&quot;&gt;Marktechpost&lt;/span&gt;
    &lt;/footer&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;style&gt;
  #m4 *{box-sizing:border-box;margin:0;padding:0}
  #m4{
    --f1:#e10500;--f2:#ff5a00;--f3:#ff8205;--f4:#ffaf00;--f5:#ffd61f;
    --bg:#15100c;--panel:#1f1813;--panel2:#2a201a;--line:#3a2c22;
    --ink:#fbeede;--mut:#c7ad96;--mut2:#9a836f;
    --t-title:#ff5a00;--t-para:#19b27e;--t-table:#3b82f6;--t-eq:#a855f7;--t-sig:#ec4899;--t-fig:#f59e0b;
    font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;
    color:var(--ink);background:var(--bg);line-height:1.5;-webkit-font-smoothing:antialiased
  }
  .m4-wrap{max-width:1020px;margin:0 auto;padding:20px}
  .m4-head{display:flex;gap:15px;align-items:flex-start;margin-bottom:16px}
  .m4-flame{width:44px;height:44px;flex:0 0 44px;border-radius:11px;
    background:linear-gradient(150deg,var(--f5),var(--f4) 32%,var(--f3) 58%,var(--f1));
    box-shadow:0 6px 22px rgba(255,90,0,.32)}
  .m4-htxt h2{font-size:18px;font-weight:700;letter-spacing:-.01em}
  .m4-htxt p{font-size:12px;color:var(--mut);margin-top:3px;max-width:760px}

  .m4-bar{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:10px;margin-bottom:10px}
  .m4-seg{display:inline-flex;background:var(--panel);border:1px solid var(--line);border-radius:11px;padding:4px;gap:3px}
  .m4-doc{background:transparent;border:none;color:var(--mut);font-size:12.5px;font-weight:600;
    padding:7px 13px;border-radius:8px;cursor:pointer;transition:.15s}
  .m4-doc:hover{color:var(--ink)}
  .m4-doc.active{color:#1a0f08;background:linear-gradient(140deg,var(--f4),var(--f3))}
  .m4-run{border:none;cursor:pointer;font-size:13px;font-weight:800;letter-spacing:.01em;
    color:#1a0f08;padding:10px 20px;border-radius:10px;
    background:linear-gradient(135deg,var(--f5),var(--f3) 55%,var(--f2));
    box-shadow:0 5px 18px rgba(255,90,0,.32);transition:.15s}
  .m4-run:hover{filter:brightness(1.06);transform:translateY(-1px)}
  .m4-run:disabled{opacity:.55;cursor:default;transform:none;box-shadow:none}

  .m4-bar2{display:flex;flex-wrap:wrap;gap:16px;margin-bottom:14px;padding-left:2px}
  .m4-tg{font-size:12.5px;color:var(--mut);display:flex;align-items:center;gap:6px;cursor:pointer;user-select:none}
  .m4-tg input{accent-color:var(--f3);width:15px;height:15px;cursor:pointer}

  .m4-stage{display:grid;grid-template-columns:1.06fr 1fr;gap:14px;align-items:start}
  .m4-docwrap{position:relative;border-radius:13px;overflow:hidden;border:1px solid var(--line);background:#fbf6ee}
  .m4-page{position:relative;z-index:1;padding:22px 24px;color:#241608;font-size:11px;min-height:430px}
  .m4-overlay{position:absolute;inset:0;z-index:2;pointer-events:none}
  .m4-bx{position:absolute;border-radius:4px;opacity:0;transform:scale(.96);
    transition:opacity .22s,transform .22s,box-shadow .12s;pointer-events:auto;cursor:pointer}
  .m4-bx.on{opacity:1;transform:scale(1)}
  .m4-bx.showbox{box-shadow:0 0 0 1.5px var(--bc)}
  .m4-bx.hl{box-shadow:0 0 0 2.4px var(--f1),0 0 0 5px rgba(225,5,0,.18);z-index:9}
  .m4-bx .m4-tag{position:absolute;top:-8px;left:-1px;font-size:7.5px;font-weight:800;
    text-transform:uppercase;letter-spacing:.03em;padding:1px 4px;border-radius:4px;color:#1a0f08;
    white-space:nowrap;display:none;line-height:1.3}
  .m4-bx.showtype .m4-tag{display:block}

  .m4-scan{position:absolute;left:0;right:0;top:0;height:46px;z-index:3;opacity:0;pointer-events:none;
    background:linear-gradient(180deg,rgba(255,175,0,0),rgba(255,130,5,.28) 60%,rgba(255,90,0,.85));
    border-bottom:2px solid var(--f2);box-shadow:0 0 26px 8px rgba(255,90,0,.35)}
  .m4-hint{position:absolute;z-index:4;left:50%;bottom:14px;transform:translateX(-50%);
    background:rgba(21,16,12,.9);color:var(--f4);font-size:11.5px;font-weight:600;
    padding:7px 14px;border-radius:20px;border:1px solid var(--line);white-space:nowrap}

  /* document content styling */
  .m4-page h3.doctitle{font-size:15px;font-weight:800;color:#2a1505;letter-spacing:-.01em}
  .m4-page .sub{color:#6b5333;font-size:10.5px}
  .m4-page .row{display:flex;justify-content:space-between;gap:14px}
  .m4-page .col{flex:1}
  .m4-page table.inv{width:100%;border-collapse:collapse;margin:2px 0;font-size:10px}
  .m4-page table.inv th{background:#efe2cf;text-align:left;padding:4px 6px;font-weight:700;color:#3a2410}
  .m4-page table.inv td{padding:4px 6px;border-bottom:1px solid #ece0cd}
  .m4-page .lbl{font-weight:700;color:#3a2410}
  .m4-page .eq{font-family:ui-monospace,Menlo,Consolas,monospace;background:#f1e8d8;
    padding:7px 10px;border-radius:6px;text-align:center;font-size:11px;color:#2a1505}
  .m4-page .fig{height:74px;border:1px dashed #c9b48f;border-radius:6px;background:
    repeating-linear-gradient(135deg,#f3ead9,#f3ead9 7px,#eaddc6 7px,#eaddc6 14px);
    display:flex;align-items:flex-end;justify-content:center;color:#7a6038;font-size:9.5px;padding:4px}
  .m4-page .sigline{border-top:1.5px solid #4a3a22;width:130px;
    padding-top:3px;color:#5a4527;font-size:9.5px}
  .m4-page .blk{margin:0}
  .m4-page .pad{margin-top:9px}
  .m4-page .pad2{margin-top:7px}
  .m4-hindi{font-size:11px}

  .m4-side{display:flex;flex-direction:column;background:var(--panel);border:1px solid var(--line);border-radius:13px;overflow:hidden}
  .m4-tabs{display:flex;border-bottom:1px solid var(--line)}
  .m4-tab{flex:1;background:transparent;border:none;color:var(--mut2);font-size:12.5px;font-weight:700;padding:10px;cursor:pointer;transition:.15s}
  .m4-tab.active{color:var(--f4);box-shadow:inset 0 -2px 0 var(--f3)}
  .m4-out{flex:1;overflow:auto;padding:13px 14px;font-size:11.5px;line-height:1.6;
    font-family:&quot;SF Mono&quot;,ui-monospace,Menlo,Consolas,monospace;color:var(--ink);
    white-space:pre-wrap;word-break:break-word;min-height:300px;max-height:430px}
  .m4-ph{color:var(--mut2)}
  .m4-out .ln{display:block;border-radius:4px;padding:0 3px;margin:0 -3px;transition:background .12s}
  .m4-out .ln.hl{background:rgba(255,130,5,.16)}
  .m4-out .k{color:var(--f4)}.m4-out .s{color:#8fd19e}.m4-out .n{color:#f2a65a}.m4-out .c{color:var(--mut2)}
  .m4-meta{border-top:1px solid var(--line);padding:9px 13px;font-size:10.5px;color:var(--mut);
    display:flex;justify-content:space-between;gap:8px;flex-wrap:wrap}
  .m4-meta b{color:var(--f4)}

  .m4-legend{display:flex;flex-wrap:wrap;gap:8px 15px;margin-top:14px;padding:11px 14px;background:var(--panel);border:1px solid var(--line);border-radius:11px}
  .m4-leg{display:flex;align-items:center;gap:6px;font-size:11.5px;color:var(--mut)}
  .m4-sw{width:12px;height:12px;border-radius:3px}

  .m4-foot{display:flex;justify-content:space-between;gap:10px;flex-wrap:wrap;margin-top:14px;font-size:10.5px;color:var(--mut2)}
  .m4-brand{color:var(--f3);font-weight:800;letter-spacing:.03em}

  @media (max-width:640px){
    .m4-stage{grid-template-columns:1fr}
    .m4-htxt h2{font-size:16px}
    .m4-out{max-height:300px}
  }
&lt;/style&gt;

&lt;script&gt;
(function(){
  var TYPES={
    title:{c:'var(--t-title)',hex:'#ff5a00',n:'Title'},
    paragraph:{c:'var(--t-para)',hex:'#19b27e',n:'Paragraph'},
    table:{c:'var(--t-table)',hex:'#3b82f6',n:'Table'},
    equation:{c:'var(--t-eq)',hex:'#a855f7',n:'Equation'},
    signature:{c:'var(--t-sig)',hex:'#ec4899',n:'Signature'},
    figure:{c:'var(--t-fig)',hex:'#f59e0b',n:'Figure'}
  };

  // Each doc = rendered HTML + ordered list of OCR-able elements (selector id, type, conf, md)
  var DOCS={
    invoice:{lang:'English',html:
      '&lt;div class=&quot;row&quot;&gt;&lt;h3 class=&quot;doctitle blk&quot; data-ocr=&quot;title&quot; data-conf=&quot;0.998&quot;&gt;ACME SUPPLIES — INVOICE&lt;/h3&gt;'+
      '&lt;div class=&quot;sub blk&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.984&quot; style=&quot;text-align:right&quot;&gt;Invoice #INV-20614&lt;br&gt;Date: 2026-06-12 · Due: 2026-07-12&lt;/div&gt;&lt;/div&gt;'+
      '&lt;div class=&quot;blk pad&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.973&quot;&gt;&lt;span class=&quot;lbl&quot;&gt;Bill To:&lt;/span&gt; Northwind Robotics Inc., 221B Pier Avenue, Santa Cruz CA&lt;/div&gt;'+
      '&lt;table class=&quot;inv pad blk&quot; data-ocr=&quot;table&quot; data-conf=&quot;0.961&quot;&gt;&lt;tr&gt;&lt;th&gt;Item&lt;/th&gt;&lt;th&gt;Qty&lt;/th&gt;&lt;th&gt;Unit&lt;/th&gt;&lt;th&gt;Total&lt;/th&gt;&lt;/tr&gt;'+
      '&lt;tr&gt;&lt;td&gt;Servo motor&lt;/td&gt;&lt;td&gt;12&lt;/td&gt;&lt;td&gt;$42&lt;/td&gt;&lt;td&gt;$504&lt;/td&gt;&lt;/tr&gt;'+
      '&lt;tr&gt;&lt;td&gt;LiDAR unit&lt;/td&gt;&lt;td&gt;3&lt;/td&gt;&lt;td&gt;$880&lt;/td&gt;&lt;td&gt;$2,640&lt;/td&gt;&lt;/tr&gt;'+
      '&lt;tr&gt;&lt;td&gt;Cabling kit&lt;/td&gt;&lt;td&gt;8&lt;/td&gt;&lt;td&gt;$35&lt;/td&gt;&lt;td&gt;$280&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;'+
      '&lt;div class=&quot;row pad&quot;&gt;&lt;div class=&quot;col&quot;&gt;&lt;/div&gt;&lt;div class=&quot;col blk&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.978&quot; style=&quot;text-align:right&quot;&gt;'+
      'Subtotal: $3,424&lt;br&gt;Tax (8.5%): $291.04&lt;br&gt;&lt;span class=&quot;lbl&quot;&gt;Total: $3,715.04&lt;/span&gt;&lt;/div&gt;&lt;/div&gt;'+
      '&lt;div class=&quot;pad&quot; style=&quot;margin-top:18px&quot;&gt;&lt;div class=&quot;sigline blk&quot; data-ocr=&quot;signature&quot; data-conf=&quot;0.912&quot;&gt;Authorized — J. Okafor&lt;/div&gt;&lt;/div&gt;'+
      '&lt;div class=&quot;blk pad2 sub&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.955&quot;&gt;Payment terms NET-30. Wire details on page 2.&lt;/div&gt;'
    },
    paper:{lang:'English',html:
      '&lt;h3 class=&quot;doctitle blk&quot; data-ocr=&quot;title&quot; data-conf=&quot;0.995&quot; style=&quot;text-align:center&quot;&gt;Sparse Attention for Long-Context Retrieval&lt;/h3&gt;'+
      '&lt;div class=&quot;sub blk pad2&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.969&quot; style=&quot;text-align:center&quot;&gt;A. Rivera, K. Sato, L. Demir — NeurAI Lab · 2026&lt;/div&gt;'+
      '&lt;div class=&quot;row pad&quot; style=&quot;gap:18px&quot;&gt;'+
      '&lt;div class=&quot;col blk&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.972&quot;&gt;&lt;span class=&quot;lbl&quot;&gt;Abstract.&lt;/span&gt; We present a sparse attention scheme that reduces memory while preserving recall across 200K-token windows.&lt;/div&gt;'+
      '&lt;div class=&quot;col blk&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.964&quot;&gt;&lt;span class=&quot;lbl&quot;&gt;1. Introduction.&lt;/span&gt; Transformer context cost scales quadratically. Prior work trades recall for speed; we target both.&lt;/div&gt;&lt;/div&gt;'+
      '&lt;div class=&quot;eq blk pad&quot; data-ocr=&quot;equation&quot; data-conf=&quot;0.918&quot;&gt;A(Q,K,V) = softmax(QKᵀ / √d_k + M) · V&lt;/div&gt;'+
      '&lt;div class=&quot;row pad&quot; style=&quot;gap:18px&quot;&gt;&lt;div class=&quot;col blk fig&quot; data-ocr=&quot;figure&quot; data-conf=&quot;0.889&quot;&gt;Figure 1 — recall vs window size&lt;/div&gt;'+
      '&lt;div class=&quot;col blk&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.951&quot;&gt;&lt;span class=&quot;lbl&quot;&gt;Results.&lt;/span&gt; Recall holds at 0.91 while peak memory drops 38% versus dense baselines on identical hardware.&lt;/div&gt;&lt;/div&gt;'+
      '&lt;div class=&quot;blk pad2 sub&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.943&quot;&gt;References: [1] Vaswani et al. [2] Beltagy et al. [3] Sato et al.&lt;/div&gt;'
    },
    form:{lang:'Hindi (rare-language group)',html:
      '&lt;h3 class=&quot;doctitle blk m4-hindi&quot; data-ocr=&quot;title&quot; data-conf=&quot;0.981&quot; style=&quot;text-align:center&quot;&gt;पंजीकरण प्रपत्र · Registration Form&lt;/h3&gt;'+
      '&lt;div class=&quot;blk pad m4-hindi&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.949&quot;&gt;नाम / Name: __________________________________&lt;/div&gt;'+
      '&lt;div class=&quot;row pad&quot; style=&quot;gap:14px&quot;&gt;&lt;div class=&quot;col blk m4-hindi&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.927&quot;&gt;जन्म तिथि / DOB: ____________&lt;/div&gt;'+
      '&lt;div class=&quot;col blk m4-hindi&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.931&quot;&gt;शहर / City: ____________&lt;/div&gt;&lt;/div&gt;'+
      '&lt;table class=&quot;inv pad blk m4-hindi&quot; data-ocr=&quot;table&quot; data-conf=&quot;0.902&quot;&gt;&lt;tr&gt;&lt;th&gt;क्षेत्र / Field&lt;/th&gt;&lt;th&gt;विवरण / Value&lt;/th&gt;&lt;/tr&gt;'+
      '&lt;tr&gt;&lt;td&gt;पहचान / ID&lt;/td&gt;&lt;td&gt;आधार / Aadhaar&lt;/td&gt;&lt;/tr&gt;&lt;tr&gt;&lt;td&gt;श्रेणी / Category&lt;/td&gt;&lt;td&gt;सामान्य / General&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;'+
      '&lt;div class=&quot;blk pad m4-hindi&quot; data-ocr=&quot;paragraph&quot; data-conf=&quot;0.871&quot;&gt;घोषणा: मैं प्रमाणित करता हूँ कि दी गई जानकारी सही है।&lt;/div&gt;'+
      '&lt;div class=&quot;pad&quot; style=&quot;margin-top:14px&quot;&gt;&lt;div class=&quot;sigline blk m4-hindi&quot; data-ocr=&quot;signature&quot; data-conf=&quot;0.808&quot;&gt;हस्ताक्षर / Signature&lt;/div&gt;&lt;/div&gt;'
    }
  };

  var page=document.getElementById('m4-page'),
      overlay=document.getElementById('m4-overlay'),
      scan=document.getElementById('m4-scan'),
      hint=document.getElementById('m4-hint'),
      out=document.getElementById('m4-out'),
      runBtn=document.getElementById('m4-run'),
      legend=document.getElementById('m4-legend');
  var curDoc='invoice',curView='md',hasRun=false,running=false,blockData=[];

  function loadDoc(name){
    curDoc=name;hasRun=false;running=false;blockData=[];
    page.innerHTML=DOCS[name].html;
    overlay.innerHTML='';
    scan.style.opacity='0';scan.style.top='0px';
    hint.style.display='block';
    out.innerHTML='&lt;span class=&quot;m4-ph&quot;&gt;// extracted output appears here after the run&lt;/span&gt;';
    document.getElementById('m4-nb').textContent='—';
    document.getElementById('m4-ac').textContent='—';
    document.getElementById('m4-mc').textContent='—';
    runBtn.disabled=false;runBtn.textContent='▸ Run OCR 4';
  }

  function collectBlocks(){
    var els=page.querySelectorAll('[data-ocr]');
    var pageRect=page.getBoundingClientRect();
    var arr=[];
    els.forEach(function(el,i){
      var r=el.getBoundingClientRect();
      arr.push({
        i:i,el:el,type:el.dataset.ocr,conf:parseFloat(el.dataset.conf),
        x:Math.max(0,r.left-pageRect.left-3),y:Math.max(0,r.top-pageRect.top-3),
        w:r.width+6,h:r.height+6,
        text:(el.innerText||'').replace(/\s+/g,' ').trim()
      });
    });
    return arr;
  }

  function confColor(c){
    if(c&gt;=0.96) return 'rgba(25,178,126,.40)';
    if(c&gt;=0.90) return 'rgba(245,158,11,.40)';
    return 'rgba(225,5,0,.42)';
  }
  function applyToggles(){
    var sb=document.getElementById('tg-box').checked,
        st=document.getElementById('tg-type').checked,
        sc=document.getElementById('tg-conf').checked;
    overlay.querySelectorAll('.m4-bx').forEach(function(bx){
      bx.classList.toggle('showbox',sb);
      bx.classList.toggle('showtype',st);
      var t=bx.dataset.type,conf=parseFloat(bx.dataset.conf);
      bx.style.background=sc?confColor(conf):'transparent';
    });
  }

  function md(b){
    var t=b.text;
    if(b.type==='title') return '# '+t;
    if(b.type==='table') return t.replace(/\s{2,}/g,' | ');
    if(b.type==='equation') return '$$ '+t+' $$';
    if(b.type==='signature') return '&gt; _signature_: '+t;
    if(b.type==='figure') return '![figure](img-0.jpeg)  &lt;!-- '+t+' --&gt;';
    return t;
  }
  function esc(s){return s.replace(/&amp;/g,'&amp;amp;').replace(/&lt;/g,'&amp;lt;').replace(/&gt;/g,'&amp;gt;');}

  function renderOut(partial){
    var data=partial||blockData;
    if(curView==='md'){
      out.innerHTML=data.map(function(b){
        return '&lt;span class=&quot;ln&quot; data-id=&quot;'+b.i+'&quot;&gt;'+esc(md(b))+'&lt;/span&gt;';
      }).join('\n\n');
    }else{
      var lines=[];
      lines.push('&lt;span class=&quot;ln c&quot;&gt;{ &quot;model&quot;: &quot;mistral-ocr-4-0&quot;, &quot;pages&quot;: [{ &quot;index&quot;: 0,&lt;/span&gt;');
      lines.push('&lt;span class=&quot;ln c&quot;&gt;  &quot;blocks&quot;: [&lt;/span&gt;');
      data.forEach(function(b,idx){
        var comma=idx&lt;data.length-1?',':'';
        lines.push('&lt;span class=&quot;ln&quot; data-id=&quot;'+b.i+'&quot;&gt;    {'+
          '&lt;span class=&quot;k&quot;&gt;&quot;type&quot;&lt;/span&gt;: &lt;span class=&quot;s&quot;&gt;&quot;'+b.type+'&quot;&lt;/span&gt;, '+
          '&lt;span class=&quot;k&quot;&gt;&quot;bbox&quot;&lt;/span&gt;: [&lt;span class=&quot;n&quot;&gt;'+Math.round(b.x)+'&lt;/span&gt;,&lt;span class=&quot;n&quot;&gt;'+Math.round(b.y)+
          '&lt;/span&gt;,&lt;span class=&quot;n&quot;&gt;'+Math.round(b.w)+'&lt;/span&gt;,&lt;span class=&quot;n&quot;&gt;'+Math.round(b.h)+'&lt;/span&gt;], '+
          '&lt;span class=&quot;k&quot;&gt;&quot;confidence&quot;&lt;/span&gt;: &lt;span class=&quot;n&quot;&gt;'+b.conf.toFixed(3)+'&lt;/span&gt;}'+comma+'&lt;/span&gt;');
      });
      lines.push('&lt;span class=&quot;ln c&quot;&gt;  ] }] }&lt;/span&gt;');
      out.innerHTML=lines.join('\n');
    }
    syncHover();
  }

  function syncHover(){
    out.querySelectorAll('.ln[data-id]').forEach(function(ln){
      ln.addEventListener('mouseenter',function(){hlBox(ln.dataset.id,true);});
      ln.addEventListener('mouseleave',function(){hlBox(ln.dataset.id,false);});
    });
  }
  function hlBox(id,on){
    var bx=overlay.querySelector('.m4-bx[data-id=&quot;'+id+'&quot;]');
    if(bx) bx.classList.toggle('hl',on);
    var ln=out.querySelector('.ln[data-id=&quot;'+id+'&quot;]');
    if(ln) ln.classList.toggle('hl',on);
  }

  function run(){
    if(running) return;
    running=true;hasRun=true;hint.style.display='none';
    runBtn.disabled=true;runBtn.textContent='Scanning…';
    overlay.innerHTML='';blockData=[];
    out.innerHTML='&lt;span class=&quot;m4-ph&quot;&gt;// scanning page…&lt;/span&gt;';

    var blocks=collectBlocks();
    var pageH=page.getBoundingClientRect().height;

    // scan-line sweep
    scan.style.transition='none';scan.style.top='-46px';scan.style.opacity='1';
    void scan.offsetWidth;
    scan.style.transition='top 1.05s linear';
    scan.style.top=(pageH+10)+'px';

    var sumConf=0,minConf=1;
    blocks.forEach(function(b,idx){
      setTimeout(function(){
        var bx=document.createElement('div');
        bx.className='m4-bx';
        bx.dataset.type=b.type;bx.dataset.conf=b.conf;bx.dataset.id=b.i;
        bx.style.left=b.x+'px';bx.style.top=b.y+'px';bx.style.width=b.w+'px';bx.style.height=b.h+'px';
        bx.style.setProperty('--bc',TYPES[b.type].hex);
        bx.innerHTML='&lt;span class=&quot;m4-tag&quot; style=&quot;background:'+TYPES[b.type].hex+'&quot;&gt;'+TYPES[b.type].n+' '+b.conf.toFixed(2)+'&lt;/span&gt;';
        bx.addEventListener('mouseenter',function(){hlBox(b.i,true);});
        bx.addEventListener('mouseleave',function(){hlBox(b.i,false);});
        overlay.appendChild(bx);
        requestAnimationFrame(function(){bx.classList.add('on');});
        blockData.push(b);
        sumConf+=b.conf;if(b.conf&lt;minConf)minConf=b.conf;
        applyToggles();
        renderOut(blockData.slice());
        document.getElementById('m4-nb').textContent=blockData.length;
        document.getElementById('m4-ac').textContent=(sumConf/blockData.length).toFixed(3);
        document.getElementById('m4-mc').textContent=minConf.toFixed(3);
        if(idx===blocks.length-1){
          running=false;runBtn.disabled=false;runBtn.textContent='↻ Run again';
          scan.style.transition='opacity .4s';scan.style.opacity='0';
          postH();
        }
      },1050 + idx*170);
    });
  }

  function buildLegend(){
    var h='';
    Object.keys(TYPES).forEach(function(k){
      h+='&lt;span class=&quot;m4-leg&quot;&gt;&lt;span class=&quot;m4-sw&quot; style=&quot;background:'+TYPES[k].hex+'&quot;&gt;&lt;/span&gt;'+TYPES[k].n+'&lt;/span&gt;';
    });
    h+='&lt;span class=&quot;m4-leg&quot;&gt;&lt;span class=&quot;m4-sw&quot; style=&quot;background:rgba(25,178,126,.7)&quot;&gt;&lt;/span&gt;conf ≥0.96&lt;/span&gt;'+
       '&lt;span class=&quot;m4-leg&quot;&gt;&lt;span class=&quot;m4-sw&quot; style=&quot;background:rgba(245,158,11,.7)&quot;&gt;&lt;/span&gt;0.90–0.96&lt;/span&gt;'+
       '&lt;span class=&quot;m4-leg&quot;&gt;&lt;span class=&quot;m4-sw&quot; style=&quot;background:rgba(225,5,0,.7)&quot;&gt;&lt;/span&gt;&amp;lt;0.90&lt;/span&gt;';
    legend.innerHTML=h;
  }

  document.querySelectorAll('.m4-doc').forEach(function(b){
    b.addEventListener('click',function(){
      document.querySelectorAll('.m4-doc').forEach(function(x){x.classList.remove('active');});
      b.classList.add('active');loadDoc(b.dataset.doc);postH();
    });
  });
  document.querySelectorAll('.m4-tab').forEach(function(b){
    b.addEventListener('click',function(){
      document.querySelectorAll('.m4-tab').forEach(function(x){x.classList.remove('active');});
      b.classList.add('active');curView=b.dataset.view;
      if(hasRun&amp;&amp;blockData.length) renderOut();
    });
  });
  ['tg-box','tg-type','tg-conf'].forEach(function(id){
    document.getElementById(id).addEventListener('change',applyToggles);
  });
  runBtn.addEventListener('click',run);

  buildLegend();loadDoc('invoice');

  function postH(){
    var el=document.getElementById('m4');
    var h=el.offsetHeight+40;
    if(window.parent){window.parent.postMessage({mocr4Height:h},'*');}
  }
  if(window.ResizeObserver){
    var ro=new ResizeObserver(function(){postH();});
    ro.observe(document.getElementById('m4'));
  }
  window.addEventListener('load',postH);
  window.addEventListener('resize',postH);
  setTimeout(postH,300);setTimeout(postH,1200);
})();
&lt;/script&gt;
"></iframe>
<script>
(function(){
  window.addEventListener('message',function(e){
    if(e.data&&e.data.mocr4Height){
      var f=document.getElementById('mocr4-frame');
      if(f){f.style.height=e.data.mocr4Height+'px';}
    }
  });
})();
</script>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out&nbsp;the <em><strong><a href="https://mistral.ai/news/ocr-4/">Mistral OCR 4 announcement</a>, <a href="https://docs.mistral.ai/models/model-cards/ocr-4-0">OCR 4 model card</a>, </strong>and<strong> <a href="https://docs.mistral.ai/studio-api/document-processing/basic_ocr">OCR Processor docs</a></strong>.</em><strong>&nbsp;</strong>Also,&nbsp;feel free to follow us on&nbsp;<strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong>&nbsp;and don’t forget to join our&nbsp;<strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong>&nbsp;and Subscribe to&nbsp;<strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram?&nbsp;<strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/mistral-ocr-4/">Mistral OCR 4 Brings Citation-Ready Structured Output to RAG, Agentic, and Enterprise Search Pipelines</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80672</post-id>
	</item>
		<item>
		<title>Datalab Releases lift: A 9B Open-Weights Vision Model That Extracts Structured JSON From PDFs Using Schemas</title>
		<link>https://www.marktechpost.com/2026/06/23/datalab-releases-lift-a-9b-open-weights-vision-model-that-extracts-structured-json-from-pdfs-using-schemas/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Tue, 23 Jun 2026 19:35:33 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Computer Vision]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Generative AI]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[OCR]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Promote]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Sponsored]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80667</guid>

					<description><![CDATA[<p>Datalab released lift, a 9B open-weights vision model that turns PDFs and images into schema-matching JSON. It uses schema-constrained decoding for valid structure and trained abstention to return null instead of hallucinating absent fields, scoring 90.2% field accuracy on a 225-document benchmark.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/datalab-releases-lift-a-9b-open-weights-vision-model-that-extracts-structured-json-from-pdfs-using-schemas/">Datalab Releases lift: A 9B Open-Weights Vision Model That Extracts Structured JSON From PDFs Using Schemas</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph"><a href="https://pxllnk.co/euw154">Datalab</a> has released <strong><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">lift</a></strong>, a 9B open-weights vision model for structured extraction. You pass it a JSON schema, and it returns a JSON object that matches. The model reads PDFs and images directly, then decodes against your schema.</p>



<p class="wp-block-paragraph">This is Datalab&#8217;s first model built purely for extraction. The team already ships open-source OCR tools: <a href="https://github.com/datalab-to/chandra">chandra</a>, <a href="https://github.com/datalab-to/marker">marker</a>, and <a href="https://github.com/datalab-to/surya">surya</a>. <a href="https://pxllnk.co/t0x8a0r">lift </a>extends that work into schema-driven field extraction.</p>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/t0x8a0r">lift</a> scores <strong>90.2% field accuracy</strong> on Datalab&#8217;s 225-document benchmark. The research team reports it as the strongest small self-hostable model they tested. It runs at <a href="https://pxllnk.co/mf4o7kl">a median of <strong>9.5 seconds per document</strong></a>.</p>



<h2 id="h-what-is-datalab-lift" class="wp-block-heading"><strong>What is Datalab lift?</strong></h2>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/t0x8a0r">lift </a>is a 9B-parameter vision model for structured extraction. It accepts standard JSON Schema as input. It returns valid JSON of that shape as output.</p>



<p class="wp-block-paragraph">The model handles multi-page documents in a single pass. It can read values that span across pages. Whole documents go in at once, not page by page.</p>



<p class="wp-block-paragraph">Two inference modes ship with the package. Local inference runs through HuggingFace. Remote inference runs through a vLLM server, which Datalab recommends for production.</p>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">The code is Apache 2.0. The weights use a modified OpenRAIL-M license.</a></p>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/t0x8a0r">lift</a> enters a small but growing field of open extraction models. Some are purpose-built, like the NuExtract family. Others are general vision-language models pressed into extraction, like Qwen3.5-9B. It pairs a vision-language base with schema-constrained decoding and trained abstention. On Datalab&#8217;s benchmark, it leads that open group on field accuracy.</p>



<h2 id="h-schema-constrained-decoding-the-core-mechanism" class="wp-block-heading"><strong>Schema-Constrained Decoding: The Core Mechanism</strong></h2>



<p class="wp-block-paragraph">The main design choice is schema-constrained decoding. lift decodes its output directly against your schema. The result is always valid JSON of the correct shape.</p>



<p class="wp-block-paragraph">Here is what happens under the hood. <a href="https://pxllnk.co/t0x8a0r">lift</a> first turns your JSON Schema into a Pydantic model. It then normalizes that into a strict JSON Schema. The schema is passed to the vLLM server as a response_format constraint.</p>



<p class="wp-block-paragraph">During generation, the server compiles the schema into a grammar. At each step, the model assigns a probability to every possible next token. The grammar defines which tokens are valid continuations. Tokens that would break the schema are masked out. The model can only sample from what remains.</p>



<p class="wp-block-paragraph">This is why the output is always valid JSON of the right shape. The structure is enforced token by token, not checked afterward.</p>



<p class="wp-block-paragraph">There is a sharp limit to this guarantee. Constrained decoding governs structure and types, not meaning. A field typed as number will hold a number. Whether it holds the <em>correct</em> number is a separate question. The model can emit a valid value that is simply wrong. Validity is not correctness.</p>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">lift</a> also widens every field to allow null. Each scalar leaf in the compiled schema accepts its type or null. So the model can abstain on any field without breaking the structure. Abstention is both trained behavior and a property of the constraint.</p>



<p class="wp-block-paragraph">You write standard JSON Schema. Supported types include string, number, integer, boolean, arrays of those, arrays of objects, and nested objects. A field description guides the model when a name is ambiguous.</p>



<p class="wp-block-paragraph">This is also where a quiet failure mode lives. Some constructs cannot be compiled: enum, anyOf/oneOf, $ref, and additionalProperties. When <a href="https://pxllnk.co/t0x8a0r">lift</a> cannot compile your schema, it does not stop. It logs a warning and generates without the constraint. The structural guarantee is gone for that run, with no hard error. Output may then fail to match your schema at all.</p>



<p class="wp-block-paragraph">The practical rule is simple. Keep schemas inside the supported subset. Validate the returned JSON against your schema downstream. Do not assume valid output just because the call returned.</p>



<p class="wp-block-paragraph"><strong>Here is a simple invoice schema:</strong></p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">{
  "type": "object",
  "properties": {
    "invoice_number": {"type": "string", "description": "Invoice identifier"},
    "total": {"type": "number", "description": "Total amount due"},
    "line_items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "description": {"type": "string"},
          "amount": {"type": "number"}
        }
      }
    }
  },
  "required": ["invoice_number", "total"]
}</code></pre></div></div>



<h2 id="h-abstention-by-default" class="wp-block-heading"><strong>Abstention by Default</strong></h2>



<p class="wp-block-paragraph">Real extraction is hard for a non-obvious reason. Beyond reading fields that exist, the real challenge is not inventing fields that are absent.</p>



<p class="wp-block-paragraph">A model that hallucinates a tax ID is worse than one returning nothing. The error is silent and hard to catch downstream. lift is trained to leave genuinely missing fields null.</p>



<p class="wp-block-paragraph">Mark a field required only when it must appear. Fields absent from a document come back null. This gives you an extractor that can report a value is not present.</p>



<h2 id="h-benchmark" class="wp-block-heading"><strong>Benchmark</strong></h2>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/euw154">Datalab</a> evaluated <a href="https://pxllnk.co/t0x8a0r">lift </a>on a 225-document extraction benchmark. Documents ran 6 to 64 pages each, with roughly 11,000 scored fields. Adversarial cases were planted throughout the set.</p>



<p class="wp-block-paragraph">Those cases include cross-page values and exhaustive lists. They also include fields that must be left null and near-miss distractors. Multi-source aggregation was tested as well.</p>



<p class="wp-block-paragraph">Every model received the same rendered page images. Each extracted every document in a single pass. Scoring was a deterministic exact-match against ground truth, with numeric tolerance and normalized strings.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th><strong>Model</strong></th><th><strong>Size</strong></th><th><strong>Field accuracy</strong></th><th><strong>Full-document accuracy</strong></th><th><strong>Median latency*</strong></th><th><strong>Features</strong></th></tr></thead><tbody><tr><td>Datalab API</td><td>—</td><td>95.9%</td><td>44.4%</td><td>30.8s</td><td>Citations + Verification</td></tr><tr><td>Gemini Flash 3.5</td><td>—</td><td>91.3%</td><td>40.0%</td><td>28.1s</td><td></td></tr><tr><td><strong>lift</strong></td><td><strong>9B</strong></td><td><strong>90.2%</strong></td><td>20.9%</td><td><strong>9.5s</strong></td><td></td></tr><tr><td>Azure Content Understanding</td><td>—</td><td>83.4%</td><td>22.2%</td><td>73.7s</td><td>Citations</td></tr><tr><td>NuExtract3</td><td>4B</td><td>81.5%</td><td>8.4%</td><td>8.3s</td><td></td></tr><tr><td>Qwen3.5-9B</td><td>9B</td><td>76.32%</td><td>24.0%</td><td>16.8s</td><td></td></tr></tbody></table></figure>



<p class="wp-block-paragraph">* <em>Per document, 8 concurrent requests. Local models (<a href="https://pxllnk.co/t0x8a0r">lift</a>, Qwen3.5-9B, NuExtract3) were served with vLLM on a single GPU. Gemini, Datalab, and Azure ran via API. Latency varies with hardware and load; treat it as relative.</em></p>



<p class="wp-block-paragraph">Two details matter here. <strong>Field accuracy</strong> is the fraction of individual fields extracted correctly. <strong>Full-document accuracy</strong> is the fraction of documents where every field is correct.</p>



<p class="wp-block-paragraph">On field accuracy, lift leads the self-hostable models. It sits ahead of NuExtract3 and the Qwen3.5-9B base.&nbsp; It is also the fastest of the accurate models in the table.</p>



<p class="wp-block-paragraph">At 9.5s median, lift is roughly 3x faster than Gemini Flash 3.5. It stays within about a point of that model&#8217;s field accuracy. Full-document accuracy is a harder metric: every field must be correct. Here <a href="https://pxllnk.co/t0x8a0r">lift</a> scores 20.9%, ahead of only NuExtract3. The hosted APIs lead, at 44.4% and 40.0%.</p>



<p class="wp-block-paragraph">A note on reading these numbers. This is Datalab&#8217;s own benchmark, so treat it as a vendor result. Its adversarial design rewards models tuned to abstain, which <a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">lift</a> is. Full-document accuracy is low for every model, topping out at 44.4%. That reflects how hard single-pass extraction is on long documents. The numbers are also a snapshot; models change.</p>



<p class="wp-block-paragraph">This is the reality of single-pass, single-model extraction on hard documents. It tells you where <a href="https://pxllnk.co/t0x8a0r">lift</a> fits. It is excellent for field-level extraction that feeds a human-in-the-loop review or aggregate analytics. It is not yet a drop-in for zero-touch, every-field-must-be-perfect automation. For that last mile, <a href="https://pxllnk.co/euw154">Datalab&#8217;s</a> hosted API adds per-field verification, citations, and confidence scores on the same approach.</p>



<h2 id="h-a-practitioner-workflow-from-schema-to-reviewed-data" class="wp-block-heading"><strong>A Practitioner Workflow: From Schema to Reviewed Data</strong></h2>



<p class="wp-block-paragraph">Three use cases show the shape of the work. <strong>Invoice processing</strong>: define invoice_number, total, and line_items, and a missing tax_id returns null. <strong>Contract review</strong>: a two-page agreement carries a value across pages, which single-pass extraction stitches together. <strong>Document pipelines</strong>: an accounts-payable queue trusts that absent due dates return null, avoiding silent errors.</p>



<p class="wp-block-paragraph">Here is one of them as an end-to-end workflow. The goal is a clean, reviewed dataset, not raw model output.</p>



<p class="wp-block-paragraph"><strong>1. Define the schema.</strong> Add a description to any field whose name is not obvious. Mark only truly mandatory fields as required.</p>



<p class="wp-block-paragraph"><strong>2. Run extraction.</strong> Pass the schema and the file to lift. Use a dict, a file path, or a saved schema name.</p>



<p class="wp-block-paragraph"><strong>3. Branch on the result.</strong> A failed call or a null extraction goes to review. A missing required value also goes to review, since null is abstention, not an error.</p>



<p class="wp-block-paragraph"><strong>4. Validate before you trust.</strong> Check the returned JSON against your schema. This catches the silent fallback when a schema could not be compiled.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">from lift import extract

schema = {
    "type": "object",
    "properties": {
        "invoice_number": {"type": "string", "description": "Invoice identifier"},
        "total": {"type": "number", "description": "Total amount due"},
        "due_date": {"type": "string", "description": "Payment due date, ISO 8601"},
        "line_items": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "description": {"type": "string"},
                    "amount": {"type": "number"}
                }
            }
        }
    },
    "required": ["invoice_number", "total"]
}

result = extract("invoice.pdf", schema)

if result.error or result.extraction is None:
    queue_for_review("invoice.pdf", reason="extraction_failed")
else:
    data = result.extraction
    # A required field can still be null. That is abstention, not a crash.
    if data.get("total") is None:
        queue_for_review("invoice.pdf", reason="missing_total")
    else:
        save(data)</code></pre></div></div>



<p class="wp-block-paragraph">A few schema-design tips that pay off in practice:</p>



<ul class="wp-block-list">
<li>Write a description for ambiguous fields; it is your main lever on accuracy.</li>



<li>Keep schemas inside the supported subset, and validate output downstream.</li>



<li>Prefer flat, shallow schemas; deep nesting is harder to extract reliably.</li>



<li>Mark fields required sparingly, so genuine gaps can return null.</li>



<li>Use &#8211;page-range (CLI) or page_range (Python) to limit long PDFs.</li>



<li>Reuse one InferenceManager across calls to amortize model load.</li>
</ul>



<h2 id="h-self-host-vs-hosted-which-to-use" class="wp-block-heading"><strong>Self-Host vs. Hosted: Which to Use</strong></h2>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">lift</a> ships as open weights, and <a href="https://pxllnk.co/euw154">Datalab</a> runs a hosted API on the same approach. The choice is about constraints, not prestige.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th><strong>Choose</strong></th><th><strong>When</strong></th></tr></thead><tbody><tr><td><strong>Self-hosted lift</strong> (open weights)</td><td>Data residency or on-prem rules apply; you need cost control at high volume; you want latency control on your own GPUs; runs must work offline.</td></tr><tr><td><strong>Hosted Datalab API</strong></td><td>You need per-field verification, citations, and confidence scores; you want the highest accuracy; you would rather not manage infrastructure; volume is low or bursty.</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">One caveat for self-hosting. Commercial use needs a license under the modified OpenRAIL-M terms. It is free for research, personal use, and startups under $5M in funding or revenue, and not for use in competition with Datalab&#8217;s API.</p>



<h2 id="h-getting-started" class="wp-block-heading"><strong>Getting Started</strong></h2>



<p class="wp-block-paragraph">The fastest path is the CLI. lift-pdf requires Python 3.12 or newer.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">pip install lift-pdf

# Serve the model with vLLM (recommended)
lift_vllm

# Extract against a schema
lift_extract input.pdf ./output --schema schema.json</code></pre></div></div>



<p class="wp-block-paragraph">Each file produces two outputs. &lt;filename&gt;.json holds the extraction matching your schema. &lt;filename&gt;_metadata.json holds page count, token count, and error info for debugging.</p>



<p class="wp-block-paragraph">The Python API is equally small:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">from lift import extract

# schema: a dict, a path, an inline JSON string, or a library name
result = extract("document.pdf", "schema.json")
if result.extraction is not None:
    data = result.extraction  # dict matching the schema</code></pre></div></div>



<p class="wp-block-paragraph">Check result.extraction for the dict matching your schema. A null extraction signals a failure you can inspect. The HuggingFace backend uses &#8211;method hf and needs pip install lift-pdf[hf].</p>



<p class="wp-block-paragraph"><strong>Schema Studio</strong> ships as a Streamlit app. It lets you build, save, and test schemas against your own documents. Install it with pip install lift-pdf[app], then run lift_app.</p>



<p class="wp-block-paragraph">For production, lift_vllm launches a Docker container with batch size scaled to your GPU. Supported GPUs are: h100, a100-80, a100/a100-40, l40s, a10, l4, 4090, 3090, t4.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- lift interactive demo — paste into a WordPress "Custom HTML" block -->
<iframe
  title="lift schema extraction demo"
  style="width:100%;border:0;display:block;border-radius:10px;overflow:hidden"
  height="780"
  loading="lazy"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;lift schema extraction demo&lt;/title&gt;
&lt;style&gt;
  :root{
    --bg:#ffffff; --ink:#0a0a0a; --soft:#52525b; --line:#e4e4e7;
    --panel:#fafafa; --chip:#f4f4f5; --hi:#000000; --hiink:#ffffff;
    --null:#a1a1aa; --mono:'SFMono-Regular',ui-monospace,'JetBrains Mono',Menlo,Consolas,monospace;
    --sans:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Helvetica,Arial,sans-serif;
  }
  *{box-sizing:border-box}
  body{margin:0;background:var(--bg);color:var(--ink);font-family:var(--sans);
    -webkit-font-smoothing:antialiased;line-height:1.45;font-size:14px}
  .wrap{max-width:980px;margin:0 auto;padding:18px 18px 8px}
  .head{border:1px solid var(--line);border-bottom:2px solid var(--ink);
    border-radius:10px 10px 0 0;padding:14px 16px;background:var(--bg)}
  .kicker{font-family:var(--mono);font-size:11px;letter-spacing:.14em;
    text-transform:uppercase;color:var(--soft)}
  .title{font-size:19px;font-weight:700;margin:4px 0 2px;letter-spacing:-.01em}
  .sub{font-size:12.5px;color:var(--soft)}
  .grid{display:grid;grid-template-columns:1fr 1fr;gap:0;
    border:1px solid var(--line);border-top:none}
  .col{padding:14px 16px}
  .col + .col{border-left:1px solid var(--line)}
  .step{font-family:var(--mono);font-size:10.5px;letter-spacing:.12em;
    text-transform:uppercase;color:var(--soft);margin:0 0 9px;display:flex;
    align-items:center;gap:8px}
  .step .n{display:inline-flex;width:18px;height:18px;align-items:center;
    justify-content:center;border:1px solid var(--ink);border-radius:50%;
    font-size:10px;color:var(--ink)}
  .tabs{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:12px}
  .tab{font-family:var(--mono);font-size:11.5px;border:1px solid var(--line);
    background:var(--bg);color:var(--ink);padding:6px 10px;border-radius:6px;
    cursor:pointer;transition:.12s}
  .tab:hover{border-color:var(--ink)}
  .tab[aria-selected=&quot;true&quot;]{background:var(--hi);color:var(--hiink);border-color:var(--hi)}
  .doc{border:1px solid var(--line);background:var(--panel);border-radius:8px;
    padding:12px;font-size:11.5px;height:250px;overflow:auto}
  .doc .pg{font-family:var(--mono);font-size:9.5px;letter-spacing:.1em;
    color:var(--soft);text-transform:uppercase;border-bottom:1px dashed var(--line);
    padding-bottom:4px;margin:0 0 8px}
  .doc h4{margin:0 0 6px;font-size:13px}
  .doc table{width:100%;border-collapse:collapse;margin:6px 0;font-size:11px}
  .doc td{border-bottom:1px solid var(--line);padding:3px 4px}
  .doc .mut{color:var(--soft)}
  .doc .row{display:flex;justify-content:space-between;gap:8px;padding:2px 0}
  .fields{display:flex;flex-direction:column;gap:6px;height:208px;overflow:auto;
    padding-right:2px}
  .f{display:flex;align-items:flex-start;gap:9px;border:1px solid var(--line);
    border-radius:7px;padding:8px 10px;cursor:pointer;background:var(--bg);
    transition:.12s}
  .f:hover{border-color:var(--ink)}
  .f input{margin:2px 0 0;accent-color:#000;width:14px;height:14px;cursor:pointer}
  .f .meta{flex:1;min-width:0}
  .f .name{font-family:var(--mono);font-size:12px}
  .f .desc{font-size:11px;color:var(--soft);margin-top:1px}
  .f .tag{font-family:var(--mono);font-size:9px;letter-spacing:.08em;
    text-transform:uppercase;border:1px solid var(--line);border-radius:4px;
    padding:1px 5px;color:var(--soft);white-space:nowrap;margin-top:3px;
    display:inline-block}
  .f .tag.absent{border-color:var(--ink);color:var(--ink)}
  .req{font-family:var(--mono);font-size:9px;color:var(--soft)}
  .ctrls{display:flex;align-items:center;gap:10px;margin-top:12px}
  .btn{font-family:var(--mono);font-size:12px;letter-spacing:.03em;
    background:var(--hi);color:var(--hiink);border:1px solid var(--hi);
    padding:9px 16px;border-radius:7px;cursor:pointer;transition:.12s}
  .btn:hover{opacity:.85}
  .btn.ghost{background:var(--bg);color:var(--ink);border-color:var(--line)}
  .btn.ghost:hover{border-color:var(--ink)}
  .out{border:1px solid var(--line);border-top:none;border-radius:0 0 10px 10px;
    padding:14px 16px;background:var(--bg)}
  .outhead{display:flex;align-items:center;justify-content:space-between;
    gap:10px;margin-bottom:8px;flex-wrap:wrap}
  .outhead .lbl{font-family:var(--mono);font-size:10.5px;letter-spacing:.12em;
    text-transform:uppercase;color:var(--soft)}
  .stats{display:flex;gap:14px;font-family:var(--mono);font-size:11px;color:var(--soft)}
  .stats b{color:var(--ink);font-weight:600}
  pre{margin:0;background:var(--panel);border:1px solid var(--line);
    border-radius:8px;padding:12px 14px;font-family:var(--mono);font-size:12px;
    line-height:1.6;overflow:auto;max-height:250px}
  .k{color:var(--ink)}
  .s{color:#27272a}
  .n{color:#3f3f46}
  .nul{color:var(--null);font-style:italic}
  .badge{font-family:var(--mono);font-size:9px;letter-spacing:.06em;
    text-transform:uppercase;border-radius:4px;padding:1px 6px;margin-left:8px}
  .badge.found{background:var(--ink);color:#fff}
  .badge.absn{background:var(--bg);color:var(--ink);border:1px solid var(--ink)}
  .note{font-size:11.5px;color:var(--soft);margin-top:10px;line-height:1.5}
  .note code{font-family:var(--mono);background:var(--chip);padding:1px 5px;
    border-radius:4px;color:var(--ink)}
  .foot{display:flex;align-items:center;justify-content:space-between;
    gap:10px;flex-wrap:wrap;padding:12px 2px 4px;margin-top:6px;
    border-top:1px solid var(--line);color:var(--soft);font-size:11px}
  .foot .brand{font-family:var(--mono);font-size:11px;color:var(--ink);font-weight:600}
  .foot .brand span{color:var(--soft);font-weight:400}
  a{color:var(--ink)}
  @media (max-width:640px){
    .grid{grid-template-columns:1fr}
    .col + .col{border-left:none;border-top:1px solid var(--line)}
    .doc{height:220px}.fields{height:auto;max-height:260px}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot;&gt;
  &lt;div class=&quot;head&quot;&gt;
    &lt;div class=&quot;kicker&quot;&gt;Interactive demo · illustrative&lt;/div&gt;
    &lt;div class=&quot;title&quot;&gt;lift — schema &amp;rarr; structured JSON&lt;/div&gt;
    &lt;div class=&quot;sub&quot;&gt;Pick a document, choose the schema fields, and extract. lift returns valid JSON of your schema's shape, and leaves genuinely absent fields &lt;code style=&quot;font-family:var(--mono)&quot;&gt;null&lt;/code&gt;.&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;grid&quot;&gt;
    &lt;div class=&quot;col&quot;&gt;
      &lt;div class=&quot;step&quot;&gt;&lt;span class=&quot;n&quot;&gt;1&lt;/span&gt; Document&lt;/div&gt;
      &lt;div class=&quot;tabs&quot; id=&quot;tabs&quot; role=&quot;tablist&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;doc&quot; id=&quot;doc&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;col&quot;&gt;
      &lt;div class=&quot;step&quot;&gt;&lt;span class=&quot;n&quot;&gt;2&lt;/span&gt; Schema fields&lt;/div&gt;
      &lt;div class=&quot;fields&quot; id=&quot;fields&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;ctrls&quot;&gt;
        &lt;button class=&quot;btn&quot; id=&quot;run&quot;&gt;Extract&lt;/button&gt;
        &lt;button class=&quot;btn ghost&quot; id=&quot;all&quot;&gt;Toggle all&lt;/button&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;out&quot;&gt;
    &lt;div class=&quot;outhead&quot;&gt;
      &lt;span class=&quot;lbl&quot;&gt;Output · &amp;lt;filename&amp;gt;.json&lt;/span&gt;
      &lt;div class=&quot;stats&quot; id=&quot;stats&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;pre id=&quot;json&quot;&gt;&lt;span class=&quot;nul&quot;&gt;// Select fields and press Extract&lt;/span&gt;&lt;/pre&gt;
    &lt;div class=&quot;note&quot; id=&quot;note&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;foot&quot;&gt;
    &lt;div class=&quot;brand&quot;&gt;Marktechpost &lt;span&gt;· interactive embed&lt;/span&gt;&lt;/div&gt;
    &lt;div&gt;Illustrative demo of lift's schema-constrained extraction. Run the real 9B model via &lt;code style=&quot;font-family:var(--mono)&quot;&gt;pip install lift-pdf&lt;/code&gt;.&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
/* ---- Illustrative dataset: fields a real lift schema might define.
   &quot;absent&quot; fields are not present in the document, so lift returns null. ---- */
const DOCS = {
  invoice: {
    label: &quot;Invoice (1 page)&quot;,
    render: `&lt;div class=&quot;pg&quot;&gt;Page 1 / 1 — RPC Wireless&lt;/div&gt;
      &lt;h4&gt;INVOICE  #INV-20458&lt;/h4&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Bill to&lt;/span&gt;&lt;span&gt;Acme Logistics LLC&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Issue date&lt;/span&gt;&lt;span&gt;2024-09-13&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Due date&lt;/span&gt;&lt;span&gt;2024-10-13&lt;/span&gt;&lt;/div&gt;
      &lt;table&gt;
        &lt;tr&gt;&lt;td&gt;Mounting hardware&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;$120.00&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;Antenna unit ×2&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;$410.00&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;Install labor&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;$310.00&lt;/td&gt;&lt;/tr&gt;
      &lt;/table&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Total due&lt;/span&gt;&lt;span&gt;&lt;b&gt;$840.00&lt;/b&gt;&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;mut&quot; style=&quot;margin-top:8px&quot;&gt;No tax registration number printed on this invoice.&lt;/div&gt;`,
    fields: [
      {name:&quot;invoice_number&quot;, desc:&quot;Invoice identifier&quot;, type:&quot;string&quot;, req:true, val:&quot;INV-20458&quot;},
      {name:&quot;total&quot;, desc:&quot;Total amount due&quot;, type:&quot;number&quot;, req:true, val:840.00},
      {name:&quot;due_date&quot;, desc:&quot;Payment due date&quot;, type:&quot;string&quot;, req:false, val:&quot;2024-10-13&quot;},
      {name:&quot;tax_id&quot;, desc:&quot;Seller tax / VAT number&quot;, type:&quot;string&quot;, req:false, absent:true},
      {name:&quot;line_items&quot;, desc:&quot;Array of {description, amount}&quot;, type:&quot;array&quot;, req:false, val:[
        {description:&quot;Mounting hardware&quot;, amount:120.00},
        {description:&quot;Antenna unit ×2&quot;, amount:410.00},
        {description:&quot;Install labor&quot;, amount:310.00}
      ]}
    ]
  },
  contract: {
    label: &quot;Service agreement (2 pages)&quot;,
    render: `&lt;div class=&quot;pg&quot;&gt;Page 1 / 2 — Master Services Agreement&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Party A&lt;/span&gt;&lt;span&gt;Northwind Systems Inc.&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Party B&lt;/span&gt;&lt;span&gt;Brightline Data Co.&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Effective&lt;/span&gt;&lt;span&gt;2026-01-05&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;mut&quot; style=&quot;margin:8px 0&quot;&gt;3. TERM. The total contract value is defined in
        Schedule A, payable across the term set out below…&lt;/div&gt;
      &lt;div class=&quot;pg&quot; style=&quot;margin-top:10px&quot;&gt;Page 2 / 2 — Schedule A&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Schedule A value&lt;/span&gt;&lt;span&gt;$48,000 over 12 months&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Termination notice&lt;/span&gt;&lt;span&gt;30 days&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Governing law&lt;/span&gt;&lt;span&gt;State of Delaware&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;mut&quot; style=&quot;margin-top:8px&quot;&gt;No auto-renewal clause appears in this agreement.&lt;/div&gt;`,
    fields: [
      {name:&quot;party_a&quot;, desc:&quot;First contracting party&quot;, type:&quot;string&quot;, req:true, val:&quot;Northwind Systems Inc.&quot;},
      {name:&quot;party_b&quot;, desc:&quot;Second contracting party&quot;, type:&quot;string&quot;, req:true, val:&quot;Brightline Data Co.&quot;},
      {name:&quot;effective_date&quot;, desc:&quot;Agreement effective date&quot;, type:&quot;string&quot;, req:false, val:&quot;2026-01-05&quot;},
      {name:&quot;total_value&quot;, desc:&quot;Contract value (spans pages 1→2)&quot;, type:&quot;string&quot;, req:false, span:true, val:&quot;$48,000 over 12 months&quot;},
      {name:&quot;termination_notice_days&quot;, desc:&quot;Required notice period&quot;, type:&quot;integer&quot;, req:false, val:30},
      {name:&quot;governing_law&quot;, desc:&quot;Governing jurisdiction&quot;, type:&quot;string&quot;, req:false, val:&quot;State of Delaware&quot;},
      {name:&quot;auto_renewal&quot;, desc:&quot;Whether the contract auto-renews&quot;, type:&quot;boolean&quot;, req:false, absent:true}
    ]
  },
  lab: {
    label: &quot;Lab report (3 pages)&quot;,
    render: `&lt;div class=&quot;pg&quot;&gt;Page 1 / 3 — Patient summary&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Patient ID&lt;/span&gt;&lt;span&gt;PT-77310&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;mut&quot;&gt;Collected&lt;/span&gt;&lt;span&gt;2026-03-02&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;pg&quot; style=&quot;margin-top:10px&quot;&gt;Page 2 / 3 — Panel results&lt;/div&gt;
      &lt;table&gt;
        &lt;tr&gt;&lt;td&gt;Hemoglobin&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;13.8 g/dL&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;White cell count&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;6.1 ×10⁹/L&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;Platelets&lt;/td&gt;&lt;td style=&quot;text-align:right&quot;&gt;240 ×10⁹/L&lt;/td&gt;&lt;/tr&gt;
      &lt;/table&gt;
      &lt;div class=&quot;pg&quot; style=&quot;margin-top:10px&quot;&gt;Page 3 / 3 — Notes&lt;/div&gt;
      &lt;div class=&quot;mut&quot;&gt;Reviewing physician sign-off pending; not yet recorded.&lt;/div&gt;`,
    fields: [
      {name:&quot;patient_id&quot;, desc:&quot;Patient identifier&quot;, type:&quot;string&quot;, req:true, val:&quot;PT-77310&quot;},
      {name:&quot;collection_date&quot;, desc:&quot;Sample collection date&quot;, type:&quot;string&quot;, req:false, val:&quot;2026-03-02&quot;},
      {name:&quot;results&quot;, desc:&quot;Array of {analyte, value} across pages&quot;, type:&quot;array&quot;, req:false, val:[
        {analyte:&quot;Hemoglobin&quot;, value:&quot;13.8 g/dL&quot;},
        {analyte:&quot;White cell count&quot;, value:&quot;6.1 ×10⁹/L&quot;},
        {analyte:&quot;Platelets&quot;, value:&quot;240 ×10⁹/L&quot;}
      ]},
      {name:&quot;reviewing_physician&quot;, desc:&quot;Signing physician name&quot;, type:&quot;string&quot;, req:false, absent:true}
    ]
  }
};

let current = &quot;invoice&quot;;
const selected = {}; // doc -&gt; Set(fieldName)

function ensureSel(doc){
  if(!selected[doc]){
    selected[doc] = new Set(DOCS[doc].fields.map(f=&gt;f.name));
  }
  return selected[doc];
}

const tabsEl = document.getElementById('tabs');
const docEl  = document.getElementById('doc');
const fieldsEl = document.getElementById('fields');
const jsonEl = document.getElementById('json');
const statsEl = document.getElementById('stats');
const noteEl = document.getElementById('note');

function buildTabs(){
  tabsEl.innerHTML = &quot;&quot;;
  Object.keys(DOCS).forEach(k=&gt;{
    const b=document.createElement('button');
    b.className='tab'; b.textContent=DOCS[k].label; b.setAttribute('role','tab');
    b.setAttribute('aria-selected', k===current ? 'true':'false');
    b.onclick=()=&gt;{ current=k; render(); };
    tabsEl.appendChild(b);
  });
}

function buildFields(){
  const sel = ensureSel(current);
  fieldsEl.innerHTML=&quot;&quot;;
  DOCS[current].fields.forEach(f=&gt;{
    const row=document.createElement('label');
    row.className='f';
    const cb=document.createElement('input');
    cb.type='checkbox'; cb.checked=sel.has(f.name);
    cb.onchange=()=&gt;{ cb.checked ? sel.add(f.name) : sel.delete(f.name); };
    const meta=document.createElement('div'); meta.className='meta';
    let tag = f.absent ? `&lt;span class=&quot;tag absent&quot;&gt;not in document&lt;/span&gt;`
            : f.span  ? `&lt;span class=&quot;tag&quot;&gt;spans pages&lt;/span&gt;` : &quot;&quot;;
    meta.innerHTML = `&lt;div class=&quot;name&quot;&gt;${f.name}&lt;span class=&quot;req&quot;&gt; : ${f.type}${f.req?' · required':''}&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;desc&quot;&gt;${f.desc}&lt;/div&gt;${tag}`;
    row.appendChild(cb); row.appendChild(meta);
    fieldsEl.appendChild(row);
  });
}

function jval(v, indent){
  const pad = '  '.repeat(indent);
  const pad2 = '  '.repeat(indent+1);
  if(v===null) return `&lt;span class=&quot;nul&quot;&gt;null&lt;/span&gt;`;
  if(typeof v==='number') return `&lt;span class=&quot;n&quot;&gt;${v}&lt;/span&gt;`;
  if(typeof v==='boolean') return `&lt;span class=&quot;n&quot;&gt;${v}&lt;/span&gt;`;
  if(typeof v==='string') return `&lt;span class=&quot;s&quot;&gt;&quot;${v}&quot;&lt;/span&gt;`;
  if(Array.isArray(v)){
    if(v.length===0) return '[]';
    const items=v.map(it=&gt;pad2+jval(it,indent+1)).join(',\n');
    return `[\n${items}\n${pad}]`;
  }
  // object
  const keys=Object.keys(v);
  const body=keys.map(k=&gt;`${pad2}&lt;span class=&quot;k&quot;&gt;&quot;${k}&quot;&lt;/span&gt;: ${jval(v[k],indent+1)}`).join(',\n');
  return `{\n${body}\n${pad}}`;
}

function extract(){
  const sel = ensureSel(current);
  const fields = DOCS[current].fields.filter(f=&gt;sel.has(f.name));
  if(fields.length===0){
    jsonEl.innerHTML = `&lt;span class=&quot;nul&quot;&gt;// No fields selected — nothing to extract&lt;/span&gt;`;
    statsEl.innerHTML=&quot;&quot;; noteEl.innerHTML=&quot;&quot;; emitSize(); return;
  }
  // Build object honoring schema shape; absent -&gt; null (abstention)
  const obj={}; let found=0, nulls=0, spans=0;
  fields.forEach(f=&gt;{
    if(f.absent){ obj[f.name]=null; nulls++; }
    else { obj[f.name]=f.val; found++; if(f.span) spans++; }
  });
  const lines = Object.keys(obj).map(k=&gt;{
    const f = fields.find(x=&gt;x.name===k);
    const badge = f.absent ? `&lt;span class=&quot;badge absn&quot;&gt;null · abstained&lt;/span&gt;`
                           : `&lt;span class=&quot;badge found&quot;&gt;found&lt;/span&gt;`;
    return `  &lt;span class=&quot;k&quot;&gt;&quot;${k}&quot;&lt;/span&gt;: ${jval(obj[k],1)}${badge}`;
  });
  jsonEl.innerHTML = `{\n${lines.join(',\n')}\n}`;
  statsEl.innerHTML = `&lt;span&gt;&lt;b&gt;${found}&lt;/b&gt; found&lt;/span&gt;&lt;span&gt;&lt;b&gt;${nulls}&lt;/b&gt; null&lt;/span&gt;&lt;span&gt;&lt;b&gt;${fields.length}&lt;/b&gt; fields&lt;/span&gt;`;
  let n = `Output is valid JSON of exactly the shape you selected — that is schema-constrained decoding. `;
  if(nulls&gt;0) n += `Fields not present in the document came back &lt;code&gt;null&lt;/code&gt; instead of a guess (abstention). `;
  if(spans&gt;0) n += `&lt;code&gt;total_value&lt;/code&gt; is resolved from values that span two pages, read in a single pass.`;
  noteEl.innerHTML = n;
  emitSize();
}

function render(){
  buildTabs();
  docEl.innerHTML = DOCS[current].render;
  buildFields();
  jsonEl.innerHTML = `&lt;span class=&quot;nul&quot;&gt;// Press Extract to run the selected schema&lt;/span&gt;`;
  statsEl.innerHTML=&quot;&quot;; noteEl.innerHTML=&quot;&quot;;
  emitSize();
}

document.getElementById('run').onclick = extract;
document.getElementById('all').onclick = ()=&gt;{
  const sel=ensureSel(current);
  const all = DOCS[current].fields;
  const allOn = all.every(f=&gt;sel.has(f.name));
  all.forEach(f=&gt; allOn ? sel.delete(f.name) : sel.add(f.name));
  buildFields(); emitSize();
};

/* auto-resize: post component offsetHeight + 40 to parent */
function emitSize(){
  requestAnimationFrame(()=&gt;{
    const h = (document.querySelector('.wrap').offsetHeight || document.body.offsetHeight) + 40;
    parent.postMessage({type:'lift-demo-height', height:h}, '*');
  });
}
window.addEventListener('load', ()=&gt;{ render(); emitSize(); });
window.addEventListener('resize', emitSize);
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>



<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">lift</a> is Datalab&#8217;s 9B open-weights vision model that extracts schema-matching JSON from PDFs and images.</li>



<li>Schema-constrained decoding guarantees valid structure; trained abstention returns null instead of hallucinating absent fields.</li>



<li>The structural guarantee covers shape, not meaning, so validate output and review low-confidence fields.</li>



<li>It posts the highest field accuracy among self-hostable models tested (90.2%), at 9.5s median per document.</li>



<li>Full-document accuracy is 20.9% — ahead of only NuExtract3 — where the hosted APIs lead.</li>



<li>Code is Apache 2.0; weights are modified OpenRAIL-M (free for research, personal use, and startups under $5M in funding or revenue).</li>
</ul>



<h4 id="h-the-links-to-try-it" class="wp-block-heading"><strong>The links to try it:</strong></h4>



<p class="wp-block-paragraph"><strong><a href="https://pxllnk.co/nmpjxqn" target="_blank" rel="noreferrer noopener">GitHub </a>· <a href="https://pxllnk.co/t0x8a0r" target="_blank" rel="noreferrer noopener">HuggingFace</a> · <a href="https://pxllnk.co/mf4o7kl" target="_blank" rel="noreferrer noopener">Playground</a> · <a href="https://pxllnk.co/euw154" target="_blank" rel="noreferrer noopener">Hosted API &amp; docs</a></strong></p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"><em><sub>Note:Thanks to the Datalab team for the thought leadership/ Resources for this article. Datalab team has supported this content/article for promotion.</sub></em></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/datalab-releases-lift-a-9b-open-weights-vision-model-that-extracts-structured-json-from-pdfs-using-schemas/">Datalab Releases lift: A 9B Open-Weights Vision Model That Extracts Structured JSON From PDFs Using Schemas</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80667</post-id>
	</item>
		<item>
		<title>How to Use NVIDIA Canary-1B-v2 for ASR, Translation, and Automatic SRT Subtitle Export in Python</title>
		<link>https://www.marktechpost.com/2026/06/23/how-to-use-nvidia-canary-1b-v2-for-asr-translation-and-automatic-srt-subtitle-export-in-python/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Tue, 23 Jun 2026 18:31:36 +0000</pubDate>
				<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Audio Language Model]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Tutorials]]></category>
		<category><![CDATA[Voice AI]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80664</guid>

					<description><![CDATA[<p>In this tutorial, we build a multilingual ASR and speech translation pipeline with NVIDIA Canary-1B-v2. We load the model on a GPU-enabled runtime, prepare audio into 16 kHz mono, and run English ASR. We then translate speech into French, German, Spanish, and Italian, and extract word and segment timestamps. We export translated subtitles as an SRT file, test long-form transcription, run batch processing, and benchmark inference speed.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/how-to-use-nvidia-canary-1b-v2-for-asr-translation-and-automatic-srt-subtitle-export-in-python/">How to Use NVIDIA Canary-1B-v2 for ASR, Translation, and Automatic SRT Subtitle Export in Python</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this <a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Voice%20AI/nvidia_canary_1b_v2_asr_translation_tutorial_marktechpost.py" target="_blank" rel="noreferrer noopener">tutorial</a>, we build a speech recognition and translation workflow using<a href="https://huggingface.co/nvidia/canary-1b-v2"> <strong>NVIDIA Canary-1B-v2</strong></a>. We begin by setting up the required audio, NeMo, NumPy, and SciPy dependencies, then load the Canary model on a GPU-enabled runtime for efficient inference. From there, we prepare audio into a clean 16 kHz mono format, perform English ASR, translate speech into multiple languages, generate word and segment timestamps, export translated subtitles as an SRT file, test long-form transcription, run batch processing, and benchmark inference speed. At the end, we have a complete multilingual ASR and speech translation pipeline that we can adapt for real audio files, subtitle generation, and large-scale transcription experiments.</p>



<h3 id="h-installing-nemo-audio-libraries-numpy-and-scipy-dependencies" class="wp-block-heading"><strong>Installing NeMo, Audio Libraries, NumPy, and SciPy Dependencies</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import os, subprocess, sys
SENTINEL = "/content/.canary_setup_done"
if not os.path.exists(SENTINEL):
   def sh(c):
       print("$", c); subprocess.run(c, shell=True, check=False)
   print(">>> PHASE 1: installing dependencies (one-time)...\n")
   sh("apt-get -qq update")
   sh("apt-get -qq install -y libsndfile1 ffmpeg > /dev/null")
   sh('pip install -q "nemo_toolkit[asr]"')
   sh("pip install -q librosa soundfile pydub")
   sh('pip install -q --force-reinstall --no-cache-dir "numpy>=2.2,&lt;2.4" "scipy>=1.15"')
   open(SENTINEL, "w").write("done")
   print("\n<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2705.png" alt="✅" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Setup complete. Restarting the runtime now.")
   print("   When it reconnects, RUN THIS CELL AGAIN to start the tutorial.")
   os.kill(os.getpid(), 9)
</code></pre></div></div>



<p class="wp-block-paragraph">We set up the environment for the NVIDIA Canary-1B-v2 tutorial. We install the required system packages, NeMo ASR toolkit, audio libraries, and compatible NumPy and SciPy versions. We then create a setup marker and restart the runtime so that the updated dependencies load cleanly before running the main tutorial.</p>



<h3 class="wp-block-heading"><strong>Loading NVIDIA Canary-1B-v2 and Checking GPU Availability</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import time, json, gc, math, urllib.request
import torch, numpy as np, soundfile as sf, librosa
print(">>> PHASE 2: running tutorial\n")
print("NumPy:", np.__version__, "| PyTorch:", torch.__version__)
print("CUDA available:", torch.cuda.is_available())
if torch.cuda.is_available():
   print("GPU:", torch.cuda.get_device_name(0),
         f"| VRAM: {torch.cuda.get_device_properties(0).total_memory/1e9:.1f} GB")
else:
   print("<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/26a0.png" alt="⚠" class="wp-smiley" style="height: 1em; max-height: 1em;" />  No GPU — will run on CPU (very slow). "
         "Set Runtime > Change runtime type > GPU.")
DEVICE = "cuda" if torch.cuda.is_available() else "cpu"
LANGS = {
   "bg":"Bulgarian","hr":"Croatian","cs":"Czech","da":"Danish","nl":"Dutch",
   "en":"English","et":"Estonian","fi":"Finnish","fr":"French","de":"German",
   "el":"Greek","hu":"Hungarian","it":"Italian","lv":"Latvian","lt":"Lithuanian",
   "mt":"Maltese","pl":"Polish","pt":"Portuguese","ro":"Romanian","sk":"Slovak",
   "sl":"Slovenian","es":"Spanish","sv":"Swedish","ru":"Russian","uk":"Ukrainian",
}
print(f"\nSupported languages ({len(LANGS)}):", ", ".join(LANGS.keys()))
from nemo.collections.asr.models import ASRModel
print("\nLoading nvidia/canary-1b-v2 ...")
t0 = time.time()
asr_model = ASRModel.from_pretrained(model_name="nvidia/canary-1b-v2").to(DEVICE).eval()
print(f"Model loaded in {time.time()-t0:.1f}s")
</code></pre></div></div>



<p class="wp-block-paragraph">We import the main libraries and check whether CUDA is available for GPU acceleration. We define the supported language dictionary to enable Canary to handle multilingual ASR and translation tasks. We then load the NVIDIA Canary-1B-v2 model from NeMo and move it to the available device for inference.</p>



<h3 class="wp-block-heading"><strong>Preparing 16 kHz Audio and Running English ASR with Translation</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">TARGET_SR = 16000
def prepare_audio(path_or_url, out_path=None):
   if str(path_or_url).startswith(("http://", "https://")):
       local = "/content/_dl_" + os.path.basename(path_or_url.split("?")[0])
       urllib.request.urlretrieve(path_or_url, local)
       path_or_url = local
   audio, _ = librosa.load(path_or_url, sr=TARGET_SR, mono=True)
   if out_path is None:
       base = os.path.splitext(os.path.basename(path_or_url))[0]
       out_path = f"/content/{base}_16k_mono.wav"
   sf.write(out_path, audio, TARGET_SR, subtype="PCM_16")
   dur = len(audio) / TARGET_SR
   print(f"Prepared: {out_path}  ({dur:.1f}s, 16kHz, mono)")
   return out_path, dur
SAMPLE_URL = "https://dldata-public.s3.us-east-2.amazonaws.com/2086-149220-0033.wav"
sample_wav, sample_dur = prepare_audio(SAMPLE_URL)
def transcribe(files, source_lang="en", target_lang="en", timestamps=False, batch_size=1):
   if isinstance(files, str):
       files = [files]
   return asr_model.transcribe(files, source_lang=source_lang, target_lang=target_lang,
                               timestamps=timestamps, batch_size=batch_size)
print("\n=== 1) BASIC ASR (English) ===")
res = transcribe(sample_wav, source_lang="en", target_lang="en")
print("Transcript:", res[0].text)
print("\n=== 2) TRANSLATION (EN audio -> X) ===")
for tgt in ["fr", "de", "es", "it"]:
   out = transcribe(sample_wav, source_lang="en", target_lang=tgt)
   print(f"  EN -> {LANGS[tgt]:&lt;10} ({tgt}): {out[0].text}")
</code></pre></div></div>



<p class="wp-block-paragraph">We create a reusable audio preparation function that downloads audio when needed and converts it into 16 kHz mono WAV format. We load the sample audio file and define a helper function for transcription and translation. We then run basic English ASR and translate the same English speech into French, German, Spanish, and Italian.</p>



<h3 class="wp-block-heading"><strong>Generating Word and Segment Timestamps and Exporting SRT Subtitles</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n=== 3) TIMESTAMPS (ASR) ===")
ts_out = transcribe(sample_wav, source_lang="en", target_lang="en", timestamps=True)
word_ts = ts_out[0].timestamp.get("word", [])
seg_ts  = ts_out[0].timestamp.get("segment", [])
print("Segments:")
for s in seg_ts:
   print(f"  [{s['start']:6.2f}s - {s['end']:6.2f}s]  {s['segment']}")
print("First 10 words:")
for w in word_ts[:10]:
   print(f"  [{w['start']:6.2f}s - {w['end']:6.2f}s]  {w['word']}")
def _srt_time(t):
   h=int(t//3600); m=int((t%3600)//60); s=int(t%60); ms=int(round((t-int(t))*1000))
   return f"{h:02d}:{m:02d}:{s:02d},{ms:03d}"
def segments_to_srt(segments, out_path="/content/output.srt"):
   lines=[]
   for i, seg in enumerate(segments, 1):
       lines += [str(i), f"{_srt_time(seg['start'])} --> {_srt_time(seg['end'])}",
                 seg["segment"].strip(), ""]
   open(out_path, "w", encoding="utf-8").write("\n".join(lines))
   print(f"Saved SRT: {out_path}")
   return out_path
print("\n=== 4) SRT EXPORT (translated French subtitles) ===")
fr_ts = transcribe(sample_wav, source_lang="en", target_lang="fr", timestamps=True)
segments_to_srt(fr_ts[0].timestamp["segment"], "/content/subtitles_fr.srt")
print(open("/content/subtitles_fr.srt").read())
</code></pre></div></div>



<p class="wp-block-paragraph">We enable timestamped transcription to extract both segment-level and word-level timing information. We print the transcript segments and the first few word timestamps to inspect how the model aligns text with audio. We also convert translated French segments into an SRT subtitle file and display the generated subtitles.</p>



<h3 class="wp-block-heading"><strong>Running Long-Form Transcription, Batch Processing, and Speed Benchmark</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n=== 5) LONG-FORM (sample tiled x6) ===")
long_audio, _ = librosa.load(sample_wav, sr=TARGET_SR, mono=True)
long_audio = np.tile(long_audio, 6)
sf.write("/content/long.wav", long_audio, TARGET_SR, subtype="PCM_16")
print(f"Long clip duration: {len(long_audio)/TARGET_SR:.1f}s")
long_out = transcribe("/content/long.wav", source_lang="en", target_lang="en", batch_size=1)
print("Long transcript (first 300 chars):", long_out[0].text[:300], "...")
print("\n=== 6) BATCH ===")
for name in ["clip_a", "clip_b"]:
   sf.write(f"/content/{name}.wav",
            librosa.load(sample_wav, sr=TARGET_SR, mono=True)[0], TARGET_SR, subtype="PCM_16")
batch = transcribe(["/content/clip_a.wav", "/content/clip_b.wav"],
                  source_lang="en", target_lang="en", batch_size=2)
for i, b in enumerate(batch):
   print(f"  file {i}: {b.text}")
print("\n=== 7) BENCHMARK ===")
t0 = time.time(); _ = transcribe(sample_wav, source_lang="en", target_lang="en")
elapsed = time.time()-t0
print(f"Audio: {sample_dur:.2f}s | Compute: {elapsed:.2f}s | RTFx ≈ {sample_dur/elapsed:.1f}x")
print("\n<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2705.png" alt="✅" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Done. Change source_lang/target_lang from the LANGS dict to try other languages.")
</code></pre></div></div>



<p class="wp-block-paragraph">We test long-form transcription by repeating the sample audio several times and passing the longer clip through the model. We also create two duplicate audio clips to demonstrate batch transcription with a batch size of two. Also, we benchmark the model by comparing audio duration with compute time and report the real-time factor speed.</p>



<h3 class="wp-block-heading"><strong>Conclusion</strong></h3>



<p class="wp-block-paragraph">In conclusion, we completed a practical end-to-end workflow for using NVIDIA Canary-1B-v2 as a multilingual ASR and speech translation system. We processed raw audio, generated accurate transcripts, translated speech into different target languages, extracted timestamps, created subtitle files, handled longer audio clips, and compared runtime performance through a simple benchmark. We now have a reusable Colab-ready pipeline that we can extend further with custom uploads, more languages, larger batches, and production-style audio processing.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Voice%20AI/nvidia_canary_1b_v2_asr_translation_tutorial_marktechpost.py" target="_blank" rel="noreferrer noopener">Full Codes with Notebook</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/how-to-use-nvidia-canary-1b-v2-for-asr-translation-and-automatic-srt-subtitle-export-in-python/">How to Use NVIDIA Canary-1B-v2 for ASR, Translation, and Automatic SRT Subtitle Export in Python</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		<enclosure url="https://dldata-public.s3.us-east-2.amazonaws.com/2086-149220-0033.wav" length="237964" type="audio/wav" />

		<post-id xmlns="com-wordpress:feed-additions:1">80664</post-id>
	</item>
		<item>
		<title>Prime Intellect Releases prime-rl 0.6.0 to Train Trillion-Parameter MoE Models on Agentic RL Workloads</title>
		<link>https://www.marktechpost.com/2026/06/23/prime-intellect-releases-prime-rl-0-6-0-to-train-trillion-parameter-moe-models-on-agentic-rl-workloads/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Tue, 23 Jun 2026 07:20:44 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80661</guid>

					<description><![CDATA[<p>Prime Intellect has released prime-rl 0.6.0, an open framework for asynchronous reinforcement learning on trillion-parameter Mixture-of-Experts models. It trained GLM-5 on SWE tasks at up to 131k sequence length, with sub-5-minute step times and 256 rollouts, on 28 H200 nodes. This breakdown covers the inference and training optimizations behind those numbers — FP8 inference, Wide Expert Parallelism, prefill/decode disaggregation, router replay, and 3-D parallelism (FSDP, EP, CP).</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/prime-intellect-releases-prime-rl-0-6-0-to-train-trillion-parameter-moe-models-on-agentic-rl-workloads/">Prime Intellect Releases prime-rl 0.6.0 to Train Trillion-Parameter MoE Models on Agentic RL Workloads</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Prime Intellect has released <a href="https://www.primeintellect.ai/blog/rl-at-1t-scale" target="_blank" rel="noreferrer noopener">prime-rl version 0.6.0</a>. The framework targets reinforcement learning on trillion-parameter Mixture-of-Experts (MoE) models. It focuses on heavy agentic workloads, like long-horizon software-engineering tasks.</p>



<p class="wp-block-paragraph">The research team trained GLM-5 on SWE tasks at up to 131k sequence length. Step times stayed under five minutes. The batch size was 256 rollouts. The run used only 28 H200 nodes.</p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li>prime-rl 0.6.0 trains trillion-parameter MoE models on agentic RL workloads.</li>



<li>GLM-5 trained on SWE at 131k sequence length, sub-5-minute steps, 28 H200 nodes.</li>



<li>Asynchronous RL disaggregates trainer and inference for independent optimization.</li>



<li>Inference uses FP8, Wide EP, P/D disaggregation, KV offloading, and router replay.</li>



<li>Training uses 3-D parallelism (FSDP, EP, CP) plus block-scaled FP8.</li>
</ul>



<h2 id="h-what-is-prime-rl-0-6-0" class="wp-block-heading"><strong>What is prime-rl 0.6.0?</strong></h2>



<p class="wp-block-paragraph">prime-rl is an open framework for asynchronous reinforcement learning. It post-trains large open-source models on agentic tasks. Version 0.6.0 extends this to trillion-parameter MoE scale.</p>



<p class="wp-block-paragraph">The example model in the announcement is <code>zai-org/GLM-5.1</code>. The optimizations also apply to other large MoE models. Examples include <code>moonshotai/Kimi-K2.7-Code</code> and <code>nvidia/NVIDIA-Nemotron-3-Ultra-550B-A55B-BF16</code>.</p>



<p class="wp-block-paragraph">A full GLM-5.1 run starts with one command on a Slurm cluster.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">uv run rl @ examples/glm5_llmd/rl.toml --output-dir /shared/outputs/glm5-llmd</code></pre></div></div>



<h2 id="h-role-of-asynchronous-rl" class="wp-block-heading"><strong>Role of asynchronous RL</strong></h2>



<p class="wp-block-paragraph">Agentic tasks have long-tail outliers. Some coding rollouts run for hours. Waiting for them before each policy update would idle GPUs.</p>



<p class="wp-block-paragraph">Asynchronous RL avoids this. The trainer and inference systems are disaggregated. They run and scale independently. The inference policy updates as soon as the optimizer step finishes.</p>



<p class="wp-block-paragraph">There is one synchronization point: the policy update. prime-rl pushes new weights as soon as they exist. Already-dispatched rollouts keep their active prefix cache. So a single rollout may mix tokens from several policy versions.</p>



<p class="wp-block-paragraph">New rollouts behave differently. They repopulate their own KV cache, even when prefixes match. A KV-cache salt forces this. Requests from too old a policy are dropped. The <code>max_off_policy_steps</code> value controls that threshold.</p>



<h2 id="h-inference-optimizations" class="wp-block-heading"><strong>Inference optimizations</strong></h2>



<p class="wp-block-paragraph">Inference is usually the throughput bottleneck in an RL system. prime-rl optimizes for throughput, while keeping latency bounded.</p>



<p class="wp-block-paragraph"><strong>FP8 inference</strong>: Lower precision speeds up prefill and decode. prime-rl uses FP8 with DeepEP and DeepGEMM kernels.</p>



<p class="wp-block-paragraph"><strong>Wide Expert Parallelism</strong>: Wide EP spreads experts across ≥32 GPUs. It pairs with a large data-parallel rank, for example 32. Each GPU holds separate experts and serves as an endpoint. Synchronization happens per-layer, through dispatch and combine operations.</p>



<p class="wp-block-paragraph"><strong>Prefill and Decode Disaggregation</strong>: Some model<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2194.png" alt="↔" class="wp-smiley" style="height: 1em; max-height: 1em;" />env pairs hit a 4:1 prefill:decode token ratio. Shared workers would inflate end-to-end latency. That reduces the benefits of PipelineRL. P/D disaggregation separates prefill and decode workers. Long tool outputs then stop throttling decode workers.</p>



<p class="wp-block-paragraph"><strong>KV cache management</strong>: High concurrency needs large KV cache space. prime-rl supports tiered offloading to CPU and disk. vLLM native offloading creates one pool per worker. Mooncake Store instead pools RAM and disk across all nodes centrally.</p>



<p class="wp-block-paragraph"><strong>Request routing</strong>: prime-rl ships a fork of vllm-router by default. It also supports the NVIDIA Dynamo router as a drop-in. Routers score workers using KV cache reuse, queue depth, and live load.</p>



<p class="wp-block-paragraph"><strong>Router replay (R3)</strong>: Trainer<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2194.png" alt="↔" class="wp-smiley" style="height: 1em; max-height: 1em;" />inference mismatch silently kills training. Router replay captures inference routing decisions. It replays them directly on the trainer. This cuts KL mismatch by roughly an order of magnitude. Routed experts have shape <code>[num_layers, top_k, seq_len]</code>. This payload can grow to hundreds of GB. At scale, the data rate reaches tens of Gbps. So prime-rl treats it as an opaque payload. Optimized PyTorch operations handle the processing.</p>



<h2 id="h-training-optimizations" class="wp-block-heading"><strong>Training optimizations</strong></h2>



<p class="wp-block-paragraph">The trainer builds on torchtitan, a PyTorch-native training codebase. It relies on 3-D parallelism: FSDP, CP, and EP. The GLM-5 case study uses all three.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Strategy</th><th>What it shards</th><th>Primary use</th><th>Key detail</th></tr></thead><tbody><tr><td><strong>FSDP (FSDP2)</strong></td><td>Parameters, gradients, optimizer states</td><td>Baseline memory amortization</td><td>Gathers weights on demand per layer via <code>fully_shard</code></td></tr><tr><td><strong>Expert Parallelism (EP)</strong></td><td>Experts within a layer</td><td>Shrinks active layer memory</td><td><code>all2all</code> dispatch/combine; torch-native or DeepEP</td></tr><tr><td><strong>Context Parallelism (CP)</strong></td><td>The sequence dimension</td><td>Long-context activation memory</td><td>Ulysses (default) or Ring Attention</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">EP exists because layers stay huge after FSDP. With 78 layers and 800B params in float32, one layer&#8217;s all-gather needs roughly 40GB. Overlapping one layer pushes that near 80GB. Setting EP=8 dispatches tokens instead of gathering full experts. torch-native all2all is slightly faster within one node. DeepEP wins when EP spans multiple nodes.</p>



<p class="wp-block-paragraph">CP matters at 131k+ sequence length. There, activations dominate memory, not parameters. GLM-5 uses DSA, which neither Ulysses nor Ring Attention parallelizes directly. So prime-rl ships a custom context-parallel implementation for it.</p>



<p class="wp-block-paragraph"><strong>FP8 training.</strong> prime-rl uses DeepGEMM block-scaled FP8, as proposed by DeepSeek V3. This rarely raises throughput, due to quantization overhead. Its real value is matching trainer and inference precision. That reduces KL mismatch and stabilizes training.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- prime-rl RL System Explorer | Marktechpost interactive embed | paste into a Custom HTML block -->
<div style="max-width:880px;margin:0 auto;">
<iframe id="primerl-explorer"
  title="prime-rl RL System Explorer"
  loading="lazy"
  style="width:100%;height:600px;border:0;overflow:hidden;display:block;"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;prime-rl RL System Explorer&lt;/title&gt;
&lt;style&gt;
  * { box-sizing: border-box; margin: 0; padding: 0; }
  :root{
    --ink:#0a0a0a; --paper:#ffffff; --mute:#6b6b6b; --line:#e2e2e2;
    --fill:#0a0a0a; --soft:#f4f4f4; --good:#0a0a0a;
  }
  html,body{ background:var(--paper); color:var(--ink);
    font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;
    -webkit-font-smoothing:antialiased; }
  .wrap{ max-width:860px; margin:0 auto; padding:26px 20px 18px; }
  .kicker{ font-size:11px; letter-spacing:2.4px; text-transform:uppercase; color:var(--mute); }
  h1{ font-size:23px; line-height:1.2; margin:6px 0 4px; font-weight:800; letter-spacing:-0.3px; }
  .sub{ font-size:13.5px; color:var(--mute); line-height:1.5; max-width:640px; }
  .grid{ display:grid; grid-template-columns:1.05fr 0.95fr; gap:22px; margin-top:22px; }
  .panel{ border:1px solid var(--line); border-radius:14px; padding:16px 16px 18px; background:var(--paper); }
  .panel h2{ font-size:12px; letter-spacing:1.6px; text-transform:uppercase; color:var(--mute); margin-bottom:14px; font-weight:700; }
  .opt{ display:flex; align-items:flex-start; gap:12px; padding:9px 0; border-top:1px solid var(--line); cursor:pointer; }
  .opt:first-of-type{ border-top:none; }
  .sw{ flex:0 0 38px; height:22px; border-radius:22px; background:#d9d9d9; position:relative; transition:background .18s ease; margin-top:1px; }
  .sw::after{ content:&quot;&quot;; position:absolute; top:2px; left:2px; width:18px; height:18px; border-radius:50%; background:var(--paper); transition:transform .18s ease; box-shadow:0 1px 2px rgba(0,0,0,.25); }
  .opt.on .sw{ background:var(--ink); }
  .opt.on .sw::after{ transform:translateX(16px); }
  .opt .meta{ flex:1; }
  .opt .name{ font-size:13.5px; font-weight:650; letter-spacing:-0.2px; }
  .opt .desc{ font-size:11.5px; color:var(--mute); line-height:1.45; margin-top:2px; }
  .metric{ margin-bottom:15px; }
  .metric:last-child{ margin-bottom:0; }
  .mtop{ display:flex; justify-content:space-between; align-items:baseline; margin-bottom:6px; }
  .mname{ font-size:12.5px; font-weight:650; }
  .mval{ font-size:13px; font-weight:800; font-variant-numeric:tabular-nums; }
  .mval small{ font-weight:600; color:var(--mute); font-size:11px; }
  .bar{ height:12px; border-radius:7px; background:var(--soft); overflow:hidden; border:1px solid var(--line); }
  .bar &gt; i{ display:block; height:100%; background:var(--fill);
    background-image:repeating-linear-gradient(45deg,rgba(255,255,255,.16) 0 6px,transparent 6px 12px);
    width:0%; transition:width .45s cubic-bezier(.2,.7,.2,1); }
  .hint{ font-size:11px; color:var(--mute); margin-top:5px; line-height:1.4; }
  .verdict{ margin-top:16px; border:1px solid var(--line); border-radius:12px; padding:12px 14px; background:var(--soft); }
  .verdict .vt{ font-size:11px; letter-spacing:1.4px; text-transform:uppercase; color:var(--mute); font-weight:700; }
  .verdict .vb{ font-size:13px; line-height:1.5; margin-top:4px; }
  .row{ display:flex; gap:8px; margin-top:14px; flex-wrap:wrap; }
  .btn{ font:inherit; font-size:11.5px; font-weight:650; letter-spacing:.2px; cursor:pointer;
    border:1px solid var(--ink); background:var(--paper); color:var(--ink); border-radius:9px; padding:7px 13px; }
  .btn.dark{ background:var(--ink); color:var(--paper); }
  .btn:active{ transform:translateY(1px); }
  .note{ font-size:10.5px; color:var(--mute); margin-top:16px; line-height:1.5; border-top:1px dashed var(--line); padding-top:10px; }
  .foot{ display:flex; justify-content:space-between; align-items:center; margin-top:14px; padding-top:12px; border-top:1px solid var(--line); flex-wrap:wrap; gap:6px; }
  .foot a{ color:var(--ink); font-weight:750; text-decoration:none; }
  .foot span{ font-size:10.5px; color:var(--mute); }
  @media (max-width:680px){ .grid{ grid-template-columns:1fr; gap:16px; } h1{ font-size:20px; } }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot; id=&quot;app&quot;&gt;
  &lt;div class=&quot;kicker&quot;&gt;Interactive · prime-rl 0.6.0&lt;/div&gt;
  &lt;h1&gt;RL System Explorer&lt;/h1&gt;
  &lt;p class=&quot;sub&quot;&gt;Toggle the optimizations from Prime Intellect&amp;#8217;s prime-rl deep dive. Watch how they shift inference throughput, training step time, and trainer&amp;#8596;inference KL mismatch. Values are illustrative, for intuition only.&lt;/p&gt;

  &lt;div class=&quot;grid&quot;&gt;
    &lt;div class=&quot;panel&quot;&gt;
      &lt;h2&gt;Optimizations&lt;/h2&gt;
      &lt;div class=&quot;opt on&quot; data-k=&quot;fp8inf&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;FP8 inference&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Low-precision prefill/decode with DeepEP + DeepGEMM kernels. Lifts throughput.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt on&quot; data-k=&quot;wideep&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;Wide Expert Parallelism&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Experts spread across &amp;#8805;32 GPUs with a large DP rank. Maximizes throughput.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt on&quot; data-k=&quot;pd&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;P/D disaggregation&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Separate prefill and decode workers. Keeps decode latency predictable.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt&quot; data-k=&quot;mooncake&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;KV offloading (Mooncake)&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Centralized RAM/disk pool across nodes. Raises concurrency.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt on&quot; data-k=&quot;async&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;Async weight updates&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Push new weights as soon as the optimizer step finishes. Cuts idle GPU time.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt&quot; data-k=&quot;r3&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;Router replay (R3)&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Replay inference routing on the trainer. Cuts KL mismatch ~10&amp;#215;.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;opt&quot; data-k=&quot;fp8train&quot;&gt;
        &lt;div class=&quot;sw&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;meta&quot;&gt;&lt;div class=&quot;name&quot;&gt;FP8 training&lt;/div&gt;&lt;div class=&quot;desc&quot;&gt;Block-scaled FP8 (DeepSeek V3). Matches trainer/inference precision.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;
        &lt;button class=&quot;btn dark&quot; id=&quot;all&quot;&gt;Enable all&lt;/button&gt;
        &lt;button class=&quot;btn&quot; id=&quot;none&quot;&gt;Naive baseline&lt;/button&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;panel&quot;&gt;
      &lt;h2&gt;Estimated System Behavior&lt;/h2&gt;

      &lt;div class=&quot;metric&quot;&gt;
        &lt;div class=&quot;mtop&quot;&gt;&lt;div class=&quot;mname&quot;&gt;Inference throughput&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;tpV&quot;&gt;1.0&lt;small&gt;&amp;#215;&lt;/small&gt;&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;bar&quot;&gt;&lt;i id=&quot;tpB&quot;&gt;&lt;/i&gt;&lt;/div&gt;
        &lt;div class=&quot;hint&quot;&gt;Higher is better. Throughput is the usual RL bottleneck.&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;metric&quot;&gt;
        &lt;div class=&quot;mtop&quot;&gt;&lt;div class=&quot;mname&quot;&gt;Training step time&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;stV&quot;&gt;12.0&lt;small&gt;min&lt;/small&gt;&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;bar&quot;&gt;&lt;i id=&quot;stB&quot;&gt;&lt;/i&gt;&lt;/div&gt;
        &lt;div class=&quot;hint&quot;&gt;Lower is better. The GLM-5 target was sub-5-minute steps.&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;metric&quot;&gt;
        &lt;div class=&quot;mtop&quot;&gt;&lt;div class=&quot;mname&quot;&gt;Trainer&amp;#8596;inference KL mismatch&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;klV&quot;&gt;High&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;bar&quot;&gt;&lt;i id=&quot;klB&quot;&gt;&lt;/i&gt;&lt;/div&gt;
        &lt;div class=&quot;hint&quot;&gt;Lower is better. Drift silently destabilizes RL training.&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;verdict&quot;&gt;
        &lt;div class=&quot;vt&quot;&gt;Read-out&lt;/div&gt;
        &lt;div class=&quot;vb&quot; id=&quot;vsay&quot;&gt;Toggle options to see the trade-offs.&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;p class=&quot;note&quot;&gt;&lt;strong&gt;Illustrative model.&lt;/strong&gt; Bars use simple weights to convey direction and trade-offs, not benchmark figures. Source: Prime Intellect, &amp;#8220;RL at 1T Scale: prime-rl Performance Deep Dive&amp;#8221; (Jun 21, 2026).&lt;/p&gt;

  &lt;div class=&quot;foot&quot;&gt;
    &lt;a href=&quot;https://www.marktechpost.com&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Marktechpost&lt;/a&gt;
    &lt;span&gt;Interactive explainer · black &amp;amp; white&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
  var state = { fp8inf:true, wideep:true, pd:true, mooncake:false, async:true, r3:false, fp8train:false };

  function compute(){
    // Throughput multiplier (baseline 1.0x)
    var tp = 1.0;
    if(state.fp8inf)  tp *= 1.55;
    if(state.wideep)  tp *= 1.45;
    if(state.pd)      tp *= 1.30;
    if(state.mooncake)tp *= 1.20;
    // Step time in minutes (baseline 12.0)
    var st = 12.0;
    if(state.async)   st -= 3.4;
    if(state.pd)      st -= 2.6;
    if(state.wideep)  st -= 1.8;
    if(state.fp8inf)  st -= 1.6;
    if(state.mooncake)st -= 0.9;
    if(st &lt; 3.6) st = 3.6;
    // KL mismatch score 0..100 (lower better), baseline 92
    var kl = 92;
    if(state.r3)       kl -= 46;
    if(state.fp8train) kl -= 28;
    if(state.async)    kl += 6; // async adds some off-policy drift
    if(kl &lt; 6) kl = 6;
    if(kl &gt; 100) kl = 100;
    return { tp:tp, st:st, kl:kl };
  }

  function klLabel(k){
    if(k &lt;= 22) return &quot;Low&quot;;
    if(k &lt;= 55) return &quot;Moderate&quot;;
    return &quot;High&quot;;
  }

  function verdict(c){
    var msgs = [];
    if(c.st &lt;= 5)  msgs.push(&quot;Step time is in the sub-5-minute target zone.&quot;);
    else if(c.st &gt;= 9) msgs.push(&quot;Step time is high; GPUs likely idle on long rollouts.&quot;);
    if(c.tp &gt;= 3.0) msgs.push(&quot;Inference throughput is well above the naive baseline.&quot;);
    else if(c.tp &lt;= 1.2) msgs.push(&quot;Throughput is near baseline; inference is the bottleneck.&quot;);
    if(c.kl &lt;= 22) msgs.push(&quot;Low KL mismatch means stable, on-policy training.&quot;);
    else if(c.kl &gt;= 70) msgs.push(&quot;High KL mismatch risks silent training instability.&quot;);
    if(!msgs.length) msgs.push(&quot;A balanced setup with room to push throughput or stability.&quot;);
    return msgs.slice(0,2).join(&quot; &quot;);
  }

  function render(){
    var c = compute();
    document.getElementById(&quot;tpV&quot;).innerHTML = c.tp.toFixed(2) + &quot;&lt;small&gt;&amp;#215;&lt;/small&gt;&quot;;
    document.getElementById(&quot;tpB&quot;).style.width = Math.min(100, (c.tp/4.0)*100).toFixed(1) + &quot;%&quot;;
    document.getElementById(&quot;stV&quot;).innerHTML = c.st.toFixed(1) + &quot;&lt;small&gt;min&lt;/small&gt;&quot;;
    // invert: shorter step =&gt; fuller bar
    document.getElementById(&quot;stB&quot;).style.width = Math.max(6, ((12.0 - c.st)/(12.0-3.6))*100).toFixed(1) + &quot;%&quot;;
    document.getElementById(&quot;klV&quot;).textContent = klLabel(c.kl);
    // invert: lower KL =&gt; fuller (better) bar
    document.getElementById(&quot;klB&quot;).style.width = Math.max(6, (100 - c.kl)).toFixed(1) + &quot;%&quot;;
    document.getElementById(&quot;vsay&quot;).textContent = verdict(c);
    sizeUp();
  }

  document.querySelectorAll(&quot;.opt&quot;).forEach(function(el){
    el.addEventListener(&quot;click&quot;, function(){
      var k = el.getAttribute(&quot;data-k&quot;);
      state[k] = !state[k];
      el.classList.toggle(&quot;on&quot;, state[k]);
      render();
    });
  });
  document.getElementById(&quot;all&quot;).addEventListener(&quot;click&quot;, function(){
    Object.keys(state).forEach(function(k){ state[k]=true; });
    document.querySelectorAll(&quot;.opt&quot;).forEach(function(el){ el.classList.add(&quot;on&quot;); });
    render();
  });
  document.getElementById(&quot;none&quot;).addEventListener(&quot;click&quot;, function(){
    Object.keys(state).forEach(function(k){ state[k]=false; });
    document.querySelectorAll(&quot;.opt&quot;).forEach(function(el){ el.classList.remove(&quot;on&quot;); });
    render();
  });

  // Auto-resize for WordPress iframe embed (offsetHeight + buffer)
  function sizeUp(){
    try{
      var h = document.getElementById(&quot;app&quot;).offsetHeight + 40;
      parent.postMessage({ type:&quot;primerl-explorer-height&quot;, height:h }, &quot;*&quot;);
    }catch(e){}
  }
  window.addEventListener(&quot;load&quot;, function(){ render(); setTimeout(sizeUp, 60); });
  window.addEventListener(&quot;resize&quot;, sizeUp);
  render();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
</div>
<script>
(function(){
  window.addEventListener("message", function(e){
    if(e && e.data && e.data.type === "primerl-explorer-height"){
      var f = document.getElementById("primerl-explorer");
      if(f && e.data.height){ f.style.height = e.data.height + "px"; }
    }
  });
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use cases with examples</strong></h2>



<ul class="wp-block-list">
<li><strong>Long-horizon SWE agents</strong>: Train a model on real repository issues. Rollouts can span 100s of turns and tool calls. P/D disaggregation keeps decode latency predictable here.</li>



<li><strong>1T-scale post-training on fewer nodes</strong>: The GLM-5 run fit on 28 H200 nodes. Wide EP and KV offloading raise concurrency and throughput.</li>



<li><strong>Stable agentic RL at scale</strong>: Router replay and FP8 training both reduce trainer<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2194.png" alt="↔" class="wp-smiley" style="height: 1em; max-height: 1em;" />inference KL mismatch. Lower mismatch means steadier training.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://www.primeintellect.ai/blog/rl-at-1t-scale" target="_blank" rel="noreferrer noopener">Technical details</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/23/prime-intellect-releases-prime-rl-0-6-0-to-train-trillion-parameter-moe-models-on-agentic-rl-workloads/">Prime Intellect Releases prime-rl 0.6.0 to Train Trillion-Parameter MoE Models on Agentic RL Workloads</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80661</post-id>
	</item>
	</channel>
</rss>
