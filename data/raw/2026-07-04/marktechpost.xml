<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	 xmlns:media="http://search.yahoo.com/mrss/" >

<channel>
	<title>MarkTechPost</title>
	<atom:link href="https://www.marktechpost.com/feed/" rel="self" type="application/rss+xml" />
	<link>https://www.marktechpost.com/</link>
	<description>An Artificial Intelligence News Platform</description>
	<lastBuildDate>Fri, 03 Jul 2026 06:00:54 +0000</lastBuildDate>
	<language>en-US</language>
	<sy:updatePeriod>
	hourly	</sy:updatePeriod>
	<sy:updateFrequency>
	1	</sy:updateFrequency>
	<generator>https://wordpress.org/?v=7.0</generator>

<image>
	<url>https://www.marktechpost.com/wp-content/uploads/2022/04/cropped-Favicon-512-x-512-1-1-32x32.png</url>
	<title>MarkTechPost</title>
	<link>https://www.marktechpost.com/</link>
	<width>32</width>
	<height>32</height>
</image> 
<site xmlns="com-wordpress:feed-additions:1">127842392</site>	<item>
		<title>Meet WebBrain: An Open-Source, Local-First AI Browser Agent That Reads Pages and Automates Tasks in Chrome and Firefox</title>
		<link>https://www.marktechpost.com/2026/07/02/meet-webbrain-an-open-source-local-first-ai-browser-agent-that-reads-pages-and-automates-tasks-in-chrome-and-firefox/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Fri, 03 Jul 2026 05:55:47 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Agents]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Promote]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Sponsored]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Uncategorized]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80818</guid>

					<description><![CDATA[<p>WebBrain is a free, MIT-licensed AI browser agent for Chrome and Firefox. It reads pages, extracts data, and automates multi-step tasks through Ask and Act modes. Run it on local models like llama.cpp or Ollama for privacy, or connect any cloud API.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/02/meet-webbrain-an-open-source-local-first-ai-browser-agent-that-reads-pages-and-automates-tasks-in-chrome-and-firefox/">Meet WebBrain: An Open-Source, Local-First AI Browser Agent That Reads Pages and Automates Tasks in Chrome and Firefox</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph"><a href="https://pxllnk.co/rlifl7h" target="_blank" rel="noreferrer noopener">WebBrain</a> is a free, open-source browser agent for Chrome and Firefox. It reads pages, extracts data, and automates multi-step tasks. Unlike most browser AI plugins, it can also run entirely on a local model.</p>



<p class="wp-block-paragraph">It is built by <a href="http://linkedin.com/in/emresokullu?originalSubdomain=tr">Emre Sokullu</a> and licensed under MIT. The full source lives on <a href="https://pxllnk.co/wdva98c" target="_blank" rel="noreferrer noopener">GitHub</a>. </p>



<p class="wp-block-paragraph">Run the agent against a local model, and no page data leaves your machine. Connect a cloud API when you want more capability.</p>



<h2 id="h-what-is-webbrain" class="wp-block-heading"><strong>What is WebBrain?</strong></h2>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/rlifl7h" target="_blank" rel="noreferrer noopener">WebBrain</a> lives in your browser&#8217;s side panel. In Chrome it uses Manifest V3 and the sidePanel API. In Firefox it uses Manifest V2 and sidebar_action. Each tab keeps its own conversation history.</p>



<p class="wp-block-paragraph">The extension operates inside your existing authenticated session. It sees your logged-in accounts exactly as you do. It stores no data externally and adds no telemetry or accounts.</p>



<p class="wp-block-paragraph">The plugin ships in English, Español, Français, Türkçe, and 中文. It auto-detects your browser language on first launch.</p>



<h2 id="h-ask-mode-act-mode-and-how-actions-actually-fire" class="wp-block-heading"><strong>Ask Mode, Act Mode, and How Actions Actually Fire</strong></h2>



<p class="wp-block-paragraph"><strong><a href="https://pxllnk.co/m6k7c5w9" target="_blank" rel="noreferrer noopener">WebBrain</a> has two modes:</strong> <strong>Ask mode </strong>is read-only and cannot change the page. <strong>Act mode </strong>can click, type, scroll, navigate, and run workflows.</p>



<p class="wp-block-paragraph">Ask mode reads pages through ordinary content scripts. Act mode is different. It drives the page through the Chrome DevTools Protocol via the chrome.debugger API. That produces <strong>trusted input events</strong> that modern sites actually honor. It also reaches cross-origin iframes and shadow DOM that content scripts cannot see.</p>



<p class="wp-block-paragraph">That power is scoped deliberately. WebBrain attaches the debugger only when an action needs it, per tab. Chrome surfaces its standard ‘WebBrain started debugging this browser’ banner while attached. Firefox has no CDP equivalent, so its Act mode is meaningfully weaker.</p>



<p class="wp-block-paragraph">Temperatures are fixed for predictability. Act mode uses temperature 0.15. Ask mode uses 0.3. Dedicated vision screenshot descriptions use 0.</p>



<h2 id="h-the-security-model" class="wp-block-heading"><strong>The Security Model</strong></h2>



<p class="wp-block-paragraph">Browser agents run on an adversarial surface. Web pages can hide prompt injections that hijack an agent&#8217;s behavior. WebBrain&#8217;s design addresses this directly.</p>



<p class="wp-block-paragraph">The agent starts in read-only Ask mode. It asks before consequential actions. You can disable those prompts in the Permissions settings. They are on by default.</p>



<p class="wp-block-paragraph">There is also a <strong>UI-first</strong> rule for mutations. For anything that creates, sends, submits, or buys, WebBrain uses the visible UI. It refuses to call REST or GraphQL endpoints directly for mutations. A per-conversation /allow-api override exists when the UI genuinely fails.</p>



<p class="wp-block-paragraph">Reading is treated separately. Fetching a README or comparing prices uses background HTTP through the fetch_url and research_url tools. Reading changes nothing remotely, so the strict rules do not apply.</p>



<h2 id="h-use-cases-with-concrete-examples" class="wp-block-heading"><strong>Use Cases, With Concrete Examples</strong></h2>



<ul class="wp-block-list">
<li>Data extraction is the obvious one: Open a catalog and ask: ‘Extract all product names and prices from this page.’ The agent reads the structure and returns rows. It also works with PDFs.</li>



<li>Research summaries are another: Ask ‘Summarize this article,’ then follow up with a specific question. WebBrain detects paywalls honestly and does not try to bypass them. It also dismisses common cookie-consent banners before reading.</li>



<li>Form filling suits repetitive signups: An optional Profile auto-fill stores a short bio in local plaintext. That text is sent to your configured LLM to complete low-stakes forms. Keep important passwords out of it.</li>



<li>Automation spans multiple steps: Try ‘Navigate to github.com and find trending repositories.’ In Act mode, the agent chains navigation, reads, and clicks.</li>
</ul>



<h2 id="h-keeping-token-costs-down" class="wp-block-heading"><strong>Keeping Token Costs Down</strong></h2>



<p class="wp-block-paragraph">Cloud tokens add up on long sessions. WebBrain bounds the cost in three ways.</p>



<ul class="wp-block-list">
<li>Screenshots are resized and iteratively JPEG-compressed before they leave your machine. That keeps image tokens small. </li>



<li>Conversation history and tool outputs are trimmed oldest-first as the context window fills. </li>



<li>You can also pair a cheap text model for planning with a separate vision model for screenshots.</li>
</ul>



<h2 id="h-how-it-compares" class="wp-block-heading"><strong>How It Compares</strong></h2>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/m6k7c5w9">WebBrain</a> sits between browser AI plugins and full agent frameworks. Here is the plugin comparison, drawn from the project&#8217;s own documentation.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th><strong>Feature</strong></th><th><strong>WebBrain</strong></th><th><strong>Claude in Chrome</strong></th></tr></thead><tbody><tr><td>Open source</td><td>MIT License</td><td>Proprietary</td></tr><tr><td>Price</td><td>Free forever</td><td>Requires Claude Pro ($20/mo)</td></tr><tr><td>Local LLM support</td><td>llama.cpp, Ollama</td><td>No — Claude only</td></tr><tr><td>Multi-provider</td><td>All OpenAI-compatible endpoints</td><td>Claude only</td></tr><tr><td>Chrome</td><td>Yes (MV3)</td><td>Yes</td></tr><tr><td>Firefox</td><td>Yes (MV2)</td><td>No</td></tr><tr><td>Side panel UI</td><td>Yes</td><td>Yes</td></tr><tr><td>Ask / Act modes</td><td>Yes</td><td>Similar</td></tr><tr><td>Fully offline</td><td>Yes (with local LLM)</td><td>No — cloud required</td></tr><tr><td>Self-hostable</td><td>Yes</td><td>No</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">Frameworks like OpenClaw or Browser-Use are a different category. Those are developer SDKs for headless pipelines. WebBrain is an end-user extension you drive from a chat panel. You can use both.</p>



<h2 id="h-running-it-providers-and-setup" class="wp-block-heading"><strong>Running It: Providers and Setup</strong></h2>



<p class="wp-block-paragraph"><a href="https://pxllnk.co/p4mn8">WebBrain</a> supports local and cloud models through one interface. Local options include llama.cpp, Ollama, LM Studio, Jan, vLLM, and SGLang. Cloud options include OpenAI, Anthropic Claude, Gemini, Mistral, DeepSeek, and xAI Grok. It also supports Groq, MiniMax, Alibaba Cloud (Qwen), Nvidia NIM, and OpenRouter.</p>



<p class="wp-block-paragraph">A built-in managed option, WebBrain Cloud, needs no local setup. It costs $5 per month per device profile under a fair-use policy. For local use, llama.cpp needs no API key.</p>



<p class="wp-block-paragraph">Starting a local server takes one command:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php"># llama.cpp — load at least a 16k-token context window
llama-server -m your-model.gguf -c 16384 --port 8080

# Ollama (OpenAI-compatible) — set the extension-origin env var
OLLAMA_ORIGINS="*" ollama serve
# then set the base URL to http://localhost:11434/v1 in settings
</code></pre></div></div>



<p class="wp-block-paragraph">Point WebBrain at the endpoint in settings. For a cross-machine vLLM server, enable CORS with &#8211;allowed-origins &#8216;[&#8220;*&#8221;]&#8217;.</p>



<p class="wp-block-paragraph">The recommended model is Qwen 3.6 35B (Qwen3.6-35B-A3B). It beat Gemma 4 on the project&#8217;s screenshot benchmark. An RTX 5090 is ideal; an RTX 4090 works with INT4 AutoRound quantization.</p>



<p class="wp-block-paragraph">Each provider is a class that extends BaseLLMProvider. It normalizes to one response shape:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">{ content: string, toolCalls: Array|null, usage: Object|null }</code></pre></div></div>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>WebBrain is a free, MIT-licensed AI browser agent for Chrome and Firefox, built by Emre Sokullu.</li>



<li>It runs on local models (llama.cpp, Ollama; Qwen 3.6 35B recommended) or any cloud API — no page data leaves your machine when local.</li>



<li>Ask mode reads pages read-only; Act mode clicks and types via the Chrome DevTools Protocol for trusted input events.</li>



<li>Security-first by design: starts read-only, approves consequential actions, and uses the UI instead of direct API calls for mutations.</li>



<li>Free forever self-hosted, or $5/month per device profile for the managed WebBrain Cloud under fair use.</li>
</ul>



<h2 id="h-interactive-explainer-with-demo" class="wp-block-heading"><strong>Interactive Explainer with Demo</strong></h2>



<h3 id="h-demo-1" class="wp-block-heading"><strong>Demo-1</strong></h3>



<!-- WebBrain interactive demo — paste into a WordPress Custom HTML block -->
<div id="webbrain-embed-wrap" style="max-width:1080px;margin:0 auto;">
  <iframe id="webbrain-frame"
    title="WebBrain Interactive Demo"
    loading="lazy"
    style="width:100%;border:0;height:600px;overflow:hidden;"
    srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;WebBrain Interactive Demo&lt;/title&gt;
&lt;style&gt;
  #wb-root, #wb-root *{box-sizing:border-box}
  #wb-root{
    --bg:#f5f7fa; --card:#ffffff; --ink:#1f2430; --muted:#6b7280;
    --line:#e5e7eb; --line2:#eef1f5; --accent:#4f8a00; --accent2:#76B900;
    --chip:#f0f4ea; --code:#0f172a; --codebg:#f6f8fa; --ok:#15803d; --warn:#b45309;
    font-family:Inter,-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;
    color:var(--ink); background:var(--bg); border:1px solid var(--line);
    border-radius:16px; padding:18px; max-width:1080px; margin:0 auto;
  }
  #wb-root .wb-head{display:flex;align-items:center;gap:10px;flex-wrap:wrap}
  #wb-root .wb-logo{width:34px;height:34px;border-radius:9px;background:var(--accent2);
    display:flex;align-items:center;justify-content:center;font-size:19px;flex:0 0 auto}
  #wb-root h2{margin:0;font-size:18px;font-weight:700;letter-spacing:-.01em}
  #wb-root .wb-sub{margin:2px 0 0;font-size:12.5px;color:var(--muted)}
  #wb-root .wb-badge{margin-left:auto;font-size:11px;font-weight:600;color:var(--warn);
    background:#fff7ed;border:1px solid #fed7aa;padding:4px 9px;border-radius:999px}
  #wb-root .wb-controls{display:flex;gap:14px;flex-wrap:wrap;align-items:center;margin:16px 0 6px}
  #wb-root .wb-seg{display:inline-flex;background:var(--line2);border-radius:10px;padding:3px}
  #wb-root .wb-seg button{border:0;background:transparent;padding:7px 16px;border-radius:8px;
    font-size:13px;font-weight:600;color:var(--muted);cursor:pointer}
  #wb-root .wb-seg button.on{background:var(--card);color:var(--ink);box-shadow:0 1px 2px rgba(0,0,0,.08)}
  #wb-root .wb-seg button.on[data-mode=&quot;act&quot;]{color:var(--accent)}
  #wb-root .wb-chips{display:flex;gap:8px;flex-wrap:wrap}
  #wb-root .wb-chip{border:1px solid var(--line);background:var(--card);border-radius:999px;
    padding:7px 13px;font-size:12.5px;font-weight:600;color:var(--ink);cursor:pointer;transition:.15s}
  #wb-root .wb-chip:hover{border-color:var(--accent2)}
  #wb-root .wb-chip.on{background:var(--chip);border-color:var(--accent2);color:var(--accent)}
  #wb-root .wb-chip .req{font-weight:600;color:var(--muted);font-size:11px}
  #wb-root .wb-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:12px}
  #wb-root .wb-pane{background:var(--card);border:1px solid var(--line);border-radius:12px;overflow:hidden;
    min-height:340px;display:flex;flex-direction:column}
  #wb-root .wb-bar{display:flex;align-items:center;gap:8px;padding:9px 11px;border-bottom:1px solid var(--line);
    background:#fbfcfe}
  #wb-root .wb-dots{display:flex;gap:5px}
  #wb-root .wb-dot{width:10px;height:10px;border-radius:50%}
  #wb-root .wb-url{flex:1;background:#fff;border:1px solid var(--line);border-radius:7px;padding:5px 10px;
    font-size:12px;color:var(--muted);font-family:&quot;JetBrains Mono&quot;,ui-monospace,Menlo,Consolas,monospace;
    white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  #wb-root .wb-page{padding:14px;font-size:13px;line-height:1.5;flex:1;overflow:auto}
  #wb-root .wb-panehead{display:flex;align-items:center;gap:8px;padding:9px 12px;border-bottom:1px solid var(--line);
    background:#fbfcfe;font-size:12.5px;font-weight:700}
  #wb-root .wb-mode-pill{margin-left:auto;font-size:11px;font-weight:700;padding:3px 9px;border-radius:999px}
  #wb-root .wb-mode-pill.ask{background:#eef2ff;color:#4338ca}
  #wb-root .wb-mode-pill.act{background:#f0f4ea;color:var(--accent)}
  #wb-root .wb-panel{flex:1;display:flex;flex-direction:column;padding:12px;gap:10px;overflow:auto}
  #wb-root .wb-prompt{display:flex;gap:8px;align-items:stretch}
  #wb-root .wb-prompt input{flex:1;border:1px solid var(--line);border-radius:9px;padding:9px 11px;font-size:13px;
    font-family:inherit;color:var(--ink)}
  #wb-root .wb-prompt input:focus{outline:2px solid var(--accent2);outline-offset:0;border-color:transparent}
  #wb-root .wb-run{border:0;background:var(--accent2);color:#0b0b0b;font-weight:700;border-radius:9px;padding:0 16px;
    font-size:13px;cursor:pointer}
  #wb-root .wb-run:disabled{opacity:.5;cursor:not-allowed}
  #wb-root .wb-stop{border:1px solid #fecaca;background:#fef2f2;color:#b91c1c;font-weight:700;border-radius:9px;
    padding:0 14px;font-size:13px;cursor:pointer}
  #wb-root .wb-log{display:flex;flex-direction:column;gap:7px;font-size:12.5px}
  #wb-root .wb-step{display:flex;gap:8px;align-items:flex-start;color:var(--ink);
    animation:wbfade .25s ease}
  @keyframes wbfade{from{opacity:0;transform:translateY(3px)}to{opacity:1;transform:none}}
  #wb-root .wb-step .tick{color:var(--ok);font-weight:700}
  #wb-root .wb-step code{font-family:&quot;JetBrains Mono&quot;,ui-monospace,Menlo,Consolas,monospace;
    background:var(--codebg);border:1px solid var(--line);border-radius:5px;padding:1px 5px;font-size:11.5px}
  #wb-root .wb-step .spin{display:inline-block;width:11px;height:11px;border:2px solid var(--line);
    border-top-color:var(--accent);border-radius:50%;animation:wbspin .7s linear infinite}
  @keyframes wbspin{to{transform:rotate(360deg)}}
  #wb-root .wb-result{background:#f8faf4;border:1px solid #dcecc4;border-radius:10px;padding:11px 12px;font-size:12.5px;
    line-height:1.5;animation:wbfade .3s ease}
  #wb-root .wb-result h4{margin:0 0 6px;font-size:12.5px;color:var(--accent)}
  #wb-root .wb-result table{width:100%;border-collapse:collapse;margin-top:4px}
  #wb-root .wb-result td{padding:4px 6px;border-bottom:1px solid #e6efd6;font-size:12px}
  #wb-root .wb-result td:last-child{text-align:right;font-weight:600}
  #wb-root .wb-token{font-size:11.5px;color:var(--muted);background:#fffbeb;border:1px dashed #fde68a;
    border-radius:8px;padding:7px 9px}
  #wb-root .wb-approve{border:1px solid #fde68a;background:#fffbeb;border-radius:10px;padding:11px 12px}
  #wb-root .wb-approve p{margin:0 0 9px;font-size:12.5px;font-weight:600;color:var(--warn)}
  #wb-root .wb-approve .row{display:flex;gap:8px}
  #wb-root .wb-approve button{border:0;border-radius:8px;padding:7px 15px;font-size:12.5px;font-weight:700;cursor:pointer}
  #wb-root .wb-yes{background:var(--accent2);color:#0b0b0b}
  #wb-root .wb-no{background:#fff;border:1px solid var(--line)!important;color:var(--ink)}
  #wb-root .wb-note{font-size:11.5px;color:var(--muted)}
  #wb-root .wb-hint{color:var(--muted);font-size:12.5px}
  #wb-root .prod{display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--line2)}
  #wb-root .prod b{color:var(--accent)}
  #wb-root .field{margin:8px 0}
  #wb-root .field label{display:block;font-size:11px;color:var(--muted);margin-bottom:3px}
  #wb-root .field .box{border:1px solid var(--line);border-radius:7px;padding:7px 9px;font-size:12.5px;background:#fff;
    min-height:32px;transition:.2s}
  #wb-root .field .box.fill{background:#f8faf4;border-color:#cfe6a8}
  #wb-root .vid{background:#0f172a;color:#e2e8f0;border-radius:9px;padding:26px 14px;text-align:center;font-size:13px}
  #wb-root .wb-foot{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:14px;padding-top:12px;
    border-top:1px solid var(--line);font-size:11.5px;color:var(--muted)}
  #wb-root .wb-foot a{color:var(--accent);font-weight:700;text-decoration:none}
  #wb-root .wb-foot .brand{margin-left:auto;font-weight:800;color:var(--accent)}
  #wb-root .wb-reset{border:1px solid var(--line);background:#fff;border-radius:8px;padding:5px 11px;font-size:12px;
    font-weight:600;cursor:pointer;color:var(--ink)}
  @media(max-width:720px){#wb-root .wb-grid{grid-template-columns:1fr}}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;wb-root&quot;&gt;
  &lt;div class=&quot;wb-head&quot;&gt;
    &lt;div class=&quot;wb-logo&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f9e0.png" alt="🧠" class="wp-smiley" style="height: 1em; max-height: 1em;" />&lt;/div&gt;
    &lt;div&gt;
      &lt;h2&gt;WebBrain &amp;mdash; Interactive Demo&lt;/h2&gt;
      &lt;p class=&quot;wb-sub&quot;&gt;Pick a task, choose Ask or Act, and watch the agent work.&lt;/p&gt;
    &lt;/div&gt;
    &lt;span class=&quot;wb-badge&quot;&gt;Simulated &amp;middot; no real LLM calls&lt;/span&gt;
  &lt;/div&gt;

  &lt;div class=&quot;wb-controls&quot;&gt;
    &lt;div class=&quot;wb-seg&quot; id=&quot;wb-modeseg&quot;&gt;
      &lt;button data-mode=&quot;ask&quot; class=&quot;on&quot;&gt;Ask mode&lt;/button&gt;
      &lt;button data-mode=&quot;act&quot;&gt;Act mode&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;wb-chips&quot; id=&quot;wb-chips&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;wb-grid&quot;&gt;
    &lt;div class=&quot;wb-pane&quot;&gt;
      &lt;div class=&quot;wb-bar&quot;&gt;
        &lt;div class=&quot;wb-dots&quot;&gt;&lt;span class=&quot;wb-dot&quot; style=&quot;background:#f87171&quot;&gt;&lt;/span&gt;
          &lt;span class=&quot;wb-dot&quot; style=&quot;background:#fbbf24&quot;&gt;&lt;/span&gt;
          &lt;span class=&quot;wb-dot&quot; style=&quot;background:#34d399&quot;&gt;&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;wb-url&quot; id=&quot;wb-url&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;wb-page&quot; id=&quot;wb-page&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;wb-pane&quot;&gt;
      &lt;div class=&quot;wb-panehead&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f9e0.png" alt="🧠" class="wp-smiley" style="height: 1em; max-height: 1em;" /> WebBrain
        &lt;span class=&quot;wb-mode-pill ask&quot; id=&quot;wb-modepill&quot;&gt;Ask&lt;/span&gt;
      &lt;/div&gt;
      &lt;div class=&quot;wb-panel&quot;&gt;
        &lt;div class=&quot;wb-prompt&quot;&gt;
          &lt;input id=&quot;wb-input&quot; type=&quot;text&quot; autocomplete=&quot;off&quot;&gt;
          &lt;button class=&quot;wb-run&quot; id=&quot;wb-run&quot;&gt;Run&lt;/button&gt;
        &lt;/div&gt;
        &lt;div class=&quot;wb-log&quot; id=&quot;wb-log&quot;&gt;
          &lt;div class=&quot;wb-hint&quot;&gt;Choose a task above, then press &lt;b&gt;Run&lt;/b&gt;. Read-only Ask mode is the default.&lt;/div&gt;
        &lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;wb-foot&quot;&gt;
    &lt;button class=&quot;wb-reset&quot; id=&quot;wb-reset&quot;&gt;Reset demo&lt;/button&gt;
    &lt;span&gt;Actions here are illustrative and safe. The real extension asks before consequential actions.&lt;/span&gt;
    &lt;span class=&quot;brand&quot;&gt;Built by Marktechpost&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var SCEN = {
    products: {
      label:&quot;Extract products&quot;, req:&quot;ask&quot;,
      url:&quot;https://example.com/products&quot;,
      prompt:&quot;Extract all product names and prices from this page&quot;,
      page:'&lt;b&gt;Product Catalog&lt;/b&gt;'+
        '&lt;div class=&quot;prod&quot;&gt;&lt;span&gt;Widget Pro&lt;/span&gt;&lt;b&gt;$29.99&lt;/b&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;prod&quot;&gt;&lt;span&gt;Super Gadget&lt;/span&gt;&lt;b&gt;$49.99&lt;/b&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;prod&quot;&gt;&lt;span&gt;MegaTool X&lt;/span&gt;&lt;b&gt;$19.99&lt;/b&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;prod&quot;&gt;&lt;span&gt;Nano Clip&lt;/span&gt;&lt;b&gt;$8.50&lt;/b&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;prod&quot;&gt;&lt;span&gt;HyperDock&lt;/span&gt;&lt;b&gt;$74.00&lt;/b&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;prod&quot;&gt;&lt;span&gt;PixelCase&lt;/span&gt;&lt;b&gt;$14.25&lt;/b&gt;&lt;/div&gt;'+
        '&lt;p class=&quot;wb-hint&quot; style=&quot;margin-top:8px&quot;&gt;&amp;hellip;and 18 more&lt;/p&gt;',
      steps:[
        {t:'Reading page', c:'get_accessibility_tree'},
        {t:'Extracting structured rows', c:'extract_data'}
      ],
      result:function(){
        return '&lt;h4&gt;Found 24 products&lt;/h4&gt;&lt;table&gt;'+
          '&lt;tr&gt;&lt;td&gt;Widget Pro&lt;/td&gt;&lt;td&gt;$29.99&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;Super Gadget&lt;/td&gt;&lt;td&gt;$49.99&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;MegaTool X&lt;/td&gt;&lt;td&gt;$19.99&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;Nano Clip&lt;/td&gt;&lt;td&gt;$8.50&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;HyperDock&lt;/td&gt;&lt;td&gt;$74.00&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;PixelCase&lt;/td&gt;&lt;td&gt;$14.25&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;/table&gt;&lt;p class=&quot;wb-note&quot; style=&quot;margin-top:7px&quot;&gt;&amp;hellip;and 18 more. Export as CSV or JSON.&lt;/p&gt;';
      }
    },
    summary: {
      label:&quot;Summarize article&quot;, req:&quot;ask&quot;,
      url:&quot;https://magazine.dev/local-ai-2026&quot;,
      prompt:&quot;Summarize this article&quot;,
      page:'&lt;b&gt;The Future of Local AI&lt;/b&gt;&lt;p class=&quot;wb-hint&quot;&gt;By Jane Smith &amp;middot; 8 min read&lt;/p&gt;'+
        '&lt;p&gt;Local LLMs now match cloud quality on many tasks. Better quantization, faster consumer GPUs, '+
        'and a privacy-first shift are the drivers. The author expects most consumer AI to run on-device by 2026.&lt;/p&gt;',
      steps:[
        {t:'Reading page text', c:'read_page'}
      ],
      result:function(){
        return '&lt;h4&gt;Summary&lt;/h4&gt;&lt;ul style=&quot;margin:0;padding-left:18px&quot;&gt;'+
          '&lt;li&gt;Local LLMs now rival cloud quality on many tasks.&lt;/li&gt;'+
          '&lt;li&gt;Drivers: better quantization, faster GPUs, privacy demand.&lt;/li&gt;'+
          '&lt;li&gt;Author predicts 70%+ of consumer AI runs on-device by 2026.&lt;/li&gt;'+
          '&lt;/ul&gt;&lt;p class=&quot;wb-note&quot; style=&quot;margin-top:8px&quot;&gt;Follow-up: &lt;a href=&quot;#&quot; id=&quot;wb-follow&quot;&gt;'+
          'What does the author predict for 2026?&lt;/a&gt;&lt;/p&gt;';
      },
      follow:{
        steps:[{t:'Recalling article context', c:'scratchpad_write'}],
        result:'&lt;h4&gt;Answer&lt;/h4&gt;&lt;p&gt;By 2026, the author expects 70%+ of consumer AI workloads to run on-device.&lt;/p&gt;'
      }
    },
    video: {
      label:&quot;Download video&quot;, req:&quot;act&quot;,
      url:&quot;https://socialvid.app/@nomad/clip-8472&quot;,
      prompt:&quot;Download this video&quot;,
      page:'&lt;div class=&quot;vid&quot;&gt;&amp;#9654; Sunset timelapse over Bodrum&lt;br&gt;&lt;span style=&quot;font-size:11px;opacity:.7&quot;&gt;'+
        '@nomad &amp;middot; 0:24&lt;/span&gt;&lt;/div&gt;&lt;p class=&quot;wb-hint&quot; style=&quot;margin-top:8px&quot;&gt;Video post&lt;/p&gt;',
      steps:[
        {t:'Listing interactive elements', c:'get_interactive_elements'},
        {t:'Capturing visible tab', c:'screenshot', token:true},
        {t:'Locating video stream', c:'download_resource_from_page'}
      ],
      approve:&quot;Download the MP4 to your machine?&quot;,
      result:function(){
        return '&lt;h4&gt;Saved&lt;/h4&gt;&lt;p&gt;Downloads/nomad-bodrum.mp4&lt;/p&gt;'+
          '&lt;p class=&quot;wb-note&quot;&gt;1080p &amp;middot; 24s &amp;middot; 4.2 MB&lt;/p&gt;';
      }
    },
    signup: {
      label:&quot;Fill signup form&quot;, req:&quot;act&quot;,
      url:&quot;https://acmestudio.io/signup&quot;,
      prompt:&quot;Sign me up using my profile&quot;,
      page:'&lt;b&gt;Create your Acme Studio account&lt;/b&gt;'+
        '&lt;div class=&quot;field&quot;&gt;&lt;label&gt;Work email&lt;/label&gt;&lt;div class=&quot;box&quot; id=&quot;f-email&quot;&gt;&lt;/div&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;field&quot;&gt;&lt;label&gt;Company&lt;/label&gt;&lt;div class=&quot;box&quot; id=&quot;f-company&quot;&gt;&lt;/div&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;field&quot;&gt;&lt;label&gt;Password&lt;/label&gt;&lt;div class=&quot;box&quot; id=&quot;f-pass&quot;&gt;&lt;/div&gt;&lt;/div&gt;'+
        '&lt;p class=&quot;wb-hint&quot;&gt;Uses optional Profile auto-fill (stored locally, plaintext).&lt;/p&gt;',
      steps:[
        {t:'Reading form fields', c:'get_accessibility_tree'},
        {t:'Filling email', c:'set_field', fill:{id:'f-email',v:'dev@example.com'}},
        {t:'Filling company', c:'set_field', fill:{id:'f-company',v:'Acme Labs'}},
        {t:'Filling throwaway password', c:'set_field', fill:{id:'f-pass',v:'&amp;bull;&amp;bull;&amp;bull;&amp;bull;&amp;bull;&amp;bull;&amp;bull;&amp;bull;'}},
        {t:'Checking values before submit', c:'verify_form'}
      ],
      approve:&quot;Submit the signup form and create the account?&quot;,
      result:function(){
        return '&lt;h4&gt;Account created&lt;/h4&gt;&lt;p&gt;Confirmation screen shown by the site.&lt;/p&gt;'+
          '&lt;p class=&quot;wb-note&quot;&gt;Profile text is sent to your LLM each turn. Keep important passwords out of it.&lt;/p&gt;';
      }
    }
  };

  var order = ['products','summary','video','signup'];
  var state = { mode:'ask', scen:'products', running:false, aborted:false };
  var timers = [];

  var $ = function(id){ return document.getElementById(id); };
  function clearTimers(){ timers.forEach(clearTimeout); timers = []; }

  function renderChips(){
    var box = $('wb-chips'); box.innerHTML='';
    order.forEach(function(k){
      var s = SCEN[k];
      var b = document.createElement('button');
      b.className = 'wb-chip' + (k===state.scen?' on':'');
      b.innerHTML = s.label + ' &lt;span class=&quot;req&quot;&gt;&amp;middot; '+(s.req==='act'?'Act':'Ask')+'&lt;/span&gt;';
      b.onclick = function(){ selectScen(k); };
      box.appendChild(b);
    });
  }

  function renderMode(){
    var seg = $('wb-modeseg').querySelectorAll('button');
    seg.forEach(function(b){ b.classList.toggle('on', b.dataset.mode===state.mode); });
    var pill = $('wb-modepill');
    pill.textContent = state.mode==='act'?'Act':'Ask';
    pill.className = 'wb-mode-pill '+(state.mode==='act'?'act':'ask');
  }

  function selectScen(k){
    if(state.running) return;
    state.scen = k;
    var s = SCEN[k];
    $('wb-url').textContent = s.url;
    $('wb-page').innerHTML = s.page;
    $('wb-input').value = s.prompt;
    if(s.req==='act' &amp;&amp; state.mode!=='act'){ state.mode='act'; }
    renderChips(); renderMode(); resetLog();
  }

  function resetLog(){
    clearTimers(); state.running=false; state.aborted=false;
    $('wb-run').style.display=''; $('wb-run').disabled=false;
    var stop=$('wb-stopbtn'); if(stop) stop.remove();
    $('wb-log').innerHTML = '&lt;div class=&quot;wb-hint&quot;&gt;Press &lt;b&gt;Run&lt;/b&gt; to start. Ask mode is read-only; Act mode can act.&lt;/div&gt;';
    var s=SCEN[state.scen];
    if(s.req==='signup'||state.scen==='signup'){ /* fields already blank in page html */ }
  }

  function addStep(html){
    var d=document.createElement('div'); d.className='wb-step'; d.innerHTML=html;
    $('wb-log').appendChild(d); postHeight(); return d;
  }

  function showStop(){
    var stop=document.createElement('button');
    stop.id='wb-stopbtn'; stop.className='wb-stop'; stop.textContent='Stop';
    stop.onclick=function(){ state.aborted=true; clearTimers(); finishAbort(); };
    $('wb-run').style.display='none';
    $('wb-run').parentNode.appendChild(stop);
  }
  function hideStop(){ var s=$('wb-stopbtn'); if(s) s.remove(); $('wb-run').style.display=''; $('wb-run').disabled=false; }

  function finishAbort(){
    state.running=false; hideStop();
    addStep('&lt;span style=&quot;color:#b91c1c;font-weight:700&quot;&gt;&amp;times;&lt;/span&gt; Stopped by user.');
  }

  function run(){
    if(state.running) return;
    var s = SCEN[state.scen];
    if(s.req==='act' &amp;&amp; state.mode!=='act'){
      $('wb-log').innerHTML='';
      addStep('&lt;span style=&quot;color:#4338ca;font-weight:700&quot;&gt;&amp;#9432;&lt;/span&gt; '+
        'Ask mode is read-only. Switch to &lt;b&gt;Act mode&lt;/b&gt; to run this task.');
      return;
    }
    state.running=true; state.aborted=false;
    $('wb-log').innerHTML=''; $('wb-run').disabled=true; showStop();
    var delay=350;
    s.steps.forEach(function(st){
      timers.push(setTimeout(function(){
        if(state.aborted) return;
        var el=addStep('&lt;span class=&quot;spin&quot;&gt;&lt;/span&gt; '+st.t+' &amp;middot; &lt;code&gt;'+st.c+'&lt;/code&gt;');
        timers.push(setTimeout(function(){
          if(state.aborted) return;
          el.innerHTML='&lt;span class=&quot;tick&quot;&gt;&amp;check;&lt;/span&gt; '+st.t+' &amp;middot; &lt;code&gt;'+st.c+'&lt;/code&gt;';
          if(st.token) addStep('&lt;div class=&quot;wb-token&quot;&gt;Token-conscious: screenshot 2000&amp;times;1200 '+
            'resized &amp;amp; JPEG-compressed &amp;rarr; ~380 image tokens (illustrative).&lt;/div&gt;');
          if(st.fill){ var f=document.getElementById(st.fill.id); if(f){ f.innerHTML=st.fill.v; f.classList.add('fill'); } }
          postHeight();
        }, 420));
      }, delay));
      delay += 780;
    });
    timers.push(setTimeout(function(){
      if(state.aborted) return;
      if(s.approve){ askApproval(s); } else { finish(s); }
    }, delay+150));
  }

  function askApproval(s){
    var box=document.createElement('div'); box.className='wb-approve';
    box.innerHTML='&lt;p&gt;&amp;#9888; Consequential action &amp;mdash; '+s.approve+'&lt;/p&gt;&lt;div class=&quot;row&quot;&gt;'+
      '&lt;button class=&quot;wb-yes&quot;&gt;Approve&lt;/button&gt;&lt;button class=&quot;wb-no&quot;&gt;Cancel&lt;/button&gt;&lt;/div&gt;';
    $('wb-log').appendChild(box); postHeight();
    box.querySelector('.wb-yes').onclick=function(){ box.remove(); finish(s); };
    box.querySelector('.wb-no').onclick=function(){
      box.remove(); state.running=false; hideStop();
      addStep('&lt;span style=&quot;color:#b91c1c;font-weight:700&quot;&gt;&amp;times;&lt;/span&gt; Action cancelled. Nothing was changed.');
    };
  }

  function finish(s){
    state.running=false; hideStop();
    var r=document.createElement('div'); r.className='wb-result'; r.innerHTML=s.result();
    $('wb-log').appendChild(r);
    addStep('&lt;span class=&quot;tick&quot;&gt;&amp;check;&lt;/span&gt; Task complete &amp;middot; &lt;code&gt;done&lt;/code&gt;');
    postHeight();
    if(s.follow){
      var link=document.getElementById('wb-follow');
      if(link) link.onclick=function(e){ e.preventDefault(); runFollow(s); };
    }
  }

  function runFollow(s){
    if(state.running) return; state.running=true; $('wb-run').disabled=true; showStop();
    var d=400;
    s.follow.steps.forEach(function(st){
      timers.push(setTimeout(function(){
        if(state.aborted) return;
        var el=addStep('&lt;span class=&quot;spin&quot;&gt;&lt;/span&gt; '+st.t+' &amp;middot; &lt;code&gt;'+st.c+'&lt;/code&gt;');
        timers.push(setTimeout(function(){
          if(state.aborted) return;
          el.innerHTML='&lt;span class=&quot;tick&quot;&gt;&amp;check;&lt;/span&gt; '+st.t+' &amp;middot; &lt;code&gt;'+st.c+'&lt;/code&gt;';
        },380));
      },d)); d+=650;
    });
    timers.push(setTimeout(function(){
      if(state.aborted) return;
      state.running=false; hideStop();
      var r=document.createElement('div'); r.className='wb-result'; r.innerHTML=s.follow.result;
      $('wb-log').appendChild(r); postHeight();
    }, d+150));
  }

  // mode buttons
  $('wb-modeseg').querySelectorAll('button').forEach(function(b){
    b.onclick=function(){ if(state.running) return; state.mode=b.dataset.mode; renderMode(); };
  });
  $('wb-run').onclick=run;
  $('wb-reset').onclick=function(){ state.mode='ask'; selectScen('products'); };
  $('wb-input').addEventListener('keydown',function(e){ if(e.key==='Enter') run(); });

  // auto-resize: component's own offsetHeight + 40 (never scrollHeight)
  function postHeight(){
    try{
      var el=document.getElementById('wb-root');
      var h=(el?el.offsetHeight:document.body.offsetHeight)+40;
      if(window.parent &amp;&amp; window.parent!==window){ window.parent.postMessage({wbHeight:h},'*'); }
    }catch(e){}
  }
  window.postHeight=postHeight;
  window.addEventListener('load',function(){ postHeight(); setTimeout(postHeight,300); });
  if(window.ResizeObserver){
    try{ new ResizeObserver(postHeight).observe(document.getElementById('wb-root')); }catch(e){}
  }

  // init
  renderChips(); renderMode(); selectScen('products');
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
</div>
<script>
(function(){
  window.addEventListener("message", function(e){
    if(e && e.data && typeof e.data.wbHeight === "number"){
      var f = document.getElementById("webbrain-frame");
      if(f){ f.style.height = e.data.wbHeight + "px"; }
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<h3 id="h-demo-2" class="wp-block-heading"><strong>Demo-</strong>2</h3>



<!-- WebBrain for Developers — interactive demo. Paste into a WordPress Custom HTML block. -->
<div id="webbrain-embed-wrap-2" style="max-width:1080px;margin:0 auto;">
  <iframe id="webbrain-frame-2"
    title="WebBrain for Developers — Interactive Demo"
    loading="lazy"
    style="width:100%;border:0;height:600px;overflow:hidden;"
    srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;WebBrain for Developers — Interactive Demo&lt;/title&gt;
&lt;style&gt;
  #wb-root, #wb-root *{box-sizing:border-box}
  #wb-root{
    --bg:#f5f7fa; --card:#ffffff; --ink:#1f2430; --muted:#6b7280;
    --line:#e5e7eb; --line2:#eef1f5; --accent:#4f8a00; --accent2:#76B900;
    --chip:#f0f4ea; --code:#0f172a; --codebg:#f6f8fa; --ok:#15803d; --warn:#b45309;
    font-family:Inter,-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;
    color:var(--ink); background:var(--bg); border:1px solid var(--line);
    border-radius:16px; padding:18px; max-width:1080px; margin:0 auto;
  }
  #wb-root .wb-head{display:flex;align-items:center;gap:10px;flex-wrap:wrap}
  #wb-root .wb-logo{width:34px;height:34px;border-radius:9px;background:var(--accent2);
    display:flex;align-items:center;justify-content:center;font-size:19px;flex:0 0 auto}
  #wb-root h2{margin:0;font-size:18px;font-weight:700;letter-spacing:-.01em}
  #wb-root .wb-sub{margin:2px 0 0;font-size:12.5px;color:var(--muted)}
  #wb-root .wb-badge{margin-left:auto;font-size:11px;font-weight:600;color:var(--warn);
    background:#fff7ed;border:1px solid #fed7aa;padding:4px 9px;border-radius:999px}
  #wb-root .wb-controls{display:flex;gap:14px;flex-wrap:wrap;align-items:center;margin:16px 0 6px}
  #wb-root .wb-seg{display:inline-flex;background:var(--line2);border-radius:10px;padding:3px}
  #wb-root .wb-seg button{border:0;background:transparent;padding:7px 16px;border-radius:8px;
    font-size:13px;font-weight:600;color:var(--muted);cursor:pointer}
  #wb-root .wb-seg button.on{background:var(--card);color:var(--ink);box-shadow:0 1px 2px rgba(0,0,0,.08)}
  #wb-root .wb-seg button.on[data-mode=&quot;act&quot;]{color:var(--accent)}
  #wb-root .wb-chips{display:flex;gap:8px;flex-wrap:wrap}
  #wb-root .wb-chip{border:1px solid var(--line);background:var(--card);border-radius:999px;
    padding:7px 13px;font-size:12.5px;font-weight:600;color:var(--ink);cursor:pointer;transition:.15s}
  #wb-root .wb-chip:hover{border-color:var(--accent2)}
  #wb-root .wb-chip.on{background:var(--chip);border-color:var(--accent2);color:var(--accent)}
  #wb-root .wb-chip .req{font-weight:600;color:var(--muted);font-size:11px}
  #wb-root .wb-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:12px}
  #wb-root .wb-pane{background:var(--card);border:1px solid var(--line);border-radius:12px;overflow:hidden;
    min-height:340px;display:flex;flex-direction:column}
  #wb-root .wb-bar{display:flex;align-items:center;gap:8px;padding:9px 11px;border-bottom:1px solid var(--line);
    background:#fbfcfe}
  #wb-root .wb-dots{display:flex;gap:5px}
  #wb-root .wb-dot{width:10px;height:10px;border-radius:50%}
  #wb-root .wb-url{flex:1;background:#fff;border:1px solid var(--line);border-radius:7px;padding:5px 10px;
    font-size:12px;color:var(--muted);font-family:&quot;JetBrains Mono&quot;,ui-monospace,Menlo,Consolas,monospace;
    white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  #wb-root .wb-page{padding:14px;font-size:13px;line-height:1.5;flex:1;overflow:auto}
  #wb-root .wb-page pre{background:var(--codebg);border:1px solid var(--line);border-radius:9px;
    padding:11px 12px;font-family:&quot;JetBrains Mono&quot;,ui-monospace,Menlo,Consolas,monospace;font-size:12px;
    line-height:1.55;overflow-x:auto;margin:8px 0;color:#0f172a}
  #wb-root .wb-panehead{display:flex;align-items:center;gap:8px;padding:9px 12px;border-bottom:1px solid var(--line);
    background:#fbfcfe;font-size:12.5px;font-weight:700}
  #wb-root .wb-mode-pill{margin-left:auto;font-size:11px;font-weight:700;padding:3px 9px;border-radius:999px}
  #wb-root .wb-mode-pill.ask{background:#eef2ff;color:#4338ca}
  #wb-root .wb-mode-pill.act{background:#f0f4ea;color:var(--accent)}
  #wb-root .wb-panel{flex:1;display:flex;flex-direction:column;padding:12px;gap:10px;overflow:auto}
  #wb-root .wb-prompt{display:flex;gap:8px;align-items:stretch}
  #wb-root .wb-prompt input{flex:1;border:1px solid var(--line);border-radius:9px;padding:9px 11px;font-size:13px;
    font-family:inherit;color:var(--ink)}
  #wb-root .wb-prompt input:focus{outline:2px solid var(--accent2);outline-offset:0;border-color:transparent}
  #wb-root .wb-run{border:0;background:var(--accent2);color:#0b0b0b;font-weight:700;border-radius:9px;padding:0 16px;
    font-size:13px;cursor:pointer}
  #wb-root .wb-run:disabled{opacity:.5;cursor:not-allowed}
  #wb-root .wb-stop{border:1px solid #fecaca;background:#fef2f2;color:#b91c1c;font-weight:700;border-radius:9px;
    padding:0 14px;font-size:13px;cursor:pointer}
  #wb-root .wb-log{display:flex;flex-direction:column;gap:7px;font-size:12.5px}
  #wb-root .wb-step{display:flex;gap:8px;align-items:flex-start;color:var(--ink);animation:wbfade .25s ease}
  @keyframes wbfade{from{opacity:0;transform:translateY(3px)}to{opacity:1;transform:none}}
  #wb-root .wb-step .tick{color:var(--ok);font-weight:700}
  #wb-root .wb-step code{font-family:&quot;JetBrains Mono&quot;,ui-monospace,Menlo,Consolas,monospace;
    background:var(--codebg);border:1px solid var(--line);border-radius:5px;padding:1px 5px;font-size:11.5px}
  #wb-root .wb-step .spin{display:inline-block;width:11px;height:11px;border:2px solid var(--line);
    border-top-color:var(--accent);border-radius:50%;animation:wbspin .7s linear infinite}
  @keyframes wbspin{to{transform:rotate(360deg)}}
  #wb-root .wb-result{background:#f8faf4;border:1px solid #dcecc4;border-radius:10px;padding:11px 12px;font-size:12.5px;
    line-height:1.5;animation:wbfade .3s ease}
  #wb-root .wb-result h4{margin:0 0 6px;font-size:12.5px;color:var(--accent)}
  #wb-root .wb-result ul{margin:0;padding-left:18px}
  #wb-root .wb-result table{width:100%;border-collapse:collapse;margin-top:4px}
  #wb-root .wb-result td{padding:4px 6px;border-bottom:1px solid #e6efd6;font-size:12px}
  #wb-root .wb-result td:first-child{font-family:&quot;JetBrains Mono&quot;,ui-monospace,Menlo,Consolas,monospace;
    font-weight:700;color:var(--accent);white-space:nowrap}
  #wb-root .wb-result pre{background:#fff;border:1px solid #dcecc4;border-radius:8px;padding:9px 10px;
    font-family:&quot;JetBrains Mono&quot;,ui-monospace,Menlo,Consolas,monospace;font-size:11.5px;overflow-x:auto;margin:6px 0 0}
  #wb-root .wb-token{font-size:11.5px;color:var(--muted);background:#fffbeb;border:1px dashed #fde68a;
    border-radius:8px;padding:7px 9px}
  #wb-root .wb-approve{border:1px solid #fde68a;background:#fffbeb;border-radius:10px;padding:11px 12px}
  #wb-root .wb-approve p{margin:0 0 9px;font-size:12.5px;font-weight:600;color:var(--warn)}
  #wb-root .wb-approve .row{display:flex;gap:8px}
  #wb-root .wb-approve button{border:0;border-radius:8px;padding:7px 15px;font-size:12.5px;font-weight:700;cursor:pointer}
  #wb-root .wb-yes{background:var(--accent2);color:#0b0b0b}
  #wb-root .wb-no{background:#fff;border:1px solid var(--line)!important;color:var(--ink)}
  #wb-root .wb-note{font-size:11.5px;color:var(--muted)}
  #wb-root .wb-hint{color:var(--muted);font-size:12.5px}
  #wb-root .repo{display:flex;align-items:center;gap:8px;font-weight:700;font-size:14px}
  #wb-root .repo .path{font-family:&quot;JetBrains Mono&quot;,ui-monospace,Menlo,Consolas,monospace}
  #wb-root .ep{display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--line2);
    font-family:&quot;JetBrains Mono&quot;,ui-monospace,Menlo,Consolas,monospace;font-size:12px}
  #wb-root .ep b{color:var(--accent)}
  #wb-root .wb-foot{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:14px;padding-top:12px;
    border-top:1px solid var(--line);font-size:11.5px;color:var(--muted)}
  #wb-root .wb-foot a{color:var(--accent);font-weight:700;text-decoration:none}
  #wb-root .wb-foot .brand{margin-left:auto;font-weight:800;color:var(--accent)}
  #wb-root .wb-reset{border:1px solid var(--line);background:#fff;border-radius:8px;padding:5px 11px;font-size:12px;
    font-weight:600;cursor:pointer;color:var(--ink)}
  @media(max-width:720px){#wb-root .wb-grid{grid-template-columns:1fr}}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;wb-root&quot;&gt;
  &lt;div class=&quot;wb-head&quot;&gt;
    &lt;div class=&quot;wb-logo&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f9e0.png" alt="🧠" class="wp-smiley" style="height: 1em; max-height: 1em;" />&lt;/div&gt;
    &lt;div&gt;
      &lt;h2&gt;WebBrain for Developers &amp;mdash; Interactive Demo&lt;/h2&gt;
      &lt;p class=&quot;wb-sub&quot;&gt;Read code, extract API specs, research sources, and act &amp;mdash; from the side panel.&lt;/p&gt;
    &lt;/div&gt;
    &lt;span class=&quot;wb-badge&quot;&gt;Simulated &amp;middot; no real LLM calls&lt;/span&gt;
  &lt;/div&gt;

  &lt;div class=&quot;wb-controls&quot;&gt;
    &lt;div class=&quot;wb-seg&quot; id=&quot;wb-modeseg&quot;&gt;
      &lt;button data-mode=&quot;ask&quot; class=&quot;on&quot;&gt;Ask mode&lt;/button&gt;
      &lt;button data-mode=&quot;act&quot;&gt;Act mode&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;wb-chips&quot; id=&quot;wb-chips&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;wb-grid&quot;&gt;
    &lt;div class=&quot;wb-pane&quot;&gt;
      &lt;div class=&quot;wb-bar&quot;&gt;
        &lt;div class=&quot;wb-dots&quot;&gt;&lt;span class=&quot;wb-dot&quot; style=&quot;background:#f87171&quot;&gt;&lt;/span&gt;
          &lt;span class=&quot;wb-dot&quot; style=&quot;background:#fbbf24&quot;&gt;&lt;/span&gt;
          &lt;span class=&quot;wb-dot&quot; style=&quot;background:#34d399&quot;&gt;&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;wb-url&quot; id=&quot;wb-url&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;wb-page&quot; id=&quot;wb-page&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;wb-pane&quot;&gt;
      &lt;div class=&quot;wb-panehead&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f9e0.png" alt="🧠" class="wp-smiley" style="height: 1em; max-height: 1em;" /> WebBrain
        &lt;span class=&quot;wb-mode-pill ask&quot; id=&quot;wb-modepill&quot;&gt;Ask&lt;/span&gt;
      &lt;/div&gt;
      &lt;div class=&quot;wb-panel&quot;&gt;
        &lt;div class=&quot;wb-prompt&quot;&gt;
          &lt;input id=&quot;wb-input&quot; type=&quot;text&quot; autocomplete=&quot;off&quot;&gt;
          &lt;button class=&quot;wb-run&quot; id=&quot;wb-run&quot;&gt;Run&lt;/button&gt;
        &lt;/div&gt;
        &lt;div class=&quot;wb-log&quot; id=&quot;wb-log&quot;&gt;
          &lt;div class=&quot;wb-hint&quot;&gt;Choose a task above, then press &lt;b&gt;Run&lt;/b&gt;. Read-only Ask mode is the default.&lt;/div&gt;
        &lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;wb-foot&quot;&gt;
    &lt;button class=&quot;wb-reset&quot; id=&quot;wb-reset&quot;&gt;Reset demo&lt;/button&gt;
    &lt;span&gt;Actions here are illustrative and safe. The real extension asks before consequential actions.&lt;/span&gt;
    &lt;span class=&quot;brand&quot;&gt;Built by Marktechpost&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var CH = 'wb-dev-demo';
  var SCEN = {
    explain: {
      label:&quot;Explain code&quot;, req:&quot;ask&quot;,
      url:&quot;https://github.com/acme/router/blob/main/debounce.js&quot;,
      prompt:&quot;Explain what this function does&quot;,
      page:'&lt;div class=&quot;repo&quot;&gt;&lt;span&gt;&amp;#128196;&lt;/span&gt;&lt;span class=&quot;path&quot;&gt;debounce.js&lt;/span&gt;&lt;/div&gt;'+
        '&lt;pre&gt;export function debounce(fn, wait) {\n  let t;\n  return (...args) =&gt; {\n'+
        '    clearTimeout(t);\n    t = setTimeout(() =&gt; fn(...args), wait);\n  };\n}&lt;/pre&gt;'+
        '&lt;p class=&quot;wb-hint&quot;&gt;Source file view&lt;/p&gt;',
      steps:[{t:'Reading file contents', c:'read_page'}],
      result:function(){
        return '&lt;h4&gt;What it does&lt;/h4&gt;&lt;ul&gt;'+
          '&lt;li&gt;Returns a wrapped version of &lt;code&gt;fn&lt;/code&gt; that delays calls.&lt;/li&gt;'+
          '&lt;li&gt;Each new call clears the pending timer and restarts it.&lt;/li&gt;'+
          '&lt;li&gt;&lt;code&gt;fn&lt;/code&gt; runs only after &lt;code&gt;wait&lt;/code&gt; ms of no new calls.&lt;/li&gt;'+
          '&lt;/ul&gt;&lt;p class=&quot;wb-note&quot; style=&quot;margin-top:8px&quot;&gt;Follow-up: '+
          '&lt;a href=&quot;#&quot; id=&quot;wb-follow&quot;&gt;How do I use it?&lt;/a&gt;&lt;/p&gt;';
      },
      follow:{
        steps:[{t:'Preparing an example', c:'scratchpad_write'}],
        result:'&lt;h4&gt;Usage&lt;/h4&gt;&lt;pre&gt;const onSearch = debounce((q) =&gt; {\n  fetchResults(q);\n}, 300);\n\ninput.addEventListener(&quot;input&quot;, e =&gt; onSearch(e.target.value));&lt;/pre&gt;'
      }
    },
    api: {
      label:&quot;Extract API endpoints&quot;, req:&quot;ask&quot;,
      url:&quot;https://docs.acme.dev/api/reference&quot;,
      prompt:&quot;List every endpoint and its method from this reference&quot;,
      page:'&lt;b&gt;Acme API Reference&lt;/b&gt;&lt;p class=&quot;wb-hint&quot;&gt;v1&lt;/p&gt;'+
        '&lt;div class=&quot;ep&quot;&gt;&lt;span&gt;List users&lt;/span&gt;&lt;b&gt;GET /v1/users&lt;/b&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;ep&quot;&gt;&lt;span&gt;Create user&lt;/span&gt;&lt;b&gt;POST /v1/users&lt;/b&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;ep&quot;&gt;&lt;span&gt;Get user&lt;/span&gt;&lt;b&gt;GET /v1/users/{id}&lt;/b&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;ep&quot;&gt;&lt;span&gt;Update user&lt;/span&gt;&lt;b&gt;PATCH /v1/users/{id}&lt;/b&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;ep&quot;&gt;&lt;span&gt;Delete user&lt;/span&gt;&lt;b&gt;DELETE /v1/users/{id}&lt;/b&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;ep&quot;&gt;&lt;span&gt;Health check&lt;/span&gt;&lt;b&gt;GET /v1/health&lt;/b&gt;&lt;/div&gt;',
      steps:[
        {t:'Reading page structure', c:'get_accessibility_tree'},
        {t:'Extracting endpoints', c:'extract_data'}
      ],
      result:function(){
        return '&lt;h4&gt;6 endpoints found&lt;/h4&gt;&lt;table&gt;'+
          '&lt;tr&gt;&lt;td&gt;GET&lt;/td&gt;&lt;td&gt;/v1/users&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;POST&lt;/td&gt;&lt;td&gt;/v1/users&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;GET&lt;/td&gt;&lt;td&gt;/v1/users/{id}&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;PATCH&lt;/td&gt;&lt;td&gt;/v1/users/{id}&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;DELETE&lt;/td&gt;&lt;td&gt;/v1/users/{id}&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;GET&lt;/td&gt;&lt;td&gt;/v1/health&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;/table&gt;&lt;p class=&quot;wb-note&quot; style=&quot;margin-top:7px&quot;&gt;Export as JSON or an OpenAPI stub.&lt;/p&gt;';
      }
    },
    research: {
      label:&quot;Research a topic&quot;, req:&quot;ask&quot;,
      url:&quot;https://blog.statelib.dev/state-in-2026&quot;,
      prompt:&quot;Compare these two state libraries and recommend one&quot;,
      page:'&lt;b&gt;State management in 2026&lt;/b&gt;&lt;p class=&quot;wb-hint&quot;&gt;By A. Rivera &amp;middot; 6 min read&lt;/p&gt;'+
        '&lt;p&gt;Two small libraries dominate new projects: StateLib and Acme Store. '+
        'Both are signal-based. They differ on bundle size, server rendering, and devtools.&lt;/p&gt;'+
        '&lt;p class=&quot;wb-hint&quot;&gt;The agent also reads docs.acme-store.dev in the background.&lt;/p&gt;',
      steps:[
        {t:'Reading blog.statelib.dev', c:'research_url'},
        {t:'Reading docs.acme-store.dev', c:'research_url'}
      ],
      result:function(){
        return '&lt;h4&gt;Summary&lt;/h4&gt;&lt;ul&gt;'+
          '&lt;li&gt;Both are signal-based and ship first-class TypeScript types.&lt;/li&gt;'+
          '&lt;li&gt;StateLib has a smaller core; Acme Store has stronger SSR support.&lt;/li&gt;'+
          '&lt;li&gt;For a server-rendered app, the sources lean Acme Store.&lt;/li&gt;'+
          '&lt;/ul&gt;&lt;p class=&quot;wb-note&quot; style=&quot;margin-top:8px&quot;&gt;Illustrative synthesis from two sources. '+
          '&lt;a href=&quot;#&quot; id=&quot;wb-follow&quot;&gt;Which is smaller?&lt;/a&gt;&lt;/p&gt;';
      },
      follow:{
        steps:[{t:'Checking published bundle sizes', c:'fetch_url'}],
        result:'&lt;h4&gt;Bundle size (illustrative)&lt;/h4&gt;&lt;table&gt;'+
          '&lt;tr&gt;&lt;td&gt;StateLib&lt;/td&gt;&lt;td&gt;3.1 kB min+gzip&lt;/td&gt;&lt;/tr&gt;'+
          '&lt;tr&gt;&lt;td&gt;Acme Store&lt;/td&gt;&lt;td&gt;5.4 kB min+gzip&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;'
      }
    },
    star: {
      label:&quot;Star the repo&quot;, req:&quot;act&quot;,
      url:&quot;https://github.com/acme/router&quot;,
      prompt:&quot;Star this repository&quot;,
      page:'&lt;div class=&quot;repo&quot;&gt;&lt;span&gt;&amp;#128193;&lt;/span&gt;&lt;span class=&quot;path&quot;&gt;acme/router&lt;/span&gt;&lt;/div&gt;'+
        '&lt;p style=&quot;margin:6px 0 10px&quot;&gt;Tiny type-safe router for modern web apps.&lt;/p&gt;'+
        '&lt;span id=&quot;gh-star&quot; style=&quot;display:inline-block;border:1px solid #d0d7de;border-radius:7px;'+
        'padding:6px 12px;font-weight:700;font-size:12.5px;background:#f6f8fa;color:#1f2430&quot;&gt;'+
        '&amp;#9734; Star &amp;middot; 8.4k&lt;/span&gt;'+
        '&lt;p class=&quot;wb-hint&quot; style=&quot;margin-top:10px&quot;&gt;Signed in as you on GitHub.&lt;/p&gt;',
      steps:[
        {t:'Finding the Star control', c:'get_interactive_elements'},
        {t:'Capturing visible tab', c:'screenshot', token:true}
      ],
      approve:&quot;Star this repository on your signed-in GitHub account?&quot;,
      finishStep:{t:'Clicking Star via the page UI', c:'click_ax'},
      onFinish:function(){
        var b=document.getElementById('gh-star');
        if(b){ b.innerHTML='&amp;#9733; Starred &amp;middot; 8.4k';
          b.style.background='#f8faf4'; b.style.borderColor='#cfe6a8'; b.style.color='#4f8a00'; }
      },
      result:function(){
        return '&lt;h4&gt;Done&lt;/h4&gt;&lt;p&gt;Repository starred through the visible UI &amp;mdash; not a direct API call.&lt;/p&gt;'+
          '&lt;p class=&quot;wb-note&quot;&gt;Ran in your authenticated session; no credentials were stored.&lt;/p&gt;';
      }
    }
  };

  var order = ['explain','api','research','star'];
  var state = { mode:'ask', scen:'explain', running:false, aborted:false };
  var timers = [];
  var $ = function(id){ return document.getElementById(id); };
  function clearTimers(){ timers.forEach(clearTimeout); timers = []; }

  function renderChips(){
    var box=$('wb-chips'); box.innerHTML='';
    order.forEach(function(k){
      var s=SCEN[k];
      var b=document.createElement('button');
      b.className='wb-chip'+(k===state.scen?' on':'');
      b.innerHTML=s.label+' &lt;span class=&quot;req&quot;&gt;&amp;middot; '+(s.req==='act'?'Act':'Ask')+'&lt;/span&gt;';
      b.onclick=function(){ selectScen(k); };
      box.appendChild(b);
    });
  }
  function renderMode(){
    $('wb-modeseg').querySelectorAll('button').forEach(function(b){ b.classList.toggle('on', b.dataset.mode===state.mode); });
    var pill=$('wb-modepill'); pill.textContent=state.mode==='act'?'Act':'Ask';
    pill.className='wb-mode-pill '+(state.mode==='act'?'act':'ask');
  }
  function selectScen(k){
    if(state.running) return;
    state.scen=k; var s=SCEN[k];
    $('wb-url').textContent=s.url; $('wb-page').innerHTML=s.page; $('wb-input').value=s.prompt;
    if(s.req==='act' &amp;&amp; state.mode!=='act'){ state.mode='act'; }
    renderChips(); renderMode(); resetLog();
  }
  function resetLog(){
    clearTimers(); state.running=false; state.aborted=false;
    $('wb-run').style.display=''; $('wb-run').disabled=false;
    var stop=$('wb-stopbtn'); if(stop) stop.remove();
    $('wb-log').innerHTML='&lt;div class=&quot;wb-hint&quot;&gt;Press &lt;b&gt;Run&lt;/b&gt; to start. Ask mode is read-only; Act mode can act.&lt;/div&gt;';
  }
  function addStep(html){
    var d=document.createElement('div'); d.className='wb-step'; d.innerHTML=html;
    $('wb-log').appendChild(d); postHeight(); return d;
  }
  function showStop(){
    var stop=document.createElement('button');
    stop.id='wb-stopbtn'; stop.className='wb-stop'; stop.textContent='Stop';
    stop.onclick=function(){ state.aborted=true; clearTimers(); finishAbort(); };
    $('wb-run').style.display='none'; $('wb-run').parentNode.appendChild(stop);
  }
  function hideStop(){ var s=$('wb-stopbtn'); if(s) s.remove(); $('wb-run').style.display=''; $('wb-run').disabled=false; }
  function finishAbort(){ state.running=false; hideStop();
    addStep('&lt;span style=&quot;color:#b91c1c;font-weight:700&quot;&gt;&amp;times;&lt;/span&gt; Stopped by user.'); }

  function run(){
    if(state.running) return;
    var s=SCEN[state.scen];
    if(s.req==='act' &amp;&amp; state.mode!=='act'){
      $('wb-log').innerHTML='';
      addStep('&lt;span style=&quot;color:#4338ca;font-weight:700&quot;&gt;&amp;#9432;&lt;/span&gt; '+
        'Ask mode is read-only. Switch to &lt;b&gt;Act mode&lt;/b&gt; to run this task.');
      return;
    }
    state.running=true; state.aborted=false;
    $('wb-log').innerHTML=''; $('wb-run').disabled=true; showStop();
    var delay=350;
    s.steps.forEach(function(st){
      timers.push(setTimeout(function(){
        if(state.aborted) return;
        var el=addStep('&lt;span class=&quot;spin&quot;&gt;&lt;/span&gt; '+st.t+' &amp;middot; &lt;code&gt;'+st.c+'&lt;/code&gt;');
        timers.push(setTimeout(function(){
          if(state.aborted) return;
          el.innerHTML='&lt;span class=&quot;tick&quot;&gt;&amp;check;&lt;/span&gt; '+st.t+' &amp;middot; &lt;code&gt;'+st.c+'&lt;/code&gt;';
          if(st.token) addStep('&lt;div class=&quot;wb-token&quot;&gt;Token-conscious: screenshot 2000&amp;times;1200 '+
            'resized &amp;amp; JPEG-compressed &amp;rarr; ~380 image tokens (illustrative).&lt;/div&gt;');
          postHeight();
        },420));
      },delay));
      delay+=780;
    });
    timers.push(setTimeout(function(){
      if(state.aborted) return;
      if(s.approve){ askApproval(s); } else { finish(s); }
    }, delay+150));
  }
  function askApproval(s){
    var box=document.createElement('div'); box.className='wb-approve';
    box.innerHTML='&lt;p&gt;&amp;#9888; Consequential action &amp;mdash; '+s.approve+'&lt;/p&gt;&lt;div class=&quot;row&quot;&gt;'+
      '&lt;button class=&quot;wb-yes&quot;&gt;Approve&lt;/button&gt;&lt;button class=&quot;wb-no&quot;&gt;Cancel&lt;/button&gt;&lt;/div&gt;';
    $('wb-log').appendChild(box); postHeight();
    box.querySelector('.wb-yes').onclick=function(){ box.remove(); finish(s); };
    box.querySelector('.wb-no').onclick=function(){
      box.remove(); state.running=false; hideStop();
      addStep('&lt;span style=&quot;color:#b91c1c;font-weight:700&quot;&gt;&amp;times;&lt;/span&gt; Action cancelled. Nothing was changed.');
    };
  }
  function finish(s){
    state.running=false; hideStop();
    if(s.finishStep){ addStep('&lt;span class=&quot;tick&quot;&gt;&amp;check;&lt;/span&gt; '+s.finishStep.t+' &amp;middot; &lt;code&gt;'+s.finishStep.c+'&lt;/code&gt;'); }
    if(s.onFinish){ try{ s.onFinish(); }catch(e){} }
    var r=document.createElement('div'); r.className='wb-result'; r.innerHTML=s.result();
    $('wb-log').appendChild(r);
    addStep('&lt;span class=&quot;tick&quot;&gt;&amp;check;&lt;/span&gt; Task complete &amp;middot; &lt;code&gt;done&lt;/code&gt;');
    postHeight();
    if(s.follow){ var link=document.getElementById('wb-follow'); if(link) link.onclick=function(e){ e.preventDefault(); runFollow(s); }; }
  }
  function runFollow(s){
    if(state.running) return; state.running=true; $('wb-run').disabled=true; showStop();
    var d=400;
    s.follow.steps.forEach(function(st){
      timers.push(setTimeout(function(){
        if(state.aborted) return;
        var el=addStep('&lt;span class=&quot;spin&quot;&gt;&lt;/span&gt; '+st.t+' &amp;middot; &lt;code&gt;'+st.c+'&lt;/code&gt;');
        timers.push(setTimeout(function(){
          if(state.aborted) return;
          el.innerHTML='&lt;span class=&quot;tick&quot;&gt;&amp;check;&lt;/span&gt; '+st.t+' &amp;middot; &lt;code&gt;'+st.c+'&lt;/code&gt;';
        },380));
      },d)); d+=650;
    });
    timers.push(setTimeout(function(){
      if(state.aborted) return;
      state.running=false; hideStop();
      var r=document.createElement('div'); r.className='wb-result'; r.innerHTML=s.follow.result;
      $('wb-log').appendChild(r); postHeight();
    }, d+150));
  }

  $('wb-modeseg').querySelectorAll('button').forEach(function(b){
    b.onclick=function(){ if(state.running) return; state.mode=b.dataset.mode; renderMode(); };
  });
  $('wb-run').onclick=run;
  $('wb-reset').onclick=function(){ state.mode='ask'; selectScen('explain'); };
  $('wb-input').addEventListener('keydown',function(e){ if(e.key==='Enter') run(); });

  function postHeight(){
    try{
      var el=document.getElementById('wb-root');
      var h=(el?el.offsetHeight:document.body.offsetHeight)+40;
      if(window.parent &amp;&amp; window.parent!==window){ window.parent.postMessage({wbHeight:h, ch:CH},'*'); }
    }catch(e){}
  }
  window.postHeight=postHeight;
  window.addEventListener('load',function(){ postHeight(); setTimeout(postHeight,300); });
  if(window.ResizeObserver){ try{ new ResizeObserver(postHeight).observe(document.getElementById('wb-root')); }catch(e){} }

  renderChips(); renderMode(); selectScen('explain');
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
</div>
<script>
(function(){
  window.addEventListener("message", function(e){
    if(e && e.data && e.data.ch === "wb-dev-demo" && typeof e.data.wbHeight === "number"){
      var f = document.getElementById("webbrain-frame-2");
      if(f){ f.style.height = e.data.wbHeight + "px"; }
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"><em><strong>WebBrain is available on the </strong></em><a href="https://pxllnk.co/p4mn8" target="_blank" rel="noreferrer noopener"><strong><em>Chrome Web Store</em></strong></a><em><strong>, </strong></em><a href="https://pxllnk.co/m6k7c5w9" target="_blank" rel="noreferrer noopener"><strong><em>Firefox Add-ons</em></strong></a><em><strong>, and </strong></em><a href="https://pxllnk.co/wdva98c" target="_blank" rel="noreferrer noopener"><strong><em>GitHub</em></strong></a><em><strong>. Product details at </strong></em><a href="https://pxllnk.co/rlifl7h" target="_blank" rel="noreferrer noopener"><strong><em>webbrain website</em></strong></a><em><strong>.</strong></em></p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"><em><sub>Note:Thanks to the Webbrain team for the thought leadership/ Resources for this article. Webbrain team has supported this content/article for promotion.</sub></em></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/02/meet-webbrain-an-open-source-local-first-ai-browser-agent-that-reads-pages-and-automates-tasks-in-chrome-and-firefox/">Meet WebBrain: An Open-Source, Local-First AI Browser Agent That Reads Pages and Automates Tasks in Chrome and Firefox</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80818</post-id>
	</item>
		<item>
		<title>Interfaze Ships diffusion-gemma-asr-small, an Open-Source Diffusion ASR Model Transcribing Six Languages via DiffusionGemma&#8217;s Parallel Denoising Decoder</title>
		<link>https://www.marktechpost.com/2026/07/02/interfaze-ships-diffusion-gemma-asr-small-an-open-source-diffusion-asr-model-transcribing-six-languages-via-diffusiongemmas-parallel-denoising-decoder/</link>
		
		<dc:creator><![CDATA[Michal Sutter]]></dc:creator>
		<pubDate>Fri, 03 Jul 2026 03:24:23 +0000</pubDate>
				<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Audio Language Model]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[TTS]]></category>
		<category><![CDATA[Voice AI]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80815</guid>

					<description><![CDATA[<p>Interfaze open-sourced diffusion-gemma-asr-small, a multilingual ASR model that transcribes via diffusion, not autoregression. It adds audio to Google's frozen DiffusionGemma using a ~42M-parameter adapter. One adapter covers six languages, with transcription cost set by denoising steps, not transcript length.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/02/interfaze-ships-diffusion-gemma-asr-small-an-open-source-diffusion-asr-model-transcribing-six-languages-via-diffusiongemmas-parallel-denoising-decoder/">Interfaze Ships diffusion-gemma-asr-small, an Open-Source Diffusion ASR Model Transcribing Six Languages via DiffusionGemma&#8217;s Parallel Denoising Decoder</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Interfaze, a young YC&#8217;s startup, has open-sourced a new speech recognition model. It is called <code>diffusion-gemma-asr-small</code>. The model transcribes audio through a diffusion decoder, not an autoregressive one. It is described as the first multilingual audio diffusion ASR model. One adapter handles six languages. The research team trained only about 42M parameters on top of a frozen 26B backbone. That is roughly 0.16% of the model&#8217;s weights.</p>



<p class="wp-block-paragraph">Here two terms matter up front. Autoregressive models generate text one token at a time. Diffusion models refine all tokens in parallel. This model uses the diffusion approach for speech-to-text.</p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li>Claimed by the Interfaze team, to be the first open-source multilingual diffusion ASR: six languages from a single ~42M-parameter adapter.</li>



<li>Transcribes via DiffusionGemma&#8217;s diffusion decoder using uniform, random-token diffusion, not the absorbing <code>&lt;mask></code> scheme.</li>



<li>Transcription cost scales with denoising steps, not transcript length.</li>



<li>Leads diffusion peers on LibriSpeech (6.6% WER vs Whisfusion&#8217;s 8.3%) but trails autoregressive Whisper.</li>



<li>The adapter ships under Apache-2.0; DiffusionGemma (Gemma terms) and whisper-small (MIT) load separately.</li>
</ul>



<h2 id="h-what-is-diffusion-gemma-asr-small" class="wp-block-heading"><strong>What is diffusion-gemma-asr-small?</strong></h2>



<p class="wp-block-paragraph"><code>diffusion-gemma-asr-small</code> is an audio-native ASR model. It converts speech to text using a discrete diffusion decoder. That decoder belongs to DiffusionGemma, Google&#8217;s 26B mixture-of-experts model. DiffusionGemma activates 4B parameters, using 128 experts with top-8 routing. It generates text by discrete diffusion instead of autoregression.</p>



<p class="wp-block-paragraph">The diffusion detail is specific. Most diffusion LLMs use an absorbing <code>&lt;mask&gt;</code> scheme. DiffusionGemma uses uniform, random-token diffusion instead. It fills a fixed-length canvas with random vocabulary tokens. Each step keeps confident predictions and re-randomizes the rest. After a few steps the noise anneals into text.</p>



<p class="wp-block-paragraph">Interfaze added audio to this text-only model. Out of the box, DiffusionGemma takes text, images, and video. It does not take audio. The repo ships only the trained adapter, about 42M parameters. The frozen backbones download separately from their own repos.</p>



<h2 id="h-how-it-works" class="wp-block-heading"><strong>How it works</strong></h2>



<p class="wp-block-paragraph">The model does not feed raw waveforms to the LLM. An early attempt tried exactly that and failed. A frozen LLM has never seen a spectrogram. The embedding space has no notion of formants or phonemes. The model learned to ignore audio and hallucinate fluent nonsense.</p>



<p class="wp-block-paragraph">The working design uses a frozen <code>whisper-small</code> encoder. It acts only as a feature extractor, not a decoder. Whisper turns 30 seconds of audio into 1500 frames. Each frame holds 768-dimensional acoustic features. A small trainable projector then compresses these frames. It uses conv layers that subsample 8× plus a linear map. The output is 188 &#8220;audio tokens&#8221; at 2816 dimensions. These tokens scatter into the prompt&#8217;s reserved <code>&lt;|audio|&gt;</code> slots. LoRA adapters let the backbone attend to this new modality. The decoder then denoises a 192-token transcript canvas. It runs bidirectionally over roughly 16 steps.</p>



<p class="wp-block-paragraph">The pipeline, from the model card, is compact:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">raw audio ─► whisper-small encoder (frozen) ─► projector (trained, ~19M)
          ─► scatter into &lt;audio> token slots of DiffusionGemma's encoder
          ─► DiffusionGemma decoder denoises a 192-token canvas (bidirectional, cross-attends audio)
          ─► transcript</code></pre></div></div>



<h2 id="h-the-training-unlock" class="wp-block-heading"><strong>The training unlock</strong></h2>



<p class="wp-block-paragraph">The first training runs stalled. Loss flatlined near 8. The failure was circular. The projector started random, so its output was noise. Attention then learned to ignore it. Almost no gradient reached the projector. The model never learned.</p>



<p class="wp-block-paragraph">The fix supervised the projector directly. The research team ran the 188 audio tokens through DiffusionGemma&#8217;s frozen <code>lm_head</code>. They applied a CTC loss against the transcript. CTC means Connectionist Temporal Classification. It aligns audio features to text without needing attention.</p>



<p class="wp-block-paragraph">This sidesteps the standoff. The audio embeddings became linearly predictive of the right words. CTC loss then dropped from 24 to 8.6 in 300 steps. On LibriSpeech test-clean, English WER fell 90% → 52% → 14.6% → 6.6% over ten epochs.</p>



<h2 id="h-performance-and-benchmarks" class="wp-block-heading"><strong>Performance and benchmarks</strong></h2>



<p class="wp-block-paragraph">WER means Word Error Rate, where lower is better. CER means Character Error Rate. The model trained on FLEURS, LibriSpeech, and VoxPopuli. All scores below use the Whisper text normalizer at 16 diffusion steps.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>benchmark</th><th>metric</th><th>score</th></tr></thead><tbody><tr><td>LibriSpeech test-clean (en)</td><td>WER</td><td>6.6%</td></tr><tr><td>FLEURS English</td><td>WER</td><td>15.7%</td></tr><tr><td>VoxPopuli English</td><td>WER</td><td>18.5%</td></tr><tr><td>FLEURS Hindi</td><td>CER</td><td>15.8%</td></tr><tr><td>FLEURS Mandarin</td><td>CER</td><td>29.6%</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">Against other diffusion or non-autoregressive ASR, it leads.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th>model</th><th>approach</th><th>LibriSpeech test-clean</th></tr></thead><tbody><tr><td>TransFusion (2022)</td><td>multinomial diffusion</td><td>~6–7% (proof-of-concept)</td></tr><tr><td>Whisfusion (Aug 2025)</td><td>Whisper-large-v3 + masked diffusion</td><td>8.3%</td></tr><tr><td><strong>diffusion-gemma-asr-small</strong> (2026)</td><td>Whisper-small + DiffusionGemma</td><td><strong>6.6%</strong></td></tr></tbody></table></figure>



<p class="wp-block-paragraph">Against autoregressive Whisper, it trails. The team frames this gap as data, not architecture.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th>benchmark</th><th>ours</th><th>Whisper-small</th><th>Whisper-large-v3</th></tr></thead><tbody><tr><td>LibriSpeech clean</td><td>6.6%</td><td>~3.4%</td><td>~2.0%</td></tr><tr><td>FLEURS-en</td><td>15.7%</td><td>~9–10%</td><td>~4–5%</td></tr><tr><td>VoxPopuli-en</td><td>18.5%</td><td>~9–11%</td><td>~7–10%</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">The denoising-step sweep shows a nearly flat curve.</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th>steps</th><th>FLEURS-en WER</th><th>speed</th></tr></thead><tbody><tr><td>8</td><td>15.7%</td><td>14.9× real-time</td></tr><tr><td>16</td><td>15.6%</td><td>10.3×</td></tr><tr><td>32</td><td>15.2%</td><td>6.5×</td></tr><tr><td>48</td><td>15.6%</td><td>4.7×</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">Going from 8 to 48 steps buys about 0.1 WER point. It costs roughly 3× the latency. The model converges in about 8 parallel passes. That is around 0.7–1.5s of model time for a 10-second clip.</p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use cases with examples</strong></h2>



<ul class="wp-block-list">
<li>Batch transcription pipelines benefit from parallel decoding. Cost is set by denoising steps, not clip length. A 10-second clip needs roughly the same passes as a shorter one.</li>



<li>Multilingual transcription runs from a single adapter. It covers English, German, French, Spanish, Hindi, and Mandarin. Teams avoid loading a separate model per language.</li>



<li>Non-autoregressive ASR research gains a reproducible baseline. The recipe grounds a frozen LLM with a small adapter. Researchers can extend it with more audio or a larger encoder.</li>
</ul>



<h2 id="h-how-to-get-started" class="wp-block-heading"><strong>How to get started</strong></h2>



<p class="wp-block-paragraph">The model lives on the Hub. It ships the adapter, <code>model.py</code>, <code>audio.py</code>, and a runnable <code>inference.py</code>. DiffusionGemma support needs <code>transformers</code> from main.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">pip install torch peft soundfile librosa huggingface_hub \
  "transformers @ git+https://github.com/huggingface/transformers.git"</code></pre></div></div>



<p class="wp-block-paragraph">Then transcribe in Python:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import sys, soundfile as sf
from huggingface_hub import snapshot_download

repo = snapshot_download("interfaze-ai/diffusion-gemma-asr-small")   # adapter, ~170 MB
sys.path.insert(0, repo)
from inference import load, transcribe

# Loads frozen DiffusionGemma-26B + whisper-small + this adapter.
model, tok, fe = load(f"{repo}/diffusion_asr_small.pt", device="cuda")

wav, sr = sf.read("audio.wav")   # 16 kHz mono float32
print(transcribe(wav, model, tok, fe, max_steps=16))</code></pre></div></div>



<p class="wp-block-paragraph">A command-line path also works from inside the downloaded repo:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">python inference.py audio.wav
</code></pre></div></div>



<p class="wp-block-paragraph">The <code>max_steps</code> argument trades speed for accuracy. The team notes 8 is near-best and fastest. The default is 16. The base models load under their own licenses: DiffusionGemma under Gemma terms, whisper-small under MIT.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- Marktechpost — Diffusion ASR Denoising Visualizer (paste into a Custom HTML block) -->
<iframe id="mtp-diffusion-asr"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;UTF-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;
&lt;title&gt;Diffusion ASR — Denoising Visualizer&lt;/title&gt;
&lt;link rel=&quot;preconnect&quot; href=&quot;https://fonts.googleapis.com&quot;&gt;
&lt;link rel=&quot;preconnect&quot; href=&quot;https://fonts.gstatic.com&quot; crossorigin&gt;
&lt;link href=&quot;https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&amp;family=JetBrains+Mono:wght@400;500;700&amp;display=swap&quot; rel=&quot;stylesheet&quot;&gt;
&lt;style&gt;
  :root{
    --bg:#F4F6F9; --surface:#FFFFFF; --ink:#14181F; --muted:#5A6472;
    --border:#E4E8EF; --violet:#6B46E5; --violet-soft:#EDE9FE; --violet-ink:#4C33B0;
    --noise:#A8B0BE; --teal:#0E9F9F; --mtp:#76B900;
    --font:'Inter',system-ui,-apple-system,Segoe UI,Roboto,sans-serif;
    --mono:'JetBrains Mono',ui-monospace,SFMono-Regular,Menlo,monospace;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  html,body{background:transparent}
  #app{
    font-family:var(--font); color:var(--ink); background:var(--bg);
    max-width:920px; margin:0 auto; padding:22px 20px 16px;
    border:1px solid var(--border); border-radius:16px;
    -webkit-font-smoothing:antialiased;
  }
  .eyebrow{
    font-family:var(--mono); font-size:11px; letter-spacing:.14em; text-transform:uppercase;
    color:var(--violet); font-weight:700; display:flex; align-items:center; gap:8px;
  }
  .eyebrow::before{content:&quot;&quot;; width:22px; height:2px; background:var(--violet); display:inline-block}
  h1{font-size:23px; line-height:1.2; font-weight:700; margin:10px 0 4px; letter-spacing:-.02em}
  .sub{color:var(--muted); font-size:13.5px; line-height:1.5; max-width:640px}

  /* pipeline strip */
  .pipe{
    display:flex; align-items:stretch; gap:6px; margin:18px 0 4px; flex-wrap:wrap;
  }
  .pnode{
    flex:1 1 0; min-width:118px; background:var(--surface); border:1px solid var(--border);
    border-radius:10px; padding:9px 11px; position:relative;
  }
  .pnode .k{font-family:var(--mono); font-size:9.5px; letter-spacing:.08em; color:var(--muted); text-transform:uppercase}
  .pnode .v{font-size:12.5px; font-weight:600; margin-top:3px; line-height:1.25}
  .pnode .tag{font-size:10.5px; color:var(--muted); margin-top:2px}
  .pnode.locked .v{color:var(--violet-ink)}
  .arrow{display:flex; align-items:center; color:var(--noise); font-weight:700; font-size:15px}
  @media(max-width:640px){ .arrow{display:none} .pnode{flex:1 1 46%} }

  /* controls */
  .controls{display:flex; flex-wrap:wrap; gap:14px; align-items:flex-end; margin:20px 0 6px}
  .cgroup{display:flex; flex-direction:column; gap:7px}
  .clabel{font-family:var(--mono); font-size:10px; letter-spacing:.1em; text-transform:uppercase; color:var(--muted)}
  .pills{display:flex; gap:6px; flex-wrap:wrap}
  .pill{
    font-family:var(--font); font-size:12.5px; font-weight:500; color:var(--ink);
    background:var(--surface); border:1px solid var(--border); border-radius:999px;
    padding:6px 12px; cursor:pointer; transition:all .15s ease; line-height:1;
  }
  .pill:hover{border-color:var(--violet)}
  .pill[aria-pressed=&quot;true&quot;]{background:var(--violet); border-color:var(--violet); color:#fff}
  .pill.step[aria-pressed=&quot;true&quot;]{background:var(--violet)}
  .actions{display:flex; gap:8px; margin-left:auto}
  .btn{
    font-family:var(--font); font-weight:600; font-size:13px; cursor:pointer;
    border-radius:9px; padding:9px 16px; border:1px solid var(--violet); transition:all .15s ease;
  }
  .btn.play{background:var(--violet); color:#fff}
  .btn.play:hover{background:var(--violet-ink)}
  .btn.play:disabled{opacity:.5; cursor:default}
  .btn.reset{background:var(--surface); color:var(--violet)}
  .btn.reset:hover{background:var(--violet-soft)}

  /* canvas */
  .stage{
    background:var(--surface); border:1px solid var(--border); border-radius:12px;
    padding:16px 16px 14px; margin-top:14px;
  }
  .stage-top{display:flex; justify-content:space-between; align-items:center; margin-bottom:12px; gap:12px}
  .stage-title{font-family:var(--mono); font-size:11px; letter-spacing:.06em; color:var(--muted); text-transform:uppercase}
  .prog-wrap{flex:1; height:5px; background:var(--border); border-radius:999px; overflow:hidden; max-width:260px}
  .prog{height:100%; width:0%; background:linear-gradient(90deg,var(--violet),#9F7AEA); transition:width .12s linear}
  .canvas{
    display:flex; flex-wrap:wrap; gap:7px; min-height:96px; align-content:flex-start;
  }
  .tok{
    font-family:var(--mono); font-size:14px; font-weight:500; padding:7px 10px; border-radius:8px;
    background:#F2F3F6; color:var(--noise); border:1px solid var(--border);
    transition:background .18s ease,color .18s ease,border-color .18s ease,transform .18s ease;
    user-select:none;
  }
  .tok.locking{background:var(--violet-soft); color:var(--violet); border-color:#C9BCF7; transform:translateY(-1px)}
  .tok.locked{background:#fff; color:var(--ink); border-color:var(--violet-soft)}
  .caption{font-size:11.5px; color:var(--muted); margin-top:12px; line-height:1.5}

  /* readouts */
  .reads{display:grid; grid-template-columns:repeat(4,1fr); gap:10px; margin-top:14px}
  @media(max-width:640px){ .reads{grid-template-columns:repeat(2,1fr)} }
  .read{background:var(--surface); border:1px solid var(--border); border-radius:11px; padding:12px 13px}
  .read .k{font-family:var(--mono); font-size:9.5px; letter-spacing:.08em; text-transform:uppercase; color:var(--muted)}
  .read .v{font-size:22px; font-weight:700; margin-top:5px; letter-spacing:-.02em; font-variant-numeric:tabular-nums}
  .read .v small{font-size:12px; font-weight:600; color:var(--muted)}
  .read.rtf .v{color:var(--teal)}
  .read.wer .v{color:var(--violet-ink)}

  /* tradeoff */
  .trade{background:var(--surface); border:1px solid var(--border); border-radius:12px; padding:15px 16px; margin-top:14px}
  .trade h3{font-size:13px; font-weight:700; margin-bottom:3px}
  .trade p{font-size:11.5px; color:var(--muted); line-height:1.5; margin-bottom:14px}
  .bars{display:grid; grid-template-columns:repeat(4,1fr); gap:14px; align-items:end}
  .barcol{display:flex; flex-direction:column; align-items:center; gap:8px; cursor:pointer}
  .barpair{display:flex; gap:5px; align-items:flex-end; height:104px}
  .bar{width:20px; border-radius:5px 5px 0 0; position:relative; transition:opacity .15s ease}
  .bar.w{background:var(--violet)}
  .bar.s{background:var(--teal)}
  .bar span{position:absolute; top:-16px; left:50%; transform:translateX(-50%); font-family:var(--mono); font-size:9px; color:var(--muted); white-space:nowrap}
  .barcol .xlab{font-family:var(--mono); font-size:11px; color:var(--muted)}
  .barcol.sel .xlab{color:var(--violet); font-weight:700}
  .barcol:not(.sel) .barpair{opacity:.42}
  .legend{display:flex; gap:16px; margin-top:12px; font-size:11px; color:var(--muted)}
  .legend i{display:inline-block; width:10px; height:10px; border-radius:3px; margin-right:5px; vertical-align:-1px}

  /* footer */
  .foot{display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:10px; margin-top:16px; padding-top:13px; border-top:1px solid var(--border)}
  .foot .src{font-size:11px; color:var(--muted); line-height:1.5}
  .foot .src a{color:var(--violet); text-decoration:none}
  .brand{font-family:var(--mono); font-size:11px; font-weight:700; color:var(--mtp); display:flex; align-items:center; gap:7px; white-space:nowrap}
  .brand::before{content:&quot;&quot;; width:9px; height:9px; border-radius:2px; background:var(--mtp); display:inline-block}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;app&quot;&gt;
  &lt;div class=&quot;eyebrow&quot;&gt;Interactive · Diffusion ASR&lt;/div&gt;
  &lt;h1&gt;Watch a transcript denoise, one parallel step at a time&lt;/h1&gt;
  &lt;p class=&quot;sub&quot;&gt;
    &lt;b&gt;diffusion-gemma-asr-small&lt;/b&gt; transcribes by denoising a 192-token canvas of random vocabulary
    tokens into text — all positions at once. Pick a language, set the denoising steps, and press play.
  &lt;/p&gt;

  &lt;!-- pipeline --&gt;
  &lt;div class=&quot;pipe&quot; id=&quot;pipe&quot;&gt;
    &lt;div class=&quot;pnode&quot; data-stage=&quot;0&quot;&gt;&lt;div class=&quot;k&quot;&gt;Input&lt;/div&gt;&lt;div class=&quot;v&quot;&gt;Raw audio&lt;/div&gt;&lt;div class=&quot;tag&quot;&gt;16 kHz mono&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;arrow&quot;&gt;→&lt;/div&gt;
    &lt;div class=&quot;pnode&quot; data-stage=&quot;1&quot;&gt;&lt;div class=&quot;k&quot;&gt;Frozen encoder&lt;/div&gt;&lt;div class=&quot;v&quot;&gt;whisper-small&lt;/div&gt;&lt;div class=&quot;tag&quot;&gt;1500 × 768-dim&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;arrow&quot;&gt;→&lt;/div&gt;
    &lt;div class=&quot;pnode&quot; data-stage=&quot;2&quot;&gt;&lt;div class=&quot;k&quot;&gt;Trained ~19M&lt;/div&gt;&lt;div class=&quot;v&quot;&gt;Projector&lt;/div&gt;&lt;div class=&quot;tag&quot;&gt;188 audio tokens&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;arrow&quot;&gt;→&lt;/div&gt;
    &lt;div class=&quot;pnode&quot; data-stage=&quot;3&quot;&gt;&lt;div class=&quot;k&quot;&gt;Frozen 26B&lt;/div&gt;&lt;div class=&quot;v&quot;&gt;DiffusionGemma&lt;/div&gt;&lt;div class=&quot;tag&quot;&gt;denoises canvas&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;arrow&quot;&gt;→&lt;/div&gt;
    &lt;div class=&quot;pnode&quot; data-stage=&quot;4&quot;&gt;&lt;div class=&quot;k&quot;&gt;Output&lt;/div&gt;&lt;div class=&quot;v&quot;&gt;Transcript&lt;/div&gt;&lt;div class=&quot;tag&quot;&gt;parallel decode&lt;/div&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- controls --&gt;
  &lt;div class=&quot;controls&quot;&gt;
    &lt;div class=&quot;cgroup&quot;&gt;
      &lt;span class=&quot;clabel&quot;&gt;Language&lt;/span&gt;
      &lt;div class=&quot;pills&quot; id=&quot;langPills&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;cgroup&quot;&gt;
      &lt;span class=&quot;clabel&quot;&gt;Denoising steps&lt;/span&gt;
      &lt;div class=&quot;pills&quot; id=&quot;stepPills&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;actions&quot;&gt;
      &lt;button class=&quot;btn reset&quot; id=&quot;resetBtn&quot;&gt;Reset&lt;/button&gt;
      &lt;button class=&quot;btn play&quot; id=&quot;playBtn&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/25b6.png" alt="▶" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Denoise&lt;/button&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- stage --&gt;
  &lt;div class=&quot;stage&quot;&gt;
    &lt;div class=&quot;stage-top&quot;&gt;
      &lt;span class=&quot;stage-title&quot;&gt;Transcript canvas&lt;/span&gt;
      &lt;div class=&quot;prog-wrap&quot;&gt;&lt;div class=&quot;prog&quot; id=&quot;prog&quot;&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;canvas&quot; id=&quot;canvas&quot;&gt;&lt;/div&gt;
    &lt;p class=&quot;caption&quot;&gt;
      Illustrative visualization of parallel diffusion denoising: confident tokens lock, the rest re-randomize each step.
      The example transcript is fixed — this animation is not live model inference.
    &lt;/p&gt;
  &lt;/div&gt;

  &lt;!-- readouts --&gt;
  &lt;div class=&quot;reads&quot;&gt;
    &lt;div class=&quot;read&quot;&gt;&lt;div class=&quot;k&quot;&gt;Step&lt;/div&gt;&lt;div class=&quot;v&quot; id=&quot;rStep&quot;&gt;0 &lt;small&gt;/ 16&lt;/small&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;read&quot;&gt;&lt;div class=&quot;k&quot;&gt;Tokens locked&lt;/div&gt;&lt;div class=&quot;v&quot; id=&quot;rLocked&quot;&gt;0 &lt;small&gt;/ 0&lt;/small&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;read rtf&quot;&gt;&lt;div class=&quot;k&quot;&gt;Real-time factor&lt;/div&gt;&lt;div class=&quot;v&quot; id=&quot;rRtf&quot;&gt;10.3×&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;read wer&quot;&gt;&lt;div class=&quot;k&quot;&gt;FLEURS-en WER&lt;/div&gt;&lt;div class=&quot;v&quot; id=&quot;rWer&quot;&gt;15.6%&lt;/div&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- tradeoff --&gt;
  &lt;div class=&quot;trade&quot;&gt;
    &lt;h3&gt;Steps vs. accuracy vs. speed&lt;/h3&gt;
    &lt;p&gt;More denoising steps barely move accuracy but cost throughput. The model converges in about 8 parallel passes. Figures are Interfaze's published FLEURS-en benchmarks.&lt;/p&gt;
    &lt;div class=&quot;bars&quot; id=&quot;bars&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;legend&quot;&gt;
      &lt;span&gt;&lt;i style=&quot;background:var(--violet)&quot;&gt;&lt;/i&gt;WER (lower is better)&lt;/span&gt;
      &lt;span&gt;&lt;i style=&quot;background:var(--teal)&quot;&gt;&lt;/i&gt;Real-time factor (higher is faster)&lt;/span&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- footer --&gt;
  &lt;div class=&quot;foot&quot;&gt;
    &lt;div class=&quot;src&quot;&gt;
      Model: &lt;a href=&quot;https://huggingface.co/interfaze-ai/diffusion-gemma-asr-small&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;interfaze-ai/diffusion-gemma-asr-small&lt;/a&gt;
      · &lt;a href=&quot;https://interfaze.ai/blog/the-first-open-source-diffusion-audio-asr-model&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Interfaze blog&lt;/a&gt;
      · &lt;a href=&quot;https://huggingface.co/spaces/interfaze-ai/diffusion-gemma-asr-demo&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Live demo&lt;/a&gt;
    &lt;/div&gt;
    &lt;div class=&quot;brand&quot;&gt;Marktechpost&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  // ---- data (all figures from Interfaze's published benchmarks) ----
  var STEP_DATA = {
    8:  {rtf:&quot;14.9×&quot;, wer:&quot;15.7%&quot;, werNum:15.7, speedNum:14.9},
    16: {rtf:&quot;10.3×&quot;, wer:&quot;15.6%&quot;, werNum:15.6, speedNum:10.3},
    32: {rtf:&quot;6.5×&quot;,  wer:&quot;15.2%&quot;, werNum:15.2, speedNum:6.5},
    48: {rtf:&quot;4.7×&quot;,  wer:&quot;15.6%&quot;, werNum:15.6, speedNum:4.7}
  };
  var STEP_LIST = [8,16,32,48];

  var SAMPLES = {
    &quot;English&quot;:  [&quot;the&quot;,&quot;diffusion&quot;,&quot;decoder&quot;,&quot;refines&quot;,&quot;every&quot;,&quot;token&quot;,&quot;in&quot;,&quot;parallel&quot;],
    &quot;German&quot;:   [&quot;der&quot;,&quot;diffusions&quot;,&quot;decoder&quot;,&quot;verarbeitet&quot;,&quot;alle&quot;,&quot;token&quot;,&quot;gleichzeitig&quot;],
    &quot;French&quot;:   [&quot;le&quot;,&quot;décodeur&quot;,&quot;de&quot;,&quot;diffusion&quot;,&quot;affine&quot;,&quot;chaque&quot;,&quot;jeton&quot;,&quot;en&quot;,&quot;parallèle&quot;],
    &quot;Spanish&quot;:  [&quot;el&quot;,&quot;decodificador&quot;,&quot;de&quot;,&quot;difusión&quot;,&quot;refina&quot;,&quot;cada&quot;,&quot;token&quot;,&quot;en&quot;,&quot;paralelo&quot;],
    &quot;Hindi&quot;:    [&quot;डिफ्यूज़न&quot;,&quot;डिकोडर&quot;,&quot;सभी&quot;,&quot;टोकन&quot;,&quot;एक&quot;,&quot;साथ&quot;,&quot;परिष्कृत&quot;,&quot;करता&quot;,&quot;है&quot;],
    &quot;Mandarin&quot;: [&quot;扩散&quot;,&quot;解码器&quot;,&quot;并行&quot;,&quot;地&quot;,&quot;细化&quot;,&quot;每个&quot;,&quot;词元&quot;]
  };
  var LANGS = Object.keys(SAMPLES);

  // pool of random &quot;vocabulary&quot; fragments to fill noisy slots
  var NOISE = [&quot;▁the&quot;,&quot;##ion&quot;,&quot;▁der&quot;,&quot;ка&quot;,&quot;的&quot;,&quot;##ing&quot;,&quot;▁le&quot;,&quot;xq&quot;,&quot;▁el&quot;,&quot;zj&quot;,&quot;##er&quot;,&quot;भ&quot;,&quot;并&quot;,
    &quot;▁und&quot;,&quot;pф&quot;,&quot;##tok&quot;,&quot;了&quot;,&quot;▁de&quot;,&quot;wм&quot;,&quot;##ité&quot;,&quot;क&quot;,&quot;器&quot;,&quot;▁par&quot;,&quot;##allel&quot;,&quot;zx&quot;,&quot;ство&quot;,&quot;##子&quot;,
    &quot;▁jeton&quot;,&quot;的&quot;,&quot;ん&quot;,&quot;##ко&quot;,&quot;▁re&quot;,&quot;qv&quot;,&quot;元&quot;,&quot;##ня&quot;,&quot;▁si&quot;,&quot;ต&quot;,&quot;##라&quot;,&quot;▁ль&quot;];

  // ---- state ----
  var state = { lang:&quot;English&quot;, steps:16, running:false, timer:null };

  var canvasEl = document.getElementById('canvas');
  var progEl   = document.getElementById('prog');
  var playBtn  = document.getElementById('playBtn');
  var resetBtn = document.getElementById('resetBtn');
  var rStep=document.getElementById('rStep'), rLocked=document.getElementById('rLocked'),
      rRtf=document.getElementById('rRtf'), rWer=document.getElementById('rWer');
  var pipeNodes = document.querySelectorAll('#pipe .pnode');

  function rnd(a){ return a[Math.floor(Math.random()*a.length)]; }
  function postSize(){
    try{ var h=document.getElementById('app').offsetHeight+40;
      parent.postMessage({type:'resize', frameHeight:h, height:h},'*'); }catch(e){}
  }

  // ---- build controls ----
  function buildPills(containerId, items, current, cls, onPick){
    var c=document.getElementById(containerId); c.innerHTML=&quot;&quot;;
    items.forEach(function(it){
      var b=document.createElement('button');
      b.className=&quot;pill&quot;+(cls?(&quot; &quot;+cls):&quot;&quot;);
      b.textContent = (cls===&quot;step&quot;) ? it : it;
      b.setAttribute('aria-pressed', String(it===current));
      b.onclick=function(){ if(state.running) return; onPick(it);
        Array.prototype.forEach.call(c.children,function(x){x.setAttribute('aria-pressed','false');});
        b.setAttribute('aria-pressed','true'); };
      c.appendChild(b);
    });
  }

  function buildBars(){
    var host=document.getElementById('bars'); host.innerHTML=&quot;&quot;;
    var werMax=20, spdMax=16; // honest axes: WER near-flat, speed clearly declining
    STEP_LIST.forEach(function(s){
      var d=STEP_DATA[s];
      var col=document.createElement('div'); col.className=&quot;barcol&quot;+(s===state.steps?&quot; sel&quot;:&quot;&quot;);
      col.setAttribute('data-step', s);
      var pair=document.createElement('div'); pair.className=&quot;barpair&quot;;
      var wh=Math.round(d.werNum/werMax*100);
      var sh=Math.round(d.speedNum/spdMax*100);
      pair.innerHTML =
        '&lt;div class=&quot;bar w&quot; style=&quot;height:'+wh+'%&quot;&gt;&lt;span&gt;'+d.wer+'&lt;/span&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;bar s&quot; style=&quot;height:'+sh+'%&quot;&gt;&lt;span&gt;'+d.rtf+'&lt;/span&gt;&lt;/div&gt;';
      var x=document.createElement('div'); x.className=&quot;xlab&quot;; x.textContent=s;
      col.appendChild(pair); col.appendChild(x);
      col.onclick=function(){ if(state.running) return; selectSteps(s);
        document.querySelectorAll('#stepPills .pill').forEach(function(p){
          p.setAttribute('aria-pressed', String(parseInt(p.textContent,10)===s)); }); };
      host.appendChild(col);
    });
  }

  function markSelectedBar(){
    document.querySelectorAll('#bars .barcol').forEach(function(c){
      c.classList.toggle('sel', parseInt(c.getAttribute('data-step'),10)===state.steps);
    });
  }

  // ---- render / animation ----
  function buildCanvas(){
    canvasEl.innerHTML=&quot;&quot;;
    var words=SAMPLES[state.lang];
    words.forEach(function(w,i){
      var t=document.createElement('span'); t.className=&quot;tok&quot;; t.setAttribute('data-i',i);
      t.setAttribute('data-word',w); t.textContent=rnd(NOISE);
      canvasEl.appendChild(t);
    });
  }

  function resetView(){
    stop();
    buildCanvas();
    var d=STEP_DATA[state.steps], n=SAMPLES[state.lang].length;
    progEl.style.width=&quot;0%&quot;;
    rStep.innerHTML='0 &lt;small&gt;/ '+state.steps+'&lt;/small&gt;';
    rLocked.innerHTML='0 &lt;small&gt;/ '+n+'&lt;/small&gt;';
    rRtf.textContent=d.rtf; rWer.textContent=d.wer;
    pipeNodes.forEach(function(p){p.classList.remove('locked');});
    playBtn.disabled=false; playBtn.textContent=&quot;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/25b6.png" alt="▶" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Denoise&quot;;
    postSize();
  }

  function lockSchedule(n, steps){
    // fraction locked after step k (ease-out), then a fixed random reveal order
    var order=[]; for(var i=0;i&lt;n;i++) order.push(i);
    for(var j=order.length-1;j&gt;0;j--){var r=Math.floor(Math.random()*(j+1)); var tmp=order[j];order[j]=order[r];order[r]=tmp;}
    var perStep=[]; // number locked target at each step
    for(var k=1;k&lt;=steps;k++){
      var f=1-Math.pow(1-k/steps,1.6);
      perStep.push(Math.min(n, Math.round(f*n)));
    }
    perStep[steps-1]=n;
    return {order:order, perStep:perStep};
  }

  function play(){
    if(state.running) return;
    state.running=true; playBtn.disabled=true; playBtn.textContent=&quot;Denoising…&quot;;
    setPillsDisabled(true);
    var words=SAMPLES[state.lang], n=words.length, steps=state.steps;
    var toks=canvasEl.querySelectorAll('.tok');
    var sched=lockSchedule(n, steps);
    var lockedSet={}; var k=0;
    var dur = steps&lt;=16 ? 140 : 95;

    state.timer=setInterval(function(){
      k++;
      var target=sched.perStep[k-1];
      // lock next tokens up to target
      var lockedCount=Object.keys(lockedSet).length;
      while(lockedCount&lt;target){
        var idx=sched.order[lockedCount];
        lockedSet[idx]=true; lockedCount++;
        (function(el){ el.textContent=el.getAttribute('data-word');
          el.classList.add('locking');
          setTimeout(function(){ el.classList.remove('locking'); el.classList.add('locked'); },180);
        })(toks[idx]);
      }
      // re-randomize still-noisy tokens
      for(var i=0;i&lt;n;i++){ if(!lockedSet[i]){ toks[i].textContent=rnd(NOISE); } }
      // pipeline pulse
      pipeNodes.forEach(function(p,pi){ p.classList.toggle('locked', pi&lt;=Math.min(4, Math.floor(k/steps*4)+1)); });
      // readouts
      progEl.style.width=Math.round(k/steps*100)+&quot;%&quot;;
      rStep.innerHTML=k+' &lt;small&gt;/ '+steps+'&lt;/small&gt;';
      rLocked.innerHTML=Object.keys(lockedSet).length+' &lt;small&gt;/ '+n+'&lt;/small&gt;';

      if(k&gt;=steps){ stop(); pipeNodes.forEach(function(p){p.classList.add('locked');});
        playBtn.textContent=&quot;✓ Done&quot;; playBtn.disabled=false; setPillsDisabled(false); postSize(); }
    }, dur);
  }

  function stop(){ if(state.timer){clearInterval(state.timer); state.timer=null;} state.running=false; }
  function setPillsDisabled(v){
    document.querySelectorAll('.pill').forEach(function(p){ p.style.pointerEvents=v?'none':'auto'; p.style.opacity=v?.6:1; });
  }

  function selectLang(l){ state.lang=l; resetView(); }
  function selectSteps(s){ state.steps=s; markSelectedBar(); resetView(); }

  // ---- init ----
  buildPills('langPills', LANGS, state.lang, &quot;&quot;, selectLang);
  buildPills('stepPills', STEP_LIST, state.steps, &quot;step&quot;, selectSteps);
  buildBars();
  playBtn.onclick=play;
  resetBtn.onclick=resetView;
  resetView();
  window.addEventListener('load', postSize);
  window.addEventListener('resize', postSize);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"
  title="Diffusion ASR Denoising Visualizer"
  scrolling="no" loading="lazy"
  style="width:100%;border:0;height:940px;overflow:hidden;display:block"></iframe>
<script>
(function(){
  window.addEventListener("message",function(e){
    if(e && e.data && e.data.type==="resize"){
      var h = e.data.frameHeight || e.data.height;
      var f = document.getElementById("mtp-diffusion-asr");
      if(f && h){ f.style.height = h + "px"; }
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph">Check out the<strong> <a href="https://huggingface.co/interfaze-ai/diffusion-gemma-asr-small" target="_blank" rel="noreferrer noopener">Model weights</a> </strong>and <strong><a href="https://interfaze.ai/blog/the-first-open-source-diffusion-audio-asr-model" target="_blank" rel="noreferrer noopener">Technical details</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/02/interfaze-ships-diffusion-gemma-asr-small-an-open-source-diffusion-asr-model-transcribing-six-languages-via-diffusiongemmas-parallel-denoising-decoder/">Interfaze Ships diffusion-gemma-asr-small, an Open-Source Diffusion ASR Model Transcribing Six Languages via DiffusionGemma&#8217;s Parallel Denoising Decoder</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80815</post-id>
	</item>
		<item>
		<title>RAG-Anything Tutorial: Build a Multimodal Retrieval Pipeline for Text, Tables, Equations, and Images in Colab</title>
		<link>https://www.marktechpost.com/2026/07/02/rag-anything-tutorial-build-a-multimodal-retrieval-pipeline-for-text-tables-equations-and-images-in-colab/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Thu, 02 Jul 2026 21:38:34 +0000</pubDate>
				<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[RAG]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80812</guid>

					<description><![CDATA[<p>In this tutorial, we build a RAG-Anything workflow to explore how multimodal retrieval works across text, tables, equations, and images. We prepare a Colab environment, enter our OpenAI API key at runtime, and generate a synthetic report with a chart and PDF. We convert that content into RAG-Anything's direct content_list format and insert it into the retrieval system. We then configure OpenAI chat, vision, and embedding functions and test naive, local, global, and hybrid modes.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/02/rag-anything-tutorial-build-a-multimodal-retrieval-pipeline-for-text-tables-equations-and-images-in-colab/">RAG-Anything Tutorial: Build a Multimodal Retrieval Pipeline for Text, Tables, Equations, and Images in Colab</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this tutorial, we build a<a href="https://github.com/HKUDS/RAG-Anything"> <strong>RAG-Anything</strong></a><strong> </strong>workflow and use it to explore how multimodal retrieval works across text, tables, equations, and images. We start by preparing the Colab environment, installing the required packages, and securely entering our OpenAI API key at runtime to keep the notebook practical and safe to run. We then create a synthetic multimodal report, generate a chart and PDF, convert the content into RAG-Anything’s direct content_list format, and insert it into the retrieval system. As we move through the tutorial, we configure clean OpenAI-based chat, vision, and embedding functions, initialize RAG-Anything, and test different retrieval modes such as naive, local, global, and hybrid.</p>



<h3 id="h-installing-rag-anything-dependencies" class="wp-block-heading"><strong>Installing RAG-Anything Dependencies</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import os
import re
import sys
import json
import time
import shutil
import hashlib
import asyncio
import inspect
import getpass
import subprocess
import importlib
import importlib.metadata
from pathlib import Path
from typing import List, Dict, Any
def run_shell(cmd, check=True):
   print(f"\n$ {cmd}")
   result = subprocess.run(cmd, shell=True, text=True)
   if check and result.returncode != 0:
       raise RuntimeError(f"Command failed: {cmd}")
   return result.returncode
print("=" * 80)
print("RAG-Anything Advanced Colab Tutorial")
print("=" * 80)
print("\n[1/10] Installing dependencies...")
for module_name in list(sys.modules):
   if module_name == "PIL" or module_name.startswith("PIL."):
       del sys.modules[module_name]
run_shell(
   'pip -q install -U '
   '"raganything[image,text]" '
   '"openai>=1.0.0" '
   '"python-dotenv" '
   '"reportlab" '
   '"pandas" '
   '"matplotlib" '
   '"tabulate"'
)
run_shell('pip -q install --no-cache-dir --force-reinstall "pillow==11.3.0"')
for module_name in list(sys.modules):
   if module_name == "PIL" or module_name.startswith("PIL."):
       del sys.modules[module_name]
importlib.invalidate_caches()
try:
   print("Pillow version:", importlib.metadata.version("Pillow"))
except Exception as e:
   print("Could not read Pillow version:", repr(e))
print("\n[2/10] Importing libraries...")
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from IPython.display import display
from reportlab.lib.pagesizes import letter
from reportlab.pdfgen import canvas
from reportlab.lib.units import inch
from openai import AsyncOpenAI
from raganything import RAGAnything, RAGAnythingConfig
from lightrag.utils import EmbeddingFunc
print("Imports successful.")
</code></pre></div></div>



<p class="wp-block-paragraph">We begin by setting up the complete Colab environment for the RAG-Anything workflow. We install the required libraries, repair the Pillow dependency, and import all the modules needed for plotting, PDF creation, OpenAI access, and RAG-Anything. We also define a reusable shell helper so the setup remains clear and easy to rerun.</p>



<h3 id="h-configuring-directories-runtime-variables" class="wp-block-heading"><strong>Configuring Directories, Runtime Variables</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n[3/10] Preparing directories and runtime settings...")
BASE_DIR = Path("/content/raganything_advanced_tutorial") if Path("/content").exists() else Path.cwd() / "raganything_advanced_tutorial"
ASSET_DIR = BASE_DIR / "assets"
OUTPUT_DIR = BASE_DIR / "output"
WORKING_DIR = BASE_DIR / "rag_storage"
LOG_DIR = BASE_DIR / "logs"
RESET_STORAGE = True
RUN_FULL_DOCUMENT_PARSE = False
PARSER_FOR_FULL_PARSE = "mineru"
PARSE_METHOD = "auto"
for d in [BASE_DIR, ASSET_DIR, OUTPUT_DIR, WORKING_DIR, LOG_DIR]:
   d.mkdir(parents=True, exist_ok=True)
if RESET_STORAGE and WORKING_DIR.exists():
   shutil.rmtree(WORKING_DIR)
   WORKING_DIR.mkdir(parents=True, exist_ok=True)
os.environ["LOG_DIR"] = str(LOG_DIR)
os.environ["SUMMARY_LANGUAGE"] = "English"
os.environ["ENABLE_LLM_CACHE"] = "false"
os.environ["ENABLE_LLM_CACHE_FOR_EXTRACT"] = "false"
os.environ["MAX_ASYNC"] = "2"
os.environ["CHUNK_SIZE"] = "900"
os.environ["CHUNK_OVERLAP_SIZE"] = "120"
os.environ["TIMEOUT"] = "240"
for var in [
   "OPENAI_API_KEY",
   "OPENAI_ORG_ID",
   "OPENAI_ORGANIZATION",
   "OPENAI_PROJECT",
   "OPENAI_DEFAULT_HEADERS",
   "LLM_BINDING_API_KEY",
   "LLM_BINDING_HOST",
]:
   os.environ.pop(var, None)
print(f"Base directory: {BASE_DIR}")
print(f"Assets directory: {ASSET_DIR}")
print(f"Storage directory: {WORKING_DIR}")
print("\n[4/10] Entering OpenAI API key securely...")
def clean_api_key(raw_value: str) -> str:
   raw_value = str(raw_value or "").strip()
   raw_value = raw_value.replace("Bearer ", "").replace("bearer ", "").strip()
   raw_value = raw_value.strip("'").strip('"').strip("`").strip()
   if "=" in raw_value:
       raw_value = raw_value.split("=", 1)[1].strip().strip("'").strip('"').strip("`")
   raw_value = re.sub(r"\s+", "", raw_value)
   raw_value = raw_value.encode("ascii", errors="ignore").decode("ascii").strip()
   return raw_value
OPENAI_API_KEY_RAW = getpass.getpass("Paste your OpenAI API key here. Input is hidden: ")
OPENAI_API_KEY = clean_api_key(OPENAI_API_KEY_RAW)
if not OPENAI_API_KEY:
   raise ValueError(
       "No API key was captured. Paste the key into the hidden input box and press Enter."
   )
print("Captured key length:", len(OPENAI_API_KEY))
print("Captured key prefix:", OPENAI_API_KEY[:12] + "...")
print("Captured key suffix:", "..." + OPENAI_API_KEY[-6:])
LLM_MODEL = "gpt-4o-mini"
VISION_MODEL = "gpt-4o-mini"
EMBEDDING_MODEL = "text-embedding-3-small"
EMBEDDING_DIM = 1536
openai_client = AsyncOpenAI(api_key=OPENAI_API_KEY)
os.environ["LLM_MODEL"] = LLM_MODEL
os.environ["VISION_MODEL"] = VISION_MODEL
os.environ["EMBEDDING_MODEL"] = EMBEDDING_MODEL
os.environ["EMBEDDING_DIM"] = str(EMBEDDING_DIM)
print("Testing OpenAI chat API with the captured key...")
try:
   test_response = await openai_client.chat.completions.create(
       model=LLM_MODEL,
       messages=[{"role": "user", "content": "Reply with exactly: ok"}],
       temperature=0,
   )
   print("Chat API test response:", test_response.choices[0].message.content)
except Exception as e:
   raise RuntimeError(
       "The key was captured, but OpenAI rejected the request or the account/model access failed. "
       "Check billing, project permissions, and make sure this is an OpenAI Platform API key."
   ) from e
print("\nTesting OpenAI embedding API...")
try:
   test_embedding = await openai_client.embeddings.create(
       model=EMBEDDING_MODEL,
       input=["RAG-Anything embedding test"],
   )
   print("Embedding vector length:", len(test_embedding.data[0].embedding))
except Exception as e:
   raise RuntimeError(
       "Chat worked, but embeddings failed. Make sure your API key has permission for embeddings."
   ) from e
print("OpenAI API key is working.")
print(f"Chat model: {LLM_MODEL}")
print(f"Vision model: {VISION_MODEL}")
print(f"Embedding model: {EMBEDDING_MODEL}")
print(f"Embedding dimension: {EMBEDDING_DIM}")
</code></pre></div></div>



<p class="wp-block-paragraph">We prepare the working directories, output folders, logs, and runtime environment variables that RAG-Anything uses during execution. We securely capture the OpenAI API key via a hidden input, clean the pasted value, and verify that both the chat and embedding calls work correctly. We also define the models and embedding dimensions that power the rest of the tutorial.</p>



<h3 id="h-generating-a-synthetic-multimodal-report" class="wp-block-heading"><strong>Generating a Synthetic Multimodal Report</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n[5/10] Creating a synthetic multimodal report...")
monthly_data = pd.DataFrame(
   {
       "Month": ["Jan", "Feb", "Mar", "Apr", "May", "Jun"],
       "Query Volume": [1200, 1700, 2100, 2600, 3300, 4100],
       "Hybrid Accuracy": [0.71, 0.74, 0.79, 0.83, 0.87, 0.91],
       "Average Latency ms": [980, 920, 850, 790, 760, 730],
   }
)
table_md = monthly_data.to_markdown(index=False)
plt.figure(figsize=(8, 4.8))
plt.plot(monthly_data["Month"], monthly_data["Query Volume"], marker="o", label="Query Volume")
plt.plot(monthly_data["Month"], monthly_data["Hybrid Accuracy"] * 4000, marker="s", label="Hybrid Accuracy scaled")
plt.title("Multimodal RAG Usage and Quality Trend")
plt.xlabel("Month")
plt.ylabel("Volume / Scaled Accuracy")
plt.legend()
plt.grid(True, alpha=0.3)
plt.text(
   0.02,
   0.95,
   "Synthetic figure: usage rises while latency falls",
   transform=plt.gca().transAxes,
   fontsize=9,
   verticalalignment="top",
   bbox=dict(boxstyle="round", alpha=0.15),
)
chart_path = ASSET_DIR / "raganything_quality_trend.png"
plt.tight_layout()
plt.savefig(chart_path, dpi=180)
plt.close()
report_pdf_path = ASSET_DIR / "synthetic_multimodal_rag_report.pdf"
c = canvas.Canvas(str(report_pdf_path), pagesize=letter)
width, height = letter
c.setFont("Helvetica-Bold", 18)
c.drawString(0.8 * inch, height - 0.8 * inch, "Synthetic Multimodal RAG Evaluation Report")
c.setFont("Helvetica", 10)
intro_lines = [
   "This report evaluates a synthetic multimodal RAG pipeline for enterprise documents.",
   "The knowledge base includes text, tables, equations, and visual evidence.",
   "The central hypothesis is that hybrid retrieval improves answer quality when evidence spans modalities.",
]
y = height - 1.25 * inch
for line in intro_lines:
   c.drawString(0.8 * inch, y, line)
   y -= 0.22 * inch
c.setFont("Helvetica-Bold", 12)
c.drawString(0.8 * inch, y - 0.1 * inch, "Table 1. Monthly system measurements")
y -= 0.4 * inch
c.setFont("Courier", 7.5)
for row in table_md.splitlines():
   c.drawString(0.8 * inch, y, row[:120])
   y -= 0.17 * inch
c.setFont("Helvetica-Bold", 12)
c.drawString(0.8 * inch, y - 0.15 * inch, "Equation 1. Weighted multimodal score")
y -= 0.45 * inch
c.setFont("Helvetica", 9)
c.drawString(
   0.8 * inch,
   y,
   "Score(q, d) = alpha * Sim_text(q, d) + beta * Sim_graph(q, d) + gamma * Sim_visual(q, d)",
)
y -= 0.5 * inch
c.drawImage(str(chart_path), 0.8 * inch, y - 2.8 * inch, width=6.5 * inch, height=2.6 * inch)
c.showPage()
c.setFont("Helvetica-Bold", 16)
c.drawString(0.8 * inch, height - 0.8 * inch, "Interpretation and Findings")
c.setFont("Helvetica", 10)
findings = [
   "Hybrid retrieval combines semantic similarity with graph-based relationship navigation.",
   "The synthetic table shows accuracy improving from 0.71 to 0.91 over six months.",
   "The generated figure shows query volume increasing while latency gradually decreases.",
   "Equation-level retrieval is useful when the question depends on scoring logic rather than plain prose.",
   "A multimodal system should preserve page index, captions, footnotes, and local image paths for traceability.",
]
y = height - 1.25 * inch
for finding in findings:
   c.drawString(0.8 * inch, y, "- " + finding)
   y -= 0.28 * inch
c.save()
print(f"Created chart: {chart_path}")
print(f"Created PDF: {report_pdf_path}")
print("\nSynthetic table:")
display(monthly_data)
</code></pre></div></div>



<p class="wp-block-paragraph">We create a synthetic multimodal report that provides realistic content for testing in RAG-Anything. We build a small performance table, generate a chart, and export a PDF containing text, a table, an equation, and a figure. We use this controlled document to clearly observe how the system handles different content types.</p>



<h3 id="h-building-the-rag-anything-content-list-for-text" class="wp-block-heading"><strong>Building the RAG-Anything content_list for Text</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n[6/10] Building direct multimodal content_list...")
content_list: List[Dict[str, Any]] = [
   {
       "type": "text",
       "text": (
           "This synthetic report evaluates a multimodal retrieval augmented generation system. "
           "The system indexes textual explanations, a structured performance table, a scoring equation, "
           "and a trend figure. The main goal is to answer questions whose evidence is distributed across "
           "several document modalities rather than one plain text passage."
       ),
       "page_idx": 0,
   },
   {
       "type": "table",
       "table_body": table_md,
       "table_caption": ["Table 1: Monthly query volume, hybrid accuracy, and average latency."],
       "table_footnote": ["Synthetic measurements created for a Colab tutorial."],
       "page_idx": 0,
   },
   {
       "type": "equation",
       "latex": r"Score(q,d)=\alpha \cdot Sim_{text}(q,d)+\beta \cdot Sim_{graph}(q,d)+\gamma \cdot Sim_{visual}(q,d)",
       "text": (
           "Weighted multimodal retrieval score. Alpha controls text similarity, beta controls graph relationship "
           "similarity, and gamma controls visual similarity."
       ),
       "page_idx": 0,
   },
   {
       "type": "image",
       "img_path": str(chart_path.resolve()),
       "image_caption": ["Figure 1: Multimodal RAG usage and quality trend."],
       "image_footnote": ["The line chart is synthetic and generated inside this tutorial."],
       "page_idx": 0,
   },
   {
       "type": "text",
       "text": (
           "The key finding is that hybrid retrieval is preferred for cross-modal questions. "
           "Local retrieval is useful for entity-specific lookup, global retrieval is useful for broader themes, "
           "and naive retrieval is a simpler baseline. In this report, hybrid accuracy rises from 0.71 in January "
           "to 0.91 in June, while average latency drops from 980 milliseconds to 730 milliseconds."
       ),
       "page_idx": 1,
   },
]
content_list_path = ASSET_DIR / "content_list.json"
with open(content_list_path, "w", encoding="utf-8") as f:
   json.dump(content_list, f, indent=2, ensure_ascii=False)
print(f"Saved content list: {content_list_path}")
</code></pre></div></div>



<p class="wp-block-paragraph">We convert the synthetic report into RAG-Anything’s direct content_list format. We represent text, tables, equations, and images as separate structured blocks with captions, footnotes, page indexes, and image paths. We save this list of multimodal content as JSON so the workflow remains transparent and reusable.</p>



<h3 class="wp-block-heading"><strong>Defining OpenAI Chat, Vision, and Embedding Functions</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n[7/10] Defining clean OpenAI model and embedding functions...")
async def llm_model_func(prompt, system_prompt=None, history_messages=None, **kwargs):
   messages = []
   if system_prompt:
       messages.append({"role": "system", "content": str(system_prompt)})
   for msg in history_messages or []:
       if isinstance(msg, dict) and "role" in msg and "content" in msg:
           messages.append(msg)
   messages.append({"role": "user", "content": str(prompt)})
   allowed_kwargs = {}
   for key in ["temperature", "top_p", "max_tokens", "response_format"]:
       if key in kwargs and kwargs[key] is not None:
           allowed_kwargs[key] = kwargs[key]
   response = await openai_client.chat.completions.create(
       model=LLM_MODEL,
       messages=messages,
       **allowed_kwargs,
   )
   return response.choices[0].message.content or ""
async def vision_model_func(
   prompt,
   system_prompt=None,
   history_messages=None,
   image_data=None,
   messages=None,
   **kwargs,
):
   allowed_kwargs = {}
   for key in ["temperature", "top_p", "max_tokens", "response_format"]:
       if key in kwargs and kwargs[key] is not None:
           allowed_kwargs[key] = kwargs[key]
   if messages:
       clean_messages = [m for m in messages if m is not None]
       response = await openai_client.chat.completions.create(
           model=VISION_MODEL,
           messages=clean_messages,
           **allowed_kwargs,
       )
       return response.choices[0].message.content or ""
   built_messages = []
   if system_prompt:
       built_messages.append({"role": "system", "content": str(system_prompt)})
   for msg in history_messages or []:
       if isinstance(msg, dict) and "role" in msg and "content" in msg:
           built_messages.append(msg)
   if image_data:
       built_messages.append(
           {
               "role": "user",
               "content": [
                   {"type": "text", "text": str(prompt)},
                   {
                       "type": "image_url",
                       "image_url": {"url": f"data:image/jpeg;base64,{image_data}"},
                   },
               ],
           }
       )
   else:
       built_messages.append({"role": "user", "content": str(prompt)})
   response = await openai_client.chat.completions.create(
       model=VISION_MODEL,
       messages=built_messages,
       **allowed_kwargs,
   )
   return response.choices[0].message.content or ""
async def openai_embedding_func(texts, **kwargs):
   if isinstance(texts, str):
       texts = [texts]
   texts = [str(t) for t in texts]
   response = await openai_client.embeddings.create(
       model=EMBEDDING_MODEL,
       input=texts,
   )
   vectors = [item.embedding for item in response.data]
   return np.array(vectors, dtype=np.float32)
embedding_func = EmbeddingFunc(
   embedding_dim=EMBEDDING_DIM,
   max_token_size=8192,
   func=openai_embedding_func,
)
print("Model and embedding functions ready.")
</code></pre></div></div>



<p class="wp-block-paragraph">We define clean OpenAI-powered functions for text generation, vision generation, and embedding creation. We handle system prompts, chat history, multimodal image inputs, and optional model parameters in a controlled way. We then wrap the embedding function with LightRAG’s EmbeddingFunc so RAG-Anything can use it during indexing and retrieval.</p>



<h3 id="h-initializing-rag-anything-and-running-hybrid-retrieval" class="wp-block-heading"><strong>Initializing RAG-Anything and Running Hybrid Retrieval</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n[8/10] Initializing RAG-Anything...")
config = RAGAnythingConfig(
   working_dir=str(WORKING_DIR),
   parser=PARSER_FOR_FULL_PARSE,
   parse_method=PARSE_METHOD,
   enable_image_processing=True,
   enable_table_processing=True,
   enable_equation_processing=True,
)
rag = RAGAnything(
   config=config,
   llm_model_func=llm_model_func,
   vision_model_func=vision_model_func,
   embedding_func=embedding_func,
)
async def maybe_await(value):
   if inspect.isawaitable(value):
       return await value
   return value
if hasattr(rag, "initialize_storages"):
   try:
       await maybe_await(rag.initialize_storages())
       print("RAG-Anything storages initialized.")
   except Exception as e:
       print("Storage initialization skipped or already handled:", repr(e))
print(f"Working directory: {WORKING_DIR}")
print("\n[9/10] Inserting multimodal content and running retrieval queries...")
async def insert_demo_content():
   await rag.insert_content_list(
       content_list=content_list,
       file_path=str(report_pdf_path.name),
       split_by_character=None,
       split_by_character_only=False,
       doc_id="synthetic-multimodal-rag-report",
       display_stats=True,
   )
await insert_demo_content()
print("Insertion complete.")
queries = [
   "What is the main purpose of the multimodal RAG report?",
   "How did hybrid accuracy and latency change from January to June?",
   "Why is hybrid retrieval better than naive retrieval for this report?",
   "What does the weighted multimodal score equation mean?",
]
async def safe_aquery(question, mode="hybrid", vlm_enhanced=False):
   try:
       return await rag.aquery(question, mode=mode, vlm_enhanced=vlm_enhanced)
   except TypeError:
       return await rag.aquery(question, mode=mode)
async def run_query_suite():
   results = []
   for mode in ["naive", "local", "global", "hybrid"]:
       print("\n" + "=" * 80)
       print(f"QUERY MODE: {mode.upper()}")
       print("=" * 80)
       for q in queries:
           print(f"\nQuestion: {q}")
           try:
               answer = await safe_aquery(q, mode=mode, vlm_enhanced=False)
           except Exception as e:
               answer = f"Query failed in mode={mode}: {repr(e)}"
           print("\nAnswer:")
           print(answer)
           print("-" * 80)
           results.append(
               {
                   "mode": mode,
                   "question": q,
                   "answer_preview": str(answer)[:700],
               }
           )
   return pd.DataFrame(results)
query_results_df = await run_query_suite()
print("\nQuery result preview:")
display(query_results_df)
</code></pre></div></div>



<p class="wp-block-paragraph">We initialize RAG-Anything with the working directory, parser settings, and multimodal processing options enabled. We insert the prepared multimodal content list into the system and let RAG-Anything process the text, table, equation, and image blocks. We then run multiple retrieval modes to compare the behavior of naive, local, global, and hybrid queries.</p>



<h3 id="h-running-explicit-multimodal-queries" class="wp-block-heading"><strong>Running Explicit Multimodal Queries</strong></h3>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n[10/10] Running explicit multimodal queries...")
async def run_multimodal_queries():
   multimodal_cases = [
       {
           "name": "Table-aware query",
           "question": (
               "Using the supplied table, identify the month with the highest hybrid accuracy, "
               "the month with the lowest latency, and explain whether the trend supports the report conclusion."
           ),
           "multimodal_content": [
               {
                   "type": "table",
                   "table_data": table_md,
                   "table_caption": "Monthly performance table",
               }
           ],
       },
       {
           "name": "Equation-aware query",
           "question": (
               "Explain how this scoring equation should affect retrieval when the user's question needs "
               "textual, graph, and visual evidence at the same time."
           ),
           "multimodal_content": [
               {
                   "type": "equation",
                   "latex": r"Score(q,d)=\alpha Sim_{text}(q,d)+\beta Sim_{graph}(q,d)+\gamma Sim_{visual}(q,d)",
                   "equation_caption": "Weighted multimodal retrieval score",
               }
           ],
       },
       {
           "name": "Combined multimodal query",
           "question": (
               "Connect the table, equation, and document conclusion into one explanation of why a multimodal "
               "hybrid retriever is useful."
           ),
           "multimodal_content": [
               {
                   "type": "table",
                   "table_data": table_md,
                   "table_caption": "Monthly performance table",
               },
               {
                   "type": "equation",
                   "latex": r"Score(q,d)=\alpha Sim_{text}(q,d)+\beta Sim_{graph}(q,d)+\gamma Sim_{visual}(q,d)",
                   "equation_caption": "Weighted multimodal retrieval score",
               },
           ],
       },
   ]
   outputs = []
   for case in multimodal_cases:
       print("\n" + "=" * 80)
       print(case["name"])
       print("=" * 80)
       print("Question:", case["question"])
       try:
           answer = await rag.aquery_with_multimodal(
               case["question"],
               multimodal_content=case["multimodal_content"],
               mode="hybrid",
           )
       except Exception as e:
           answer = f"Multimodal query failed: {repr(e)}"
       print("\nAnswer:")
       print(answer)
       outputs.append(
           {
               "case": case["name"],
               "question": case["question"],
               "answer_preview": str(answer)[:900],
           }
       )
   return pd.DataFrame(outputs)
multimodal_results_df = await run_multimodal_queries()
print("\nMultimodal result preview:")
display(multimodal_results_df)
print("\nOptional full-parser path:")
print("RUN_FULL_DOCUMENT_PARSE is currently:", RUN_FULL_DOCUMENT_PARSE)
async def optional_full_document_parse():
   if not RUN_FULL_DOCUMENT_PARSE:
       print(
           "Skipping parser-based PDF ingestion. "
           "Set RUN_FULL_DOCUMENT_PARSE=True near the top to test MinerU/Docling/PaddleOCR parsing."
       )
       return
   print("Starting full document parsing.")
   await rag.process_document_complete(
       file_path=str(report_pdf_path),
       output_dir=str(OUTPUT_DIR),
       parse_method=PARSE_METHOD,
       parser=PARSER_FOR_FULL_PARSE,
       display_stats=True,
       doc_id="parser-processed-synthetic-report",
   )
   answer = await safe_aquery(
       "After full parsing, what figures, tables, and equations are present in the report?",
       mode="hybrid",
       vlm_enhanced=False,
   )
   print(answer)
await optional_full_document_parse()
print("\n" + "=" * 80)
print("Tutorial complete.")
print("=" * 80)
print(f"Assets directory: {ASSET_DIR}")
print(f"RAG storage directory: {WORKING_DIR}")
print(f"Output directory: {OUTPUT_DIR}")
print("\nGenerated files:")
for path in sorted(ASSET_DIR.glob("*")):
   print(" -", path)
</code></pre></div></div>



<p class="wp-block-paragraph">We test explicit multimodal queries that directly provide table and equation content at query time. We ask the system to reason over structured values, scoring logic, and combined multimodal evidence. We finish by showing the result previews, keeping the optional full-document parser path available for deeper PDF ingestion experiments.</p>



<h3 class="wp-block-heading"><strong>Conclusion</strong></h3>



<p class="wp-block-paragraph">In conclusion, we have a working RAG-Anything pipeline that can ingest multimodal content and answer questions using both standard and multimodal retrieval paths. We saw how text passages, markdown tables, LaTeX equations, and generated figures can be represented as separate content blocks while still contributing to a shared retrieval system. We also tested multiple query modes and compared the system&#8217;s responses when questions require direct facts, broader context, or cross-modal reasoning.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph">Check out the<strong> </strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/LLM%20Projects/rag_anything_multimodal_hybrid_retrieval_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener"><strong>Full Codes here</strong></a>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/02/rag-anything-tutorial-build-a-multimodal-retrieval-pipeline-for-text-tables-equations-and-images-in-colab/">RAG-Anything Tutorial: Build a Multimodal Retrieval Pipeline for Text, Tables, Equations, and Images in Colab</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80812</post-id>
	</item>
		<item>
		<title>Meet Alibaba&#8217;s Page Agent: A JavaScript In-Page GUI Agent That Controls Web Interfaces With Natural Language Through the DOM</title>
		<link>https://www.marktechpost.com/2026/07/02/meet-alibabas-page-agent-a-javascript-in-page-gui-agent-that-controls-web-interfaces-with-natural-language-through-the-dom/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Thu, 02 Jul 2026 20:51:39 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Agents]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[For Devs]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80809</guid>

					<description><![CDATA[<p>Alibaba's Page Agent runs as client-side JavaScript inside the webpage. It reads the live DOM as text, then clicks and types from natural-language commands. No screenshots, no multimodal model, and no backend rewrite are required.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/02/meet-alibabas-page-agent-a-javascript-in-page-gui-agent-that-controls-web-interfaces-with-natural-language-through-the-dom/">Meet Alibaba&#8217;s Page Agent: A JavaScript In-Page GUI Agent That Controls Web Interfaces With Natural Language Through the DOM</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Most browser automation runs from the outside. Playwright, Puppeteer, Selenium, and browser-use all drive a browser from an external process. They read the page through screenshots or the Chrome DevTools Protocol.</p>



<p class="wp-block-paragraph">Alibaba&#8217;s <strong><a href="https://github.com/alibaba/page-agent" target="_blank" rel="noreferrer noopener">Page Agent</a></strong> takes the opposite path. The agent lives inside the webpage as plain JavaScript. It reads the live DOM as text and acts as the real user. No headless browser, no screenshots, no multi-modal model.</p>



<p class="wp-block-paragraph">The project is open-source under the MIT license. The codebase is TypeScript-first. It builds on browser-use, from which its DOM processing and prompt are derived.</p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li>Page Agent runs inside the page as JavaScript, reading the live DOM as text, not screenshots.</li>



<li>DOM dehydration compresses the page into a <code>FlatDomTree</code> so smaller text models can act precisely.</li>



<li>It is model-agnostic through any OpenAI-compatible endpoint and ships under the MIT license.</li>



<li>Prompt-level safety and single-page scope are real limits; keep server-side validation for risky actions.</li>



<li>Best fit: copilots and form-filling inside apps you own, not external or locked-down sites.</li>
</ul>



<h2 id="h-what-is-page-agent" class="wp-block-heading"><strong>What is Page Agent?</strong></h2>



<p class="wp-block-paragraph">Page Agent is a client-side library for adding agent behavior to a web app. You embed it, then issue commands in natural language. The agent finds elements, clicks buttons, and fills forms from within the page.</p>



<p class="wp-block-paragraph">Because it runs in the browser session, it inherits the user&#8217;s cookies, session, and authentication. There is no separate backend to write. The existing UI validation and security rules stay in place.</p>



<p class="wp-block-paragraph">The design is model-agnostic. You bring your own large language model through any OpenAI-compatible endpoint. Only text is sent to the model, so a strong text model is enough.</p>



<h2 id="h-how-dom-dehydration-works" class="wp-block-heading"><strong>How DOM Dehydration Works</strong></h2>



<p class="wp-block-paragraph">The core technique is what the team calls <strong>DOM dehydration</strong>. A modern page can hold thousands of nodes. Sending raw HTML to a model would be slow and expensive.</p>



<p class="wp-block-paragraph">When a command arrives, the agent scans the Document Object Model. It identifies every interactive element, such as buttons, links, and input fields. Each element receives an index plus a role and a label.</p>



<p class="wp-block-paragraph">The live DOM is converted into a <code>FlatDomTree</code>, a clean text map of what matters. Redundant markup is stripped out. The model reads this compact representation, not pixels.</p>



<p class="wp-block-paragraph">The interactive demo on this page mirrors this loop. Watch the &#8220;Dehydrated DOM&#8221; and &#8220;Action trace&#8221; panels update as commands run.</p>



<!-- Page Agent interactive demo -- paste into a WordPress "Custom HTML" block -->
<div id="pa-embed-wrap" style="max-width:100%;margin:0 auto;">
  <iframe id="pa-embed-frame" title="Page Agent interactive demo" loading="lazy"
    style="width:100%;border:0;height:600px;overflow:hidden;display:block;"
    srcdoc="&lt;!doctype html&gt;&lt;html lang=&quot;en&quot;&gt;&lt;head&gt;&lt;meta charset=&quot;utf-8&quot;&gt;&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width,initial-scale=1&quot;&gt;&lt;style&gt;html,body{margin:0;padding:0;background:#f6f7f4}&lt;/style&gt;&lt;/head&gt;&lt;body&gt;&lt;div id=&quot;pa-demo-root&quot;&gt;
  &lt;div class=&quot;pa-wrap&quot;&gt;
    &lt;div class=&quot;pa-head&quot;&gt;
      &lt;div class=&quot;pa-badge&quot;&gt;Illustrative simulation&lt;/div&gt;
      &lt;h3&gt;Page Agent &amp;mdash; in-page GUI agent, live demo&lt;/h3&gt;
      &lt;p class=&quot;pa-sub&quot;&gt;Type a natural-language command. The agent scans the DOM, builds a dehydrated element index, then clicks and types like a real user. This is a rule-based re-creation of the pattern, not a live LLM call.&lt;/p&gt;
    &lt;/div&gt;

    &lt;div class=&quot;pa-cmd&quot;&gt;
      &lt;div class=&quot;pa-chips&quot; id=&quot;pa-chips&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;pa-inputrow&quot;&gt;
        &lt;span class=&quot;pa-prompt&quot;&gt;&amp;gt;&lt;/span&gt;
        &lt;input id=&quot;pa-input&quot; type=&quot;text&quot; autocomplete=&quot;off&quot; spellcheck=&quot;false&quot;
          placeholder=&quot;e.g. Fill the form for John Smith at Acme, Enterprise tier&quot; /&gt;
        &lt;button id=&quot;pa-run&quot; class=&quot;pa-btn-run&quot;&gt;Run&lt;/button&gt;
        &lt;button id=&quot;pa-reset&quot; class=&quot;pa-btn-reset&quot; title=&quot;Reset the mock app&quot;&gt;Reset&lt;/button&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;pa-grid&quot;&gt;
      &lt;!-- Mock web app (the &quot;page&quot; the agent controls) --&gt;
      &lt;div class=&quot;pa-panel&quot;&gt;
        &lt;div class=&quot;pa-panel-h&quot;&gt;
          &lt;span class=&quot;pa-dot pa-dot-a&quot;&gt;&lt;/span&gt;&lt;span class=&quot;pa-dot pa-dot-b&quot;&gt;&lt;/span&gt;&lt;span class=&quot;pa-dot pa-dot-c&quot;&gt;&lt;/span&gt;
          &lt;span class=&quot;pa-panel-title&quot;&gt;Acme CRM &amp;mdash; New Customer&lt;/span&gt;
        &lt;/div&gt;
        &lt;div class=&quot;pa-app&quot; id=&quot;pa-app&quot;&gt;
          &lt;label class=&quot;pa-field&quot;&gt;
            &lt;span&gt;Full name&lt;/span&gt;
            &lt;input data-pa=&quot;name&quot; type=&quot;text&quot; placeholder=&quot;Name&quot; /&gt;
          &lt;/label&gt;
          &lt;label class=&quot;pa-field&quot;&gt;
            &lt;span&gt;Email&lt;/span&gt;
            &lt;input data-pa=&quot;email&quot; type=&quot;text&quot; placeholder=&quot;name@company.com&quot; /&gt;
          &lt;/label&gt;
          &lt;label class=&quot;pa-field&quot;&gt;
            &lt;span&gt;Company&lt;/span&gt;
            &lt;input data-pa=&quot;company&quot; type=&quot;text&quot; placeholder=&quot;Company&quot; /&gt;
          &lt;/label&gt;
          &lt;label class=&quot;pa-field&quot;&gt;
            &lt;span&gt;Plan tier&lt;/span&gt;
            &lt;select data-pa=&quot;tier&quot;&gt;
              &lt;option value=&quot;&quot;&gt;Select tier&lt;/option&gt;
              &lt;option&gt;Free&lt;/option&gt;
              &lt;option&gt;Pro&lt;/option&gt;
              &lt;option&gt;Enterprise&lt;/option&gt;
            &lt;/select&gt;
          &lt;/label&gt;
          &lt;label class=&quot;pa-field&quot;&gt;
            &lt;span&gt;Notes&lt;/span&gt;
            &lt;textarea data-pa=&quot;notes&quot; rows=&quot;2&quot; placeholder=&quot;Optional notes&quot;&gt;&lt;/textarea&gt;
          &lt;/label&gt;
          &lt;div class=&quot;pa-actions&quot;&gt;
            &lt;button data-pa=&quot;save&quot; class=&quot;pa-save&quot;&gt;Save customer&lt;/button&gt;
            &lt;button data-pa=&quot;clear&quot; class=&quot;pa-clear&quot;&gt;Clear&lt;/button&gt;
          &lt;/div&gt;
          &lt;div class=&quot;pa-toast&quot; id=&quot;pa-toast&quot;&gt;&lt;/div&gt;
        &lt;/div&gt;
      &lt;/div&gt;

      &lt;!-- Agent internals --&gt;
      &lt;div class=&quot;pa-panel pa-panel-side&quot;&gt;
        &lt;div class=&quot;pa-tabs&quot;&gt;
          &lt;button class=&quot;pa-tab pa-tab-active&quot; data-tab=&quot;dom&quot;&gt;Dehydrated DOM&lt;/button&gt;
          &lt;button class=&quot;pa-tab&quot; data-tab=&quot;trace&quot;&gt;Action trace&lt;/button&gt;
        &lt;/div&gt;
        &lt;div class=&quot;pa-tabbody&quot;&gt;
          &lt;div class=&quot;pa-tabpane pa-pane-active&quot; id=&quot;pa-pane-dom&quot;&gt;
            &lt;p class=&quot;pa-hint&quot;&gt;Interactive elements the agent sees as text &amp;mdash; each gets an index, role and label. No screenshots.&lt;/p&gt;
            &lt;div class=&quot;pa-domlist&quot; id=&quot;pa-domlist&quot;&gt;&lt;/div&gt;
          &lt;/div&gt;
          &lt;div class=&quot;pa-tabpane&quot; id=&quot;pa-pane-trace&quot;&gt;
            &lt;p class=&quot;pa-hint&quot;&gt;Calls the agent issues to &lt;code&gt;PageController&lt;/code&gt;.&lt;/p&gt;
            &lt;pre class=&quot;pa-trace&quot; id=&quot;pa-trace&quot;&gt;&lt;span class=&quot;pa-muted&quot;&gt;// run a command to see the trace&lt;/span&gt;&lt;/pre&gt;
          &lt;/div&gt;
        &lt;/div&gt;
        &lt;div class=&quot;pa-status&quot; id=&quot;pa-status&quot;&gt;&lt;span class=&quot;pa-status-idle&quot;&gt;Idle&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;pa-foot&quot;&gt;
      &lt;span&gt;Built by &lt;strong&gt;Marktechpost&lt;/strong&gt;&lt;/span&gt;
      &lt;span class=&quot;pa-foot-r&quot;&gt;Pattern based on &lt;code&gt;alibaba/page-agent&lt;/code&gt; (MIT)&lt;/span&gt;
    &lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;style&gt;
#pa-demo-root *{box-sizing:border-box;margin:0;padding:0}
#pa-demo-root{
  --bg:#f6f7f4;--card:#ffffff;--ink:#161a13;--muted:#6b7280;--line:#e6e8e2;
  --green:#5f9400;--green-2:#76b900;--green-soft:#eef6df;--amber:#b45309;
  font-family:'Inter',-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Helvetica,Arial,sans-serif;
  color:var(--ink);background:var(--bg);line-height:1.5;
  -webkit-font-smoothing:antialiased;padding:16px;
}
#pa-demo-root .pa-wrap{max-width:940px;margin:0 auto}
#pa-demo-root .pa-mono,#pa-demo-root code,#pa-demo-root pre,#pa-demo-root .pa-domlist,#pa-demo-root .pa-prompt{
  font-family:'JetBrains Mono',ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;
}
#pa-demo-root .pa-head{margin-bottom:14px}
#pa-demo-root .pa-badge{
  display:inline-block;font-size:11px;font-weight:600;letter-spacing:.04em;text-transform:uppercase;
  color:var(--amber);background:#fef3e2;border:1px solid #f7dcae;border-radius:999px;padding:3px 10px;margin-bottom:8px;
}
#pa-demo-root h3{font-size:19px;font-weight:700;letter-spacing:-.01em}
#pa-demo-root .pa-sub{color:var(--muted);font-size:13.5px;margin-top:5px;max-width:760px}
#pa-demo-root .pa-cmd{
  background:var(--card);border:1px solid var(--line);border-radius:12px;padding:12px;margin-bottom:14px;
}
#pa-demo-root .pa-chips{display:flex;flex-wrap:wrap;gap:7px;margin-bottom:10px}
#pa-demo-root .pa-chip{
  font-size:12.5px;color:#374151;background:#f3f5ef;border:1px solid var(--line);
  border-radius:999px;padding:6px 11px;cursor:pointer;transition:all .15s;
}
#pa-demo-root .pa-chip:hover{border-color:var(--green);color:var(--green);background:var(--green-soft)}
#pa-demo-root .pa-inputrow{display:flex;align-items:center;gap:8px}
#pa-demo-root .pa-prompt{color:var(--green);font-weight:700;font-size:15px}
#pa-demo-root #pa-input{
  flex:1;border:1px solid var(--line);border-radius:9px;padding:11px 12px;font-size:14px;
  background:#fbfcfa;color:var(--ink);outline:none;transition:border .15s;min-width:0;
}
#pa-demo-root #pa-input:focus{border-color:var(--green)}
#pa-demo-root .pa-btn-run{
  background:var(--green);color:#fff;border:none;border-radius:9px;padding:11px 18px;font-size:14px;
  font-weight:600;cursor:pointer;transition:background .15s;
}
#pa-demo-root .pa-btn-run:hover{background:#4f7d00}
#pa-demo-root .pa-btn-run:disabled{opacity:.55;cursor:not-allowed}
#pa-demo-root .pa-btn-reset{
  background:#fff;color:#374151;border:1px solid var(--line);border-radius:9px;padding:11px 14px;
  font-size:13px;cursor:pointer;
}
#pa-demo-root .pa-btn-reset:hover{border-color:#c9cdc2}
#pa-demo-root .pa-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}
#pa-demo-root .pa-panel{
  background:var(--card);border:1px solid var(--line);border-radius:12px;overflow:hidden;display:flex;flex-direction:column;
}
#pa-demo-root .pa-panel-h{
  display:flex;align-items:center;gap:6px;padding:10px 13px;border-bottom:1px solid var(--line);background:#fbfcfa;
}
#pa-demo-root .pa-dot{width:10px;height:10px;border-radius:50%}
#pa-demo-root .pa-dot-a{background:#f0a4a4}#pa-demo-root .pa-dot-b{background:#f2d09a}#pa-demo-root .pa-dot-c{background:#a8dca0}
#pa-demo-root .pa-panel-title{font-size:12.5px;color:var(--muted);margin-left:6px;font-weight:500}
#pa-demo-root .pa-app{padding:15px}
#pa-demo-root .pa-field{display:block;margin-bottom:11px}
#pa-demo-root .pa-field&gt;span{display:block;font-size:12px;color:var(--muted);margin-bottom:4px;font-weight:500}
#pa-demo-root .pa-field input,#pa-demo-root .pa-field select,#pa-demo-root .pa-field textarea{
  width:100%;border:1px solid var(--line);border-radius:8px;padding:9px 10px;font-size:13.5px;
  background:#fff;color:var(--ink);outline:none;font-family:inherit;transition:border .15s,box-shadow .15s;resize:vertical;
}
#pa-demo-root .pa-field input:focus,#pa-demo-root .pa-field select:focus,#pa-demo-root .pa-field textarea:focus{border-color:var(--green)}
#pa-demo-root .pa-hl{
  border-color:var(--green-2)!important;box-shadow:0 0 0 3px rgba(118,185,0,.22)!important;background:var(--green-soft)!important;
}
#pa-demo-root .pa-actions{display:flex;gap:9px;margin-top:4px}
#pa-demo-root .pa-save{background:var(--green);color:#fff;border:none;border-radius:8px;padding:9px 16px;font-size:13.5px;font-weight:600;cursor:pointer}
#pa-demo-root .pa-clear{background:#fff;color:#374151;border:1px solid var(--line);border-radius:8px;padding:9px 14px;font-size:13.5px;cursor:pointer}
#pa-demo-root .pa-btn-flash{animation:pa-flash .5s ease}
@keyframes pa-flash{0%{box-shadow:0 0 0 0 rgba(118,185,0,.5)}100%{box-shadow:0 0 0 14px rgba(118,185,0,0)}}
#pa-demo-root .pa-toast{
  margin-top:12px;font-size:13px;padding:0 12px;max-height:0;overflow:hidden;border-radius:8px;
  background:var(--green-soft);color:#2f6b00;border:1px solid transparent;transition:all .25s;opacity:0;
}
#pa-demo-root .pa-toast.pa-show{max-height:60px;padding:10px 12px;opacity:1;border-color:#cfe6a3}
#pa-demo-root .pa-panel-side{min-height:100%}
#pa-demo-root .pa-tabs{display:flex;border-bottom:1px solid var(--line);background:#fbfcfa}
#pa-demo-root .pa-tab{
  flex:1;background:none;border:none;padding:11px;font-size:12.5px;font-weight:600;color:var(--muted);cursor:pointer;
  border-bottom:2px solid transparent;font-family:inherit;
}
#pa-demo-root .pa-tab-active{color:var(--green);border-bottom-color:var(--green)}
#pa-demo-root .pa-tabbody{padding:13px;flex:1}
#pa-demo-root .pa-tabpane{display:none}
#pa-demo-root .pa-pane-active{display:block}
#pa-demo-root .pa-hint{font-size:12px;color:var(--muted);margin-bottom:9px}
#pa-demo-root .pa-hint code{background:#f0f2ec;padding:1px 5px;border-radius:4px;font-size:11.5px;color:#3f6212}
#pa-demo-root .pa-domlist{display:flex;flex-direction:column;gap:5px;max-height:270px;overflow-y:auto}
#pa-demo-root .pa-domrow{
  display:flex;align-items:center;gap:8px;font-size:12px;padding:6px 8px;border:1px solid var(--line);
  border-radius:7px;background:#fbfcfa;transition:all .15s;
}
#pa-demo-root .pa-domrow.pa-active{border-color:var(--green-2);background:var(--green-soft)}
#pa-demo-root .pa-idx{
  background:#eef2e6;color:#3f6212;font-weight:700;border-radius:5px;padding:1px 7px;font-size:11px;min-width:26px;text-align:center;
}
#pa-demo-root .pa-domrow.pa-active .pa-idx{background:var(--green);color:#fff}
#pa-demo-root .pa-role{color:#7c8598}
#pa-demo-root .pa-label{color:var(--ink);font-weight:500}
#pa-demo-root .pa-trace{
  background:#fbfcfa;border:1px solid var(--line);border-radius:8px;padding:11px;font-size:12px;
  color:#374151;white-space:pre-wrap;word-break:break-word;max-height:270px;overflow-y:auto;line-height:1.7;
}
#pa-demo-root .pa-trace .pa-call{color:var(--green)}
#pa-demo-root .pa-trace .pa-arg{color:#b45309}
#pa-demo-root .pa-muted{color:#9aa1ab}
#pa-demo-root .pa-status{padding:9px 13px;border-top:1px solid var(--line);font-size:12px;background:#fbfcfa}
#pa-demo-root .pa-status-idle{color:var(--muted)}
#pa-demo-root .pa-status-run{color:var(--green);font-weight:600}
#pa-demo-root .pa-status-run::before{content:&quot;&quot;;display:inline-block;width:7px;height:7px;border-radius:50%;background:var(--green);margin-right:7px;animation:pa-pulse 1s infinite}
@keyframes pa-pulse{0%,100%{opacity:1}50%{opacity:.3}}
#pa-demo-root .pa-status-done{color:#2f6b00;font-weight:600}
#pa-demo-root .pa-status-err{color:#b91c1c;font-weight:600}
#pa-demo-root .pa-foot{
  display:flex;justify-content:space-between;align-items:center;margin-top:14px;padding-top:12px;
  border-top:1px solid var(--line);font-size:12px;color:var(--muted);
}
#pa-demo-root .pa-foot strong{color:var(--green)}
#pa-demo-root .pa-foot code{background:#f0f2ec;padding:1px 6px;border-radius:4px;font-size:11px;color:#3f6212}
@media(max-width:640px){
  #pa-demo-root .pa-grid{grid-template-columns:1fr}
  #pa-demo-root h3{font-size:17px}
  #pa-demo-root .pa-foot{flex-direction:column;gap:6px;align-items:flex-start}
}
&lt;/style&gt;

&lt;script&gt;
(function(){
  var root=document.getElementById('pa-demo-root');
  if(!root)return;
  var app=root.querySelector('#pa-app');
  var domlist=root.querySelector('#pa-domlist');
  var trace=root.querySelector('#pa-trace');
  var statusEl=root.querySelector('#pa-status');
  var input=root.querySelector('#pa-input');
  var runBtn=root.querySelector('#pa-run');
  var resetBtn=root.querySelector('#pa-reset');
  var chipsWrap=root.querySelector('#pa-chips');
  var toast=root.querySelector('#pa-toast');

  var fields={
    name:app.querySelector('[data-pa=&quot;name&quot;]'),
    email:app.querySelector('[data-pa=&quot;email&quot;]'),
    company:app.querySelector('[data-pa=&quot;company&quot;]'),
    tier:app.querySelector('[data-pa=&quot;tier&quot;]'),
    notes:app.querySelector('[data-pa=&quot;notes&quot;]'),
    save:app.querySelector('[data-pa=&quot;save&quot;]'),
    clear:app.querySelector('[data-pa=&quot;clear&quot;]')
  };

  // Dehydrated element index (what the agent &quot;reads&quot;)
  var dom=[
    {i:1,role:'input',label:'Full name',el:fields.name},
    {i:2,role:'input',label:'Email',el:fields.email},
    {i:3,role:'input',label:'Company',el:fields.company},
    {i:4,role:'select',label:'Plan tier',el:fields.tier},
    {i:5,role:'textarea',label:'Notes',el:fields.notes},
    {i:6,role:'button',label:'Save customer',el:fields.save},
    {i:7,role:'button',label:'Clear',el:fields.clear}
  ];

  function renderDom(activeIdx){
    domlist.innerHTML='';
    dom.forEach(function(d){
      var row=document.createElement('div');
      row.className='pa-domrow'+(activeIdx===d.i?' pa-active':'');
      row.innerHTML='&lt;span class=&quot;pa-idx&quot;&gt;['+d.i+']&lt;/span&gt;'+
        '&lt;span class=&quot;pa-role&quot;&gt;'+d.role+'&lt;/span&gt;'+
        '&lt;span class=&quot;pa-label&quot;&gt;'+d.label+'&lt;/span&gt;';
      domlist.appendChild(row);
    });
  }

  var chips=[
    'Fill the form for John Smith at Acme, Enterprise tier',
    'Set email to john@acme.io',
    'Add note: renewal due in Q3',
    'Click Save',
    'Clear the form'
  ];
  chips.forEach(function(c){
    var b=document.createElement('button');
    b.className='pa-chip';b.textContent=c;
    b.addEventListener('click',function(){input.value=c;run();});
    chipsWrap.appendChild(b);
  });

  function setStatus(kind,msg){
    var cls=kind==='run'?'pa-status-run':kind==='done'?'pa-status-done':kind==='err'?'pa-status-err':'pa-status-idle';
    statusEl.innerHTML='&lt;span class=&quot;'+cls+'&quot;&gt;'+msg+'&lt;/span&gt;';
  }
  function traceLine(html){
    if(trace.querySelector('.pa-muted'))trace.innerHTML='';
    trace.innerHTML+=html+'\n';
    trace.scrollTop=trace.scrollHeight;
  }
  function call(fn,arg){
    traceLine('&lt;span class=&quot;pa-call&quot;&gt;'+fn+'&lt;/span&gt;(&lt;span class=&quot;pa-arg&quot;&gt;'+arg+'&lt;/span&gt;)');
  }
  function sleep(ms){return new Promise(function(r){setTimeout(r,ms)})}

  function showToast(msg){
    toast.textContent=msg;toast.classList.add('pa-show');
    setTimeout(function(){toast.classList.remove('pa-show')},2600);
  }

  function findByLabel(label){
    for(var k=0;k&lt;dom.length;k++){if(dom[k].label.toLowerCase()===label.toLowerCase())return dom[k]}
    return null;
  }
  function highlight(d){
    dom.forEach(function(x){x.el.classList.remove('pa-hl')});
    renderDom(d?d.i:0);
    if(d)d.el.classList.add('pa-hl');
  }
  function clearHl(){dom.forEach(function(x){x.el.classList.remove('pa-hl')});renderDom(0);}

  async function typeInto(d,text){
    highlight(d);
    call('inputText',d.i+', &quot;'+text+'&quot;');
    await sleep(320);
    d.el.value='';
    for(var c=0;c&lt;text.length;c++){d.el.value+=text[c];await sleep(16);}
    await sleep(180);
  }
  async function selectTier(d,val){
    highlight(d);
    call('selectOption',d.i+', &quot;'+val+'&quot;');
    await sleep(320);d.el.value=val;await sleep(220);
  }
  async function clickBtn(d){
    highlight(d);
    call('clickElement',d.i);
    await sleep(300);
    d.el.classList.add('pa-btn-flash');
    setTimeout(function(){d.el.classList.remove('pa-btn-flash')},500);
    await sleep(220);
  }

  // Very small rule-based parser (illustrative, not an LLM)
  function parse(cmd){
    var acts=[],t=cmd.trim(),low=t.toLowerCase();
    var m;
    // combined fill: &quot;... for NAME at COMPANY, TIER tier&quot;
    m=t.match(/for\s+(.+?)\s+at\s+(.+?),\s*(free|pro|enterprise)\s*tier/i);
    if(m){
      acts.push({type:'input',field:'name',val:m[1].trim()});
      acts.push({type:'input',field:'company',val:m[2].trim()});
      acts.push({type:'tier',val:cap(m[3])});
      return acts;
    }
    m=t.match(/(?:set|enter)\s+name\s+(?:to\s+)?(.+)/i);if(m){acts.push({type:'input',field:'name',val:m[1].trim()});return acts;}
    m=t.match(/(?:set|enter)\s+email\s+(?:to\s+)?(\S+)/i);if(m){acts.push({type:'input',field:'email',val:m[1].trim()});return acts;}
    m=t.match(/(?:set|enter)\s+company\s+(?:to\s+)?(.+)/i);if(m){acts.push({type:'input',field:'company',val:m[1].trim()});return acts;}
    m=t.match(/(?:set\s+)?tier\s+(?:to\s+)?(free|pro|enterprise)/i);if(m){acts.push({type:'tier',val:cap(m[1])});return acts;}
    m=t.match(/(?:add\s+note|set\s+notes?)\s*:?\s*(.+)/i);if(m){acts.push({type:'input',field:'notes',val:m[1].trim()});return acts;}
    if(/\b(save|submit)\b/.test(low)){acts.push({type:'click',field:'save'});return acts;}
    if(/\b(clear|reset|empty)\b/.test(low)){acts.push({type:'click',field:'clear'});return acts;}
    return acts;
  }
  function cap(s){return s.charAt(0).toUpperCase()+s.slice(1).toLowerCase()}

  var busy=false;
  async function run(){
    if(busy)return;
    var cmd=input.value.trim();
    if(!cmd){setStatus('idle','Type a command first');return;}
    var acts=parse(cmd);
    busy=true;runBtn.disabled=true;

    setStatus('run','Scanning DOM &amp;mdash; building dehydrated tree');
    call('updateTree','');
    renderDom(0);
    await sleep(520);

    if(!acts.length){
      setStatus('err','No matching action &amp;mdash; try an example chip');
      traceLine('&lt;span class=&quot;pa-muted&quot;&gt;// command not recognized by the demo parser&lt;/span&gt;');
      busy=false;runBtn.disabled=false;return;
    }

    setStatus('run','Executing '+acts.length+' action'+(acts.length&gt;1?'s':''));
    for(var a=0;a&lt;acts.length;a++){
      var act=acts[a];
      if(act.type==='input'){await typeInto(findByLabelForField(act.field),act.val);}
      else if(act.type==='tier'){await selectTier(fields.tier?domByEl(fields.tier):null,act.val);}
      else if(act.type==='click'){
        var d=domByEl(fields[act.field]);await clickBtn(d);
        if(act.field==='save'){showToast('Customer saved '+(fields.name.value?'\u2014 '+fields.name.value:''));}
        if(act.field==='clear'){clearForm();}
      }
    }
    clearHl();
    setStatus('done','Done \u2014 '+acts.length+' action'+(acts.length&gt;1?'s':'')+' completed');
    busy=false;runBtn.disabled=false;
  }

  function domByEl(el){for(var k=0;k&lt;dom.length;k++){if(dom[k].el===el)return dom[k]}return null}
  function findByLabelForField(field){
    var map={name:'Full name',email:'Email',company:'Company',notes:'Notes'};
    return findByLabel(map[field]);
  }
  function clearForm(){
    fields.name.value='';fields.email.value='';fields.company.value='';
    fields.tier.value='';fields.notes.value='';
  }

  runBtn.addEventListener('click',run);
  input.addEventListener('keydown',function(e){if(e.key==='Enter')run();});
  resetBtn.addEventListener('click',function(){
    clearForm();clearHl();trace.innerHTML='&lt;span class=&quot;pa-muted&quot;&gt;// run a command to see the trace&lt;/span&gt;';
    setStatus('idle','Idle');input.value='';
  });

  // tabs
  root.querySelectorAll('.pa-tab').forEach(function(tab){
    tab.addEventListener('click',function(){
      root.querySelectorAll('.pa-tab').forEach(function(x){x.classList.remove('pa-tab-active')});
      root.querySelectorAll('.pa-tabpane').forEach(function(x){x.classList.remove('pa-pane-active')});
      tab.classList.add('pa-tab-active');
      root.querySelector('#pa-pane-'+tab.getAttribute('data-tab')).classList.add('pa-pane-active');
    });
  });

  renderDom(0);

  // Auto-resize for WordPress iframe embedding
  function postHeight(){
    var h=root.offsetHeight+40;
    if(window.parent)window.parent.postMessage({paDemoHeight:h},'*');
  }
  window.addEventListener('load',postHeight);
  setTimeout(postHeight,300);
  var ro=new MutationObserver(postHeight);
  ro.observe(root,{subtree:true,childList:true,attributes:true});
  window.addEventListener('resize',postHeight);
})();
&lt;/script&gt;
&lt;/body&gt;&lt;/html&gt;"></iframe>
</div>
<script>
(function(){
  window.addEventListener("message",function(e){
    if(e&&e.data&&e.data.paDemoHeight){
      var f=document.getElementById("pa-embed-frame");
      if(f){f.style.height=e.data.paDemoHeight+"px";}
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Under the hood, the agent delegates work to a <code>PageController</code>:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">await this.pageController.updateTree()
await this.pageController.clickElement(index)
await this.pageController.inputText(index, text)
await this.pageController.scroll({ down: true, numPages: 1 })</code></pre></div></div>



<p class="wp-block-paragraph">The monorepo splits these concerns into small packages. <code>@page-agent/core</code> holds the headless agent logic. <code>page-agent</code> is the full entry class with a UI panel. <code>@page-agent/page-controller</code> handles DOM extraction and element indexing, with optional visual feedback through a <code>SimulatorMask</code>.</p>



<p class="wp-block-paragraph">Developers keep control of scope. Operation allowlists limit which actions the agent may run. Data masking can hide sensitive fields, such as passwords, from the model. Custom knowledge can be injected so the agent follows your domain rules.</p>



<h2 id="h-how-it-compares" class="wp-block-heading"><strong>How It Compares</strong></h2>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Approach</th><th>Where it runs</th><th>Reads the page via</th><th>Setup</th><th>Best fit</th></tr></thead><tbody><tr><td>Page Agent</td><td>Inside the page (client-side JS)</td><td>Dehydrated text DOM</td><td>One script tag or npm</td><td>Copilots inside apps you own</td></tr><tr><td>Selenium / Playwright / Puppeteer</td><td>External process</td><td>DOM via driver (WebDriver/CDP)</td><td>Driver plus runtime or server</td><td>Scripted end-to-end testing</td></tr><tr><td>browser-use</td><td>External process</td><td>DOM plus optional vision</td><td>Python plus a browser</td><td>Autonomous multi-site agents</td></tr><tr><td>WebMCP</td><td>Server-side tools</td><td>Structured function calls</td><td>Requires standard adoption</td><td>Native agent tool access</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">The takeaway is scope, not speed. Page Agent fits products you control and can add code to. External drivers still win for cross-site scraping and locked-down environments.</p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use Cases, With Examples</strong></h2>



<ul class="wp-block-list">
<li><strong>SaaS AI copilot</strong>: Ship an assistant that operates the product, not one that only gives instructions. A support bot can perform the steps for the user instead of describing them.</li>



<li><strong>Smart form filling</strong>: Collapse a multi-step ERP or CRM form into one instruction. A user types &#8216;Submit a travel expense for $50 for lunch yesterday.&#8217; The agent handles the navigation and data entry.</li>



<li><strong>Accessibility</strong>: Pair it with the Web Speech API for voice control. Any web app becomes reachable through natural language, with screen-reader friendly announcements.</li>



<li><strong>Legacy app modernization</strong>: It can wrap a legacy internal tool that has no API. You add a command bar without changing the original code.</li>
</ul>



<h2 id="h-quick-start" class="wp-block-heading"><strong>Quick Start</strong></h2>



<p class="wp-block-paragraph">For evaluation, one script tag loads Page Agent with a free testing LLM:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">&lt;script src="https://cdn.jsdelivr.net/npm/page-agent@1.10.0/dist/iife/page-agent.demo.js" crossorigin="true">&lt;/script></code></pre></div></div>



<p class="wp-block-paragraph">That demo endpoint is for technical evaluation only. Production use needs your own model credentials.</p>



<p class="wp-block-paragraph"><strong>For a build, install the package and configure your endpoint:</strong></p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import { PageAgent } from 'page-agent'

const agent = new PageAgent({
  model: 'qwen3.5-plus',
  baseURL: 'https://dashscope.aliyuncs.com/compatible-mode/v1',
  apiKey: 'YOUR_API_KEY',
  language: 'en-US',
})

await agent.execute('Click the login button')</code></pre></div></div>



<p class="wp-block-paragraph">The <code>model</code> and <code>baseURL</code> fields accept any OpenAI-compatible provider. Swapping models is mostly a base-URL and key change.</p>



<p class="wp-block-paragraph"><strong>Note:</strong> a key passed to <code>new PageAgent</code> ships inside your client bundle. For production, proxy requests through your own backend instead. The agent can also show each critical action for approval before running it.</p>



<h2 id="h-what-works-and-what-doesn-t" class="wp-block-heading"><strong>What Works and What Doesn&#8217;t</strong></h2>



<ul class="wp-block-list">
<li><strong>Strong integration story</strong>: A copilot ships in a few lines of code. There is no backend rewrite and no extension to distribute.</li>



<li><strong>Lower model cost</strong>: Because only text moves, you avoid multi-modal models and their pricing. Precision comes from reading structure, not guessing from pixels.</li>



<li><strong>Prompt-based safety has limits</strong>: Rules like &#8220;never auto-submit a payment form&#8221; live in the system prompt. These are persuasive guides, not hard guarantees. For sensitive or destructive actions, keep server-side validation. Prompt instructions should not be your only control.</li>



<li><strong>Single-page focus</strong>: The core library targets interactions within one view. It cannot move across tabs or windows by itself. Multi-page automation needs the optional Chrome extension, which requires separate install and permissions. A Beta MCP server also lets outside agents, like Claude Desktop or Copilot, drive it.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph">Check out the<strong> <a href="https://github.com/alibaba/page-agent" target="_blank" rel="noreferrer noopener">GitHub Repo</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/02/meet-alibabas-page-agent-a-javascript-in-page-gui-agent-that-controls-web-interfaces-with-natural-language-through-the-dom/">Meet Alibaba&#8217;s Page Agent: A JavaScript In-Page GUI Agent That Controls Web Interfaces With Natural Language Through the DOM</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80809</post-id>
	</item>
		<item>
		<title>The Google Health API Got a CLI: ghealth is an Open-Source Tool for Your Fitbit Air Data</title>
		<link>https://www.marktechpost.com/2026/07/02/the-google-health-api-got-a-cli-ghealth-is-an-open-source-tool-for-your-fitbit-air-data/</link>
		
		<dc:creator><![CDATA[Michal Sutter]]></dc:creator>
		<pubDate>Thu, 02 Jul 2026 08:46:56 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80806</guid>

					<description><![CDATA[<p>The Google Health API now has an open-source CLI. ghealth is a single Go binary that exposes 40 data types as agent-ready JSON. It is a community project, not an official Google release. Here's how it works, and what to check before granting OAuth access.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/02/the-google-health-api-got-a-cli-ghealth-is-an-open-source-tool-for-your-fitbit-air-data/">The Google Health API Got a CLI: ghealth is an Open-Source Tool for Your Fitbit Air Data</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">The <strong><a href="https://github.com/Google-Health-API/google-health-cli" target="_blank" rel="noreferrer noopener">Google Health API </a></strong>is the official successor to the Fitbit Web API. It targets the Google Health API v4 and moves developers onto Google OAuth 2.0. Now an open-source CLI command-line tool called <code>ghealth</code> wraps that API for terminals and AI agents.</p>



<p class="wp-block-paragraph">The tool is a single Go binary under the Apache 2.0 license. It exposes 40 verified data types as structured JSON. That design lets you pipe sleep, heart rate, and step data into an agent&#8217;s context.</p>



<h2 id="h-what-is-ghealth" class="wp-block-heading"><strong>What is ghealth?</strong></h2>



<p class="wp-block-paragraph"><code>ghealth</code> is a wrapper over the Google Health API v4. You build it from source with <code>go build -o ghealth .</code>. It ships as one self-contained binary.</p>



<p class="wp-block-paragraph">The tool is explicitly agent-first. Every command returns simplified JSON with a stable shape. It also provides deterministic exit codes, a <code>--dry-run</code> flag, and a <code>--raw</code> flag.</p>



<p class="wp-block-paragraph">The repository ships two Agent Skills as <code>SKILL.md</code> files. One covers auth, setup, and global flags. The other documents all 40 data types, operations, patterns, and gotchas. Agents install them with <code>npx skills add</code>.</p>



<p class="wp-block-paragraph">The CLI lives under the <code>Google-Health-API</code> GitHub organization. That organization also hosts long-standing Fitbit open-source repositories.</p>



<h2 id="h-the-data-surface-40-verified-types" class="wp-block-heading"><strong>The Data Surface: 40 Verified Types</strong></h2>



<p class="wp-block-paragraph">The 40 types cover most Fitbit and Pixel Watch signals. Examples include <code>steps</code>, <code>heart-rate</code>, <code>sleep</code>, <code>weight</code>, <code>oxygen-saturation</code>, and <code>heart-rate-variability</code>. Clinical types like <code>electrocardiogram</code> require the <code>ecg.readonly</code> scope.</p>



<p class="wp-block-paragraph">Each type supports a subset of operations. Common ones are <code>list</code>, <code>rollup</code>, <code>daily-rollup</code>, and <code>reconcile</code>. Writable types (<code>exercise</code>, <code>sleep</code>, <code>weight</code>, <code>body-fat</code>, <code>height</code>) add <code>create</code>, <code>update</code>, and <code>delete</code>.</p>



<p class="wp-block-paragraph">The <code>reconcile</code> operation merges overlapping data points from multiple sources. That mirrors the Reconciled Stream in the v4 API.</p>



<p class="wp-block-paragraph">Sleep is a good example for pattern analysis. The default <code>list</code> returns a summary. Adding <code>--detail</code> returns stage-by-stage data (awake, deep, REM). That helps you spot patterns week over week.</p>



<h2 id="h-setup-what-actually-happens" class="wp-block-heading"><strong>Setup: What Actually Happens</strong></h2>



<p class="wp-block-paragraph">Setup runs through one command: <code>ghealth setup</code>. A wizard walks you through the GCP project and OAuth. You create a Desktop-type OAuth client in the Google Cloud Console.</p>



<p class="wp-block-paragraph">You bring your own OAuth credentials. The tool holds no shared key. Files are written under <code>~/.config/ghealth/</code> with file mode 0600. Tokens refresh automatically.</p>



<p class="wp-block-paragraph">All Google Health API scopes are classified as Restricted. Google requires a privacy and security review for production access. For personal use, you authorize your own project against your own account. The API returns data from Fitbit, Pixel Watch, and connected third-party sources.</p>



<p class="wp-block-paragraph">The headless flow uses PKCE with an S256 challenge. It also validates a random <code>state</code> parameter on completion.</p>



<h2 id="h-hands-on-commands-and-output" class="wp-block-heading"><strong>Hands-On: Commands and Output</strong></h2>



<p class="wp-block-paragraph">Reading data is consistent across types. Every read returns an object with rows under <code>dataPoints</code>.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php"># Recent heart rate readings
ghealth data heart-rate list --from today --limit 10

# Daily step totals for a week
ghealth data steps daily-rollup --from 2026-03-22 --to 2026-03-29

# Sleep stages for the last five nights
ghealth data sleep list --limit 5 --detail</code></pre></div></div>



<p class="wp-block-paragraph">Step totals return aggregated JSON:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">{
  "dataPoints": [
    {"date": "2026-03-28", "countSum": "9037"},
    {"date": "2026-03-27", "countSum": "2408"}
  ]
}</code></pre></div></div>



<p class="wp-block-paragraph">Output is simplified by default. Use <code>--raw</code> for the original API response. Use <code>--format csv</code> or <code>--format table</code> for other shapes. The <code>-o</code> flag writes a file and prints a schema preview.</p>



<p class="wp-block-paragraph">Pagination is lossless. A large <code>list</code> returns a <code>nextPageToken</code>. You pass it back with <code>--page-token</code> to fetch the next page.</p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use Cases With Examples</strong></h2>



<ul class="wp-block-list">
<li><strong>Feed sleep patterns into an agent</strong>: Pull several nights with <code>--detail</code>. Pipe the JSON into a Claude Code or Codex session. Ask the agent to summarize deep-sleep trends over the week.</li>



<li><strong>Load workouts into pandas</strong>: Run <code>ghealth data exercise export-tcx --id &lt;id> --output ride.csv --as csv</code>. Each row is one trackpoint with heart rate and GPS. Then run <code>pd.read_csv</code> on the file.</li>



<li><strong>Build a resting heart-rate view</strong>: Query <code>daily-resting-heart-rate</code> over 30 days. Emit CSV with <code>--format csv</code>. Chart it in a notebook or a dashboard.</li>
</ul>



<h2 id="h-how-ghealth-compares" class="wp-block-heading"><strong>How ghealth Compares</strong></h2>



<p class="wp-block-paragraph">The table below sets <code>ghealth</code> against the raw API and two other CLIs. The other two CLIs both self-identify as unofficial.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Attribute</th><th>ghealth (this CLI)</th><th>Google Health API v4 (direct REST)</th><th>rudrankriyam/Google-Health-CLI</th><th>googlehealth-cli (npm)</th></tr></thead><tbody><tr><td>Install</td><td><code>git clone</code> + <code>go build</code></td><td>None; call HTTP/gRPC yourself</td><td>Build from Go source</td><td><code>npm i -g googlehealth-cli</code></td></tr><tr><td>Language</td><td>Go, single binary</td><td>Any</td><td>Go</td><td>Node.js</td></tr><tr><td>Auth</td><td>Your own OAuth client, PKCE S256</td><td>Google OAuth 2.0</td><td>Your own OAuth client</td><td>Your own OAuth client</td></tr><tr><td>Agent output</td><td>Simplified JSON, exit codes, <code>SKILL.md</code></td><td>Raw JSON / gRPC</td><td>Predictable JSON</td><td>Stable <code>--json</code> envelope</td></tr><tr><td>Data types</td><td>40 verified against live API</td><td>Full v4 surface</td><td>Tracks documented v4 surface</td><td>Subset of types</td></tr><tr><td>Official status</td><td>No; community, in Google-Health-API org</td><td>Yes; Google</td><td>No; states unofficial</td><td>No; states unaffiliated</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">For raw control, the direct REST API is the ground truth. For terminal and agent use, <code>ghealth</code> reduces auth and formatting boilerplate.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- ghealth CLI interactive demo — paste into a WordPress "Custom HTML" block -->
<div id="mtp-ghealth-embed" style="max-width:860px;margin:0 auto;">
  <iframe
    id="mtp-ghealth-iframe"
    title="ghealth CLI playground"
    loading="lazy"
    style="width:100%;border:0;height:600px;overflow:hidden;"
    srcdoc="&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;style&gt;
  #ghp-root *{box-sizing:border-box;margin:0;padding:0}
  #ghp-root{
    font-family:Inter,-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Arial,sans-serif;
    color:#1c2620;background:#f6f8f5;border:1px solid #e2e8dd;border-radius:14px;
    padding:20px;max-width:820px;margin:0 auto;line-height:1.5;
  }
  #ghp-root h2{font-size:18px;font-weight:700;color:#152018;margin-bottom:2px;letter-spacing:-.01em}
  #ghp-root .ghp-sub{font-size:13px;color:#5c6b60;margin-bottom:16px}
  #ghp-root .ghp-badge{
    display:inline-block;font-size:11px;font-weight:600;color:#3a5a00;background:#eef6dd;
    border:1px solid #cfe39a;border-radius:999px;padding:3px 10px;margin-bottom:14px;letter-spacing:.02em
  }
  #ghp-root .ghp-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:12px}
  #ghp-root label{display:block;font-size:11px;font-weight:600;color:#5c6b60;margin-bottom:4px;text-transform:uppercase;letter-spacing:.04em}
  #ghp-root select,#ghp-root input[type=number]{
    width:100%;font-size:13px;font-family:inherit;color:#1c2620;background:#fff;
    border:1px solid #d5ded0;border-radius:8px;padding:8px 10px;appearance:none;-webkit-appearance:none
  }
  #ghp-root select:focus,#ghp-root input:focus{outline:none;border-color:#76B900;box-shadow:0 0 0 3px rgba(118,185,0,.15)}
  #ghp-root .ghp-check{display:flex;align-items:center;gap:8px;font-size:13px;color:#1c2620;margin:2px 0 14px}
  #ghp-root .ghp-check input{width:16px;height:16px;accent-color:#76B900}
  #ghp-root .ghp-check.ghp-disabled{opacity:.4}
  #ghp-root .ghp-cmdbar{display:flex;align-items:stretch;gap:8px;margin-bottom:10px}
  #ghp-root .ghp-cmd{
    flex:1;font-family:&quot;JetBrains Mono&quot;,ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;
    font-size:13px;color:#0f2b00;background:#eef4e4;border:1px solid #d5e3bf;border-radius:8px;
    padding:11px 12px;overflow-x:auto;white-space:nowrap
  }
  #ghp-root .ghp-cmd .ghp-tok{color:#4a7c00;font-weight:600}
  #ghp-root .ghp-cmd .ghp-flag{color:#8a6d00}
  #ghp-root .ghp-copy{
    font-family:inherit;font-size:12px;font-weight:600;color:#152018;background:#fff;
    border:1px solid #d5ded0;border-radius:8px;padding:0 14px;cursor:pointer;white-space:nowrap
  }
  #ghp-root .ghp-copy:hover{border-color:#76B900;color:#3a5a00}
  #ghp-root .ghp-out{
    font-family:&quot;JetBrains Mono&quot;,ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;
    font-size:12.5px;line-height:1.55;color:#22322a;background:#fbfdf9;border:1px solid #e2e8dd;
    border-radius:10px;padding:14px 16px;overflow-x:auto;white-space:pre;min-height:120px
  }
  #ghp-root .ghp-out .k{color:#4a7c00}
  #ghp-root .ghp-out .s{color:#8a4b00}
  #ghp-root .ghp-out .n{color:#0a5aa0}
  #ghp-root .ghp-out .p{color:#8a9485}
  #ghp-root .ghp-note{font-size:12px;color:#5c6b60;margin-top:10px}
  #ghp-root .ghp-note code{background:#eef4e4;border-radius:4px;padding:1px 5px;font-family:&quot;JetBrains Mono&quot;,monospace;font-size:11.5px;color:#3a5a00}
  #ghp-root .ghp-foot{margin-top:16px;padding-top:12px;border-top:1px solid #e2e8dd;font-size:12px;color:#7a8a7e}
  #ghp-root .ghp-foot b{color:#76B900;font-weight:700}
  @media (max-width:640px){
    #ghp-root .ghp-grid{grid-template-columns:repeat(2,1fr)}
    #ghp-root{padding:16px}
  }
&lt;/style&gt;

&lt;div id=&quot;ghp-root&quot;&gt;
  &lt;div class=&quot;ghp-badge&quot;&gt;Illustrative sample data — not a live API call&lt;/div&gt;
  &lt;h2&gt;ghealth CLI playground&lt;/h2&gt;
  &lt;p class=&quot;ghp-sub&quot;&gt;Build a &lt;code style=&quot;font-family:monospace&quot;&gt;ghealth&lt;/code&gt; command and preview the JSON, table, or CSV output shape. Outputs mirror the real response envelope; the values are illustrative.&lt;/p&gt;

  &lt;div class=&quot;ghp-grid&quot;&gt;
    &lt;div&gt;
      &lt;label for=&quot;ghp-type&quot;&gt;Data type&lt;/label&gt;
      &lt;select id=&quot;ghp-type&quot;&gt;
        &lt;option value=&quot;heart-rate&quot;&gt;heart-rate&lt;/option&gt;
        &lt;option value=&quot;steps&quot;&gt;steps&lt;/option&gt;
        &lt;option value=&quot;sleep&quot;&gt;sleep&lt;/option&gt;
        &lt;option value=&quot;weight&quot;&gt;weight&lt;/option&gt;
        &lt;option value=&quot;exercise&quot;&gt;exercise&lt;/option&gt;
        &lt;option value=&quot;daily-resting-heart-rate&quot;&gt;daily-resting-heart-rate&lt;/option&gt;
      &lt;/select&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;label for=&quot;ghp-op&quot;&gt;Operation&lt;/label&gt;
      &lt;select id=&quot;ghp-op&quot;&gt;&lt;/select&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;label for=&quot;ghp-fmt&quot;&gt;Format&lt;/label&gt;
      &lt;select id=&quot;ghp-fmt&quot;&gt;
        &lt;option value=&quot;json&quot;&gt;json (default)&lt;/option&gt;
        &lt;option value=&quot;table&quot;&gt;table&lt;/option&gt;
        &lt;option value=&quot;csv&quot;&gt;csv&lt;/option&gt;
      &lt;/select&gt;
    &lt;/div&gt;
    &lt;div&gt;
      &lt;label for=&quot;ghp-limit&quot;&gt;--limit&lt;/label&gt;
      &lt;input id=&quot;ghp-limit&quot; type=&quot;number&quot; min=&quot;1&quot; max=&quot;50&quot; value=&quot;3&quot;&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ghp-check&quot; id=&quot;ghp-detail-wrap&quot;&gt;
    &lt;input type=&quot;checkbox&quot; id=&quot;ghp-detail&quot;&gt;
    &lt;label for=&quot;ghp-detail&quot; style=&quot;text-transform:none;margin:0;font-weight:500;color:#1c2620&quot;&gt;
      &lt;code style=&quot;font-family:monospace&quot;&gt;--detail&lt;/code&gt; &amp;nbsp;stage-by-stage sleep breakdown (awake, deep, REM, light)
    &lt;/label&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ghp-cmdbar&quot;&gt;
    &lt;div class=&quot;ghp-cmd&quot; id=&quot;ghp-cmd&quot;&gt;&lt;/div&gt;
    &lt;button class=&quot;ghp-copy&quot; id=&quot;ghp-copy&quot;&gt;Copy&lt;/button&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ghp-out&quot; id=&quot;ghp-out&quot;&gt;&lt;/div&gt;

  &lt;p class=&quot;ghp-note&quot; id=&quot;ghp-note&quot;&gt;&lt;/p&gt;

  &lt;div class=&quot;ghp-foot&quot;&gt;Built by &lt;b&gt;Marktechpost&lt;/b&gt; · Educational demo of the open-source &lt;code style=&quot;font-family:monospace&quot;&gt;ghealth&lt;/code&gt; CLI for the Google Health API v4&lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var root = document.getElementById('ghp-root');
  var $ = function(id){return document.getElementById(id);};

  // Operations available per type (subset of the CLI's real surface)
  var OPS = {
    'heart-rate':['list'],
    'steps':['list','daily-rollup'],
    'sleep':['list'],
    'weight':['list'],
    'exercise':['list'],
    'daily-resting-heart-rate':['list']
  };

  // Illustrative sample rows keyed by type+op. Shapes follow the CLI README envelope.
  function sample(type, op, detail){
    if(type==='heart-rate' &amp;&amp; op==='list') return [
      {time:'2026-03-29T16:33:07+01:00', beatsPerMinute:'80', source:'Google Pixel Watch 4 (41mm)'},
      {time:'2026-03-29T16:33:02+01:00', beatsPerMinute:'79', source:'Google Pixel Watch 4 (41mm)'},
      {time:'2026-03-29T16:32:57+01:00', beatsPerMinute:'81', source:'Google Pixel Watch 4 (41mm)'}
    ];
    if(type==='steps' &amp;&amp; op==='list') return [
      {'interval.startTime':'2026-03-29T08:00:00+01:00', 'interval.endTime':'2026-03-29T08:01:00+01:00'},
      {'interval.startTime':'2026-03-29T08:01:00+01:00', 'interval.endTime':'2026-03-29T08:02:00+01:00'},
      {'interval.startTime':'2026-03-29T08:02:00+01:00', 'interval.endTime':'2026-03-29T08:03:00+01:00'}
    ];
    if(type==='steps' &amp;&amp; op==='daily-rollup') return [
      {date:'2026-03-28', countSum:'9037'},
      {date:'2026-03-27', countSum:'2408'},
      {date:'2026-03-26', countSum:'6474'}
    ];
    if(type==='sleep'){
      if(detail) return [
        {date:'2026-03-29', minutesAsleep:'421', minutesAwake:'38', 'stageMinutes.deep':'74', 'stageMinutes.rem':'96', 'stageMinutes.light':'251', 'stageMinutes.awake':'38'},
        {date:'2026-03-28', minutesAsleep:'389', minutesAwake:'44', 'stageMinutes.deep':'61', 'stageMinutes.rem':'82', 'stageMinutes.light':'246', 'stageMinutes.awake':'44'},
        {date:'2026-03-27', minutesAsleep:'447', minutesAwake:'29', 'stageMinutes.deep':'88', 'stageMinutes.rem':'104', 'stageMinutes.light':'255', 'stageMinutes.awake':'29'}
      ];
      return [
        {date:'2026-03-29', minutesAsleep:'421', minutesAwake:'38'},
        {date:'2026-03-28', minutesAsleep:'389', minutesAwake:'44'},
        {date:'2026-03-27', minutesAsleep:'447', minutesAwake:'29'}
      ];
    }
    if(type==='weight') return [
      {time:'2026-03-29T07:12:00+01:00', weightGrams:'75500', source:'Aria Air'},
      {time:'2026-03-27T07:05:00+01:00', weightGrams:'75720', source:'Aria Air'},
      {time:'2026-03-25T07:20:00+01:00', weightGrams:'75880', source:'Aria Air'}
    ];
    if(type==='exercise') return [
      {time:'2026-03-29T18:02:00+01:00', type:'RUN', durationMin:'42', calories:'511', avgHeartRate:'148'},
      {time:'2026-03-27T07:31:00+01:00', type:'BIKE', durationMin:'68', calories:'642', avgHeartRate:'131'},
      {time:'2026-03-25T19:10:00+01:00', type:'STRENGTH', durationMin:'35', calories:'240', avgHeartRate:'112'}
    ];
    if(type==='daily-resting-heart-rate') return [
      {date:'2026-03-28', beatsPerMinute:'58'},
      {date:'2026-03-27', beatsPerMinute:'57'},
      {date:'2026-03-26', beatsPerMinute:'59'}
    ];
    return [];
  }

  function clampLimit(rows, limit){
    var n = Math.max(1, Math.min(rows.length, limit||rows.length));
    return rows.slice(0, n);
  }

  // ---- renderers ----
  function esc(s){return String(s).replace(/&amp;/g,'&amp;amp;').replace(/&lt;/g,'&amp;lt;').replace(/&gt;/g,'&amp;gt;');}

  function renderJSON(rows){
    var lines = [];
    lines.push('{');
    lines.push('  &lt;span class=&quot;k&quot;&gt;&quot;dataPoints&quot;&lt;/span&gt;: [');
    rows.forEach(function(r,i){
      var keys = Object.keys(r);
      var parts = keys.map(function(k){
        return '&lt;span class=&quot;k&quot;&gt;&quot;'+esc(k)+'&quot;&lt;/span&gt;: &lt;span class=&quot;s&quot;&gt;&quot;'+esc(r[k])+'&quot;&lt;/span&gt;';
      });
      lines.push('    {'+parts.join(', ')+'}'+(i&lt;rows.length-1?',':''));
    });
    lines.push('  ]');
    lines.push('}');
    return lines.join('\n');
  }

  function columns(rows){
    var seen = {}, cols = [];
    rows.forEach(function(r){Object.keys(r).forEach(function(k){if(!seen[k]){seen[k]=1;cols.push(k);}});});
    return cols;
  }

  function renderTable(rows){
    var cols = columns(rows);
    var widths = cols.map(function(c){
      var w = c.length;
      rows.forEach(function(r){var v=(r[c]==null?'':String(r[c]));if(v.length&gt;w)w=v.length;});
      return w;
    });
    function pad(v,w){v=String(v==null?'':v);while(v.length&lt;w)v+=' ';return v;}
    var head = cols.map(function(c,i){return '&lt;span class=&quot;k&quot;&gt;'+esc(pad(c,widths[i]))+'&lt;/span&gt;';}).join('  ');
    var sep = cols.map(function(c,i){return esc(Array(widths[i]+1).join('-'));}).join('  ');
    var body = rows.map(function(r){
      return cols.map(function(c,i){return esc(pad(r[c]==null?'':r[c],widths[i]));}).join('  ');
    }).join('\n');
    return head+'\n'+sep+'\n'+body;
  }

  function renderCSV(rows){
    var cols = columns(rows);
    var head = '&lt;span class=&quot;k&quot;&gt;'+esc(cols.join(','))+'&lt;/span&gt;';
    var body = rows.map(function(r){
      return cols.map(function(c){var v=(r[c]==null?'':String(r[c]));return /[&quot;,\n]/.test(v)?'&quot;'+v.replace(/&quot;/g,'&quot;&quot;')+'&quot;':esc(v);}).join(',');
    }).join('\n');
    return head+'\n'+body;
  }

  // ---- command string ----
  function buildCmd(type, op, fmt, detail, limit){
    var html = '&lt;span class=&quot;ghp-tok&quot;&gt;ghealth&lt;/span&gt; data '+esc(type)+' '+esc(op);
    if(op==='list'){
      html += ' &lt;span class=&quot;ghp-flag&quot;&gt;--from&lt;/span&gt; today';
      html += ' &lt;span class=&quot;ghp-flag&quot;&gt;--limit&lt;/span&gt; '+esc(limit);
      if(type==='sleep' &amp;&amp; detail) html += ' &lt;span class=&quot;ghp-flag&quot;&gt;--detail&lt;/span&gt;';
    } else if(op==='daily-rollup'){
      html += ' &lt;span class=&quot;ghp-flag&quot;&gt;--from&lt;/span&gt; 2026-03-26 &lt;span class=&quot;ghp-flag&quot;&gt;--to&lt;/span&gt; 2026-03-29';
    }
    if(fmt!=='json') html += ' &lt;span class=&quot;ghp-flag&quot;&gt;--format&lt;/span&gt; '+esc(fmt);
    return html;
  }
  function plainCmd(type, op, fmt, detail, limit){
    var s = 'ghealth data '+type+' '+op;
    if(op==='list'){s+=' --from today --limit '+limit;if(type==='sleep'&amp;&amp;detail)s+=' --detail';}
    else if(op==='daily-rollup'){s+=' --from 2026-03-26 --to 2026-03-29';}
    if(fmt!=='json') s+=' --format '+fmt;
    return s;
  }

  // ---- notes ----
  function noteFor(type, op){
    if(type==='steps' &amp;&amp; op==='list') return 'Note: &lt;code&gt;steps list&lt;/code&gt; returns minute intervals with no counts. Use &lt;code&gt;daily-rollup&lt;/code&gt; to get real totals (&lt;code&gt;countSum&lt;/code&gt;).';
    if(op==='daily-rollup') return 'Note: a missing date means the device was not worn — not a zero. A &lt;code&gt;countSum&lt;/code&gt; of &quot;0&quot; is a true zero.';
    if(type==='sleep') return 'Note: default output is a summary. Add &lt;code&gt;--detail&lt;/code&gt; for per-stage minutes (awake, deep, REM, light).';
    return 'Note: reads return an object with rows under &lt;code&gt;dataPoints&lt;/code&gt;. Add &lt;code&gt;--raw&lt;/code&gt; for the original API response.';
  }

  // ---- wiring ----
  function refreshOps(){
    var type = $('ghp-type').value;
    var ops = OPS[type];
    var sel = $('ghp-op');
    var prev = sel.value;
    sel.innerHTML = '';
    ops.forEach(function(o){
      var opt = document.createElement('option');
      opt.value = o; opt.textContent = o + (o==='list'?' (default)':'');
      sel.appendChild(opt);
    });
    if(ops.indexOf(prev)&gt;=0) sel.value = prev;
  }

  function syncDetail(){
    var type = $('ghp-type').value, op = $('ghp-op').value;
    var wrap = $('ghp-detail-wrap');
    var enabled = (type==='sleep' &amp;&amp; op==='list');
    wrap.classList.toggle('ghp-disabled', !enabled);
    $('ghp-detail').disabled = !enabled;
    if(!enabled) $('ghp-detail').checked = false;
  }

  function postHeight(){
    try{
      var h = root.offsetHeight + 40; // component's own offsetHeight + 40 (never scrollHeight)
      parent.postMessage({type:'ghealth-resize', height:h}, '*');
    }catch(e){}
  }

  function render(){
    syncDetail();
    var type = $('ghp-type').value, op = $('ghp-op').value, fmt = $('ghp-fmt').value;
    var detail = $('ghp-detail').checked;
    var limit = parseInt($('ghp-limit').value,10)||3;

    $('ghp-cmd').innerHTML = buildCmd(type, op, fmt, detail, limit);
    $('ghp-copy').setAttribute('data-cmd', plainCmd(type, op, fmt, detail, limit));

    var rows = clampLimit(sample(type, op, detail), (op==='list'?limit:3));
    var out;
    if(fmt==='json') out = renderJSON(rows);
    else if(fmt==='table') out = renderTable(rows);
    else out = renderCSV(rows);
    $('ghp-out').innerHTML = out;

    $('ghp-note').innerHTML = noteFor(type, op);
    postHeight();
  }

  $('ghp-type').addEventListener('change', function(){refreshOps();render();});
  $('ghp-op').addEventListener('change', render);
  $('ghp-fmt').addEventListener('change', render);
  $('ghp-detail').addEventListener('change', render);
  $('ghp-limit').addEventListener('input', render);

  $('ghp-copy').addEventListener('click', function(){
    var t = this.getAttribute('data-cmd')||'';
    var btn = this;
    function done(){btn.textContent='Copied';setTimeout(function(){btn.textContent='Copy';},1200);}
    if(navigator.clipboard &amp;&amp; navigator.clipboard.writeText){navigator.clipboard.writeText(t).then(done,done);}
    else{var ta=document.createElement('textarea');ta.value=t;document.body.appendChild(ta);ta.select();try{document.execCommand('copy');}catch(e){}document.body.removeChild(ta);done();}
  });

  refreshOps();
  render();
  window.addEventListener('load', postHeight);
  window.addEventListener('resize', postHeight);
  setTimeout(postHeight, 300);
})();
&lt;/script&gt;
"></iframe>
</div>
<script>
(function(){
  var f = document.getElementById('mtp-ghealth-iframe');
  window.addEventListener('message', function(e){
    if(e && e.data && e.data.type === 'ghealth-resize' && e.data.height){
      f.style.height = e.data.height + 'px';
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/Google-Health-API/google-health-cli" target="_blank" rel="noreferrer noopener">Repo</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/02/the-google-health-api-got-a-cli-ghealth-is-an-open-source-tool-for-your-fitbit-air-data/">The Google Health API Got a CLI: ghealth is an Open-Source Tool for Your Fitbit Air Data</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80806</post-id>
	</item>
		<item>
		<title>Using Lift to Turn Research PDFs into Structured JSON with Controlled, Schema-Guided Field-Level Evaluation</title>
		<link>https://www.marktechpost.com/2026/07/01/using-lift-to-turn-research-pdfs-into-structured-json-with-controlled-schema-guided-field-level-evaluation/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Wed, 01 Jul 2026 21:09:53 +0000</pubDate>
				<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[OCR]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80799</guid>

					<description><![CDATA[<p>In this tutorial, we build a full PDF-to-structured-data workflow around Lift, built for controlled evaluation rather than a one-off demo. We prepare a Colab GPU environment, load Lift in 4-bit NF4, and generate synthetic research reports with deliberate distractors. We then run schema-guided extraction, score every field against ground truth, and assemble the results into a queryable knowledge base. The result is a repeatable extraction benchmark, not just raw model outputs.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/01/using-lift-to-turn-research-pdfs-into-structured-json-with-controlled-schema-guided-field-level-evaluation/">Using Lift to Turn Research PDFs into Structured JSON with Controlled, Schema-Guided Field-Level Evaluation</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this tutorial, <a href="https://pxllnk.co/rc5yap">we build a complete PDF-to-structured-data extraction</a> workflow around<a href="https://pxllnk.co/rc5yap" target="_blank" rel="noreferrer noopener"> <strong>Lift</strong></a>, with a focus on controlled evaluation rather than a simple demo run. We begin by preparing a Colab-compatible GPU environment, selecting the appropriate precision mode for the available hardware, and patching model loading to ensure the Lift backend runs reliably even on constrained 16 GB GPUs via 4-bit NF4 quantization. From there, we generate synthetic multi-page research reports with deliberately placed distractors, including validation-versus-test metric ambiguity, baseline-versus-proposed-model comparisons, missing code-release cases, and boolean state-of-the-art claims. This provides a realistic testbed for schema-guided extraction, in which the model must recover titles, authors, datasets, metrics, hyperparameters, limitations, and repository links from document layouts rather than plain text.</p>



<h2 id="h-configuring-runtime-and-dependencies" class="wp-block-heading"><strong>Configuring Runtime and Dependencies</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">N_DOCS               = 3
FORCE_FULL_PRECISION = False
FORCE_4BIT           = False
SHOW_FIRST_PAGE      = True
RUN_ON_REAL_PDF      = False
REAL_PDF_URL         = "https://arxiv.org/pdf/1512.03385"
REAL_PDF_PAGES       = "0-3"
PIN_PILLOW           = True
PILLOW_VERSION       = "11.3.0"
import os, sys, subprocess, json, re, time, warnings
warnings.filterwarnings("ignore")
os.environ["TOKENIZERS_PARALLELISM"] = "false"
def pip(*pkgs, upgrade=False):
   """Install without invoking a shell (so '[hf]' is never glob-expanded)."""
   args = [sys.executable, "-m", "pip", "install", "-q"] + (["-U"] if upgrade else []) + list(pkgs)
   print("  pip install", *pkgs)
   subprocess.run(args, check=False)
print("STEP 1/7 · Installing lift + light dependencies (first run is the slow one)…")
pip("reportlab", "pypdfium2", "pandas", "matplotlib")
pip("lift-pdf[hf]")
pip("bitsandbytes", "accelerate", upgrade=True)
if PIN_PILLOW:
   pip(f"pillow=={PILLOW_VERSION}")
   if "PIL" in sys.modules:
       import PIL
       if getattr(PIL, "__version__", "") != PILLOW_VERSION:
           print(f"     Pinned Pillow {PILLOW_VERSION} on disk, but a stale Pillow "
                 f"({getattr(PIL, '__version__', '?')}) is already loaded in memory.")
           print("     Restarting the runtime now — just re-run the cell(s) after it reconnects.")
           os.kill(os.getpid(), 9)
print("     …install finished.\n")
import torch
</code></pre></div></div>



<p class="wp-block-paragraph">We <a href="https://pxllnk.co/rc5yap">configure the tutorial runtime by defining the main execution knobs for corpus size, precision mode, preview rendering, and optional real-PDF extraction</a>. We also install the core dependencies required for PDF generation, rendering, plotting, and <a href="https://pxllnk.co/rc5yap" target="_blank" rel="noreferrer noopener"><strong>Lift</strong></a>’s Hugging Face backend. The Pillow pinning logic is important because it prevents a known Colab compatibility issue in which newer Pillow builds can break downstream imports via torchvision and transformers.</p>



<h2 class="wp-block-heading"><strong>Loading Lift 4-bit Backend</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def detect_gpu():
   if not torch.cuda.is_available():
       raise SystemExit(
           "\n✗ No CUDA GPU found. In Colab: Runtime ▸ Change runtime type ▸ GPU "
           "(A100 is best; L4/T4 also work).\n"
       )
   p  = torch.cuda.get_device_properties(0)
   cc = torch.cuda.get_device_capability(0)
   return p.name, p.total_memory / 1e9, cc
def enable_4bit(compute_dtype):
   """
   Load lift's weights in 4-bit NF4 no matter which transformers Auto* class it uses
   internally. We inject a quantization_config + on-GPU device_map, and neutralize any
   later model.to()/.cuda() (which is illegal on a bnb-quantized model). This is what lets
   a ~10 B model fit on a 16 GB T4 / 24 GB L4.
   """
   import inspect, functools, transformers
   from transformers import BitsAndBytesConfig
   bnb = BitsAndBytesConfig(
       load_in_4bit=True,
       bnb_4bit_quant_type="nf4",
       bnb_4bit_use_double_quant=True,
       bnb_4bit_compute_dtype=compute_dtype,
   )
   def patch(cls):
       try:
           cm   = inspect.getattr_static(cls, "from_pretrained")
           orig = cm.__func__ if isinstance(cm, (classmethod, staticmethod)) else cm
       except Exception:
           return
       @functools.wraps(orig)
       def inner(cls_, *args, **kwargs):
           kwargs.setdefault("quantization_config", bnb)
           kwargs.setdefault("device_map", {"": 0})
           model = orig(cls_, *args, **kwargs)
           try:
               model.to   = lambda *a, **k: model
               model.cuda = lambda *a, **k: model
           except Exception:
               pass
           return model
       cls.from_pretrained = classmethod(inner)
   for name in ["AutoModelForImageTextToText", "AutoModelForMultimodalLM",
                "AutoModelForVision2Seq", "AutoModelForCausalLM", "AutoModel"]:
       c = getattr(transformers, name, None)
       if c is not None:
           patch(c)
   try:
       from transformers.modeling_utils import PreTrainedModel
       patch(PreTrainedModel)
   except Exception:
       pass
print("STEP 2/7 · Preparing the model backend…")
gpu_name, vram, cc = detect_gpu()
use_4bit      = FORCE_4BIT or (vram &lt; 34 and not FORCE_FULL_PRECISION)
compute_dtype = torch.bfloat16 if cc[0] >= 8 else torch.float16
print(f"     GPU: {gpu_name} | ~{vram:.0f} GB | compute capability {cc[0]}.{cc[1]}")
print(f"     Load mode: {'4-bit NF4' if use_4bit else 'full bf16'} (compute dtype {compute_dtype})")
os.environ.setdefault("TORCH_DEVICE", "cuda:0")
os.environ.setdefault("MODEL_CHECKPOINT", "datalab-to/lift")
if use_4bit:
   enable_4bit(compute_dtype)
from lift import extract
from lift.model import InferenceManager
print("     Loading lift weights (≈20 GB download on first run)…")
_t = time.time()
MODEL = InferenceManager(method="hf")
print(f"     ✓ model ready in {time.time() - _t:.0f}s\n")
def run_lift(pdf_path, schema, page_range=None):
   kw = {"model": MODEL}
   if page_range:
       kw["page_range"] = page_range
   result = extract(pdf_path, schema, **kw)
   return getattr(result, "extraction", None)
</code></pre></div></div>



<p class="wp-block-paragraph">We prepare the <a href="https://pxllnk.co/rc5yap" target="_blank" rel="noreferrer noopener"><strong>Lift</strong></a> inference backend by detecting available CUDA GPUs, estimating VRAM usage, and choosing between full-precision and 4-bit NF4 loading. The 4-bit patch injects a BitsAndBytes quantization configuration into compatible Transformers model loaders, allowing the model to fit on smaller GPUs such as T4 or L4. <a href="https://pxllnk.co/rc5yap">We then initialize a reusable InferenceManager that avoids reloading the model for each document and makes the extraction pipeline practical for batch processing.</a></p>



<h2 class="wp-block-heading"><strong>Building the Synthetic Corpus</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">DOCS = [
   dict(
       title="SolarNet: Efficient Land-Cover Classification from Multispectral Satellite Imagery",
       authors=[("Maya Okafor", "TU Delft"),
                ("Liang Wei", "TU Delft"),
                ("Priya Ramachandran", "European Space Research Institute")],
       task="satellite image land-cover classification",
       method="SolarNet",
       datasets=["EuroSAT", "BigEarthNet", "So2Sat"],
       primary_benchmark="EuroSAT",
       metric_name="Top-1 accuracy",
       test_acc=96.4, val_acc=97.1, baseline_name="ResNet-50",
       baseline_val=92.0, baseline_test=91.2,
       params_m=42.7, optimizer="AdamW", lr=0.0003, batch=128, epochs=90,
       beats_sota=True, prior_best=95.1,
       code_url=None,
       funding_note="This work was supported by the Open Earth Initiative. "
                    "The authors do not release source code for the trained models.",
       limitations=["Accuracy degrades on scenes with heavy cloud cover.",
                    "Trained only on imagery at 10 m spatial resolution."],
   ),
   dict(
       title="GraphMoE: Mixture-of-Experts Message Passing for Molecular Property Prediction",
       authors=[("Sofia Álvarez", "ETH Zürich"),
                ("Daniel Kim", "ETH Zürich"),
                ("Yara Haddad", "Genentech"),
                ("Tom Becker", "ETH Zürich")],
       task="molecular property prediction",
       method="GraphMoE",
       datasets=["OGB-MolHIV", "QM9", "ZINC"],
       primary_benchmark="OGB-MolHIV",
       metric_name="ROC-AUC",
       test_acc=0.812, val_acc=0.828, baseline_name="GIN",
       baseline_val=0.784, baseline_test=0.771,
       params_m=8.3, optimizer="Adam", lr=0.001, batch=256, epochs=120,
       beats_sota=True, prior_best=0.799,
       code_url="https://github.com/mol-ai/graphmoe",
       funding_note="Funded by the Swiss NSF. Code and pretrained checkpoints are available "
                    "at https://github.com/mol-ai/graphmoe.",
       limitations=["Expert routing adds ~15% inference latency versus a dense GNN.",
                    "Evaluated only on small-molecule datasets under 50 heavy atoms."],
   ),
   dict(
       title="AcoustiFormer: A Compact Transformer for Environmental Sound Classification",
       authors=[("Noah Fischer", "University of Edinburgh"),
                ("Aisha Bello", "University of Edinburgh"),
                ("Kenji Watanabe", "Sony CSL")],
       task="environmental sound classification",
       method="AcoustiFormer",
       datasets=["ESC-50", "UrbanSound8K"],
       primary_benchmark="ESC-50",
       metric_name="accuracy",
       test_acc=88.7, val_acc=90.3, baseline_name="CNN14",
       baseline_val=90.8, baseline_test=89.2,
       params_m=22.1, optimizer="AdamW", lr=0.0005, batch=64, epochs=200,
       beats_sota=False, prior_best=89.2,
       code_url="https://github.com/audio-lab/acoustiformer",
       funding_note="Code available at https://github.com/audio-lab/acoustiformer.",
       limitations=["A larger CNN baseline still outperforms our model on ESC-50.",
                    "Performance was not evaluated on real-time streaming audio."],
   ),
][:N_DOCS]
def ground_truth(d):
   """Reshape a source dict into the exact JSON shape our schema asks for."""
   return {
       "title": d["title"],
       "authors": [{"name": n, "affiliation": a} for (n, a) in d["authors"]],
       "primary_task": d["task"],
       "proposed_method_name": d["method"],
       "datasets": d["datasets"],
       "headline_metric": {"name": d["metric_name"],
                           "value": d["test_acc"],
                           "benchmark": d["primary_benchmark"]},
       "num_parameters_millions": d["params_m"],
       "hyperparameters": {"optimizer": d["optimizer"], "learning_rate": d["lr"],
                           "batch_size": d["batch"], "epochs": d["epochs"]},
       "beats_prior_sota": d["beats_sota"],
       "code_url": d["code_url"],
       "limitations": d["limitations"],
   }
</code></pre></div></div>



<p class="wp-block-paragraph">We define a small but carefully controlled synthetic corpus of machine-learning research reports with structured metadata. Each document includes realistic fields such as authors, datasets, benchmark metrics, hyperparameters, model size, code availability, limitations, and SOTA claims. The ground_truth function reshapes the same source metadata into the exact JSON structure expected by the extraction schema, providing a precise reference for evaluation.</p>



<h2 class="wp-block-heading"><strong>Rendering Multi-Page PDF Reports</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def render_pdf(d, path):
   """Draw a realistic 3-page report. Page breaks are forced so the headline metric on
   page 1 (abstract) is physically separated from the results table on page 3."""
   from reportlab.lib.pagesizes import LETTER
   from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
   from reportlab.lib.units import inch
   from reportlab.lib import colors
   from reportlab.platypus import (SimpleDocTemplate, Paragraph, Spacer,
                                   Table, TableStyle, PageBreak)
   ss = getSampleStyleSheet()
   H1   = ParagraphStyle("H1", parent=ss["Title"], fontSize=16, leading=20, spaceAfter=6)
   AUTH = ParagraphStyle("AUTH", parent=ss["Normal"], fontSize=9.5, textColor=colors.grey, spaceAfter=10)
   H2   = ParagraphStyle("H2", parent=ss["Heading2"], fontSize=12, spaceBefore=8, spaceAfter=4)
   BODY = ParagraphStyle("BODY", parent=ss["Normal"], fontSize=10, leading=14, spaceAfter=6)
   sota_phrase = (f"surpassing the previous best of {d['prior_best']}"
                  if d["beats_sota"] else
                  f"approaching but not exceeding the previous best of {d['prior_best']}")
   authors_line = ", ".join(f"{n} ({a})" for (n, a) in d["authors"])
   story = []
   story += [Paragraph(d["title"], H1), Paragraph(authors_line, AUTH), Paragraph("Abstract", H2)]
   story += [Paragraph(
       f"We introduce {d['method']}, a model for {d['task']}. On the {d['primary_benchmark']} "
       f"benchmark, {d['method']} attains {d['test_acc']} {d['metric_name']} on the held-out "
       f"test set, {sota_phrase}. Our {d['params_m']}M-parameter model is evaluated across "
       f"{len(d['datasets'])} datasets ({', '.join(d['datasets'])}). "
       f"Extensive ablations confirm the contribution of each component.", BODY)]
   story += [Paragraph("Keywords", H2),
             Paragraph(f"{d['task']}; representation learning; {d['primary_benchmark']}", BODY),
             PageBreak()]
   story += [Paragraph("1  Method and Training Details", H2)]
   story += [Paragraph(
       f"{d['method']} is trained end-to-end with the {d['optimizer']} optimizer. "
       f"We tune on a validation split and report final numbers on the test split. "
       f"The full training configuration is summarized in Table 1.", BODY)]
   hp = [["Hyperparameter", "Value"],
         ["Optimizer", d["optimizer"]],
         ["Learning rate", str(d["lr"])],
         ["Batch size", str(d["batch"])],
         ["Epochs", str(d["epochs"])],
         ["Parameters", f"{d['params_m']}M"]]
   t1 = Table(hp, colWidths=[2.4 * inch, 2.0 * inch])
   t1.setStyle(TableStyle([
       ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#2b3a67")),
       ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
       ("FONTSIZE", (0, 0), (-1, -1), 9.5),
       ("GRID", (0, 0), (-1, -1), 0.4, colors.grey),
       ("ROWBACKGROUNDS", (0, 1), (-1, -1), [colors.white, colors.HexColor("#eef1f8")]),
       ("LEFTPADDING", (0, 0), (-1, -1), 8), ("TOPPADDING", (0, 0), (-1, -1), 4),
       ("BOTTOMPADDING", (0, 0), (-1, -1), 4)]))
   story += [Spacer(1, 4), t1, Spacer(1, 6),
             Paragraph("&lt;b>Table 1.&lt;/b> Training configuration.", BODY),
             Paragraph("2  Datasets", H2),
             Paragraph(
                 f"We evaluate on {', '.join(d['datasets'])}. {d['primary_benchmark']} is our "
                 f"primary benchmark; the remaining datasets are used for generalization "
                 f"studies.", BODY),
             PageBreak()]
   story += [Paragraph("3  Results", H2)]
   res = [["Method", f"Val. {d['metric_name']}", f"Test {d['metric_name']}"],
          [f"{d['baseline_name']} (baseline)", str(d["baseline_val"]), str(d["baseline_test"])],
          [f"{d['method']} (ours)", str(d["val_acc"]), str(d["test_acc"])]]
   t2 = Table(res, colWidths=[2.6 * inch, 1.7 * inch, 1.7 * inch])
   t2.setStyle(TableStyle([
       ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#7a2e2e")),
       ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
       ("FONTSIZE", (0, 0), (-1, -1), 9.5),
       ("GRID", (0, 0), (-1, -1), 0.4, colors.grey),
       ("FONTNAME", (0, 2), (-1, 2), "Helvetica-Bold"),
       ("ROWBACKGROUNDS", (0, 1), (-1, -1), [colors.white, colors.HexColor("#f7eeee")]),
       ("LEFTPADDING", (0, 0), (-1, -1), 8), ("TOPPADDING", (0, 0), (-1, -1), 4),
       ("BOTTOMPADDING", (0, 0), (-1, -1), 4)]))
   story += [Spacer(1, 4), t2, Spacer(1, 6),
             Paragraph(f"&lt;b>Table 2.&lt;/b> Results on {d['primary_benchmark']}. "
                       f"Best test result in bold.", BODY),
             Paragraph("4  Limitations", H2)]
   for lim in d["limitations"]:
       story += [Paragraph("• " + lim, BODY)]
   story += [Paragraph("5  Funding and Code Availability", H2),
             Paragraph(d["funding_note"], BODY)]
   SimpleDocTemplate(path, pagesize=LETTER,
                     topMargin=0.8 * inch, bottomMargin=0.8 * inch,
                     leftMargin=0.9 * inch, rightMargin=0.9 * inch).build(story)
print("STEP 3/7 · Generating synthetic report PDFs…")
CORPUS = []
for i, d in enumerate(DOCS):
   path = f"/content/report_{i}.pdf" if os.path.isdir("/content") else f"report_{i}.pdf"
   render_pdf(d, path)
   CORPUS.append((d, ground_truth(d), path))
   print(f"     ✓ {os.path.basename(path)}  —  {d['method']}")
print()
if SHOW_FIRST_PAGE:
   try:
       import pypdfium2 as pdfium, matplotlib.pyplot as plt
       pg  = pdfium.PdfDocument(CORPUS[0][2])[0]
       img = pg.render(scale=2.0).to_pil()
       plt.figure(figsize=(6.4, 8.3)); plt.imshow(img); plt.axis("off")
       plt.title("What lift reads — page 1 of report_0.pdf", fontsize=10); plt.show()
   except Exception as e:
       print("     (page preview skipped:", e, ")\n")
</code></pre></div></div>



<p class="wp-block-paragraph">We render each synthetic research report as a multi-page PDF using ReportLab, including formatted sections, tables, page breaks, authorship metadata, results, and limitations. The layout deliberately spreads important evidence across pages so the extraction task behaves more like real document mining rather than simple text parsing. The optional page preview uses pypdfium2 and Matplotlib to verify what the model visually receives before extraction begins.</p>



<h2 class="wp-block-heading"><strong>Defining the Extraction Schema</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">SCHEMA = {
   "type": "object",
   "properties": {
       "title": {"type": "string", "description": "The full title of the paper"},
       "authors": {
           "type": "array",
           "description": "Every author listed, in order",
           "items": {"type": "object", "properties": {
               "name":        {"type": "string"},
               "affiliation": {"type": "string", "description": "The author's institution"},
           }},
       },
       "primary_task": {"type": "string",
                        "description": "The main machine-learning task the paper addresses"},
       "proposed_method_name": {"type": "string",
                                "description": "Name of the model/method the paper introduces "
                                               "(not a baseline it compares against)"},
       "datasets": {"type": "array", "items": {"type": "string"},
                    "description": "All benchmark datasets the paper evaluates on"},
       "headline_metric": {
           "type": "object",
           "description": "The primary reported result for the proposed method",
           "properties": {
               "name":      {"type": "string", "description": "Metric name, e.g. Top-1 accuracy or ROC-AUC"},
               "value":     {"type": "number", "description": "The proposed method's value for this metric on "
                                                             "the PRIMARY benchmark's TEST set — not the "
                                                             "validation number and not a baseline's number"},
               "benchmark": {"type": "string", "description": "The dataset the headline metric is reported on"},
           }},
       "num_parameters_millions": {"type": "number",
                                   "description": "Total parameter count of the proposed model, in millions"},
       "hyperparameters": {
           "type": "object",
           "properties": {
               "optimizer":     {"type": "string"},
               "learning_rate": {"type": "number"},
               "batch_size":    {"type": "integer"},
               "epochs":        {"type": "integer"},
           }},
       "beats_prior_sota": {"type": "boolean",
                            "description": "true only if the paper claims its proposed method beats the "
                                           "previous state of the art on the primary benchmark; otherwise false"},
       "code_url": {"type": "string",
                    "description": "URL of the released source-code repository. Return null if the paper "
                                   "does not release code"},
       "limitations": {"type": "array", "items": {"type": "string"},
                       "description": "Limitations the authors explicitly acknowledge"},
   },
   "required": ["title", "proposed_method_name", "headline_metric"],
}
</code></pre></div></div>



<p class="wp-block-paragraph">We define a JSON Schema that tells Lift exactly which fields to extract and how each field should be interpreted. The schema descriptions are technically important because they disambiguate proposed-method values from baseline values, test metrics from validation metrics, and released-code URLs from explicit no-code cases. This turns the extraction task into a controlled, schema-guided information retrieval task rather than an open-ended summarization task.</p>



<h2 class="wp-block-heading"><strong>Scoring Against Ground Truth</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def _norm(s):
   return re.sub(r"\s+", " ", str(s).strip().lower()).strip(" .,:;/")
def _num(x):
   try:    return float(str(x).replace("%", "").replace(",", "").strip())
   except Exception: return None
def leaf_equal(gt, pr):
   if gt is None and pr is None:                       return True
   if gt is None or pr is None:                        return False
   if isinstance(gt, bool) or isinstance(pr, bool):    return bool(gt) == bool(pr)
   a, b = _num(gt), _num(pr)
   if a is not None and b is not None:
       return abs(a - b) &lt; 1e-6 if b == 0 else abs(a - b) / max(abs(a), abs(b)) &lt; 5e-3
   return _norm(gt) == _norm(pr)
def flatten(o, prefix=""):
   out = {}
   if isinstance(o, dict):
       for k, v in o.items():
           out.update(flatten(v, f"{prefix}.{k}" if prefix else k))
   elif isinstance(o, list):
       for i, v in enumerate(o):
           out.update(flatten(v, f"{prefix}[{i}]"))
   else:
       out[prefix] = o
   return out
def score(gt, pred):
   fg, fp = flatten(gt), flatten(pred or {})
   rows, correct = [], 0
   for key, gv in fg.items():
       present = key in fp
       pv = fp.get(key)
       ok = (gv is None and (not present or pv is None)) or (present and leaf_equal(gv, pv))
       correct += int(ok)
       rows.append((key, gv, (pv if present else None), ok))
   return (correct / len(fg) if fg else 0.0), rows
print("STEP 4/7 · Extracting with lift and scoring against ground truth…\n")
results = []
for i, (src, gt, path) in enumerate(CORPUS):
   t0 = time.time()
   pred = run_lift(path, SCHEMA)
   dt = time.time() - t0
   acc, rows = score(gt, pred)
   results.append(dict(src=src, gt=gt, pred=pred, acc=acc, rows=rows, seconds=dt))
   print(f"     doc {i} · {src['method']:&lt;14} field accuracy {acc*100:5.1f}%   ({dt:.1f}s)")
r0 = results[0]
print("\n" + "=" * 90)
print(f"DETAILED VIEW · document 0 · {r0['src']['method']}")
print("=" * 90)
print("Raw JSON lift returned (guaranteed to match the schema shape):\n")
print(json.dumps(r0["pred"], indent=2, ensure_ascii=False))
import pandas as pd
pd.set_option("display.max_colwidth", 46)
pd.set_option("display.width", 120)
grade = pd.DataFrame([{"field": k,
                      "ground_truth": ("∅ (null)" if g is None else g),
                      "lift_predicted": ("∅ (null)" if p is None else p),
                      "✓": "✓" if ok else "✗"}
                     for (k, g, p, ok) in r0["rows"]])
print("\nField-by-field grade:\n")
print(grade.to_string(index=False))
print("\nWhat to look for:")
print("  • headline_metric.value should be the TEST number, not the higher validation number,")
print("    and not the baseline row — that's the near-miss-distractor test.")
print("  • code_url should be ∅ (null): report 0 explicitly releases no code (abstention).")
print("  • every author + affiliation and every dataset should be present (exhaustive lists).")
</code></pre></div></div>



<p class="wp-block-paragraph">We implement a field-level scoring system that flattens nested JSON outputs and compares predictions against ground truth with type-aware logic. Numeric values are evaluated with tolerance, strings are normalized before comparison, booleans are handled explicitly, and missing/null values are treated carefully for abstention cases. The snippet then runs Lift on every generated PDF, records accuracy and latency, and prints a detailed diagnostic view for the first document.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n" + "=" * 90)</code></pre></div></div>



<h2 id="h-assembling-the-knowledge-base" class="wp-block-heading"><strong>Assembling the Knowledge Base</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("STEP 5/7 · Assembling the extractions into a queryable research knowledge base")
print("=" * 90)
def g(d, path, default=None):
   cur = d
   for key in path.split("."):
       if isinstance(cur, dict) and cur.get(key) is not None:
           cur = cur[key]
       else:
           return default
   return cur
kb = pd.DataFrame([{
   "method":     g(r["pred"], "proposed_method_name"),
   "task":       g(r["pred"], "primary_task"),
   "benchmark":  g(r["pred"], "headline_metric.benchmark"),
   "metric":     g(r["pred"], "headline_metric.name"),
   "score":      g(r["pred"], "headline_metric.value"),
   "params_M":   g(r["pred"], "num_parameters_millions"),
   "beats_sota": g(r["pred"], "beats_prior_sota"),
   "authors":    len(g(r["pred"], "authors", []) or []),
   "code":       g(r["pred"], "code_url"),
   "field_acc":  round(r["acc"], 3),
} for r in results])
print("\nResearch knowledge base (one row per mined paper):\n")
print(kb.to_string(index=False))
print("\nExample query — papers that claim to beat SOTA, best result first:\n")
won = kb[kb["beats_sota"] == True].sort_values("score", ascending=False)
print((won if len(won) else "  (none in this sample)").to_string(index=False) if len(won) else "  (none)")
overall = sum(r["acc"] for r in results) / len(results)
print(f"\nSTEP 6/7 · Overall field accuracy across {len(results)} documents: {overall*100:.1f}%")
print("     (Datalab report lift at ~90.2% field accuracy on their 225-doc benchmark.)")
try:
   import matplotlib.pyplot as plt
   labels = [r["src"]["method"] for r in results]
   accs   = [r["acc"] * 100 for r in results]
   plt.figure(figsize=(7, 3.6))
   bars = plt.bar(labels, accs, color="#2b3a67")
   plt.axhline(90.2, ls="--", color="#7a2e2e", lw=1.4, label="lift benchmark (90.2%)")
   for b, a in zip(bars, accs):
       plt.text(b.get_x() + b.get_width()/2, a + 1, f"{a:.0f}%", ha="center", fontsize=9)
   plt.ylim(0, 108); plt.ylabel("Field accuracy (%)")
   plt.title("Per-document extraction accuracy on the synthetic corpus")
   plt.legend(fontsize=8); plt.tight_layout(); plt.show()
except Exception as e:
   print("     (chart skipped:", e, ")")
if RUN_ON_REAL_PDF:
   print("\n" + "=" * 90)
   print(f"STEP 7/7 · Bonus — extracting from a REAL paper: {REAL_PDF_URL}")
   print("=" * 90)
   try:
       import urllib.request
       real_path = "/content/real_paper.pdf" if os.path.isdir("/content") else "real_paper.pdf"
       urllib.request.urlretrieve(REAL_PDF_URL, real_path)
       pred_real = run_lift(real_path, SCHEMA, page_range=REAL_PDF_PAGES)
       print("\nExtraction (no ground truth — real papers are genuinely harder):\n")
       print(json.dumps(pred_real, indent=2, ensure_ascii=False))
       print("\nReal papers vary wildly in layout; tighten field `description`s and use "
             "page_range to point lift at the sections that carry the answer.")
   except Exception as e:
       print("     real-PDF pass failed:", e)
else:
   print("\nSTEP 7/7 · (skipped) set RUN_ON_REAL_PDF = True to also mine a real arXiv PDF.")
print("\n<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2705.png" alt="✅" class="wp-smiley" style="height: 1em; max-height: 1em;" /> Done. You now have: schema-valid extractions, a scored grade, and a knowledge base.")
print("   Next: swap in your own PDFs + schema, or reuse MODEL across thousands of files.")
</code></pre></div></div>



<p class="wp-block-paragraph">We convert the extracted records into a compact research knowledge base where each row represents one mined paper and key fields become queryable columns. The tutorial then demonstrates a simple analytical query for papers claiming to beat prior SOTA and computes overall extraction accuracy across the corpus. Finally, it visualizes per-document accuracy and optionally applies the same schema to a real arXiv PDF, extending the workflow from controlled benchmarking to practical document intelligence.</p>



<h2 class="wp-block-heading"><strong>Conclusion</strong></h2>



<p class="wp-block-paragraph">In conclusion, we have more than raw model outputs: we have a repeatable extraction benchmark with ground-truth labels, field-level scoring, error inspection, and a compact research knowledge base assembled from the mined PDFs. The tutorial showed how structured JSON schemas can turn unstructured technical documents into queryable records while also exposing the failure modes that matter in real document intelligence systems, such as metric confusion, incomplete lists, unsupported null fields, and incorrect SOTA interpretation. The optional real-PDF stage extended the same pipeline beyond synthetic reports, making the notebook a practical template for evaluating Lift on custom research corpora, regulatory filings, technical reports, or any long-form documents where precise structured extraction is more important than generic summarization.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph">Check out the Repo for <a href="https://pxllnk.co/rc5yap" target="_blank" rel="noreferrer noopener"><strong>Lift</strong></a> and<strong> <a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/LLM%20Projects/lift_schema_guided_pdf_extraction_benchmark_marktechpost.py" target="_blank" rel="noreferrer noopener">Full Implementation Codes here</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/01/using-lift-to-turn-research-pdfs-into-structured-json-with-controlled-schema-guided-field-level-evaluation/">Using Lift to Turn Research PDFs into Structured JSON with Controlled, Schema-Guided Field-Level Evaluation</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80799</post-id>
	</item>
		<item>
		<title>Anthropic Redeploys Claude Fable 5 on July 1 After US Export Controls Lift, Adds New Cybersecurity Classifier</title>
		<link>https://www.marktechpost.com/2026/07/01/anthropic-redeploys-claude-fable-5-on-july-1-after-us-export-controls-lift-adds-new-cybersecurity-classifier/</link>
		
		<dc:creator><![CDATA[Michal Sutter]]></dc:creator>
		<pubDate>Wed, 01 Jul 2026 20:41:56 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial General Intelligence]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Generative AI]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80795</guid>

					<description><![CDATA[<p>Anthropic is redeploying Claude Fable 5 on July 1 after US export controls were lifted. A new safety classifier blocks the technique in the Amazon report over 99% of the time, routing flagged requests to Opus 4.8. The company also proposed a four-criteria jailbreak severity framework with Amazon, Microsoft, and Google.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/01/anthropic-redeploys-claude-fable-5-on-july-1-after-us-export-controls-lift-adds-new-cybersecurity-classifier/">Anthropic Redeploys Claude Fable 5 on July 1 After US Export Controls Lift, Adds New Cybersecurity Classifier</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Anthropic is redeploying <strong><a href="https://www.anthropic.com/news/redeploying-fable-5" target="_blank" rel="noreferrer noopener">Claude Fable 5</a></strong>, its most capable generally available model. On June 30, it <a href="https://www.anthropic.com/news/redeploying-fable-5">announced</a> that US export controls had lifted. The controls had covered <strong>Claude Fable 5</strong> and <strong>Claude Mythos 5</strong>. Fable 5 returned to users globally on Wednesday, July 1. Mythos 5 access is restored to a set of US organizations.</p>



<p class="wp-block-paragraph">The models were pulled on June 12. A US government directive restricted them to non-foreign-nationals. Anthropic could not verify nationality in real time. So it suspended both models for everyone.</p>



<p class="wp-block-paragraph">This article explains what triggered the block. It covers the new safeguard and the proposed jailbreak framework. It also shows how Fable 5 compares to rivals like GLM-5.2.</p>



<h2 id="h-quick-facts" class="wp-block-heading"><strong>Quick facts</strong></h2>



<ul class="wp-block-list">
<li><strong>Model:</strong> Claude Fable 5 (a Mythos-class model made safe for general use)</li>



<li><strong>Event:</strong> Redeployed July 1, 2026 after export controls lifted</li>



<li><strong>Reason for pause:</strong> An <a href="https://www.anthropic.com/news/redeploying-fable-5">Amazon report</a> on a safeguard bypass</li>



<li><strong>Fix:</strong> A new safety classifier that blocks the reported technique</li>



<li><strong>Pricing:</strong> $10 per million input tokens, $50 per million output tokens</li>



<li><strong>Where:</strong> Claude Platform, Claude.ai, Claude Code, Claude Cowork</li>
</ul>



<h2 id="h-what-happened-the-timeline" class="wp-block-heading"><strong>What happened: the timeline</strong></h2>



<p class="wp-block-paragraph">Anthropic <a href="https://www.anthropic.com/news/claude-fable-5-mythos-5">launched</a> Fable 5 and Mythos 5 on June 9. Both share the same underlying model. Fable 5 ships with strong safeguards for general use. Mythos 5 has some safeguards lifted for defensive cybersecurity partners.</p>



<p class="wp-block-paragraph">On June 12, the US government applied export controls. The order took effect immediately. Anthropic suspended access rather than risk non-compliance.</p>



<p class="wp-block-paragraph">The trigger was a report from Amazon researchers. They found a method of bypassing Fable 5&#8217;s safeguards. The prompt made the model identify a number of software vulnerabilities. In one case, it produced code showing how to exploit one vulnerability.</p>



<p class="wp-block-paragraph">By June 26, the government approved restoring Mythos 5 for some US organizations. On June 30, the controls were fully lifted.</p>



<h2 id="h-why-anthropic-says-the-finding-was-not-unique" class="wp-block-heading"><strong>Why Anthropic says the finding was not unique</strong></h2>



<p class="wp-block-paragraph">Anthropic tested whether the finding was unique to Fable 5. It was not.</p>



<p class="wp-block-paragraph">Less capable models identified the same vulnerabilities. That list includes Claude Opus 4.8, GPT-5.5, and Kimi K2.7.</p>



<p class="wp-block-paragraph">For the single exploit demonstration, every tested model reproduced it. That set included Haiku 4.5, Sonnet 4.6, Opus 4.6, and Opus 4.7. It also covered Opus 4.8, GPT-5.4, GPT-5.5, and Kimi K2.7.</p>



<p class="wp-block-paragraph">The Anthropic team states the technique exposed no unique Mythos-level cyber capabilities. It called the case a borderline one for Fable 5&#8217;s safeguards. The blocked behavior involved only routine defensive cybersecurity work.</p>



<h2 id="h-how-the-new-classifier-works" class="wp-block-heading"><strong>How the new classifier works</strong></h2>



<p class="wp-block-paragraph">Anthropic still moved to close the gap. It trained an improved safety classifier for the reported behavior.</p>



<p class="wp-block-paragraph">The classifier blocks the specific technique in over 99% of cases. Blocked requests are not refused outright. They are routed to Claude Opus 4.8 instead. Users are notified when this fallback happens.</p>



<p class="wp-block-paragraph">Researchers from the Department of Commerce&#8217;s <a href="https://www.nist.gov/caisi">CAISI</a> tested both old and new safeguards. They agree the safeguards are extraordinarily strong. The tradeoff is more false positives during routine coding and debugging.</p>



<p class="wp-block-paragraph">This reflects Anthropic&#8217;s &#8216;defense in depth&#8217; design. Classifiers are smaller AI systems that detect harmful cyber tasks. A deliberate &#8216;safety margin&#8217; also blocks some benign requests. Fable 5 uses a much larger safety margin than prior models.</p>



<h2 id="h-the-proposed-jailbreak-severity-framework" class="wp-block-heading"><strong>The proposed jailbreak severity framework</strong></h2>



<p class="wp-block-paragraph">The episode exposed a gap. The industry has no shared standard for scoring a &#8216;jailbreak,&#8217; a technique that bypasses a model&#8217;s safeguards.</p>



<p class="wp-block-paragraph">Anthropic is drafting one with Amazon, Microsoft, Google, and other Glasswing partners. <strong>The draft scores a jailbreak on four criteria:</strong></p>



<ol class="wp-block-list">
<li><strong>Capability gain</strong> — how far beyond existing tools it takes the user.</li>



<li><strong>Breadth of capability gain</strong> — how many distinct offensive tasks it unlocks.</li>



<li><strong>Ease of weaponization</strong> — how much human effort an attack still needs.</li>



<li><strong>Discoverability</strong> — how easily someone can obtain the technique.</li>
</ol>



<p class="wp-block-paragraph">For the most severe class, Anthropic will deploy preliminary mitigations immediately. It is also standing up 24/7 monitoring of jailbreak submission channels. </p>



<h2 id="h-interactive-scorer" class="wp-block-heading"><strong>Interactive scorer</strong></h2>



<p class="wp-block-paragraph">Try this embedded interactive scorer to see how these four criteria combine.</p>



<!-- Marktechpost interactive embed: Jailbreak Severity Scorer (Anthropic theme) -->
<iframe id="mtp-fable5-scorer"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;UTF-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;
&lt;title&gt;Jailbreak Severity Scorer&lt;/title&gt;
&lt;style&gt;
  :root{
    --bg:#F0EEE6; --card:#FBFAF7; --card2:#FFFFFF; --line:#E4E0D6;
    --clay:#CC785C; --clay-hi:#D97757; --clay-soft:rgba(204,120,92,.12);
    --text:#141413; --muted:#73706A;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  body{
    font-family:Inter,-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,sans-serif;
    background:var(--bg); color:var(--text); padding:20px;
    -webkit-font-smoothing:antialiased;
  }
  #app{max-width:760px;margin:0 auto}
  .head{border:1px solid var(--line);border-radius:16px;background:
    linear-gradient(180deg,#FFFFFF,#F7F5EF);padding:22px 22px 20px;margin-bottom:16px}
  .kicker{display:inline-block;font-size:11px;letter-spacing:.14em;text-transform:uppercase;
    color:var(--clay);border:1px solid var(--clay);border-radius:999px;padding:4px 10px;margin-bottom:12px}
  h1{font-size:22px;line-height:1.25;font-weight:700;margin-bottom:8px}
  h1 span{color:var(--clay)}
  .sub{color:var(--muted);font-size:14px;line-height:1.55;max-width:64ch}
  .grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-bottom:16px}
  .card{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:16px}
  .crit-name{font-size:14px;font-weight:600;margin-bottom:2px}
  .crit-desc{font-size:12px;color:var(--muted);line-height:1.5;margin-bottom:12px;min-height:32px}
  .row{display:flex;justify-content:space-between;align-items:center;margin-bottom:8px}
  .val{font-variant-numeric:tabular-nums;font-weight:700;color:var(--clay);font-size:14px}
  input[type=range]{-webkit-appearance:none;width:100%;height:6px;border-radius:6px;
    background:linear-gradient(90deg,var(--clay) 0%,var(--clay) var(--fill,50%),#E0DBD0 var(--fill,50%));
    outline:none;cursor:pointer}
  input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;width:18px;height:18px;border-radius:50%;
    background:#fff;border:3px solid var(--clay);cursor:pointer;box-shadow:0 0 0 4px rgba(204,120,92,.15)}
  input[type=range]::-moz-range-thumb{width:18px;height:18px;border-radius:50%;background:#fff;
    border:3px solid var(--clay);cursor:pointer}
  .ticks{display:flex;justify-content:space-between;font-size:10px;color:#9c988e;margin-top:6px}
  .result{background:var(--card2);border:1px solid var(--line);border-radius:14px;padding:20px;margin-bottom:16px}
  .score-wrap{display:flex;align-items:center;gap:20px;flex-wrap:wrap}
  .dial{position:relative;width:120px;height:120px;flex:0 0 auto}
  .dial svg{transform:rotate(-90deg)}
  .dial .num{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center}
  .dial .num b{font-size:30px;line-height:1;font-variant-numeric:tabular-nums}
  .dial .num small{font-size:10px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin-top:2px}
  .tier{flex:1;min-width:220px}
  .tier .badge{display:inline-block;font-size:12px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;
    padding:6px 12px;border-radius:8px;margin-bottom:10px}
  .tier p{font-size:13px;color:var(--muted);line-height:1.55}
  .tier .resp{margin-top:10px;font-size:13px;color:var(--text);line-height:1.55;
    border-left:2px solid var(--clay);padding-left:12px}
  .maps{font-size:12px;color:var(--muted);margin-top:14px;line-height:1.6}
  .maps b{color:var(--text)}
  .note{font-size:11px;color:#8a867e;line-height:1.6;border-top:1px solid var(--line);padding-top:14px}
  .foot{text-align:center;margin-top:18px;font-size:12px;color:var(--muted)}
  .foot b{color:var(--clay);font-weight:700}
  .foot a{color:var(--muted);text-decoration:none;border-bottom:1px dotted #b7b2a6}
  @media(max-width:640px){.grid{grid-template-columns:1fr}h1{font-size:19px}}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;app&quot;&gt;
  &lt;div class=&quot;head&quot;&gt;
    &lt;span class=&quot;kicker&quot;&gt;Interactive Demo&lt;/span&gt;
    &lt;h1&gt;Jailbreak &lt;span&gt;Severity Scorer&lt;/span&gt;&lt;/h1&gt;
    &lt;p class=&quot;sub&quot;&gt;Score an AI jailbreak on the four criteria Anthropic proposed with Amazon, Microsoft, and Google. Move each slider to see the composite severity and the suggested response tier update live.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;grid&quot; id=&quot;criteria&quot;&gt;&lt;/div&gt;

  &lt;div class=&quot;result&quot;&gt;
    &lt;div class=&quot;score-wrap&quot;&gt;
      &lt;div class=&quot;dial&quot;&gt;
        &lt;svg width=&quot;120&quot; height=&quot;120&quot; viewBox=&quot;0 0 120 120&quot;&gt;
          &lt;circle cx=&quot;60&quot; cy=&quot;60&quot; r=&quot;52&quot; fill=&quot;none&quot; stroke=&quot;#E4E0D6&quot; stroke-width=&quot;10&quot;/&gt;
          &lt;circle id=&quot;arc&quot; cx=&quot;60&quot; cy=&quot;60&quot; r=&quot;52&quot; fill=&quot;none&quot; stroke=&quot;#CC785C&quot; stroke-width=&quot;10&quot;
            stroke-linecap=&quot;round&quot; stroke-dasharray=&quot;326.7&quot; stroke-dashoffset=&quot;326.7&quot;/&gt;
        &lt;/svg&gt;
        &lt;div class=&quot;num&quot;&gt;&lt;b id=&quot;scoreNum&quot;&gt;0&lt;/b&gt;&lt;small&gt;Severity&lt;/small&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;tier&quot;&gt;
        &lt;span class=&quot;badge&quot; id=&quot;tierBadge&quot;&gt;&amp;mdash;&lt;/span&gt;
        &lt;p id=&quot;tierDesc&quot;&gt;&lt;/p&gt;
        &lt;div class=&quot;resp&quot; id=&quot;tierResp&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;maps&quot; id=&quot;rowMap&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;p class=&quot;note&quot;&gt;&lt;b&gt;Illustrative tool.&lt;/b&gt; Anthropic's framework is a published work in progress and does not define numeric thresholds. The score here is an equal-weighted average built only to demonstrate the four criteria; it is not an official Anthropic score. Criteria and response language are adapted from Anthropic's June 30, 2026 post, &quot;Redeploying Claude Fable 5.&quot;&lt;/p&gt;

  &lt;div class=&quot;foot&quot;&gt;Built by &lt;b&gt;Marktechpost&lt;/b&gt; &amp;middot; Source: &lt;a href=&quot;https://www.anthropic.com/news/redeploying-fable-5&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;anthropic.com&lt;/a&gt;&lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
  var criteria = [
    {k:&quot;cap&quot;, name:&quot;1 \u00b7 Capability gain&quot;, desc:&quot;How far beyond existing tools does the jailbreak take the user?&quot;, lo:&quot;Matches weak tools&quot;, hi:&quot;Accelerates experts&quot;},
    {k:&quot;brd&quot;, name:&quot;2 \u00b7 Breadth of gain&quot;, desc:&quot;For how many distinct offensive tasks does the same technique work?&quot;, lo:&quot;One narrow target&quot;, hi:&quot;Many targets&quot;},
    {k:&quot;ease&quot;, name:&quot;3 \u00b7 Ease of weaponization&quot;, desc:&quot;How much human effort turns the jailbreak into a real attack?&quot;, lo:&quot;Skilled, many retries&quot;, hi:&quot;Works first try&quot;},
    {k:&quot;disc&quot;, name:&quot;4 \u00b7 Discoverability&quot;, desc:&quot;How easy is it for someone to obtain the technique?&quot;, lo:&quot;Specialist knowledge&quot;, hi:&quot;Widely known online&quot;}
  ];
  var state = {cap:35, brd:30, ease:25, disc:20};
  var wrap = document.getElementById(&quot;criteria&quot;);

  criteria.forEach(function(c){
    var el = document.createElement(&quot;div&quot;);
    el.className = &quot;card&quot;;
    el.innerHTML =
      '&lt;div class=&quot;crit-name&quot;&gt;'+c.name+'&lt;/div&gt;'+
      '&lt;div class=&quot;crit-desc&quot;&gt;'+c.desc+'&lt;/div&gt;'+
      '&lt;div class=&quot;row&quot;&gt;&lt;span style=&quot;font-size:12px;color:#73706A&quot;&gt;Low&lt;/span&gt;'+
      '&lt;span class=&quot;val&quot; id=&quot;v-'+c.k+'&quot;&gt;'+state[c.k]+'&lt;/span&gt;'+
      '&lt;span style=&quot;font-size:12px;color:#73706A&quot;&gt;High&lt;/span&gt;&lt;/div&gt;'+
      '&lt;input type=&quot;range&quot; min=&quot;0&quot; max=&quot;100&quot; value=&quot;'+state[c.k]+'&quot; id=&quot;s-'+c.k+'&quot;&gt;'+
      '&lt;div class=&quot;ticks&quot;&gt;&lt;span&gt;'+c.lo+'&lt;/span&gt;&lt;span&gt;'+c.hi+'&lt;/span&gt;&lt;/div&gt;';
    wrap.appendChild(el);
    var input = el.querySelector(&quot;#s-&quot;+c.k);
    input.style.setProperty(&quot;--fill&quot;, state[c.k]+&quot;%&quot;);
    input.addEventListener(&quot;input&quot;, function(){
      state[c.k] = parseInt(this.value,10);
      this.style.setProperty(&quot;--fill&quot;, this.value+&quot;%&quot;);
      document.getElementById(&quot;v-&quot;+c.k).textContent = this.value;
      render();
    });
  });

  var arc = document.getElementById(&quot;arc&quot;);
  var CIRC = 326.7;

  function tierFor(s){
    if(s &lt; 30) return {label:&quot;Minor&quot;, color:&quot;#7A9A65&quot;,
      desc:&quot;Very unlikely to be harmful. The prompt intrudes into the safety margin but not core harmful behavior.&quot;,
      resp:&quot;Response: log and monitor. Track through standard red-team channels.&quot;,
      row:&quot;Maps to &lt;b&gt;Row C&lt;/b&gt; (minor jailbreak) in Anthropic's diagram.&quot;};
    if(s &lt; 55) return {label:&quot;Narrow harmful&quot;, color:&quot;#D4A24E&quot;,
      desc:&quot;Unlocks a specific harmful behavior. Severity stays low-to-moderate because the technique is narrow.&quot;,
      resp:&quot;Response: prioritize triage. Investigate and patch the specific behavior.&quot;,
      row:&quot;Maps to &lt;b&gt;Row D&lt;/b&gt; (narrow harmful jailbreak).&quot;};
    if(s &lt; 78) return {label:&quot;Severe&quot;, color:&quot;#CC785C&quot;,
      desc:&quot;Broad or high-capability gain that is easy to reproduce. Approaches serious real-world risk.&quot;,
      resp:&quot;Response: notify government counterparts. Share safeguards for independent testing.&quot;,
      row:&quot;Trends toward &lt;b&gt;Row E&lt;/b&gt; (universal jailbreak) territory.&quot;};
    return {label:&quot;Critical&quot;, color:&quot;#B0402F&quot;,
      desc:&quot;A universal jailbreak: wide range of harmful behaviors, easy to weaponize and widely obtainable.&quot;,
      resp:&quot;Response: deploy preliminary mitigations immediately upon confirmation, per Anthropic's stated policy.&quot;,
      row:&quot;Maps to &lt;b&gt;Row E&lt;/b&gt; (universal jailbreak) &amp;mdash; the most concerning category.&quot;};
  }

  function render(){
    var s = Math.round((state.cap + state.brd + state.ease + state.disc)/4);
    document.getElementById(&quot;scoreNum&quot;).textContent = s;
    var t = tierFor(s);
    arc.style.strokeDashoffset = CIRC - (CIRC * s/100);
    arc.setAttribute(&quot;stroke&quot;, t.color);
    var badge = document.getElementById(&quot;tierBadge&quot;);
    badge.textContent = t.label;
    badge.style.background = &quot;rgba(204,120,92,.12)&quot;;
    badge.style.color = t.color;
    badge.style.border = &quot;1px solid &quot;+t.color;
    document.getElementById(&quot;tierDesc&quot;).textContent = t.desc;
    document.getElementById(&quot;tierResp&quot;).textContent = t.resp;
    document.getElementById(&quot;tierResp&quot;).style.borderLeftColor = t.color;
    document.getElementById(&quot;rowMap&quot;).innerHTML = t.row;
    postHeight();
  }

  function postHeight(){
    try{
      var h = document.getElementById(&quot;app&quot;).offsetHeight + 40;
      parent.postMessage({type:&quot;mtp-embed-height&quot;, height:h}, &quot;*&quot;);
    }catch(e){}
  }
  window.addEventListener(&quot;load&quot;, postHeight);
  window.addEventListener(&quot;resize&quot;, postHeight);
  render();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"
  style="width:100%;border:0;overflow:hidden;display:block;"
  height="600" scrolling="no" loading="lazy"
  title="Jailbreak Severity Scorer"></iframe>
<script>
(function(){
  window.addEventListener("message", function(e){
    if(e.data && e.data.type === "mtp-embed-height"){
      var f = document.getElementById("mtp-fable5-scorer");
      if(f) f.style.height = e.data.height + "px";
    }
  });
})();
</script>



<p class="wp-block-paragraph" id="h-"></p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use cases with examples</strong></h2>



<p class="wp-block-paragraph">Fable 5 targets long-horizon, agentic work. Here is where early engineers can apply it.</p>



<ul class="wp-block-list">
<li><strong>Codebase migrations</strong>: <a href="https://stripe.com/">Stripe</a> reported a codebase-wide migration in one day. The job spanned a 50-million-line Ruby codebase. Doing it by hand would take a team over two months.</li>



<li><strong>Financial analysis</strong>: On <a href="https://www.hebbia.com/">Hebbia</a>&#8216;s Finance Benchmark, Fable 5 posts the highest score. It gains on chart, table, and document reasoning.</li>



<li><strong>Vision-to-code</strong>: Fable 5 can rebuild a web app&#8217;s source code from screenshots alone.</li>



<li><strong>Long-running agents</strong>: File-based memory helps it stay focused across millions of tokens.</li>
</ul>



<h2 id="h-how-fable-5-compares" class="wp-block-heading"><strong>How Fable 5 compares</strong></h2>



<p class="wp-block-paragraph">The pause created an opening for rivals. Days after the suspension, Zhipu AI <a href="https://www.scmp.com/tech/tech-trends/article/3357115/zhipu-ais-stock-rockets-after-chinese-firm-makes-glm-52-open-source">released</a> GLM-5.2 as open weights. Independent testers rank it the strongest openly available model.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Model</th><th>Developer</th><th>Access</th><th>Context</th><th>Price (in/out per 1M)</th><th>Reported benchmark</th><th>Cyber safeguards</th></tr></thead><tbody><tr><td><strong>Claude Fable 5</strong></td><td>Anthropic</td><td>General (Platform, .ai, Code, Cowork)</td><td>Long-context</td><td>$10 / $50</td><td>Led <a href="https://www.latent.space/p/ainews-glm-gpt-glm-52-passes-vibe">AA-Briefcase</a> at 1587 Elo</td><td>Strongest applied; falls back to Opus 4.8</td></tr><tr><td><strong>Claude Mythos 5</strong></td><td>Anthropic</td><td>Glasswing / trusted US orgs</td><td>Long-context</td><td>$10 / $50</td><td>Same base model as Fable 5</td><td>Cyber safeguards lifted</td></tr><tr><td><strong>Claude Opus 4.8</strong></td><td>Anthropic</td><td>General</td><td>Long-context</td><td>~$5 / $25</td><td><a href="https://felloai.com/glm-5-2/">SWE-bench Pro 69.2</a>; Terminal-Bench 85.0</td><td>Standard</td></tr><tr><td><strong>GLM-5.2</strong></td><td>Zhipu AI (Z.ai)</td><td>Open weights (MIT)</td><td>1M tokens</td><td>~$1.40 / $4.40</td><td>SWE-bench Pro 62.1; Terminal-Bench 81.0</td><td>None (open weight)</td></tr><tr><td><strong>GPT-5.5</strong></td><td>OpenAI</td><td>General</td><td>Long-context</td><td>~$5 / $30</td><td>SWE-bench Pro 58.6</td><td>Standard</td></tr></tbody></table><figcaption class="wp-element-caption"><em>Benchmark and price figures are self-reported or from independent testers. Sources: <a href="https://felloai.com/glm-5-2/">felloai</a>, <a href="https://www.latent.space/p/ainews-glm-gpt-glm-52-passes-vibe">Latent Space</a>, <a href="https://www.trendingtopics.eu/glm-5-2-chinas-zhipu-ai-beats-even-googles-top-models-with-its-new-open-llm/">TrendingTopics</a>.</em></figcaption></figure>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">GLM-5.2 uses a Mixture-of-Experts design. It has roughly 750 billion total parameters. Only about 40 billion activate per token. On <a href="https://semgrep.dev/blog/2026/we-have-mythos-at-home-glm-52-beats-claude-in-our-cyber-benchmarks/">Semgrep</a>&#8216;s IDOR benchmark, it scored 39% F1. That beat Claude Code at 32% on the same prompt.</p>



<p class="wp-block-paragraph">The gap narrows on cost. On AA-Briefcase, Fable 5 averaged $31 per task. GLM-5.2 averaged $2.40.</p>



<h2 id="h-access-and-a-quick-api-example" class="wp-block-heading"><strong>Access and a quick API example</strong></h2>



<p class="wp-block-paragraph">For Pro, Max, Team, and select Enterprise plans, Fable 5 is included through July 7. It covers up to 50% of weekly usage limits. After that, access moves to <a href="https://support.claude.com/en/articles/12429409-manage-usage-credits-for-paid-claude-plans">usage credits</a>. Anthropic is also re-enabling Fable 5 on AWS, Google Cloud, and Microsoft Foundry.</p>



<p class="wp-block-paragraph">Developers call the model with the <code>claude-fable-5</code> string:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">from anthropic import Anthropic

# Reads your key from the ANTHROPIC_API_KEY environment variable
client = Anthropic()

message = client.messages.create(
    model="claude-fable-5",
    max_tokens=1024,
    messages=[
        {"role": "user", "content": "Refactor this module for readability."}
    ],
)

print(message.content)</code></pre></div></div>



<p class="wp-block-paragraph">If a classifier fires, the response comes from Opus 4.8. Your code path stays the same.</p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key takeaways</strong></h2>



<ul class="wp-block-list">
<li>Fable 5 returns July 1 after export controls were lifted.</li>



<li>A new classifier blocks the reported bypass in over 99% of cases.</li>



<li>Blocked requests route to Opus 4.8, not an outright refusal.</li>



<li>Anthropic proposes a four-criteria framework for scoring jailbreaks.</li>



<li>GLM-5.2 emerged as a cheaper open-weight rival during the pause.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the<strong> <a href="https://www.anthropic.com/news/redeploying-fable-5" target="_blank" rel="noreferrer noopener">Technical details</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/01/anthropic-redeploys-claude-fable-5-on-july-1-after-us-export-controls-lift-adds-new-cybersecurity-classifier/">Anthropic Redeploys Claude Fable 5 on July 1 After US Export Controls Lift, Adds New Cybersecurity Classifier</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80795</post-id>
	</item>
		<item>
		<title>NVIDIA Releases Nemotron-Labs-TwoTower: an Open-Weight Diffusion Language Model Built on a Frozen Autoregressive Nemotron-3-Nano-30B-A3B Backbone</title>
		<link>https://www.marktechpost.com/2026/07/01/nvidia-releases-nemotron-labs-twotower/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Wed, 01 Jul 2026 08:10:17 +0000</pubDate>
				<category><![CDATA[AI Paper Summary]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80792</guid>

					<description><![CDATA[<p>NVIDIA has released Nemotron-Labs-TwoTower, a diffusion language model built on a pretrained autoregressive backbone. It ships as open weights under the NVIDIA Nemotron Open Model License. The release targets a throughput bottleneck in text generation. Autoregressive (AR) models decode one token at a time. That serial process caps generation throughput. Discrete diffusion language models take [&#8230;]</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/01/nvidia-releases-nemotron-labs-twotower/">NVIDIA Releases Nemotron-Labs-TwoTower: an Open-Weight Diffusion Language Model Built on a Frozen Autoregressive Nemotron-3-Nano-30B-A3B Backbone</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">NVIDIA has released <strong><a href="https://arxiv.org/pdf/2606.26493" target="_blank" rel="noreferrer noopener">Nemotron-Labs-TwoTower</a></strong>, a diffusion language model built on a pretrained autoregressive backbone. It ships as open weights under the NVIDIA Nemotron Open Model License. The release targets a throughput bottleneck in text generation.</p>



<p class="wp-block-paragraph">Autoregressive (AR) models decode one token at a time. That serial process caps generation throughput. Discrete diffusion language models take another route. They generate tokens in parallel and refine them iteratively.</p>



<p class="wp-block-paragraph">Most diffusion language models use one network for two jobs. It represents clean tokens and denoises corrupted ones at every step. TwoTower separates these jobs into two towers. It keeps 98.7% of the AR baseline&#8217;s aggregate benchmark quality. It also reports 2.42× higher wall-clock generation throughput.</p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li>TwoTower splits diffusion into a frozen AR context tower and a trained denoiser tower.</li>



<li>It retains 98.7% of AR quality at 2.42× throughput (γ=0.8, S=16, 2×H100).</li>



<li>The denoiser trained on ~2.1T tokens; the backbone used 25T.</li>



<li>One checkpoint runs diffusion, mock-AR, and AR decoding modes.</li>
</ul>



<h2 id="h-nemotron-labs-twotower" class="wp-block-heading"><strong>Nemotron-Labs-TwoTower</strong></h2>



<p class="wp-block-paragraph">TwoTower is a block-wise autoregressive diffusion model. It is instantiated on Nemotron-3-Nano-30B-A3B, an open-weight hybrid backbone. That backbone interleaves Mamba-2, self-attention, and mixture-of-experts (MoE) layers.</p>



<p class="wp-block-paragraph">Each tower has 52 layers: 23 Mamba-2, 6 self-attention, and 23 MoE. The released checkpoint ships both towers, roughly 60B total parameters. Active parameters per token are about 3B per tower. The MoE uses 128 routable experts, of which 6 activate, plus 2 shared experts.</p>



<p class="wp-block-paragraph">Both towers start as copies of the same backbone checkpoint. Only the denoiser tower is trained. The AR context tower stays frozen. The denoiser was trained on ~2.1T tokens, a fraction of the backbone&#8217;s 25T-token pretraining.</p>



<h2 id="h-how-the-two-towers-work" class="wp-block-heading"><strong>How the Two Towers Work</strong></h2>



<p class="wp-block-paragraph">The AR context tower runs causally over the prompt and committed tokens. It produces per-layer KV cache and final Mamba-2 states. It preserves the backbone&#8217;s autoregressive capability.</p>



<p class="wp-block-paragraph">The diffusion denoiser tower refines noisy blocks. Within a block, it uses bidirectional in-block attention. It stays causal with respect to past clean blocks.</p>



<p class="wp-block-paragraph">The towers connect layer-by-layer. Denoiser layer <em>i</em> cross-attends to context tower layer <em>i</em>. This layer-aligned cross-attention gives multi-scale access to the backbone&#8217;s representations. Prior approaches broadcast only the last hidden state.</p>



<p class="wp-block-paragraph">Two more denoiser modifications matter. Mamba-2 layers seed their initial state from the context tower&#8217;s Mamba state. The diffusion timestep modulates each layer through adaLN-single time conditioning. That adaLN module adds only ~1.5M parameters.</p>



<p class="wp-block-paragraph">Generation runs block by block. Each block starts as <em>S</em> [MASK] tokens. The denoiser refines it over <em>T</em> steps, then commits it. The context tower then processes committed tokens to update its caches.</p>



<p class="wp-block-paragraph">This explains why multiple denoising steps can still beat one-token decoding. Autoregressive decoding commits exactly one token per step. TwoTower commits multiple tokens per step early in refinement.</p>



<h2 id="h-benchmarks" class="wp-block-heading"><strong>Benchmarks</strong></h2>



<p class="wp-block-paragraph">Evaluations use BF16 on 2×H100 GPUs. The default operating point is confidence unmasking, threshold γ=0.8, block size S=16. The table compares the AR baseline against TwoTower diffusion decoding.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Task</th><th>Nemotron-3-Nano-30B-A3B (AR)</th><th>Nemotron-Labs-TwoTower (diffusion)</th></tr></thead><tbody><tr><td>MMLU (5-shot, acc)</td><td>78.56</td><td>78.24</td></tr><tr><td>MMLU-Pro (5-shot, CoT EM)</td><td>62.59</td><td>60.93</td></tr><tr><td>ARC-Challenge (25-shot, acc_norm)</td><td>91.72</td><td>92.66</td></tr><tr><td>WinoGrande (5-shot, acc)</td><td>76.09</td><td>76.09</td></tr><tr><td>RACE (0-shot, acc)</td><td>88.90</td><td>88.90</td></tr><tr><td>HumanEval (0-shot)</td><td>79.27</td><td>75.58</td></tr><tr><td>MBPP-Sanitized (3-shot)</td><td>74.71</td><td>74.28</td></tr><tr><td>GSM8K (8-shot, acc)</td><td>92.49</td><td>90.14</td></tr><tr><td>MATH-500 (4-shot)</td><td>84.40</td><td>80.60</td></tr><tr><td>MMLU Global Lite (5-shot)</td><td>73.97</td><td>73.94</td></tr><tr><td>MGSM (8-shot, avg acc)</td><td>80.80</td><td>80.40</td></tr><tr><td><strong>Quality retained</strong></td><td>100%</td><td><strong>98.7%</strong></td></tr><tr><td><strong>Generation throughput (× AR)</strong></td><td>1.0×</td><td><strong>2.42×</strong></td></tr></tbody></table></figure>



<p class="wp-block-paragraph">General knowledge stays within about one point of the AR baseline. Code and math show modest degradation. Commonsense and multilingual scores are recovered or slightly improved. Lowering γ commits more tokens per step and raises throughput, with reduced quality.</p>



<h2 id="h-running-it-three-generation-modes" class="wp-block-heading"><strong>Running It: Three Generation Modes</strong></h2>



<p class="wp-block-paragraph">The checkpoint exposes three inference paths. Full two-tower diffusion uses 2 GPUs, about 59GB per GPU in BF16. AR-only mode runs on a single 80GB GPU.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import torch
from transformers import AutoTokenizer, AutoModelForCausalLM

model_name = "nvidia/Nemotron-Labs-TwoTower-30B-A3B-Base-BF16"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(
    model_name, torch_dtype=torch.bfloat16, trust_remote_code=True,
)
# context tower -> GPU 0, denoiser tower -> GPU 1
model.place_towers_on_devices("cuda:0", "cuda:1")
model.eval()

prompt = "France is a country "
inputs = tokenizer(prompt, return_tensors="pt").to("cuda:0")

outputs = model.generate_mask_diffusion(
    inputs["input_ids"], max_new_tokens=128,
    block_size=16, steps_per_block=16, mask_token_id=3,
    temperature=0.1, confidence_threshold=0.8,
    eos_token_id=tokenizer.eos_token_id,
)
print(tokenizer.decode(outputs[0][inputs["input_ids"].shape[1]:], skip_special_tokens=True))</code></pre></div></div>



<p class="wp-block-paragraph">The three modes are <code>generate_mask_diffusion()</code>, <code>generate_mock_ar()</code>, and <code>generate_ar()</code>. Mask diffusion commits up to <code>block_size</code> tokens per step. Mock-AR and AR commit one token per step.</p>



<h2 id="h-where-it-fits-use-cases" class="wp-block-heading"><strong>Where It Fits: Use Cases</strong></h2>



<p class="wp-block-paragraph">The most direct use case is faster batch generation. A data team producing synthetic text can trade a small quality drop for throughput. At γ=0.8, that trade is 1.3% quality for 2.42× speed.</p>



<p class="wp-block-paragraph">A second use case is tuning the quality–throughput trade-off. Raising γ preserves more quality, as per the NVIDIA&#8217;s paper. Lowering γ commits more tokens per step for speed.</p>



<p class="wp-block-paragraph">A third use case is drop-in adaptation. The context tower keeps its LM head for speculative decoding, verification, or AR scoring. Teams can run AR and diffusion from one checkpoint.</p>



<h2 id="h-strengths-and-weaknesses" class="wp-block-heading"><strong>Strengths and Weaknesses</strong></h2>



<h4 id="h-strengths" class="wp-block-heading"><strong>Strengths:</strong></h4>



<ul class="wp-block-list">
<li>Open weights under the NVIDIA Nemotron Open Model License; ready for commercial use</li>



<li>98.7% of AR quality retained at 2.42× throughput at the default operating point</li>



<li>One checkpoint supports diffusion, mock-AR, and AR decoding</li>



<li>Denoiser trained on ~2.1T tokens, not a full re-pretrain</li>



<li>Sequence-length cache memory scales like the AR baseline</li>
</ul>



<h4 id="h-weaknesses" class="wp-block-heading"><strong>Weaknesses:</strong></h4>



<ul class="wp-block-list">
<li>Full two-tower diffusion needs 2 GPUs and ~59GB per GPU in BF16</li>



<li>Code and math degrade more than general knowledge (HumanEval 79.27 → 75.58)</li>



<li>Keeping both towers resident raises the fixed model-weight memory footprint</li>



<li>Released checkpoint is a base model, before instruction tuning or alignment</li>



<li>Throughput past 3× comes with larger quality loss</li>
</ul>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- Nemotron-Labs-TwoTower interactive demo — paste into a WordPress "Custom HTML" block -->
<iframe id="mtp-twotower-frame" title="TwoTower Block Diffusion Demo" style="width:100%;border:0;overflow:hidden;display:block" height="600" scrolling="no" srcdoc="&lt;!-- Nemotron-Labs-TwoTower interactive demo — Marktechpost. Paste into a WordPress Custom HTML block via the iframe srcdoc snippet. --&gt;
&lt;div id=&quot;mtp-twotower-demo&quot;&gt;
  &lt;div class=&quot;tt-wrap&quot;&gt;

    &lt;header class=&quot;tt-head&quot;&gt;
      &lt;div class=&quot;tt-badge&quot;&gt;MARKTECHPOST · INTERACTIVE&lt;/div&gt;
      &lt;h2&gt;TwoTower Block Diffusion — Confidence-Unmasking Sampler&lt;/h2&gt;
      &lt;p class=&quot;tt-sub&quot;&gt;
        Watch a block of &lt;b&gt;[MASK]&lt;/b&gt; tokens resolve in parallel. Each diffusion step predicts all masked
        positions, commits the confident subset above threshold &lt;b&gt;&amp;#947;&lt;/b&gt;, and re-masks the rest.
        Illustrative simulation of Algorithm&amp;nbsp;2 &amp;mdash; benchmark numbers below are measured values from the paper.
      &lt;/p&gt;
    &lt;/header&gt;

    &lt;section class=&quot;tt-facts&quot;&gt;
      &lt;div class=&quot;tt-fact&quot;&gt;&lt;span class=&quot;tt-num&quot;&gt;2.42&amp;#215;&lt;/span&gt;&lt;span class=&quot;tt-lbl&quot;&gt;wall-clock throughput vs AR&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;tt-fact&quot;&gt;&lt;span class=&quot;tt-num&quot;&gt;98.7%&lt;/span&gt;&lt;span class=&quot;tt-lbl&quot;&gt;AR quality retained&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;tt-fact&quot;&gt;&lt;span class=&quot;tt-num&quot;&gt;~2.1T&lt;/span&gt;&lt;span class=&quot;tt-lbl&quot;&gt;denoiser training tokens&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;tt-fact&quot;&gt;&lt;span class=&quot;tt-num&quot;&gt;52&lt;/span&gt;&lt;span class=&quot;tt-lbl&quot;&gt;layers / tower (23 Mamba-2, 6 attn, 23 MoE)&lt;/span&gt;&lt;/div&gt;
    &lt;/section&gt;

    &lt;section class=&quot;tt-controls&quot;&gt;
      &lt;div class=&quot;tt-ctrl&quot;&gt;
        &lt;label&gt;Block size &lt;b&gt;S&lt;/b&gt; &lt;span id=&quot;tt-sval&quot; class=&quot;tt-pill&quot;&gt;16&lt;/span&gt;&lt;/label&gt;
        &lt;select id=&quot;tt-blocksize&quot;&gt;
          &lt;option value=&quot;8&quot;&gt;8&lt;/option&gt;
          &lt;option value=&quot;16&quot; selected&gt;16 (default)&lt;/option&gt;
          &lt;option value=&quot;32&quot;&gt;32&lt;/option&gt;
          &lt;option value=&quot;64&quot;&gt;64&lt;/option&gt;
        &lt;/select&gt;
      &lt;/div&gt;
      &lt;div class=&quot;tt-ctrl&quot;&gt;
        &lt;label&gt;Denoising steps &lt;b&gt;T&lt;/b&gt; &lt;span id=&quot;tt-tval&quot; class=&quot;tt-pill&quot;&gt;16&lt;/span&gt;&lt;/label&gt;
        &lt;input id=&quot;tt-steps&quot; type=&quot;range&quot; min=&quot;1&quot; max=&quot;16&quot; value=&quot;16&quot; step=&quot;1&quot;&gt;
      &lt;/div&gt;
      &lt;div class=&quot;tt-ctrl&quot;&gt;
        &lt;label&gt;Confidence threshold &lt;b&gt;&amp;#947;&lt;/b&gt; &lt;span id=&quot;tt-gval&quot; class=&quot;tt-pill&quot;&gt;0.80&lt;/span&gt;&lt;/label&gt;
        &lt;input id=&quot;tt-gamma&quot; type=&quot;range&quot; min=&quot;0.50&quot; max=&quot;0.95&quot; value=&quot;0.80&quot; step=&quot;0.01&quot;&gt;
      &lt;/div&gt;
      &lt;div class=&quot;tt-ctrl&quot;&gt;
        &lt;label&gt;Temperature &lt;span id=&quot;tt-tempval&quot; class=&quot;tt-pill&quot;&gt;0.10&lt;/span&gt;&lt;/label&gt;
        &lt;input id=&quot;tt-temp&quot; type=&quot;range&quot; min=&quot;0.0&quot; max=&quot;1.0&quot; value=&quot;0.10&quot; step=&quot;0.05&quot;&gt;
      &lt;/div&gt;
    &lt;/section&gt;

    &lt;section class=&quot;tt-buttons&quot;&gt;
      &lt;button id=&quot;tt-run&quot; class=&quot;tt-btn tt-primary&quot;&gt;&amp;#9654; Run generation&lt;/button&gt;
      &lt;button id=&quot;tt-step&quot; class=&quot;tt-btn&quot;&gt;Step&lt;/button&gt;
      &lt;button id=&quot;tt-reset&quot; class=&quot;tt-btn tt-ghost&quot;&gt;Reset&lt;/button&gt;
    &lt;/section&gt;

    &lt;section class=&quot;tt-stage&quot;&gt;
      &lt;div class=&quot;tt-blockpanel&quot;&gt;
        &lt;div class=&quot;tt-row&quot;&gt;
          &lt;span class=&quot;tt-tag&quot;&gt;Prompt (context tower, causal)&lt;/span&gt;
          &lt;span class=&quot;tt-prompt&quot;&gt;France is a country&lt;/span&gt;
        &lt;/div&gt;
        &lt;div class=&quot;tt-row tt-blockmeta&quot;&gt;
          &lt;span&gt;Block &lt;b id=&quot;tt-blockidx&quot;&gt;1&lt;/b&gt;&lt;/span&gt;
          &lt;span&gt;Step &lt;b id=&quot;tt-stepidx&quot;&gt;0&lt;/b&gt;/&lt;span id=&quot;tt-stepmax&quot;&gt;16&lt;/span&gt;&lt;/span&gt;
          &lt;span&gt;Masked fraction &lt;b&gt;&amp;#964;&lt;/b&gt; = &lt;b id=&quot;tt-tau&quot;&gt;1.00&lt;/b&gt;&lt;/span&gt;
        &lt;/div&gt;
        &lt;div id=&quot;tt-grid&quot; class=&quot;tt-grid&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;tt-committed&quot;&gt;
          &lt;span class=&quot;tt-tag&quot;&gt;Committed sequence&lt;/span&gt;
          &lt;div id=&quot;tt-output&quot; class=&quot;tt-output&quot;&gt;&lt;/div&gt;
        &lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;tt-sidepanel&quot;&gt;
        &lt;div class=&quot;tt-metric&quot;&gt;&lt;span class=&quot;tt-mnum&quot; id=&quot;tt-mcommit&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;tt-mlbl&quot;&gt;tokens committed this step&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;tt-metric&quot;&gt;&lt;span class=&quot;tt-mnum&quot; id=&quot;tt-mtotalsteps&quot;&gt;0&lt;/span&gt;&lt;span class=&quot;tt-mlbl&quot;&gt;diffusion steps used (this block)&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;tt-metric&quot;&gt;&lt;span class=&quot;tt-mnum&quot; id=&quot;tt-mavg&quot;&gt;0.00&lt;/span&gt;&lt;span class=&quot;tt-mlbl&quot;&gt;avg tokens / step&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;tt-metric tt-hl&quot;&gt;&lt;span class=&quot;tt-mnum&quot; id=&quot;tt-mpar&quot;&gt;1.0&amp;#215;&lt;/span&gt;&lt;span class=&quot;tt-mlbl&quot;&gt;parallelism vs 1-token AR (illustrative)&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/section&gt;

    &lt;section class=&quot;tt-heatwrap&quot;&gt;
      &lt;div class=&quot;tt-tag&quot;&gt;Commit map &amp;mdash; position (rows) &amp;#215; diffusion step (cols)&lt;/div&gt;
      &lt;p class=&quot;tt-heatnote&quot;&gt;Earlier positions commit earlier, producing the upper-left triangular pattern the paper reports.&lt;/p&gt;
      &lt;div id=&quot;tt-heat&quot; class=&quot;tt-heat&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;tt-legend&quot;&gt;
        &lt;span class=&quot;tt-swatch tt-s0&quot;&gt;&lt;/span&gt;masked
        &lt;span class=&quot;tt-swatch tt-s1&quot;&gt;&lt;/span&gt;committed early
        &lt;span class=&quot;tt-swatch tt-s2&quot;&gt;&lt;/span&gt;committed late
      &lt;/div&gt;
    &lt;/section&gt;

    &lt;footer class=&quot;tt-foot&quot;&gt;
      &lt;span&gt;Mechanism: block-wise autoregressive diffusion · bidirectional in-block attention · layer-aligned cross-attention · context-seeded Mamba-2 · adaLN-single time conditioning&lt;/span&gt;
      &lt;span class=&quot;tt-brand&quot;&gt;Built by &lt;b&gt;Marktechpost&lt;/b&gt;&lt;/span&gt;
    &lt;/footer&gt;
  &lt;/div&gt;

&lt;style&gt;
  #mtp-twotower-demo *{box-sizing:border-box;margin:0;padding:0}
  #mtp-twotower-demo{
    --bg:#0b0b0b; --panel:#111; --panel2:#161616; --line:#242424;
    --green:#76B900; --green-dim:#3f6300; --txt:#e8e8e8; --mut:#9a9a9a;
    font-family:'Inter',-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Helvetica,Arial,sans-serif;
    background:var(--bg); color:var(--txt); padding:22px; border-radius:14px;
    border:1px solid var(--line); line-height:1.5;
  }
  #mtp-twotower-demo .tt-wrap{max-width:920px;margin:0 auto}
  #mtp-twotower-demo h2{font-size:20px;font-weight:800;letter-spacing:-.2px;margin:6px 0 8px;color:#fff}
  #mtp-twotower-demo .tt-badge{display:inline-block;font-size:10px;font-weight:800;letter-spacing:1.5px;
    color:var(--green);border:1px solid var(--green-dim);border-radius:999px;padding:3px 10px}
  #mtp-twotower-demo .tt-sub{font-size:13px;color:var(--mut);max-width:760px}
  #mtp-twotower-demo b{color:#fff}
  #mtp-twotower-demo .tt-sub b{color:var(--green)}

  #mtp-twotower-demo .tt-facts{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:18px 0}
  #mtp-twotower-demo .tt-fact{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:12px}
  #mtp-twotower-demo .tt-num{display:block;font-size:22px;font-weight:800;color:var(--green)}
  #mtp-twotower-demo .tt-lbl{display:block;font-size:11px;color:var(--mut);margin-top:2px}

  #mtp-twotower-demo .tt-controls{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin:6px 0 14px}
  #mtp-twotower-demo .tt-ctrl label{display:flex;justify-content:space-between;align-items:center;font-size:12px;color:var(--mut);margin-bottom:6px}
  #mtp-twotower-demo .tt-pill{background:var(--panel2);border:1px solid var(--line);color:var(--green);
    font-weight:700;padding:1px 8px;border-radius:6px;font-size:12px}
  #mtp-twotower-demo select,#mtp-twotower-demo input[type=range]{width:100%}
  #mtp-twotower-demo select{background:var(--panel2);color:var(--txt);border:1px solid var(--line);
    border-radius:8px;padding:8px;font-size:13px}
  #mtp-twotower-demo input[type=range]{-webkit-appearance:none;appearance:none;height:5px;border-radius:5px;
    background:var(--line);outline:none}
  #mtp-twotower-demo input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;width:16px;height:16px;
    border-radius:50%;background:var(--green);cursor:pointer;border:2px solid #0b0b0b}
  #mtp-twotower-demo input[type=range]::-moz-range-thumb{width:16px;height:16px;border-radius:50%;
    background:var(--green);cursor:pointer;border:2px solid #0b0b0b}

  #mtp-twotower-demo .tt-buttons{display:flex;gap:10px;margin-bottom:18px;flex-wrap:wrap}
  #mtp-twotower-demo .tt-btn{font-family:inherit;font-size:13px;font-weight:700;padding:10px 18px;border-radius:9px;
    border:1px solid var(--line);background:var(--panel2);color:var(--txt);cursor:pointer;transition:.15s}
  #mtp-twotower-demo .tt-btn:hover{border-color:var(--green-dim)}
  #mtp-twotower-demo .tt-primary{background:var(--green);color:#0b0b0b;border-color:var(--green)}
  #mtp-twotower-demo .tt-primary:hover{filter:brightness(1.08)}
  #mtp-twotower-demo .tt-ghost{background:transparent;color:var(--mut)}
  #mtp-twotower-demo .tt-btn:disabled{opacity:.45;cursor:not-allowed}

  #mtp-twotower-demo .tt-stage{display:grid;grid-template-columns:1fr 220px;gap:16px}
  #mtp-twotower-demo .tt-blockpanel,#mtp-twotower-demo .tt-sidepanel{background:var(--panel);border:1px solid var(--line);
    border-radius:12px;padding:16px}
  #mtp-twotower-demo .tt-row{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-bottom:10px}
  #mtp-twotower-demo .tt-tag{font-size:10px;letter-spacing:1px;text-transform:uppercase;color:var(--mut);font-weight:700}
  #mtp-twotower-demo .tt-prompt{font-family:'SFMono-Regular',Consolas,monospace;font-size:13px;color:#fff;
    background:var(--panel2);padding:4px 10px;border-radius:6px;border:1px solid var(--line)}
  #mtp-twotower-demo .tt-blockmeta{font-size:12px;color:var(--mut);gap:18px}
  #mtp-twotower-demo .tt-blockmeta b{color:var(--green)}

  #mtp-twotower-demo .tt-grid{display:flex;flex-wrap:wrap;gap:6px;margin:12px 0 4px}
  #mtp-twotower-demo .tt-cell{min-width:38px;height:40px;display:flex;align-items:center;justify-content:center;
    padding:0 8px;border-radius:8px;font-family:'SFMono-Regular',Consolas,monospace;font-size:13px;font-weight:600;
    background:var(--panel2);border:1px solid var(--line);color:var(--mut);transition:.25s}
  #mtp-twotower-demo .tt-cell.tt-masked{color:#5a5a5a;border-style:dashed}
  #mtp-twotower-demo .tt-cell.tt-just{background:var(--green);color:#0b0b0b;border-color:var(--green);
    transform:translateY(-2px)}
  #mtp-twotower-demo .tt-cell.tt-done{background:rgba(118,185,0,.12);color:#cfe9a0;border-color:var(--green-dim)}

  #mtp-twotower-demo .tt-committed{margin-top:14px;border-top:1px solid var(--line);padding-top:12px}
  #mtp-twotower-demo .tt-output{font-family:'SFMono-Regular',Consolas,monospace;font-size:13px;color:#dfeecb;
    min-height:24px;margin-top:6px;word-spacing:2px}

  #mtp-twotower-demo .tt-sidepanel{display:flex;flex-direction:column;gap:12px}
  #mtp-twotower-demo .tt-metric{background:var(--panel2);border:1px solid var(--line);border-radius:9px;padding:10px 12px}
  #mtp-twotower-demo .tt-metric.tt-hl{border-color:var(--green-dim);background:rgba(118,185,0,.07)}
  #mtp-twotower-demo .tt-mnum{display:block;font-size:22px;font-weight:800;color:#fff}
  #mtp-twotower-demo .tt-hl .tt-mnum{color:var(--green)}
  #mtp-twotower-demo .tt-mlbl{display:block;font-size:10.5px;color:var(--mut);margin-top:1px}

  #mtp-twotower-demo .tt-heatwrap{background:var(--panel);border:1px solid var(--line);border-radius:12px;
    padding:16px;margin-top:16px}
  #mtp-twotower-demo .tt-heatnote{font-size:12px;color:var(--mut);margin:6px 0 12px}
  #mtp-twotower-demo .tt-heat{display:grid;gap:3px}
  #mtp-twotower-demo .tt-hcell{width:100%;aspect-ratio:1;border-radius:2px;background:#141414}
  #mtp-twotower-demo .tt-legend{display:flex;align-items:center;gap:8px;font-size:11px;color:var(--mut);margin-top:12px}
  #mtp-twotower-demo .tt-swatch{width:12px;height:12px;border-radius:3px;display:inline-block;margin-left:10px}
  #mtp-twotower-demo .tt-s0{background:#141414;border:1px solid var(--line);margin-left:0}
  #mtp-twotower-demo .tt-s1{background:var(--green)}
  #mtp-twotower-demo .tt-s2{background:var(--green-dim)}

  #mtp-twotower-demo .tt-foot{display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;
    margin-top:16px;padding-top:14px;border-top:1px solid var(--line);font-size:11px;color:var(--mut)}
  #mtp-twotower-demo .tt-brand b{color:var(--green)}

  @media (max-width:680px){
    #mtp-twotower-demo{padding:16px}
    #mtp-twotower-demo .tt-facts{grid-template-columns:repeat(2,1fr)}
    #mtp-twotower-demo .tt-controls{grid-template-columns:repeat(2,1fr)}
    #mtp-twotower-demo .tt-stage{grid-template-columns:1fr}
  }
&lt;/style&gt;

&lt;script&gt;
(function(){
  var root=document.getElementById('mtp-twotower-demo');
  // Illustrative token stream that &quot;generates&quot; as blocks resolve.
  var STREAM=(&quot;located in Western Europe , bordered by Belgium , Germany , Switzerland , &quot;
    +&quot;Italy , Spain , and Andorra . Its capital city Paris sits on the river Seine &quot;
    +&quot;and anchors a population of roughly sixty seven million people today across the country&quot;)
    .split(/\s+/);

  var els={
    grid:document.getElementById('tt-grid'), out:document.getElementById('tt-output'),
    heat:document.getElementById('tt-heat'),
    sval:document.getElementById('tt-sval'), tval:document.getElementById('tt-tval'),
    gval:document.getElementById('tt-gval'), tempval:document.getElementById('tt-tempval'),
    blocksize:document.getElementById('tt-blocksize'), steps:document.getElementById('tt-steps'),
    gamma:document.getElementById('tt-gamma'), temp:document.getElementById('tt-temp'),
    run:document.getElementById('tt-run'), step:document.getElementById('tt-step'), reset:document.getElementById('tt-reset'),
    blockidx:document.getElementById('tt-blockidx'), stepidx:document.getElementById('tt-stepidx'),
    stepmax:document.getElementById('tt-stepmax'), tau:document.getElementById('tt-tau'),
    mcommit:document.getElementById('tt-mcommit'), mtotalsteps:document.getElementById('tt-mtotalsteps'),
    mavg:document.getElementById('tt-mavg'), mpar:document.getElementById('tt-mpar')
  };

  var S=16,T=16,GAMMA=0.80,TEMP=0.10;
  var state=null, timer=null, streamPtr=0, committedWords=[];

  function cfg(){
    S=parseInt(els.blocksize.value,10); T=parseInt(els.steps.value,10);
    GAMMA=parseFloat(els.gamma.value); TEMP=parseFloat(els.temp.value);
    els.sval.textContent=S; els.tval.textContent=T;
    els.gval.textContent=GAMMA.toFixed(2); els.tempval.textContent=TEMP.toFixed(2);
    els.stepmax.textContent=T;
  }

  // Per-position latent &quot;confidence&quot; that grows with step, biased left and front-loaded.
  function confidence(i,r){
    var left=1.15-(i/S)*0.9;                  // left positions easier
    var frontLoad=1-Math.exp(-0.62*r*Math.max(0.2,left)); // saturating growth over steps
    var noise=(Math.random()-0.5)*(0.12+TEMP*0.6); // temperature widens spread
    return Math.max(0,Math.min(1,frontLoad+noise));
  }

  function newBlock(){
    state={
      cells:new Array(S).fill(null),   // null=masked, else word
      commitStep:new Array(S).fill(0), // step at which committed
      r:0, done:false, commitLog:[]
    };
    renderGrid(true); renderHeat(); updateMeta(0,0);
  }

  function renderGrid(fresh){
    els.grid.innerHTML='';
    for(var i=0;i&lt;S;i++){
      var c=document.createElement('div'); c.className='tt-cell';
      if(state.cells[i]===null){ c.classList.add('tt-masked'); c.textContent='[M]'; }
      else { c.classList.add('tt-done'); c.textContent=state.cells[i]; }
      els.grid.appendChild(c);
    }
  }

  function renderHeat(){
    els.heat.style.gridTemplateColumns='repeat('+T+',1fr)';
    els.heat.innerHTML='';
    for(var pos=0;pos&lt;S;pos++){
      for(var stp=1;stp&lt;=T;stp++){
        var cell=document.createElement('div'); cell.className='tt-hcell';
        var cs=state.commitStep[pos];
        if(cs===stp){
          var frac=(stp-1)/Math.max(1,(T-1));
          // early = bright green, late = dim green
          var g=Math.round(185-frac*95), r2=Math.round(118-frac*79), b=0;
          cell.style.background='rgb('+r2+','+g+','+b+')';
        }
        els.heat.appendChild(cell);
      }
    }
  }

  function updateMeta(committedThisStep,totalCommitted){
    var masked=state.cells.filter(function(x){return x===null;}).length;
    var tau=masked/S;
    els.blockidx.textContent=committedWords.length? (Math.floor(streamPtr/S)+1):1;
    els.stepidx.textContent=state.r; els.tau.textContent=tau.toFixed(2);
    els.mcommit.textContent=committedThisStep;
    els.mtotalsteps.textContent=state.r;
    var filled=S-masked;
    els.mavg.textContent=state.r? (filled/state.r).toFixed(2):'0.00';
    els.mpar.textContent=state.r? (filled/state.r).toFixed(2)+'\u00d7':'1.0\u00d7';
  }

  function stepOnce(){
    if(!state) newBlock();
    if(state.done){ advanceBlock(); return; }
    state.r++;
    var maskedIdx=[]; for(var i=0;i&lt;S;i++) if(state.cells[i]===null) maskedIdx.push(i);
    var committedThisStep=0;
    var forceFinish=(state.r&gt;=T); // floor: guarantee completion within T
    var scored=maskedIdx.map(function(i){return {i:i,c:confidence(i,state.r)};});
    // ensure progress: if nothing would pass gamma and not final, commit the top one
    var anyPass=scored.some(function(s){return s.c&gt;=GAMMA;});
    scored.forEach(function(s){
      var commit = forceFinish || s.c&gt;=GAMMA;
      if(commit){
        var word = STREAM[(streamPtr+s.i)%STREAM.length];
        state.cells[s.i]=word; state.commitStep[s.i]=state.r; committedThisStep++;
      }
    });
    if(!anyPass &amp;&amp; !forceFinish &amp;&amp; committedThisStep===0){
      scored.sort(function(a,b){return b.c-a.c;});
      var top=scored[0];
      state.cells[top.i]=STREAM[(streamPtr+top.i)%STREAM.length];
      state.commitStep[top.i]=state.r; committedThisStep=1;
    }
    // render with &quot;just committed&quot; flash
    els.grid.innerHTML='';
    for(var j=0;j&lt;S;j++){
      var cell=document.createElement('div'); cell.className='tt-cell';
      if(state.cells[j]===null){ cell.classList.add('tt-masked'); cell.textContent='[M]'; }
      else if(state.commitStep[j]===state.r){ cell.classList.add('tt-just'); cell.textContent=state.cells[j]; }
      else { cell.classList.add('tt-done'); cell.textContent=state.cells[j]; }
      els.grid.appendChild(cell);
    }
    renderHeat();
    var masked=state.cells.filter(function(x){return x===null;}).length;
    updateMeta(committedThisStep, S-masked);
    if(masked===0){ state.done=true; }
  }

  function advanceBlock(){
    // commit block into output stream, start next block
    for(var i=0;i&lt;S;i++){ committedWords.push(state.cells[i]); }
    streamPtr+=S;
    els.out.textContent=committedWords.join(' ');
    if(streamPtr&gt;=STREAM.length){ // finished the passage
      stop(); els.run.textContent='\u21bb Replay'; return;
    }
    newBlock();
  }

  function loop(){ stepOnce(); if(state.done){ setTimeout(advanceBlock, 450); } }

  function run(){
    if(timer){ stop(); return; }
    if(committedWords.length &amp;&amp; streamPtr&gt;=STREAM.length){ hardReset(); }
    els.run.textContent='\u23f8 Pause';
    timer=setInterval(loop, 620);
  }
  function stop(){ if(timer){clearInterval(timer);timer=null;} els.run.textContent='\u25b6 Run generation'; }

  function hardReset(){
    stop(); cfg(); streamPtr=0; committedWords=[]; els.out.textContent='';
    newBlock(); els.run.textContent='\u25b6 Run generation';
  }

  els.run.addEventListener('click',run);
  els.step.addEventListener('click',function(){
    stop();
    if(state &amp;&amp; state.done){ advanceBlock(); return; } // finished block -&gt; start next
    stepOnce();
  });
  els.reset.addEventListener('click',hardReset);
  [els.blocksize,els.steps,els.gamma,els.temp].forEach(function(el){
    el.addEventListener('input',function(){ hardReset(); });
  });

  cfg(); newBlock();

  // WordPress auto-resize: report our own height to the parent.
  function reportHeight(){
    try{
      var h=root.offsetHeight+40;
      if(window.parent&amp;&amp;window.parent!==window){
        window.parent.postMessage({mtpTwoTowerHeight:h},'*');
      }
    }catch(e){}
  }
  var ro=window.ResizeObserver?new ResizeObserver(reportHeight):null;
  if(ro) ro.observe(root);
  window.addEventListener('load',reportHeight);
  setInterval(reportHeight,1200);
})();
&lt;/script&gt;
&lt;/div&gt;
"></iframe>
<script>
(function(){
  window.addEventListener("message",function(e){
    var d=e&&e.data; if(d&&d.mtpTwoTowerHeight){
      var f=document.getElementById("mtp-twotower-frame");
      if(f){f.style.height=d.mtpTwoTowerHeight+"px";}
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the<strong> <a href="https://arxiv.org/pdf/2606.26493" target="_blank" rel="noreferrer noopener">Paper</a></strong> and <a href="https://huggingface.co/collections/nvidia/nemotron-labs-twotower" target="_blank" rel="noreferrer noopener"><strong>Weights</strong></a>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>



<p class="wp-block-paragraph"></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/01/nvidia-releases-nemotron-labs-twotower/">NVIDIA Releases Nemotron-Labs-TwoTower: an Open-Weight Diffusion Language Model Built on a Frozen Autoregressive Nemotron-3-Nano-30B-A3B Backbone</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80792</post-id>
	</item>
		<item>
		<title>Google AI Introduces TabFM: A Hybrid-Attention Tabular Foundation Model for Zero-Shot Classification and Regression</title>
		<link>https://www.marktechpost.com/2026/07/01/google-ai-introduces-tabfm-a-hybrid-attention-tabular-foundation-model-for-zero-shot-classification-and-regression/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Wed, 01 Jul 2026 07:48:59 +0000</pubDate>
				<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80789</guid>

					<description><![CDATA[<p>Google Research has released TabFM, a foundation model for tabular data. It performs zero-shot classification and regression through in-context learning. Predictions come from a single forward pass, with no per-dataset training, hyperparameter tuning, or feature engineering.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/01/google-ai-introduces-tabfm-a-hybrid-attention-tabular-foundation-model-for-zero-shot-classification-and-regression/">Google AI Introduces TabFM: A Hybrid-Attention Tabular Foundation Model for Zero-Shot Classification and Regression</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Google Research introduced <a href="https://research.google/blog/introducing-tabfm-a-zero-shot-foundation-model-for-tabular-data/" target="_blank" rel="noreferrer noopener">TabFM</a>, a foundation model built for tabular data. TabFM performs classification and regression without dataset-specific training. Every prediction comes from a single forward pass. The model reframes tabular prediction as an in-context learning problem. It is available now on Hugging Face and GitHub.</p>



<h2 id="h-tl-dr" class="wp-block-heading"><strong>TL;DR</strong></h2>



<ul class="wp-block-list">
<li>TabFM predicts on unseen tables with no training, tuning, or feature engineering.</li>



<li>It reads the full dataset as one prompt, then predicts via in-context learning.</li>



<li>The architecture combines TabPFN-style row/column attention with TabICL-style in-context learning.</li>



<li>Training used hundreds of millions of synthetic datasets from structural causal models.</li>



<li>Google BigQuery will expose TabFM through an AI.PREDICT SQL command soon.</li>
</ul>



<h2 id="h-what-is-tabfm" class="wp-block-heading"><strong>What is TabFM?</strong></h2>



<p class="wp-block-paragraph">Tabular data forms the backbone of enterprise data infrastructure. Tasks like customer churn and financial fraud detection live in tables. For years, tree-based methods dominated this space. XGBoost, AdaBoost, and random forests offered robust results on structured data. Google frames TabFM as the tabular counterpart to TimesFM, its zero-shot time-series model.</p>



<p class="wp-block-paragraph">That reliability carried a cost. Fitting XGBoost to a new dataset is rarely one <code>.fit()</code> call. Data scientists spend hours on hyperparameter optimization and feature engineering. They do this just to extract a reliable signal from raw data. TabFM targets exactly that bottleneck.</p>



<p class="wp-block-paragraph">TabFM applies the zero-shot logic that large language models made familiar. LLMs learn new tasks from in-context examples, without updating any weights. This technique is called in-context learning (ICL). TabFM brings the same idea to tables. It generates predictions on previously unseen tables in one pass.</p>



<h2 id="h-how-it-works" class="wp-block-heading"><strong>How It Works</strong></h2>



<p class="wp-block-paragraph">Traditional models update parameters for each dataset&#8217;s distribution. TabFM skips that step entirely. It takes the whole dataset as a single unified prompt. That prompt holds both training examples and target testing rows. The model reads column and row relationships at inference time.</p>



<p class="wp-block-paragraph">Tables are not text. They are two-dimensional and inherently orderless. Swapping two rows or two columns does not change their meaning. Standard language models process one-dimensional, ordered sequences instead. To bridge that gap, TabFM synthesizes TabPFN and TabICL into a hybrid design. </p>



<p class="wp-block-paragraph"><strong>It relies on three mechanisms</strong>:</p>



<ul class="wp-block-list">
<li><strong>Alternating row and column attention:</strong> The raw table passes through a multilayer attention module. Following TabPFN, attention alternates across columns (features) and rows (examples). This deep contextualization captures feature interactions and dependencies. It performs work that would otherwise need manual feature crafting.</li>



<li><strong>Row compression:</strong> Each row&#8217;s cross-attended information compresses into a single dense vector.</li>



<li><strong>In-context learning:</strong> A dedicated Transformer runs over these compressed embeddings. Following TabICL, attending to compressed rows cuts computation cost sharply. Prediction stays efficient even on much larger datasets.</li>
</ul>



<h2 id="h-training-on-synthetic-data-at-scale" class="wp-block-heading"><strong>Training On Synthetic Data at Scale</strong></h2>



<p class="wp-block-paragraph">Foundation models need vast, diverse data. High-quality tabular datasets are scarce in the open-source space. Industrial tables carry proprietary schemas and sensitive information. That makes them inaccessible for broad pre-training.</p>



<p class="wp-block-paragraph">Synthetic tables can be generated to be arbitrarily large. Google&#8217;s research team calls them effectively the only viable option at this scale. So TabFM trains entirely on hundreds of millions of synthetic datasets. These are generated dynamically using structural causal models (SCMs). Each incorporates a wide variety of random functions. The approach captures distributions and complex feature relationships found in real tables. The research team reports the model generalizes well to unseen real-world data.</p>



<h2 id="h-performance-and-benchmarking" class="wp-block-heading"><strong>Performance and Benchmarking</strong></h2>



<p class="wp-block-paragraph">The research team evaluated TabFM on TabArena. TabArena is a living benchmark that computes Elo scores from head-to-head win rates. The evaluation spans 38 classification datasets and 13 regression datasets. Sample sizes range from 700 to 150,000.</p>



<p class="wp-block-paragraph">Two configurations were tested. Plain TabFM runs out-of-the-box in a single forward pass. It needs no tuning or cross-validation. TabFM-Ensemble adds cross features and SVD (Singular Value Decomposition) features. It computes optimal weights for a 32-way ensemble using a non-negative least squares solver. For classification, it also adds Platt scaling as a calibration step.</p>



<p class="wp-block-paragraph">The research team reports TabFM consistently outperforms heavily tuned, industry-standard supervised algorithms. Full per-fold metrics and head-to-head win rates sit on the GitHub page.</p>



<figure class="wp-block-table is-style-stripes"><table class="has-fixed-layout"><thead><tr><th>Aspect</th><th>Traditional GBDT (XGBoost)</th><th>TabFM</th><th>TabFM-Ensemble</th></tr></thead><tbody><tr><td>Per-dataset training</td><td>Required</td><td>None (in-context learning)</td><td>None</td></tr><tr><td>Hyperparameter tuning</td><td>Extensive, manual</td><td>None</td><td>Ensemble weights via NNLS</td></tr><tr><td>Feature engineering</td><td>Manual, domain-specific</td><td>Learned by attention</td><td>Adds cross + SVD features</td></tr><tr><td>Prediction</td><td>After full training</td><td>Single forward pass</td><td>32-way ensemble</td></tr><tr><td>Calibration</td><td>Manual (optional)</td><td>—</td><td>Platt scaling (classification)</td></tr></tbody></table></figure>



<h2 id="h-getting-started-installation-and-code" class="wp-block-heading"><strong>Getting Started: Installation and Code</strong></h2>



<p class="wp-block-paragraph">Installation clones the repository and installs it locally. The base install uses CPU-only JAX. A <code>cuda</code> extra pulls the CUDA 12 plugin and NVIDIA libraries for GPU runs.</p>



<p class="wp-block-paragraph">Core requirements are specific. You need Python 3.11 or later. It pins <code>jax==0.10.1</code> and <code>flax==0.12.7</code>, using the modern <code>flax.nnx</code> API. Hugging Face Hub downloads the pre-trained weights automatically.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import numpy as np
import pandas as pd
from tabfm import tabfm_v1_0_0
from tabfm import TabFMClassifier

# Load pre-trained TabFM v1.0.0 (downloads from Hugging Face)
model = tabfm_v1_0_0.load()

# scikit-learn compatible classifier
clf = TabFMClassifier(model=model)

X_train = pd.DataFrame({
    "age": [25.0, 45.0, 35.0, 50.0],
    "job": ["engineer", "manager", "engineer", "manager"],
    "income": [80000, 120000, 90000, 130000]
})
y_train = np.array(["low_risk", "high_risk", "low_risk", "high_risk"])

X_test = pd.DataFrame({
    "age": [30.0, 48.0],
    "job": ["engineer", "manager"],
    "income": [85000, 125000]
})

clf.fit(X_train, y_train)
predictions = clf.predict(X_test)
probabilities = clf.predict_proba(X_test)

print("Predictions:", predictions)
print("Class Probabilities:\n", probabilities)</code></pre></div></div>



<p class="wp-block-paragraph">Here <code>fit()</code> prepares ordinal encoders and numerical scalers. It does not train model weights on your data. The regressor mirrors this pattern with <code>TabFMRegressor</code> and <code>reg.predict()</code>.</p>



<h2 id="h-use-cases-with-examples" class="wp-block-heading"><strong>Use Cases With Examples</strong></h2>



<p class="wp-block-paragraph">The API fits common predictive tasks directly. For customer churn, the context holds past customers labeled churned or retained. TabFM scores churn risk for new customers in one pass.</p>



<p class="wp-block-paragraph">For credit risk, rows carry age, job, and income features. Labels mark <code>low_risk</code> or <code>high_risk</code>, as in the sample code. New applicants get scored without a training cycle.</p>



<p class="wp-block-paragraph">For regression, house price prediction is a natural fit. Context rows carry square footage and neighborhood. TabFM returns a predicted price for unseen listings.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive Explainer</strong></h2>



<!-- TabFM Zero-Shot Playground — Marktechpost interactive embed. Paste into a Custom HTML block. -->
<iframe
  id="tabfm-playground"
  style="width:100%;border:0;overflow:hidden;display:block;"
  height="600"
  scrolling="no"
  loading="lazy"
  title="TabFM Zero-Shot Prediction Playground"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;UTF-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;
&lt;title&gt;TabFM Zero-Shot Playground&lt;/title&gt;
&lt;style&gt;
  :root{
    --bg:#f6f8fb; --card:#ffffff; --ink:#0f172a; --muted:#64748b;
    --line:#e5e9f0; --line2:#eef2f7; --accent:#2563eb; --accent-soft:#eff4ff;
    --green:#76B900; --amber:#b45309; --pos:#16a34a; --neg:#dc2626;
    --shadow:0 1px 2px rgba(15,23,42,.04),0 8px 24px rgba(15,23,42,.06);
    --radius:14px;
  }
  *{box-sizing:border-box}
  body{margin:0;background:var(--bg);color:var(--ink);
    font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Inter,Helvetica,Arial,sans-serif;
    -webkit-font-smoothing:antialiased;line-height:1.5;}
  .wrap{max-width:860px;margin:0 auto;padding:20px 16px 8px;}
  .app{background:var(--card);border:1px solid var(--line);border-radius:var(--radius);box-shadow:var(--shadow);overflow:hidden;}
  .hd{padding:20px 22px 16px;border-bottom:1px solid var(--line2);}
  .eyebrow{font-size:11px;font-weight:700;letter-spacing:.09em;text-transform:uppercase;color:var(--accent);}
  h1{margin:6px 0 4px;font-size:21px;font-weight:700;letter-spacing:-.02em;}
  .sub{margin:0;font-size:13.5px;color:var(--muted);}
  .tabs{display:flex;gap:6px;padding:14px 22px 0;flex-wrap:wrap;}
  .tab{appearance:none;border:1px solid var(--line);background:#fff;color:var(--muted);
    font-size:13px;font-weight:600;padding:9px 14px;border-radius:9px;cursor:pointer;transition:.15s;}
  .tab:hover{border-color:#cdd7e6;color:var(--ink);}
  .tab.on{background:var(--accent);border-color:var(--accent);color:#fff;box-shadow:0 4px 12px rgba(37,99,235,.22);}
  .body{padding:18px 22px 22px;}
  .lead{font-size:13px;color:var(--muted);margin:0 0 14px;}
  .lead b{color:var(--ink);font-weight:650;}
  .panel{border:1px solid var(--line);border-radius:12px;overflow:hidden;margin-bottom:16px;}
  .panel-hd{display:flex;align-items:center;justify-content:space-between;gap:10px;
    padding:11px 14px;background:#fbfcfe;border-bottom:1px solid var(--line2);}
  .panel-hd .t{font-size:12.5px;font-weight:700;letter-spacing:.02em;}
  .panel-hd .t .pill{display:inline-block;margin-left:8px;font-size:10.5px;font-weight:700;
    color:var(--accent);background:var(--accent-soft);padding:2px 7px;border-radius:20px;vertical-align:middle;}
  .rowbtns{display:flex;gap:6px;}
  .mini{appearance:none;border:1px solid var(--line);background:#fff;color:var(--muted);
    font-size:12px;font-weight:600;padding:5px 10px;border-radius:7px;cursor:pointer;transition:.15s;}
  .mini:hover{border-color:#cdd7e6;color:var(--ink);}
  .mini:disabled{opacity:.4;cursor:not-allowed;}
  .tablewrap{overflow-x:auto;}
  table{width:100%;border-collapse:collapse;font-size:13px;min-width:440px;}
  th{font-size:11px;text-transform:uppercase;letter-spacing:.05em;color:var(--muted);
    text-align:left;padding:9px 12px;background:#fafbfd;border-bottom:1px solid var(--line2);font-weight:700;white-space:nowrap;}
  td{padding:6px 8px;border-bottom:1px solid var(--line2);}
  tr:last-child td{border-bottom:none;}
  .cell input,.cell select,.io input,.io select{
    width:100%;border:1px solid var(--line);border-radius:7px;padding:7px 8px;font-size:13px;
    background:#fff;color:var(--ink);font-family:inherit;transition:.15s;}
  .cell input:focus,.cell select:focus,.io input:focus,.io select:focus{
    outline:none;border-color:var(--accent);box-shadow:0 0 0 3px rgba(37,99,235,.12);}
  .lbl-y{color:var(--neg);font-weight:700;} .lbl-n{color:var(--pos);font-weight:700;}
  .delcol{width:34px;text-align:center;}
  .del{border:none;background:none;color:#cbd5e1;cursor:pointer;font-size:16px;line-height:1;padding:4px;transition:.15s;}
  .del:hover{color:var(--neg);}
  .io-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:12px;padding:14px;}
  .io label{display:block;font-size:11px;font-weight:700;letter-spacing:.04em;text-transform:uppercase;color:var(--muted);margin-bottom:5px;}
  .runbar{display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin:2px 0 4px;}
  .run{appearance:none;border:none;background:var(--accent);color:#fff;font-size:14px;font-weight:700;
    padding:12px 20px;border-radius:10px;cursor:pointer;transition:.15s;box-shadow:0 6px 16px rgba(37,99,235,.24);}
  .run:hover{background:#1d4ed8;transform:translateY(-1px);}
  .run:active{transform:translateY(0);}
  .run-note{font-size:12px;color:var(--muted);}
  .result{margin-top:16px;border:1px solid var(--line);border-radius:12px;padding:16px 18px;
    background:linear-gradient(180deg,#fbfcff,#ffffff);opacity:0;transform:translateY(6px);
    transition:.35s ease;}
  .result.show{opacity:1;transform:none;}
  .result .rlabel{font-size:11px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;color:var(--muted);margin-bottom:8px;}
  .verdict{font-size:26px;font-weight:800;letter-spacing:-.02em;margin:0 0 2px;}
  .verdict.pos{color:var(--pos);} .verdict.neg{color:var(--neg);} .verdict.num{color:var(--accent);}
  .conf{font-size:13px;color:var(--muted);margin:0 0 14px;}
  .bars{display:flex;flex-direction:column;gap:10px;}
  .bar-row{display:grid;grid-template-columns:96px 1fr 52px;align-items:center;gap:10px;font-size:12.5px;}
  .bar-name{font-weight:650;} 
  .bar-track{display:block;height:10px;background:#eef2f7;border-radius:20px;overflow:hidden;}
  .bar-fill{display:block;height:100%;border-radius:20px;width:0;transition:width .6s cubic-bezier(.2,.7,.2,1);}
  .bar-val{text-align:right;font-variant-numeric:tabular-nums;color:var(--muted);font-weight:650;}
  .explain{margin-top:14px;font-size:12.5px;color:var(--muted);border-top:1px dashed var(--line);padding-top:12px;}
  .explain b{color:var(--ink);}
  .neighbors{margin-top:10px;font-size:12px;color:var(--muted);}
  .neighbors code{background:#f1f5f9;border-radius:5px;padding:1px 6px;font-size:11.5px;color:#334155;}
  .note{display:flex;gap:9px;align-items:flex-start;background:#fffbeb;border:1px solid #fde68a;
    border-radius:10px;padding:11px 13px;font-size:12px;color:var(--amber);line-height:1.45;margin-top:4px;}
  .note svg{flex:0 0 auto;margin-top:1px;}
  .ft{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap;
    padding:13px 22px;border-top:1px solid var(--line2);background:#fbfcfe;font-size:12px;color:var(--muted);}
  .ft a{color:var(--accent);text-decoration:none;font-weight:600;}
  .ft a:hover{text-decoration:underline;}
  .brand{font-weight:800;letter-spacing:-.01em;color:var(--green);}
  @media(max-width:560px){
    h1{font-size:19px;} .io-grid{grid-template-columns:1fr 1fr;} 
    .bar-row{grid-template-columns:82px 1fr 46px;}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot;&gt;
  &lt;div class=&quot;app&quot;&gt;
    &lt;div class=&quot;hd&quot;&gt;
      &lt;div class=&quot;eyebrow&quot;&gt;Interactive Explainer&lt;/div&gt;
      &lt;h1&gt;TabFM Zero-Shot Prediction Playground&lt;/h1&gt;
      &lt;p class=&quot;sub&quot;&gt;Feed a small table as context. Predict a new row in one pass &amp;mdash; no training step.&lt;/p&gt;
    &lt;/div&gt;

    &lt;div class=&quot;tabs&quot; id=&quot;tabs&quot;&gt;
      &lt;button class=&quot;tab on&quot; data-task=&quot;churn&quot;&gt;Customer Churn &amp;middot; Classification&lt;/button&gt;
      &lt;button class=&quot;tab&quot; data-task=&quot;house&quot;&gt;House Price &amp;middot; Regression&lt;/button&gt;
    &lt;/div&gt;

    &lt;div class=&quot;body&quot;&gt;
      &lt;p class=&quot;lead&quot; id=&quot;lead&quot;&gt;&lt;/p&gt;

      &lt;div class=&quot;panel&quot;&gt;
        &lt;div class=&quot;panel-hd&quot;&gt;
          &lt;div class=&quot;t&quot;&gt;Context table &lt;span class=&quot;pill&quot; id=&quot;ctxpill&quot;&gt;the &quot;prompt&quot;&lt;/span&gt;&lt;/div&gt;
          &lt;div class=&quot;rowbtns&quot;&gt;
            &lt;button class=&quot;mini&quot; id=&quot;addRow&quot;&gt;+ Add row&lt;/button&gt;
            &lt;button class=&quot;mini&quot; id=&quot;resetRows&quot;&gt;Reset&lt;/button&gt;
          &lt;/div&gt;
        &lt;/div&gt;
        &lt;div class=&quot;tablewrap&quot;&gt;&lt;table id=&quot;ctxTable&quot;&gt;&lt;/table&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;panel&quot;&gt;
        &lt;div class=&quot;panel-hd&quot;&gt;&lt;div class=&quot;t&quot;&gt;New row to predict &lt;span class=&quot;pill&quot;&gt;test input&lt;/span&gt;&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;io-grid&quot; id=&quot;testInputs&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;runbar&quot;&gt;
        &lt;button class=&quot;run&quot; id=&quot;runBtn&quot;&gt;Predict &amp;rarr; single forward pass&lt;/button&gt;
        &lt;span class=&quot;run-note&quot; id=&quot;runNote&quot;&gt;&lt;/span&gt;
      &lt;/div&gt;

      &lt;div class=&quot;result&quot; id=&quot;result&quot;&gt;&lt;/div&gt;

      &lt;div class=&quot;note&quot;&gt;
        &lt;svg width=&quot;15&quot; height=&quot;15&quot; viewBox=&quot;0 0 24 24&quot; fill=&quot;none&quot; stroke=&quot;currentColor&quot; stroke-width=&quot;2.2&quot; stroke-linecap=&quot;round&quot;&gt;&lt;path d=&quot;M12 9v4M12 17h.01M10.3 3.9 1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0Z&quot;/&gt;&lt;/svg&gt;
        &lt;span&gt;&lt;b&gt;Illustrative demo.&lt;/b&gt; This runs a small in-browser, distance-weighted nearest-neighbor predictor to
        show the &lt;b&gt;learn-from-context, no-training&lt;/b&gt; idea behind in-context learning. It is not the actual TabFM
        model, weights, or architecture. For real predictions, use the official TabFM package.&lt;/span&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;div class=&quot;ft&quot;&gt;
      &lt;span&gt;Concept demo of Google Research&amp;rsquo;s TabFM in-context workflow&lt;/span&gt;
      &lt;span&gt;Built by &lt;span class=&quot;brand&quot;&gt;Marktechpost&lt;/span&gt;&lt;/span&gt;
    &lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  &quot;use strict&quot;;

  var TASKS = {
    churn: {
      lead: '&lt;b&gt;Task:&lt;/b&gt; predict whether a customer churns. TabFM reads labeled customers as context. It then scores a new customer in one forward pass, with no retraining.',
      numeric: [
        {key:&quot;tenure&quot;, label:&quot;Tenure (months)&quot;, min:0, max:72},
        {key:&quot;charges&quot;, label:&quot;Monthly charges ($)&quot;, min:15, max:130}
      ],
      cat: [
        {key:&quot;contract&quot;, label:&quot;Contract&quot;, opts:[&quot;Month-to-month&quot;,&quot;One year&quot;,&quot;Two year&quot;]}
      ],
      target:{key:&quot;churn&quot;, label:&quot;Churn&quot;, opts:[&quot;Yes&quot;,&quot;No&quot;]},
      rows:[
        {tenure:2, charges:95, contract:&quot;Month-to-month&quot;, churn:&quot;Yes&quot;},
        {tenure:1, charges:80, contract:&quot;Month-to-month&quot;, churn:&quot;Yes&quot;},
        {tenure:8, charges:105, contract:&quot;Month-to-month&quot;, churn:&quot;Yes&quot;},
        {tenure:45, charges:70, contract:&quot;Two year&quot;, churn:&quot;No&quot;},
        {tenure:60, charges:55, contract:&quot;Two year&quot;, churn:&quot;No&quot;},
        {tenure:36, charges:90, contract:&quot;One year&quot;, churn:&quot;No&quot;}
      ],
      test:{tenure:4, charges:98, contract:&quot;Month-to-month&quot;}
    },
    house: {
      lead: '&lt;b&gt;Task:&lt;/b&gt; predict a house price. TabFM reads recent sales as context. It then estimates a new listing&amp;rsquo;s price in one forward pass, with no retraining.',
      numeric: [
        {key:&quot;sqft&quot;, label:&quot;Sq. footage&quot;, min:600, max:4000},
        {key:&quot;beds&quot;, label:&quot;Bedrooms&quot;, min:1, max:6}
      ],
      cat: [
        {key:&quot;hood&quot;, label:&quot;Neighborhood&quot;, opts:[&quot;A&quot;,&quot;B&quot;,&quot;C&quot;]}
      ],
      target:{key:&quot;price&quot;, label:&quot;Price ($)&quot;, numeric:true},
      rows:[
        {sqft:1200, beds:2, hood:&quot;A&quot;, price:250000},
        {sqft:1500, beds:3, hood:&quot;A&quot;, price:310000},
        {sqft:2500, beds:4, hood:&quot;B&quot;, price:550000},
        {sqft:2200, beds:3, hood:&quot;B&quot;, price:495000},
        {sqft:3000, beds:5, hood:&quot;C&quot;, price:620000},
        {sqft:1800, beds:3, hood:&quot;C&quot;, price:410000}
      ],
      test:{sqft:1900, beds:3, hood:&quot;B&quot;}
    }
  };

  var state = { task:&quot;churn&quot;, rows:[], test:{} };
  var $ = function(id){return document.getElementById(id);};

  function clone(o){return JSON.parse(JSON.stringify(o));}

  function loadTask(name){
    state.task = name;
    state.rows = clone(TASKS[name].rows);
    state.test = clone(TASKS[name].test);
    $(&quot;lead&quot;).innerHTML = TASKS[name].lead;
    $(&quot;ctxpill&quot;).textContent = name === &quot;churn&quot; ? &quot;labeled examples&quot; : &quot;recent sales&quot;;
    renderTable();
    renderTestInputs();
    $(&quot;result&quot;).className = &quot;result&quot;;
    $(&quot;result&quot;).innerHTML = &quot;&quot;;
    $(&quot;runNote&quot;).textContent = &quot;&quot;;
  }

  function cols(){
    var t = TASKS[state.task];
    return t.numeric.concat(t.cat);
  }

  function renderTable(){
    var t = TASKS[state.task], tbl = $(&quot;ctxTable&quot;);
    var head = &quot;&lt;thead&gt;&lt;tr&gt;&quot;;
    cols().forEach(function(c){ head += &quot;&lt;th&gt;&quot;+c.label+&quot;&lt;/th&gt;&quot;; });
    head += &quot;&lt;th&gt;&quot;+t.target.label+&quot;&lt;/th&gt;&lt;th class='delcol'&gt;&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&quot;;
    var bodyHtml = &quot;&lt;tbody&gt;&quot;;
    state.rows.forEach(function(row,ri){
      bodyHtml += &quot;&lt;tr&gt;&quot;;
      cols().forEach(function(c){
        bodyHtml += &quot;&lt;td class='cell'&gt;&quot;+cellInput(c,row[c.key],ri)+&quot;&lt;/td&gt;&quot;;
      });
      // target cell
      if(t.target.numeric){
        bodyHtml += &quot;&lt;td class='cell'&gt;&lt;input type='number' data-ri='&quot;+ri+&quot;' data-k='&quot;+t.target.key+&quot;' value='&quot;+row[t.target.key]+&quot;'&gt;&lt;/td&gt;&quot;;
      } else {
        var sel = &quot;&lt;select data-ri='&quot;+ri+&quot;' data-k='&quot;+t.target.key+&quot;'&gt;&quot;;
        t.target.opts.forEach(function(o){ sel += &quot;&lt;option &quot;+(row[t.target.key]===o?&quot;selected&quot;:&quot;&quot;)+&quot;&gt;&quot;+o+&quot;&lt;/option&gt;&quot;; });
        sel += &quot;&lt;/select&gt;&quot;;
        bodyHtml += &quot;&lt;td class='cell'&gt;&quot;+sel+&quot;&lt;/td&gt;&quot;;
      }
      bodyHtml += &quot;&lt;td class='delcol'&gt;&lt;button class='del' data-del='&quot;+ri+&quot;' title='Remove row'&gt;&amp;times;&lt;/button&gt;&lt;/td&gt;&quot;;
      bodyHtml += &quot;&lt;/tr&gt;&quot;;
    });
    bodyHtml += &quot;&lt;/tbody&gt;&quot;;
    tbl.innerHTML = head + bodyHtml;

    tbl.querySelectorAll(&quot;input,select&quot;).forEach(function(el){
      el.addEventListener(&quot;change&quot;, function(){
        var ri = +el.getAttribute(&quot;data-ri&quot;), k = el.getAttribute(&quot;data-k&quot;);
        var v = el.value;
        var numeric = (el.type === &quot;number&quot;);
        state.rows[ri][k] = numeric ? parseFloat(v) : v;
      });
    });
    tbl.querySelectorAll(&quot;.del&quot;).forEach(function(b){
      b.addEventListener(&quot;click&quot;, function(){
        if(state.rows.length&lt;=2) return;
        state.rows.splice(+b.getAttribute(&quot;data-del&quot;),1);
        renderTable();
      });
    });
    $(&quot;addRow&quot;).disabled = state.rows.length&gt;=10;
  }

  function cellInput(c,val,ri){
    if(c.opts){
      var s = &quot;&lt;select data-ri='&quot;+ri+&quot;' data-k='&quot;+c.key+&quot;'&gt;&quot;;
      c.opts.forEach(function(o){ s += &quot;&lt;option &quot;+(val===o?&quot;selected&quot;:&quot;&quot;)+&quot;&gt;&quot;+o+&quot;&lt;/option&gt;&quot;; });
      return s+&quot;&lt;/select&gt;&quot;;
    }
    return &quot;&lt;input type='number' data-ri='&quot;+ri+&quot;' data-k='&quot;+c.key+&quot;' value='&quot;+val+&quot;'&gt;&quot;;
  }

  function renderTestInputs(){
    var t = TASKS[state.task], wrap = $(&quot;testInputs&quot;);
    var html = &quot;&quot;;
    cols().forEach(function(c){
      html += &quot;&lt;div class='io'&gt;&lt;label&gt;&quot;+c.label+&quot;&lt;/label&gt;&quot;;
      if(c.opts){
        html += &quot;&lt;select data-tk='&quot;+c.key+&quot;'&gt;&quot;;
        c.opts.forEach(function(o){ html += &quot;&lt;option &quot;+(state.test[c.key]===o?&quot;selected&quot;:&quot;&quot;)+&quot;&gt;&quot;+o+&quot;&lt;/option&gt;&quot;; });
        html += &quot;&lt;/select&gt;&quot;;
      } else {
        html += &quot;&lt;input type='number' data-tk='&quot;+c.key+&quot;' value='&quot;+state.test[c.key]+&quot;'&gt;&quot;;
      }
      html += &quot;&lt;/div&gt;&quot;;
    });
    wrap.innerHTML = html;
    wrap.querySelectorAll(&quot;input,select&quot;).forEach(function(el){
      el.addEventListener(&quot;change&quot;, function(){
        var k = el.getAttribute(&quot;data-tk&quot;);
        state.test[k] = (el.type===&quot;number&quot;) ? parseFloat(el.value) : el.value;
      });
    });
  }

  // ---- illustrative predictor: distance-weighted k-NN over context rows ----
  function ranges(){
    var t = TASKS[state.task], r = {};
    t.numeric.forEach(function(c){
      var vals = state.rows.map(function(row){return row[c.key];});
      var mn = Math.min.apply(null,vals), mx = Math.max.apply(null,vals);
      r[c.key] = (mx-mn) || 1;
    });
    return r;
  }

  function distance(a,b,rg){
    var t = TASKS[state.task], d = 0;
    t.numeric.forEach(function(c){
      var diff = (a[c.key]-b[c.key])/rg[c.key];
      d += diff*diff;
    });
    t.cat.forEach(function(c){
      if(a[c.key]!==b[c.key]) d += 1;
    });
    return Math.sqrt(d);
  }

  function predict(){
    var t = TASKS[state.task], rg = ranges();
    var scored = state.rows.map(function(row){
      var dist = distance(state.test,row,rg);
      return {row:row, dist:dist, w:1/(dist*dist+0.05)};
    }).sort(function(a,b){return a.dist-b.dist;});

    var k = Math.min(5, scored.length);
    var top = scored.slice(0,k);
    var wsum = top.reduce(function(s,x){return s+x.w;},0);

    if(t.target.numeric){
      var est = top.reduce(function(s,x){return s + x.w*x.row[t.target.key];},0)/wsum;
      showRegression(est, top);
    } else {
      var probs = {};
      t.target.opts.forEach(function(o){ probs[o]=0; });
      top.forEach(function(x){ probs[x.row[t.target.key]] += x.w; });
      t.target.opts.forEach(function(o){ probs[o] = probs[o]/wsum; });
      showClassification(probs, top);
    }
  }

  function fmtMoney(v){ return &quot;$&quot;+Math.round(v).toLocaleString(&quot;en-US&quot;); }

  function neighborLine(top){
    var t = TASKS[state.task];
    var parts = top.map(function(x){
      var lab = t.target.numeric ? fmtMoney(x.row[t.target.key]) : x.row[t.target.key];
      return &quot;&lt;code&gt;&quot;+lab+&quot;&lt;/code&gt;&quot;;
    });
    return &quot;Nearest context rows used: &quot;+parts.join(&quot; &quot;);
  }

  function showClassification(probs, top){
    var t = TASKS[state.task];
    var best = t.target.opts[0];
    t.target.opts.forEach(function(o){ if(probs[o]&gt;probs[best]) best=o; });
    var isChurn = (best===&quot;Yes&quot;);
    var conf = (probs[best]*100).toFixed(1);
    var bars = &quot;&quot;;
    t.target.opts.forEach(function(o){
      var pct = (probs[o]*100);
      var color = (o===&quot;Yes&quot;) ? &quot;var(--neg)&quot; : &quot;var(--pos)&quot;;
      bars += &quot;&lt;div class='bar-row'&gt;&lt;span class='bar-name'&gt;&quot;+o+&quot;&lt;/span&gt;&quot;+
              &quot;&lt;span class='bar-track'&gt;&lt;span class='bar-fill' style='background:&quot;+color+&quot;' data-pct='&quot;+pct.toFixed(1)+&quot;'&gt;&lt;/span&gt;&lt;/span&gt;&quot;+
              &quot;&lt;span class='bar-val'&gt;&quot;+pct.toFixed(1)+&quot;%&lt;/span&gt;&lt;/div&gt;&quot;;
    });
    $(&quot;result&quot;).innerHTML =
      &quot;&lt;div class='rlabel'&gt;Prediction &amp;middot; class probabilities&lt;/div&gt;&quot;+
      &quot;&lt;p class='verdict &quot;+(isChurn?&quot;neg&quot;:&quot;pos&quot;)+&quot;'&gt;Churn: &quot;+best+&quot;&lt;/p&gt;&quot;+
      &quot;&lt;p class='conf'&gt;Confidence &quot;+conf+&quot;% &amp;middot; decided purely from the context rows above.&lt;/p&gt;&quot;+
      &quot;&lt;div class='bars'&gt;&quot;+bars+&quot;&lt;/div&gt;&quot;+
      &quot;&lt;div class='neighbors'&gt;&quot;+neighborLine(top)+&quot;&lt;/div&gt;&quot;+
      &quot;&lt;div class='explain'&gt;In TabFM, this maps to &lt;b&gt;predict_proba()&lt;/b&gt;. No weights were updated &amp;mdash; &quot;+
      &quot;the answer is read from the context in a single forward pass.&lt;/div&gt;&quot;;
    reveal();
  }

  function showRegression(est, top){
    $(&quot;result&quot;).innerHTML =
      &quot;&lt;div class='rlabel'&gt;Prediction &amp;middot; estimated value&lt;/div&gt;&quot;+
      &quot;&lt;p class='verdict num'&gt;&quot;+fmtMoney(est)+&quot;&lt;/p&gt;&quot;+
      &quot;&lt;p class='conf'&gt;Estimated from the &quot;+top.length+&quot; nearest context rows, weighted by similarity.&lt;/p&gt;&quot;+
      &quot;&lt;div class='neighbors'&gt;&quot;+neighborLine(top)+&quot;&lt;/div&gt;&quot;+
      &quot;&lt;div class='explain'&gt;In TabFM, this maps to &lt;b&gt;reg.predict()&lt;/b&gt;. No per-dataset training ran &amp;mdash; &quot;+
      &quot;the estimate comes from the context in one forward pass.&lt;/div&gt;&quot;;
    reveal();
  }

  function reveal(){
    var r = $(&quot;result&quot;);
    r.className = &quot;result show&quot;;
    setTimeout(function(){
      r.querySelectorAll(&quot;.bar-fill&quot;).forEach(function(f){
        f.style.width = f.getAttribute(&quot;data-pct&quot;)+&quot;%&quot;;
      });
    },40);
    $(&quot;runNote&quot;).textContent = &quot;Try editing the context, then predict again.&quot;;
    postSize();
  }

  // ---- wiring ----
  $(&quot;tabs&quot;).addEventListener(&quot;click&quot;, function(e){
    var b = e.target.closest(&quot;.tab&quot;); if(!b) return;
    $(&quot;tabs&quot;).querySelectorAll(&quot;.tab&quot;).forEach(function(x){x.classList.remove(&quot;on&quot;);});
    b.classList.add(&quot;on&quot;);
    loadTask(b.getAttribute(&quot;data-task&quot;));
    postSize();
  });
  $(&quot;addRow&quot;).addEventListener(&quot;click&quot;, function(){
    var t = TASKS[state.task];
    var nr = clone(state.rows[state.rows.length-1] || t.rows[0]);
    state.rows.push(nr);
    renderTable();
    postSize();
  });
  $(&quot;resetRows&quot;).addEventListener(&quot;click&quot;, function(){
    state.rows = clone(TASKS[state.task].rows);
    renderTable();
    postSize();
  });
  $(&quot;runBtn&quot;).addEventListener(&quot;click&quot;, predict);

  // ---- auto-resize for WordPress iframe embed ----
  function postSize(){
    var h = document.body.scrollHeight;
    if(window.parent){ window.parent.postMessage({tabfmHeight:h}, &quot;*&quot;); }
  }
  window.addEventListener(&quot;load&quot;, postSize);
  window.addEventListener(&quot;resize&quot;, postSize);
  var ro = window.ResizeObserver ? new ResizeObserver(postSize) : null;
  if(ro) ro.observe(document.body);

  loadTask(&quot;churn&quot;);
  setTimeout(postSize, 60);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
<script>
(function(){
  var f = document.getElementById("tabfm-playground");
  window.addEventListener("message", function(e){
    if(e && e.data && typeof e.data.tabfmHeight === "number"){
      f.style.height = (e.data.tabfmHeight + 40) + "px";
    }
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the<strong> <a href="https://github.com/google-research/tabfm" target="_blank" rel="noreferrer noopener">Repo</a> </strong>and<strong> <a href="https://research.google/blog/introducing-tabfm-a-zero-shot-foundation-model-for-tabular-data/" target="_blank" rel="noreferrer noopener">Technical details</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>



<p class="wp-block-paragraph"></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/01/google-ai-introduces-tabfm-a-hybrid-attention-tabular-foundation-model-for-zero-shot-classification-and-regression/">Google AI Introduces TabFM: A Hybrid-Attention Tabular Foundation Model for Zero-Shot Classification and Regression</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80789</post-id>
	</item>
		<item>
		<title>CUP (Common Useful Python): Building Reliable Python Workflows with Baidu&#8217;s Utility Toolkit</title>
		<link>https://www.marktechpost.com/2026/06/30/cup-common-useful-python-building-reliable-python-workflows-with-baidus-utility-toolkit/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Wed, 01 Jul 2026 06:03:21 +0000</pubDate>
				<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=80786</guid>

					<description><![CDATA[<p>In this tutorial, we explore CUP, Baidu's Common Useful Python library, as a practical utility toolkit for stronger Python workflows. We install it in a Colab-friendly environment and walk its subsystems step by step. We cover logging, decorators, nested configuration, caching, ID generation, thread pools, scheduling, and Linux resource monitoring. Along the way, we connect each module to real tasks like automation, concurrency, and reliability checks.</p>
<p>The post <a href="https://www.marktechpost.com/2026/06/30/cup-common-useful-python-building-reliable-python-workflows-with-baidus-utility-toolkit/">CUP (Common Useful Python): Building Reliable Python Workflows with Baidu&#8217;s Utility Toolkit</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this tutorial, we explore<a href="https://github.com/baidu/CUP"> <strong>CUP</strong></a>, Baidu’s Common Useful Python library, as a practical utility toolkit for building stronger Python workflows. We begin by setting up the library in a Colab-friendly environment and then move through its major subsystems step by step, including logging, decorators, nested configuration, caching, ID generation, thread pools, interruptible threads, delayed execution, time utilities, Linux resource monitoring, file locking, networking helpers, object storage interfaces, type maps, and built-in testing assertions. As we progress, we do not just call functions at random; we observe how each module fits into real-world development tasks such as monitoring, automation, concurrency, configuration management, and reliability checks.</p>



<h2 id="h-cup-setup-and-logging" class="wp-block-heading"><strong>CUP Setup and Logging</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import os
import sys
import time
import threading
import tempfile
import datetime
import subprocess
def banner(title):
   line = "=" * 70
   print("\n" + line + "\n" + title + "\n" + line)
def skip(exc):
   """Report a gracefully-skipped section without aborting the notebook."""
   print("   [skipped — {}: {}]".format(type(exc).__name__, exc))
banner("0. SETUP  (install + cup.platforms + cup.version)")
subprocess.run(
   [sys.executable, "-m", "pip", "install", "-q", "cup", "pytz"],
   check=False,
)
import cup
ver = getattr(cup, "__version__", None)
if ver is None:
   try:
       from cup import version as _v
       ver = getattr(_v, "VERSION", None) or getattr(_v, "__version__", "unknown")
   except Exception:
       ver = "unknown"
print("CUP version :", ver)
print("Python      :", sys.version.split()[0])
try:
   from cup import platforms
   print("is_linux    :", platforms.is_linux())
   print("is_mac      :", platforms.is_mac())
   print("is_windows  :", platforms.is_windows())
   print("is_py3      :", platforms.is_py3())
except Exception as e:
   skip(e)
banner("1. LOGGING  (cup.log)")
LOGFILE = os.path.join(tempfile.gettempdir(), "cup_tutorial.log")
try:
   from cup import log
   log.init_comlog(
       "cup_tutorial",
       log.INFO,
       LOGFILE,
       log.ROTATION,
       10 * 1024 * 1024,
       True,
       False,
   )
   log.info("hello from cup.log — written to file AND stdout")
   log.warning("a warning line")
   log.info_if(2 > 1, "info_if(True)  -> emitted")
   log.info_if(1 > 2, "info_if(False) -> you will NOT see this")
   log.setloglevel(log.DEBUG)
   log.debug("debug visible after setloglevel(DEBUG)")
   try:
       with open(LOGFILE) as fh:
           last = [ln for ln in fh.read().splitlines() if ln.strip()][-1]
       parsed = log.parse(last)
       print("parsed last log line ->")
       for k in ("loglevel", "date", "time", "pid", "srcline", "msg"):
           if isinstance(parsed, dict) and k in parsed:
               print("   {:8}: {}".format(k, parsed[k]))
   except Exception as e:
       skip(e)
except Exception as e:
   skip(e)
</code></pre></div></div>



<p class="wp-block-paragraph">We begin by setting up the CUP tutorial environment and installing the required packages directly from Python. We define helper functions that keep the notebook readable and allow failed sections to be skipped safely. We then explore CUP version details, platform checks, and structured logging to understand the library&#8217;s foundation.</p>



<h2 class="wp-block-heading"><strong>Decorators and Nested Config</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">banner("2. DECORATORS  (cup.decorators)")
try:
   from cup import decorators
   @decorators.Singleton
   class AppConfig(object):
       def __init__(self):
           self.created_at = time.time()
   a, b = AppConfig(), AppConfig()
   print("Singleton: a is b ->", a is b, "(same created_at:",
         a.created_at == b.created_at, ")")
   @decorators.TraceUsedTime(
       b_print_stdout=True,
       enter_msg="event_id=0xABCDE enter",
       leave_msg="event_id=0xABCDE leave",
   )
   def heavy_compute():
       time.sleep(0.2)
       return sum(range(200000))
   print("heavy_compute() =", heavy_compute())
   @decorators.needlinux
   def linux_only():
       return "this body is allowed to run on Linux"
   print("needlinux ->", linux_only())
except Exception as e:
   skip(e)
banner("3. RICH NESTED CONFIG  (cup.util.conf)")
CONF_PATH = os.path.join(tempfile.gettempdir(), "cup_demo.conf")
CONF_TEXT = """\
# ---- global scalars (layer 0) ----
host: abc.com
port: 12345
debug: false
[monitor]
enabled: true
interval: 60
regex: sshd
[.thresholds]
cpu_max: 90
mem_max: 80
[..actions]
on_breach: alert
[storage]
@path: /data/disk1
@path: /data/disk2
@path: /data/disk3
"""
try:
   from cup.util import conf
   with open(CONF_PATH, "w") as fh:
       fh.write(CONF_TEXT)
   cfg = conf.Configure2Dict(CONF_PATH, separator=":").get_dict()
   print("host                          :", cfg["host"])
   print("port                          :", cfg["port"])
   print("monitor.enabled               :", cfg["monitor"]["enabled"])
   print("monitor.regex                 :", cfg["monitor"]["regex"])
   print("monitor.thresholds.cpu_max    :", cfg["monitor"]["thresholds"]["cpu_max"])
   print("monitor.thresholds.actions    :",
         cfg["monitor"]["thresholds"]["actions"]["on_breach"])
   print("storage.path (repeated @ -> list):", list(cfg["storage"]["path"]))
   cfg["port"] = "10085"
   cfg["monitor"]["thresholds"]["actions"]["on_breach"] = "restart"
   NEW_PATH = CONF_PATH + ".new"
   conf.Dict2Configure(cfg, separator=":").write_conf(NEW_PATH)
   re_read = conf.Configure2Dict(NEW_PATH, separator=":").get_dict()
   print("round-trip port               :", re_read["port"], "(was 12345)")
   print("round-trip on_breach          :",
         re_read["monitor"]["thresholds"]["actions"]["on_breach"], "(was alert)")
except Exception as e:
   skip(e)
</code></pre></div></div>



<p class="wp-block-paragraph">We move on to CUP decorators and see how they help us create single-instance classes, track execution time, and protect Linux-only functions. We then work with CUP’s rich configuration system and load a nested configuration file with sections, child sections, and repeated values. We also update the configuration and write it back to disk to confirm that the read-modify-write flow works correctly.</p>



<h2 class="wp-block-heading"><strong>Caching, IDs, Thread Pools</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">banner("4. IN-MEMORY KV CACHE  (cup.cache)")
try:
   from cup import cache
   kv = cache.KVCache(name="demo")
   kv.set({"user:1": "alice", "user:2": "bob"}, expire_sec=2)
   kv.set({"config:flag": "on"}, expire_sec=None)
   print("size after sets         :", kv.size())
   print("get user:1              :", kv.get("user:1"))
   print("get missing key         :", kv.get("nope"))
   print("sleeping 2.2s to let the 2s-TTL keys expire ...")
   time.sleep(2.2)
   print("get user:1 (expired)    :", kv.get("user:1"))
   print("get config:flag (eternal):", kv.get("config:flag"))
   reclaimed = kv.pop_n_expired(0)
   print("pop_n_expired reclaimed :", list(reclaimed.keys()) if reclaimed else [])
except Exception as e:
   skip(e)
banner("5. UNIQUE ID GENERATION  (cup.services.generator)")
try:
   from cup.services import generator
   gman = generator.CGeneratorMan()
   print("uniqname                :", gman.get_uniqname())
   print("next_uniq_num           :", gman.get_next_uniq_num())
   print("next_uniq_num (again)   :", gman.get_next_uniq_num(), "(monotonic)")
   if hasattr(gman, "get_uuid"):
       try:
           print("get_uuid                :", gman.get_uuid())
       except Exception as e:
           skip(e)
   if hasattr(gman, "get_random_str"):
       try:
           print("get_random_str(16)      :", gman.get_random_str(16))
       except Exception as e:
           skip(e)
   print("singleton check         :", generator.CGeneratorMan() is gman)
   try:
       cyc = generator.CycleIDGenerator("127.0.0.1", 8080)
       i1, i2 = cyc.next_id(), cyc.next_id()
       print("CycleIDGenerator id #1  :", i1)
       print("CycleIDGenerator id #2  :", i2, "(incremented)")
       print("id #1 as hex            :", generator.CycleIDGenerator.id2_hexstring(i1))
   except Exception as e:
       skip(e)
except Exception as e:
   skip(e)
banner("6. THREAD POOL  (cup.services.threadpool)")
try:
   from cup.services import threadpool
   pool = threadpool.ThreadPool(minthreads=2, maxthreads=4, name="demo-pool")
   pool.start()
   results, rlock = [], threading.Lock()
   def square(n):
       time.sleep(0.03)
       with rlock:
           results.append(n * n)
       return n * n
   for i in range(8):
       pool.add_1job(square, i)
   callback_log = []
   def on_done(ok, result):
       callback_log.append((ok, result))
   pool.add_1job_with_callback(on_done, square, 100)
   def will_fail():
       raise RuntimeError("boom inside worker")
   pool.add_1job_with_callback(on_done, will_fail)
   time.sleep(0.5)
   print("live stats              :", pool.get_stats())
   pool.stop()
   print("squares collected       :", sorted(results))
   print("callback results        :", callback_log)
except Exception as e:
   skip(e)
</code></pre></div></div>



<p class="wp-block-paragraph">We use CUP’s in-memory cache to store key-value pairs with temporary and permanent lifetimes. We then generate unique names, counters, UUID-style values, random strings, and cycling IDs for distributed-style identifiers. We also create a thread pool, submit jobs, collect results, and observe callback behavior for both successful and failed tasks.</p>



<h2 class="wp-block-heading"><strong>Threads, Scheduling, Time Utilities</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">banner("7. INTERRUPTIBLE THREADS + RW LOCK  (cup.thread)")
try:
   from cup import thread as cupthread
   rw = cupthread.RWLock()
   rw.acquire_readlock()
   rw.acquire_readlock()
   print("acquired 2 read locks concurrently")
   rw.release_readlock()
   rw.release_readlock()
   rw.acquire_writelock()
   print("acquired exclusive write lock")
   rw.release_writelock()
   stop_flag = {"running": True}
   def busy_loop():
       while stop_flag["running"]:
           time.sleep(0.05)
   t = cupthread.CupThread(target=busy_loop)
   t.daemon = True
   t.start()
   print("worker python tid       :", t.get_my_tid())
   stop_flag["running"] = False
   ok = t.terminate()
   print("CupThread.terminate ->  :", ok)
except Exception as e:
   skip(e)
banner("8. DELAY / CRON EXECUTOR  (cup.services.executor)")
try:
   from cup.services import executor
   URGENCY = getattr(executor, "URGENCY_NORMAL", 0)
   svc = executor.ExecutionService(
       delay_exe_thdnum=2, queue_exec_thdnum=2, name="exec-demo"
   )
   svc.start()
   fired = []
   def task(tag):
       fired.append((tag, round(time.time(), 3)))
   svc.delay_exec(0.3, task, URGENCY, "delayed-0.3s")
   svc.queue_exec(task, URGENCY, "queued-now")
   time.sleep(0.8)
   svc.stop()
   print("fired (tag, ts)         :", fired)
   try:
       import pytz
       import hashlib
       tz = pytz.timezone("Asia/Shanghai")
       timer = {"minute": [0, 30], "hour": None,
                "weekday": None, "monthday": None, "month": None}
       task_uuid = hashlib.md5(b"demo-cron").hexdigest()
       ctask = executor.CronTask("demo-cron", tz, timer, task_uuid, task, "cron-arg")
       print("cron next_schedtime     :", ctask.next_schedtime())
       print("cron 32-byte taskid     :", ctask.taskid())
   except Exception as e:
       skip(e)
except Exception as e:
   skip(e)
banner("9. TIME HELPERS  (cup.timeplus)")
try:
   from cup import timeplus
   import pytz
   print("get_str_now (default)   :", timeplus.get_str_now())
   print("get_str_now (custom fmt):", timeplus.get_str_now("%Y/%m/%d %H:%M:%S"))
   tp = timeplus.TimePlus(pytz.timezone("Asia/Shanghai"))
   now_utc = datetime.datetime.utcnow()
   print("naive utc now           :", now_utc)
   print("-> Shanghai local       :", tp.utc2local(now_utc))
except Exception as e:
   skip(e)
</code></pre></div></div>



<p class="wp-block-paragraph">We explore CUP’s thread utilities by using a reader-writer lock and an interruptible thread. We then run delayed and queued tasks through the execution service to understand simple scheduling behavior. We also create a cron-style task and inspect its next scheduled run time without waiting for the real clock to tick.</p>



<h2 class="wp-block-heading"><strong>System Resources and Storage</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">banner("10. SYSTEM RESOURCES  (cup.res.linux)")
try:
   from cup.res import linux as reslinux
   try:
       print("cpu cores               :", reslinux.get_cpu_nums())
   except Exception as e:
       skip(e)
   try:
       cpu = reslinux.get_cpu_usage(intvl_in_sec=1)
       print("cpu usage sample        :", cpu, "| usr=", getattr(cpu, "usr", "?"))
   except Exception as e:
       skip(e)
   try:
       mem = reslinux.get_meminfo()
       gb = 1024.0 ** 3
       print("mem total / available   : {:.2f} GB / {:.2f} GB".format(
           mem.total / gb, mem.available / gb))
   except Exception as e:
       skip(e)
   try:
       print("kernel version          :", reslinux.get_kernel_version())
   except Exception as e:
       skip(e)
   try:
       all_pids = reslinux.pids()
       print("live process count      :", len(all_pids))
   except Exception as e:
       skip(e)
   try:
       me = reslinux.Process(os.getpid())
       name = me.get_process_name() if hasattr(me, "get_process_name") else "?"
       status = me.get_process_status() if hasattr(me, "get_process_status") else "?"
       print("this process name/status:", name, "/", status)
   except Exception as e:
       skip(e)
except Exception as e:
   skip(e)
banner("11. FILE UTILITIES  (cup.exfile)")
try:
   from cup import exfile
   lock_path = os.path.join(tempfile.gettempdir(), "cup_demo.lock")
   flock = exfile.LockFile(lock_path)
   got = flock.lock(blocking=True)
   print("acquired file lock      :", got)
   print("lock file path          :", flock.filepath())
   flock.unlock()
   print("released file lock      : True")
except Exception as e:
   skip(e)
banner("12. NETWORKING HELPERS  (cup.net)")
try:
   from cup import net as cupnet
   try:
       print("local hostname          :", cupnet.get_local_hostname())
   except Exception as e:
       skip(e)
   try:
       print("host ip                 :", cupnet.get_hostip())
   except Exception as e:
       skip(e)
   try:
       print("local port 9999 free?   :", cupnet.localport_free(9999))
   except Exception as e:
       skip(e)
except Exception as e:
   skip(e)
banner("13. UNIFIED OBJECT STORAGE  (cup.storage.obj)")
try:
   from cup.storage import obj as cupobj
   print("available backends      :",
         [n for n in dir(cupobj) if n.endswith("ObjectSystem")])
   los = None
   for ctor in (lambda: cupobj.LocalObjectSystem(tempfile.gettempdir()),
                lambda: cupobj.LocalObjectSystem({}),
                lambda: cupobj.LocalObjectSystem()):
       try:
           los = ctor()
           break
       except Exception:
           continue
   if los is None:
       print("   LocalObjectSystem ctor varies by version — see docs for "
             "your release's exact signature.")
   else:
       print("LocalObjectSystem ready :", type(los).__name__,
             "(put/get/head/delete available)")
except Exception as e:
   skip(e)
</code></pre></div></div>



<p class="wp-block-paragraph">We inspect Linux system resources, including CPU cores, CPU usage, memory, kernel version, process count, and current process status. We then use CUP’s file utility module to acquire and release an advisory lock for safe single-instance execution. We also test networking helpers and probe the object storage interface to see which storage backends are available.</p>



<h2 class="wp-block-heading"><strong>Type Maps and Assertions</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">banner("14. BIDIRECTIONAL TYPE MAPS  (cup.flag)")
try:
   from cup import flag
   tman = flag.TypeMan()
   tman.register_types({"LOGIN": 1, "LOGOUT": 2, "HEARTBEAT": 3, "DATA": 4})
   print("LOGIN     -> number     :", tman.getnumber_bykey("LOGIN"))
   print("number 2  -> key        :", tman.getkey_bynumber(2))
   print("all registered keys     :", list(tman.get_key_list()))
except Exception as e:
   skip(e)
banner("15. BUILT-IN TEST ASSERTIONS  (cup.unittest)")
try:
   from cup import unittest as cup_unittest
   cup_unittest.assert_eq(2 + 2, 4)
   cup_unittest.assert_ne(1, 2)
   cup_unittest.assert_true(isinstance([], list))
   cup_unittest.assert_gt(5, 3)
   cup_unittest.assert_eq_one("b", ["a", "b", "c"])
   def raises_value_error():
       raise ValueError("expected")
   cup_unittest.expect_raise(raises_value_error, ValueError)
   print("all passing assertions   : OK")
   try:
       cup_unittest.assert_eq(1, 2, "intentional failure demo")
   except Exception as e:
       print("caught intended failure  :", type(e).__name__)
except Exception as e:
   skip(e)
banner("DONE — you exercised 15 CUP subsystems")
print("""
What you just used, by module:
 cup.log .................. structured, rotating, parseable logging
 cup.decorators ........... Singleton, TraceUsedTime, platform guards
 cup.util.conf ............ nested config with sections + @arrays (round-trip)
 cup.cache ................ TTL KV cache (get() extends lifetime)
 cup.services.generator ... host-unique names, counters, cycling 128-bit IDs
 cup.services.threadpool .. pool with result/failure callbacks + live stats
 cup.thread ............... interruptible CupThread + reader/writer RWLock
 cup.services.executor .... delay_exec / queue_exec + crontab-style CronTask
 cup.timeplus ............. now-formatting + safe tz&lt;->UTC conversion
 cup.res.linux ............ /proc cpu, memory, kernel, process introspection
 cup.exfile ............... advisory single-instance file locking
 cup.net .................. hostname / ip / free-port helpers
 cup.storage.obj .......... one API over Local / FTP / S3 / AFS backends
 cup.flag ................. bidirectional key&lt;->number type tables
 cup.unittest ............. assertion toolkit that plays well with try/except
Next steps: API reference at https://cup.iobusy.com  •  source at
https://github.com/baidu/CUP (browse src/cup/ and the cup_test/ suite).
""")
</code></pre></div></div>



<p class="wp-block-paragraph">We use CUP’s flag utilities to create bidirectional mappings between message names and numeric codes. We then run CUP’s built-in assertion helpers to validate equality, inequality, truth checks, comparisons, membership, and expected exceptions. We finish the tutorial by summarizing every CUP subsystem we exercise and connecting each one to a practical development use case.</p>



<h2 class="wp-block-heading"><strong>Conclusion</strong></h2>



<p class="wp-block-paragraph">In conclusion, we completed a broad hands-on tour of CUP and understand how its utilities can support production-style Python applications. We saw how CUP helps us structure logs, manage runtime configuration, cache temporary data, generate unique identifiers, coordinate threads, inspect system resources, protect files with locks, and validate behavior through assertions. It sets us a compact but useful foundation for using CUP in scripts, backend services, automation jobs, and system-level Python projects where reliability, observability, and practical tooling matter.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph">Check out the<strong> <a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Distributed%20Systems/baidu_cup_practical_python_system_utilities_tutorial_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">FULL CODES here</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/06/30/cup-common-useful-python-building-reliable-python-workflows-with-baidus-utility-toolkit/">CUP (Common Useful Python): Building Reliable Python Workflows with Baidu&#8217;s Utility Toolkit</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">80786</post-id>
	</item>
	</channel>
</rss>
