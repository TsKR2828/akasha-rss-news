<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	 xmlns:media="http://search.yahoo.com/mrss/" >

<channel>
	<title>MarkTechPost</title>
	<atom:link href="https://www.marktechpost.com/feed/" rel="self" type="application/rss+xml" />
	<link>https://www.marktechpost.com/</link>
	<description>An Artificial Intelligence News Platform</description>
	<lastBuildDate>Wed, 29 Jul 2026 19:30:36 +0000</lastBuildDate>
	<language>en-US</language>
	<sy:updatePeriod>
	hourly	</sy:updatePeriod>
	<sy:updateFrequency>
	1	</sy:updateFrequency>
	<generator>https://wordpress.org/?v=7.0.2</generator>

<image>
	<url>https://www.marktechpost.com/wp-content/uploads/2022/04/cropped-Favicon-512-x-512-1-1-32x32.png</url>
	<title>MarkTechPost</title>
	<link>https://www.marktechpost.com/</link>
	<width>32</width>
	<height>32</height>
</image> 
<site xmlns="com-wordpress:feed-additions:1">127842392</site>	<item>
		<title>Prompt Engineering vs Loop Engineering vs Graph Engineering: What Changes at Each Layer</title>
		<link>https://www.marktechpost.com/2026/07/29/prompt-engineering-vs-loop-engineering-vs-graph-engineering-what-changes-at-each-layer/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Wed, 29 Jul 2026 19:30:35 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Agents]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Context Engineering]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Generative AI]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Uncategorized]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81422</guid>

					<description><![CDATA[<p>Three terms now compete for the same line in AI engineering job descriptions. Prompt engineering is the established one. Loop engineering entered the AI vocabulary in late 2025 and dominated developer discussion through June 2026. Graph engineering followed roughly six weeks later. They get used interchangeably. Should they be? The three are not competing techniques. [&#8230;]</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/29/prompt-engineering-vs-loop-engineering-vs-graph-engineering-what-changes-at-each-layer/">Prompt Engineering vs Loop Engineering vs Graph Engineering: What Changes at Each Layer</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Three terms now compete for the same line in AI engineering job descriptions. Prompt engineering is the established one. Loop engineering entered the AI vocabulary in late 2025 and dominated developer discussion through June 2026. Graph engineering followed roughly six weeks later.</p>



<p class="wp-block-paragraph">They get used interchangeably. <strong>Should they be?</strong></p>



<p class="wp-block-paragraph">The three are not competing techniques. They are three different <strong>units of control</strong>, stacked. A prompt controls one model response. A loop controls one agent&#8217;s behavior cycle. A graph controls the organization of many agents. Each layer preserves the layer beneath it. A prompt does not disappear once a loop is built around it. it stops being the thing typed by hand.</p>



<p class="wp-block-paragraph">This article separates the three: what gets designed at each layer, what the published claim says about when the higher layers pay for themselves, and where the skepticism is warranted.</p>



<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>One task, three layers</title>
<style>
  :root{
    --bg:#0B1E1E; --panel:#122C2C; --line:#274B4B; --ink:#E8F5F1; --muted:#8FB3AC;
    --prompt:#9BD7FF; --loop:#F2B705; --graph:#3FD9A8; --human:#FF9E7D;
    --mono:ui-monospace,SFMono-Regular,"JetBrains Mono",Menlo,Consolas,monospace;
    --sans:-apple-system,BlinkMacSystemFont,"Segoe UI",Inter,Roboto,Helvetica,Arial,sans-serif;
  }
  *{box-sizing:border-box}
  html,body{margin:0;padding:0;background:transparent}
  #plg-app{background:var(--bg); color:var(--ink); font-family:var(--sans);
    border:1px solid var(--line); border-radius:14px; overflow:hidden; max-width:920px; margin:0 auto}

  .hd{padding:20px 22px 16px; border-bottom:1px solid var(--line); background:linear-gradient(180deg,#143333 0%,#0B1E1E 100%)}
  .eyebrow{font-family:var(--mono); font-size:11px; letter-spacing:.16em; text-transform:uppercase; color:var(--muted); margin:0 0 8px}
  .hd h2{margin:0; font-size:clamp(19px,2.6vw,25px); line-height:1.2; letter-spacing:-.02em; font-weight:700}
  .hd p{margin:8px 0 0; color:var(--muted); font-size:14px; line-height:1.55; max-width:64ch}

  .taskbar{display:flex; align-items:center; gap:14px; flex-wrap:wrap;
    padding:14px 22px; background:#0E2525; border-bottom:1px solid var(--line)}
  .taskbar .tl{font-family:var(--mono); font-size:10px; letter-spacing:.14em; text-transform:uppercase; color:var(--muted)}
  .taskbar .tv{font-size:14.5px; font-weight:600; flex:1; min-width:210px}
  .btn{appearance:none; cursor:pointer; border:1px solid var(--line); background:#0B1E1E; color:var(--ink);
    font-family:var(--mono); font-size:11.5px; letter-spacing:.06em; text-transform:uppercase;
    padding:9px 15px; border-radius:7px; transition:border-color .18s, background .18s}
  .btn:hover{border-color:var(--graph); background:#123232}
  .btn:focus-visible{outline:2px solid var(--graph); outline-offset:2px}
  .btn.primary{border-color:var(--graph); color:var(--graph)}
  .btn[disabled]{opacity:.45; cursor:not-allowed}

  .lanes{display:grid; grid-template-columns:repeat(3,1fr); background:var(--panel)}
  .lane{padding:0 0 14px; border-right:1px solid var(--line)}
  .lane:last-child{border-right:0}
  .lh{padding:13px 15px 11px; border-bottom:1px solid var(--line)}
  .lh .k{font-family:var(--mono); font-size:10px; letter-spacing:.13em; text-transform:uppercase; margin:0 0 4px}
  .lane.p .lh .k{color:var(--prompt)} .lane.l .lh .k{color:var(--loop)} .lane.g .lh .k{color:var(--graph)}
  .lh h3{margin:0; font-size:15px; letter-spacing:-.01em; font-weight:650}
  .lh .sub{margin:4px 0 0; font-size:12px; line-height:1.45; color:var(--muted)}

  .steps{list-style:none; margin:0; padding:12px 15px 0; min-height:250px}
  .steps li{display:flex; gap:9px; align-items:flex-start; margin-bottom:9px;
    font-size:12.5px; line-height:1.45; opacity:0; transform:translateY(5px);
    transition:opacity .3s ease, transform .3s ease}
  .steps li.show{opacity:1; transform:none}
  .steps li .dot{flex:0 0 auto; width:7px; height:7px; border-radius:50%; margin-top:5px; background:var(--line)}
  .lane.p .steps li.show .dot{background:var(--prompt)}
  .lane.l .steps li.show .dot{background:var(--loop)}
  .lane.g .steps li.show .dot{background:var(--graph)}
  .steps li.hum .dot{background:var(--human) !important; box-shadow:0 0 0 3px rgba(255,158,125,.16)}
  .steps li .tx{color:var(--muted)}
  .steps li.hum .tx{color:var(--ink)}
  .steps li .tx b{color:var(--ink); font-weight:600}
  .steps li .tx em{font-style:normal; color:var(--human); font-family:var(--mono); font-size:10px;
    letter-spacing:.1em; text-transform:uppercase; display:block; margin-top:2px}

  .lstat{margin:8px 15px 0; padding-top:11px; border-top:1px dashed var(--line);
    display:flex; gap:14px; flex-wrap:wrap; font-family:var(--mono); font-size:10.5px; color:var(--muted)}
  .lstat b{color:var(--ink)}

  .cmp{border-top:1px solid var(--line); background:#0E2525; padding:16px 22px 18px}
  .cmp h4{margin:0 0 11px; font-family:var(--mono); font-size:10px; letter-spacing:.13em;
    text-transform:uppercase; color:var(--muted); font-weight:500}
  table{width:100%; border-collapse:collapse; font-size:13px}
  th,td{text-align:left; padding:9px 10px; vertical-align:top; line-height:1.45; border-bottom:1px solid #1c3a3a}
  thead th{font-family:var(--mono); font-size:10px; letter-spacing:.1em; text-transform:uppercase; color:var(--muted); font-weight:500}
  tbody th{font-family:var(--mono); font-size:10.5px; letter-spacing:.06em; color:var(--muted); font-weight:500; width:150px}
  td{color:var(--ink)}
  .c-p{color:var(--prompt)} .c-l{color:var(--loop)} .c-g{color:var(--graph)}
  tbody tr:last-child th, tbody tr:last-child td{border-bottom:0}

  .ft{padding:11px 22px; border-top:1px solid var(--line); background:#0E2525;
      display:flex; justify-content:space-between; gap:10px; flex-wrap:wrap;
      font-family:var(--mono); font-size:10.5px; letter-spacing:.05em; color:var(--muted)}
  .ft b{color:var(--graph); font-weight:600}

  @media (max-width:760px){
    .hd{padding:16px 15px 13px}
    .taskbar{padding:13px 15px}
    .lanes{grid-template-columns:1fr}
    .lane{border-right:0; border-bottom:1px solid var(--line)}
    .lane:last-child{border-bottom:0}
    .steps{min-height:0; padding-bottom:4px}
    .cmp{padding:14px 15px 16px; overflow-x:auto}
    table{min-width:520px}
    .ft{padding:10px 15px}
  }
  @media (prefers-reduced-motion:reduce){ .steps li{transition:none} }
</style>
</head>
<body>
<div id="plg-app">
  <div class="hd">
    <p class="eyebrow">Interactive explainer &middot; 01</p>
    <h2>One task, three layers &mdash; watch what changes</h2>
    <p>The three terms are not competing techniques. They are three different units of control. Here is the same job handled at each layer, step by step. Orange dots mark the moments a human is required.</p>
  </div>

  <div class="taskbar">
    <span class="tl">Task</span>
    <span class="tv">Fix the failing tests in the auth module, then open a pull request.</span>
    <button class="btn primary" id="run">Run all three</button>
    <button class="btn" id="reset">Reset</button>
  </div>

  <div class="lanes">
    <div class="lane p">
      <div class="lh">
        <p class="k">Layer 1</p>
        <h3>Prompt engineering</h3>
        <p class="sub">You control one model response. You are the loop.</p>
      </div>
      <ul class="steps" id="sp"></ul>
      <div class="lstat">Your turns <b id="hp">0</b> &middot; Model calls <b id="mp">0</b></div>
    </div>

    <div class="lane l">
      <div class="lh">
        <p class="k">Layer 2</p>
        <h3>Loop engineering</h3>
        <p class="sub">You control one agent&#8217;s cycle. The loop does the prompting.</p>
      </div>
      <ul class="steps" id="sl"></ul>
      <div class="lstat">Your turns <b id="hl">0</b> &middot; Model calls <b id="ml">0</b></div>
    </div>

    <div class="lane g">
      <div class="lh">
        <p class="k">Layer 3</p>
        <h3>Graph engineering</h3>
        <p class="sub">You control how many agents are organised.</p>
      </div>
      <ul class="steps" id="sg"></ul>
      <div class="lstat">Your turns <b id="hg">0</b> &middot; Nodes <b id="mg">0</b> &middot; Parallel <b id="pg">0</b></div>
    </div>
  </div>

  <div class="cmp">
    <h4>What actually differs</h4>
    <table>
      <thead>
        <tr><th>&nbsp;</th><th class="c-p">Prompt</th><th class="c-l">Loop</th><th class="c-g">Graph</th></tr>
      </thead>
      <tbody>
        <tr>
          <th>Unit of control</th>
          <td>One model response</td>
          <td>One agent&#8217;s behaviour cycle</td>
          <td>An organisation of agents</td>
        </tr>
        <tr>
          <th>What you write</th>
          <td>Instructions, examples, output format</td>
          <td>Trigger, tools, stop condition, retry budget</td>
          <td>Nodes, edges, shared state, failure routes</td>
        </tr>
        <tr>
          <th>Who says &#8220;again?&#8221;</th>
          <td>A human, every turn</td>
          <td>A verifier the loop calls itself</td>
          <td>A routing rule written in advance</td>
        </tr>
        <tr>
          <th>Where it breaks</th>
          <td>Ambiguous or overstuffed instruction</td>
          <td>It cannot tell done from stuck</td>
          <td>Context never crossed an edge you forgot to draw</td>
        </tr>
        <tr>
          <th>Enough when</th>
          <td>One shot, a person reads the result</td>
          <td>Repetitive, machine-checkable, one domain</td>
          <td>Cross-domain work with parallel branches</td>
        </tr>
      </tbody>
    </table>
  </div>

  <div class="ft">
    <span>Illustrative walkthrough &middot; step counts are not benchmarks</span>
    <span><b>Built by Marktechpost</b></span>
  </div>
</div>

<script>
(function(){
  var PROMPT = [
    {h:1, t:"<b>You</b> write the prompt: fix the failing auth tests", tag:"you type"},
    {h:0, t:"Model returns a patch for two files"},
    {h:1, t:"<b>You</b> run pytest &mdash; two tests still fail", tag:"you verify"},
    {h:1, t:"<b>You</b> paste the traceback back into the chat", tag:"you re-prompt"},
    {h:0, t:"Model returns a revised patch"},
    {h:1, t:"<b>You</b> run pytest &mdash; green &mdash; and open the PR", tag:"you finish"}
  ];
  var LOOP = [
    {h:0, t:"<b>Trigger</b> fires on schedule at 09:00"},
    {h:0, t:"<b>Act</b> &mdash; reads the CI log, edits the failing files"},
    {h:0, t:"<b>Verify</b> &mdash; runs pytest, two tests still fail"},
    {h:0, t:"<b>Decide</b> &mdash; below the bar, retry with updated state"},
    {h:0, t:"<b>Act</b> &mdash; revises the patch"},
    {h:0, t:"<b>Verify</b> &mdash; pytest green, stop condition met"},
    {h:0, t:"<b>Exit</b> &mdash; opens the PR, writes the state file"},
    {h:1, t:"<b>You</b> review the PR you did not write", tag:"you review"}
  ];
  var GRAPH = [
    {h:0, t:"<b>Plan node</b> splits the task into three branches"},
    {h:0, t:"<b>Parallel</b> &mdash; test analyst, patch writer, security reviewer run at once"},
    {h:0, t:"<b>Join node</b> merges the three branch outputs"},
    {h:0, t:"<b>Validator</b> gate fails on the security branch"},
    {h:0, t:"<b>Retry edge</b> routes work back to the patch writer"},
    {h:0, t:"<b>Validator</b> passes on the second attempt"},
    {h:0, t:"<b>Ship node</b> opens the PR and checkpoints state"},
    {h:1, t:"<b>You</b> review the PR you did not write", tag:"you review"}
  ];

  var timers = [];
  function clearAll(){ timers.forEach(clearTimeout); timers = []; }
  function el(id){ return document.getElementById(id); }

  function build(listId, data){
    var ul = el(listId); ul.innerHTML = "";
    data.forEach(function(s){
      var li = document.createElement("li");
      if(s.h) li.className = "hum";
      li.innerHTML = '<i class="dot"></i><span class="tx">' + s.t + (s.tag ? '<em>' + s.tag + '</em>' : '') + '</span>';
      ul.appendChild(li);
    });
  }

  function reset(){
    clearAll();
    build("sp", PROMPT); build("sl", LOOP); build("sg", GRAPH);
    ["hp","mp","hl","ml","hg","mg","pg"].forEach(function(i){ el(i).textContent = "0"; });
    el("run").disabled = false;
    ping();
  }

  function play(listId, data, pace, counters){
    var ul = el(listId), items = ul.querySelectorAll("li");
    var hum = 0, calls = 0;
    data.forEach(function(s, i){
      timers.push(setTimeout(function(){
        items[i].classList.add("show");
        if(s.h) hum++; else calls++;
        el(counters.h).textContent = String(hum);
        if(counters.m) el(counters.m).textContent = String(counters.mv ? counters.mv(calls) : calls);
        ping();
      }, pace * (i + 1)));
    });
  }

  el("reset").addEventListener("click", reset);
  el("run").addEventListener("click", function(){
    reset();
    el("run").disabled = true;
    play("sp", PROMPT, 620, {h:"hp", m:"mp"});
    play("sl", LOOP,  620, {h:"hl", m:"ml"});
    play("sg", GRAPH, 620, {h:"hg", m:"mg"});
    timers.push(setTimeout(function(){ el("pg").textContent = "3"; ping(); }, 620 * 2));
    timers.push(setTimeout(function(){ el("run").disabled = false; }, 620 * 9));
  });

  function ping(){
    try{
      var h = document.getElementById("plg-app").offsetHeight + 40;
      parent.postMessage({ plgFrame:"embed1-one-task-three-layers", height:h }, "*");
    }catch(e){}
  }
  window.addEventListener("load", ping);
  window.addEventListener("resize", ping);
  setTimeout(ping, 250);
  reset();
})();
</script>
</body>
</html>




<p class="wp-block-paragraph"></p>



<h2 id="h-the-stack-in-order" class="wp-block-heading"><strong>The stack, in order</strong></h2>



<p class="wp-block-paragraph">Each step in the progression was named in practice before it appeared in vendor documentation.</p>



<ol class="wp-block-list">
<li><strong>Prompt engineering</strong> covers writing and structuring the instruction for a single call. <a href="https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents">Anthropic&#8217;s guidance</a> is to separate a system prompt into labeled sections — background information, instructions, tool guidance, output description — delineated with XML tags or Markdown headers. The recommendation is to supply the minimal set of information that fully specifies the expected behavior. Minimal does not mean short.</li>



<li><strong>Context engineering</strong> came next. Anthropic describes it as the natural progression of prompt engineering. The question moves from finding the right words to deciding what configuration of tokens belongs in the window at all. Context is a finite resource, and the engineering problem is optimizing the utility of those tokens against model constraints.</li>



<li><strong>Harness engineering</strong> covers the environment a single agent runs inside: files, tools, memory, feedback.</li>



<li><strong>Loop engineering</strong> sits one floor above the harness. A June 2026 arXiv paper on agentic AI in building engineering, <a href="https://arxiv.org/abs/2606.25139">Buildrix</a>, sets out the same four-step progression explicitly — prompt, then context, then harness, then loop — with the final layer defining how a system repeatedly observes, acts, verifies and recovers.</li>



<li><strong>Graph engineering</strong> is the newest label and the least settled. <a href="https://www.truefoundry.com/blog/graph-engineering-enterprise-guide">One enterprise writeup</a> notes that the term&#8217;s provenance is unresolved and that it collides with an older knowledge-graph usage of the same word. The underlying practice, graph-based orchestration, has a documented lineage in multi-agent systems research.</li>
</ol>



<h3 id="h-layer-1-prompt-engineering" class="wp-block-heading"><strong>Layer 1: Prompt Engineering</strong></h3>



<p class="wp-block-paragraph">The defining assumption is that a human is present at every iteration. A prompt is written, the model responds, the output is judged, the prompt is revised.</p>



<p class="wp-block-paragraph">That assumption is what breaks. High volume. Multi-step tasks. No human available to grade the output. Results that feed the next step automatically. Any one of these, and the prompt alone stops being sufficient.</p>



<p class="wp-block-paragraph">Nothing about the prompt got worse. The surrounding conditions changed.</p>



<p class="wp-block-paragraph">Prompt engineering also does not vanish inside the higher layers. Anthropic&#8217;s <a href="https://www.anthropic.com/engineering/multi-agent-research-system">multi-agent research writeup</a> reports that prompt engineering was the <strong>primary lever</strong> for fixing coordination failures. Early versions spawned 50 subagents for simple queries, and the fix was prompting rather than topology.</p>



<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Prompt engineering, explained</title>
<style>
  :root{
    --bg:#0B1E1E; --panel:#122C2C; --line:#274B4B; --ink:#E8F5F1; --muted:#8FB3AC;
    --prompt:#9BD7FF; --human:#FF9E7D; --graph:#3FD9A8;
    --mono:ui-monospace,SFMono-Regular,"JetBrains Mono",Menlo,Consolas,monospace;
    --sans:-apple-system,BlinkMacSystemFont,"Segoe UI",Inter,Roboto,Helvetica,Arial,sans-serif;
  }
  *{box-sizing:border-box}
  html,body{margin:0;padding:0;background:transparent}
  #plg-app{background:var(--bg); color:var(--ink); font-family:var(--sans);
    border:1px solid var(--line); border-radius:14px; overflow:hidden; max-width:920px; margin:0 auto}

  .hd{padding:20px 22px 16px; border-bottom:1px solid var(--line); background:linear-gradient(180deg,#143333 0%,#0B1E1E 100%)}
  .eyebrow{font-family:var(--mono); font-size:11px; letter-spacing:.16em; text-transform:uppercase; color:var(--prompt); margin:0 0 8px}
  .hd h2{margin:0; font-size:clamp(19px,2.6vw,25px); line-height:1.2; letter-spacing:-.02em; font-weight:700}
  .hd .def{margin:10px 0 0; font-size:14.5px; line-height:1.6; color:var(--muted); max-width:66ch}
  .hd .def b{color:var(--ink)}

  .flowwrap{padding:16px 22px 10px; background:var(--panel); border-bottom:1px solid var(--line)}
  .scroller{overflow-x:auto; -webkit-overflow-scrolling:touch}
  svg{width:100%; height:auto; display:block}
  .lb{font-family:var(--mono); font-size:11px; fill:var(--muted)}
  .lbb{font-family:var(--sans); font-size:13px; fill:var(--ink); font-weight:600}
  .bx{fill:#0E2525; stroke:var(--line); stroke-width:1.3}
  .swipe{display:none; margin:6px 0 0; font-family:var(--mono); font-size:10px;
    letter-spacing:.1em; text-transform:uppercase; color:var(--muted)}
  .cap{margin:2px 0 0; font-size:12.5px; line-height:1.5; color:var(--muted); text-align:center}

  .split{display:grid; grid-template-columns:1fr 1fr; background:var(--panel)}
  .col{padding:16px 20px}
  .col + .col{border-left:1px solid var(--line); background:#0E2525}
  .col h3{margin:0 0 4px; font-family:var(--mono); font-size:10.5px; letter-spacing:.13em;
    text-transform:uppercase; color:var(--muted); font-weight:500}
  .col .sub{margin:0 0 12px; font-size:12.5px; line-height:1.5; color:var(--muted)}

  .blk{display:block; width:100%; text-align:left; appearance:none; cursor:pointer;
    border:1px solid var(--line); background:#0B1E1E; color:var(--ink); border-radius:8px;
    padding:10px 12px; margin-bottom:8px; transition:border-color .18s, background .18s}
  .blk:hover{border-color:var(--prompt); background:#0f2730}
  .blk:focus-visible{outline:2px solid var(--prompt); outline-offset:2px}
  .blk.on{border-color:var(--prompt); background:#0f2a33}
  .blk .tag{font-family:var(--mono); font-size:11px; color:var(--prompt); letter-spacing:.02em}
  .blk .ln{display:block; margin-top:4px; font-size:12.5px; line-height:1.45; color:var(--muted)}

  #expl h4{margin:0 0 7px; font-size:15.5px; line-height:1.35; letter-spacing:-.01em; font-weight:650}
  #expl p{margin:0 0 10px; font-size:13.5px; line-height:1.6; color:var(--muted)}
  #expl p b{color:var(--ink)}
  #expl .quote{border-left:2px solid var(--prompt); padding-left:12px; font-size:13px; line-height:1.6; color:var(--muted)}

  .brk{border-top:1px solid var(--line); background:var(--panel); padding:16px 22px 18px}
  .brk h3{margin:0 0 4px; font-family:var(--mono); font-size:10.5px; letter-spacing:.13em;
    text-transform:uppercase; color:var(--muted); font-weight:500}
  .brk .sub{margin:0 0 12px; font-size:13.5px; line-height:1.55; color:var(--muted); max-width:66ch}
  .brk .sub b{color:var(--ink)}
  .chips{display:flex; gap:8px; flex-wrap:wrap; margin-bottom:12px}
  .chip{appearance:none; cursor:pointer; border:1px solid var(--line); background:#0E2525; color:var(--muted);
    font-family:var(--mono); font-size:11px; letter-spacing:.05em; padding:8px 13px; border-radius:20px;
    transition:border-color .18s, color .18s, background .18s}
  .chip:hover{color:var(--ink); border-color:var(--human)}
  .chip:focus-visible{outline:2px solid var(--human); outline-offset:2px}
  .chip.on{color:var(--human); border-color:var(--human); background:#2a1a14}
  .conseq{border:1px solid var(--line); border-radius:9px; background:#0E2525; padding:13px 15px;
    font-size:13.5px; line-height:1.6; color:var(--muted); min-height:74px}
  .conseq b{color:var(--ink)}
  .conseq .verdict{display:block; margin-top:7px; font-family:var(--mono); font-size:11px;
    letter-spacing:.06em; color:var(--human)}

  .ft{padding:11px 22px; border-top:1px solid var(--line); background:#0E2525;
      display:flex; justify-content:space-between; gap:10px; flex-wrap:wrap;
      font-family:var(--mono); font-size:10.5px; letter-spacing:.05em; color:var(--muted)}
  .ft b{color:var(--graph); font-weight:600}

  @media (max-width:700px){
    .hd{padding:16px 15px 13px}
    .flowwrap,.brk{padding-left:15px; padding-right:15px}
    .split{grid-template-columns:1fr}
    .col + .col{border-left:0; border-top:1px solid var(--line)}
    .col{padding:15px}
    .ft{padding:10px 15px}
    svg{min-width:560px}
    .swipe{display:block}
  }
  @media (prefers-reduced-motion:reduce){ .anim{animation:none !important} }
</style>
</head>
<body>
<div id="plg-app">
  <div class="hd">
    <p class="eyebrow">Interactive explainer &middot; 02 &middot; Layer 1</p>
    <h2>What prompt engineering is</h2>
    <p class="def"><b>Prompt engineering is designing the text of a single call to a model.</b> One input, one forward pass, one output &mdash; and a human who reads the result and decides whether to run it again. That last part is the assumption everything above this layer exists to remove.</p>
  </div>

  <div class="flowwrap">
    <div class="scroller">
      <svg viewBox="0 0 720 150" role="img" aria-label="Prompt enters a model, one response leaves, a human judges it and may rewrite the prompt.">
        <defs>
          <marker id="a1" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
            <path d="M0,0 L8,4 L0,8 z" fill="#9BD7FF"/>
          </marker>
          <marker id="a2" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
            <path d="M0,0 L8,4 L0,8 z" fill="#FF9E7D"/>
          </marker>
        </defs>

        <rect class="bx" x="20" y="30" width="140" height="58" rx="9"/>
        <text class="lbb" x="90" y="55" text-anchor="middle">Prompt</text>
        <text class="lb" x="90" y="73" text-anchor="middle">what you write</text>

        <rect x="255" y="24" width="140" height="70" rx="9" fill="#0E2525" stroke="#9BD7FF" stroke-width="1.6"/>
        <text class="lbb" x="325" y="53" text-anchor="middle">Model</text>
        <text class="lb" x="325" y="71" text-anchor="middle">one forward pass</text>

        <rect class="bx" x="490" y="30" width="140" height="58" rx="9"/>
        <text class="lbb" x="560" y="55" text-anchor="middle">Response</text>
        <text class="lb" x="560" y="73" text-anchor="middle">one output</text>

        <circle cx="680" cy="59" r="22" fill="#2a1a14" stroke="#FF9E7D" stroke-width="1.5"/>
        <text class="lb" x="680" y="63" text-anchor="middle" style="fill:#FF9E7D">you</text>

        <line x1="160" y1="59" x2="247" y2="59" stroke="#9BD7FF" stroke-width="1.6" marker-end="url(#a1)"/>
        <line x1="395" y1="59" x2="482" y2="59" stroke="#9BD7FF" stroke-width="1.6" marker-end="url(#a1)"/>
        <line x1="630" y1="59" x2="654" y2="59" stroke="#FF9E7D" stroke-width="1.6" marker-end="url(#a2)"/>

        <path d="M680,84 C680,128 300,138 90,110" fill="none" stroke="#FF9E7D" stroke-width="1.4"
              stroke-dasharray="5 5" marker-end="url(#a2)"/>
        <text class="lb" x="380" y="140" text-anchor="middle" style="fill:#FF9E7D">you judge it, then rewrite the prompt yourself</text>

        <circle r="5" fill="#9BD7FF" class="anim">
          <animateMotion dur="2.8s" repeatCount="indefinite" path="M160,59 L247,59 M395,59 L482,59"/>
          <animate attributeName="opacity" values="0;1;1;0" dur="2.8s" repeatCount="indefinite"/>
        </circle>
      </svg>
    </div>
    <p class="swipe">Swipe the diagram sideways &rarr;</p>
    <p class="cap">The cycle exists here too &mdash; but you are the one closing it, every single turn.</p>
  </div>

  <div class="split">
    <div class="col">
      <h3>Anatomy of a prompt</h3>
      <p class="sub">Tap a section. Anthropic&#8217;s guidance is to delineate parts with XML tags or Markdown headers rather than writing one undifferentiated block.</p>
      <button class="blk on" data-k="bg"><span class="tag">&lt;background_information&gt;</span><span class="ln">What the model needs to know before it starts</span></button>
      <button class="blk" data-k="ins"><span class="tag">&lt;instructions&gt;</span><span class="ln">The task itself, and the rules for doing it</span></button>
      <button class="blk" data-k="tool"><span class="tag">## Tool guidance</span><span class="ln">Which tool to reach for, and when</span></button>
      <button class="blk" data-k="out"><span class="tag">## Output description</span><span class="ln">The exact shape the answer should take</span></button>
      <button class="blk" data-k="ex"><span class="tag">&lt;examples&gt;</span><span class="ln">A few cases that cover the edges you actually hit</span></button>
    </div>

    <div class="col" id="expl">
      <h3>What this section does</h3>
      <h4 id="ex-t">Background information</h4>
      <p id="ex-b"></p>
      <p class="quote" id="ex-q"></p>
    </div>
  </div>

  <div class="brk">
    <h3>The assumption that breaks</h3>
    <p class="sub">Prompt engineering assumes <b>a human is present at every iteration</b>. Tap a condition to see what happens when that stops being true.</p>
    <div class="chips">
      <button class="chip on" data-c="vol">high volume</button>
      <button class="chip" data-c="multi">multi-step task</button>
      <button class="chip" data-c="nobody">nobody available to grade it</button>
      <button class="chip" data-c="chain">output feeds the next step</button>
    </div>
    <div class="conseq" id="cq"></div>
  </div>

  <div class="ft">
    <span>Layer 1 of 3 &middot; the layer most tasks stop at</span>
    <span><b>Built by Marktechpost</b></span>
  </div>
</div>

<script>
(function(){
  var SEC = {
    bg:{t:"Background information",
      b:"Everything the model cannot infer: the codebase conventions, the domain vocabulary, the constraint that exists because of one incident two years ago. Left out, the model fills the gap with a confident guess.",
      q:"Anthropic's advice is to aim for the minimal set of information that fully describes the expected behaviour &mdash; and it is explicit that minimal does not mean short."},
    ins:{t:"Instructions",
      b:"The task and the rules for performing it. The common failure sits at two extremes: brittle if-else instructions hardcoded for cases you already saw, or instructions so general they assume context the model never received.",
      q:"Start at the minimum, watch how the model behaves, then add clear instructions and examples where precision is actually missing."},
    tool:{t:"Tool guidance",
      b:"Which tools exist, what each is for, and how to choose. An agent searching the web for something that only lives in Slack is doomed before it starts &mdash; the failure is in the tool description, not the reasoning.",
      q:"Anthropic reported that rewriting a bad tool description cut task completion time by 40% for later agents using it."},
    out:{t:"Output description",
      b:"The shape of the answer: the schema, the field names, the length, the format. This is the section that most often turns a model call from a demo into something another program can consume.",
      q:"Once output is machine-readable, it can be checked mechanically &mdash; which is the precondition for building a loop on top of it."},
    ex:{t:"Examples",
      b:"A handful of demonstrations, chosen to cover the cases that actually go wrong rather than the ones that already work. Examples move behaviour further per token than more prose instructions usually do.",
      q:"The point of examples is coverage of edges, not volume. Ten redundant examples teach less than three well-chosen ones."}
  };

  var CQ = {
    vol:{b:"You cannot sit in the chair for ten thousand runs. The prompt may be perfect and still be the wrong unit of work &mdash; there is no human bandwidth to close the cycle.",
         v:"\u2192 you need a trigger, which means a loop"},
    multi:{b:"Step three depends on what step two discovered. A single call cannot branch on information it does not have yet, and re-prompting by hand turns you into the control flow.",
         v:"\u2192 you need iteration, which means a loop"},
    nobody:{b:"Nobody reads the output before something acts on it. Quality now has to be decided by a check the system can run itself &mdash; tests, a schema, a rubric, a second model.",
         v:"\u2192 you need a stop condition, which means a loop"},
    chain:{b:"The response is consumed by the next step automatically. An error no longer costs you a re-run; it propagates, and it does so while nobody is looking.",
         v:"\u2192 you need verification and failure routing"}
  };

  function el(id){ return document.getElementById(id); }

  function showSec(k){
    var d = SEC[k];
    el("ex-t").textContent = d.t;
    el("ex-b").innerHTML = d.b;
    el("ex-q").innerHTML = d.q;
    [].slice.call(document.querySelectorAll(".blk")).forEach(function(b){
      b.classList.toggle("on", b.getAttribute("data-k") === k);
    });
    ping();
  }

  function showCq(k){
    var d = CQ[k];
    el("cq").innerHTML = d.b + '<span class="verdict">' + d.v + '</span>';
    [].slice.call(document.querySelectorAll(".chip")).forEach(function(c){
      c.classList.toggle("on", c.getAttribute("data-c") === k);
    });
    ping();
  }

  [].slice.call(document.querySelectorAll(".blk")).forEach(function(b){
    b.addEventListener("click", function(){ showSec(b.getAttribute("data-k")); });
  });
  [].slice.call(document.querySelectorAll(".chip")).forEach(function(c){
    c.addEventListener("click", function(){ showCq(c.getAttribute("data-c")); });
  });

  function ping(){
    try{
      var h = document.getElementById("plg-app").offsetHeight + 40;
      parent.postMessage({ plgFrame:"embed2-prompt-explained", height:h }, "*");
    }catch(e){}
  }
  window.addEventListener("load", ping);
  window.addEventListener("resize", ping);
  setTimeout(ping, 250);

  showSec("bg"); showCq("vol");
})();
</script>
</body>
</html>




<p class="wp-block-paragraph"></p>



<h3 id="h-layer-2-loop-engineering" class="wp-block-heading"><strong>Layer 2: loop engineering</strong></h3>



<p class="wp-block-paragraph">The framing is that a coding agent is a brute-force tool for finding solutions. The craft is designing the goal, the tools and the loop and not only the prompt.</p>



<p class="wp-block-paragraph">The term reached mainstream developer discussion in June 2026, after a widely shared post argued that engineers should stop prompting coding agents and start designing the loops that prompt them. The Claude Code team at Anthropic described the same shift on stage that week.</p>



<p class="wp-block-paragraph">The <a href="https://addyosmani.com/blog/loop-engineering/">most detailed public breakdown</a> identifies five primitives, plus a sixth element that holds them together:</p>



<ol class="wp-block-list">
<li><strong>Automations</strong>: a schedule or event that performs discovery and triage without supervision</li>



<li><strong>Worktrees</strong>: isolation so parallel agents cannot edit the same files</li>



<li><strong>Skills</strong>: project knowledge written down once in a <code>SKILL.md</code>, rather than re-explained every session</li>



<li><strong>Plugins and connectors</strong>: MCP-based access to the issue tracker, database or staging API</li>



<li><strong>Sub-agents</strong>: a maker/checker split, since the model that wrote the code grades it too generously</li>



<li><strong>State</strong>: a markdown file or board outside the conversation, because the model forgets between runs</li>
</ol>



<p class="wp-block-paragraph">Two in-session features are very important. <code>/loop</code> re-runs on a cadence. <code>/goal</code> runs until a written condition is actually true, with a separate small model checking after each turn — so the agent that wrote the code is not the agent grading it. Both Claude Code and the Codex app ship equivalents.</p>



<p class="wp-block-paragraph">The cycle is not the hard part. The <strong>stop condition</strong> is. A loop that cannot mechanically distinguish done from stuck does not fail loudly. It keeps spending tokens.</p>



<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Loop engineering, explained</title>
<style>
  :root{
    --bg:#0B1E1E; --panel:#122C2C; --line:#274B4B; --ink:#E8F5F1; --muted:#8FB3AC;
    --loop:#F2B705; --graph:#3FD9A8; --fail:#FF8A73;
    --mono:ui-monospace,SFMono-Regular,"JetBrains Mono",Menlo,Consolas,monospace;
    --sans:-apple-system,BlinkMacSystemFont,"Segoe UI",Inter,Roboto,Helvetica,Arial,sans-serif;
  }
  *{box-sizing:border-box}
  html,body{margin:0;padding:0;background:transparent}
  #plg-app{background:var(--bg); color:var(--ink); font-family:var(--sans);
    border:1px solid var(--line); border-radius:14px; overflow:hidden; max-width:920px; margin:0 auto}

  .hd{padding:20px 22px 16px; border-bottom:1px solid var(--line); background:linear-gradient(180deg,#143333 0%,#0B1E1E 100%)}
  .eyebrow{font-family:var(--mono); font-size:11px; letter-spacing:.16em; text-transform:uppercase; color:var(--loop); margin:0 0 8px}
  .hd h2{margin:0; font-size:clamp(19px,2.6vw,25px); line-height:1.2; letter-spacing:-.02em; font-weight:700}
  .hd .def{margin:10px 0 0; font-size:14.5px; line-height:1.6; color:var(--muted); max-width:66ch}
  .hd .def b{color:var(--ink)}

  .band{display:grid; grid-template-columns:1fr 1fr; background:var(--panel)}
  .side{padding:16px 20px}
  .side + .side{border-left:1px solid var(--line); background:#0E2525}
  .side h3{margin:0 0 4px; font-family:var(--mono); font-size:10.5px; letter-spacing:.13em;
    text-transform:uppercase; color:var(--muted); font-weight:500}
  .side .sub{margin:0 0 12px; font-size:12.5px; line-height:1.5; color:var(--muted)}

  .ring{max-width:290px; margin:0 auto}
  svg{width:100%; height:auto; display:block}
  .node{fill:#0E2525; stroke:var(--line); stroke-width:1.3; transition:stroke .2s, fill .2s}
  .node.on{stroke:var(--loop); fill:#241f08}
  .nl{font-family:var(--mono); font-size:11px; fill:var(--muted); transition:fill .2s}
  .nl.on{fill:var(--loop)}
  .ns{font-family:var(--mono); font-size:8.5px; fill:#5f8880}
  .ctr{font-family:var(--mono); font-size:24px; fill:var(--ink); font-weight:700}
  .ctrl{font-family:var(--mono); font-size:9px; fill:var(--muted); letter-spacing:.14em}

  .btns{display:flex; gap:8px; margin-top:12px; flex-wrap:wrap; justify-content:center}
  .btn{appearance:none; cursor:pointer; border:1px solid var(--line); background:#0E2525; color:var(--ink);
    font-family:var(--mono); font-size:11.5px; letter-spacing:.06em; text-transform:uppercase;
    padding:9px 14px; border-radius:7px; transition:border-color .18s, background .18s}
  .btn:hover{border-color:var(--loop); background:#1a2418}
  .btn:focus-visible{outline:2px solid var(--loop); outline-offset:2px}
  .btn.primary{border-color:var(--loop); color:var(--loop)}
  .btn[disabled]{opacity:.45; cursor:not-allowed}

  .log{font-family:var(--mono); font-size:12px; line-height:1.65; height:196px; overflow-y:auto;
    border:1px solid var(--line); border-radius:8px; padding:10px 12px; background:#0B1E1E}
  .log div{margin-bottom:3px; opacity:0; animation:fade .28s forwards}
  @keyframes fade{to{opacity:1}}
  .t-dim{color:var(--muted)} .t-act{color:var(--ink)} .t-ok{color:var(--graph)}
  .t-warn{color:var(--loop)} .t-bad{color:var(--fail)}
  .verdict{margin-top:11px; border-left:2px solid var(--loop); padding-left:12px;
    font-size:13.5px; line-height:1.6; color:var(--muted); min-height:60px}
  .verdict b{color:var(--ink)}

  .prims{border-top:1px solid var(--line); background:var(--panel); padding:16px 22px 18px}
  .prims h3{margin:0 0 4px; font-family:var(--mono); font-size:10.5px; letter-spacing:.13em;
    text-transform:uppercase; color:var(--muted); font-weight:500}
  .prims .sub{margin:0 0 12px; font-size:13.5px; line-height:1.55; color:var(--muted); max-width:68ch}
  .prims .sub b{color:var(--ink)}
  .grid{display:grid; grid-template-columns:repeat(6,1fr); gap:8px; margin-bottom:12px}
  .pc{appearance:none; cursor:pointer; border:1px solid var(--line); background:#0E2525; color:var(--muted);
    border-radius:9px; padding:11px 8px; text-align:center; transition:border-color .18s, color .18s, background .18s}
  .pc:hover{color:var(--ink); border-color:var(--loop)}
  .pc:focus-visible{outline:2px solid var(--loop); outline-offset:2px}
  .pc.on{color:var(--loop); border-color:var(--loop); background:#1d1a0c}
  .pc .n{display:block; font-family:var(--mono); font-size:9px; letter-spacing:.1em; opacity:.65; margin-bottom:4px}
  .pc .t{display:block; font-size:12.5px; line-height:1.3; font-weight:600}
  .pd{border:1px solid var(--line); border-radius:9px; background:#0E2525; padding:13px 15px; min-height:96px}
  .pd h4{margin:0 0 6px; font-size:15px; letter-spacing:-.01em; font-weight:650}
  .pd p{margin:0 0 8px; font-size:13.5px; line-height:1.6; color:var(--muted)}
  .pd p:last-child{margin-bottom:0}
  .pd .impl{font-family:var(--mono); font-size:11.5px; color:var(--loop); letter-spacing:.02em}

  .ft{padding:11px 22px; border-top:1px solid var(--line); background:#0E2525;
      display:flex; justify-content:space-between; gap:10px; flex-wrap:wrap;
      font-family:var(--mono); font-size:10.5px; letter-spacing:.05em; color:var(--muted)}
  .ft b{color:var(--graph); font-weight:600}

  @media (max-width:760px){
    .hd{padding:16px 15px 13px}
    .band{grid-template-columns:1fr}
    .side + .side{border-left:0; border-top:1px solid var(--line)}
    .side{padding:15px}
    .prims{padding:15px}
    .grid{grid-template-columns:repeat(3,1fr)}
    .ft{padding:10px 15px}
    .log{height:170px}
  }
  @media (prefers-reduced-motion:reduce){ .log div{animation:none; opacity:1} }
</style>
</head>
<body>
<div id="plg-app">
  <div class="hd">
    <p class="eyebrow">Interactive explainer &middot; 03 &middot; Layer 2</p>
    <h2>What loop engineering is</h2>
    <p class="def"><b>Loop engineering is designing the system that prompts the agent, instead of prompting it yourself.</b> The prompt does not disappear &mdash; it becomes a component inside a cycle that decides when to fire, what counts as passing, and when to stop trying. The skill was named in September 2025 and became the dominant developer topic in June 2026.</p>
  </div>

  <div class="band">
    <div class="side">
      <h3>The cycle</h3>
      <p class="sub">Four phases and a memory. Press run to watch one complete, then force a failure to see why the guardrails exist.</p>
      <div class="ring">
        <svg viewBox="0 0 300 244" role="img" aria-label="Loop cycle: trigger, act, verify, decide, with state outside the cycle">
          <circle cx="150" cy="120" r="64" fill="none" stroke="#274B4B" stroke-width="1" stroke-dasharray="3 6"/>
          <text class="ctr" id="iterN" x="150" y="120" text-anchor="middle">0</text>
          <text class="ctrl" x="150" y="138" text-anchor="middle">ITERATION</text>

          <rect class="node" id="n-trigger" x="103" y="4" width="94" height="40" rx="7"/>
          <text class="nl" id="l-trigger" x="150" y="21" text-anchor="middle">trigger</text>
          <text class="ns" x="150" y="34" text-anchor="middle">cron / event / goal</text>

          <rect class="node" id="n-act" x="210" y="100" width="86" height="40" rx="7"/>
          <text class="nl" id="l-act" x="253" y="117" text-anchor="middle">act</text>
          <text class="ns" x="253" y="130" text-anchor="middle">tools + edits</text>

          <rect class="node" id="n-verify" x="103" y="196" width="94" height="40" rx="7"/>
          <text class="nl" id="l-verify" x="150" y="213" text-anchor="middle">verify</text>
          <text class="ns" x="150" y="226" text-anchor="middle">tests / 2nd model</text>

          <rect class="node" id="n-decide" x="4" y="100" width="86" height="40" rx="7"/>
          <text class="nl" id="l-decide" x="47" y="117" text-anchor="middle">decide</text>
          <text class="ns" x="47" y="130" text-anchor="middle">retry or exit</text>

          <path d="M197,22 C242,30 262,60 253,100" fill="none" stroke="#274B4B" stroke-width="1.2"/>
          <path d="M253,140 C262,180 242,210 197,218" fill="none" stroke="#274B4B" stroke-width="1.2"/>
          <path d="M103,218 C58,210 38,180 47,140" fill="none" stroke="#274B4B" stroke-width="1.2"/>
          <path d="M47,100 C38,60 58,30 103,22" fill="none" stroke="#274B4B" stroke-width="1.2"/>
        </svg>
      </div>
      <div class="btns">
        <button class="btn primary" id="run">Run the loop</button>
        <button class="btn" id="runfail">Force a stall</button>
        <button class="btn" id="reset">Reset</button>
      </div>
    </div>

    <div class="side">
      <h3>Run log</h3>
      <p class="sub">Everything below happens with nobody watching. That is the whole point, and the whole risk.</p>
      <div class="log" id="log" aria-live="polite"><div class="t-dim">Idle. Press &#8220;Run the loop&#8221;.</div></div>
      <p class="verdict" id="verdict">A loop needs a stopping rule it can evaluate without you. Without one it does not fail loudly &mdash; it just keeps spending.</p>
    </div>
  </div>

  <div class="prims">
    <h3>What you actually build</h3>
    <p class="sub">A working loop needs <b>five primitives plus a place to remember things</b>. Both Claude Code and the Codex app now ship all six. Tap one.</p>
    <div class="grid">
      <button class="pc on" data-p="auto"><span class="n">01</span><span class="t">Automations</span></button>
      <button class="pc" data-p="wt"><span class="n">02</span><span class="t">Worktrees</span></button>
      <button class="pc" data-p="sk"><span class="n">03</span><span class="t">Skills</span></button>
      <button class="pc" data-p="mcp"><span class="n">04</span><span class="t">Connectors</span></button>
      <button class="pc" data-p="sub"><span class="n">05</span><span class="t">Sub-agents</span></button>
      <button class="pc" data-p="st"><span class="n">06</span><span class="t">State</span></button>
    </div>
    <div class="pd" id="pd"></div>
  </div>

  <div class="ft">
    <span>Layer 2 of 3 &middot; illustrative simulation, not measured</span>
    <span><b>Built by Marktechpost</b></span>
  </div>
</div>

<script>
(function(){
  var PRIMS = {
    auto:{t:"Automations &mdash; the heartbeat",
      b:"A schedule or event that does discovery and triage by itself. This is what makes a loop an actual loop rather than one run you did once. Findings come to you; you stop going around checking.",
      i:"/loop re-runs on a cadence \u00b7 /goal runs until a written condition is true"},
    wt:{t:"Worktrees &mdash; isolation",
      b:"A separate working directory on its own branch, sharing the same repository history. Two agents editing the same file is the same problem as two engineers committing to the same lines without talking.",
      i:"git worktree \u00b7 --worktree \u00b7 isolation: worktree on a subagent"},
    sk:{t:"Skills &mdash; written-down intent",
      b:"Project knowledge captured once instead of re-explained every session. Without it the loop re-derives your conventions from zero on every cycle; with it, context compounds.",
      i:"a folder with SKILL.md, plus optional scripts and references"},
    mcp:{t:"Connectors &mdash; reach",
      b:"A loop that can only see the filesystem is a small loop. Connectors let it read the issue tracker, query a database, hit a staging API, post to a channel. This is the difference between an agent that says what it would fix and a loop that opens the PR.",
      i:"built on MCP \u00b7 the same connector usually works across tools"},
    sub:{t:"Sub-agents &mdash; maker and checker",
      b:"Split the agent that writes from the agent that checks. The model that produced the code grades its own work far too generously. A second agent, often on a different model, catches what the first one talked itself into.",
      i:"a fresh model decides whether the loop is done, not the one that did the work"},
    st:{t:"State &mdash; memory outside the context",
      b:"A markdown file, a board, anything that lives outside the single conversation and records what is done and what is next. The model forgets everything between runs, so the memory has to sit on disk. The agent forgets; the repo does not.",
      i:"AGENTS.md, progress files, or an issue tracker via a connector"}
  };

  function el(id){ return document.getElementById(id); }
  var phases = ["trigger","act","verify","decide"];
  var running = false, timers = [];

  function clearT(){ timers.forEach(clearTimeout); timers = []; }
  function lit(p){
    phases.forEach(function(x){
      el("n-"+x).classList.toggle("on", x===p);
      el("l-"+x).classList.toggle("on", x===p);
    });
  }
  function clearLit(){ phases.forEach(function(x){ el("n-"+x).classList.remove("on"); el("l-"+x).classList.remove("on"); }); }

  function line(cls, txt){
    var d = document.createElement("div");
    d.className = cls; d.textContent = txt;
    var L = el("log"); L.appendChild(d); L.scrollTop = L.scrollHeight; ping();
  }

  function reset(){
    running = false; clearT();
    el("log").innerHTML = '<div class="t-dim">Idle. Press "Run the loop".</div>';
    el("iterN").textContent = "0";
    el("verdict").innerHTML = 'A loop needs a stopping rule it can evaluate without you. Without one it does not fail loudly &mdash; it just keeps spending.';
    clearLit(); el("run").disabled = false; el("runfail").disabled = false; ping();
  }
  el("reset").addEventListener("click", reset);

  function go(stall){
    if(running) return;
    running = true; el("run").disabled = true; el("runfail").disabled = true;
    el("log").innerHTML = "";
    el("iterN").textContent = "0";

    var script = stall ? [
      ["trigger","t-dim","> trigger fired: scheduled run 09:00"],
      ["act","t-act",   "  iter 1 \u00b7 act: edited 2 files, ran the suite"],
      ["verify","t-dim","  iter 1 \u00b7 verify: 3 failing \u2192 below bar"],
      ["decide","t-dim","  iter 1 \u00b7 decide: retry with updated state"],
      ["act","t-act",   "  iter 2 \u00b7 act: revised the same two files"],
      ["verify","t-dim","  iter 2 \u00b7 verify: 3 failing \u2192 no change"],
      ["decide","t-warn","  iter 2 \u00b7 decide: NO PROGRESS \u2192 escalate to human"]
    ] : [
      ["trigger","t-dim","> trigger fired: scheduled run 09:00"],
      ["act","t-act",   "  iter 1 \u00b7 act: read CI log, edited 2 files"],
      ["verify","t-dim","  iter 1 \u00b7 verify: 2 failing \u2192 below bar"],
      ["decide","t-dim","  iter 1 \u00b7 decide: retry with updated state"],
      ["act","t-act",   "  iter 2 \u00b7 act: revised the patch"],
      ["verify","t-dim","  iter 2 \u00b7 verify: suite green \u2192 bar met"],
      ["decide","t-ok",  "  iter 2 \u00b7 decide: PASS \u2192 exit, open PR, write state"]
    ];

    var iter = 0;
    script.forEach(function(s, i){
      timers.push(setTimeout(function(){
        lit(s[0]);
        if(s[0] === "act"){ iter++; el("iterN").textContent = String(iter); }
        line(s[1], s[2]);
      }, 520 * i));
    });

    timers.push(setTimeout(function(){
      el("verdict").innerHTML = stall
        ? "<b>No-progress detection fired.</b> Two iterations changed nothing, so the loop handed the task to a person instead of burning the rest of its budget re-trying the same failure."
        : "<b>It exited on the stop condition.</b> The loop stopped because a check it could run itself came back true. That is the only kind of \u201cdone\u201d a loop can act on \u2014 not your judgement, a test.";
      running = false; el("run").disabled = false; el("runfail").disabled = false;
      setTimeout(clearLit, 700); ping();
    }, 520 * script.length));
  }

  el("run").addEventListener("click", function(){ go(false); });
  el("runfail").addEventListener("click", function(){ go(true); });

  function showP(k){
    var d = PRIMS[k];
    el("pd").innerHTML = "<h4>" + d.t + "</h4><p>" + d.b + "</p><p class=\"impl\">" + d.i + "</p>";
    [].slice.call(document.querySelectorAll(".pc")).forEach(function(c){
      c.classList.toggle("on", c.getAttribute("data-p") === k);
    });
    ping();
  }
  [].slice.call(document.querySelectorAll(".pc")).forEach(function(c){
    c.addEventListener("click", function(){ showP(c.getAttribute("data-p")); });
  });

  function ping(){
    try{
      var h = document.getElementById("plg-app").offsetHeight + 40;
      parent.postMessage({ plgFrame:"embed3-loop-explained", height:h }, "*");
    }catch(e){}
  }
  window.addEventListener("load", ping);
  window.addEventListener("resize", ping);
  setTimeout(ping, 250);

  showP("auto");
})();
</script>
</body>
</html>




<p class="wp-block-paragraph"></p>



<h3 id="h-layer-3-graph-engineering" class="wp-block-heading"><strong>Layer 3: graph engineering</strong></h3>



<p class="wp-block-paragraph">In July 2026 the discussion moved from loops to graphs. Loops made agent behavior programmable. Graphs make agent organizations programmable.</p>



<p class="wp-block-paragraph">The structural point most often missed is that production multi-agent systems run <strong>two graphs at once</strong>.</p>



<p class="wp-block-paragraph">The <strong>org graph</strong> is stable. Long-lived agents hold named roles, own a zone, and accumulate context over time. It changes on redeploy.</p>



<p class="wp-block-paragraph">The <strong>work graph</strong> is ephemeral. Task nodes exist only while the work does. Edges split for parallel paths, merge on convergence, and disappear when evidence makes a branch unnecessary.</p>



<p class="wp-block-paragraph">The org graph answers <em>who</em>. The work graph answers <em>what, right now</em>.</p>



<p class="wp-block-paragraph">The skepticism around the label is fair. Sub-agents with a defined purpose already form a graph, and the technology preceded the vocabulary. <a href="https://docs.langchain.com/oss/python/langgraph/graph-api">LangGraph</a> shipped its graph API long before the term existed. Anthropic&#8217;s <a href="https://www.anthropic.com/engineering/building-effective-agents">five workflow patterns</a> from December 2024 — prompt chaining, routing, parallelization, orchestrator-workers, evaluator-optimizer — are graph topologies described in prose. What is new is a shared name for decisions those frameworks always forced: what are the nodes, what are the edges, what is in the state. </p>



<p class="wp-block-paragraph">The concrete artifact is worth knowing. In LangGraph a <code>StateGraph</code> is declared over a state schema. Nodes register with <code>add_node</code>. Edges wire up with <code>add_edge</code> and <code>add_conditional_edges</code>. <code>START</code> and <code>END</code> are marked, then the graph compiles. Nodes are plain functions that receive state and return partial updates. Context does not cross a node boundary unless an edge carries it. That last part describes the entire failure mode.</p>



<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Org graph vs work graph</title>
<style>
  :root{
    --bg:#0B1E1E; --panel:#122C2C; --line:#274B4B; --ink:#E8F5F1; --muted:#8FB3AC;
    --graph:#3FD9A8; --loop:#F2B705; --fail:#FF8A73; --new:#9BD7FF;
    --mono:ui-monospace,SFMono-Regular,"JetBrains Mono",Menlo,Consolas,monospace;
    --sans:-apple-system,BlinkMacSystemFont,"Segoe UI",Inter,Roboto,Helvetica,Arial,sans-serif;
  }
  *{box-sizing:border-box}
  html,body{margin:0;padding:0;background:transparent}
  #plg-app{background:var(--bg); color:var(--ink); font-family:var(--sans);
    border:1px solid var(--line); border-radius:14px; overflow:hidden; max-width:920px; margin:0 auto}
  .hd{padding:20px 22px 16px; border-bottom:1px solid var(--line); background:linear-gradient(180deg,#143333 0%,#0B1E1E 100%)}
  .eyebrow{font-family:var(--mono); font-size:11px; letter-spacing:.16em; text-transform:uppercase; color:var(--graph); margin:0 0 8px}
  .hd h2{margin:0; font-size:clamp(19px,2.6vw,25px); line-height:1.2; letter-spacing:-.02em; font-weight:700}
  .hd .def{margin:10px 0 0; font-size:14.5px; line-height:1.6; color:var(--muted); max-width:66ch}
  .hd .def b{color:var(--ink)}
  .hd p{margin:8px 0 0; color:var(--muted); font-size:14px; line-height:1.55; max-width:64ch}

  .prims{border-top:1px solid var(--line); background:var(--panel); padding:16px 22px 18px}
  .prims h3{margin:0 0 4px; font-family:var(--mono); font-size:10.5px; letter-spacing:.13em;
    text-transform:uppercase; color:var(--muted); font-weight:500}
  .prims .sub{margin:0 0 12px; font-size:13.5px; line-height:1.55; color:var(--muted); max-width:68ch}
  .prims .sub b{color:var(--ink)}
  .pgrid{display:grid; grid-template-columns:repeat(4,1fr); gap:8px; margin-bottom:12px}
  .pc{appearance:none; cursor:pointer; border:1px solid var(--line); background:#0E2525; color:var(--muted);
    border-radius:9px; padding:11px 9px; text-align:left; transition:border-color .18s, color .18s, background .18s}
  .pc:hover{color:var(--ink); border-color:var(--graph)}
  .pc:focus-visible{outline:2px solid var(--graph); outline-offset:2px}
  .pc.on{color:var(--graph); border-color:var(--graph); background:#10312a}
  .pc .t{display:block; font-size:13px; line-height:1.3; font-weight:600}
  .pc .c{display:block; font-family:var(--mono); font-size:9.5px; letter-spacing:.06em; opacity:.7; margin-top:4px}
  .pd{border:1px solid var(--line); border-radius:9px; background:#0E2525; padding:13px 15px; min-height:92px}
  .pd h4{margin:0 0 6px; font-size:15px; letter-spacing:-.01em; font-weight:650}
  .pd p{margin:0 0 8px; font-size:13.5px; line-height:1.6; color:var(--muted)}
  .pd p:last-child{margin-bottom:0}
  .pd .impl{font-family:var(--mono); font-size:11.5px; color:var(--graph); letter-spacing:.02em}

  .switch{display:flex; border-bottom:1px solid var(--line); background:#0E2525}
  .sw{flex:1; appearance:none; border:0; background:transparent; cursor:pointer; padding:13px 8px 12px;
    color:var(--muted); font-family:var(--mono); font-size:12px; letter-spacing:.08em; text-transform:uppercase;
    border-bottom:2px solid transparent; transition:color .18s, border-color .18s, background .18s}
  .sw:hover{color:var(--ink); background:#123030}
  .sw:focus-visible{outline:2px solid var(--graph); outline-offset:-3px}
  .sw[aria-selected="true"]{color:var(--ink); background:var(--panel); border-bottom-color:var(--graph)}

  .canvas{background:var(--panel); padding:14px 18px 4px}
  svg{width:100%; height:auto; display:block}
  .nd rect{fill:#0E2525; stroke:var(--line); stroke-width:1.3; transition:stroke .22s, fill .22s}
  .nd{cursor:pointer}
  .nd:hover rect{stroke:var(--graph)}
  .nd.sel rect{stroke:var(--graph); stroke-width:2; fill:#10312a}
  .nd.resident rect{stroke-dasharray:none}
  .nd.ephemeral rect{stroke-dasharray:5 4}
  .nd.spawned rect{stroke:var(--new)}
  .nd.cancelled rect{stroke:var(--fail); opacity:.5}
  .nd text.n{font-family:var(--sans); font-size:12.5px; fill:var(--ink); font-weight:600}
  .nd text.s{font-family:var(--mono); font-size:9.5px; fill:var(--muted); letter-spacing:.05em}
  .nd.cancelled text{opacity:.55}
  .ed{stroke:var(--line); stroke-width:1.4; fill:none; transition:stroke .22s}
  .ed.live{stroke:var(--graph)}
  .ed.retry{stroke:var(--loop); stroke-dasharray:4 4}
  .ed.new{stroke:var(--new)}

  .legend{display:flex; gap:16px; flex-wrap:wrap; padding:2px 18px 12px; background:var(--panel);
    font-family:var(--mono); font-size:10px; letter-spacing:.06em; color:var(--muted); text-transform:uppercase}
  .legend span{display:flex; align-items:center; gap:6px}
  .chip{width:16px; height:10px; border-radius:2px; border:1.3px solid var(--line); background:#0E2525; display:inline-block}
  .chip.d{border-style:dashed}
  .chip.n{border-color:var(--new)}

  .ctl{display:flex; gap:8px; padding:0 18px 14px; background:var(--panel); flex-wrap:wrap; align-items:center}
  .btn{appearance:none; cursor:pointer; border:1px solid var(--line); background:#0E2525; color:var(--ink);
    font-family:var(--mono); font-size:11.5px; letter-spacing:.06em; text-transform:uppercase;
    padding:9px 14px; border-radius:7px; transition:border-color .18s, background .18s}
  .btn:hover{border-color:var(--graph); background:#123232}
  .btn:focus-visible{outline:2px solid var(--graph); outline-offset:2px}
  .btn.primary{border-color:var(--graph); color:var(--graph)}
  .btn[disabled]{opacity:.4; cursor:not-allowed}

  .panelrow{display:grid; grid-template-columns:1fr 1fr; gap:0; border-top:1px solid var(--line)}
  .pane{padding:15px 18px; background:#0E2525}
  .pane + .pane{border-left:1px solid var(--line)}
  .pane h3{margin:0 0 8px; font-family:var(--mono); font-size:10.5px; letter-spacing:.13em;
    text-transform:uppercase; color:var(--muted); font-weight:500}
  .pane p{margin:0; font-size:13.5px; line-height:1.55; color:var(--muted)}
  .pane p b{color:var(--ink)}
  #evlog{font-family:var(--mono); font-size:11.5px; line-height:1.7; color:var(--muted); max-height:120px; overflow-y:auto}
  #evlog div{margin-bottom:2px}
  #evlog .hi{color:var(--new)}
  #evlog .wr{color:var(--loop)}
  #evlog .bd{color:var(--fail)}

  .ft{padding:11px 22px; border-top:1px solid var(--line); background:#0E2525;
      display:flex; justify-content:space-between; gap:10px; flex-wrap:wrap;
      font-family:var(--mono); font-size:10.5px; letter-spacing:.05em; color:var(--muted)}
  .ft b{color:var(--graph); font-weight:600}

  @media (max-width:700px){
    .hd{padding:16px 15px 13px}
    .canvas,.legend,.ctl{padding-left:14px; padding-right:14px}
    .panelrow{grid-template-columns:1fr}
    .pane + .pane{border-left:0; border-top:1px solid var(--line)}
    .pane{padding:14px}
    .prims{padding:15px}
    .pgrid{grid-template-columns:repeat(2,1fr)}
    .ft{padding:10px 15px}
    .sw{font-size:10.5px}
  }
  @media (prefers-reduced-motion:reduce){ .pulse{display:none} }
</style>
</head>
<body>
<div id="plg-app">
  <div class="hd">
    <p class="eyebrow">Interactive explainer &middot; 04 &middot; Layer 3</p>
    <h2>What graph engineering is</h2>
    <p class="def"><b>Graph engineering is designing the organisation of many agents, rather than the behaviour of one.</b> Loops made agent behaviour programmable; graphs make agent organisations programmable. You declare which nodes exist, which transitions are legal, and what happens when a branch fails. In practice two graphs run at once &mdash; a stable one that answers <em>who owns what</em>, and a per-task one that answers <em>what needs doing right now</em>.</p>
  </div>

  <div class="switch" role="tablist" aria-label="Graph view">
    <button class="sw" id="sw-org" role="tab" aria-selected="true">Org graph &middot; stable</button>
    <button class="sw" id="sw-work" role="tab" aria-selected="false">Work graph &middot; per task</button>
  </div>

  <div class="canvas">
    <svg id="cv" viewBox="0 0 760 280" role="img" aria-label="Interactive agent graph"></svg>
  </div>

  <div class="legend">
    <span><i class="chip"></i> resident agent</span>
    <span><i class="chip d"></i> ephemeral worker</span>
    <span><i class="chip n"></i> spawned at runtime</span>
    <span>tap a node for detail</span>
  </div>

  <div class="ctl">
    <button class="btn primary" id="flow">Send a task through</button>
    <button class="btn" id="evidence">New evidence arrives</button>
    <button class="btn" id="rst">Reset</button>
  </div>

  <div class="panelrow">
    <div class="pane">
      <h3>Node detail</h3>
      <p id="detail"><b>Nothing selected.</b> In a graph the node is the unit you name, scope and give tools to. Writing one is closer to writing a job description than writing a prompt.</p>
    </div>
    <div class="pane">
      <h3>Graph events</h3>
      <div id="evlog"><div>ready.</div></div>
    </div>
  </div>

  <div class="prims">
    <h3>What you actually declare</h3>
    <p class="sub">A graph is not a metaphor here &mdash; it is the literal artifact you write. In <b>LangGraph</b> you build a <code>StateGraph</code> over a state schema, register nodes, wire edges, then compile it. Tap a primitive.</p>
    <div class="pgrid">
      <button class="pc on" data-p="nodes"><span class="t">Nodes</span><span class="c">who does the work</span></button>
      <button class="pc" data-p="edges"><span class="t">Edges</span><span class="c">what may happen next</span></button>
      <button class="pc" data-p="state"><span class="t">State</span><span class="c">what travels between them</span></button>
      <button class="pc" data-p="fail"><span class="t">Failure routes</span><span class="c">what happens when a node breaks</span></button>
    </div>
    <div class="pd" id="pd"></div>
  </div>

  <div class="ft">
    <span>Layer 3 of 3 &middot; illustrative topology, not a specific product&#8217;s runtime</span>
    <span><b>Built by Marktechpost</b></span>
  </div>
</div>

<script>
(function(){
  var svgNS = "http://www.w3.org/2000/svg";
  var cv = document.getElementById("cv");
  var mode = "org", selected = null, evidenceCount = 0;

  var ORG = {
    box:"0 0 760 280", mbox:"0 0 340 424",
    nodes:[
      {id:"sec", x:60,  y:30,  w:150, h:52, mx:8,   my:8,   mw:158, mh:48,
       n:"Security agent", s:"owns auth + audit", kind:"resident",
       d:"<b>Security agent.</b> A long-lived node that owns one zone: auth, permissions, audit logs. It keeps its own context across runs, so a reviewer that has seen 300 pull requests carries the threat model implicitly."},
      {id:"data", x:60, y:114, w:150, h:52, mx:174, my:8,   mw:158, mh:48,
       n:"Data agent", s:"owns schema + queries", kind:"resident",
       d:"<b>Data agent.</b> Owns schema, migrations and query patterns. Zone ownership is what stops two agents editing the same surface from conflicting assumptions."},
      {id:"api", x:60,  y:198, w:150, h:52, mx:91,  my:80,  mw:158, mh:48,
       n:"API agent", s:"owns endpoints", kind:"resident",
       d:"<b>API agent.</b> Owns endpoints, rate limits and contracts. Each resident node runs its own loop inside its zone; cross-zone requests travel along declared edges."},
      {id:"router", x:300, y:114, w:150, h:52, mx:91, my:172, mw:158, mh:48,
       n:"Router", s:"routes by domain", kind:"resident",
       d:"<b>Router.</b> A deterministic node, not necessarily a model call. Classifying an input and dispatching it is Anthropic's routing workflow pattern, expressed as an edge set."},
      {id:"human", x:540, y:30, w:160, h:52, mx:8,  my:264, mw:158, mh:48,
       n:"Human checkpoint", s:"approval gate", kind:"resident",
       d:"<b>Human checkpoint.</b> A first-class node type. Graph engineering makes the escalation path explicit instead of leaving it to whoever notices the failure."},
      {id:"mem", x:540, y:114, w:160, h:52, mx:174, my:264, mw:158, mh:48,
       n:"Shared state", s:"durable, outside context", kind:"resident",
       d:"<b>Shared state.</b> In LangGraph this is the state object that flows along edges; nodes read it and return partial updates. Context does not cross a boundary unless you drew the edge that carries it."},
      {id:"obs", x:540, y:198, w:160, h:52, mx:91,  my:340, mw:158, mh:48,
       n:"Observability", s:"traces per node", kind:"resident",
       d:"<b>Observability.</b> A loop trace is one thread. A graph trace has to answer which nodes ran, in what order, at what token cost, and where execution diverged from the plan."}
    ],
    edges:[
      {a:"sec", b:"router"}, {a:"data", b:"router"}, {a:"api", b:"router"},
      {a:"router", b:"human"}, {a:"router", b:"mem"}, {a:"router", b:"obs"}
    ]
  };

  var WORK_BASE = {
    box:"0 0 760 280", mbox:"0 0 340 460",
    nodes:[
      {id:"plan", x:30,  y:114, w:130, h:52, mx:91, my:8, mw:158, mh:48,
       n:"Plan", s:"decompose task", kind:"resident",
       d:"<b>Plan node.</b> Produces the work graph itself: which nodes to spawn, in what order, where parallel execution is safe. This generator is the piece with no prompt-engineering equivalent."},
      {id:"r1", x:230, y:26,  w:130, h:52, mx:8, my:88, mw:158, mh:48,
       n:"Research A", s:"ephemeral", kind:"ephemeral",
       d:"<b>Ephemeral worker.</b> Exists only while the task does. It gets its own context window, explores one branch, and returns a compressed result rather than its whole trace."},
      {id:"r2", x:230, y:114, w:130, h:52, mx:174, my:88, mw:158, mh:48,
       n:"Research B", s:"ephemeral", kind:"ephemeral",
       d:"<b>Parallel branch.</b> Fan-out is where graphs beat loops: independent directions explored at once. It is also where cost multiplies, since each branch pays for its own context."},
      {id:"synth", x:430, y:70, w:130, h:52, mx:91, my:240, mw:158, mh:48,
       n:"Synthesise", s:"merge branches", kind:"resident",
       d:"<b>Join node.</b> Merges branch outputs. Every join is a place where an assumption made in one branch can contradict an assumption made in another &mdash; the failure mode single-threaded agents avoid by construction."},
      {id:"gate", x:430, y:170, w:130, h:52, mx:91, my:320, mw:158, mh:48,
       n:"Validator", s:"pass / fail gate", kind:"resident",
       d:"<b>Validator.</b> The graph-level analogue of a loop's stop condition. On failure the edge routes backwards, so failure handling is topology, not exception handling."},
      {id:"ship", x:620, y:114, w:120, h:52, mx:91, my:400, mw:158, mh:48,
       n:"Ship", s:"terminal", kind:"resident",
       d:"<b>Terminal node.</b> Reaching it is the graph's definition of done. In LangGraph this is the END sentinel that every path must eventually reach."}
    ],
    edges:[
      {a:"plan", b:"r1"}, {a:"plan", b:"r2"},
      {a:"r1", b:"synth"}, {a:"r2", b:"synth"},
      {a:"synth", b:"gate"}, {a:"gate", b:"ship"},
      {a:"gate", b:"r2", retry:true}
    ]
  };

  var work = clone(WORK_BASE);
  function clone(o){ return JSON.parse(JSON.stringify(o)); }

  function current(){ return mode === "org" ? ORG : work; }
  function findNode(id){ return current().nodes.filter(function(n){ return n.id===id; })[0]; }

  function mk(tag, attrs){
    var e = document.createElementNS(svgNS, tag);
    for(var k in attrs){ e.setAttribute(k, attrs[k]); }
    return e;
  }

  function isMobile(){ return window.innerWidth < 700; }

  function pos(n){
    return isMobile() &#038;&#038; n.mx !== undefined
      ? { x:n.mx, y:n.my, w:n.mw, h:n.mh }
      : { x:n.x,  y:n.y,  w:n.w,  h:n.h };
  }

  function edgePath(na, nb){
    var a = pos(na), b = pos(nb);
    var acx = a.x + a.w/2, acy = a.y + a.h/2;
    var bcx = b.x + b.w/2, bcy = b.y + b.h/2;
    var dx = bcx - acx, dy = bcy - acy;
    if(Math.abs(dy) > Math.abs(dx)){
      var ay = dy > 0 ? a.y + a.h : a.y;
      var by = dy > 0 ? b.y : b.y + b.h;
      var vm = (ay + by)/2;
      return "M" + acx + "," + ay + " C" + acx + "," + vm + " " + bcx + "," + vm + " " + bcx + "," + by;
    }
    var ax = dx > 0 ? a.x + a.w : a.x;
    var bx = dx > 0 ? b.x : b.x + b.w;
    var hm = (ax + bx)/2;
    return "M" + ax + "," + acy + " C" + hm + "," + acy + " " + hm + "," + bcy + " " + bx + "," + bcy;
  }

  function draw(){
    cv.innerHTML = "";
    var g = current();
    cv.setAttribute("viewBox", isMobile() ? g.mbox : g.box);

    var defs = mk("defs");
    ["#274B4B","#3FD9A8","#F2B705","#9BD7FF"].forEach(function(c,i){
      var m = mk("marker",{id:"m"+i, markerWidth:8, markerHeight:8, refX:7, refY:4, orient:"auto"});
      m.appendChild(mk("path",{d:"M0,0 L8,4 L0,8 z", fill:c}));
      defs.appendChild(m);
    });
    cv.appendChild(defs);

    g.edges.forEach(function(e, i){
      var a = g.nodes.filter(function(n){return n.id===e.a;})[0];
      var b = g.nodes.filter(function(n){return n.id===e.b;})[0];
      if(!a || !b) return;
      var cls = "ed" + (e.retry ? " retry" : "") + (e.isNew ? " new" : "");
      var mk_ = e.retry ? "url(#m2)" : (e.isNew ? "url(#m3)" : "url(#m0)");
      cv.appendChild(mk("path",{ d:edgePath(a,b), class:cls, "marker-end":mk_, "data-e":i }));
    });

    g.nodes.forEach(function(n){
      var grp = mk("g",{ class:"nd " + n.kind + (n.spawned?" spawned":"") + (n.cancelled?" cancelled":"") + (selected===n.id?" sel":""), tabindex:0, role:"button" });
      var p = pos(n);
      grp.appendChild(mk("rect",{ x:p.x, y:p.y, width:p.w, height:p.h, rx:9 }));
      var t1 = mk("text",{ x:p.x + p.w/2, y:p.y + 21, "text-anchor":"middle", class:"n" });
      t1.textContent = n.n; grp.appendChild(t1);
      var t2 = mk("text",{ x:p.x + p.w/2, y:p.y + 37, "text-anchor":"middle", class:"s" });
      t2.textContent = n.s; grp.appendChild(t2);
      grp.addEventListener("click", function(){ select(n.id); });
      grp.addEventListener("keydown", function(ev){ if(ev.key==="Enter"||ev.key===" "){ ev.preventDefault(); select(n.id); } });
      cv.appendChild(grp);
    });
    ping();
  }

  function select(id){
    selected = id;
    var n = findNode(id);
    if(n) document.getElementById("detail").innerHTML = n.d;
    draw();
  }

  function ev(txt, cls){
    var L = document.getElementById("evlog");
    var d = document.createElement("div");
    if(cls) d.className = cls;
    d.textContent = txt;
    L.appendChild(d); L.scrollTop = L.scrollHeight;
    ping();
  }

  function setMode(m){
    mode = m; selected = null;
    document.getElementById("sw-org").setAttribute("aria-selected", String(m==="org"));
    document.getElementById("sw-work").setAttribute("aria-selected", String(m==="work"));
    document.getElementById("evidence").disabled = (m !== "work");
    document.getElementById("detail").innerHTML = m === "org"
      ? "<b>Org graph.</b> Stable membership. Nodes are long-lived, own a zone, and keep context between tasks. You redeploy to change it."
      : "<b>Work graph.</b> Generated per task and thrown away. Nodes split, merge, reorder or vanish as evidence arrives.";
    ev(m === "org" ? "view: org graph (stable)" : "view: work graph (per task)");
    draw();
  }

  document.getElementById("sw-org").addEventListener("click", function(){ setMode("org"); });
  document.getElementById("sw-work").addEventListener("click", function(){ setMode("work"); });

  document.getElementById("flow").addEventListener("click", function(){
    var g = current();
    var paths = [].slice.call(cv.querySelectorAll("path.ed"));
    paths.forEach(function(p, i){
      setTimeout(function(){
        p.classList.add("live");
        setTimeout(function(){ p.classList.remove("live"); }, 620);
      }, i*170);
    });
    ev(mode === "org" ? "task routed through resident zones" : "work graph executed: fan-out \u2192 join \u2192 gate");
  });

  document.getElementById("evidence").addEventListener("click", function(){
    if(mode !== "work") return;
    evidenceCount++;
    if(evidenceCount === 1){
      work.nodes.push({ id:"r3", x:230, y:200, w:130, h:52, mx:91, my:164, mw:158, mh:48,
        n:"Research C", s:"spawned", kind:"ephemeral", spawned:true,
        d:"<b>Spawned at runtime.</b> The task turned out wider than planned, so the work graph added a node and wired it in. Nothing about the org graph changed." });
      work.edges.push({ a:"plan", b:"r3", isNew:true });
      work.edges.push({ a:"r3", b:"synth", isNew:true });
      ev("scope expanded \u2192 spawned node Research C, wired 2 edges", "hi");
    } else if(evidenceCount === 2){
      var n = work.nodes.filter(function(x){ return x.id==="r1"; })[0];
      if(n){ n.cancelled = true; n.s = "cancelled"; n.d = "<b>Cancelled.</b> Evidence from a sibling branch made this task unnecessary. In a work graph, a node disappearing is a normal state transition, not an error."; }
      ev("branch A made redundant \u2192 node cancelled", "wr");
    } else if(evidenceCount === 3){
      ev("validator failed \u2192 retry edge routed work back to branch B", "bd");
      var retry = cv.querySelector("path.ed.retry");
      if(retry){ retry.classList.add("live"); setTimeout(function(){ retry.classList.remove("live"); }, 900); }
    } else {
      ev("no further changes \u2014 press reset to start over");
    }
    draw();
  });

  document.getElementById("rst").addEventListener("click", function(){
    work = clone(WORK_BASE); evidenceCount = 0; selected = null;
    document.getElementById("evlog").innerHTML = "<div>reset.</div>";
    draw();
  });

  var GPRIMS = {
    nodes:{t:"Nodes &mdash; who does the work",
      b:"A node can be an agent running its own loop, a deterministic function, a router, or a human checkpoint. Writing one is closer to writing a job description than a prompt: what domain does it own, which tools can it reach, what context does it keep between tasks?",
      i:"builder.add_node(\"name\", fn) \u00b7 a node is a plain function of state"},
    edges:{t:"Edges &mdash; what may happen next",
      b:"Edges declare the legal transitions. Fixed edges always fire; conditional edges pick a destination from a routing function. Anthropic's five workflow patterns \u2014 prompt chaining, routing, parallelization, orchestrator-workers, evaluator-optimizer \u2014 are all edge sets described in prose.",
      i:"add_edge(START, \"a\") \u00b7 add_conditional_edges(\"a\", router, {...}) \u00b7 END"},
    state:{t:"State &mdash; what travels between them",
      b:"A shared data structure representing the current snapshot of the application. Nodes read it and return partial updates, which are merged. This is the single hardest part: context does not cross a node boundary unless you drew the edge that carries it, and a missing edge means a downstream agent acts blind.",
      i:"StateGraph(OverallState) \u00b7 nodes return only the keys they change"},
    fail:{t:"Failure routes &mdash; topology, not exception handling",
      b:"When a loop fails, one agent failed. When a graph node fails you have to trace whether the failure propagated, which downstream nodes consumed bad input, and whether the work graph is now inconsistent. So failure handling gets drawn as edges: retry the node, fall back, escalate to a human, or abort the branch.",
      i:"the retry edge in the diagram above is this, made visible"}
  };

  function showP(k){
    var d = GPRIMS[k];
    document.getElementById("pd").innerHTML =
      "<h4>" + d.t + "</h4><p>" + d.b + "</p><p class=\"impl\">" + d.i + "</p>";
    [].slice.call(document.querySelectorAll(".pc")).forEach(function(c){
      c.classList.toggle("on", c.getAttribute("data-p") === k);
    });
    ping();
  }
  [].slice.call(document.querySelectorAll(".pc")).forEach(function(c){
    c.addEventListener("click", function(){ showP(c.getAttribute("data-p")); });
  });

  function ping(){
    try{
      var h = document.getElementById("plg-app").offsetHeight + 40;
      parent.postMessage({ plgFrame:"embed4-graph-explained", height:h }, "*");
    }catch(e){}
  }
  var wasMobile = isMobile();
  window.addEventListener("load", ping);
  window.addEventListener("resize", function(){
    if(isMobile() !== wasMobile){ wasMobile = isMobile(); draw(); }
    ping();
  });
  setTimeout(ping, 250);

  setMode("org");
  showP("nodes");
})();
</script>
</body>
</html>




<p class="wp-block-paragraph" id="h-"></p>



<h2 id="h-how-to-choose-the-layer" class="wp-block-heading"><strong>How to choose the layer</strong></h2>



<p class="wp-block-paragraph">Work through the questions in order. The first &#8220;no&#8221; is usually the answer.</p>



<ol class="wp-block-list">
<li><strong>Does a person read every output before anything acts on it?</strong> If yes, the prompt layer is sufficient. A loop buys unsupervised execution, not autonomy.</li>



<li><strong>Can &#8220;done&#8221; be checked by something other than a human?</strong> Tests, a schema, a rubric, a second model. If not, there is no stop condition — only a budget.</li>



<li><strong>Does the task fit inside one agent&#8217;s context and one domain?</strong> If yes, build the loop. A single reasoning trace is the cheapest way to keep assumptions consistent.</li>



<li><strong>Do independent branches need to run at the same time?</strong> If yes, this is a graph problem: declare the nodes, the edges, the shared state and the failure routes. If no, extend the loop&#8217;s tools before adding agents.</li>
</ol>



<p class="wp-block-paragraph">A <a href="https://arxiv.org/abs/2607.00038">July 2026 arXiv paper</a> on coding-agent loops states the relationship correctly. A loop is a prompt repeated with scaffolding around it, and loop engineering is complementary to prompt engineering rather than its replacement. The same holds one floor up. Graphs are built from loops, and loops are built from prompts.</p>



<p class="wp-block-paragraph">The final caution is about the operator rather than the architecture. Two engineers can build an identical loop and get opposite outcomes. One moves faster on work they understand deeply. The other avoids understanding the work at all. The system cannot tell the difference. That is what makes the higher layers harder to design than prompts, not easier.</p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li><strong>Three units of control, not three competing methods:</strong> a prompt controls one response, a loop controls one agent&#8217;s cycle, a graph controls the organization of many agents.</li>



<li><strong>A loop is only as good as its stop condition</strong> — without a mechanical check for &#8220;done,&#8221; an unattended run terminates on token budget rather than correctness.</li>



<li><strong>Two graphs run simultaneously in production:</strong> a stable org graph answering <em>who owns what</em>, and a per-task work graph that splits, merges and cancels as evidence arrives.</li>



<li><strong>Published numbers set the price:</strong> +90.2% on an internal research eval, but roughly 15× the tokens of a chat, with token spend alone explaining 80% of the variance.</li>



<li><strong>Most tasks never reach the top of the stack</strong>: the counter-case stands for writing-heavy work, where dispersed decisions produce conflicting assumptions.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<h3 id="h-sources" class="wp-block-heading"><strong>Sources</strong></h3>



<ul class="wp-block-list">
<li>anthropic.com/engineering — <a href="https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents">Effective context engineering for AI agents</a></li>



<li>anthropic.com/engineering — <a href="https://www.anthropic.com/engineering/building-effective-agents">Building effective agents</a> (Dec 2024)</li>



<li>anthropic.com/engineering — <a href="https://www.anthropic.com/engineering/multi-agent-research-system">How we built our multi-agent research system</a> (13 Jun 2025)</li>



<li>simonwillison.net — <a href="https://simonwillison.net/2025/Sep/30/designing-agentic-loops/">Designing agentic loops</a> (30 Sep 2025)</li>



<li>addyosmani.com — <a href="https://addyosmani.com/blog/loop-engineering/">Loop Engineering</a> (7 Jun 2026)</li>



<li>firecrawl.dev — <a href="https://www.firecrawl.dev/blog/loop-engineering">Loop Engineering</a></li>



<li>explainx.ai — <a href="https://explainx.ai/blog/graph-engineering-ai-agents-multi-agent-organizations-2026">Graph Engineering</a> (18 Jul 2026)</li>



<li>truefoundry.com — <a href="https://www.truefoundry.com/blog/graph-engineering-enterprise-guide">Graph Engineering for Multi-Agent Systems</a></li>



<li>aibuilderclub.com — <a href="https://www.aibuilderclub.com/blog/graph-engineering-guide-2026">Graph Engineering Guide 2026</a></li>



<li>cognition.com — <a href="https://cognition.com/blog/dont-build-multi-agents">Don&#8217;t Build Multi-Agents</a> (Jun 2025)</li>



<li>langchain.com — <a href="https://www.langchain.com/blog/how-and-when-to-build-multi-agent-systems">How and when to build multi-agent systems</a></li>



<li>docs.langchain.com — <a href="https://docs.langchain.com/oss/python/langgraph/graph-api">LangGraph Graph API</a></li>



<li>arXiv — <a href="https://arxiv.org/abs/2606.25139">Buildrix</a> · <a href="https://arxiv.org/abs/2607.00038">Stop Hand-Holding Your Coding Agent</a></li>
</ul>
<p>The post <a href="https://www.marktechpost.com/2026/07/29/prompt-engineering-vs-loop-engineering-vs-graph-engineering-what-changes-at-each-layer/">Prompt Engineering vs Loop Engineering vs Graph Engineering: What Changes at Each Layer</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81422</post-id>
	</item>
		<item>
		<title>Liquid AI Releases LFM2.5-Encoder-230M and LFM2.5-Encoder-350M: Bidirectional Encoders That Stay Fast at 8K Context on CPU</title>
		<link>https://www.marktechpost.com/2026/07/29/liquid-ai-releases-lfm2-5-encoder-230m-and-lfm2-5-encoder-350m-bidirectional-encoders-that-stay-fast-at-8k-context-on-cpu/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Wed, 29 Jul 2026 09:38:42 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Python]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81415</guid>

					<description><![CDATA[<p>Liquid AI released two open-weight bidirectional encoders, LFM2.5-Encoder-230M and LFM2.5-Encoder-350M. Both carry an 8,192-token context and are built on the LFM2 hybrid backbone. The 350M ranks fourth of 14 models on a 17-task GLUE, SuperGLUE, and multilingual suite, behind only larger models. The 230M clears one 8K-token forward pass on CPU in about 28 seconds.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/29/liquid-ai-releases-lfm2-5-encoder-230m-and-lfm2-5-encoder-350m-bidirectional-encoders-that-stay-fast-at-8k-context-on-cpu/">Liquid AI Releases LFM2.5-Encoder-230M and LFM2.5-Encoder-350M: Bidirectional Encoders That Stay Fast at 8K Context on CPU</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Liquid AI has released two open-weight bidirectional encoders, <a href="https://huggingface.co/LiquidAI/LFM2.5-Encoder-230M">LFM2.5-Encoder-230M</a> and <a href="https://huggingface.co/LiquidAI/LFM2.5-Encoder-350M">LFM2.5-Encoder-350M</a>. Both are masked language models built on the LFM2 hybrid backbone. Both carry an 8,192-token context. </p>



<p class="wp-block-paragraph">Encoders sit underneath classifiers, intent routers, safety filters, and PII detectors. Those jobs run continuously, usually without a GPU, and increasingly on longer inputs. <a href="https://huggingface.co/papers/1810.04805">BERT</a> established the class. <a href="https://huggingface.co/papers/2412.13663">ModernBERT</a> pushed its accuracy, speed, and context. Liquid AI&#8217;s argument is that the LFM2 architecture continues that line, because its cost grows more slowly as inputs get longer.</p>



<h2 id="h-how-a-decoder-became-an-encoder" class="wp-block-heading"><strong>How a decoder became an encoder</strong></h2>



<p class="wp-block-paragraph">The encoders are not trained from scratch. They are initialized from the <a href="https://huggingface.co/LiquidAI/LFM2.5-230M">LFM2.5-230M</a> and LFM2.5-350M decoder backbones, <strong>then converted with three changes:</strong></p>



<ul class="wp-block-list">
<li>First, the causal attention mask is replaced with a bidirectional one, so every token attends to both sides. </li>



<li>Second, the LFM2 short convolutions are made non-causal using symmetric center padding, so each token&#8217;s convolution mixes in neighbours on both sides. </li>



<li>Third, the model is trained with a masked language modeling objective at a 30% mask rate. That is denser than BERT&#8217;s 15%, following evidence Liquid AI cites that a higher mask rate helps at this scale.</li>
</ul>



<p class="wp-block-paragraph"><strong>Training runs in two stages:</strong></p>



<ul class="wp-block-list">
<li>Stage one establishes general language competence with a short-context MLM objective on a large web corpus at 1,024 tokens. </li>



<li>Stage two extends context to 8,192 tokens on the full data mix, strengthening factual, legal, and multilingual competence.</li>
</ul>



<p class="wp-block-paragraph">Architecturally, the backbone interleaves gated short-convolution blocks with grouped-query attention, the same design described in the <a href="https://arxiv.org/abs/2511.23404">LFM2 technical report</a>. Both checkpoints use a hidden size of 1024 and a 65,536-token vocabulary, and support 15 languages. The license is the LFM Open License v1.0.</p>



<p class="wp-block-paragraph">The embed below breaks down the conversion, the full ranking table, the per-task scores, and the CPU latency figures.</p>



<iframe id="mtp-lfm-enc-1" srcdoc="&lt;style&gt;
*{box-sizing:border-box}
#lfm-term{background:#ffffff;color:#1c1917;font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,&quot;Liberation Mono&quot;,monospace;border:1px solid #dedbd8;border-radius:12px;overflow:hidden;max-width:900px;margin:0 auto;font-size:14px;line-height:1.55}
#lfm-term .bar{display:flex;align-items:center;gap:8px;background:linear-gradient(180deg,#fafaf9 0%,#f5f5f4 100%);border-bottom:1px solid #e7e5e4;padding:10px 14px;flex-wrap:wrap}
#lfm-term .dot{width:11px;height:11px;border-radius:50%;display:inline-block;border:1px solid rgba(0,0,0,.08)}
#lfm-term .d1{background:#e5e5e4}#lfm-term .d2{background:#d6d3d1}#lfm-term .d3{background:#c7c3c0}
#lfm-term .bartitle{color:#57534e;font-size:12px;margin-left:8px;letter-spacing:.04em}
#lfm-term .body{padding:16px 16px 8px}
#lfm-term .prompt{color:#0c0a09;font-weight:700}
#lfm-term .cmds{display:flex;flex-wrap:wrap;gap:7px;margin:10px 0 14px}
#lfm-term .cmd{background:#fafaf9;border:1px solid #e7e5e4;color:#57534e;padding:6px 11px;border-radius:6px;cursor:pointer;font-family:inherit;font-size:12.5px;transition:.15s}
#lfm-term .cmd:hover{border-color:#a8a29e;color:#0c0a09}
#lfm-term .cmd.on{background:#0c0a09;border-color:#0c0a09;color:#fafaf9;font-weight:700}
#lfm-term .out{background:#fcfcfb;border:1px solid #eceae8;border-radius:9px;padding:14px;min-height:230px}
#lfm-term h4{margin:0 0 10px;color:#0c0a09;font-size:14px;font-weight:700;letter-spacing:.02em}
#lfm-term p{margin:0 0 10px;color:#57534e;font-size:13px}
#lfm-term .o{color:#0c0a09;font-weight:700}
#lfm-term .w{color:#0c0a09;font-weight:700}
#lfm-term .dim{color:#78716c}
#lfm-term table{width:100%;border-collapse:collapse;font-size:12.5px}
#lfm-term th{text-align:left;color:#0c0a09;border-bottom:1px solid #d6d3d1;padding:6px 6px;font-weight:700;white-space:nowrap}
#lfm-term td{padding:5px 6px;border-bottom:1px solid #eeecea;color:#44403c}
#lfm-term tr.us td{color:#0c0a09;background:#f5f5f4;font-weight:600}
#lfm-term tr.us td:first-child{box-shadow:inset 2px 0 0 #0c0a09}
#lfm-term .bwrap{background:#eeecea;height:8px;border-radius:4px;width:110px;overflow:hidden;display:inline-block;vertical-align:middle}
#lfm-term .bfill{height:100%;background:#c7c3c0;display:block}
#lfm-term tr.us .bfill{background:#0c0a09}
#lfm-term select{background:#fff;color:#0c0a09;border:1px solid #d6d3d1;border-radius:6px;padding:6px 9px;font-family:inherit;font-size:12.5px;margin-bottom:12px}
#lfm-term .step{border-left:2px solid #0c0a09;padding:0 0 0 12px;margin:0 0 12px}
#lfm-term .step b{color:#0c0a09;display:block;margin-bottom:2px;font-size:13px}
#lfm-term .step span{color:#57534e;font-size:12.5px}
#lfm-term .kv{display:grid;grid-template-columns:1fr 1fr;gap:6px 18px}
#lfm-term .kv div{border-bottom:1px solid #eeecea;padding:5px 0;font-size:12.5px;color:#57534e;display:flex;justify-content:space-between;gap:10px}
#lfm-term .kv div b{color:#0c0a09;font-weight:700;text-align:right}
#lfm-term .lat{display:flex;flex-direction:column;gap:12px;margin:6px 0 12px}
#lfm-term .latrow{display:flex;align-items:center;gap:10px;font-size:12.5px}
#lfm-term .latlab{width:190px;color:#44403c;flex-shrink:0}
#lfm-term .latbar{height:22px;border-radius:4px;display:flex;align-items:center;padding-left:8px;font-weight:700;font-size:11.5px;white-space:nowrap}
#lfm-term .note{border-top:1px solid #eceae8;margin-top:12px;padding-top:10px;color:#78716c;font-size:11.5px}
#lfm-term .foot{border-top:1px solid #e7e5e4;background:#fafaf9;padding:10px 16px;color:#78716c;font-size:11px;display:flex;justify-content:space-between;flex-wrap:wrap;gap:6px}
#lfm-term .foot b{color:#0c0a09;font-weight:700}
@media(max-width:640px){
#lfm-term{font-size:13px}
#lfm-term .body{padding:12px 11px 6px}
#lfm-term .out{padding:11px;overflow-x:auto}
#lfm-term table{font-size:11.5px}
#lfm-term .bwrap{width:56px}
#lfm-term .kv{grid-template-columns:1fr}
#lfm-term .latlab{width:118px;font-size:11.5px}
#lfm-term .cmd{font-size:11.5px;padding:5px 9px}
#lfm-term .bar{padding:9px 11px}
#lfm-term .bartitle{margin-left:0;font-size:10.5px;flex-basis:100%;line-height:1.4}
}
&lt;/style&gt;
&lt;div id=&quot;lfm-term&quot;&gt;
  &lt;div class=&quot;bar&quot;&gt;
    &lt;span class=&quot;dot d1&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot d2&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot d3&quot;&gt;&lt;/span&gt;
    &lt;span class=&quot;bartitle&quot;&gt;lfm2.5-encoders &amp;#8212; liquid ai &amp;#8212; released july 28, 2026&lt;/span&gt;
  &lt;/div&gt;
  &lt;div class=&quot;body&quot;&gt;
    &lt;div&gt;&lt;span class=&quot;prompt&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;w&quot;&gt;lfm-encoder&lt;/span&gt; &lt;span class=&quot;dim&quot;&gt;--inspect&lt;/span&gt;&lt;/div&gt;
    &lt;div class=&quot;cmds&quot; id=&quot;lfm-cmds&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;out&quot; id=&quot;lfm-out&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;
  &lt;div class=&quot;foot&quot;&gt;
    &lt;span&gt;Sources: Liquid AI blog &amp;#183; Hugging Face model cards &amp;#183; Liquid4All/encoder_eval&lt;/span&gt;
    &lt;span&gt;Built by &lt;b&gt;Marktechpost&lt;/b&gt;&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;
&lt;script&gt;
(function(){
var RANK=[
[&quot;XLM-R XL&quot;,&quot;3.5B&quot;,83.06,1.16,0],
[&quot;ModernBERT-large&quot;,&quot;395M&quot;,81.68,2.49,0],
[&quot;XLM-R large&quot;,&quot;560M&quot;,81.34,1.66,0],
[&quot;LFM2.5-Encoder-350M&quot;,&quot;350M&quot;,81.02,1.00,1],
[&quot;mDeBERTa-v3&quot;,&quot;280M&quot;,80.37,1.06,0],
[&quot;LFM2.5-Encoder-230M&quot;,&quot;230M&quot;,79.29,1.02,1],
[&quot;ModernBERT-base&quot;,&quot;149M&quot;,78.19,1.39,0],
[&quot;XLM-R base&quot;,&quot;280M&quot;,77.46,1.63,0],
[&quot;EuroBERT-210M&quot;,&quot;210M&quot;,76.87,2.00,0],
[&quot;mGTE-MLM&quot;,&quot;305M&quot;,76.53,1.85,0],
[&quot;LFM2.5-ColBERT-350M&quot;,&quot;350M&quot;,76.18,1.25,0],
[&quot;EuroBERT-610M&quot;,&quot;610M&quot;,75.87,2.03,0],
[&quot;LFM2.5-Embedding-350M&quot;,&quot;350M&quot;,75.68,0.83,0],
[&quot;EuroBERT-2.1B&quot;,&quot;2.1B&quot;,72.19,5.59,0]];

var TASKS=[&quot;XNLI&quot;,&quot;PAWS-X&quot;,&quot;Amazon&quot;,&quot;MASSIVE&quot;,&quot;SeaHorse&quot;,&quot;CoLA*&quot;,&quot;SST-2*&quot;,&quot;MRPC*&quot;,&quot;STS-B*&quot;,&quot;QQP*&quot;,&quot;MNLI*&quot;,&quot;QNLI*&quot;,&quot;RTE*&quot;,&quot;BoolQ*&quot;,&quot;CB*&quot;,&quot;WiC*&quot;,&quot;WSC*&quot;];
var PER={
&quot;LFM2.5-Encoder-350M&quot;:[79.82,91.53,60.57,85.70,54.96,84.43,95.11,87.21,91.59,92.08,89.03,93.97,75.23,81.52,83.21,69.66,61.73],
&quot;LFM2.5-Encoder-230M&quot;:[77.63,90.86,59.97,85.52,54.61,81.42,94.08,80.20,90.99,91.71,87.98,92.96,67.29,76.54,83.21,70.31,62.69],
&quot;ModernBERT-base (149M)&quot;:[76.64,92.17,58.98,85.32,45.19,83.07,94.79,84.46,90.75,91.23,88.68,93.04,58.70,74.78,81.07,66.90,63.46],
&quot;ModernBERT-large (395M)&quot;:[81.76,92.46,60.42,85.65,40.20,83.37,96.10,88.14,92.16,91.81,90.65,94.36,81.59,81.68,88.21,70.16,69.81],
&quot;XLM-R XL (3.5B)&quot;:[87.12,93.30,62.29,88.21,59.51,84.58,95.69,87.65,90.20,91.72,90.09,94.47,82.38,83.70,89.88,66.55,64.62],
&quot;EuroBERT-2.1B&quot;:[70.52,92.34,60.45,85.40,6.84,68.99,92.50,82.94,66.44,91.03,81.56,93.56,53.29,77.23,82.86,57.90,63.46]};

var CMDS=[
{k:&quot;arch&quot;,label:&quot;arch --diff&quot;,fn:vArch},
{k:&quot;bench&quot;,label:&quot;bench --rank&quot;,fn:vBench},
{k:&quot;task&quot;,label:&quot;bench --per-task&quot;,fn:vTask},
{k:&quot;speed&quot;,label:&quot;speed --cpu 8192&quot;,fn:vSpeed},
{k:&quot;spec&quot;,label:&quot;spec --models&quot;,fn:vSpec},
{k:&quot;proto&quot;,label:&quot;eval --protocol&quot;,fn:vProto}];

var out=document.getElementById(&quot;lfm-out&quot;);
var bar=document.getElementById(&quot;lfm-cmds&quot;);

CMDS.forEach(function(c,i){
  var b=document.createElement(&quot;button&quot;);
  b.className=&quot;cmd&quot;+(i===0?&quot; on&quot;:&quot;&quot;);
  b.textContent=c.label;
  b.onclick=function(){
    var all=bar.querySelectorAll(&quot;.cmd&quot;);
    for(var j=0;j&lt;all.length;j++){all[j].className=&quot;cmd&quot;;}
    b.className=&quot;cmd on&quot;;
    out.innerHTML=c.fn();
    bind();
    resize();
  };
  bar.appendChild(b);
});

function vArch(){
  return '&lt;h4&gt;&lt;span class=&quot;o&quot;&gt;&amp;#62;&lt;/span&gt; Decoder &amp;#8594; bidirectional encoder: three changes&lt;/h4&gt;'+
  '&lt;p&gt;Both encoders are initialised from the LFM2.5 decoder backbones, then converted.&lt;/p&gt;'+
  '&lt;div class=&quot;step&quot;&gt;&lt;b&gt;01 &amp;#183; Bidirectional attention mask&lt;/b&gt;&lt;span&gt;The causal mask is replaced. Every token attends to left and right context.&lt;/span&gt;&lt;/div&gt;'+
  '&lt;div class=&quot;step&quot;&gt;&lt;b&gt;02 &amp;#183; Non-causal short convolutions&lt;/b&gt;&lt;span&gt;Symmetric center padding lets each convolution mix neighbours on both sides.&lt;/span&gt;&lt;/div&gt;'+
  '&lt;div class=&quot;step&quot;&gt;&lt;b&gt;03 &amp;#183; Masked language modeling at 30%&lt;/b&gt;&lt;span&gt;Denser than BERT&amp;#39;s 15%, following evidence that a higher mask rate helps at this scale.&lt;/span&gt;&lt;/div&gt;'+
  '&lt;div class=&quot;step&quot;&gt;&lt;b&gt;Two-stage training&lt;/b&gt;&lt;span&gt;Stage 1: short-context MLM on a large web corpus at 1,024 tokens. Stage 2: long-context adaptation to 8,192 tokens on the full data mix, strengthening factual, legal and multilingual competence.&lt;/span&gt;&lt;/div&gt;'+
  '&lt;div class=&quot;note&quot;&gt;Initialised from LFM2.5-230M-Base and LFM2.5-350M-Base. Backbone interleaves gated short-convolution blocks with grouped-query attention.&lt;/div&gt;';
}

function vBench(){
  var lo=70,hi=84,h='';
  h+='&lt;h4&gt;&lt;span class=&quot;o&quot;&gt;&amp;#62;&lt;/span&gt; 17-task mean &amp;#183; 14 models &amp;#183; avg@5 fresh seeds&lt;/h4&gt;';
  h+='&lt;p&gt;Every model is fully fine-tuned per task. Tasks span GLUE, SuperGLUE and multilingual classification.&lt;/p&gt;';
  h+='&lt;table&gt;&lt;tr&gt;&lt;th&gt;#&lt;/th&gt;&lt;th&gt;Model&lt;/th&gt;&lt;th&gt;Params&lt;/th&gt;&lt;th&gt;Mean&lt;/th&gt;&lt;th&gt;&amp;#177; std&lt;/th&gt;&lt;th&gt;&lt;/th&gt;&lt;/tr&gt;';
  RANK.forEach(function(r,i){
    var pct=Math.max(2,Math.round((r[2]-lo)/(hi-lo)*100));
    h+='&lt;tr class=&quot;'+(r[4]?'us':'')+'&quot;&gt;&lt;td&gt;'+(i+1)+'&lt;/td&gt;&lt;td&gt;'+r[0]+'&lt;/td&gt;&lt;td class=&quot;dim&quot;&gt;'+r[1]+'&lt;/td&gt;&lt;td&gt;&lt;b&gt;'+r[2].toFixed(2)+'&lt;/b&gt;&lt;/td&gt;&lt;td class=&quot;dim&quot;&gt;&amp;#177;'+r[3].toFixed(2)+'&lt;/td&gt;'+
    '&lt;td&gt;&lt;span class=&quot;bwrap&quot;&gt;&lt;span class=&quot;bfill&quot; style=&quot;width:'+pct+'%&quot;&gt;&lt;/span&gt;&lt;/span&gt;&lt;/td&gt;&lt;/tr&gt;';
  });
  h+='&lt;/table&gt;';
  h+='&lt;div class=&quot;note&quot;&gt;LFM2.5-Encoder-350M ranks 4th; the three models above it are all larger, including one nearly 10&amp;#215; its size. LFM2.5-Encoder-230M ranks 6th, above ModernBERT-base and every EuroBERT. Both score above Liquid AI&amp;#39;s own retriever models.&lt;/div&gt;';
  return h;
}

function vTask(){
  var keys=Object.keys(PER);
  var h='&lt;h4&gt;&lt;span class=&quot;o&quot;&gt;&amp;#62;&lt;/span&gt; Per-task scores &amp;#183; selected models&lt;/h4&gt;';
  h+='&lt;select id=&quot;lfm-sel&quot;&gt;';
  keys.forEach(function(k){h+='&lt;option value=&quot;'+k+'&quot;&gt;'+k+'&lt;/option&gt;';});
  h+='&lt;/select&gt;&lt;div id=&quot;lfm-tt&quot;&gt;&lt;/div&gt;';
  h+='&lt;div class=&quot;note&quot;&gt;* = dev split (GLUE / SuperGLUE test labels are hidden). The five multilingual columns are labelled test. SeaHorse and STS-B are Spearman&amp;#215;100; all other tasks are accuracy. Six of 14 models shown; the full matrix is on the Hugging Face model cards.&lt;/div&gt;';
  return h;
}

function drawTask(k){
  var v=PER[k],h='&lt;table&gt;&lt;tr&gt;&lt;th&gt;Task&lt;/th&gt;&lt;th&gt;Score&lt;/th&gt;&lt;th&gt;&lt;/th&gt;&lt;/tr&gt;';
  for(var i=0;i&lt;TASKS.length;i++){
    var pct=Math.max(2,Math.round(v[i]));
    h+='&lt;tr class=&quot;'+(k.indexOf(&quot;LFM2.5&quot;)===0?&quot;us&quot;:&quot;&quot;)+'&quot;&gt;&lt;td&gt;'+TASKS[i]+'&lt;/td&gt;&lt;td&gt;&lt;b&gt;'+v[i].toFixed(2)+'&lt;/b&gt;&lt;/td&gt;'+
    '&lt;td&gt;&lt;span class=&quot;bwrap&quot;&gt;&lt;span class=&quot;bfill&quot; style=&quot;width:'+pct+'%&quot;&gt;&lt;/span&gt;&lt;/span&gt;&lt;/td&gt;&lt;/tr&gt;';
  }
  h+='&lt;/table&gt;';
  document.getElementById(&quot;lfm-tt&quot;).innerHTML=h;
}

function vSpeed(){
  var h='&lt;h4&gt;&lt;span class=&quot;o&quot;&gt;&amp;#62;&lt;/span&gt; One forward pass at 8,192 tokens &amp;#183; CPU&lt;/h4&gt;';
  h+='&lt;p&gt;Liquid AI reports two measured points at full 8K context on CPU.&lt;/p&gt;';
  h+='&lt;div class=&quot;lat&quot;&gt;';
  h+='&lt;div class=&quot;latrow&quot;&gt;&lt;span class=&quot;latlab&quot;&gt;LFM2.5-Encoder-230M&lt;/span&gt;&lt;span class=&quot;latbar&quot; style=&quot;width:28%;background:#0c0a09;color:#fafaf9&quot;&gt;~28 s&lt;/span&gt;&lt;/div&gt;';
  h+='&lt;div class=&quot;latrow&quot;&gt;&lt;span class=&quot;latlab&quot;&gt;ModernBERT-base (149M)&lt;/span&gt;&lt;span class=&quot;latbar&quot; style=&quot;width:92%;background:#e7e5e4;color:#44403c&quot;&gt;over 1 min 30 s&lt;/span&gt;&lt;/div&gt;';
  h+='&lt;/div&gt;';
  h+='&lt;div class=&quot;kv&quot;&gt;'+
  '&lt;div&gt;&lt;span&gt;Speed-up quoted in blog&lt;/span&gt;&lt;b&gt;~3.7&amp;#215;&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Speed-up quoted on model cards&lt;/span&gt;&lt;b&gt;3.3&amp;#215;&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Context measured&lt;/span&gt;&lt;b&gt;8,192 tokens&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Practical framing&lt;/span&gt;&lt;b&gt;full contract in under 30 s&lt;/b&gt;&lt;/div&gt;'+
  '&lt;/div&gt;';
  h+='&lt;p style=&quot;margin-top:12px&quot;&gt;&lt;span class=&quot;o&quot;&gt;GPU:&lt;/span&gt; ModernBERT-base leads below roughly 1K tokens on the Apple GPU. The LFM2.5 encoders take the lead from about 2K tokens, with a smaller margin than on CPU.&lt;/p&gt;';
  h+='&lt;div class=&quot;note&quot;&gt;The blog and the model cards give different multipliers for the same comparison (3.7&amp;#215; vs 3.3&amp;#215;). Neither page names the specific CPU used for the measurement.&lt;/div&gt;';
  return h;
}

function vSpec(){
  return '&lt;h4&gt;&lt;span class=&quot;o&quot;&gt;&amp;#62;&lt;/span&gt; Model details&lt;/h4&gt;'+
  '&lt;table&gt;&lt;tr&gt;&lt;th&gt;Property&lt;/th&gt;&lt;th&gt;Encoder-230M&lt;/th&gt;&lt;th&gt;Encoder-350M&lt;/th&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Type&lt;/td&gt;&lt;td&gt;Bidirectional encoder (MLM)&lt;/td&gt;&lt;td&gt;Bidirectional encoder (MLM)&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Backbone&lt;/td&gt;&lt;td&gt;LFM2&lt;/td&gt;&lt;td&gt;LFM2&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Total parameters&lt;/td&gt;&lt;td&gt;~229.7M&lt;/td&gt;&lt;td&gt;~354.5M&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Hidden size&lt;/td&gt;&lt;td&gt;1024&lt;/td&gt;&lt;td&gt;1024&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Vocabulary size&lt;/td&gt;&lt;td&gt;65,536&lt;/td&gt;&lt;td&gt;65,536&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Context length&lt;/td&gt;&lt;td&gt;8,192 tokens&lt;/td&gt;&lt;td&gt;8,192 tokens&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Languages&lt;/td&gt;&lt;td&gt;15&lt;/td&gt;&lt;td&gt;15&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;License&lt;/td&gt;&lt;td&gt;LFM Open License v1.0&lt;/td&gt;&lt;td&gt;LFM Open License v1.0&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;/table&gt;'+
  '&lt;div class=&quot;note&quot;&gt;Classes: &lt;span class=&quot;w&quot;&gt;Lfm2BidirectionalModel&lt;/span&gt; for the body, &lt;span class=&quot;w&quot;&gt;Lfm2BidirectionalForMaskedLM&lt;/span&gt; for masked-LM loading. Both are wired through &lt;span class=&quot;w&quot;&gt;auto_map&lt;/span&gt; and require &lt;span class=&quot;w&quot;&gt;trust_remote_code=True&lt;/span&gt;. Languages: English, German, Spanish, French, Italian, Dutch, Polish, Portuguese, Arabic, Hindi, Japanese, Russian, Turkish, Vietnamese, Chinese.&lt;/div&gt;';
}

function vProto(){
  return '&lt;h4&gt;&lt;span class=&quot;o&quot;&gt;&amp;#62;&lt;/span&gt; How the comparison was run&lt;/h4&gt;'+
  '&lt;p&gt;The harness is open-sourced under Apache-2.0. Its stated goal is that only model weights vary between two cells of the table.&lt;/p&gt;'+
  '&lt;div class=&quot;kv&quot;&gt;'+
  '&lt;div&gt;&lt;span&gt;Models&lt;/span&gt;&lt;b&gt;14&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Tasks&lt;/span&gt;&lt;b&gt;17&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Task split&lt;/span&gt;&lt;b&gt;5 multilingual / 8 GLUE / 4 SuperGLUE&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Precision (all models)&lt;/span&gt;&lt;b&gt;fp32 master + bf16 autocast&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Optimizer&lt;/span&gt;&lt;b&gt;AdamW, &amp;#946;&amp;#8322;=0.95, wd=0.1&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Warmup / batch&lt;/span&gt;&lt;b&gt;10% linear / 32&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;LR search&lt;/span&gt;&lt;b&gt;10 rates, 1e-5 to 1e-4&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Selection seeds&lt;/span&gt;&lt;b&gt;3 (42&amp;#8211;44)&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Reporting seeds&lt;/span&gt;&lt;b&gt;5 fresh (45&amp;#8211;49)&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;transformers pin&lt;/span&gt;&lt;b&gt;4.56.2&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Selection jobs&lt;/span&gt;&lt;b&gt;~7,100&lt;/b&gt;&lt;/div&gt;'+
  '&lt;div&gt;&lt;span&gt;Reporting jobs&lt;/span&gt;&lt;b&gt;~1,200&lt;/b&gt;&lt;/div&gt;'+
  '&lt;/div&gt;'+
  '&lt;div class=&quot;note&quot;&gt;Stated limits from the repository: GLUE and SuperGLUE report dev splits because their test labels are hidden, and WSC is retained for completeness but is uninformative, since no model beats the majority class under this recipe.&lt;/div&gt;';
}

function bind(){
  var s=document.getElementById(&quot;lfm-sel&quot;);
  if(s){s.onchange=function(){drawTask(s.value);resize();};drawTask(s.value);}
}

function resize(){
  try{
    var h=document.getElementById(&quot;lfm-term&quot;).offsetHeight+40;
    parent.postMessage({lfmEncHeight:h},&quot;*&quot;);
  }catch(e){}
}

out.innerHTML=vArch();
bind();
resize();
window.addEventListener(&quot;load&quot;,resize);
setTimeout(resize,120);
setTimeout(resize,600);
window.addEventListener(&quot;resize&quot;,resize);
})();
&lt;/script&gt;
" style="width:100%;border:0;overflow:hidden;display:block;" height="620" scrolling="no" loading="lazy" title="LFM2.5-Encoders interactive"></iframe>
<script>
(function(){
  window.addEventListener("message", function(e){
    if(!e.data || !e.data.lfmEncHeight) return;
    var f = document.getElementById("mtp-lfm-enc-1");
    if(f) f.style.height = e.data.lfmEncHeight + "px";
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<h2 id="h-the-benchmark-results" class="wp-block-heading"><strong>The benchmark results</strong></h2>



<p class="wp-block-paragraph">Liquid AI evaluated 14 models on 17 tasks pulled from GLUE, SuperGLUE, and multilingual classification. Every model is fully fine-tuned per task, and the reported score is that fine-tuned model&#8217;s result.</p>



<p class="wp-block-paragraph">LFM2.5-Encoder-350M posts a 17-task mean of 81.02 (±1.00), ranking fourth. The three models ahead of it are all larger. They are XLM-R XL at 3.5B (83.06), ModernBERT-large at 395M (81.68), and XLM-R large at 560M (81.34). The top model is nearly 10 times its size.</p>



<p class="wp-block-paragraph">LFM2.5-Encoder-230M posts 79.29 (±1.02), ranking sixth. It beats <a href="https://huggingface.co/answerdotai/ModernBERT-base">ModernBERT-base</a> at 78.19 and every <a href="https://huggingface.co/collections/EuroBERT/eurobert">EuroBERT</a> model in the table, including EuroBERT-610M (75.87) and EuroBERT-2.1B (72.19). Both new encoders also score above Liquid AI&#8217;s own retrieval siblings, LFM2.5-ColBERT-350M (76.18) and LFM2.5-Embedding-350M (75.68). That gap is the stated reason Liquid AI built a general-purpose encoder instead of reusing the retrievers.</p>



<p class="wp-block-paragraph">The methodology is the more interesting part, and it is <a href="https://github.com/Liquid4All/encoder_eval">open-sourced under Apache-2.0</a>. Every model is loaded with fp32 master weights and bf16 autocast, so the table compares models rather than number formats. Every model uses the same AdamW recipe, taken from the EuroBERT card. Learning rate is selected per model and task across 10 rates and 3 seeds. Scores are then reported as the mean over 5 fresh seeds that never touched selection. The <code>transformers</code> version is pinned to 4.56.2 so dependency drift is not an uncontrolled variable.</p>



<h2 id="h-use-cases-and-deployment-environments" class="wp-block-heading"><strong>Use Cases and Deployment Environments</strong></h2>



<p class="wp-block-paragraph">The release names three settings. Edge and embedded devices come first. A car&#8217;s onboard compute or an industrial controller has no spare GPU, and cannot afford a cloud round trip. Regulated and on-premise systems in finance, healthcare, and legal, where documents are long, sensitive, and cannot leave in-house infrastructure. And high-volume cost-sensitive pipelines, where a small encoder acts as a cheap first pass in front of a larger model.</p>



<p class="wp-block-paragraph">Liquid AI also puts a useful number on the context window: 8,192 tokens is roughly 13 to 15 pages. One forward pass covers a full contract or a complete patient record.</p>



<p class="wp-block-paragraph">To show what a fine-tuned encoder looks like, the research team shipped five demos. Each runs in a CPU-only Hugging Face Space. They cover <a href="https://huggingface.co/spaces/LiquidAI/prompt-routing">zero-shot prompt routing</a>, <a href="https://huggingface.co/spaces/LiquidAI/policy-linting">zero-shot policy linting</a>, and <a href="https://huggingface.co/spaces/LiquidAI/spellchecker">spell checking</a>. A <a href="https://huggingface.co/spaces/LiquidAI/pii-detection">PII detector</a> handles 40 information types across 16 languages. A bonus <a href="https://huggingface.co/spaces/LiquidAI/masked-diffusion">masked-diffusion</a> demo runs the encoder as a chatbot that generates by iteratively unmasking.</p>



<h2 id="h-getting-them-running" class="wp-block-heading"><strong>Getting them running</strong></h2>



<p class="wp-block-paragraph">Both encoders load through <code>transformers</code>. The body is exposed as <code>Lfm2BidirectionalModel</code> and masked-LM loading uses <code>Lfm2BidirectionalForMaskedLM</code>. Both are wired through <code>auto_map</code>, so <code>trust_remote_code=True</code> is required on every load call.</p>



<p class="wp-block-paragraph">A base encoder produces general-purpose representations, not task outputs, so fine-tuning is mandatory. Liquid AI&#8217;s <a href="https://github.com/Liquid4All/cookbook/tree/main/examples/lfm-encoder-classification">fine-tuning tutorial</a> walks through long legal documents at an 8k context configuration. The model selection guidance is straightforward: 350M when accuracy matters most, 230M for tighter hardware or higher throughput.</p>



<iframe id="mtp-lfm-enc-2" srcdoc="&lt;style&gt;
*{box-sizing:border-box}
#lfm-dep{background:#ffffff;color:#1c1917;font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,&quot;Liberation Mono&quot;,monospace;border:1px solid #dedbd8;border-radius:12px;overflow:hidden;max-width:900px;margin:0 auto;font-size:14px;line-height:1.55}
#lfm-dep .hd{background:linear-gradient(180deg,#fafaf9 0%,#f5f5f4 100%);border-bottom:1px solid #e7e5e4;padding:13px 16px}
#lfm-dep .hd h3{margin:0;font-size:14px;color:#0c0a09;letter-spacing:.02em;font-weight:700}
#lfm-dep .hd span{color:#57534e;font-size:11.5px}
#lfm-dep .modes{display:flex;gap:7px;padding:12px 16px 0;flex-wrap:wrap}
#lfm-dep .mode{background:#fafaf9;border:1px solid #e7e5e4;color:#57534e;padding:6px 12px;border-radius:6px;cursor:pointer;font-family:inherit;font-size:12.5px;transition:.15s}
#lfm-dep .mode:hover{border-color:#a8a29e;color:#0c0a09}
#lfm-dep .mode.on{background:#0c0a09;border-color:#0c0a09;color:#fafaf9;font-weight:700}
#lfm-dep .wrap{display:flex;gap:14px;padding:14px 16px 6px;align-items:flex-start}
#lfm-dep .nav{width:194px;flex-shrink:0;display:flex;flex-direction:column;gap:5px}
#lfm-dep .nb{background:#fff;border:1px solid #eceae8;border-radius:7px;color:#57534e;text-align:left;padding:8px 10px;cursor:pointer;font-family:inherit;font-size:12.5px;transition:.15s;display:flex;gap:9px;align-items:flex-start}
#lfm-dep .nb:hover{border-color:#c7c3c0;color:#0c0a09}
#lfm-dep .nb.on{border-color:#0c0a09;color:#0c0a09;background:#f5f5f4;font-weight:600}
#lfm-dep .nb i{font-style:normal;color:#78716c;font-weight:700;flex-shrink:0}
#lfm-dep .nb.on i{color:#0c0a09}
#lfm-dep .pane{flex:1;min-width:0;background:#fcfcfb;border:1px solid #eceae8;border-radius:9px;padding:15px;min-height:300px}
#lfm-dep h4{margin:0 0 8px;color:#0c0a09;font-size:14px;font-weight:700}
#lfm-dep p{margin:0 0 11px;color:#57534e;font-size:13px}
#lfm-dep .o{color:#0c0a09;font-weight:700}
#lfm-dep .w{color:#0c0a09;font-weight:700}
#lfm-dep .dim{color:#57534e}
#lfm-dep pre{background:#0c0a09;border:1px solid #0c0a09;border-radius:8px;padding:12px;margin:0 0 11px;overflow-x:auto;color:#f5f5f4;font-family:inherit;font-size:12.5px;line-height:1.6;white-space:pre}
#lfm-dep .cp{background:#fafaf9;border:1px solid #e7e5e4;color:#57534e;border-radius:5px;padding:4px 9px;font-family:inherit;font-size:11px;cursor:pointer;float:right;margin:0 0 6px 8px}
#lfm-dep .cp:hover{border-color:#0c0a09;color:#0c0a09}
#lfm-dep table{width:100%;border-collapse:collapse;font-size:12.5px;margin-bottom:11px}
#lfm-dep th{text-align:left;color:#0c0a09;border-bottom:1px solid #d6d3d1;padding:6px;font-weight:700;white-space:nowrap}
#lfm-dep td{padding:6px;border-bottom:1px solid #eeecea;color:#44403c;vertical-align:top}
#lfm-dep ul{margin:0 0 11px;padding-left:17px;color:#57534e;font-size:12.8px}
#lfm-dep li{margin-bottom:5px}
#lfm-dep .note{border-top:1px solid #eceae8;margin-top:11px;padding-top:10px;color:#78716c;font-size:11.5px}
#lfm-dep .warn{border:1px solid #e7e5e4;border-left:2px solid #0c0a09;background:#f5f5f4;border-radius:7px;padding:10px 12px;color:#44403c;font-size:12px;margin-bottom:11px}
#lfm-dep .pick{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:12px}
#lfm-dep .card{border:1px solid #e7e5e4;border-radius:9px;padding:12px;background:linear-gradient(180deg,#ffffff 0%,#fafaf9 100%)}
#lfm-dep .card b{color:#0c0a09;display:block;margin-bottom:5px;font-size:13px}
#lfm-dep .card span{color:#57534e;font-size:12.3px}
#lfm-dep .foot{border-top:1px solid #e7e5e4;background:#fafaf9;padding:10px 16px;color:#78716c;font-size:11px;display:flex;justify-content:space-between;flex-wrap:wrap;gap:6px}
#lfm-dep .foot b{color:#0c0a09;font-weight:700}
@media(max-width:640px){
#lfm-dep{font-size:13px}
#lfm-dep .wrap{flex-direction:column;padding:12px 11px 4px;gap:11px}
#lfm-dep .nav{width:100%;flex-direction:row;flex-wrap:wrap}
#lfm-dep .nb{flex:1 1 44%;font-size:11.5px;padding:7px 8px}
#lfm-dep .pane{padding:12px;min-height:0}
#lfm-dep pre{font-size:11.3px;overflow-x:auto}
#lfm-dep .pick{grid-template-columns:1fr}
#lfm-dep table{font-size:11.5px}
#lfm-dep .modes{padding:11px 11px 0}
#lfm-dep .hd{padding:11px}
}
&lt;/style&gt;
&lt;div id=&quot;lfm-dep&quot;&gt;
  &lt;div class=&quot;hd&quot;&gt;
    &lt;h3&gt;LFM2.5-Encoders &amp;#8212; from download to deployment&lt;/h3&gt;
    &lt;span&gt;Every command below is taken from the official model cards and release blog&lt;/span&gt;
  &lt;/div&gt;
  &lt;div class=&quot;modes&quot; id=&quot;lfm-modes&quot;&gt;&lt;/div&gt;
  &lt;div class=&quot;wrap&quot;&gt;
    &lt;div class=&quot;nav&quot; id=&quot;lfm-nav&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;pane&quot; id=&quot;lfm-pane&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;
  &lt;div class=&quot;foot&quot;&gt;
    &lt;span&gt;Verified 29 July 2026 against huggingface.co/LiquidAI and liquid.ai/blog&lt;/span&gt;
    &lt;span&gt;Built by &lt;b&gt;Marktechpost&lt;/b&gt;&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;
&lt;script&gt;
(function(){
var GROUPS={
&quot;Step by step&quot;:[
{t:&quot;01 &amp;#183; Pick a size&quot;,fn:s1},
{t:&quot;02 &amp;#183; Install&quot;,fn:s2},
{t:&quot;03 &amp;#183; Verify with MLM&quot;,fn:s3},
{t:&quot;04 &amp;#183; Attach a head&quot;,fn:s4},
{t:&quot;05 &amp;#183; Fine-tune&quot;,fn:s5},
{t:&quot;06 &amp;#183; Try the demos&quot;,fn:s6}],
&quot;Deployment options&quot;:[
{t:&quot;CPU / on-prem&quot;,fn:d1},
{t:&quot;Edge &amp;#38; embedded&quot;,fn:d2},
{t:&quot;Browser (WebGPU)&quot;,fn:d3},
{t:&quot;GPU serving&quot;,fn:d4},
{t:&quot;What is not shipped&quot;,fn:d5}]
};

var modes=document.getElementById(&quot;lfm-modes&quot;);
var nav=document.getElementById(&quot;lfm-nav&quot;);
var pane=document.getElementById(&quot;lfm-pane&quot;);
var cur=&quot;Step by step&quot;;

Object.keys(GROUPS).forEach(function(g,i){
  var b=document.createElement(&quot;button&quot;);
  b.className=&quot;mode&quot;+(i===0?&quot; on&quot;:&quot;&quot;);
  b.innerHTML=g;
  b.onclick=function(){
    var all=modes.querySelectorAll(&quot;.mode&quot;);
    for(var j=0;j&lt;all.length;j++){all[j].className=&quot;mode&quot;;}
    b.className=&quot;mode on&quot;;
    cur=g;buildNav();
  };
  modes.appendChild(b);
});

function buildNav(){
  nav.innerHTML=&quot;&quot;;
  GROUPS[cur].forEach(function(it,i){
    var b=document.createElement(&quot;button&quot;);
    b.className=&quot;nb&quot;+(i===0?&quot; on&quot;:&quot;&quot;);
    b.innerHTML='&lt;i&gt;&amp;#9656;&lt;/i&gt;&lt;span&gt;'+it.t+'&lt;/span&gt;';
    b.onclick=function(){
      var all=nav.querySelectorAll(&quot;.nb&quot;);
      for(var j=0;j&lt;all.length;j++){all[j].className=&quot;nb&quot;;}
      b.className=&quot;nb on&quot;;
      pane.innerHTML=it.fn();bindCopy();resize();
    };
    nav.appendChild(b);
  });
  pane.innerHTML=GROUPS[cur][0].fn();bindCopy();resize();
}

function code(id,txt){
  return '&lt;button class=&quot;cp&quot; data-c=&quot;'+id+'&quot;&gt;copy&lt;/button&gt;&lt;pre id=&quot;'+id+'&quot;&gt;'+txt+'&lt;/pre&gt;';
}

function s1(){
  return '&lt;h4&gt;Choose between the two checkpoints&lt;/h4&gt;'+
  '&lt;p&gt;Both are open-weight under the LFM Open License v1.0 and share an 8,192-token context.&lt;/p&gt;'+
  '&lt;div class=&quot;pick&quot;&gt;'+
  '&lt;div class=&quot;card&quot;&gt;&lt;b&gt;LFM2.5-Encoder-350M&lt;/b&gt;&lt;span&gt;~354.5M parameters. Liquid AI recommends it when accuracy matters most. 17-task mean of 81.02, fourth of 14 models tested.&lt;/span&gt;&lt;/div&gt;'+
  '&lt;div class=&quot;card&quot;&gt;&lt;b&gt;LFM2.5-Encoder-230M&lt;/b&gt;&lt;span&gt;~229.7M parameters. Recommended for tighter hardware or higher throughput. 17-task mean of 79.29, sixth of 14.&lt;/span&gt;&lt;/div&gt;'+
  '&lt;/div&gt;'+
  '&lt;p&gt;Repository IDs:&lt;/p&gt;'+
  code(&quot;c1&quot;,&quot;LiquidAI/LFM2.5-Encoder-230M\nLiquidAI/LFM2.5-Encoder-350M&quot;)+
  '&lt;div class=&quot;note&quot;&gt;These are masked language models, not chat models. They produce general-purpose representations and must be fine-tuned before they emit task outputs.&lt;/div&gt;';
}

function s2(){
  return '&lt;h4&gt;Install&lt;/h4&gt;'+
  '&lt;p&gt;The encoders load through Transformers. No custom runtime is required.&lt;/p&gt;'+
  code(&quot;c2&quot;,&quot;pip install -U transformers&quot;)+
  '&lt;p&gt;Optional, if your GPU supports it:&lt;/p&gt;'+
  code(&quot;c3&quot;,&quot;pip install flash-attn&quot;)+
  '&lt;div class=&quot;warn&quot;&gt;&lt;span class=&quot;o&quot;&gt;Note:&lt;/span&gt; the architecture is not in Transformers upstream. Both classes are wired through &lt;span class=&quot;w&quot;&gt;auto_map&lt;/span&gt;, so every load call needs &lt;span class=&quot;w&quot;&gt;trust_remote_code=True&lt;/span&gt;, which executes model-authored Python from the Hub.&lt;/div&gt;';
}

function s3(){
  return '&lt;h4&gt;Verify the install with masked-token prediction&lt;/h4&gt;'+
  '&lt;p&gt;This is the smoke test from the model card. Swap the model id for the 350M checkpoint if preferred.&lt;/p&gt;'+
  code(&quot;c4&quot;,&quot;from transformers import AutoModelForMaskedLM, AutoTokenizer\nimport torch\n\nmodel_id = &amp;quot;LiquidAI/LFM2.5-Encoder-230M&amp;quot;\ntok = AutoTokenizer.from_pretrained(model_id, trust_remote_code=True)\nmlm = AutoModelForMaskedLM.from_pretrained(model_id, trust_remote_code=True)\n\ntext = f&amp;quot;The capital of France is {tok.mask_token}.&amp;quot;\nenc = tok(text, return_tensors=&amp;quot;pt&amp;quot;)\nwith torch.no_grad():\n    logits = mlm(**enc).logits\npos = (enc[&amp;quot;input_ids&amp;quot;][0] == tok.mask_token_id).nonzero()[0].item()\nprint([tok.decode([t]).strip() for t in logits[0, pos].topk(5).indices.tolist()])&quot;)+
  '&lt;p class=&quot;dim&quot;&gt;&lt;span class=&quot;o&quot;&gt;Expected output:&lt;/span&gt; &lt;span class=&quot;w&quot;&gt;[&amp;#39;Paris&amp;#39;, &amp;#39;Strasbourg&amp;#39;, &amp;#39;Paris&amp;#39;, &amp;#39;Lyon&amp;#39;, &amp;#39;Versailles&amp;#39;]&lt;/span&gt;&lt;/p&gt;';
}

function s4(){
  return '&lt;h4&gt;Load the body and attach your own head&lt;/h4&gt;'+
  '&lt;p&gt;For downstream work you load the encoder body, then add a classification, token-classification, regression or retrieval head.&lt;/p&gt;'+
  code(&quot;c5&quot;,&quot;from transformers import AutoModel\n\nbody = AutoModel.from_pretrained(\n    &amp;quot;LiquidAI/LFM2.5-Encoder-230M&amp;quot;,\n    trust_remote_code=True,\n)&quot;)+
  '&lt;p&gt;Tasks Liquid AI recommends for these checkpoints:&lt;/p&gt;'+
  '&lt;ul&gt;'+
  '&lt;li&gt;&lt;span class=&quot;w&quot;&gt;Text classification&lt;/span&gt; &amp;#8212; sentiment, topic, intent and routing, moderation, business-text linting&lt;/li&gt;'+
  '&lt;li&gt;&lt;span class=&quot;w&quot;&gt;Token classification&lt;/span&gt; &amp;#8212; named-entity recognition, span extraction, sequence labeling&lt;/li&gt;'+
  '&lt;li&gt;&lt;span class=&quot;w&quot;&gt;Retrieval and reranking&lt;/span&gt; &amp;#8212; backbone for dense or late-interaction (ColBERT-style) retrievers&lt;/li&gt;'+
  '&lt;li&gt;&lt;span class=&quot;w&quot;&gt;Semantic similarity&lt;/span&gt; &amp;#8212; STS, paraphrase, duplicate detection&lt;/li&gt;'+
  '&lt;li&gt;&lt;span class=&quot;w&quot;&gt;NLI and extractive QA&lt;/span&gt; &amp;#8212; sentence-pair reasoning, answer-span extraction&lt;/li&gt;'+
  '&lt;/ul&gt;'+
  '&lt;div class=&quot;note&quot;&gt;Liquid AI&amp;#39;s own eval harness uses exactly this pattern: it loads the body via AutoModel, then attaches a fresh mean-pool plus linear classifier head.&lt;/div&gt;';
}

function s5(){
  return '&lt;h4&gt;Fine-tune&lt;/h4&gt;'+
  '&lt;p&gt;Standard BERT-style fine-tuning. The model cards publish these starting points, to be tuned per task.&lt;/p&gt;'+
  '&lt;table&gt;&lt;tr&gt;&lt;th&gt;Hyperparameter&lt;/th&gt;&lt;th&gt;Suggested range&lt;/th&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Learning rate&lt;/td&gt;&lt;td&gt;1e-5 &amp;#8211; 5e-5&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Warmup ratio&lt;/td&gt;&lt;td&gt;0.1&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Weight decay&lt;/td&gt;&lt;td&gt;0.1&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Epochs&lt;/td&gt;&lt;td&gt;3 &amp;#8211; 20 (early stopping, patience 3)&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Precision&lt;/td&gt;&lt;td&gt;bf16 autocast (fp32 master weights)&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;/table&gt;'+
  '&lt;div class=&quot;note&quot;&gt;Liquid AI&amp;#39;s fine-tuning tutorial in the Liquid4All cookbook walks through fine-tuning on long legal documents at an 8k context configuration.&lt;/div&gt;';
}

function s6(){
  return '&lt;h4&gt;Try before you build&lt;/h4&gt;'+
  '&lt;p&gt;Liquid AI shipped five demos built from fine-tuned LFM2.5-Encoders. Each runs in a CPU-only Hugging Face Space.&lt;/p&gt;'+
  '&lt;table&gt;&lt;tr&gt;&lt;th&gt;Demo&lt;/th&gt;&lt;th&gt;What it does&lt;/th&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Zero-shot prompt routing&lt;/td&gt;&lt;td&gt;Define routing lanes as free text. The model scores the whole prompt against every lane in one pass.&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Zero-shot policy linting&lt;/td&gt;&lt;td&gt;Check text against rules written as free text. Scores every token against every rule in one pass.&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Spell checking&lt;/td&gt;&lt;td&gt;Corrects misspellings token by token.&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;PII detection&lt;/td&gt;&lt;td&gt;Spots and removes 40 kinds of personal information across 16 languages.&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;tr&gt;&lt;td&gt;Masked-diffusion generation&lt;/td&gt;&lt;td&gt;Bonus. Runs the encoder as a chatbot that generates by iteratively unmasking rather than left to right.&lt;/td&gt;&lt;/tr&gt;'+
  '&lt;/table&gt;'+
  '&lt;div class=&quot;note&quot;&gt;The five Spaces are hosted under the LiquidAI organisation on Hugging Face.&lt;/div&gt;';
}

function d1(){
  return '&lt;h4&gt;CPU and on-premise&lt;/h4&gt;'+
  '&lt;p&gt;This is the case the release is built around. At 8,192 tokens on CPU, Liquid AI measures about 28 seconds per forward pass for LFM2.5-Encoder-230M, against over a minute and a half for ModernBERT-base.&lt;/p&gt;'+
  '&lt;ul&gt;'+
  '&lt;li&gt;8,192 tokens is roughly 13 to 15 pages, so one forward pass can cover a full contract or record&lt;/li&gt;'+
  '&lt;li&gt;Liquid AI targets finance, healthcare and legal, where documents are long and cannot leave in-house infrastructure&lt;/li&gt;'+
  '&lt;li&gt;A small encoder also works as a cheap first pass in front of a larger model, triaging requests so the expensive model runs only when needed&lt;/li&gt;'+
  '&lt;/ul&gt;'+
  '&lt;div class=&quot;warn&quot;&gt;&lt;span class=&quot;o&quot;&gt;Open question:&lt;/span&gt; neither the blog nor the model cards state which CPU produced the 28-second figure. Benchmark on your own hardware before sizing a pipeline.&lt;/div&gt;';
}

function d2(){
  return '&lt;h4&gt;Edge and embedded&lt;/h4&gt;'+
  '&lt;p&gt;Liquid AI names onboard vehicle compute, industrial controllers and consumer devices as targets. These rarely have a spare GPU and often cannot afford a round trip to the cloud.&lt;/p&gt;'+
  '&lt;ul&gt;'+
  '&lt;li&gt;Intent routing and command classification directly on the CPU that is already present&lt;/li&gt;'+
  '&lt;li&gt;Content filtering and safety checks without a network hop&lt;/li&gt;'+
  '&lt;li&gt;The 230M checkpoint is the one Liquid AI points to for tight latency and memory budgets&lt;/li&gt;'+
  '&lt;/ul&gt;'+
  '&lt;div class=&quot;note&quot;&gt;The encoders inherit the LFM2 hybrid backbone, which Liquid AI designed through hardware-in-the-loop architecture search under edge latency and memory constraints.&lt;/div&gt;';
}

function d3(){
  return '&lt;h4&gt;Browser deployment&lt;/h4&gt;'+
  '&lt;p&gt;The LFM2.5-Encoder-230M model card states that the model runs in the browser on WebGPU, and calls its small footprint especially well suited to on-device and browser deployment.&lt;/p&gt;'+
  '&lt;div class=&quot;warn&quot;&gt;&lt;span class=&quot;o&quot;&gt;Scope note:&lt;/span&gt; the WebGPU claim appears on the 230M card only. The 350M card lists the same fast and on-device highlight but does not repeat the browser claim.&lt;/div&gt;'+
  '&lt;div class=&quot;note&quot;&gt;No packaged browser build is documented in the release. Treat this as a supported target rather than a shipped artifact.&lt;/div&gt;';
}

function d4(){
  return '&lt;h4&gt;GPU serving&lt;/h4&gt;'+
  '&lt;p&gt;On GPU the margin is smaller. Liquid AI reports that ModernBERT-base leads below roughly 1K tokens on the Apple GPU, and that the LFM2.5 encoders take the lead from about 2K tokens.&lt;/p&gt;'+
  '&lt;p&gt;For maximum efficiency on supported GPUs, the model cards recommend Flash Attention 2:&lt;/p&gt;'+
  code(&quot;c6&quot;,&quot;pip install flash-attn\n# then load the model as normal&quot;)+
  '&lt;div class=&quot;note&quot;&gt;Conclusion from the release: for long inputs the LFM2.5-Encoders are the faster choice, and on CPU dramatically so. For short inputs on GPU, ModernBERT-base remains competitive.&lt;/div&gt;';
}

function d5(){
  return '&lt;h4&gt;What the release does not include&lt;/h4&gt;'+
  '&lt;p&gt;Worth checking before you plan a rollout.&lt;/p&gt;'+
  '&lt;ul&gt;'+
  '&lt;li&gt;Liquid AI&amp;#39;s complete model library matrix lists GGUF, MLX and ONNX exports for its text, vision, audio and Nanos models. As of this writing the encoders are not yet listed in that matrix, and the model cards document a Transformers path only.&lt;/li&gt;'+
  '&lt;li&gt;No inference provider is serving either encoder on Hugging Face at launch.&lt;/li&gt;'+
  '&lt;li&gt;The published speed-up multiplier differs between sources: the release blog says about 3.7&amp;#215;, the model cards say 3.3&amp;#215;. Both describe the same 8K CPU comparison against ModernBERT-base.&lt;/li&gt;'+
  '&lt;li&gt;Loading requires trust_remote_code=True, which some enterprise environments restrict.&lt;/li&gt;'+
  '&lt;/ul&gt;'+
  '&lt;div class=&quot;note&quot;&gt;The evaluation harness itself is open-sourced under Apache-2.0, and the model weights are under the LFM Open License v1.0. Check the license terms against your deployment before shipping.&lt;/div&gt;';
}

function bindCopy(){
  var bs=pane.querySelectorAll(&quot;.cp&quot;);
  for(var i=0;i&lt;bs.length;i++){
    (function(b){
      b.onclick=function(){
        var el=document.getElementById(b.getAttribute(&quot;data-c&quot;));
        if(!el)return;
        var t=el.innerText;
        try{
          if(navigator.clipboard&amp;&amp;navigator.clipboard.writeText){navigator.clipboard.writeText(t);}
          else{var ta=document.createElement(&quot;textarea&quot;);ta.value=t;document.body.appendChild(ta);ta.select();document.execCommand(&quot;copy&quot;);document.body.removeChild(ta);}
          b.textContent=&quot;copied&quot;;setTimeout(function(){b.textContent=&quot;copy&quot;;},1400);
        }catch(e){b.textContent=&quot;select manually&quot;;setTimeout(function(){b.textContent=&quot;copy&quot;;},1600);}
      };
    })(bs[i]);
  }
}

function resize(){
  try{
    var h=document.getElementById(&quot;lfm-dep&quot;).offsetHeight+40;
    parent.postMessage({lfmDepHeight:h},&quot;*&quot;);
  }catch(e){}
}

buildNav();
window.addEventListener(&quot;load&quot;,resize);
setTimeout(resize,120);
setTimeout(resize,600);
window.addEventListener(&quot;resize&quot;,resize);
})();
&lt;/script&gt;
" style="width:100%;border:0;overflow:hidden;display:block;" height="640" scrolling="no" loading="lazy" title="LFM2.5-Encoders interactive"></iframe>
<script>
(function(){
  window.addEventListener("message", function(e){
    if(!e.data || !e.data.lfmDepHeight) return;
    var f = document.getElementById("mtp-lfm-enc-2");
    if(f) f.style.height = e.data.lfmDepHeight + "px";
  });
})();
</script>




<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>Liquid AI released LFM2.5-Encoder-230M and LFM2.5-Encoder-350M, open-weight bidirectional encoders with 8,192-token context.</li>



<li>Both convert LFM2.5 decoders into encoders via bidirectional masking, non-causal short convolutions, and 30% MLM.</li>



<li>LFM2.5-Encoder-350M ranks 4th of 14 models at 81.02; the three above it are all larger.</li>



<li>At 8K tokens on CPU, the 230M runs one forward pass in ~28s versus over 90s for ModernBERT-base.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"><strong>Check out the <a href="https://www.liquid.ai/blog/lfm2-5-encoders">Liquid AI blog post</a>, the <a href="https://huggingface.co/blog/LiquidAI/lfm2-5-encoders">Hugging Face write-up</a>, and the model cards for <a href="https://huggingface.co/LiquidAI/LFM2.5-Encoder-230M">LFM2.5-Encoder-230M</a> and <a href="https://huggingface.co/LiquidAI/LFM2.5-Encoder-350M">LFM2.5-Encoder-350M</a>.</strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/29/liquid-ai-releases-lfm2-5-encoder-230m-and-lfm2-5-encoder-350m-bidirectional-encoders-that-stay-fast-at-8k-context-on-cpu/">Liquid AI Releases LFM2.5-Encoder-230M and LFM2.5-Encoder-350M: Bidirectional Encoders That Stay Fast at 8K Context on CPU</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81415</post-id>
	</item>
		<item>
		<title>Building Non-Interactive Agentic Coding Workflows with Moonshot AI’s Kimi CLI, JSONL Streaming, Testing, and Session Memory</title>
		<link>https://www.marktechpost.com/2026/07/28/building-non-interactive-agentic-coding-workflows-with-moonshot-ais-kimi-cli-jsonl-streaming-testing-and-session-memory/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Tue, 28 Jul 2026 23:04:44 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81410</guid>

					<description><![CDATA[<p>In this tutorial, we configure and operate Kimi CLI as a fully non-interactive AI coding agent. We install the CLI through uv with an isolated Python 3.13 environment, configure Moonshot API authentication through a TOML-based provider and model definition, and build a reusable Python wrapper for executing non-interactive CLI commands. We then apply Kimi to [&#8230;]</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/28/building-non-interactive-agentic-coding-workflows-with-moonshot-ais-kimi-cli-jsonl-streaming-testing-and-session-memory/">Building Non-Interactive Agentic Coding Workflows with Moonshot AI’s Kimi CLI, JSONL Streaming, Testing, and Session Memory</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this tutorial, we configure and operate<a href="https://github.com/MoonshotAI/kimi-cli"> <strong>Kimi CLI</strong></a><strong> </strong>as a fully non-interactive AI coding agent. We install the CLI through uv with an isolated Python 3.13 environment, configure Moonshot API authentication through a TOML-based provider and model definition, and build a reusable Python wrapper for executing non-interactive CLI commands. We then apply Kimi to a realistic project workflow in which we inspect a codebase, identify implementation risks, autonomously modify source files, generate unit tests, execute validation commands, and iterate until the project passes its test suite. We also explore structured JSONL event streams, persistent multi-turn sessions, plan mode, model selection, Ralph loops, MCP integrations, session export, and web-based access, giving us a practical foundation for embedding Kimi CLI into automated development and agentic engineering pipelines.</p>



<h2 id="h-environment-setup-and-kimi-cli-installation" class="wp-block-heading"><strong>Environment Setup and Kimi CLI Installation</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import os, subprocess, textwrap, json, getpass, pathlib, shutil
HOME = pathlib.Path.home()
def sh(cmd, check=True, env=None, cwd=None):
   """Run a shell command, stream its output, return CompletedProcess."""
   print(f"\n$ {cmd}")
   e = {**os.environ, **(env or {})}
   r = subprocess.run(cmd, shell=True, env=e, cwd=cwd,
                      capture_output=True, text=True)
   if r.stdout: print(r.stdout)
   if r.stderr: print(r.stderr[-2000:])
   if check and r.returncode != 0:
       raise RuntimeError(f"Command failed ({r.returncode}): {cmd}")
   return r
print("=" * 70, "\nPART 1: Installing uv + Kimi CLI\n", "=" * 70)
sh("curl -LsSf https://astral.sh/uv/install.sh | sh")
UV_BIN = str(HOME / ".local" / "bin")
os.environ["PATH"] = f"{UV_BIN}:{os.environ['PATH']}"
sh("uv tool install --python 3.13 kimi-cli")
sh("kimi --version")
</code></pre></div></div>



<p class="wp-block-paragraph">We begin by importing the required Python modules and defining a reusable shell-command helper for controlled subprocess execution. We install uv, add its binary directory to the environment path, and use it to provision Kimi CLI with an isolated Python 3.13 runtime. We then verify the installation by querying the installed Kimi CLI version.</p>



<h2 id="h-configuring-moonshot-api-authentication-and-model-access" class="wp-block-heading"><strong>Configuring Moonshot API Authentication and Model Access</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("=" * 70, "\nPART 2: Configuring API access\n", "=" * 70)
try:
   from google.colab import userdata
   API_KEY = userdata.get("MOONSHOT_API_KEY")
   print("Loaded key from Colab Secrets.")
except Exception:
   API_KEY = getpass.getpass("Paste your Moonshot API key (hidden): ")
BASE_URL   = "https://api.moonshot.ai/v1"
MODEL_NAME = "kimi-k2-0711-preview"
kimi_dir = HOME / ".kimi"
kimi_dir.mkdir(exist_ok=True)
(kimi_dir / "config.toml").write_text(textwrap.dedent(f"""\
   default_model = "kimi-k2"
   [providers.moonshot]
   type = "kimi"
   base_url = "{BASE_URL}"
   api_key = "{API_KEY}"
   [models.kimi-k2]
   provider = "moonshot"
   model = "{MODEL_NAME}"
   max_context_size = 131072
"""))
print("Wrote ~/.kimi/config.toml")
</code></pre></div></div>



<p class="wp-block-paragraph">We securely retrieve the Moonshot API key from Google Colab Secrets or request it via a hidden input prompt. We define the Moonshot API endpoint and target Kimi model, then create the required .kimi configuration directory. We write the provider, model, context window, and default model settings to config.toml for non-interactive authentication.</p>



<h2 id="h-building-a-reusable-non-interactive-kimi-execution-wrapper" class="wp-block-heading"><strong>Building a Reusable Non-Interactive Kimi Execution Wrapper</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def kimi(prompt, work_dir=".", yolo=False, cont=False, quiet=True,
        stream_json=False, extra="", timeout=600):
   """Run one headless Kimi CLI turn and return its stdout."""
   flags = []
   if stream_json:
       flags.append("--print --output-format stream-json")
   elif quiet:
       flags.append("--quiet")
   else:
       flags.append("--print")
   if yolo: flags.append("--yolo")
   if cont: flags.append("--continue")
   flags.append(f'-w "{work_dir}"')
   if extra: flags.append(extra)
   cmd = f'kimi {" ".join(flags)} -p "{prompt}"'
   print(f"\n$ {cmd}\n" + "-" * 60)
   r = subprocess.run(cmd, shell=True, capture_output=True,
                      text=True, timeout=timeout)
   out = r.stdout.strip()
   print(out if out else r.stderr[-1500:])
   return out
</code></pre></div></div>



<p class="wp-block-paragraph">We define a Python wrapper that executes Kimi CLI prompts programmatically without requiring an interactive terminal session. We dynamically assemble flags for quiet output, streamed JSON events, autonomous tool approval, session continuation, working-directory isolation, and step limits. We capture the command output, display the final response or error details, and return the result for further processing.</p>



<h2 id="h-creating-and-analyzing-a-realistic-sample-project" class="wp-block-heading"><strong>Creating and Analyzing a Realistic Sample Project</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("=" * 70, "\nPART 4: Demo A — codebase Q&amp;A\n", "=" * 70)
proj = pathlib.Path("/content/demo_project")
if proj.exists(): shutil.rmtree(proj)
(proj / "app").mkdir(parents=True)
(proj / "app" / "inventory.py").write_text(textwrap.dedent("""\
   class Inventory:
       def __init__(self):
           self.items = {}
       def add(self, name, qty):
           self.items[name] = self.items.get(name, 0) + qty
       def remove(self, name, qty):
           # BUG: allows negative stock and KeyError on missing items
           self.items[name] = self.items[name] - qty
       def total(self):
           return sum(self.items.values())
"""))
(proj / "app" / "main.py").write_text(textwrap.dedent("""\
   from inventory import Inventory
   inv = Inventory()
   inv.add("widget", 10)
   inv.remove("widget", 3)
   print("Total stock:", inv.total())
"""))
(proj / "README.md").write_text("# Demo: a tiny inventory service\n")
kimi("Summarize this project's structure and purpose in under 120 words, "
    "then list any bugs or design risks you can spot in inventory.py.",
    work_dir=str(proj))
</code></pre></div></div>



<p class="wp-block-paragraph">We create a small inventory management project that includes a Python module, an executable script, and a README file. We intentionally include implementation defects to enable Kimi to inspect the repository structure and identify functional and design risks. We then run a read-only project analysis prompt in the project directory and request a concise technical assessment.</p>



<h2 id="h-automating-code-repair-testing-and-independent-validation" class="wp-block-heading"><strong>Automating Code Repair, Testing, and Independent Validation</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("=" * 70, "\nPART 5: Demo B — Kimi fixes the bug &amp; adds tests\n", "=" * 70)
kimi("Fix the bugs in app/inventory.py: remove() must raise KeyError->ValueError "
    "for unknown items and never allow negative stock. Then create tests.py at "
    "the project root using unittest covering add/remove/total and edge cases, "
    "run it with 'python -m unittest tests -v', and iterate until all tests pass. "
    "Finally print the test results.",
    work_dir=str(proj), yolo=True, extra="--max-steps-per-turn 30")
print("\n--- Files after Kimi's edits ---")
for f in sorted(proj.rglob("*.py")):
   print(f"\n### {f.relative_to(proj)} ###\n{f.read_text()}")
sh("python -m unittest tests -v", cwd=str(proj), check=False)
</code></pre></div></div>



<p class="wp-block-paragraph">We allow Kimi to modify the project autonomously, correct the inventory logic, generate unit tests, and execute the test suite. We configure a maximum agent-step limit so the model can iteratively diagnose failures and refine its implementation until validation succeeds. We inspect the resulting Python files and independently rerun the tests to confirm that the generated changes work correctly.</p>



<h2 id="h-exploring-structured-jsonl-sessions-and-advanced-features" class="wp-block-heading"><strong>Exploring Structured JSONL, Sessions, and Advanced Features</strong></h2>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("=" * 70, "\nPART 6: Demo C — machine-readable JSONL events\n", "=" * 70)
raw = kimi("In one sentence, what does app/main.py print when run?",
          work_dir=str(proj), stream_json=True, quiet=False)
print("\nParsed event types:")
for line in raw.splitlines():
   try:
       evt = json.loads(line)
       print(" •", evt.get("type", "?"), "-",
             str(evt)[:100].replace("\n", " "))
   except json.JSONDecodeError:
       pass
print("=" * 70, "\nPART 7: Demo D — conversational memory\n", "=" * 70)
kimi("Remember this: our release codename is BLUE-FALCON.", work_dir=str(proj))
kimi("What is our release codename? Answer with just the codename.",
    work_dir=str(proj), cont=True)
print("=" * 70, "\nPART 8: Power-user reference\n", "=" * 70)
print(textwrap.dedent("""
 # Plan mode — read-only exploration, produces an implementation plan:
 kimi --quiet --plan -w /content/demo_project -p "Plan adding SQLite persistence"
 # Pick a different model at runtime (must exist in config.toml):
 kimi --quiet -m kimi-k2 -p "hello"
 # Thinking mode (if the model supports it):
 kimi --quiet --thinking -p "Prove sqrt(2) is irrational"
 # Ralph loop — feed the same prompt repeatedly for one big task
 # until the agent outputs &lt;choice>STOP&lt;/choice> or the limit hits:
 kimi --print --yolo --max-ralph-iterations 5 -w /content/demo_project \\
      -p "Keep improving test coverage; STOP when everything is covered."
 # MCP tools — give Kimi extra capabilities via an MCP config file:
 #   /content/mcp.json -> {"mcpServers": {"context7":
 #        {"url": "https://mcp.context7.com/mcp",
 #         "headers": {"CONTEXT7_API_KEY": "YOUR_KEY"}}}}
 kimi --quiet --mcp-config-file /content/mcp.json -p "Use context7 to ..."
 # Export a session (context.jsonl, wire.jsonl, state.json) for debugging:
 kimi export --yes
 # Web UI (needs a tunnel on Colab, e.g. cloudflared/ngrok, since it
 # binds locally): kimi web --no-open --port 5494
"""))
print("Tutorial complete <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2714.png" alt="✔" class="wp-smiley" style="height: 1em; max-height: 1em;" /> — Kimi CLI is installed, authenticated, and has "
     "explored, fixed, tested, and remembered a real project headlessly.")
</code></pre></div></div>



<p class="wp-block-paragraph">We execute Kimi with streamed JSON output and parse each JSONL event to inspect machine-readable response types. We demonstrate persistent multi-turn memory by storing a release codename and retrieving it during an ongoing session in the same working directory. We conclude by reviewing advanced commands for planning, model switching, thinking mode, Ralph loops, MCP tools, session export, and web access.</p>



<p class="wp-block-paragraph">In conclusion, we established an end-to-end Kimi CLI workflow that runs entirely without relying on an interactive terminal session. We moved from environment provisioning and API configuration to project analysis, autonomous code repair, test generation, structured output parsing, and session continuation. We also independently verified the agent’s changes, which helps us distinguish generated output from actual execution results and improves the reliability of the workflow. With the reusable wrapper and reference commands in place, we can now extend the same architecture to larger repositories, CI-style validation tasks, MCP-enabled toolchains, iterative software improvement loops, and other programmable AI-assisted development workflows.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Agentic%20AI%20Codes/Kimi_CLI_Advanced_Agentic_Coding_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">Full Code here</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/28/building-non-interactive-agentic-coding-workflows-with-moonshot-ais-kimi-cli-jsonl-streaming-testing-and-session-memory/">Building Non-Interactive Agentic Coding Workflows with Moonshot AI’s Kimi CLI, JSONL Streaming, Testing, and Session Memory</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81410</post-id>
	</item>
		<item>
		<title>Fireworks AI Releases Fireworks Nexus: A Drop-In Routing and Cost-Control Layer That Moves Routine Coding Work to Open-Weight Models</title>
		<link>https://www.marktechpost.com/2026/07/28/fireworks-ai-releases-fireworks-nexus-a-drop-in-routing-and-cost-control-layer-that-moves-routine-coding-work-to-open-weight-models/</link>
		
		<dc:creator><![CDATA[Michal Sutter]]></dc:creator>
		<pubDate>Tue, 28 Jul 2026 22:45:56 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81406</guid>

					<description><![CDATA[<p>Fireworks AI has released Fireworks Nexus, an AI management and routing platform aimed at engineering organizations. It connects the coding tools developers already use to a managed layer of open-weight models. The problem it targets is well documented. Forbes reported that Uber exhausted its entire 2026 AI budget in four months. Claude Code had reached [&#8230;]</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/28/fireworks-ai-releases-fireworks-nexus-a-drop-in-routing-and-cost-control-layer-that-moves-routine-coding-work-to-open-weight-models/">Fireworks AI Releases Fireworks Nexus: A Drop-In Routing and Cost-Control Layer That Moves Routine Coding Work to Open-Weight Models</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Fireworks AI has released <a href="https://fireworks.ai/blog/fireworks-nexus">Fireworks Nexus</a>, an AI management and routing platform aimed at engineering organizations. It connects the coding tools developers already use to a managed layer of open-weight models. </p>



<p class="wp-block-paragraph">The problem it targets is well documented. Forbes <a href="https://www.forbes.com/sites/janakirammsv/2026/05/17/uber-burns-its-2026-ai-budget-in-four-months-on-claude-code/">reported</a> that Uber exhausted its entire 2026 AI budget in four months. Claude Code had reached roughly 5,000 engineers after a December rollout. Fireworks cites the same report, noting agentic adoption climbed from about a third of engineers to more than four-fifths in two months.</p>



<p class="wp-block-paragraph">Fireworks frames the underlying issue as a mismatch, not an overspend. Most organizations run routine work at frontier prices. Operational complexity has made switching to open-weight models unattractive for platform teams.</p>



<h2 id="h-fireworks-nexus" class="wp-block-heading"><strong>Fireworks Nexus</strong></h2>



<p class="wp-block-paragraph"><strong>Nexus is composed of three components:</strong></p>



<p class="wp-block-paragraph"><strong>1. Enterprise controls and cost observability</strong>: Teams set budgets at the team or company level. They track ROI across models and tools, and enforce policy from one place. Requests run on the Fireworks production inference platform, with <a href="https://docs.fireworks.ai/deployments/regions">US-hosted endpoints</a>, <a href="https://docs.fireworks.ai/guides/security_compliance/data_handling">zero data retention</a>, and coverage across 20 global data centers.</p>



<p class="wp-block-paragraph"><strong>2. Workflow continuity</strong>: <a href="https://github.com/fw-ai/fireconnect">FireConnect</a> is a one-line install that maps harness model slots to Fireworks models. It is released under Apache 2.0 and can be installed from the <a href="https://app.fireworks.ai/connect">Fireworks Dashboard</a> in a single command. Claude Code, Codex and OpenCode keep working unchanged. FireConnect runs on Fireworks Serverless APIs that are Anthropic- and OpenAI-compatible, so most tools connect with a base URL and a model ID.</p>



<p class="wp-block-paragraph"><strong>3. Intelligent traffic management</strong>: A custom trained model scores each request&#8217;s difficulty. Routine requests go to a cost-effective open-weight model served by Fireworks. Difficult requests pass through to your existing provider on your own key, which Fireworks states is never stored server-side. The research team reports this typically delivers a 3–5× cost reduction.</p>



<p class="wp-block-paragraph">One important note for anyone planning a rollout. The router is a research preview. It currently routes between Claude Opus 5 and GLM-5.2, so the pass-through path requires an Anthropic key. An all-open configuration routes between Kimi K3 and GLM-5.2 instead. </p>



<iframe id="mtp-nexus-explainer" title="Fireworks Nexus: interactive explainer and benchmark data" srcdoc="&lt;style&gt;
  :root{
    --bg:#0a0a0a; --panel:#111111; --panel2:#161616; --line:#272727;
    --fg:#ededed; --dim:#8b8b8b; --amber:#ff7a18; --amber2:#ffa04d; --white:#ffffff;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  #mtpnx{
    font-family:ui-monospace,SFMono-Regular,&quot;JetBrains Mono&quot;,&quot;Roboto Mono&quot;,Menlo,Consolas,monospace;
    background:var(--bg); color:var(--fg); border:1px solid var(--line); border-radius:10px;
    overflow:hidden; font-size:13px; line-height:1.55; -webkit-font-smoothing:antialiased;
  }
  #mtpnx .bar{display:flex;align-items:center;gap:8px;padding:9px 12px;background:#0d0d0d;border-bottom:1px solid var(--line)}
  #mtpnx .dot{width:10px;height:10px;border-radius:50%;background:#2e2e2e}
  #mtpnx .dot.a{background:var(--amber)}
  #mtpnx .barname{margin-left:8px;color:var(--dim);font-size:11px;letter-spacing:.06em;text-transform:uppercase}
  #mtpnx .barright{margin-left:auto;color:#4d4d4d;font-size:11px}
  #mtpnx .cmds{display:flex;flex-wrap:wrap;gap:6px;padding:12px 12px 4px;background:var(--panel)}
  #mtpnx .cmd{
    appearance:none;background:transparent;border:1px solid var(--line);color:var(--dim);
    font:inherit;font-size:12px;padding:6px 10px;border-radius:6px;cursor:pointer;transition:.15s;
  }
  #mtpnx .cmd:hover{border-color:#3d3d3d;color:var(--fg)}
  #mtpnx .cmd:focus-visible{outline:2px solid var(--amber);outline-offset:2px}
  #mtpnx .cmd[aria-selected=&quot;true&quot;]{border-color:var(--amber);color:var(--amber);background:rgba(255,122,24,.08)}
  #mtpnx .cmd .sig{color:#5a5a5a;margin-right:6px}
  #mtpnx .cmd[aria-selected=&quot;true&quot;] .sig{color:var(--amber2)}
  #mtpnx .screen{padding:14px 14px 18px;background:var(--panel)}
  #mtpnx .prompt{color:#5a5a5a;font-size:12px;margin-bottom:12px;word-break:break-word}
  #mtpnx .prompt b{color:var(--amber);font-weight:600}
  #mtpnx .prompt i{font-style:normal;color:var(--fg)}
  #mtpnx .cur{display:inline-block;width:7px;height:13px;background:var(--amber);vertical-align:-2px;animation:mtpblink 1.1s steps(1) infinite}
  @keyframes mtpblink{0%,50%{opacity:1}51%,100%{opacity:0}}
  #mtpnx h4{font-size:12px;letter-spacing:.08em;text-transform:uppercase;color:var(--amber);margin:0 0 8px;font-weight:600}
  #mtpnx p{color:#c2c2c2;font-size:12.5px;margin:0 0 10px}
  #mtpnx .note{color:#777;font-size:11.5px;border-left:2px solid #333;padding-left:9px;margin-top:12px}
  #mtpnx .grid3{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}
  #mtpnx .box{background:var(--panel2);border:1px solid var(--line);border-radius:8px;padding:12px;cursor:pointer;transition:.15s}
  #mtpnx .box:hover,#mtpnx .box.on{border-color:var(--amber);background:rgba(255,122,24,.06)}
  #mtpnx .box .n{color:var(--amber);font-size:11px;letter-spacing:.1em}
  #mtpnx .box .t{color:var(--white);font-size:13px;margin:6px 0 6px;font-weight:600}
  #mtpnx .box .d{color:#9a9a9a;font-size:11.5px;line-height:1.5}
  #mtpnx .detail{margin-top:10px;background:#0d0d0d;border:1px dashed #303030;border-radius:8px;padding:12px;color:#c2c2c2;font-size:12px;min-height:64px}
  #mtpnx .detail ul{margin:6px 0 0 16px}
  #mtpnx .detail li{margin:3px 0}
  #mtpnx table{width:100%;border-collapse:collapse;font-size:12px}
  #mtpnx th{text-align:left;color:#7c7c7c;font-weight:500;font-size:11px;letter-spacing:.05em;text-transform:uppercase;padding:6px 8px;border-bottom:1px solid var(--line);cursor:pointer;user-select:none;white-space:nowrap}
  #mtpnx th:hover{color:var(--amber)}
  #mtpnx th .ar{color:var(--amber);font-size:9px}
  #mtpnx td{padding:7px 8px;border-bottom:1px solid #1b1b1b;white-space:nowrap}
  #mtpnx tr.hl td{background:rgba(255,122,24,.07)}
  #mtpnx .mdl{color:var(--white)}
  #mtpnx .tag{font-size:10px;color:#7c7c7c;border:1px solid #2e2e2e;border-radius:4px;padding:1px 5px;margin-left:6px}
  #mtpnx .tag.o{color:var(--amber2);border-color:#57330f}
  #mtpnx .barwrap{display:flex;align-items:center;gap:7px;min-width:130px}
  #mtpnx .barbg{flex:1;height:6px;background:#1e1e1e;border-radius:3px;overflow:hidden;min-width:52px}
  #mtpnx .barfill{height:100%;background:linear-gradient(90deg,var(--amber),var(--amber2));border-radius:3px;transition:width .5s ease}
  #mtpnx .barfill.g{background:#3f3f3f}
  #mtpnx .num{color:var(--white)}
  #mtpnx .sim{background:var(--panel2);border:1px solid var(--line);border-radius:8px;padding:14px;margin-top:12px}
  #mtpnx .slrow{display:flex;align-items:center;gap:12px;flex-wrap:wrap}
  #mtpnx input[type=range]{-webkit-appearance:none;appearance:none;flex:1;min-width:170px;height:4px;background:#262626;border-radius:2px;outline:none}
  #mtpnx input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;width:16px;height:16px;border-radius:50%;background:var(--amber);cursor:pointer;border:2px solid #0a0a0a}
  #mtpnx input[type=range]::-moz-range-thumb{width:16px;height:16px;border-radius:50%;background:var(--amber);cursor:pointer;border:2px solid #0a0a0a}
  #mtpnx input[type=range]:focus-visible{outline:2px solid var(--amber);outline-offset:4px}
  #mtpnx .big{color:var(--amber);font-size:22px;font-weight:600;letter-spacing:-.01em}
  #mtpnx .lbl{color:#7c7c7c;font-size:11px;letter-spacing:.05em;text-transform:uppercase}
  #mtpnx .scroll{overflow-x:auto}
  #mtpnx .ft{display:flex;flex-wrap:wrap;gap:8px;justify-content:space-between;align-items:center;padding:10px 14px;background:#0d0d0d;border-top:1px solid var(--line);font-size:11px;color:#666}
  #mtpnx .ft a{color:var(--amber);text-decoration:none}
  #mtpnx .ft a:hover{text-decoration:underline}
  #mtpnx .brand{color:var(--amber);letter-spacing:.04em}
  @media (max-width:640px){
    #mtpnx{font-size:12px}
    #mtpnx .grid3{grid-template-columns:1fr}
    #mtpnx .cmd{font-size:11px;padding:5px 8px}
    #mtpnx .screen{padding:12px 10px 14px}
    #mtpnx .big{font-size:19px}
    #mtpnx th,#mtpnx td{padding:6px 6px;font-size:11px}
  }
  @media (prefers-reduced-motion:reduce){
    #mtpnx .cur{animation:none}
    #mtpnx .barfill{transition:none}
  }
&lt;/style&gt;

&lt;div id=&quot;mtpnx&quot;&gt;
  &lt;div class=&quot;bar&quot;&gt;
    &lt;span class=&quot;dot a&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot&quot;&gt;&lt;/span&gt;
    &lt;span class=&quot;barname&quot;&gt;fireworks-nexus &amp;#8212; explainer&lt;/span&gt;
    &lt;span class=&quot;barright&quot;&gt;read-only&lt;/span&gt;
  &lt;/div&gt;

  &lt;div class=&quot;cmds&quot; role=&quot;tablist&quot; aria-label=&quot;Explainer commands&quot;&gt;
    &lt;button class=&quot;cmd&quot; role=&quot;tab&quot; aria-selected=&quot;true&quot; data-v=&quot;stack&quot;&gt;&lt;span class=&quot;sig&quot;&gt;$&lt;/span&gt;nexus explain --stack&lt;/button&gt;
    &lt;button class=&quot;cmd&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-v=&quot;arize&quot;&gt;&lt;span class=&quot;sig&quot;&gt;$&lt;/span&gt;bench --source arize&lt;/button&gt;
    &lt;button class=&quot;cmd&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-v=&quot;diff&quot;&gt;&lt;span class=&quot;sig&quot;&gt;$&lt;/span&gt;bench --by-difficulty&lt;/button&gt;
    &lt;button class=&quot;cmd&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-v=&quot;faros&quot;&gt;&lt;span class=&quot;sig&quot;&gt;$&lt;/span&gt;bench --source faros&lt;/button&gt;
    &lt;button class=&quot;cmd&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-v=&quot;route&quot;&gt;&lt;span class=&quot;sig&quot;&gt;$&lt;/span&gt;route --policies&lt;/button&gt;
  &lt;/div&gt;

  &lt;div class=&quot;screen&quot;&gt;
    &lt;div class=&quot;prompt&quot;&gt;&lt;b&gt;nexus@fireworks&lt;/b&gt;:&lt;i&gt;~&lt;/i&gt;$ &lt;span id=&quot;mtpnx-cmd&quot;&gt;nexus explain --stack&lt;/span&gt;&lt;span class=&quot;cur&quot;&gt;&lt;/span&gt;&lt;/div&gt;
    &lt;div id=&quot;mtpnx-out&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;Data: Arize + Fireworks (2,400 runs) and Faros AI (211 tasks). Figures reproduced as published.&lt;/span&gt;
    &lt;span class=&quot;brand&quot;&gt;Built by Marktechpost&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var root=document.getElementById('mtpnx');
  var out=document.getElementById('mtpnx-out');
  var cmdEcho=document.getElementById('mtpnx-cmd');
  var tabs=root.querySelectorAll('.cmd');

  var arize=[
    {m:'gpt-oss-120b',p:'Fireworks',open:1,pass:33,att:0.018,succ:0.054,rt:3.0},
    {m:'gemini-3.1-flash-lite',p:'Google',open:0,pass:40,att:0.026,succ:0.063,rt:2.5},
    {m:'Kimi K2.6',p:'Fireworks',open:1,pass:42,att:0.163,succ:0.384,rt:2.4},
    {m:'GLM-5.2',p:'Fireworks',open:1,pass:42,att:0.208,succ:0.500,rt:2.4},
    {m:'DeepSeek V4 Pro',p:'Fireworks',open:1,pass:39,att:0.230,succ:0.588,rt:2.6},
    {m:'GPT-5.5',p:'OpenAI',open:0,pass:67,att:0.424,succ:0.636,rt:1.5},
    {m:'Kimi K3',p:'Fireworks',open:1,pass:66,att:0.441,succ:0.670,rt:1.5},
    {m:'GPT-5',p:'OpenAI',open:0,pass:41,att:0.317,succ:0.769,rt:2.4},
    {m:'Claude Sonnet 5',p:'Anthropic',open:0,pass:49,att:0.494,succ:1.014,rt:2.1},
    {m:'gemini-3.5-flash',p:'Google',open:0,pass:23,att:0.288,succ:1.233,rt:4.3}
  ];
  var faros=[
    {r:'CG',n:'Claude Code + GLM-5.2',s:0.568,c:89.7,t:321,cost:0.92},
    {r:'CK',n:'Claude Code + Kimi K2.6',s:0.566,c:90.0,t:386,cost:1.78},
    {r:'OG',n:'OpenCode + GLM-5.2',s:0.556,c:95.5,t:620,cost:1.01},
    {r:'CO',n:'Claude Code + Opus 4.8',s:0.521,c:99.7,t:775,cost:1.76},
    {r:'OK',n:'OpenCode + Kimi K2.6',s:0.514,c:94.8,t:557,cost:0.86},
    {r:'Codex',n:'Codex + GPT-5.5',s:0.466,c:92.8,t:392,cost:2.06},
    {r:'OO',n:'OpenCode + Opus 4.8',s:0.454,c:100.0,t:544,cost:1.05}
  ];
  var diff=[
    {m:'gpt-oss-120b',e:65,md:32,h:14},
    {m:'gemini-3.1-flash-lite',e:69,md:48,h:8},
    {m:'Kimi K2.6',e:73,md:43,h:21},
    {m:'GPT-5.5',e:69,md:75,h:51},
    {m:'Kimi K3',e:100,md:72,h:32}
  ];
  var stack=[
    {n:'01',t:'Enterprise controls',d:'Budgets, ROI tracking and policy in one console.',
     b:['Set budgets at team or company level','Track ROI across models and tools','US-hosted endpoints and zero data retention','Runs on Fireworks inference across 20 global data centers']},
    {n:'02',t:'Workflow continuity',d:'FireConnect keeps Claude Code, Codex and OpenCode intact.',
     b:['One-line install, Apache 2.0 licensed','Maps harness model slots to Fireworks models','Anthropic- and OpenAI-compatible serverless APIs','Most tools connect with a base URL and a model ID']},
    {n:'03',t:'Intelligent routing',d:'A trained scorer grades each request, then picks the rung.',
     b:['Routine work goes to an open-weight model on Fireworks','Hard work passes through on your own provider key','Your key is never stored server-side','Research preview: Claude Opus 5 to GLM-5.2, or K3 to GLM-5.2']}
  ];

  var sortKey='succ', sortAsc=true;

  function bar(v,max,grey){
    var w=Math.max(2,Math.round((v/max)*100));
    return '&lt;div class=&quot;barwrap&quot;&gt;&lt;div class=&quot;barbg&quot;&gt;&lt;div class=&quot;barfill'+(grey?' g':'')+'&quot; style=&quot;width:'+w+'%&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;';
  }
  function usd(v){return '$'+v.toFixed(3);}

  function renderStack(){
    var h='&lt;h4&gt;What ships in Fireworks Nexus&lt;/h4&gt;&lt;p&gt;Three parts. Select one to expand the detail.&lt;/p&gt;&lt;div class=&quot;grid3&quot;&gt;';
    stack.forEach(function(s,i){
      h+='&lt;div class=&quot;box'+(i===0?' on':'')+'&quot; data-i=&quot;'+i+'&quot; tabindex=&quot;0&quot; role=&quot;button&quot;&gt;&lt;div class=&quot;n&quot;&gt;'+s.n+'&lt;/div&gt;&lt;div class=&quot;t&quot;&gt;'+s.t+'&lt;/div&gt;&lt;div class=&quot;d&quot;&gt;'+s.d+'&lt;/div&gt;&lt;/div&gt;';
    });
    h+='&lt;/div&gt;&lt;div class=&quot;detail&quot; id=&quot;mtpnx-det&quot;&gt;&lt;/div&gt;';
    h+='&lt;div class=&quot;note&quot;&gt;Component descriptions follow the Fireworks Nexus announcement of 26 July 2026. The router is a research preview.&lt;/div&gt;';
    out.innerHTML=h;
    paintDetail(0);
    root.querySelectorAll('.box').forEach(function(b){
      var go=function(){
        root.querySelectorAll('.box').forEach(function(x){x.classList.remove('on');});
        b.classList.add('on'); paintDetail(+b.getAttribute('data-i'));
      };
      b.addEventListener('click',go);
      b.addEventListener('keydown',function(e){if(e.key==='Enter'||e.key===' '){e.preventDefault();go();}});
    });
  }
  function paintDetail(i){
    var s=stack[i],d=document.getElementById('mtpnx-det');
    d.innerHTML='&lt;span style=&quot;color:#ff7a18&quot;&gt;&amp;#9656; '+s.t+'&lt;/span&gt;&lt;ul&gt;&lt;li&gt;'+s.b.join('&lt;/li&gt;&lt;li&gt;')+'&lt;/li&gt;&lt;/ul&gt;';
    size();
  }

  function renderArize(){
    var max=1.233;
    var rows=arize.slice().sort(function(a,b){
      var x=a[sortKey],y=b[sortKey];
      if(typeof x==='string'){return sortAsc?x.localeCompare(y):y.localeCompare(x);}
      return sortAsc?x-y:y-x;
    });
    var h='&lt;h4&gt;Cost per successful task &amp;#8212; 2,400 runs&lt;/h4&gt;';
    h+='&lt;p&gt;40 Terminal-Bench tasks, 10 models, 6 trials each. Click a column to re-sort.&lt;/p&gt;&lt;div class=&quot;scroll&quot;&gt;&lt;table&gt;&lt;thead&gt;&lt;tr&gt;';
    [['m','Model'],['pass','Pass rate'],['att','$/attempt'],['succ','$/success'],['rt','Retry tax']].forEach(function(c){
      h+='&lt;th data-k=&quot;'+c[0]+'&quot;&gt;'+c[1]+(sortKey===c[0]?' &lt;span class=&quot;ar&quot;&gt;'+(sortAsc?'&amp;#9650;':'&amp;#9660;')+'&lt;/span&gt;':'')+'&lt;/th&gt;';
    });
    h+='&lt;th&gt;Relative cost per success&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;';
    rows.forEach(function(r){
      h+='&lt;tr'+(r.open?' class=&quot;hl&quot;':'')+'&gt;&lt;td class=&quot;mdl&quot;&gt;'+r.m+'&lt;span class=&quot;tag'+(r.open?' o':'')+'&quot;&gt;'+(r.open?'open':r.p)+'&lt;/span&gt;&lt;/td&gt;'+
         '&lt;td class=&quot;num&quot;&gt;'+r.pass+'%&lt;/td&gt;&lt;td&gt;'+usd(r.att)+'&lt;/td&gt;&lt;td class=&quot;num&quot;&gt;'+usd(r.succ)+'&lt;/td&gt;&lt;td&gt;'+r.rt.toFixed(1)+'&amp;#215;&lt;/td&gt;&lt;td&gt;'+bar(r.succ,max)+'&lt;/td&gt;&lt;/tr&gt;';
    });
    h+='&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;';
    h+='&lt;div class=&quot;note&quot;&gt;Cost per successful task = all spend across every attempt, divided by completed tasks. Highlighted rows are open-weight models served by Fireworks. Prices as of July 2026; pass rates carry roughly a &amp;#177;6 point 95% confidence interval.&lt;/div&gt;';
    out.innerHTML=h;
    out.querySelectorAll('th[data-k]').forEach(function(th){
      th.addEventListener('click',function(){
        var k=th.getAttribute('data-k');
        if(k===sortKey){sortAsc=!sortAsc;}else{sortKey=k;sortAsc=(k!=='pass');}
        renderArize();
      });
    });
    size();
  }

  function renderDiff(){
    var h='&lt;h4&gt;Where the frontier premium actually goes&lt;/h4&gt;&lt;p&gt;Pass rate split by task difficulty. Same runs, different question.&lt;/p&gt;&lt;div class=&quot;scroll&quot;&gt;&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Model&lt;/th&gt;&lt;th&gt;Easy&lt;/th&gt;&lt;th&gt;&lt;/th&gt;&lt;th&gt;Medium&lt;/th&gt;&lt;th&gt;&lt;/th&gt;&lt;th&gt;Hard&lt;/th&gt;&lt;th&gt;&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;';
    diff.forEach(function(r){
      var fr=(r.m==='GPT-5.5'||r.m==='Kimi K3');
      h+='&lt;tr'+(fr?' class=&quot;hl&quot;':'')+'&gt;&lt;td class=&quot;mdl&quot;&gt;'+r.m+'&lt;/td&gt;'+
         '&lt;td class=&quot;num&quot;&gt;'+r.e+'%&lt;/td&gt;&lt;td&gt;'+bar(r.e,100,!fr)+'&lt;/td&gt;'+
         '&lt;td class=&quot;num&quot;&gt;'+r.md+'%&lt;/td&gt;&lt;td&gt;'+bar(r.md,100,!fr)+'&lt;/td&gt;'+
         '&lt;td class=&quot;num&quot;&gt;'+r.h+'%&lt;/td&gt;&lt;td&gt;'+bar(r.h,100,!fr)+'&lt;/td&gt;&lt;/tr&gt;';
    });
    h+='&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;';
    h+='&lt;div class=&quot;note&quot;&gt;On easy tasks the cheap models are competitive: Kimi K2.6 passes 73% where GPT-5.5 passes 69%. On hard tasks only the top tier holds up. That gap is the whole argument for routing by difficulty instead of buying one model for every job.&lt;/div&gt;';
    out.innerHTML=h; size();
  }

  function renderFaros(){
    var maxc=2.06;
    var h='&lt;h4&gt;211 real engineering tasks, 7 model-and-harness routes&lt;/h4&gt;';
    h+='&lt;p&gt;Faros AI replayed historical pull requests across 12 repositories. Judge score is a model-based rubric.&lt;/p&gt;&lt;div class=&quot;scroll&quot;&gt;&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Route&lt;/th&gt;&lt;th&gt;Judge score&lt;/th&gt;&lt;th&gt;Cache share&lt;/th&gt;&lt;th&gt;Avg time&lt;/th&gt;&lt;th&gt;Avg $/task&lt;/th&gt;&lt;th&gt;Relative cost&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;';
    faros.forEach(function(r){
      var hi=(r.r==='CG');
      h+='&lt;tr'+(hi?' class=&quot;hl&quot;':'')+'&gt;&lt;td class=&quot;mdl&quot;&gt;'+r.n+'&lt;/td&gt;&lt;td class=&quot;num&quot;&gt;'+r.s.toFixed(3)+'&lt;/td&gt;&lt;td&gt;'+r.c.toFixed(1)+'%&lt;/td&gt;&lt;td&gt;'+r.t+'s&lt;/td&gt;&lt;td class=&quot;num&quot;&gt;$'+r.cost.toFixed(2)+'&lt;/td&gt;&lt;td&gt;'+bar(r.cost,maxc,!hi)+'&lt;/td&gt;&lt;/tr&gt;';
    });
    h+='&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;';
    h+='&lt;div class=&quot;note&quot;&gt;Claude Code on GLM-5.2 scored 0.568 against 0.521 for Claude Code on Opus 4.8, at $0.92 per task against $1.76. Cache share was near-identical between the two top routes, so the cost gap is not a caching artifact. Results are cohort-specific, not a universal leaderboard.&lt;/div&gt;';
    out.innerHTML=h; size();
  }

  function renderRoute(){
    var pol=[
      {n:'gpt-oss-120b alone',s:'8 / 40',c:0.054},
      {n:'GPT-5.5 alone (best single model)',s:'25 / 40',c:0.636},
      {n:'Oracle routing (ceiling, needs hindsight)',s:'34 / 40',c:0.228},
      {n:'Escalation ladder (deployable)',s:'32.3 / 40',c:0.525},
      {n:'Naive escalation through all 10 models',s:'34.5 / 40',c:1.319}
    ];
    var h='&lt;h4&gt;Measured routing policies&lt;/h4&gt;&lt;p&gt;Simulated over the same 2,400 real runs. Coverage counts tasks solved in at least 4 of 6 trials.&lt;/p&gt;&lt;div class=&quot;scroll&quot;&gt;&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Policy&lt;/th&gt;&lt;th&gt;Reliably solves&lt;/th&gt;&lt;th&gt;$/successful task&lt;/th&gt;&lt;th&gt;&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;';
    pol.forEach(function(p){
      var hi=p.n.indexOf('Escalation')===0;
      h+='&lt;tr'+(hi?' class=&quot;hl&quot;':'')+'&gt;&lt;td class=&quot;mdl&quot;&gt;'+p.n+'&lt;/td&gt;&lt;td&gt;'+p.s+'&lt;/td&gt;&lt;td class=&quot;num&quot;&gt;$'+p.c.toFixed(3)+'&lt;/td&gt;&lt;td&gt;'+bar(p.c,1.319,!hi)+'&lt;/td&gt;&lt;/tr&gt;';
    });
    h+='&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;';
    h+='&lt;div class=&quot;sim&quot;&gt;&lt;div class=&quot;lbl&quot;&gt;Illustrative blend &amp;#8212; drag to set the share of work handled by the cheap rung&lt;/div&gt;'+
        '&lt;div class=&quot;slrow&quot; style=&quot;margin-top:10px&quot;&gt;&lt;span class=&quot;lbl&quot;&gt;cheap&lt;/span&gt;&lt;input id=&quot;mtpnx-sl&quot; type=&quot;range&quot; min=&quot;0&quot; max=&quot;100&quot; value=&quot;70&quot; aria-label=&quot;Share of tasks routed to the cheap model&quot;&gt;&lt;span class=&quot;lbl&quot;&gt;frontier&lt;/span&gt;&lt;/div&gt;'+
        '&lt;div class=&quot;slrow&quot; style=&quot;margin-top:12px&quot;&gt;'+
        '&lt;div&gt;&lt;div class=&quot;lbl&quot;&gt;Routed cheap&lt;/div&gt;&lt;div class=&quot;big&quot; id=&quot;mtpnx-share&quot;&gt;70%&lt;/div&gt;&lt;/div&gt;'+
        '&lt;div&gt;&lt;div class=&quot;lbl&quot;&gt;Blended $/success&lt;/div&gt;&lt;div class=&quot;big&quot; id=&quot;mtpnx-cost&quot;&gt;$0.229&lt;/div&gt;&lt;/div&gt;'+
        '&lt;div&gt;&lt;div class=&quot;lbl&quot;&gt;vs GPT-5.5 alone&lt;/div&gt;&lt;div class=&quot;big&quot; id=&quot;mtpnx-save&quot;&gt;2.8&amp;#215; lower&lt;/div&gt;&lt;/div&gt;'+
        '&lt;/div&gt;&lt;/div&gt;';
    h+='&lt;div class=&quot;note&quot;&gt;The slider is a straight linear blend of two measured endpoints, $0.054 for gpt-oss-120b and $0.636 for GPT-5.5. It is an illustration of the arithmetic, not a measured result, and it ignores coverage: the cheap model reliably solves only 8 of 40 tasks on its own. Fireworks quotes a 3&amp;#8211;5&amp;#215; cost reduction for its own router, which is in research preview.&lt;/div&gt;';
    out.innerHTML=h;
    var sl=document.getElementById('mtpnx-sl');
    function upd(){
      var f=+sl.value/100, c=0.054*f+0.636*(1-f);
      document.getElementById('mtpnx-share').textContent=sl.value+'%';
      document.getElementById('mtpnx-cost').textContent='$'+c.toFixed(3);
      document.getElementById('mtpnx-save').textContent=(0.636/c).toFixed(1)+'\u00d7 lower';
    }
    sl.addEventListener('input',upd); upd(); size();
  }

  var views={stack:renderStack,arize:renderArize,diff:renderDiff,faros:renderFaros,route:renderRoute};

  tabs.forEach(function(t){
    t.addEventListener('click',function(){
      tabs.forEach(function(x){x.setAttribute('aria-selected','false');});
      t.setAttribute('aria-selected','true');
      cmdEcho.textContent=t.textContent.replace(/^\$/,'').trim();
      sortKey='succ'; sortAsc=true;
      views[t.getAttribute('data-v')]();
    });
  });

  function size(){
    var h=(root.offsetHeight||600)+40;
    try{ if(window.frameElement){window.frameElement.style.height=h+'px';} }catch(e){}
    try{ parent.postMessage({mtpEmbed:'nexus-explainer',height:h},'*'); }catch(e){}
  }
  window.addEventListener('resize',size);
  renderStack();
  setTimeout(size,120); setTimeout(size,600);
})();
&lt;/script&gt;
" scrolling="no" loading="lazy" style="width:100%;height:760px;border:0;display:block;overflow:hidden;background:#0a0a0a;border-radius:10px;"></iframe>
<script>
(function(){
  var f=document.getElementById('mtp-nexus-explainer');
  window.addEventListener('message',function(e){
    if(e&&e.data&&e.data.mtpEmbed==='nexus-explainer'&&e.data.height){ f.style.height=e.data.height+'px'; }
  },false);
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-performance" class="wp-block-heading"><strong>Performance</strong></h2>



<p class="wp-block-paragraph">Fireworks research team has been testing Nexus with dev teams including Notion and Doximity. Preliminary results show a one-third reduction in cost per merged pull request. The research team also reports a blended token rate roughly a quarter of the closed model labs. These are vendor figures from a preview program, and should be read that way.</p>



<p class="wp-block-paragraph"><strong>The more useful evidence comes from two independent evaluations Fireworks cites</strong>.</p>



<p class="wp-block-paragraph"><a href="https://www.faros.ai/blog/open-models-vs-frontier-models">Faros AI</a> ran 211 real engineering tasks drawn from 12 repositories across seven model-and-harness routes. Claude Code on GLM-5.2 scored 0.568 on a model-based rubric judge. Claude Code on Opus 4.8 scored 0.521. The cost gap was larger than the quality gap: $0.92 per task against $1.76. Cache share was 89.7% and 99.7% respectively, so caching does not explain the result. Faros stresses that the cohort is company-specific and not a universal leaderboard.</p>



<p class="wp-block-paragraph"><a href="https://arize.com/blog/cost-per-successful-task-ai-model-benchmark">Arize</a>, in a joint study with Fireworks, evaluated 10 models on 40 Terminal-Bench tasks at six trials each. That is 2,400 runs and $626 of API spend, graded by each task&#8217;s own test suite. The headline metric is cost per successful task, which counts every failed and retried attempt.</p>



<p class="wp-block-paragraph">Two findings from that study bear directly on the Nexus design. On easy tasks the frontier premium buys nothing: Kimi K2.6 passed 73% where GPT-5.5 passed 69%. On hard tasks only the top tier competes, with GPT-5.5 at 51% and Kimi K3 at 32%. Arize&#8217;s evaluation harness is <a href="https://github.com/Arize-ai/fireworks-cost-benchmark">open source</a>, so teams can rerun it on their own workloads.</p>



<p class="wp-block-paragraph">Routing simulated over those runs beat every single-model strategy. A deliberate escalation ladder reached $0.525 per successful task while reliably solving 32.3 of 40 tasks. GPT-5.5 alone cost $0.636 and reliably solved 25 of 40. Naive escalation through all ten models cost $1.319, worse than any single model tested. Ladder design is not optional.</p>



<h2 id="h-how-teams-actually-turn-it-on" class="wp-block-heading"><strong>How teams actually turn it on</strong></h2>



<p class="wp-block-paragraph">There are three practical paths, and they have different blast radii.</p>



<p class="wp-block-paragraph">The FireConnect path is the lowest friction. The installer requires the Claude Code CLI already on <code>PATH</code>, prompts once for a Fireworks API key, registers a plugin marketplace, and writes Claude Code settings with a timestamped backup. It exposes <code>/fireconnect:on</code>, <code>/fireconnect:off</code>, <code>/fireconnect:setup</code>, <code>/fireconnect:models</code> and <code>/fireconnect:set-models</code>.</p>



<p class="wp-block-paragraph">The second path skips the plugin entirely. Set <code>ANTHROPIC_BASE_URL</code> and a Fireworks key, or use the <a href="https://docs.fireworks.ai/tools-sdks/openai-compatibility">OpenAI-compatible</a> client with a changed base URL and model ID. Arize noted that adding a model to its harness was one config line. That interchangeability is what makes an escalation ladder buildable.</p>



<p class="wp-block-paragraph">The third path is the router itself, which sits in front of an existing frontier contract. </p>



<iframe id="mtp-nexus-deploy" title="Fireworks Nexus: step-by-step deployment paths" srcdoc="&lt;style&gt;
  :root{
    --bg:#0a0a0a; --panel:#111111; --panel2:#161616; --line:#272727;
    --fg:#ededed; --dim:#8b8b8b; --amber:#ff7a18; --amber2:#ffa04d; --white:#ffffff;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  #mtpdp{
    font-family:ui-monospace,SFMono-Regular,&quot;JetBrains Mono&quot;,&quot;Roboto Mono&quot;,Menlo,Consolas,monospace;
    background:var(--bg); color:var(--fg); border:1px solid var(--line); border-radius:10px;
    overflow:hidden; font-size:13px; line-height:1.55; -webkit-font-smoothing:antialiased;
  }
  #mtpdp .bar{display:flex;align-items:center;gap:8px;padding:9px 12px;background:#0d0d0d;border-bottom:1px solid var(--line)}
  #mtpdp .dot{width:10px;height:10px;border-radius:50%;background:#2e2e2e}
  #mtpdp .dot.a{background:var(--amber)}
  #mtpdp .barname{margin-left:8px;color:var(--dim);font-size:11px;letter-spacing:.06em;text-transform:uppercase}
  #mtpdp .cmds{display:flex;flex-wrap:wrap;gap:6px;padding:12px 12px 4px;background:var(--panel)}
  #mtpdp .cmd{appearance:none;background:transparent;border:1px solid var(--line);color:var(--dim);font:inherit;font-size:12px;padding:6px 10px;border-radius:6px;cursor:pointer;transition:.15s}
  #mtpdp .cmd:hover{border-color:#3d3d3d;color:var(--fg)}
  #mtpdp .cmd:focus-visible{outline:2px solid var(--amber);outline-offset:2px}
  #mtpdp .cmd[aria-selected=&quot;true&quot;]{border-color:var(--amber);color:var(--amber);background:rgba(255,122,24,.08)}
  #mtpdp .screen{padding:14px 14px 18px;background:var(--panel)}
  #mtpdp .lede{color:#c2c2c2;font-size:12.5px;margin-bottom:14px}
  #mtpdp .lede b{color:var(--white);font-weight:600}
  #mtpdp .steps{display:flex;gap:6px;margin-bottom:14px;flex-wrap:wrap}
  #mtpdp .pip{flex:1;min-width:44px;height:3px;background:#242424;border-radius:2px;border:0;padding:0;cursor:pointer}
  #mtpdp .pip.on{background:var(--amber)}
  #mtpdp .pip.done{background:#5c3411}
  #mtpdp .pip:focus-visible{outline:2px solid var(--amber);outline-offset:3px}
  #mtpdp .stepno{color:var(--amber);font-size:11px;letter-spacing:.12em;text-transform:uppercase}
  #mtpdp h4{color:var(--white);font-size:14.5px;margin:6px 0 8px;font-weight:600;letter-spacing:-.01em}
  #mtpdp p{color:#c2c2c2;font-size:12.5px;margin:0 0 10px}
  #mtpdp .codewrap{position:relative;margin:10px 0}
  #mtpdp pre{background:#0d0d0d !important;border:1px solid #232323;border-left:2px solid var(--amber);border-radius:6px;padding:12px 60px 12px 12px !important;overflow-x:auto;color:#e8e8e8 !important;font-size:12px !important;line-height:1.6;white-space:pre}
  #mtpdp code{background:transparent !important;color:inherit !important;font-family:inherit !important;font-size:inherit !important;padding:0 !important}
  #mtpdp .cp{position:absolute;top:8px;right:8px;background:#1a1a1a;border:1px solid #2e2e2e;color:#9a9a9a;font:inherit;font-size:10.5px;padding:3px 8px;border-radius:4px;cursor:pointer}
  #mtpdp .cp:hover{border-color:var(--amber);color:var(--amber)}
  #mtpdp .cp:focus-visible{outline:2px solid var(--amber);outline-offset:2px}
  #mtpdp ul{margin:4px 0 10px 17px;color:#b4b4b4;font-size:12px}
  #mtpdp li{margin:3px 0}
  #mtpdp .note{color:#8a8a8a;font-size:11.5px;border-left:2px solid #333;padding-left:9px;margin-top:12px}
  #mtpdp .warn{border-left-color:var(--amber);color:#d3a679}
  #mtpdp .nav{display:flex;gap:8px;margin-top:16px;align-items:center;flex-wrap:wrap}
  #mtpdp .nav button{background:transparent;border:1px solid var(--line);color:var(--fg);font:inherit;font-size:12px;padding:7px 14px;border-radius:6px;cursor:pointer}
  #mtpdp .nav button:hover:not(:disabled){border-color:var(--amber);color:var(--amber)}
  #mtpdp .nav button:disabled{opacity:.35;cursor:not-allowed}
  #mtpdp .nav .next{background:var(--amber);border-color:var(--amber);color:#0a0a0a;font-weight:600}
  #mtpdp .nav .next:hover:not(:disabled){background:var(--amber2);border-color:var(--amber2);color:#0a0a0a}
  #mtpdp .nav .cnt{margin-left:auto;color:#666;font-size:11px}
  #mtpdp table{width:100%;border-collapse:collapse;font-size:12px;margin:8px 0}
  #mtpdp th{text-align:left;color:#7c7c7c;font-weight:500;font-size:11px;letter-spacing:.05em;text-transform:uppercase;padding:6px 8px;border-bottom:1px solid var(--line)}
  #mtpdp td{padding:7px 8px;border-bottom:1px solid #1b1b1b;color:#d0d0d0}
  #mtpdp td b{color:var(--amber2);font-weight:500}
  #mtpdp .scroll{overflow-x:auto}
  #mtpdp .ft{display:flex;flex-wrap:wrap;gap:8px;justify-content:space-between;align-items:center;padding:10px 14px;background:#0d0d0d;border-top:1px solid var(--line);font-size:11px;color:#666}
  #mtpdp .ft a{color:var(--amber);text-decoration:none}
  #mtpdp .ft a:hover{text-decoration:underline}
  #mtpdp .brand{color:var(--amber);letter-spacing:.04em}
  @media (max-width:640px){
    #mtpdp{font-size:12px}
    #mtpdp .screen{padding:12px 10px 14px}
    #mtpdp .cmd{font-size:11px;padding:5px 8px}
    #mtpdp pre{font-size:11px !important;padding:12px 12px 34px 12px !important}
    #mtpdp .cp{top:auto;bottom:6px;right:6px}
    #mtpdp h4{font-size:13.5px}
    #mtpdp th,#mtpdp td{padding:6px;font-size:11px}
    #mtpdp .nav button{flex:1}
  }
  @media (prefers-reduced-motion:reduce){#mtpdp *{transition:none !important}}
&lt;/style&gt;

&lt;div id=&quot;mtpdp&quot;&gt;
  &lt;div class=&quot;bar&quot;&gt;
    &lt;span class=&quot;dot a&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot&quot;&gt;&lt;/span&gt;
    &lt;span class=&quot;barname&quot;&gt;fireworks-nexus &amp;#8212; deployment paths&lt;/span&gt;
  &lt;/div&gt;

  &lt;div class=&quot;cmds&quot; role=&quot;tablist&quot; aria-label=&quot;Deployment paths&quot;&gt;
    &lt;button class=&quot;cmd&quot; role=&quot;tab&quot; aria-selected=&quot;true&quot; data-p=&quot;0&quot;&gt;Path A &amp;#183; FireConnect + Claude Code&lt;/button&gt;
    &lt;button class=&quot;cmd&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-p=&quot;1&quot;&gt;Path B &amp;#183; Base URL + model ID&lt;/button&gt;
    &lt;button class=&quot;cmd&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-p=&quot;2&quot;&gt;Path C &amp;#183; Router (research preview)&lt;/button&gt;
  &lt;/div&gt;

  &lt;div class=&quot;screen&quot;&gt;
    &lt;div class=&quot;lede&quot; id=&quot;mtpdp-lede&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;steps&quot; id=&quot;mtpdp-pips&quot;&gt;&lt;/div&gt;
    &lt;div id=&quot;mtpdp-body&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;nav&quot;&gt;
      &lt;button id=&quot;mtpdp-prev&quot;&gt;&amp;#8592; Back&lt;/button&gt;
      &lt;button id=&quot;mtpdp-next&quot; class=&quot;next&quot;&gt;Next &amp;#8594;&lt;/button&gt;
      &lt;span class=&quot;cnt&quot; id=&quot;mtpdp-cnt&quot;&gt;&lt;/span&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;Commands and settings reproduced from the open-source &lt;a href=&quot;https://github.com/fw-ai/fireconnect&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;fw-ai/fireconnect&lt;/a&gt; repository and Fireworks docs.&lt;/span&gt;
    &lt;span class=&quot;brand&quot;&gt;Built by Marktechpost&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var root=document.getElementById('mtpdp');
  var body=document.getElementById('mtpdp-body');
  var pips=document.getElementById('mtpdp-pips');
  var lede=document.getElementById('mtpdp-lede');
  var cnt=document.getElementById('mtpdp-cnt');
  var prev=document.getElementById('mtpdp-prev');
  var next=document.getElementById('mtpdp-next');
  var tabs=root.querySelectorAll('.cmd');

  var paths=[
    {
      lede:'&lt;b&gt;Path A.&lt;/b&gt; Point an existing Claude Code install at Fireworks-hosted open models. Nothing about the harness changes.',
      steps:[
        {t:'Install Claude Code first',
         h:'&lt;p&gt;FireConnect does not install or upgrade Claude Code. The CLI has to be on your PATH before you start.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Installation guide: &lt;a style=&quot;color:#ffa04d&quot; href=&quot;https://docs.claude.com/en/docs/claude-code/overview&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Claude Code docs&lt;/a&gt;&lt;/li&gt;&lt;li&gt;Node.js is used to write settings. If it is missing, the installer asks before adding it via Homebrew or apt.&lt;/li&gt;&lt;/ul&gt;'},
        {t:'Create a Fireworks API key',
         h:'&lt;p&gt;Generate a key in the Fireworks dashboard. Keys are prefixed &lt;code&gt;fw_&lt;/code&gt;.&lt;/p&gt;',
         code:'https://app.fireworks.ai/settings/users/api-keys'},
        {t:'Run the one-line installer',
         h:'&lt;p&gt;Apache 2.0 licensed. It registers the plugin marketplace, installs the FireConnect plugin, applies the default model mapping and writes Claude Code settings.&lt;/p&gt;',
         code:'sh -c &quot;$(curl -fsSL https://raw.githubusercontent.com/fw-ai/fireconnect/main/install.sh)&quot;'},
        {t:'Or install non-interactively',
         h:'&lt;p&gt;Pass the key through the environment for scripted or fleet-wide rollout.&lt;/p&gt;',
         code:'curl -fsSL https://raw.githubusercontent.com/fw-ai/fireconnect/main/install.sh \\\n  | FIREWORKS_API_KEY=&quot;fw_...&quot; bash'},
        {t:'Or set it up manually inside Claude Code',
         h:'&lt;p&gt;Add the marketplace, then install the plugin from it. Paste the key when prompted, accept the default base URL, restart.&lt;/p&gt;',
         code:'/plugin marketplace add https://github.com/fw-ai/fireconnect.git\n/plugin install fireconnect@fireconnect'},
        {t:'Check what got written',
         h:'&lt;p&gt;FireConnect writes these environment settings, preserving unrelated keys and backing up any existing settings file.&lt;/p&gt;',
         code:'{\n  &quot;env&quot;: {\n    &quot;ANTHROPIC_BASE_URL&quot;: &quot;https://api.fireworks.ai/inference&quot;,\n    &quot;ANTHROPIC_API_KEY&quot;: &quot;fw_YOUR_FIREWORKS_API_KEY&quot;,\n    &quot;ANTHROPIC_AUTH_TOKEN&quot;: &quot;fw_YOUR_FIREWORKS_API_KEY&quot;,\n    &quot;ANTHROPIC_MODEL&quot;: &quot;accounts/fireworks/routers/kimi-k2p6-turbo&quot;,\n    &quot;ANTHROPIC_DEFAULT_OPUS_MODEL&quot;: &quot;accounts/fireworks/routers/kimi-k2p6-turbo&quot;,\n    &quot;ANTHROPIC_DEFAULT_SONNET_MODEL&quot;: &quot;accounts/fireworks/models/glm-5p1&quot;,\n    &quot;ANTHROPIC_DEFAULT_HAIKU_MODEL&quot;: &quot;accounts/fireworks/models/minimax-m2p5&quot;,\n    &quot;CLAUDE_CODE_SUBAGENT_MODEL&quot;: &quot;accounts/fireworks/models/minimax-m2p5&quot;\n  }\n}',
         after:'&lt;div class=&quot;scroll&quot;&gt;&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Harness slot&lt;/th&gt;&lt;th&gt;Default target&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;&lt;tr&gt;&lt;td&gt;opus&lt;/td&gt;&lt;td&gt;&lt;b&gt;kimi-k2p6-turbo&lt;/b&gt;&lt;/td&gt;&lt;/tr&gt;&lt;tr&gt;&lt;td&gt;sonnet&lt;/td&gt;&lt;td&gt;&lt;b&gt;glm-5p1&lt;/b&gt;&lt;/td&gt;&lt;/tr&gt;&lt;tr&gt;&lt;td&gt;haiku&lt;/td&gt;&lt;td&gt;&lt;b&gt;minimax-m2p5&lt;/b&gt;&lt;/td&gt;&lt;/tr&gt;&lt;tr&gt;&lt;td&gt;subagent&lt;/td&gt;&lt;td&gt;&lt;b&gt;minimax-m2p5&lt;/b&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;&lt;div class=&quot;note&quot;&gt;This is the default mapping documented in the repository. Short model IDs are accepted everywhere and expanded on write, so &lt;code&gt;kimi-k2p6-turbo&lt;/code&gt; becomes &lt;code&gt;accounts/fireworks/routers/kimi-k2p6-turbo&lt;/code&gt;.&lt;/div&gt;'},
        {t:'Restart, then toggle routing on and off',
         h:'&lt;p&gt;Fully restart Claude Code and send any message to confirm. The plugin exposes five slash commands.&lt;/p&gt;',
         code:'/fireconnect:on\n/fireconnect:off\n/fireconnect:setup\n/fireconnect:models\n/fireconnect:set-models',
         after:'&lt;div class=&quot;note&quot;&gt;&lt;code&gt;/fireconnect:off&lt;/code&gt; removes Fireworks routing and restores the previous provider variables when a backup exists.&lt;/div&gt;'},
        {t:'Recover from the terminal if the slash commands fail',
         h:'&lt;p&gt;A standalone CLI ships in the same repository for when Claude Code cannot run plugin commands.&lt;/p&gt;',
         code:'fireconnect list\nfireconnect status\nfireconnect set --main kimi-k2p6-turbo --haiku minimax-m2p5\nfireconnect on --api-key fw_...\nfireconnect off\nfireconnect reset',
         after:'&lt;div class=&quot;note warn&quot;&gt;If an older install registered a marketplace source under &lt;code&gt;/tmp&lt;/code&gt;, rerun the installer to replace it with a durable source before relying on &lt;code&gt;/fireconnect:on&lt;/code&gt; after a reboot.&lt;/div&gt;'}
      ]
    },
    {
      lede:'&lt;b&gt;Path B.&lt;/b&gt; Skip the plugin. Fireworks serverless APIs are Anthropic- and OpenAI-compatible, so most harnesses connect with a base URL and a model ID.',
      steps:[
        {t:'Point an Anthropic-compatible harness at Fireworks',
         h:'&lt;p&gt;Set two environment variables. Any tool that reads the standard Anthropic variables will follow.&lt;/p&gt;',
         code:'export ANTHROPIC_BASE_URL=&quot;https://api.fireworks.ai/inference&quot;\nexport ANTHROPIC_API_KEY=&quot;fw_YOUR_FIREWORKS_API_KEY&quot;\nexport ANTHROPIC_MODEL=&quot;accounts/fireworks/models/glm-5p1&quot;'},
        {t:'Or use the OpenAI-compatible path',
         h:'&lt;p&gt;One code path, two changes: the base URL and the model ID. Fireworks documents the compatible surface in its &lt;a style=&quot;color:#ffa04d&quot; href=&quot;https://docs.fireworks.ai/tools-sdks/openai-compatibility&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;OpenAI compatibility guide&lt;/a&gt;.&lt;/p&gt;',
         code:'from openai import OpenAI\n\nclient = OpenAI(\n    base_url=&quot;https://api.fireworks.ai/inference/v1&quot;,\n    api_key=&quot;fw_YOUR_FIREWORKS_API_KEY&quot;,\n)\n\nresp = client.chat.completions.create(\n    model=&quot;accounts/fireworks/models/glm-5p1&quot;,\n    messages=[{&quot;role&quot;: &quot;user&quot;, &quot;content&quot;: &quot;Summarize this diff.&quot;}],\n)\nprint(resp.choices[0].message.content)'},
        {t:'Confirm the model ID before you ship it',
         h:'&lt;p&gt;Model IDs and the curated catalog change. Check the live list rather than copying an ID from a blog post.&lt;/p&gt;',
         code:'https://fireworks.ai/models',
         after:'&lt;div class=&quot;note&quot;&gt;Arize noted that adding a model to its benchmark harness was a single config line: a base URL, a model ID and a price. That interchangeability is what makes an escalation ladder practical.&lt;/div&gt;'},
        {t:'Decide what the platform layer has to guarantee',
         h:'&lt;p&gt;Fireworks documents the controls that usually gate an enterprise rollout.&lt;/p&gt;',
         after:'&lt;div class=&quot;scroll&quot;&gt;&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Control&lt;/th&gt;&lt;th&gt;Where it is documented&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;&lt;tr&gt;&lt;td&gt;Regional and US-hosted endpoints&lt;/td&gt;&lt;td&gt;&lt;a style=&quot;color:#ffa04d&quot; href=&quot;https://docs.fireworks.ai/deployments/regions&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;docs.fireworks.ai/deployments/regions&lt;/a&gt;&lt;/td&gt;&lt;/tr&gt;&lt;tr&gt;&lt;td&gt;Zero data retention&lt;/td&gt;&lt;td&gt;&lt;a style=&quot;color:#ffa04d&quot; href=&quot;https://docs.fireworks.ai/guides/security_compliance/data_handling&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Data handling guide&lt;/a&gt;&lt;/td&gt;&lt;/tr&gt;&lt;tr&gt;&lt;td&gt;Budgets, policy and ROI tracking&lt;/td&gt;&lt;td&gt;Nexus console&lt;/td&gt;&lt;/tr&gt;&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;&lt;div class=&quot;note&quot;&gt;Fireworks states that requests run on the same production inference platform across 20 global data centers.&lt;/div&gt;'}
      ]
    },
    {
      lede:'&lt;b&gt;Path C.&lt;/b&gt; The Nexus router sits in front of an existing frontier contract and grades each request before it picks a rung. This component is a research preview.',
      steps:[
        {t:'Understand what the router decides',
         h:'&lt;p&gt;A custom trained model scores request difficulty, then splits traffic.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Routine requests go to a cost-effective open-weight model served by Fireworks.&lt;/li&gt;&lt;li&gt;Difficult requests pass through to your existing provider, on your own key.&lt;/li&gt;&lt;li&gt;Fireworks states that the pass-through key is never stored server-side.&lt;/li&gt;&lt;/ul&gt;',
         after:'&lt;div class=&quot;note&quot;&gt;Fireworks quotes a typical 3&amp;#8211;5&amp;#215; cost reduction from this arrangement, and a 33% drop in cost per merged pull request in preliminary testing with teams including Notion and Doximity. These are vendor figures.&lt;/div&gt;'},
        {t:'Pick one of the two supported pairs',
         h:'&lt;p&gt;Today the research preview routes between one fixed pair, or an all-open pair if you do not want a frontier fallback at all.&lt;/p&gt;',
         after:'&lt;div class=&quot;scroll&quot;&gt;&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Mode&lt;/th&gt;&lt;th&gt;Cheap rung&lt;/th&gt;&lt;th&gt;Escalation rung&lt;/th&gt;&lt;th&gt;Needs your key?&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;&lt;tr&gt;&lt;td&gt;Hybrid&lt;/td&gt;&lt;td&gt;&lt;b&gt;GLM-5.2&lt;/b&gt;&lt;/td&gt;&lt;td&gt;&lt;b&gt;Claude Opus 5&lt;/b&gt;&lt;/td&gt;&lt;td&gt;Yes, an Anthropic key&lt;/td&gt;&lt;/tr&gt;&lt;tr&gt;&lt;td&gt;Fully open&lt;/td&gt;&lt;td&gt;&lt;b&gt;GLM-5.2&lt;/b&gt;&lt;/td&gt;&lt;td&gt;&lt;b&gt;Kimi K3&lt;/b&gt;&lt;/td&gt;&lt;td&gt;No&lt;/td&gt;&lt;/tr&gt;&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;'},
        {t:'Design the ladder deliberately',
         h:'&lt;p&gt;Routing only pays if the rungs are chosen carefully. In the Arize study a naive ladder through all ten models cost $1.319 per successful task, worse than every single model tested.&lt;/p&gt;',
         after:'&lt;div class=&quot;scroll&quot;&gt;&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Policy&lt;/th&gt;&lt;th&gt;Reliably solves&lt;/th&gt;&lt;th&gt;$ / successful task&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;&lt;tr&gt;&lt;td&gt;GPT-5.5 alone&lt;/td&gt;&lt;td&gt;25 / 40&lt;/td&gt;&lt;td&gt;$0.636&lt;/td&gt;&lt;/tr&gt;&lt;tr&gt;&lt;td&gt;Deliberate escalation ladder&lt;/td&gt;&lt;td&gt;32.3 / 40&lt;/td&gt;&lt;td&gt;&lt;b&gt;$0.525&lt;/b&gt;&lt;/td&gt;&lt;/tr&gt;&lt;tr&gt;&lt;td&gt;Naive escalation, all 10 models&lt;/td&gt;&lt;td&gt;34.5 / 40&lt;/td&gt;&lt;td&gt;$1.319&lt;/td&gt;&lt;/tr&gt;&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;'},
        {t:'Route by task class, or escalate on a real signal',
         h:'&lt;p&gt;Escalation assumes you can tell a run failed. Where you cannot, route by request class instead, which needs a classifier rather than a verifier.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Cheap deterministic checks: does it compile, do the tests pass, does the JSON validate, did the SQL run.&lt;/li&gt;&lt;li&gt;Distress signals visible in traces: repeated commands, non-zero exits, a run costing several times the median.&lt;/li&gt;&lt;li&gt;Silent failures finish clean and wrong. Those need a verifier, a judge, or a downstream signal.&lt;/li&gt;&lt;/ul&gt;'},
        {t:'Measure it on your own workload before you commit',
         h:'&lt;p&gt;Both third-party harnesses are public. Run them against your repositories rather than trusting a leaderboard.&lt;/p&gt;',
         code:'git clone https://github.com/Arize-ai/fireworks-cost-benchmark',
         after:'&lt;div class=&quot;note&quot;&gt;Faros published its full methodology for the 211-task run, and Arize open-sourced the agent, the Docker task runner, the grading and the routing analysis. Both stress that results are cohort-specific.&lt;/div&gt;'}
      ]
    }
  ];

  var p=0, s=0;

  function esc(t){return t.replace(/&amp;/g,'&amp;amp;').replace(/&lt;/g,'&amp;lt;').replace(/&gt;/g,'&amp;gt;');}

  function render(){
    var path=paths[p], step=path.steps[s];
    lede.innerHTML=path.lede;

    var ph='';
    path.steps.forEach(function(_,i){
      ph+='&lt;button class=&quot;pip '+(i===s?'on':(i&lt;s?'done':''))+'&quot; data-i=&quot;'+i+'&quot; aria-label=&quot;Step '+(i+1)+'&quot;&gt;&lt;/button&gt;';
    });
    pips.innerHTML=ph;
    pips.querySelectorAll('.pip').forEach(function(b){
      b.addEventListener('click',function(){s=+b.getAttribute('data-i');render();});
    });

    var h='&lt;div class=&quot;stepno&quot;&gt;Step '+(s+1)+' of '+path.steps.length+'&lt;/div&gt;&lt;h4&gt;'+step.t+'&lt;/h4&gt;';
    if(step.h) h+=step.h;
    if(step.code) h+='&lt;div class=&quot;codewrap&quot;&gt;&lt;button class=&quot;cp&quot; type=&quot;button&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;&lt;code&gt;'+esc(step.code)+'&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;';
    if(step.after) h+=step.after;
    body.innerHTML=h;

    var cp=body.querySelector('.cp');
    if(cp){
      cp.addEventListener('click',function(){
        var txt=body.querySelector('pre code').textContent;
        var done=function(){cp.textContent='copied';setTimeout(function(){cp.textContent='copy';},1400);};
        if(navigator.clipboard&amp;&amp;navigator.clipboard.writeText){
          navigator.clipboard.writeText(txt).then(done,function(){fallback(txt,done);});
        }else{fallback(txt,done);}
      });
    }

    prev.disabled=(s===0);
    next.disabled=(s===path.steps.length-1);
    next.textContent=(s===path.steps.length-1)?'Done':'Next \u2192';
    cnt.textContent=path.steps.length+' steps';
    size();
  }

  function fallback(txt,cb){
    var ta=document.createElement('textarea');
    ta.value=txt; ta.setAttribute('readonly','');
    ta.style.position='absolute'; ta.style.left='-9999px';
    document.body.appendChild(ta); ta.select();
    try{document.execCommand('copy');cb();}catch(e){}
    document.body.removeChild(ta);
  }

  prev.addEventListener('click',function(){if(s&gt;0){s--;render();}});
  next.addEventListener('click',function(){if(s&lt;paths[p].steps.length-1){s++;render();}});

  tabs.forEach(function(t){
    t.addEventListener('click',function(){
      tabs.forEach(function(x){x.setAttribute('aria-selected','false');});
      t.setAttribute('aria-selected','true');
      p=+t.getAttribute('data-p'); s=0; render();
    });
  });

  function size(){
    var h=(root.offsetHeight||600)+40;
    try{ if(window.frameElement){window.frameElement.style.height=h+'px';} }catch(e){}
    try{ parent.postMessage({mtpEmbed:'nexus-deploy',height:h},'*'); }catch(e){}
  }
  window.addEventListener('resize',size);
  render();
  setTimeout(size,120); setTimeout(size,600);
})();
&lt;/script&gt;
" scrolling="no" loading="lazy" style="width:100%;height:700px;border:0;display:block;overflow:hidden;background:#0a0a0a;border-radius:10px;"></iframe>
<script>
(function(){
  var f=document.getElementById('mtp-nexus-deploy');
  window.addEventListener('message',function(e){
    if(e&&e.data&&e.data.mtpEmbed==='nexus-deploy'&&e.data.height){ f.style.height=e.data.height+'px'; }
  },false);
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>Independent runs from Faros AI (211 tasks) and Arize (2,400 runs) support routing by difficulty, not by brand.</li>



<li>Fireworks Nexus is a drop-in AI management and routing layer for engineering orgs, announced July 26, 2026.</li>



<li>Three parts ship: enterprise cost controls, FireConnect for workflow continuity, and a difficulty-aware router.</li>



<li>FireConnect is Apache 2.0, installs in one line, and keeps Claude Code, Codex and OpenCode unchanged.</li>



<li>Fireworks quotes 3–5× cost reduction and a 33% drop in cost per merged PR in preliminary customer testing.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"><strong>Sources:</strong> <a href="https://fireworks.ai/blog/fireworks-nexus">Fireworks Nexus announcement</a>, <a href="https://github.com/fw-ai/fireconnect">FireConnect repository</a>, <a href="https://www.faros.ai/blog/open-models-vs-frontier-models">Faros AI evaluation</a> and <a href="https://arize.com/blog/cost-per-successful-task-ai-model-benchmark">Arize cost-per-successful-task benchmark</a></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/28/fireworks-ai-releases-fireworks-nexus-a-drop-in-routing-and-cost-control-layer-that-moves-routine-coding-work-to-open-weight-models/">Fireworks AI Releases Fireworks Nexus: A Drop-In Routing and Cost-Control Layer That Moves Routine Coding Work to Open-Weight Models</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81406</post-id>
	</item>
		<item>
		<title>Microsoft AI Releases MAI-Cyber-1-Flash: A 5B-Active-Parameter Cyber Model That Pushes MDASH to 95.95% on CyberGym</title>
		<link>https://www.marktechpost.com/2026/07/28/microsoft-ai-releases-mai-cyber-1-flash-a-5b-active-parameter-cyber-model-that-pushes-mdash-to-95-95-on-cybergym/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Tue, 28 Jul 2026 08:33:30 +0000</pubDate>
				<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Python]]></category>
		<category><![CDATA[Security]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81401</guid>

					<description><![CDATA[<p>Microsoft AI has released MAI-Cyber-1-Flash, its first model built specifically for cyber defense. It is a 137B total, 5B active sparse MoE fine-tune of MAI-Code-1-Flash with a 256k context window. The model does not ship as a standalone endpoint — it runs inside MDASH, Microsoft's multi-model agentic scanning harness, where it handles up to 90% of tasks and pushes the system to 95.95% on CyberGym.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/28/microsoft-ai-releases-mai-cyber-1-flash-a-5b-active-parameter-cyber-model-that-pushes-mdash-to-95-95-on-cybergym/">Microsoft AI Releases MAI-Cyber-1-Flash: A 5B-Active-Parameter Cyber Model That Pushes MDASH to 95.95% on CyberGym</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Microsoft AI has released <a href="https://microsoft.ai/models/mai-cyber-1-flash/">MAI-Cyber-1-Flash</a>, its first model built specifically for cyber defense. The model does not ship as a standalone endpoint. It runs inside <strong>MDASH</strong>, Microsoft&#8217;s multi-model agentic scanning harness. </p>



<h2 id="h-mai-cyber-1-flash" class="wp-block-heading"><a href="https://microsoft.ai/models/mai-cyber-1-flash/"><strong>MAI-Cyber-1-Flash</strong></a></h2>



<p class="wp-block-paragraph"><a href="https://microsoft.ai/pdf/MAI-Cyber-1-Flash-Model-Card.pdf">MAI-Cyber-1-Flash</a> is a transformer with self-attention and sparse Mixture-of-Experts layers. It carries <strong>137B total parameters with 5B active</strong>, and a <strong>256k context length</strong>. Inputs and outputs are text only.</p>



<p class="wp-block-paragraph">It is a cybersecurity-specialized fine-tune of <a href="https://microsoft.ai/models/mai-code-1-flash/">MAI-Code-1-Flash</a>, the lightweight agentic coding model already embedded in GitHub Copilot and VS Code. The release describes it as derived from the <a href="https://microsoft.ai/models/mai-thinking-1/">MAI-Thinking-1</a> lineage. </p>



<div id="mtp-maicyber-explainer">
<iframe id="mtp-maicyber-explainer-frame" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;MAI-Cyber-1-Flash Terminal Explainer&lt;/title&gt;
&lt;style&gt;
  *{box-sizing:border-box;margin:0;padding:0}
  body{background:transparent;font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,&quot;Liberation Mono&quot;,monospace}
  #mtpRoot{background:#0b0b0b;border:1px solid #2a2a2a;border-radius:10px;overflow:hidden;color:#e9e9e9}
  .bar{display:flex;align-items:center;gap:8px;padding:10px 14px;background:#151515;border-bottom:1px solid #2a2a2a}
  .dot{width:11px;height:11px;border-radius:50%}
  .d1{background:#F25022}.d2{background:#FFB900}.d3{background:#4d4d4d}
  .bartitle{margin-left:8px;font-size:12px;color:#8f8f8f;letter-spacing:.04em;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .hd{padding:18px 18px 6px}
  .hd h3{font-size:17px;color:#fff;font-weight:700;letter-spacing:-.01em}
  .hd p{margin-top:6px;font-size:12.5px;color:#9a9a9a;line-height:1.65}
  .hd b{color:#FFB900;font-weight:600}
  .cmds{display:flex;flex-wrap:wrap;gap:7px;padding:14px 18px 0}
  .cmd{background:#161616;border:1px solid #2e2e2e;color:#c9c9c9;font-family:inherit;font-size:11.5px;padding:7px 11px;border-radius:5px;cursor:pointer;transition:.14s;white-space:nowrap}
  .cmd:hover{border-color:#F25022;color:#fff}
  .cmd.on{background:#F25022;border-color:#F25022;color:#fff;font-weight:600}
  .cmd .p{color:#FFB900;margin-right:5px}
  .cmd.on .p{color:#fff}
  .scr{margin:14px 18px 18px;background:#000;border:1px solid #262626;border-radius:7px;padding:15px 16px;min-height:230px}
  .ln{font-size:12px;color:#6f6f6f;margin-bottom:12px}
  .ln .p{color:#F25022;font-weight:700}
  .ln .c{color:#fff}
  .cur{display:inline-block;width:7px;height:13px;background:#FFB900;vertical-align:-2px;animation:bl 1s step-end infinite}
  @keyframes bl{50%{opacity:0}}
  table{width:100%;border-collapse:collapse;font-size:12px}
  td{padding:7px 4px;border-bottom:1px solid #1c1c1c;vertical-align:top;line-height:1.55}
  td.k{color:#8b8b8b;width:44%}
  td.v{color:#fff}
  td.v em{font-style:normal;color:#FFB900}
  .row{margin-bottom:13px}
  .rl{display:flex;justify-content:space-between;font-size:11.5px;margin-bottom:5px;gap:10px}
  .rl span:first-child{color:#cfcfcf}
  .rl span:last-child{color:#FFB900;font-weight:700;font-variant-numeric:tabular-nums}
  .track{height:16px;background:#141414;border:1px solid #242424;border-radius:3px;overflow:hidden}
  .fill{height:100%;width:0;transition:width .9s cubic-bezier(.22,.9,.3,1)}
  .f1{background:linear-gradient(90deg,#F25022,#FFB900)}
  .f2{background:#5a5a5a}
  .f3{background:#3a3a3a}
  .note{font-size:11px;color:#7a7a7a;line-height:1.65;margin-top:12px;border-top:1px dashed #242424;padding-top:11px}
  .note b{color:#FFB900;font-weight:600}
  .split{display:flex;gap:3px;margin:6px 0 14px;height:34px;border-radius:4px;overflow:hidden;border:1px solid #262626}
  .s90{flex:0 0 90%;background:linear-gradient(90deg,#F25022,#FFB900);display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:#0b0b0b}
  .s10{flex:1;background:#3a3a3a;display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;color:#fff}
  .pipe{display:flex;flex-wrap:wrap;gap:6px;margin:4px 0 12px}
  .st{flex:1 1 88px;background:#141414;border:1px solid #262626;border-left:2px solid #F25022;border-radius:4px;padding:8px 9px}
  .st b{display:block;font-size:11px;color:#FFB900;margin-bottom:3px}
  .st span{font-size:10.5px;color:#9a9a9a;line-height:1.5}
  .warn{background:#160d07;border:1px solid #3d2410;border-left:3px solid #FFB900;border-radius:4px;padding:10px 12px;margin-top:12px;font-size:11.5px;color:#c8c8c8;line-height:1.65}
  .warn b{color:#FFB900}
  .ft{display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap;padding:11px 18px;background:#111;border-top:1px solid #242424;font-size:10.5px;color:#767676}
  .ft a{color:#FFB900;text-decoration:none;font-weight:600}
  .ft a:hover{text-decoration:underline}
  @media(max-width:640px){
    .hd h3{font-size:15px}
    .cmd{font-size:10.5px;padding:6px 9px}
    .scr{margin:12px 12px 12px;padding:12px}
    .hd{padding:14px 12px 4px}
    .cmds{padding:12px 12px 0}
    td{font-size:11px}
    td.k{width:50%}
    .st{flex:1 1 100%}
    .ft{padding:10px 12px}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;mtpRoot&quot;&gt;
  &lt;div class=&quot;bar&quot;&gt;
    &lt;span class=&quot;dot d1&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot d2&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot d3&quot;&gt;&lt;/span&gt;
    &lt;span class=&quot;bartitle&quot;&gt;defender@mdash: ~/mai-cyber-1-flash&lt;/span&gt;
  &lt;/div&gt;

  &lt;div class=&quot;hd&quot;&gt;
    &lt;h3&gt;MAI-Cyber-1-Flash &amp;#8212; Interactive Spec &amp;amp; Benchmark Explorer&lt;/h3&gt;
    &lt;p&gt;Microsoft AI&amp;#8217;s first cyber model, announced &lt;b&gt;27 July 2026&lt;/b&gt;, runs inside &lt;b&gt;MDASH&lt;/b&gt;, Microsoft&amp;#8217;s multi-model agentic scanning harness. Click a command to inspect the released numbers.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;cmds&quot; id=&quot;cmds&quot;&gt;&lt;/div&gt;
  &lt;div class=&quot;scr&quot; id=&quot;scr&quot;&gt;&lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;Sources: Microsoft AI model card &amp;amp; launch post, Microsoft Security Blog (MDASH)&lt;/span&gt;
    &lt;span&gt;Built by &lt;a href=&quot;https://www.marktechpost.com/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Marktechpost&lt;/a&gt;&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
var VIEWS = [
  {
    cmd: &quot;cat model.spec&quot;,
    render: function () {
      return '&lt;table&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Model name&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;MAI-Cyber-1-Flash&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Parameters&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;&lt;em&gt;137B total, 5B active&lt;/em&gt;&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Architecture&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;Transformer, self-attention + sparse Mixture-of-Experts&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Base model&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;Cybersecurity fine-tune of MAI-Code-1-Flash&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Context length&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;&lt;em&gt;256k&lt;/em&gt;&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Inputs / Outputs&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;Text / Text&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Release date&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;27 July 2026&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Developer of record&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;Microsoft Ireland Operations Limited&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Languages&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;English, most official EU languages, plus languages inherited from MAI-Code-1-Flash&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Distribution&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;Azure AI Foundry Private Preview, solely for use inside MDASH&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;/table&gt;' +
        '&lt;div class=&quot;note&quot;&gt;The model card lists MAI-Cyber-1-Flash as a fine-tune of &lt;b&gt;MAI-Code-1-Flash&lt;/b&gt;. The launch post describes it as derived from the &lt;b&gt;MAI-Thinking-1&lt;/b&gt; lineage &amp;#8212; the same family, one generation up.&lt;/div&gt;';
    }
  },
  {
    cmd: &quot;run bench --cybergym&quot;,
    render: function () {
      return bar(&quot;MDASH + MAI-Cyber-1-Flash + GPT-5.4&quot;, &quot;95.95%&quot;, 95.95, &quot;f1&quot;) +
        bar(&quot;MDASH baseline, May 2026 (GA models only)&quot;, &quot;88.45%&quot;, 88.45, &quot;f2&quot;) +
        bar(&quot;Next best public leaderboard entry, May 2026&quot;, &quot;83.1%&quot;, 83.1, &quot;f3&quot;) +
        '&lt;div class=&quot;note&quot;&gt;CyberGym is a public suite of &lt;b&gt;1,507 real-world vulnerability reproduction tasks&lt;/b&gt; drawn from &lt;b&gt;188 OSS-Fuzz projects&lt;/b&gt;. Microsoft ran it at CyberGym&amp;#8217;s default level 1 configuration. The launch chart places the four competing systems &amp;#8212; including Anthropic&amp;#8217;s Mythos, Google Gemini and OpenAI GPT configurations &amp;#8212; between &lt;b&gt;83.2% and 85.6%&lt;/b&gt;, and Microsoft describes its result as &lt;b&gt;+12 points above Mythos&lt;/b&gt;.&lt;/div&gt;';
    }
  },
  {
    cmd: &quot;show routing --split&quot;,
    render: function () {
      return '&lt;div class=&quot;rl&quot;&gt;&lt;span&gt;Task routing inside MDASH&lt;/span&gt;&lt;span&gt;90 / 10&lt;/span&gt;&lt;/div&gt;' +
        '&lt;div class=&quot;split&quot;&gt;&lt;div class=&quot;s90&quot;&gt;MAI-Cyber-1-Flash &amp;#8212; up to 90% of tasks&lt;/div&gt;&lt;div class=&quot;s10&quot;&gt;GPT-5.4&lt;/div&gt;&lt;/div&gt;' +
        '&lt;div class=&quot;pipe&quot;&gt;' +
        '&lt;div class=&quot;st&quot;&gt;&lt;b&gt;Prepare&lt;/b&gt;&lt;span&gt;Index source, map attack surface from commit history&lt;/span&gt;&lt;/div&gt;' +
        '&lt;div class=&quot;st&quot;&gt;&lt;b&gt;Scan&lt;/b&gt;&lt;span&gt;Auditor agents emit candidate findings&lt;/span&gt;&lt;/div&gt;' +
        '&lt;div class=&quot;st&quot;&gt;&lt;b&gt;Validate&lt;/b&gt;&lt;span&gt;Debater agents argue reachability and exploitability&lt;/span&gt;&lt;/div&gt;' +
        '&lt;div class=&quot;st&quot;&gt;&lt;b&gt;Dedupe&lt;/b&gt;&lt;span&gt;Collapse semantically equivalent findings&lt;/span&gt;&lt;/div&gt;' +
        '&lt;div class=&quot;st&quot;&gt;&lt;b&gt;Prove&lt;/b&gt;&lt;span&gt;Build and execute triggering inputs&lt;/span&gt;&lt;/div&gt;' +
        '&lt;/div&gt;' +
        '&lt;div class=&quot;note&quot;&gt;MDASH orchestrates &lt;b&gt;100+ specialized agents&lt;/b&gt;. Microsoft reports a &lt;b&gt;50% cost saving&lt;/b&gt; versus its previous best MDASH configuration (GPT-5.4 + 5.4 mini + 5.3 codex). The model card frames the same result differently: replacing &lt;b&gt;80% of the existing models&lt;/b&gt; in MDASH moved CyberGym from &lt;b&gt;88.4% to 95.95%&lt;/b&gt;.&lt;/div&gt;';
    }
  },
  {
    cmd: &quot;cat evals --model-only&quot;,
    render: function () {
      return '&lt;table&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;CVEBench (exploit real web app CVEs)&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;&lt;em&gt;0.314&lt;/em&gt;&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;CyberSecEval4 &amp;#8212; Threat Intel&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;&lt;em&gt;0.553&lt;/em&gt;&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;CyberSecEval4 &amp;#8212; Malware Analysis&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;&lt;em&gt;0.33&lt;/em&gt;&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;CRSBench (full-pipeline cyber reasoning)&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;&lt;em&gt;0.651&lt;/em&gt; (POV=1200)&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;ExploitGym &amp;#8212; Kernel / Userspace / Browser&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;&lt;em&gt;0 / 0 / 0&lt;/em&gt;&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;/table&gt;' +
        '&lt;div class=&quot;warn&quot;&gt;&lt;b&gt;Read these correctly.&lt;/b&gt; These are model-only scores from a lightweight terminal harness, not MDASH results. The headline 95.95% is a &lt;b&gt;system&lt;/b&gt; number. The straight zeros on ExploitGym are the point of a defense-calibrated model, not a failure: it patches bugs, it does not write exploits.&lt;/div&gt;';
    }
  },
  {
    cmd: &quot;cat safety.log&quot;,
    render: function () {
      return '&lt;table&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Red team framework&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;PyRIT, Microsoft AI Red Team&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Automated adversarial corpus&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;&lt;em&gt;1M+ multi-turn jailbreak conversations&lt;/em&gt;&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Test environment&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;Network-isolated; no production systems, no public internet&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Independent assessment&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;Third party found &lt;em&gt;no critical-severity findings&lt;/em&gt;&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Capability suppression&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;Trained to forget content unrelated to cyber defense, including weapons and child-safety domains&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;tr&gt;&lt;td class=&quot;k&quot;&gt;Access control&lt;/td&gt;&lt;td class=&quot;v&quot;&gt;Restricted to select MDASH customers, subject to review and approval&lt;/td&gt;&lt;/tr&gt;' +
        '&lt;/table&gt;' +
        '&lt;div class=&quot;warn&quot;&gt;Microsoft calls this a &lt;b&gt;security-first calibration&lt;/b&gt; and warns it cuts both ways: safeguards may trigger on ambiguous requests &lt;b&gt;even when the defensive goal is legitimate&lt;/b&gt;. Expect refusals during evaluation. Microsoft says calibration may be relaxed as operational data accumulates.&lt;/div&gt;';
    }
  }
];

var scr = document.getElementById(&quot;scr&quot;);
var cmds = document.getElementById(&quot;cmds&quot;);
var active = 0;

function bar(label, val, pct, cls) {
  return '&lt;div class=&quot;row&quot;&gt;&lt;div class=&quot;rl&quot;&gt;&lt;span&gt;' + label + '&lt;/span&gt;&lt;span&gt;' + val + '&lt;/span&gt;&lt;/div&gt;' +
    '&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill ' + cls + '&quot; data-w=&quot;' + pct + '&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;';
}

function ping() {
  var h = document.getElementById(&quot;mtpRoot&quot;).offsetHeight + 40;
  if (window.parent) window.parent.postMessage({ mtpEmbed: &quot;mai-cyber-explainer&quot;, height: h }, &quot;*&quot;);
}

function draw(i) {
  active = i;
  var b = cmds.children;
  for (var k = 0; k &lt; b.length; k++) b[k].className = (k === i ? &quot;cmd on&quot; : &quot;cmd&quot;);
  scr.innerHTML = '&lt;div class=&quot;ln&quot;&gt;&lt;span class=&quot;p&quot;&gt;defender@mdash:~$&lt;/span&gt; &lt;span class=&quot;c&quot;&gt;' + VIEWS[i].cmd + '&lt;/span&gt; &lt;span class=&quot;cur&quot;&gt;&lt;/span&gt;&lt;/div&gt;' + VIEWS[i].render();
  var fills = scr.querySelectorAll(&quot;.fill&quot;);
  setTimeout(function () {
    for (var j = 0; j &lt; fills.length; j++) fills[j].style.width = fills[j].getAttribute(&quot;data-w&quot;) + &quot;%&quot;;
    setTimeout(ping, 60);
  }, 40);
  ping();
}

for (var i = 0; i &lt; VIEWS.length; i++) {
  (function (i) {
    var b = document.createElement(&quot;button&quot;);
    b.className = &quot;cmd&quot;;
    b.innerHTML = '&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt;' + VIEWS[i].cmd;
    b.onclick = function () { draw(i); };
    cmds.appendChild(b);
  })(i);
}

draw(0);
window.addEventListener(&quot;load&quot;, ping);
window.addEventListener(&quot;resize&quot;, ping);
setTimeout(ping, 400);
setTimeout(ping, 1200);
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
" style="width:100%;border:0;display:block;height:800px;overflow:hidden" scrolling="no" loading="lazy" title="Marktechpost interactive embed"></iframe>
</div>
<style>
#mtp-maicyber-explainer{background:transparent!important;margin:22px 0!important;padding:0!important;border:0!important}
#mtp-maicyber-explainer iframe{background:transparent!important;border:0!important;width:100%!important}
#mtp-maicyber-explainer hr,#mtp-maicyber-explainer p:empty,#mtp-maicyber-explainer del,#mtp-maicyber-explainer s{display:none!important}
</style>
<script>
(function(){
  var f=document.getElementById("mtp-maicyber-explainer-frame");
  window.addEventListener("message",function(e){
    var d=e.data;
    if(!d||d.mtpEmbed!=="mai-cyber-explainer"||!d.height)return;
    f.style.height=d.height+"px";
  });
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-benchmarks" class="wp-block-heading"><strong>Benchmarks</strong></h2>



<p class="wp-block-paragraph"><a href="https://www.cybergym.io/cybergym/" target="_blank" rel="noreferrer noopener">CyberGym</a> is a public suite of 1,507 real-world vulnerability reproduction tasks drawn from 188 OSS-Fuzz projects. Microsoft evaluated at CyberGym&#8217;s default level 1 configuration, which supplies vulnerable source and a high-level description.</p>



<p class="wp-block-paragraph">MDASH running MAI-Cyber-1-Flash alongside GPT-5.4 scores <strong>95.95%</strong>. Microsoft frames this as roughly 12 points above Anthropic&#8217;s Mythos, and the launch chart places the four competing systems between 83.2% and 85.6%.</p>



<p class="wp-block-paragraph">When Microsoft first <a href="https://www.microsoft.com/en-us/security/blog/2026/05/12/defense-at-ai-speed-microsofts-new-multi-model-agentic-security-system-tops-leading-industry-benchmark/">detailed MDASH</a> in May 2026, the harness scored <strong>88.45%</strong> on CyberGym using only generally available models. That was already the top public leaderboard score, about five points ahead of the next entry at 83.1%. The research team states the improvement plainly: replacing 80% of the existing models in MDASH moved the harness from 88.4% to 95.95%.</p>



<h2 id="h-why-the-routing-is-the-real-product" class="wp-block-heading"><strong>Why the routing is the real product</strong></h2>



<p class="wp-block-paragraph">MDASH manages <strong>over 100 specialized agents</strong> through five stages: Prepare, Scan, Validate, Dedupe, and Prove. Auditor agents flag findings, debater agents argue exploitability (using disagreement as signal), and the Prove stage executes triggering inputs with ASan for C/C++ targets.</p>



<p class="wp-block-paragraph">To control frontier model costs at scale, MAI-Cyber-1-Flash handles <strong>up to 90% of MDASH tasks</strong>, escalating the hardest 10% to GPT-5.4. This routing yields a <strong>50% cost saving</strong> over the previous configuration of GPT-5.4, 5.4 mini, and 5.3 codex.</p>



<p class="wp-block-paragraph">MDASH was developed by Microsoft&#8217;s <strong>Autonomous Code Security (ACS)</strong> team, featuring members from the DARPA AI Cyber Challenge-winning Team Atlanta. In May, MDASH-assisted work generated <strong>16 CVEs</strong> (including four Critical remote code execution flaws) in the Windows networking and authentication stack. Retrospectively, it recovered <strong>96% of 28 MSRC cases in </strong><strong>clfs.sys</strong> and <strong>100% of 7 cases in </strong><strong>tcpip.sys</strong> over a five-year window.</p>



<h2 id="h-performance" class="wp-block-heading"><strong>Performance</strong></h2>



<p class="wp-block-paragraph">The research team present standalone results from a lightweight terminal harness:</p>



<figure class="wp-block-table"><table class="has-fixed-layout"><thead><tr><th>Benchmark</th><th>MAI-Cyber-1-Flash</th></tr></thead><tbody><tr><td>CVEBench</td><td>0.314</td></tr><tr><td>CyberSecEval4 — Threat Intel</td><td>0.553</td></tr><tr><td>CyberSecEval4 — Malware Analysis</td><td>0.33</td></tr><tr><td>CRSBench</td><td>0.651 (POV=1200)</td></tr><tr><td>ExploitGym — Kernel / Userspace / Browser</td><td>0 / 0 / 0</td></tr></tbody></table></figure>



<p class="wp-block-paragraph">The straight zeros on ExploitGym are deliberate, not a defect. Microsoft team states the model was trained to perform defensive tasks such as patching bugs, and not offensive tasks such as deploying malware. A 5B-active model that cannot generate exploits but can drive a 95.95% discovery pipeline is exactly the artifact a defender-only product needs.</p>



<h2 id="h-how-to-use-it" class="wp-block-heading"><strong>How to use it</strong></h2>



<div id="mtp-maicyber-deploy">
<iframe id="mtp-maicyber-deploy-frame" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;MAI-Cyber-1-Flash Access and Deployment&lt;/title&gt;
&lt;style&gt;
  *{box-sizing:border-box;margin:0;padding:0}
  body{background:transparent;font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,&quot;Liberation Mono&quot;,monospace}
  #mtpRoot2{background:#0b0b0b;border:1px solid #2a2a2a;border-radius:10px;overflow:hidden;color:#e9e9e9}
  .hd{padding:18px 18px 0}
  .kick{font-size:10.5px;letter-spacing:.16em;color:#F25022;font-weight:700;text-transform:uppercase}
  .hd h3{font-size:17px;color:#fff;margin-top:8px;font-weight:700;letter-spacing:-.01em}
  .hd p{margin-top:6px;font-size:12.5px;color:#9a9a9a;line-height:1.65}
  .hd b{color:#FFB900;font-weight:600}
  .secl{font-size:10.5px;letter-spacing:.14em;color:#6f6f6f;text-transform:uppercase;padding:20px 18px 9px;font-weight:700}
  .paths{display:flex;gap:8px;padding:0 18px;flex-wrap:wrap}
  .path{flex:1 1 170px;background:#141414;border:1px solid #282828;border-radius:7px;padding:12px 13px;cursor:pointer;transition:.14s;text-align:left}
  .path:hover{border-color:#F25022}
  .path.on{border-color:#F25022;background:#170e08}
  .path b{display:block;font-size:12px;color:#fff;margin-bottom:5px;font-weight:700}
  .path span{font-size:10.5px;color:#8f8f8f;line-height:1.55;display:block}
  .path .tag{display:inline-block;margin-top:8px;font-size:9.5px;letter-spacing:.06em;padding:3px 7px;border-radius:3px;background:#241505;color:#FFB900;border:1px solid #3d2410;font-weight:700}
  .panel{margin:12px 18px 0;background:#000;border:1px solid #262626;border-radius:7px;padding:14px 16px}
  .panel .t{font-size:12.5px;color:#FFB900;font-weight:700;margin-bottom:8px}
  .panel .d{font-size:12px;color:#c2c2c2;line-height:1.75}
  .panel .d a{color:#F25022;font-weight:600;text-decoration:none;border-bottom:1px solid #4a2314}
  .panel .d a:hover{color:#FFB900}
  .meta{display:flex;flex-wrap:wrap;gap:7px;margin-top:11px}
  .chip{font-size:10.5px;padding:5px 9px;border-radius:4px;background:#131313;border:1px solid #272727;color:#9c9c9c}
  .chip b{color:#fff;font-weight:600}
  .steps{padding:0 18px}
  .step{display:flex;gap:12px;align-items:flex-start;background:#111;border:1px solid #232323;border-radius:7px;padding:12px 13px;margin-bottom:7px;cursor:pointer;transition:.14s;text-align:left;width:100%}
  .step:hover{border-color:#3d3d3d}
  .step.on{border-color:#F25022;background:#150d07}
  .num{flex:0 0 26px;height:26px;border-radius:5px;background:#1e1e1e;color:#FFB900;font-size:11.5px;font-weight:700;display:flex;align-items:center;justify-content:center;border:1px solid #2e2e2e}
  .step.on .num{background:#F25022;color:#fff;border-color:#F25022}
  .stx{flex:1;min-width:0}
  .stx b{display:block;font-size:12.5px;color:#fff;font-weight:700;margin-bottom:2px}
  .stx span{font-size:11px;color:#8a8a8a;line-height:1.6}
  .body{display:none;margin-top:9px;padding-top:9px;border-top:1px dashed #2a2a2a;font-size:11.5px;color:#c2c2c2;line-height:1.8}
  .step.on .body{display:block}
  .body code{background:#000;border:1px solid #2a2a2a;color:#FFB900;padding:2px 6px;border-radius:3px;font-size:11px}
  .body a{color:#F25022;font-weight:600;text-decoration:none;border-bottom:1px solid #4a2314}
  .body a:hover{color:#FFB900}
  .warn{margin:16px 18px 0;background:#160d07;border:1px solid #3d2410;border-left:3px solid #FFB900;border-radius:5px;padding:11px 13px;font-size:11.5px;color:#c8c8c8;line-height:1.7}
  .warn b{color:#FFB900}
  .ft{display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap;margin-top:18px;padding:11px 18px;background:#111;border-top:1px solid #242424;font-size:10.5px;color:#767676}
  .ft a{color:#FFB900;text-decoration:none;font-weight:600}
  .ft a:hover{text-decoration:underline}
  @media(max-width:640px){
    .hd{padding:14px 12px 0}
    .hd h3{font-size:15px}
    .paths{padding:0 12px}
    .path{flex:1 1 100%}
    .panel{margin:12px 12px 0;padding:12px}
    .steps{padding:0 12px}
    .secl{padding:16px 12px 8px}
    .warn{margin:14px 12px 0}
    .ft{padding:10px 12px}
    .stx b{font-size:12px}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;mtpRoot2&quot;&gt;

  &lt;div class=&quot;hd&quot;&gt;
    &lt;div class=&quot;kick&quot;&gt;Deployment Guide&lt;/div&gt;
    &lt;h3&gt;How to actually get MAI-Cyber-1-Flash&lt;/h3&gt;
    &lt;p&gt;There is no weights download and no open API. Access runs through &lt;b&gt;three gated doors&lt;/b&gt;, and all of them route back to Microsoft&amp;#8217;s customer vetting. Pick your door, then follow the seven steps.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;secl&quot;&gt;Step 1 &amp;#8212; Choose your access path&lt;/div&gt;
  &lt;div class=&quot;paths&quot; id=&quot;paths&quot;&gt;&lt;/div&gt;
  &lt;div class=&quot;panel&quot; id=&quot;panel&quot;&gt;&lt;/div&gt;

  &lt;div class=&quot;secl&quot;&gt;Step 2 &amp;#8212; The deployment sequence&lt;/div&gt;
  &lt;div class=&quot;steps&quot; id=&quot;steps&quot;&gt;&lt;/div&gt;

  &lt;div class=&quot;warn&quot;&gt;
    &lt;b&gt;Before you scope a pilot:&lt;/b&gt; the model is calibrated to refuse when a request is ambiguous or thin on context, so budget for false refusals in your evaluation. Microsoft also states plainly that generated code may be incorrect and must be reviewed and tested before production use. Treat MDASH output as &lt;b&gt;proven candidates&lt;/b&gt;, not merged patches.
  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;Verified against Microsoft AI and Microsoft Security primary sources, 28 July 2026&lt;/span&gt;
    &lt;span&gt;Built by &lt;a href=&quot;https://www.marktechpost.com/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Marktechpost&lt;/a&gt;&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
var PATHS = [
  {
    n: &quot;MDASH private preview&quot;,
    s: &quot;The harness itself, for teams that want agentic code scanning on their own repositories.&quot;,
    tag: &quot;PRIVATE PREVIEW&quot;,
    t: &quot;Path A &amp;#8212; Register interest in MDASH&quot;,
    d: 'MDASH is Microsoft&amp;#8217;s multi-model agentic scanning harness, first detailed on the &lt;a href=&quot;https://www.microsoft.com/en-us/security/blog/2026/05/12/defense-at-ai-speed-microsofts-new-multi-model-agentic-security-system-tops-leading-industry-benchmark/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Microsoft Security Blog&lt;/a&gt; in May 2026 and still limited-access. This is the only route that gets you MAI-Cyber-1-Flash doing vulnerability discovery on your code. Sign-up runs through &lt;a href=&quot;https://aka.ms/AI-drivenScanningHarness&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;aka.ms/AI-drivenScanningHarness&lt;/a&gt;.',
    chips: [&quot;Scope: &lt;b&gt;your codebases&lt;/b&gt;&quot;, &quot;Controls: &lt;b&gt;RBAC, tenant isolation, sandboxed execution with no internet access&lt;/b&gt;&quot;, &quot;Status: &lt;b&gt;limited private preview&lt;/b&gt;&quot;]
  },
  {
    n: &quot;Azure AI Foundry&quot;,
    s: &quot;Model-level access, gated, and usable only inside MDASH &amp;#8212; not as a general endpoint.&quot;,
    tag: &quot;FROM 3 AUG 2026&quot;,
    t: &quot;Path B &amp;#8212; Azure AI Foundry Private Preview&quot;,
    d: 'The model card states MAI-Cyber-1-Flash is offered through &lt;a href=&quot;https://azure.microsoft.com/en-us/products/ai-foundry&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Azure AI Foundry&lt;/a&gt; Private Preview &lt;b&gt;solely for use within MDASH&lt;/b&gt;, with access restricted and subject to additional approval because of the dual-use nature of cyber capability. Reporting from the launch puts the Foundry availability date at 3 August 2026, alongside Microsoft&amp;#8217;s existing customer vetting process.',
    chips: [&quot;Not a general-purpose endpoint&quot;, &quot;Integration target: &lt;b&gt;MDASH only&lt;/b&gt;&quot;, &quot;Approval: &lt;b&gt;additional review required&lt;/b&gt;&quot;]
  },
  {
    n: &quot;Project Perception&quot;,
    s: &quot;The agentic security system, entering public preview inside Microsoft Defender.&quot;,
    tag: &quot;PUBLIC PREVIEW 3 AUG&quot;,
    t: &quot;Path C &amp;#8212; Project Perception in Microsoft Defender&quot;,
    d: '&lt;a href=&quot;https://www.microsoft.com/en-us/security/business/ai-powered-cybersecurity/project-perception-agentic-system&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Project Perception&lt;/a&gt; is the broadest door and the only one with published commercial terms. Red agents probe like an attacker, blue agents investigate and rank risk, green agents remediate and harden. Microsoft says Perception will use MAI-Cyber-1-Flash for more security workflows over time, beyond software vulnerability work. It lands in Microsoft Defender first, then extends across the Microsoft Security portfolio.',
    chips: [&quot;Pricing: &lt;b&gt;consumption-based, metered in Security Compute Units (SCUs)&lt;/b&gt;&quot;, &quot;Human sign-off required on &lt;b&gt;high-impact actions&lt;/b&gt;&quot;, &quot;Entry point: &lt;b&gt;Microsoft Defender&lt;/b&gt;&quot;]
  }
];

var STEPS = [
  {
    t: &quot;Confirm you clear the vetting gate&quot;,
    s: &quot;Access is restricted to verified defenders.&quot;,
    b: 'Microsoft describes the model as available only to verified defenders, restricted to select MDASH customers and subject to customer review and approval. Any use outside defensive security operations inside MDASH is declared &lt;b&gt;out of scope&lt;/b&gt;. If you are a red-team vendor or a researcher without an enterprise agreement, this is the step that stops you.'
  },
  {
    t: &quot;Register interest and get scoped&quot;,
    s: &quot;One form, then a Microsoft-side approval loop.&quot;,
    b: 'Submit through &lt;a href=&quot;https://aka.ms/AI-drivenScanningHarness&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;aka.ms/AI-drivenScanningHarness&lt;/a&gt;. Expect to declare which repositories, which languages, and which security team owns triage. MDASH has been in limited private preview since May 2026 and Microsoft says it is already deployed by Fortune 500 customers.'
  },
  {
    t: &quot;Prepare the target&quot;,
    s: &quot;Ingest source, build indices, model the attack surface.&quot;,
    b: 'The Prepare stage ingests the source target, builds language-aware indices, then derives the attack surface and threat model by analyzing past commits. Commit history is doing real work here &amp;#8212; a shallow clone will cost you signal.'
  },
  {
    t: &quot;Add domain plugins where the model is blind&quot;,
    s: &quot;This is where most of the tuning effort lands.&quot;,
    b: 'MDASH is extensible by design. Plugins inject context foundation models cannot infer: kernel calling conventions, IRP rules, lock invariants, IPC trust boundaries, codec state machines. Microsoft&amp;#8217;s own CLFS proving plugin, which knows how to construct a triggering log file, is credited as part of why &lt;code&gt;clfs.sys&lt;/code&gt; recall hit 96%. A &lt;a href=&quot;https://codeql.github.com/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;CodeQL&lt;/a&gt; database can also be plugged in.'
  },
  {
    t: &quot;Run the five-stage pipeline&quot;,
    s: &quot;Prepare, Scan, Validate, Dedupe, Prove.&quot;,
    b: 'Auditor agents emit candidate findings. Debater agents argue for and against reachability and exploitability &amp;#8212; disagreement between models is treated as signal. Dedupe collapses semantically equivalent findings. Prove constructs and executes triggering inputs, validating pre-conditions dynamically, for example with ASan on C/C++. A finding that survives Prove is the deliverable.'
  },
  {
    t: &quot;Let the router do the cost work&quot;,
    s: &quot;90% small model, 10% frontier escalation.&quot;,
    b: 'MAI-Cyber-1-Flash is designed to handle up to 90% of tasks, with MDASH escalating the hardest 10% to a larger frontier model &amp;#8212; currently OpenAI&amp;#8217;s GPT-5.4. Microsoft reports this cuts cost roughly in half versus its previous best MDASH configuration. You do not hand-route; the harness does.'
  },
  {
    t: &quot;Wire findings into remediation, with a human on the gate&quot;,
    s: &quot;Perception closes the loop from finding to fix.&quot;,
    b: 'Agentic code scanning feeds Project Perception, where green agents write and deploy fixes. Microsoft keeps &lt;b&gt;high-impact actions under human sign-off&lt;/b&gt; and describes decisions as scoped, traceable and replayable. Issues or abuse concerns go to the &lt;a href=&quot;https://www.microsoft.com/en-us/msrc&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Microsoft Security Response Center&lt;/a&gt;.'
  }
];

var pathsEl = document.getElementById(&quot;paths&quot;);
var panelEl = document.getElementById(&quot;panel&quot;);
var stepsEl = document.getElementById(&quot;steps&quot;);
var openStep = 0;

function ping2() {
  var h = document.getElementById(&quot;mtpRoot2&quot;).offsetHeight + 40;
  if (window.parent) window.parent.postMessage({ mtpEmbed: &quot;mai-cyber-deploy&quot;, height: h }, &quot;*&quot;);
}

function showPath(i) {
  var b = pathsEl.children;
  for (var k = 0; k &lt; b.length; k++) b[k].className = (k === i ? &quot;path on&quot; : &quot;path&quot;);
  var p = PATHS[i], chips = &quot;&quot;;
  for (var c = 0; c &lt; p.chips.length; c++) chips += '&lt;span class=&quot;chip&quot;&gt;' + p.chips[c] + '&lt;/span&gt;';
  panelEl.innerHTML = '&lt;div class=&quot;t&quot;&gt;' + p.t + '&lt;/div&gt;&lt;div class=&quot;d&quot;&gt;' + p.d + '&lt;/div&gt;&lt;div class=&quot;meta&quot;&gt;' + chips + '&lt;/div&gt;';
  ping2();
}

for (var i = 0; i &lt; PATHS.length; i++) {
  (function (i) {
    var b = document.createElement(&quot;button&quot;);
    b.className = &quot;path&quot;;
    b.innerHTML = '&lt;b&gt;' + PATHS[i].n + '&lt;/b&gt;&lt;span&gt;' + PATHS[i].s + '&lt;/span&gt;&lt;span class=&quot;tag&quot;&gt;' + PATHS[i].tag + '&lt;/span&gt;';
    b.onclick = function () { showPath(i); };
    pathsEl.appendChild(b);
  })(i);
}

function toggleStep(i) {
  openStep = (openStep === i ? -1 : i);
  var b = stepsEl.children;
  for (var k = 0; k &lt; b.length; k++) b[k].className = (k === openStep ? &quot;step on&quot; : &quot;step&quot;);
  ping2();
  setTimeout(ping2, 80);
}

for (var j = 0; j &lt; STEPS.length; j++) {
  (function (j) {
    var b = document.createElement(&quot;button&quot;);
    b.className = &quot;step&quot;;
    b.innerHTML = '&lt;span class=&quot;num&quot;&gt;' + (j + 1) + '&lt;/span&gt;&lt;span class=&quot;stx&quot;&gt;&lt;b&gt;' + STEPS[j].t + '&lt;/b&gt;&lt;span&gt;' + STEPS[j].s + '&lt;/span&gt;&lt;span class=&quot;body&quot;&gt;' + STEPS[j].b + '&lt;/span&gt;&lt;/span&gt;';
    b.onclick = function () { toggleStep(j); };
    stepsEl.appendChild(b);
  })(j);
}

showPath(0);
toggleStep(0);
window.addEventListener(&quot;load&quot;, ping2);
window.addEventListener(&quot;resize&quot;, ping2);
setTimeout(ping2, 400);
setTimeout(ping2, 1200);
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
" style="width:100%;border:0;display:block;height:1180px;overflow:hidden" scrolling="no" loading="lazy" title="Marktechpost interactive embed"></iframe>
</div>
<style>
#mtp-maicyber-deploy{background:transparent!important;margin:22px 0!important;padding:0!important;border:0!important}
#mtp-maicyber-deploy iframe{background:transparent!important;border:0!important;width:100%!important}
#mtp-maicyber-deploy hr,#mtp-maicyber-deploy p:empty,#mtp-maicyber-deploy del,#mtp-maicyber-deploy s{display:none!important}
</style>
<script>
(function(){
  var f=document.getElementById("mtp-maicyber-deploy-frame");
  window.addEventListener("message",function(e){
    var d=e.data;
    if(!d||d.mtpEmbed!=="mai-cyber-deploy"||!d.height)return;
    f.style.height=d.height+"px";
  });
})();
</script>



<p class="wp-block-paragraph" id="h-"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li><strong>MAI-Cyber-1-Flash is 137B total / 5B active</strong>, a sparse MoE fine-tune of MAI-Code-1-Flash with 256k context.</li>



<li><strong>95.95% on CyberGym is a system score</strong> — MDASH plus the new model plus GPT-5.4, up from 88.45% in May 2026.</li>



<li><strong>It handles up to 90% of MDASH tasks</strong>, escalating the hard 10% to GPT-5.4 for a claimed 50% cost cut.</li>



<li><strong>ExploitGym scores are 0/0/0 by design</strong> — the model patches bugs, it does not write exploits.</li>



<li><strong>Access is gated</strong></li>
</ul>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/28/microsoft-ai-releases-mai-cyber-1-flash-a-5b-active-parameter-cyber-model-that-pushes-mdash-to-95-95-on-cybergym/">Microsoft AI Releases MAI-Cyber-1-Flash: A 5B-Active-Parameter Cyber Model That Pushes MDASH to 95.95% on CyberGym</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81401</post-id>
	</item>
		<item>
		<title>Deploying a 1-Bit Bonsai-27B Model with PrismML llama.cpp and OpenAI-Compatible Local Inference Workflows</title>
		<link>https://www.marktechpost.com/2026/07/28/deploying-a-1-bit-bonsai-27b-model-with-prismml-llama-cpp-and-openai-compatible-local-inference-workflows/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Tue, 28 Jul 2026 07:53:48 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81398</guid>

					<description><![CDATA[<p>In this tutorial, we deploy the 1-bit Bonsai-27B language model using the PrismML fork of llama.cpp, which provides the specialized CUDA kernels required to decode the model’s Q1_0_g128 GGUF quantization format</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/28/deploying-a-1-bit-bonsai-27b-model-with-prismml-llama-cpp-and-openai-compatible-local-inference-workflows/">Deploying a 1-Bit Bonsai-27B Model with PrismML llama.cpp and OpenAI-Compatible Local Inference Workflows</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this tutorial, we deploy the<a href="https://huggingface.co/prism-ml/Bonsai-27B-gguf"> <strong>1-bit Bonsai-27B</strong></a> language model using the PrismML fork of llama.cpp, which provides the specialized CUDA kernels required to decode the model’s Q1_0_g128 GGUF quantization format. We begin by validating the GPU runtime, installing the required Python dependencies, compiling the CUDA-enabled inference binaries, and downloading the compressed model weights from Hugging Face. We then test the model through llama-cli, launch an OpenAI-compatible local inference server, and interact with it through a reusable Python client that supports standard completions, streamed responses, multi-turn conversations, and code generation. We also examine optional configurations for throughput benchmarking, quantized key-value caching, long-context inference, speculative decoding, and multimodal extensions.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import os
import sys
import time
import json
import shutil
import subprocess
import multiprocessing
WORK_DIR      = "/content"
REPO_URL      = "https://github.com/PrismML-Eng/llama.cpp"
REPO_DIR      = os.path.join(WORK_DIR, "llama.cpp")
BUILD_DIR     = os.path.join(REPO_DIR, "build")
BIN_DIR       = os.path.join(BUILD_DIR, "bin")
HF_REPO       = "prism-ml/Bonsai-27B-gguf"
MODEL_FILE    = "Bonsai-27B-Q1_0.gguf"
MODEL_PATH    = os.path.join(WORK_DIR, MODEL_FILE)
SERVER_HOST   = "127.0.0.1"
SERVER_PORT   = 8080
SERVER_URL    = f"http://{SERVER_HOST}:{SERVER_PORT}"
GEN_PARAMS    = {"temperature": 0.7, "top_p": 0.95, "top_k": 20}
CTX_SIZE      = 8192
N_GPU_LAYERS  = 99
USE_KV_Q4     = False
def sh(cmd, check=True, **kw):
   """Run a shell command, streaming output to the notebook."""
   print(f"\n$ {cmd}")
   return subprocess.run(cmd, shell=True, check=check, **kw)
print("=" * 70)
print("[1/7] Checking environment")
print("=" * 70)
gpu = subprocess.run("nvidia-smi --query-gpu=name,memory.total --format=csv,noheader",
                    shell=True, capture_output=True, text=True)
if gpu.returncode != 0:
   sys.exit("No GPU detected. In Colab: Runtime -> Change runtime type -> GPU (T4).")
print(f"GPU detected: {gpu.stdout.strip()}")
print("Bonsai-27B needs only ~5.2 GB peak at 4K context — any Colab GPU works.")
sh("pip -q install huggingface_hub requests")
</code></pre></div></div>



<p class="wp-block-paragraph">We configure the Colab workspace, model repository, server endpoint, inference parameters, context size, and GPU offloading settings required throughout the tutorial. We define a reusable shell-command function and verify that the runtime exposes a compatible NVIDIA GPU before continuing. We then install the Hugging Face Hub and HTTP client dependencies needed for model retrieval and API communication.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("=" * 70)
print("[2/7] Building PrismML llama.cpp fork with CUDA (cached after 1st run)")
print("=" * 70)
if not os.path.isdir(REPO_DIR):
   sh(f"git clone --depth 1 {REPO_URL} {REPO_DIR}")
else:
   print("Repo already cloned — skipping.")
cli_bin    = os.path.join(BIN_DIR, "llama-cli")
server_bin = os.path.join(BIN_DIR, "llama-server")
bench_bin  = os.path.join(BIN_DIR, "llama-bench")
if not (os.path.exists(cli_bin) and os.path.exists(server_bin)):
   jobs = multiprocessing.cpu_count()
   sh(f"cmake -S {REPO_DIR} -B {BUILD_DIR} -DGGML_CUDA=ON -DCMAKE_BUILD_TYPE=Release")
   sh(f"cmake --build {BUILD_DIR} -j{jobs} --target llama-cli llama-server llama-bench")
else:
   print("Binaries already built — skipping.")
</code></pre></div></div>



<p class="wp-block-paragraph">We clone the PrismML fork of llama.cpp, which provides the specialized kernels required for the model’s Q1_0_g128 quantization format. We configure a CUDA-enabled release build with CMake and compile the command-line, server, and benchmarking executables. We also reuse previously generated binaries when they already exist, reducing repeated setup time in the same Colab session.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("=" * 70)
print("[3/7] Downloading weights from Hugging Face")
print("=" * 70)
from huggingface_hub import hf_hub_download
if not os.path.exists(MODEL_PATH):
   downloaded = hf_hub_download(repo_id=HF_REPO, filename=MODEL_FILE,
                                local_dir=WORK_DIR)
   print(f"Downloaded to: {downloaded}")
else:
   print("Model already on disk — skipping.")
print(f"Model size on disk: {os.path.getsize(MODEL_PATH) / 1e9:.2f} GB")
</code></pre></div></div>



<p class="wp-block-paragraph">We connect to the Hugging Face Hub and download the Bonsai-27B GGUF model into the Colab workspace. We skip the transfer when the model file is already available locally, allowing subsequent runs to proceed more efficiently. We then calculate and display the deployed model size to confirm that the compressed weights are stored correctly.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("=" * 70)
print("[4/7] Smoke test with llama-cli")
print("=" * 70)
sh(
   f'{cli_bin} -m {MODEL_PATH} '
   f'-p "Explain in two sentences why 1-bit quantization saves memory." '
   f'-n 128 -ngl {N_GPU_LAYERS} '
   f'--temp {GEN_PARAMS["temperature"]} '
   f'--top-p {GEN_PARAMS["top_p"]} --top-k {GEN_PARAMS["top_k"]} '
   f'-no-cnv 2>/dev/null',
   check=False,
)
print("=" * 70)
print("[5/7] Starting llama-server (OpenAI-compatible API)")
print("=" * 70)
import requests
kv_flags = "-ctk q4_0 -ctv q4_0" if USE_KV_Q4 else ""
server_cmd = (
   f"{server_bin} -m {MODEL_PATH} "
   f"--host {SERVER_HOST} --port {SERVER_PORT} "
   f"-ngl {N_GPU_LAYERS} -c {CTX_SIZE} {kv_flags}"
)
print(f"$ {server_cmd}  (background)")
server_log = open(os.path.join(WORK_DIR, "server.log"), "w")
server_proc = subprocess.Popen(server_cmd, shell=True,
                              stdout=server_log, stderr=server_log)
for _ in range(120):
   try:
       if requests.get(f"{SERVER_URL}/health", timeout=2).status_code == 200:
           print("Server is up.")
           break
   except requests.exceptions.RequestException:
       pass
   time.sleep(2)
else:
   server_proc.kill()
   sys.exit("Server failed to start — check /content/server.log")
</code></pre></div></div>



<p class="wp-block-paragraph">We perform a command-line smoke test to verify that the compiled runtime can load the quantized model and generate a valid response. We then start llama-server with full GPU layer offloading, the selected context window, and optional quantized KV-cache settings. We repeatedly query the health endpoint until the OpenAI-compatible inference service becomes ready for client requests.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("=" * 70)
print("[6/7] Talking to Bonsai-27B via the OpenAI-compatible API")
print("=" * 70)
def chat(messages, stream=False, max_tokens=512, **overrides):
   """Minimal OpenAI-compatible chat client for the local llama-server."""
   payload = {
       "model": "bonsai-27b",
       "messages": messages,
       "max_tokens": max_tokens,
       "stream": stream,
       **GEN_PARAMS,
       **overrides,
   }
   if not stream:
       r = requests.post(f"{SERVER_URL}/v1/chat/completions", json=payload)
       r.raise_for_status()
       return r.json()["choices"][0]["message"]["content"]
   r = requests.post(f"{SERVER_URL}/v1/chat/completions", json=payload, stream=True)
   r.raise_for_status()
   full = []
   for line in r.iter_lines():
       if not line or not line.startswith(b"data: "):
           continue
       chunk = line[len(b"data: "):]
       if chunk == b"[DONE]":
           break
       delta = json.loads(chunk)["choices"][0]["delta"].get("content", "")
       full.append(delta)
       print(delta, end="", flush=True)
   print()
   return "".join(full)
SYSTEM = {"role": "system", "content": "You are a helpful assistant"}
print("\n--- 6a: basic completion ---")
answer = chat([SYSTEM, {"role": "user",
                       "content": "What is the capital of France? One sentence."}])
print(answer)
print("\n--- 6b: math reasoning, streamed token-by-token ---")
chat([SYSTEM, {"role": "user",
              "content": "A train travels 120 km at 80 km/h, then 90 km at "
                         "60 km/h. What is its average speed for the whole "
                         "trip? Show your reasoning briefly."}],
    stream=True, max_tokens=700)
print("\n--- 6c: multi-turn chat ---")
history = [SYSTEM]
for user_msg in ["My name is Priya and I love graph algorithms.",
                "Suggest one project idea that combines my interest with LLMs.",
                "What was my name again?"]:
   history.append({"role": "user", "content": user_msg})
   reply = chat(history, max_tokens=300)
   history.append({"role": "assistant", "content": reply})
   print(f"\nUSER: {user_msg}\nBONSAI: {reply}")
print("\n--- 6d: code generation ---")
print(chat([SYSTEM, {"role": "user",
                    "content": "Write a Python function that returns the n-th "
                               "Fibonacci number using memoization. Code only."}],
          max_tokens=400))
</code></pre></div></div>



<p class="wp-block-paragraph">We define a reusable Python chat client that sends OpenAI-compatible requests to the locally hosted Bonsai-27B server. We support both standard and server-sent-event streaming responses while applying the configured temperature, top-p, and top-k sampling parameters. We then evaluate basic factual answering, mathematical reasoning, multi-turn conversational memory, and Python code generation.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("=" * 70)
print("[7/7] Optional extras")
print("=" * 70)
RUN_BENCHMARK = False
if RUN_BENCHMARK:
   sh(f"{bench_bin} -m {MODEL_PATH} -ngl {N_GPU_LAYERS}", check=False)
print("""
NOTES &amp; NEXT STEPS
------------------
* Long context: the model supports up to 262K tokens. On Colab, raise
 CTX_SIZE and set USE_KV_Q4 = True (4-bit KV cache) — with it, 100K-token
 contexts fit in roughly 6.8 GB peak, well inside a T4's 16 GB.
* Speculative decoding: the repo ships a DSpark drafter (Q4_1, ~1.79 GB)
 that gives a lossless ~1.37x decode speedup on CUDA. See the PrismML
 llama.cpp fork's README for the serving flags, and download the drafter
 pack from the same HF repo if you want to try it.
* Vision: an optional ~0.63 GB mmproj pack adds image input; it is only
 loaded when an image arrives, so text-only serving never pays for it.
* Quality vs size: if you want more headroom, the ternary sibling
 (prism-ml/Ternary-Bonsai-27B-gguf, ~5.9 GB, ~95% of FP16) is a drop-in
 swap — just change HF_REPO / MODEL_FILE above.
* Shutting down: run  server_proc.kill()  in a later cell to free the GPU.
""")
print("Done. The server is still running — call chat([...]) from new cells!")
</code></pre></div></div>



<p class="wp-block-paragraph">We expose an optional benchmarking switch that measures prompt-processing and token-generation performance with the compiled llama-bench executable. We review advanced deployment options, including long-context inference, 4-bit KV caching, speculative decoding, vision support, and a higher-capacity ternary model variant. We finish while keeping the server process active so that we can continue calling the chat function from additional Colab cells.</p>



<p class="wp-block-paragraph">In conclusion, we established a complete local inference workflow for running Bonsai-27B. We used the PrismML implementation to preserve compatibility with the model’s highly compressed 1.125-bit weight representation while keeping the full inference pipeline accessible through both command-line and OpenAI-compatible interfaces. We validated reasoning, conversational memory, streaming generation, and programming capabilities, while retaining control over sampling parameters, context length, GPU offloading, and KV-cache precision. This setup gives us a platform for experimenting with low-bit large language models, evaluating their efficiency and output quality, and integrating them into Python applications without relying on an external hosted inference service.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/LLM%20Projects/bonsai_27b_1bit_prismml_tutorial_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">Full Code here</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/28/deploying-a-1-bit-bonsai-27b-model-with-prismml-llama-cpp-and-openai-compatible-local-inference-workflows/">Deploying a 1-Bit Bonsai-27B Model with PrismML llama.cpp and OpenAI-Compatible Local Inference Workflows</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81398</post-id>
	</item>
		<item>
		<title>Kimi AI and kvcache-ai Open Sources &#8216;AgentENV&#8217;: A Distributed System that Powers Agentic Reinforcement Learning (RL) Training for Kimi K3</title>
		<link>https://www.marktechpost.com/2026/07/27/kimi-ai-and-kvcache-ai-open-sources-agentenv/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Mon, 27 Jul 2026 20:48:53 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81394</guid>

					<description><![CDATA[<p>Moonshot AI's Kimi team and kvcache-ai open-sourced AgentENV (AENV) under MIT, as part of Kimi K3 Open Day. It runs agent sandboxes as Firecracker microVMs with millisecond snapshot, resume, and 16-way fork, behind an E2B-compatible API.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/27/kimi-ai-and-kvcache-ai-open-sources-agentenv/">Kimi AI and kvcache-ai Open Sources &#8216;AgentENV&#8217;: A Distributed System that Powers Agentic Reinforcement Learning (RL) Training for Kimi K3</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Moonshot AI&#8217;s Kimi team and <a href="https://github.com/kvcache-ai">kvcache-ai</a> have open-sourced <strong><a href="https://github.com/kvcache-ai/AgentENV">AgentENV (AENV)</a></strong>, a distributed platform for running agent environments at scale. AgentENV powers agentic reinforcement learning (RL) training for <strong>Kimi K3</strong>, Moonshot&#8217;s 2.8-trillion-parameter Mixture-of-Experts model. The code ships under an <strong>MIT license</strong>.</p>



<h2 id="h-why-environment-infra-holds-back-agentic-rl" class="wp-block-heading"><strong>Why Environment Infra Holds Back Agentic RL</strong></h2>



<p class="wp-block-paragraph">Agentic RL does not just sample text. It requires the model to act inside a real computer. Every rollout needs an isolated Linux environment with a filesystem, a network stack, and live processes.</p>



<p class="wp-block-paragraph">That requirement creates a hard trade-off. Containers start fast but share the host kernel, which weakens isolation for model-generated code. Full virtual machines isolate properly but boot slowly and hold memory while idle.</p>



<p class="wp-block-paragraph">AgentENV targets exactly that gap. It runs <a href="https://github.com/firecracker-microvm/firecracker">Firecracker</a> microVMs, then makes idle, restart, and branching cheap enough to run at training scale.</p>



<h2 id="h-inside-agentenv-s-firecracker-architecture" class="wp-block-heading"><strong>Inside AgentENV’s Firecracker Architecture</strong></h2>



<p class="wp-block-paragraph">Each sandbox is a Firecracker microVM with its own Linux kernel, filesystem, and network namespace. Requests hit an Axum HTTP API, which forwards to an orchestrator that manages the sandbox lifecycle.</p>



<p class="wp-block-paragraph">Storage is where the design gets interesting. The rootfs is served through a <strong>ublk</strong> userspace block device backed by <strong><a href="https://github.com/containerd/overlaybd">overlaybd</a></strong> layered images. Read-only base layers are shared across sandboxes, and each sandbox writes into its own upper layer.</p>



<p class="wp-block-paragraph">Inside every guest, a daemon called <strong>envd</strong> handles command execution, file operations, and health reporting on port <code>49983</code>. A reverse proxy routes HTTP and WebSocket traffic from clients to services running inside the VM.</p>



<p class="wp-block-paragraph">The project also lists two density mechanisms. The host page cache is shared across storage and memory-snapshot data. Memory ballooning returns reclaimable guest memory to the host, which sustains overcommit as environments diverge over time. </p>



<iframe id="mtp-aenv-explainer" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;style&gt;
  *{box-sizing:border-box;margin:0;padding:0}
  body{background:transparent;font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,&quot;Liberation Mono&quot;,&quot;Courier New&quot;,monospace;-webkit-font-smoothing:antialiased}
  #aenvx{--bg:#0a0a0a;--panel:#121212;--panel2:#171717;--bd:#282828;--or:#FF6B1A;--or2:#FF8C42;--ordim:#7a3510;--w:#FFFFFF;--mut:#9a9a9a;
    background:var(--bg);border:1px solid var(--bd);border-radius:10px;overflow:hidden;color:var(--w);max-width:100%}

  /* title bar */
  .tb{display:flex;align-items:center;gap:10px;background:#0e0e0e;border-bottom:1px solid var(--bd);padding:10px 14px}
  .dots{display:flex;gap:6px}
  .dots i{width:11px;height:11px;border-radius:50%;display:block}
  .d1{background:#FF6B1A}.d2{background:#5c3a22}.d3{background:#2f2f2f}
  .tbt{font-size:12px;color:var(--mut);letter-spacing:.02em;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
  .tbt b{color:var(--or2);font-weight:600}

  /* nav */
  .nav{display:flex;flex-wrap:wrap;gap:6px;padding:12px 14px 0 14px;background:var(--bg)}
  .nav button{font:inherit;font-size:12px;cursor:pointer;background:var(--panel);color:var(--mut);border:1px solid var(--bd);
    border-radius:6px;padding:7px 11px;transition:.15s;white-space:nowrap}
  .nav button:hover{color:var(--w);border-color:#3d3d3d}
  .nav button.on{background:rgba(255,107,26,.13);color:var(--or2);border-color:var(--or)}

  .stage{padding:14px}
  .pane{display:none}
  .pane.on{display:block}

  .cmdline{font-size:12px;color:var(--mut);padding:8px 12px;background:var(--panel);border:1px solid var(--bd);
    border-left:2px solid var(--or);border-radius:0 6px 6px 0;margin-bottom:12px;overflow-x:auto;white-space:nowrap}
  .cmdline .p{color:var(--or)}
  .cmdline .c{color:var(--w)}

  .grid{display:grid;grid-template-columns:minmax(0,0.9fr) minmax(0,1.1fr);gap:12px}
  @media(max-width:640px){.grid{grid-template-columns:1fr}}

  .list{display:flex;flex-direction:column;gap:6px}
  .row{text-align:left;font:inherit;font-size:12.5px;cursor:pointer;background:var(--panel);color:var(--mut);
    border:1px solid var(--bd);border-radius:6px;padding:10px 11px;transition:.15s;display:flex;gap:9px;align-items:flex-start;width:100%}
  .row:hover{border-color:#3d3d3d;color:var(--w)}
  .row.on{background:rgba(255,107,26,.10);border-color:var(--or);color:var(--w)}
  .row .ix{color:var(--or);font-size:11px;flex:0 0 auto;padding-top:1px}
  .row .lb{line-height:1.4}
  .row .sub{display:block;color:#6e6e6e;font-size:10.5px;margin-top:3px}
  .row.on .sub{color:#a86a44}

  .det{background:var(--panel2);border:1px solid var(--bd);border-radius:8px;padding:14px;min-height:190px}
  .det h4{font-size:13px;color:var(--or2);margin-bottom:9px;font-weight:600;letter-spacing:.01em}
  .det p{font-size:12.5px;line-height:1.75;color:#cfcfcf;margin-bottom:9px}
  .det p:last-child{margin-bottom:0}
  .det code{background:#000;color:var(--or2);padding:1px 5px;border-radius:3px;font-size:11.5px;border:1px solid #262626}
  .det ul{list-style:none;margin:0}
  .det li{font-size:12.5px;line-height:1.7;color:#cfcfcf;padding-left:15px;position:relative;margin-bottom:5px}
  .det li:before{content:&quot;\203A&quot;;position:absolute;left:0;color:var(--or)}

  /* perf bars */
  .bars{display:flex;flex-direction:column;gap:13px;background:var(--panel2);border:1px solid var(--bd);border-radius:8px;padding:16px}
  .bar .hd{display:flex;justify-content:space-between;align-items:baseline;font-size:12px;margin-bottom:6px;gap:10px}
  .bar .hd span:first-child{color:#d4d4d4}
  .bar .hd span:last-child{color:var(--or2);font-weight:600;white-space:nowrap}
  .track{height:9px;background:#000;border:1px solid var(--bd);border-radius:5px;overflow:hidden}
  .fill{height:100%;background:linear-gradient(90deg,var(--ordim),var(--or) 60%,var(--or2));width:0;border-radius:4px;
    transition:width 1.1s cubic-bezier(.22,1,.36,1)}
  .bnote{font-size:11px;color:#7d7d7d;line-height:1.6;margin-top:2px}

  .kv{display:grid;grid-template-columns:1fr;gap:0;border:1px solid var(--bd);border-radius:8px;overflow:hidden;margin-top:12px}
  .kv div{display:flex;justify-content:space-between;gap:12px;font-size:12px;padding:9px 12px;background:var(--panel);border-bottom:1px solid var(--bd)}
  .kv div:last-child{border-bottom:0}
  .kv b{color:var(--mut);font-weight:400}
  .kv span{color:var(--w);text-align:right}

  /* lifecycle chips */
  .chips{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:12px}
  .chip{font:inherit;font-size:11.5px;cursor:pointer;background:var(--panel);color:var(--mut);border:1px solid var(--bd);
    border-radius:20px;padding:6px 13px;transition:.15s}
  .chip:hover{color:var(--w);border-color:#3d3d3d}
  .chip.on{background:var(--or);color:#0a0a0a;border-color:var(--or);font-weight:600}

  .warn{font-size:11px;color:#8a8a8a;line-height:1.65;margin-top:12px;padding:9px 11px;background:#0e0e0e;
    border:1px dashed #303030;border-radius:6px}
  .warn b{color:var(--or2);font-weight:600}

  .ft{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px;
    border-top:1px solid var(--bd);background:#0e0e0e;padding:10px 14px;font-size:10.5px;color:#6e6e6e}
  .ft a{color:var(--or);text-decoration:none}
  .ft a:hover{text-decoration:underline}
  .ft .brand{color:var(--or)}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;aenvx&quot;&gt;

  &lt;div class=&quot;tb&quot;&gt;
    &lt;div class=&quot;dots&quot;&gt;&lt;i class=&quot;d1&quot;&gt;&lt;/i&gt;&lt;i class=&quot;d2&quot;&gt;&lt;/i&gt;&lt;i class=&quot;d3&quot;&gt;&lt;/i&gt;&lt;/div&gt;
    &lt;div class=&quot;tbt&quot;&gt;&lt;b&gt;aenv&lt;/b&gt;@kvcache-ai:~$ agentenv --explain &lt;b&gt;AgentENV (AENV)&lt;/b&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;nav&quot; id=&quot;nav&quot;&gt;
    &lt;button class=&quot;on&quot; data-p=&quot;arch&quot;&gt;--architecture&lt;/button&gt;
    &lt;button data-p=&quot;life&quot;&gt;--lifecycle&lt;/button&gt;
    &lt;button data-p=&quot;perf&quot;&gt;--performance&lt;/button&gt;
    &lt;button data-p=&quot;store&quot;&gt;--storage&lt;/button&gt;
    &lt;button data-p=&quot;net&quot;&gt;--network&lt;/button&gt;
  &lt;/div&gt;

  &lt;div class=&quot;stage&quot;&gt;

    &lt;!-- ARCH --&gt;
    &lt;div class=&quot;pane on&quot; id=&quot;p-arch&quot;&gt;
      &lt;div class=&quot;cmdline&quot;&gt;&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;c&quot;&gt;aenv describe --components&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;grid&quot;&gt;
        &lt;div class=&quot;list&quot; id=&quot;archlist&quot;&gt;
          &lt;button class=&quot;row on&quot; data-k=&quot;a0&quot;&gt;&lt;span class=&quot;ix&quot;&gt;01&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;API Server&lt;span class=&quot;sub&quot;&gt;Axum HTTP, E2B-compatible endpoints&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;a1&quot;&gt;&lt;span class=&quot;ix&quot;&gt;02&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Orchestrator&lt;span class=&quot;sub&quot;&gt;Sandbox lifecycle state machine&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;a2&quot;&gt;&lt;span class=&quot;ix&quot;&gt;03&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Firecracker microVM&lt;span class=&quot;sub&quot;&gt;Kernel-level isolation per sandbox&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;a3&quot;&gt;&lt;span class=&quot;ix&quot;&gt;04&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Block Device Layer&lt;span class=&quot;sub&quot;&gt;overlaybd + ublk, copy-on-write&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;a4&quot;&gt;&lt;span class=&quot;ix&quot;&gt;05&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;envd&lt;span class=&quot;sub&quot;&gt;In-guest daemon, port 49983&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;a5&quot;&gt;&lt;span class=&quot;ix&quot;&gt;06&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Reverse Proxy&lt;span class=&quot;sub&quot;&gt;HTTP + WebSocket into sandboxes&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;a6&quot;&gt;&lt;span class=&quot;ix&quot;&gt;07&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Snapshot Manager&lt;span class=&quot;sub&quot;&gt;Committed snapshot repository&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;a7&quot;&gt;&lt;span class=&quot;ix&quot;&gt;08&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Gateway + Scheduler&lt;span class=&quot;sub&quot;&gt;Multi-node control plane (prototype)&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
        &lt;/div&gt;
        &lt;div class=&quot;det&quot; id=&quot;archdet&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;!-- LIFECYCLE --&gt;
    &lt;div class=&quot;pane&quot; id=&quot;p-life&quot;&gt;
      &lt;div class=&quot;cmdline&quot;&gt;&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;c&quot;&gt;aenv describe --sandbox-states&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;chips&quot; id=&quot;chips&quot;&gt;
        &lt;button class=&quot;chip on&quot; data-k=&quot;s0&quot;&gt;Creating&lt;/button&gt;
        &lt;button class=&quot;chip&quot; data-k=&quot;s1&quot;&gt;Running&lt;/button&gt;
        &lt;button class=&quot;chip&quot; data-k=&quot;s2&quot;&gt;Pausing&lt;/button&gt;
        &lt;button class=&quot;chip&quot; data-k=&quot;s3&quot;&gt;Paused&lt;/button&gt;
        &lt;button class=&quot;chip&quot; data-k=&quot;s4&quot;&gt;Resuming&lt;/button&gt;
        &lt;button class=&quot;chip&quot; data-k=&quot;s5&quot;&gt;Snapshotting&lt;/button&gt;
        &lt;button class=&quot;chip&quot; data-k=&quot;s6&quot;&gt;Forking&lt;/button&gt;
        &lt;button class=&quot;chip&quot; data-k=&quot;s7&quot;&gt;Killing&lt;/button&gt;
      &lt;/div&gt;
      &lt;div class=&quot;det&quot; id=&quot;lifedet&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;warn&quot;&gt;&lt;b&gt;Note:&lt;/b&gt; A sandbox TTL defaults to auto-&lt;b&gt;pause&lt;/b&gt; on expiry, not delete. Pass &lt;code&gt;autoPause: false&lt;/code&gt; on the create API to delete instead.&lt;/div&gt;
    &lt;/div&gt;

    &lt;!-- PERF --&gt;
    &lt;div class=&quot;pane&quot; id=&quot;p-perf&quot;&gt;
      &lt;div class=&quot;cmdline&quot;&gt;&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;c&quot;&gt;aenv describe --latency-targets&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;bars&quot; id=&quot;bars&quot;&gt;
        &lt;div class=&quot;bar&quot;&gt;&lt;div class=&quot;hd&quot;&gt;&lt;span&gt;Boot or resume from snapshot&lt;/span&gt;&lt;span&gt;&amp;lt; 50 ms&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; data-w=&quot;25&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnote&quot;&gt;Snapshot-backed environments; restores running processes and open connections.&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;bar&quot;&gt;&lt;div class=&quot;hd&quot;&gt;&lt;span&gt;Pause a running environment&lt;/span&gt;&lt;span&gt;&amp;lt; 100 ms&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; data-w=&quot;50&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnote&quot;&gt;Captures a memory snapshot plus a disk snapshot of the writable layer.&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;bar&quot;&gt;&lt;div class=&quot;hd&quot;&gt;&lt;span&gt;Incremental snapshot capture&lt;/span&gt;&lt;span&gt;&amp;lt; 100 ms&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; data-w=&quot;50&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnote&quot;&gt;Holds even under heavy disk modification, per the project README.&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;bar&quot;&gt;&lt;div class=&quot;hd&quot;&gt;&lt;span&gt;Fork fan-out per source sandbox&lt;/span&gt;&lt;span&gt;up to 16&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; data-w=&quot;80&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnote&quot;&gt;Children land on the same node and inherit filesystem, memory and resources.&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;kv&quot;&gt;
        &lt;div&gt;&lt;b&gt;Default guest RAM&lt;/b&gt;&lt;span&gt;1024 MiB&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;&lt;b&gt;Default vCPU count&lt;/b&gt;&lt;span&gt;2&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;&lt;b&gt;Default sandbox timeout&lt;/b&gt;&lt;span&gt;15 s&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;&lt;b&gt;CLI start / resume TTL&lt;/b&gt;&lt;span&gt;300 s&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;&lt;b&gt;envd control-plane port&lt;/b&gt;&lt;span&gt;49983&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;&lt;b&gt;Warm pool watermarks&lt;/b&gt;&lt;span&gt;2 low / 64 high&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;warn&quot;&gt;&lt;b&gt;Accuracy note:&lt;/b&gt; These latency figures are &lt;b&gt;reported by the AgentENV project&lt;/b&gt; in its README and documentation. No independent third-party benchmark has been published.&lt;/div&gt;
    &lt;/div&gt;

    &lt;!-- STORAGE --&gt;
    &lt;div class=&quot;pane&quot; id=&quot;p-store&quot;&gt;
      &lt;div class=&quot;cmdline&quot;&gt;&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;c&quot;&gt;aenv describe --snapshot-storage&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;grid&quot;&gt;
        &lt;div class=&quot;list&quot; id=&quot;stlist&quot;&gt;
          &lt;button class=&quot;row on&quot; data-k=&quot;t0&quot;&gt;&lt;span class=&quot;ix&quot;&gt;L1&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Builder staging&lt;span class=&quot;sub&quot;&gt;Manager-owned temporary workspace&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;t1&quot;&gt;&lt;span class=&quot;ix&quot;&gt;L2&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Committed snapshot repository&lt;span class=&quot;sub&quot;&gt;Durable source of truth&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;t2&quot;&gt;&lt;span class=&quot;ix&quot;&gt;L3&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Node-local runtime cache&lt;span class=&quot;sub&quot;&gt;Launch-time derived configs&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;t3&quot;&gt;&lt;span class=&quot;ix&quot;&gt;BE&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Repository backends&lt;span class=&quot;sub&quot;&gt;posix_fs and oss&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;t4&quot;&gt;&lt;span class=&quot;ix&quot;&gt;BD&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;On-demand image loading&lt;span class=&quot;sub&quot;&gt;overlaybd bounded local cache&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;t5&quot;&gt;&lt;span class=&quot;ix&quot;&gt;P2P&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Optional P2P visibility&lt;span class=&quot;sub&quot;&gt;iroh transport, off by default&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
        &lt;/div&gt;
        &lt;div class=&quot;det&quot; id=&quot;stdet&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;!-- NET --&gt;
    &lt;div class=&quot;pane&quot; id=&quot;p-net&quot;&gt;
      &lt;div class=&quot;cmdline&quot;&gt;&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;c&quot;&gt;aenv describe --network --compat&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;grid&quot;&gt;
        &lt;div class=&quot;list&quot; id=&quot;netlist&quot;&gt;
          &lt;button class=&quot;row on&quot; data-k=&quot;n0&quot;&gt;&lt;span class=&quot;ix&quot;&gt;01&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Per-sandbox network namespace&lt;span class=&quot;sub&quot;&gt;Egress on by default&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;n1&quot;&gt;&lt;span class=&quot;ix&quot;&gt;02&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Node-level egress guardrails&lt;span class=&quot;sub&quot;&gt;always_denied_cidrs&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;n2&quot;&gt;&lt;span class=&quot;ix&quot;&gt;03&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Per-sandbox allowOut / denyOut&lt;span class=&quot;sub&quot;&gt;Editable on a running sandbox&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;n3&quot;&gt;&lt;span class=&quot;ix&quot;&gt;04&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Sandbox proxy domains&lt;span class=&quot;sub&quot;&gt;{port}-{sandboxID}.{domain}&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;n4&quot;&gt;&lt;span class=&quot;ix&quot;&gt;05&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;E2B API compatibility&lt;span class=&quot;sub&quot;&gt;Existing SDK code, unmodified&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
          &lt;button class=&quot;row&quot; data-k=&quot;n5&quot;&gt;&lt;span class=&quot;ix&quot;&gt;06&lt;/span&gt;&lt;span class=&quot;lb&quot;&gt;Custom extension hooks&lt;span class=&quot;sub&quot;&gt;VPN, firewall, extra mounts&lt;/span&gt;&lt;/span&gt;&lt;/button&gt;
        &lt;/div&gt;
        &lt;div class=&quot;det&quot; id=&quot;netdet&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;Sources: kvcache-ai/AgentENV README + official docs &amp;middot; MIT license &amp;middot; verified 27 Jul 2026&lt;/span&gt;
    &lt;span class=&quot;brand&quot;&gt;&amp;copy; Marktechpost&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var DATA = {
    a0:{t:&quot;API Server (Axum)&quot;,h:&quot;&lt;p&gt;The HTTP entry point. It validates the request, checks authentication, then forwards it to the orchestrator.&lt;/p&gt;&lt;p&gt;It exposes &lt;code&gt;E2B-compatible&lt;/code&gt; endpoints for sandbox and template management, which is why unmodified E2B SDK code works against it.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Default listen address &lt;code&gt;0.0.0.0:8000&lt;/code&gt;&lt;/li&gt;&lt;li&gt;Health probe at &lt;code&gt;GET /health&lt;/code&gt;&lt;/li&gt;&lt;/ul&gt;&quot;},
    a1:{t:&quot;Orchestrator&quot;,h:&quot;&lt;p&gt;A state machine that manages sandbox lifecycle transitions: create, pause, resume, snapshot, fork and delete.&lt;/p&gt;&lt;p&gt;It also runs background timeout eviction, polling every &lt;code&gt;1000 ms&lt;/code&gt; by default, and can auto-resume a paused sandbox when a data-plane request arrives for it.&lt;/p&gt;&quot;},
    a2:{t:&quot;Firecracker microVM&quot;,h:&quot;&lt;p&gt;Every sandbox is a Firecracker microVM with its own Linux kernel, filesystem and network stack. Isolation is kernel-level, not namespace-level.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Requires &lt;code&gt;/dev/kvm&lt;/code&gt; on the host&lt;/li&gt;&lt;li&gt;Requires Linux kernel &lt;code&gt;6.8+&lt;/code&gt;&lt;/li&gt;&lt;li&gt;Warm Firecracker processes are pre-spawned for snapshot resume&lt;/li&gt;&lt;/ul&gt;&quot;},
    a3:{t:&quot;Block Device Layer (overlaybd + ublk)&quot;,h:&quot;&lt;p&gt;Rootfs is served through a userspace block device managed by &lt;code&gt;uvm-ublk-daemon&lt;/code&gt;, backed by overlaybd layered images.&lt;/p&gt;&lt;p&gt;Read-only base layers are shared across sandboxes; each sandbox gets a writable upper layer. Local disk acts as a bounded cache that evicts cold data, so total image size can exceed disk capacity.&lt;/p&gt;&quot;},
    a4:{t:&quot;envd (in-guest daemon)&quot;,h:&quot;&lt;p&gt;Runs inside every microVM and handles command execution, file operations, output streaming and health reporting.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Control-plane port &lt;code&gt;49983&lt;/code&gt;&lt;/li&gt;&lt;li&gt;Expected version &lt;code&gt;0.5.15&lt;/code&gt;, baked into the tools drive&lt;/li&gt;&lt;li&gt;Init timeout &lt;code&gt;60 s&lt;/code&gt;&lt;/li&gt;&lt;/ul&gt;&quot;},
    a5:{t:&quot;Reverse Proxy&quot;,h:&quot;&lt;p&gt;Routes client HTTP and WebSocket traffic to services running inside a sandbox.&lt;/p&gt;&lt;p&gt;Three addressing modes are supported: the &lt;code&gt;/proxy&lt;/code&gt; prefix, routing headers, and host-based URLs shaped like &lt;code&gt;{port}-{sandboxID}.{domain}&lt;/code&gt; when proxy domains are configured.&lt;/p&gt;&quot;},
    a6:{t:&quot;Snapshot Manager&quot;,h:&quot;&lt;p&gt;Owns committed snapshots, which are the durable runtime primitive. Templates and sandbox launches are both built on them.&lt;/p&gt;&lt;p&gt;A committed snapshot is split into &lt;code&gt;snapshot.json&lt;/code&gt; (context, rootfs, drive and memory layer references) and &lt;code&gt;firecracker-manifest.json&lt;/code&gt; (launch metadata such as virtual sizes).&lt;/p&gt;&quot;},
    a7:{t:&quot;Gateway + Scheduler&quot;,h:&quot;&lt;p&gt;The multi-node control plane, described in the docs as a &lt;b&gt;prototype&lt;/b&gt;. The gateway is an HTTP/WebSocket reverse proxy on &lt;code&gt;:8080&lt;/code&gt;; the scheduler does gRPC node selection and sandbox binding on &lt;code&gt;:9090&lt;/code&gt;.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Strategies: &lt;code&gt;round_robin&lt;/code&gt; (default) or &lt;code&gt;random&lt;/code&gt;&lt;/li&gt;&lt;li&gt;Bindings are in-memory unless &lt;code&gt;SCHEDULER_REDIS_ADDR&lt;/code&gt; is set&lt;/li&gt;&lt;li&gt;Run the scheduler as a single replica&lt;/li&gt;&lt;/ul&gt;&quot;},

    s0:{t:&quot;Creating&quot;,h:&quot;&lt;p&gt;The VM is booting, block devices are being attached, and networking is being configured.&lt;/p&gt;&lt;p&gt;Two entry paths exist: a &lt;b&gt;warm start&lt;/b&gt; from a template snapshot, and a &lt;b&gt;cold start&lt;/b&gt; that pulls an OCI image and converts it into a block device at runtime.&lt;/p&gt;&lt;p&gt;&lt;code&gt;aenv start my-base&lt;/code&gt; &amp;nbsp;/&amp;nbsp; &lt;code&gt;aenv start --cold ubuntu:24.04&lt;/code&gt;&lt;/p&gt;&quot;},
    s1:{t:&quot;Running&quot;,h:&quot;&lt;p&gt;The VM is ready. Commands execute, proxy traffic is routed, and the TTL clock is ticking.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;code&gt;aenv connect &amp;lt;sandbox-id&amp;gt;&lt;/code&gt; attaches a shell&lt;/li&gt;&lt;li&gt;&lt;code&gt;aenv exec &amp;lt;sandbox-id&amp;gt; ls -la /&lt;/code&gt; runs one command&lt;/li&gt;&lt;li&gt;&lt;code&gt;aenv timeout &amp;lt;sandbox-id&amp;gt; 600&lt;/code&gt; extends the TTL&lt;/li&gt;&lt;/ul&gt;&quot;},
    s2:{t:&quot;Pausing&quot;,h:&quot;&lt;p&gt;Memory and disk snapshots are captured. The README states pause completes in under &lt;code&gt;100 ms&lt;/code&gt;.&lt;/p&gt;&lt;p&gt;Pausing also releases the VM process and the network namespace, so idle environments stop consuming CPU and memory.&lt;/p&gt;&lt;p&gt;&lt;code&gt;aenv pause &amp;lt;sandbox-id&amp;gt;&lt;/code&gt;&lt;/p&gt;&quot;},
    s3:{t:&quot;Paused&quot;,h:&quot;&lt;p&gt;The VM is stopped and snapshot artifacts are stored. No resources are consumed.&lt;/p&gt;&lt;p&gt;Paused sandbox state is persisted under &lt;code&gt;$AENV_HOME/persisted-sandboxes/&lt;/code&gt; so it survives a server restart.&lt;/p&gt;&quot;},
    s4:{t:&quot;Resuming&quot;,h:&quot;&lt;p&gt;The sandbox is restored from its paused snapshot. Per the README, resume completes in under &lt;code&gt;50 ms&lt;/code&gt;.&lt;/p&gt;&lt;p&gt;It picks up exactly where it left off, including running processes and open network connections.&lt;/p&gt;&lt;p&gt;&lt;code&gt;aenv resume &amp;lt;sandbox-id&amp;gt;&lt;/code&gt;&lt;/p&gt;&quot;},
    s5:{t:&quot;Snapshotting&quot;,h:&quot;&lt;p&gt;A persistent snapshot is captured from a &lt;b&gt;running&lt;/b&gt; sandbox, which then returns to Running.&lt;/p&gt;&lt;p&gt;Recoverable failures leave the sandbox running. Terminal failures, where the runtime was mutated past safe resume, tear the sandbox down.&lt;/p&gt;&lt;p&gt;&lt;code&gt;aenv snapshot create &amp;lt;sandbox-id&amp;gt; --name my-checkpoint&lt;/code&gt;&lt;/p&gt;&quot;},
    s6:{t:&quot;Forking&quot;,h:&quot;&lt;p&gt;A running sandbox is cloned into up to &lt;b&gt;16&lt;/b&gt; child sandboxes on the same node. The source is briefly paused, then resumes.&lt;/p&gt;&lt;p&gt;Children inherit the source filesystem, memory and resource configuration &amp;mdash; the primitive that makes parallel rollouts from one prepared state practical.&lt;/p&gt;&lt;p&gt;&lt;code&gt;POST /sandboxes/&amp;lt;id&amp;gt;/fork&lt;/code&gt; with &lt;code&gt;{\&quot;count\&quot;: 3}&lt;/code&gt;&lt;/p&gt;&quot;},
    s7:{t:&quot;Killing&quot;,h:&quot;&lt;p&gt;The VM is torn down and resources are released.&lt;/p&gt;&lt;p&gt;&lt;code&gt;aenv delete &amp;lt;sandbox-id&amp;gt;&lt;/code&gt; (alias &lt;code&gt;aenv rm&lt;/code&gt;)&lt;/p&gt;&quot;},

    t0:{t:&quot;Layer 1 &amp;mdash; Builder staging&quot;,h:&quot;&lt;p&gt;A manager-owned temporary workspace used while executing a single build. It holds local snapshot artifacts captured from the build sandbox.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;code&gt;vm_state.bin&lt;/code&gt;&lt;/li&gt;&lt;li&gt;&lt;code&gt;mem_image.json&lt;/code&gt;&lt;/li&gt;&lt;li&gt;&lt;code&gt;rootfs/image.json&lt;/code&gt;, &lt;code&gt;drives/&amp;lt;id&amp;gt;/image.json&lt;/code&gt;&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;This is a &lt;code&gt;TemplateBuilder&lt;/code&gt; implementation detail, not the durable record.&lt;/p&gt;&quot;},
    t1:{t:&quot;Layer 2 &amp;mdash; Committed snapshot repository&quot;,h:&quot;&lt;p&gt;The durable source of truth, wrapped by the template API.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;code&gt;repository/catalog/aliases/&lt;/code&gt;&lt;/li&gt;&lt;li&gt;&lt;code&gt;repository/snapshots/&amp;lt;id&amp;gt;/snapshot.json&lt;/code&gt;&lt;/li&gt;&lt;li&gt;&lt;code&gt;repository/snapshots/&amp;lt;id&amp;gt;/firecracker-manifest.json&lt;/code&gt;&lt;/li&gt;&lt;li&gt;&lt;code&gt;repository/managed-layers/&amp;lt;digest&amp;gt;.overlaybd.commit&lt;/code&gt;&lt;/li&gt;&lt;/ul&gt;&quot;},
    t2:{t:&quot;Layer 3 &amp;mdash; Node-local runtime cache&quot;,h:&quot;&lt;p&gt;Before launch, AgentENV derives runnable overlaybd configs from the committed snapshot and materializes them locally.&lt;/p&gt;&lt;p&gt;These are launch-time derived inputs, not committed metadata. &lt;code&gt;memory_layers&lt;/code&gt;, &lt;code&gt;rootfs.layers&lt;/code&gt; and per-drive layers each resolve into a local &lt;code&gt;image.json&lt;/code&gt;.&lt;/p&gt;&quot;},
    t3:{t:&quot;Repository backends&quot;,h:&quot;&lt;p&gt;Set with &lt;code&gt;[snapshot].repository_backend&lt;/code&gt;.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;code&gt;posix_fs&lt;/code&gt; (default) &amp;mdash; a shared distributed filesystem path&lt;/li&gt;&lt;li&gt;&lt;code&gt;oss&lt;/code&gt; &amp;mdash; implemented through a shared S3-compatible client, so &lt;code&gt;region&lt;/code&gt; is required&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Shared storage wants at least 1 Gbps; the docs strongly recommend 10 Gbps or faster.&lt;/p&gt;&quot;},
    t4:{t:&quot;On-demand image loading&quot;,h:&quot;&lt;p&gt;Images load on demand through overlaybd. Local disk is a bounded cache that keeps hot data and evicts cold data.&lt;/p&gt;&lt;p&gt;Nodes therefore do not need to pre-warm every image or hold a complete copy of every snapshot, so the addressable image set can exceed local disk.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Image cache budget defaults to &lt;code&gt;100 GB&lt;/code&gt;&lt;/li&gt;&lt;li&gt;Remote block cache defaults to &lt;code&gt;10 GB&lt;/code&gt;&lt;/li&gt;&lt;/ul&gt;&quot;},
    t5:{t:&quot;Optional P2P visibility&quot;,h:&quot;&lt;p&gt;Disabled by default. When &lt;code&gt;[p2p].enabled = true&lt;/code&gt;, the &lt;code&gt;iroh&lt;/code&gt; transport advertises committed artifacts to peer nodes.&lt;/p&gt;&lt;p&gt;P2P does not change the committed snapshot model. The repository stays the source of truth, and a failed P2P publish does not roll back a successful commit.&lt;/p&gt;&quot;},

    n0:{t:&quot;Per-sandbox network namespace&quot;,h:&quot;&lt;p&gt;Each sandbox runs in its own network namespace. Outbound internet access is enabled by default.&lt;/p&gt;&lt;p&gt;Disable it at creation with &lt;code&gt;allowInternetAccess: false&lt;/code&gt; on &lt;code&gt;POST /sandboxes&lt;/code&gt;.&lt;/p&gt;&quot;},
    n1:{t:&quot;Node-level egress guardrails&quot;,h:&quot;&lt;p&gt;Installed &lt;b&gt;before&lt;/b&gt; per-sandbox rules, so a sandbox API request cannot override them.&lt;/p&gt;&lt;p&gt;Default &lt;code&gt;always_denied_cidrs&lt;/code&gt; covers &lt;code&gt;10.0.0.0/8&lt;/code&gt;, &lt;code&gt;100.64.0.0/10&lt;/code&gt;, &lt;code&gt;127.0.0.0/8&lt;/code&gt;, &lt;code&gt;169.254.0.0/16&lt;/code&gt;, &lt;code&gt;172.16.0.0/12&lt;/code&gt; and &lt;code&gt;192.168.0.0/16&lt;/code&gt;.&lt;/p&gt;&quot;},
    n2:{t:&quot;Per-sandbox allowOut / denyOut&quot;,h:&quot;&lt;p&gt;Pass a &lt;code&gt;network&lt;/code&gt; object at creation. Allowed entries take precedence over denied entries.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;code&gt;allowOut&lt;/code&gt; accepts CIDR, IP or domain patterns&lt;/li&gt;&lt;li&gt;&lt;code&gt;denyOut&lt;/code&gt; accepts CIDR or IP&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Rules can be updated live via &lt;code&gt;PUT /sandboxes/&amp;lt;id&amp;gt;/network&lt;/code&gt;.&lt;/p&gt;&quot;},
    n3:{t:&quot;Sandbox proxy domains&quot;,h:&quot;&lt;p&gt;Configure &lt;code&gt;[sandbox_proxy].domains&lt;/code&gt; to enable host-based data-plane URLs shaped like &lt;code&gt;{port}-{sandboxID}.{domain}&lt;/code&gt;.&lt;/p&gt;&lt;p&gt;The domain must resolve to the gateway, usually via wildcard DNS for &lt;code&gt;*.sandbox.example.com&lt;/code&gt;. The first configured domain is what sandbox responses advertise.&lt;/p&gt;&quot;},
    n4:{t:&quot;E2B API compatibility&quot;,h:&quot;&lt;p&gt;AgentENV exposes an E2B-compatible HTTP API. Point &lt;code&gt;E2B_API_URL&lt;/code&gt; at your server and the official Python or TypeScript SDK works without code changes.&lt;/p&gt;&lt;p&gt;The E2B CLI also works, though the docs recommend the native &lt;code&gt;aenv&lt;/code&gt; CLI for AgentENV workflows.&lt;/p&gt;&quot;},
    n5:{t:&quot;Custom extension hooks&quot;,h:&quot;&lt;p&gt;An optional external HTTP service AgentENV calls during the sandbox lifecycle, so you can add VPN wiring, firewall rules or extra mounts without forking the project.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;code&gt;POST {url}/sandbox-hook/start-fresh&lt;/code&gt;&lt;/li&gt;&lt;li&gt;&lt;code&gt;/start-resume&lt;/code&gt;, &lt;code&gt;/patch-params&lt;/code&gt;, &lt;code&gt;/stop&lt;/code&gt;&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Fully disabled when &lt;code&gt;[custom_extension].url&lt;/code&gt; is unset.&lt;/p&gt;&quot;}
  };

  function render(el,key){var d=DATA[key];if(!d)return;el.innerHTML=&quot;&lt;h4&gt;&quot;+d.t+&quot;&lt;/h4&gt;&quot;+d.h;ping();}

  function wire(listId,detId,first){
    var list=document.getElementById(listId),det=document.getElementById(detId);
    if(!list)return;
    list.addEventListener(&quot;click&quot;,function(e){
      var b=e.target.closest(&quot;button&quot;);if(!b||!b.dataset.k)return;
      list.querySelectorAll(&quot;button&quot;).forEach(function(x){x.classList.remove(&quot;on&quot;);});
      b.classList.add(&quot;on&quot;);render(det,b.dataset.k);
    });
    render(det,first);
  }

  wire(&quot;archlist&quot;,&quot;archdet&quot;,&quot;a0&quot;);
  wire(&quot;chips&quot;,&quot;lifedet&quot;,&quot;s0&quot;);
  wire(&quot;stlist&quot;,&quot;stdet&quot;,&quot;t0&quot;);
  wire(&quot;netlist&quot;,&quot;netdet&quot;,&quot;n0&quot;);

  var fired=false;
  function runBars(){
    if(fired)return;fired=true;
    document.querySelectorAll(&quot;#bars .fill&quot;).forEach(function(f,i){
      setTimeout(function(){f.style.width=f.dataset.w+&quot;%&quot;;},120*i+80);
    });
  }

  var nav=document.getElementById(&quot;nav&quot;);
  nav.addEventListener(&quot;click&quot;,function(e){
    var b=e.target.closest(&quot;button&quot;);if(!b||!b.dataset.p)return;
    nav.querySelectorAll(&quot;button&quot;).forEach(function(x){x.classList.remove(&quot;on&quot;);});
    b.classList.add(&quot;on&quot;);
    document.querySelectorAll(&quot;.pane&quot;).forEach(function(p){p.classList.remove(&quot;on&quot;);});
    var pane=document.getElementById(&quot;p-&quot;+b.dataset.p);
    if(pane)pane.classList.add(&quot;on&quot;);
    if(b.dataset.p===&quot;perf&quot;)runBars();
    ping();
  });

  function ping(){
    var el=document.getElementById(&quot;aenvx&quot;);
    if(!el)return;
    parent.postMessage({aenvHeight:el.offsetHeight+40},&quot;*&quot;);
  }
  window.addEventListener(&quot;load&quot;,ping);
  window.addEventListener(&quot;resize&quot;,ping);
  setTimeout(ping,60);setTimeout(ping,400);setTimeout(ping,1200);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
" style="width:100%;height:900px;border:0;overflow:hidden;display:block;" scrolling="no" loading="lazy" title="AgentENV interactive explainer"></iframe>
<script>
(function(){
  var f=document.getElementById("mtp-aenv-explainer");
  window.addEventListener("message",function(e){
    if(!e.data||typeof e.data!=="object")return;
    var h=e.data.aenvHeight;
    if(typeof h==="number"&&h>120&&h<20000){f.style.height=h+"px";}
  });
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-snapshot-pause-resume-fork" class="wp-block-heading"><strong>Snapshot, pause, resume, fork</strong></h2>



<p class="wp-block-paragraph">These four features are the reason the project exists. AgentENV snapshots memory and filesystem changes incrementally rather than writing a full image each time.</p>



<p class="wp-block-paragraph">The reported figures are as follows. Snapshot-backed environments boot or resume in under <strong>50 ms</strong> and pause in under <strong>100 ms</strong>. Incremental snapshot capture completes in under <strong>100 ms</strong>, even under heavy disk modification.</p>



<p class="wp-block-paragraph">Fork is the feature most specific to RL. A running sandbox can clone into <strong>up to 16 independent child sandboxes</strong> on the same node. The source pauses briefly during capture, then resumes. Every child inherits the source filesystem, memory, and resource configuration.</p>



<p class="wp-block-paragraph">The practical effect is that expensive setup runs once. A team can install dependencies, clone a repository, and reach a task state. That exact state then branches into parallel rollouts. Snapshots persist to S3-compatible object storage or a shared distributed filesystem.</p>



<p class="wp-block-paragraph">One default is worth noting. Every sandbox carries a TTL, and expiry triggers a <strong>pause</strong>, not a delete. Deletion requires passing <code>autoPause: false</code> on the create API.</p>



<h2 id="h-on-demand-loading-and-the-snapshot-repository" class="wp-block-heading"><strong>On-demand loading and the snapshot repository</strong></h2>



<p class="wp-block-paragraph">Images load on demand through overlaybd. Local disk acts as a bounded cache that retains hot data and evicts cold data.</p>



<p class="wp-block-paragraph">This is what makes the fleet-level story work. Nodes do not need to pre-warm every image or hold a complete copy of every snapshot. The addressable image set can therefore exceed local disk capacity while startup stays fast cluster-wide.</p>



<p class="wp-block-paragraph">Snapshot state is organized in three layers. A builder staging workspace holds artifacts during a build. A committed snapshot repository is the durable source of truth. A node-local runtime cache holds launch-time derived configs.</p>



<p class="wp-block-paragraph">Two repository backends are supported: <code>posix_fs</code> (default) and <code>oss</code>. The <code>oss</code> path runs through a shared S3-compatible client, so an explicit <code>region</code> is required.</p>



<p class="wp-block-paragraph">An optional peer-to-peer transport based on <code>iroh</code> can advertise committed artifacts to peer nodes. It is <strong>disabled by default</strong>. The documentation is explicit that P2P does not change the committed snapshot model. For shared storage, the docs ask for at least 1 Gbps and strongly recommend 10 Gbps or faster.</p>



<h2 id="h-e2b-compatibility-is-the-adoption-lever" class="wp-block-heading"><strong>E2B compatibility is the adoption lever</strong></h2>



<p class="wp-block-paragraph">AgentENV exposes an <strong><a href="https://kvcache-ai.github.io/AgentENV/integration/e2b.html">E2B-compatible</a> HTTP API</strong>. Point <code>E2B_API_URL</code> at your server and the official <a href="https://github.com/e2b-dev/e2b">E2B</a> Python or TypeScript SDK works without code changes.</p>



<p class="wp-block-paragraph">This is a deliberate distribution choice. Teams already running agents on E2B can self-host the runtime without rewriting their agent code. A native <code>aenv</code> CLI is also provided, and the docs recommend it for AgentENV-specific workflows.</p>



<h2 id="h-deployment-paths" class="wp-block-heading"><strong>Deployment paths</strong></h2>



<p class="wp-block-paragraph">Prerequisites are Linux kernel <strong>6.8+</strong> and <code>/dev/kvm</code> access; the install script additionally requires Ubuntu 24.04. The <code>aenv</code> CLI supports Linux and macOS on x86_64 and arm64. The server itself is Linux-only, because it needs KVM.</p>



<p class="wp-block-paragraph"><strong>Five paths are documented:</strong></p>



<ul class="wp-block-list">
<li>An install script that runs the server as a systemd service</li>



<li>A Docker image published at <code>ghcr.io/kvcache-ai/aenv-server</code></li>



<li>A Docker Compose stack that simulates a multi-node cluster</li>



<li>Kubernetes manifests with a gateway, scheduler, and node DaemonSet</li>



<li>A build-from-source route using the Rust toolchain</li>
</ul>



<p class="wp-block-paragraph">Multi-node deployments add a <strong>gateway</strong> on <code>:8080</code> and a <strong>scheduler</strong> on <code>:9090</code>. </p>



<iframe id="mtp-aenv-deploy" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;style&gt;
  *{box-sizing:border-box;margin:0;padding:0}
  body{background:transparent;font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,&quot;Liberation Mono&quot;,&quot;Courier New&quot;,monospace;-webkit-font-smoothing:antialiased}
  #aenvdep{--bg:#0a0a0a;--panel:#121212;--panel2:#171717;--bd:#282828;--or:#FF6B1A;--or2:#FF8C42;--w:#FFFFFF;--mut:#9a9a9a;
    background:var(--bg);border:1px solid var(--bd);border-radius:10px;overflow:hidden;color:var(--w);max-width:100%}

  .hd{background:#0e0e0e;border-bottom:1px solid var(--bd);padding:14px}
  .hd h3{font-size:14px;color:var(--w);font-weight:600;letter-spacing:.01em;margin-bottom:5px}
  .hd h3 em{font-style:normal;color:var(--or)}
  .hd p{font-size:11.5px;color:var(--mut);line-height:1.6}

  .pre{display:flex;flex-wrap:wrap;gap:6px;padding:12px 14px;background:#0c0c0c;border-bottom:1px solid var(--bd)}
  .pill{font-size:10.5px;color:#c9c9c9;background:var(--panel);border:1px solid var(--bd);border-radius:20px;padding:5px 11px;white-space:nowrap}
  .pill b{color:var(--or2);font-weight:600}

  .tabs{display:flex;flex-wrap:wrap;gap:6px;padding:12px 14px 0 14px}
  .tabs button{font:inherit;font-size:11.5px;cursor:pointer;background:var(--panel);color:var(--mut);border:1px solid var(--bd);
    border-radius:6px;padding:8px 12px;transition:.15s;white-space:nowrap}
  .tabs button:hover{color:var(--w);border-color:#3d3d3d}
  .tabs button.on{background:rgba(255,107,26,.13);color:var(--or2);border-color:var(--or)}

  .body{padding:14px}
  .path{display:none}
  .path.on{display:block}

  .meta{display:flex;flex-wrap:wrap;gap:14px;background:var(--panel2);border:1px solid var(--bd);border-radius:8px;padding:11px 13px;margin-bottom:14px}
  .meta div{font-size:11px;color:var(--mut);line-height:1.5}
  .meta div span{display:block;color:var(--w);font-size:12px;margin-top:2px}

  .step{position:relative;padding-left:34px;padding-bottom:18px}
  .step:last-child{padding-bottom:4px}
  .step:before{content:attr(data-n);position:absolute;left:0;top:0;width:23px;height:23px;border-radius:50%;
    background:rgba(255,107,26,.13);border:1px solid var(--or);color:var(--or2);font-size:11px;
    display:flex;align-items:center;justify-content:center;font-weight:600}
  .step:after{content:&quot;&quot;;position:absolute;left:11px;top:26px;bottom:2px;width:1px;background:var(--bd)}
  .step:last-child:after{display:none}
  .step h5{font-size:12.5px;color:var(--w);font-weight:600;margin-bottom:5px;line-height:1.5}
  .step p{font-size:11.5px;color:var(--mut);line-height:1.7;margin-bottom:8px}
  .step p code{background:#000;color:var(--or2);padding:1px 5px;border-radius:3px;border:1px solid #262626;font-size:11px}

  .code{position:relative;background:#000;border:1px solid var(--bd);border-radius:6px;margin-bottom:8px}
  .code pre{margin:0;padding:11px 44px 11px 12px;overflow-x:auto;font-size:11.5px;line-height:1.75;color:#dcdcdc;white-space:pre}
  .code .cm{color:#6d6d6d}
  .code .fl{color:var(--or2)}
  .cp{position:absolute;top:7px;right:7px;font:inherit;font-size:9.5px;cursor:pointer;background:var(--panel);
    color:var(--mut);border:1px solid var(--bd);border-radius:4px;padding:3px 7px;transition:.15s}
  .cp:hover{color:var(--or2);border-color:var(--or)}
  .cp.ok{color:var(--or);border-color:var(--or)}

  .note{font-size:11px;color:#8a8a8a;line-height:1.7;padding:9px 11px;background:#0e0e0e;border:1px dashed #303030;border-radius:6px;margin-bottom:8px}
  .note b{color:var(--or2);font-weight:600}

  table{width:100%;border-collapse:collapse;border:1px solid var(--bd);border-radius:8px;overflow:hidden;margin-bottom:14px}
  th,td{text-align:left;font-size:11px;padding:9px 11px;border-bottom:1px solid var(--bd);line-height:1.55}
  th{background:#0e0e0e;color:var(--or2);font-weight:600}
  td{background:var(--panel);color:#c9c9c9}
  td:first-child{color:var(--w)}
  tr:last-child td{border-bottom:0}

  .lang{display:flex;gap:6px;margin-bottom:9px}
  .lang button{font:inherit;font-size:11px;cursor:pointer;background:var(--panel);color:var(--mut);border:1px solid var(--bd);
    border-radius:5px;padding:5px 11px;transition:.15s}
  .lang button.on{background:rgba(255,107,26,.13);color:var(--or2);border-color:var(--or)}
  .snip{display:none}.snip.on{display:block}

  .ft{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px;
    border-top:1px solid var(--bd);background:#0e0e0e;padding:10px 14px;font-size:10.5px;color:#6e6e6e}
  .ft .brand{color:var(--or)}
  @media(max-width:640px){.hd h3{font-size:13px}.step{padding-left:30px}.code pre{font-size:10.5px}}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;aenvdep&quot;&gt;

  &lt;div class=&quot;hd&quot;&gt;
    &lt;h3&gt;Deploy &lt;em&gt;AgentENV&lt;/em&gt; &amp;mdash; five supported paths, from one laptop to a Kubernetes cluster&lt;/h3&gt;
    &lt;p&gt;Every command below is taken verbatim from the official kvcache-ai/AgentENV documentation.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;pre&quot;&gt;
    &lt;span class=&quot;pill&quot;&gt;Prereq &amp;middot; &lt;b&gt;Linux kernel 6.8+&lt;/b&gt;&lt;/span&gt;
    &lt;span class=&quot;pill&quot;&gt;Prereq &amp;middot; &lt;b&gt;/dev/kvm&lt;/b&gt; access&lt;/span&gt;
    &lt;span class=&quot;pill&quot;&gt;Install script &amp;middot; &lt;b&gt;Ubuntu 24.04&lt;/b&gt;&lt;/span&gt;
    &lt;span class=&quot;pill&quot;&gt;CLI &amp;middot; &lt;b&gt;Linux + macOS&lt;/b&gt;, x86_64 / arm64&lt;/span&gt;
    &lt;span class=&quot;pill&quot;&gt;License &amp;middot; &lt;b&gt;MIT&lt;/b&gt;&lt;/span&gt;
  &lt;/div&gt;

  &lt;div class=&quot;tabs&quot; id=&quot;tabs&quot;&gt;
    &lt;button class=&quot;on&quot; data-t=&quot;script&quot;&gt;1 &amp;middot; Install script&lt;/button&gt;
    &lt;button data-t=&quot;docker&quot;&gt;2 &amp;middot; Docker&lt;/button&gt;
    &lt;button data-t=&quot;compose&quot;&gt;3 &amp;middot; Docker Compose&lt;/button&gt;
    &lt;button data-t=&quot;k8s&quot;&gt;4 &amp;middot; Kubernetes&lt;/button&gt;
    &lt;button data-t=&quot;src&quot;&gt;5 &amp;middot; Build from source&lt;/button&gt;
    &lt;button data-t=&quot;use&quot;&gt;6 &amp;middot; Use it (E2B SDK)&lt;/button&gt;
  &lt;/div&gt;

  &lt;div class=&quot;body&quot;&gt;

    &lt;!-- SCRIPT --&gt;
    &lt;div class=&quot;path on&quot; id=&quot;t-script&quot;&gt;
      &lt;div class=&quot;meta&quot;&gt;
        &lt;div&gt;Topology&lt;span&gt;Single node&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Runs as&lt;span&gt;systemd service&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Best for&lt;span&gt;First install, dev host&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Time&lt;span&gt;~5 minutes&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;1&quot;&gt;
        &lt;h5&gt;Install the server and the aenv CLI&lt;/h5&gt;
        &lt;p&gt;The script installs both. Set &lt;code&gt;AENV_HOME_PATH&lt;/code&gt; to choose the data directory; without it, AgentENV stores runtime dependencies and data in &lt;code&gt;/var/lib/aenv&lt;/code&gt;.&lt;/p&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;curl -fsSL https://raw.githubusercontent.com/kvcache-ai/AgentENV/main/scripts/install.sh | sudo bash&lt;/pre&gt;&lt;/div&gt;
        &lt;div class=&quot;note&quot;&gt;&lt;b&gt;What it does:&lt;/b&gt; installs missing download and checksum commands, provisions KVM permissions, loads the &lt;code&gt;ublk_drv&lt;/code&gt; kernel module, and downloads runtime assets on first run. Installation needs root, but the service itself runs as a dedicated &lt;code&gt;aenv&lt;/code&gt; account.&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;2&quot;&gt;
        &lt;h5&gt;Start the service&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;sudo systemctl start aenv&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;3&quot;&gt;
        &lt;h5&gt;Authenticate the CLI&lt;/h5&gt;
        &lt;p&gt;Any non-empty string works as the API key for local development.&lt;/p&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;aenv auth
&lt;span class=&quot;cm&quot;&gt;# AENV server URL [http://localhost:8000]: http://127.0.0.1:8000&lt;/span&gt;
&lt;span class=&quot;cm&quot;&gt;# API key: dummy&lt;/span&gt;&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;4&quot;&gt;
        &lt;h5&gt;Pull a template and run your first sandbox&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;aenv pull ubuntu:22.04 &lt;span class=&quot;fl&quot;&gt;--name&lt;/span&gt; ubuntu
aenv start ubuntu     &lt;span class=&quot;cm&quot;&gt;# starts a sandbox and attaches a shell&lt;/span&gt;&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;5&quot;&gt;
        &lt;h5&gt;Tune the config, then restart&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;sudo vim /var/lib/aenv/config/config.toml
sudo systemctl restart aenv&lt;/pre&gt;&lt;/div&gt;
        &lt;p&gt;Change the listening port by editing &lt;code&gt;API_ADDR&lt;/code&gt; in &lt;code&gt;/etc/default/aenv&lt;/code&gt;.&lt;/p&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;!-- DOCKER --&gt;
    &lt;div class=&quot;path&quot; id=&quot;t-docker&quot;&gt;
      &lt;div class=&quot;meta&quot;&gt;
        &lt;div&gt;Topology&lt;span&gt;Single node&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Runs as&lt;span&gt;Privileged container&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Best for&lt;span&gt;Hosts without a Rust toolchain&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Still needs&lt;span&gt;/dev/kvm&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;1&quot;&gt;
        &lt;h5&gt;Prepare the host&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;curl -fsSL https://raw.githubusercontent.com/kvcache-ai/AgentENV/main/scripts/docker-setup.sh | sudo bash&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;2&quot;&gt;
        &lt;h5&gt;Pull and run the server image&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;docker pull ghcr.io/kvcache-ai/aenv-server:latest

docker run --rm -it \
  &lt;span class=&quot;fl&quot;&gt;--device&lt;/span&gt; /dev/kvm &lt;span class=&quot;fl&quot;&gt;--privileged&lt;/span&gt; -v /dev:/dev \
  -p 8000:8000 \
  ghcr.io/kvcache-ai/aenv-server:latest&lt;/pre&gt;&lt;/div&gt;
        &lt;div class=&quot;note&quot;&gt;&lt;b&gt;Why privileged:&lt;/b&gt; Firecracker needs network namespace operations &amp;mdash; veth pairs and iptables. The server auto-downloads the Firecracker binary, kernel and rootfs on first start.&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;3&quot;&gt;
        &lt;h5&gt;Verify&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;curl http://127.0.0.1:8000/health&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;4&quot;&gt;
        &lt;h5&gt;Install the CLI separately&lt;/h5&gt;
        &lt;p&gt;Required with the Docker path, or when the CLI runs on a different machine from the server.&lt;/p&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;curl -fsSL https://raw.githubusercontent.com/kvcache-ai/AgentENV/main/scripts/install-cli.sh | bash&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;5&quot;&gt;
        &lt;h5&gt;Optional &amp;mdash; mount a custom config&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;curl -fsSL https://raw.githubusercontent.com/kvcache-ai/AgentENV/main/config/default.toml -o config.toml
vim config.toml

docker run --rm -it \
  &lt;span class=&quot;fl&quot;&gt;--device&lt;/span&gt; /dev/kvm &lt;span class=&quot;fl&quot;&gt;--privileged&lt;/span&gt; -v /dev:/dev \
  -v &quot;$PWD/config.toml:/workspace/config/default.toml:ro&quot; \
  -p 8000:8000 \
  ghcr.io/kvcache-ai/aenv-server:latest&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;!-- COMPOSE --&gt;
    &lt;div class=&quot;path&quot; id=&quot;t-compose&quot;&gt;
      &lt;div class=&quot;meta&quot;&gt;
        &lt;div&gt;Topology&lt;span&gt;Multi-node simulation&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Control plane&lt;span&gt;Gateway + scheduler&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Best for&lt;span&gt;Testing cluster topology&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Extra dep&lt;span&gt;build-essential&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;table&gt;
        &lt;tr&gt;&lt;th&gt;Service&lt;/th&gt;&lt;th&gt;Port&lt;/th&gt;&lt;th&gt;Role&lt;/th&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;Gateway&lt;/td&gt;&lt;td&gt;:8080&lt;/td&gt;&lt;td&gt;HTTP / WebSocket reverse proxy&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;Scheduler&lt;/td&gt;&lt;td&gt;:9090&lt;/td&gt;&lt;td&gt;gRPC node selection and sandbox binding&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;agentenv-a&lt;/td&gt;&lt;td&gt;:8001&lt;/td&gt;&lt;td&gt;Runtime node A&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;agentenv-b&lt;/td&gt;&lt;td&gt;:8002&lt;/td&gt;&lt;td&gt;Runtime node B&lt;/td&gt;&lt;/tr&gt;
      &lt;/table&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;1&quot;&gt;
        &lt;h5&gt;Clone and prepare the host&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;git clone https://github.com/kvcache-ai/AgentENV.git
cd AgentENV
sudo bash scripts/docker-setup.sh&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;2&quot;&gt;
        &lt;h5&gt;Bring the cluster up&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;make deploy-up&lt;/pre&gt;&lt;/div&gt;
        &lt;p&gt;To enable host-based sandbox data-plane URLs, pass a shared proxy domain that resolves to the gateway:&lt;/p&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;SANDBOX_PROXY_DOMAINS=sandbox.example.com make deploy-up&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;3&quot;&gt;
        &lt;h5&gt;Verify through the gateway&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;curl http://127.0.0.1:8080/health
curl http://127.0.0.1:8080/nodes
curl http://127.0.0.1:8001/health   &lt;span class=&quot;cm&quot;&gt;# direct node check&lt;/span&gt;&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;4&quot;&gt;
        &lt;h5&gt;Operate&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;make deploy-ps      &lt;span class=&quot;cm&quot;&gt;# container status&lt;/span&gt;
make deploy-logs    &lt;span class=&quot;cm&quot;&gt;# stream all logs&lt;/span&gt;
make deploy-down    &lt;span class=&quot;cm&quot;&gt;# tear down&lt;/span&gt;&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;!-- K8S --&gt;
    &lt;div class=&quot;path&quot; id=&quot;t-k8s&quot;&gt;
      &lt;div class=&quot;meta&quot;&gt;
        &lt;div&gt;Topology&lt;span&gt;Multi-node cluster&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Runtime nodes&lt;span&gt;Privileged DaemonSet&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Scheduler&lt;span&gt;Single replica&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Namespace&lt;span&gt;agentenv-system&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;table&gt;
        &lt;tr&gt;&lt;th&gt;Workload&lt;/th&gt;&lt;th&gt;Kind&lt;/th&gt;&lt;th&gt;Purpose&lt;/th&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;agentenv-gateway&lt;/td&gt;&lt;td&gt;Deployment + ClusterIP&lt;/td&gt;&lt;td&gt;HTTP reverse proxy for client traffic&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;agentenv-scheduler&lt;/td&gt;&lt;td&gt;Deployment, 1 replica&lt;/td&gt;&lt;td&gt;gRPC node selection and binding&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;agentenv-node&lt;/td&gt;&lt;td&gt;DaemonSet (privileged)&lt;/td&gt;&lt;td&gt;One runtime Pod per cluster node&lt;/td&gt;&lt;/tr&gt;
        &lt;tr&gt;&lt;td&gt;agentenv-nodes&lt;/td&gt;&lt;td&gt;Headless Service&lt;/td&gt;&lt;td&gt;EndpointSlice discovery for the scheduler&lt;/td&gt;&lt;/tr&gt;
      &lt;/table&gt;

      &lt;div class=&quot;note&quot;&gt;&lt;b&gt;Why a DaemonSet:&lt;/b&gt; each Pod needs host-local &lt;code&gt;/dev/kvm&lt;/code&gt;, sandbox networking uses host iptables and network namespaces, and runtime assets plus committed snapshot state are cached per host at &lt;code&gt;/var/lib/aenv&lt;/code&gt;.&lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;1&quot;&gt;
        &lt;h5&gt;Build the three images&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;git clone https://github.com/kvcache-ai/AgentENV.git
cd AgentENV
make k8s-build   &lt;span class=&quot;cm&quot;&gt;# runtime + gateway + scheduler&lt;/span&gt;&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;2&quot;&gt;
        &lt;h5&gt;Prepare every worker node&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;sudo bash scripts/docker-setup.sh&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;3&quot;&gt;
        &lt;h5&gt;Render, then apply&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;make k8s-render   &lt;span class=&quot;cm&quot;&gt;# preview manifests&lt;/span&gt;
make k8s-apply    &lt;span class=&quot;cm&quot;&gt;# apply to cluster&lt;/span&gt;&lt;/pre&gt;&lt;/div&gt;
        &lt;p&gt;Add a proxy domain if you need host-based sandbox URLs:&lt;/p&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;SANDBOX_PROXY_DOMAINS=sandbox.example.com make k8s-apply&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;4&quot;&gt;
        &lt;h5&gt;Operate&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;make k8s-redeploy   &lt;span class=&quot;cm&quot;&gt;# rollout restart&lt;/span&gt;
make k8s-delete     &lt;span class=&quot;cm&quot;&gt;# delete all resources&lt;/span&gt;&lt;/pre&gt;&lt;/div&gt;
        &lt;div class=&quot;note&quot;&gt;&lt;b&gt;Caveat:&lt;/b&gt; sandbox bindings are held in memory, so the scheduler should run as a single replica and bindings are lost on restart. Set &lt;code&gt;SCHEDULER_REDIS_ADDR&lt;/code&gt; for persistent bindings. The gateway is ClusterIP by default &amp;mdash; add your own Ingress or LoadBalancer.&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;!-- SOURCE --&gt;
    &lt;div class=&quot;path&quot; id=&quot;t-src&quot;&gt;
      &lt;div class=&quot;meta&quot;&gt;
        &lt;div&gt;Topology&lt;span&gt;Single node&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Needs&lt;span&gt;Rust stable toolchain&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Host&lt;span&gt;Ubuntu 24.04&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Best for&lt;span&gt;Development and testing&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;1&quot;&gt;
        &lt;h5&gt;Clone and build&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;git clone https://github.com/kvcache-ai/AgentENV.git
cd AgentENV

make            &lt;span class=&quot;cm&quot;&gt;# debug build&lt;/span&gt;
make release    &lt;span class=&quot;cm&quot;&gt;# release build, recommended for production&lt;/span&gt;&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;2&quot;&gt;
        &lt;h5&gt;Start the server&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;API_ADDR=0.0.0.0:8000 make start-server-release&lt;/pre&gt;&lt;/div&gt;
        &lt;p&gt;Runtime assets are downloaded on first start. Override the config path with &lt;code&gt;AENV_CONFIG_PATH&lt;/code&gt;.&lt;/p&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;3&quot;&gt;
        &lt;h5&gt;Install the CLI from source&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;make install-aenv&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;4&quot;&gt;
        &lt;h5&gt;Point snapshots at shared storage&lt;/h5&gt;
        &lt;p&gt;Use at least 1 Gbps to the storage backend; the docs strongly recommend 10 Gbps or faster.&lt;/p&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;&lt;span class=&quot;cm&quot;&gt;[snapshot]&lt;/span&gt;
repository_backend = &quot;oss&quot;

&lt;span class=&quot;cm&quot;&gt;[backend.oss]&lt;/span&gt;
endpoint = &quot;YOUR_ENDPOINT&quot;
bucket = &quot;YOUR_BUCKET&quot;
region = &quot;YOUR_REGION&quot;
prefix = &quot;YOUR_PREFIX&quot;
cache_max_size_gb = 100
access_key_id = &quot;YOUR_ACCESS_KEY_ID&quot;
access_key_secret = &quot;YOUR_ACCESS_KEY_SECRET&quot;

&lt;span class=&quot;cm&quot;&gt;[image.cache.remote_blocks]&lt;/span&gt;
max_size_gb = 100&lt;/pre&gt;&lt;/div&gt;
        &lt;div class=&quot;note&quot;&gt;&lt;b&gt;Or use a shared filesystem instead:&lt;/b&gt; set &lt;code&gt;repository_backend = &quot;posix_fs&quot;&lt;/code&gt; and point &lt;code&gt;[backend.posix_fs].snapshot_store&lt;/code&gt; at a shared path such as &lt;code&gt;/mnt/aenv-snapshots&lt;/code&gt;.&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

    &lt;!-- USE --&gt;
    &lt;div class=&quot;path&quot; id=&quot;t-use&quot;&gt;
      &lt;div class=&quot;meta&quot;&gt;
        &lt;div&gt;Interface&lt;span&gt;E2B-compatible HTTP API&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;SDKs&lt;span&gt;Python and TypeScript&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Code changes&lt;span&gt;None&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;Native option&lt;span&gt;aenv CLI&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;1&quot;&gt;
        &lt;h5&gt;Point the E2B SDK at your AgentENV server&lt;/h5&gt;
        &lt;p&gt;Use port &lt;code&gt;8000&lt;/code&gt; for single node, or &lt;code&gt;8080&lt;/code&gt; for the gateway in a multi-node deployment.&lt;/p&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;export E2B_API_URL=http://127.0.0.1:8000
export E2B_SANDBOX_URL=${E2B_API_URL}
export E2B_API_KEY=e2b_000000
export E2B_ACCESS_TOKEN=dummy&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;2&quot;&gt;
        &lt;h5&gt;Create, run, pause, kill&lt;/h5&gt;
        &lt;div class=&quot;lang&quot; id=&quot;lang&quot;&gt;
          &lt;button class=&quot;on&quot; data-l=&quot;py&quot;&gt;python&lt;/button&gt;
          &lt;button data-l=&quot;ts&quot;&gt;typescript&lt;/button&gt;
        &lt;/div&gt;

        &lt;div class=&quot;snip on&quot; id=&quot;l-py&quot;&gt;
          &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;from e2b import Sandbox, SandboxQuery, SandboxState

sandbox = Sandbox.create(&quot;&amp;lt;template-id&amp;gt;&quot;)

running = Sandbox.list(
    limit=20,
    query=SandboxQuery(state=[SandboxState.RUNNING]),
)
print(running.next_items())

result = sandbox.commands.run(&quot;echo hello world&quot;)
print(result.stdout, end=&quot;&quot;)

sandbox.beta_pause()
sandbox.kill()&lt;/pre&gt;&lt;/div&gt;
        &lt;/div&gt;

        &lt;div class=&quot;snip&quot; id=&quot;l-ts&quot;&gt;
          &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;import { Sandbox } from &quot;e2b&quot;;

const sandbox = await Sandbox.create(&quot;&amp;lt;template-id&amp;gt;&quot;, {
  apiKey: process.env.E2B_API_KEY,
});

const running = Sandbox.list({
  apiKey: process.env.E2B_API_KEY,
  limit: 20,
  query: { state: [&quot;running&quot;] },
});
console.log(await running.nextItems());

sandbox.commands.run(&quot;echo hello world&quot;);

await Sandbox.Pause(sandbox.sandboxId, {
  apiKey: process.env.E2B_API_KEY,
});
await sandbox.kill();&lt;/pre&gt;&lt;/div&gt;
        &lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;3&quot;&gt;
        &lt;h5&gt;Fork one prepared sandbox into parallel rollouts&lt;/h5&gt;
        &lt;p&gt;Forking clones a &lt;b&gt;running&lt;/b&gt; sandbox into up to 16 children on the same node. The source pauses briefly, then resumes.&lt;/p&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;curl -X POST \
  -H 'X-API-Key: test-key' \
  -H 'Content-Type: application/json' \
  -d '{&quot;count&quot;: 3}' \
  http://127.0.0.1:8000/sandboxes/&amp;lt;sandbox-id&amp;gt;/fork&lt;/pre&gt;&lt;/div&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;4&quot;&gt;
        &lt;h5&gt;Checkpoint state you want to branch from later&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;aenv snapshot create &amp;lt;sandbox-id&amp;gt; &lt;span class=&quot;fl&quot;&gt;--name&lt;/span&gt; my-checkpoint
aenv snapshot list
aenv start my-checkpoint&lt;/pre&gt;&lt;/div&gt;
        &lt;p&gt;Snapshots share the same catalog as templates, so delete them with &lt;code&gt;aenv template delete&lt;/code&gt;.&lt;/p&gt;
      &lt;/div&gt;

      &lt;div class=&quot;step&quot; data-n=&quot;5&quot;&gt;
        &lt;h5&gt;Lock down egress per sandbox&lt;/h5&gt;
        &lt;div class=&quot;code&quot;&gt;&lt;button class=&quot;cp&quot;&gt;copy&lt;/button&gt;&lt;pre&gt;curl -X POST \
  -H 'X-API-Key: test-key' \
  -H 'Content-Type: application/json' \
  -d '{
    &quot;templateID&quot;: &quot;my-ubuntu&quot;,
    &quot;network&quot;: {
      &quot;allowOut&quot;: [&quot;8.8.8.8/32&quot;, &quot;example.com&quot;, &quot;*.example.com&quot;],
      &quot;denyOut&quot;: [&quot;0.0.0.0/0&quot;]
    }
  }' \
  http://127.0.0.1:8000/sandboxes&lt;/pre&gt;&lt;/div&gt;
        &lt;div class=&quot;note&quot;&gt;&lt;b&gt;Allowed entries take precedence over denied entries.&lt;/b&gt; Node-level &lt;code&gt;always_denied_cidrs&lt;/code&gt; rules are installed first, so a sandbox request cannot override them.&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;

  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;Commands verified against kvcache-ai.github.io/AgentENV &amp;middot; 27 Jul 2026&lt;/span&gt;
    &lt;span class=&quot;brand&quot;&gt;Built by Marktechpost&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var tabs=document.getElementById(&quot;tabs&quot;);
  tabs.addEventListener(&quot;click&quot;,function(e){
    var b=e.target.closest(&quot;button&quot;);if(!b||!b.dataset.t)return;
    tabs.querySelectorAll(&quot;button&quot;).forEach(function(x){x.classList.remove(&quot;on&quot;);});
    b.classList.add(&quot;on&quot;);
    document.querySelectorAll(&quot;.path&quot;).forEach(function(p){p.classList.remove(&quot;on&quot;);});
    var el=document.getElementById(&quot;t-&quot;+b.dataset.t);
    if(el)el.classList.add(&quot;on&quot;);
    ping();
  });

  var lang=document.getElementById(&quot;lang&quot;);
  if(lang){
    lang.addEventListener(&quot;click&quot;,function(e){
      var b=e.target.closest(&quot;button&quot;);if(!b||!b.dataset.l)return;
      lang.querySelectorAll(&quot;button&quot;).forEach(function(x){x.classList.remove(&quot;on&quot;);});
      b.classList.add(&quot;on&quot;);
      document.querySelectorAll(&quot;.snip&quot;).forEach(function(s){s.classList.remove(&quot;on&quot;);});
      var el=document.getElementById(&quot;l-&quot;+b.dataset.l);
      if(el)el.classList.add(&quot;on&quot;);
      ping();
    });
  }

  document.addEventListener(&quot;click&quot;,function(e){
    var b=e.target.closest(&quot;.cp&quot;);if(!b)return;
    var pre=b.parentNode.querySelector(&quot;pre&quot;);if(!pre)return;
    var txt=pre.innerText;
    var done=function(){b.textContent=&quot;copied&quot;;b.classList.add(&quot;ok&quot;);
      setTimeout(function(){b.textContent=&quot;copy&quot;;b.classList.remove(&quot;ok&quot;);},1400);};
    if(navigator.clipboard&amp;&amp;navigator.clipboard.writeText){
      navigator.clipboard.writeText(txt).then(done,function(){fallback(txt,done);});
    }else{fallback(txt,done);}
  });

  function fallback(txt,cb){
    var ta=document.createElement(&quot;textarea&quot;);
    ta.value=txt;ta.style.position=&quot;fixed&quot;;ta.style.opacity=&quot;0&quot;;
    document.body.appendChild(ta);ta.select();
    try{document.execCommand(&quot;copy&quot;);cb();}catch(err){}
    document.body.removeChild(ta);
  }

  function ping(){
    var el=document.getElementById(&quot;aenvdep&quot;);
    if(!el)return;
    parent.postMessage({aenvDepHeight:el.offsetHeight+40},&quot;*&quot;);
  }
  window.addEventListener(&quot;load&quot;,ping);
  window.addEventListener(&quot;resize&quot;,ping);
  setTimeout(ping,60);setTimeout(ping,400);setTimeout(ping,1200);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
" style="width:100%;height:900px;border:0;overflow:hidden;display:block;" scrolling="no" loading="lazy" title="AgentENV deployment guide"></iframe>
<script>
(function(){
  var f=document.getElementById("mtp-aenv-deploy");
  window.addEventListener("message",function(e){
    if(!e.data||typeof e.data!=="object")return;
    var h=e.data.aenvDepHeight;
    if(typeof h==="number"&&h>120&&h<20000){f.style.height=h+"px";}
  });
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>Deployment covers install script, Docker, Docker Compose, and Kubernetes; the multi-node control plane is documented as a prototype.</li>



<li>AgentENV runs each agent environment as a Firecracker microVM, not a container, so isolation is kernel-level.</li>



<li>The project reports boot or resume under 50 ms, and pause under 100 ms, from snapshots.</li>



<li>A running sandbox can fork into up to 16 independent children on the same node.</li>



<li>The HTTP API is E2B-compatible, so existing E2B Python and TypeScript SDK code runs unchanged.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"><em>Check out the <a href="https://github.com/kvcache-ai/AgentENV">GitHub repository</a> and <a href="https://kvcache-ai.github.io/AgentENV/">documentation</a>. All credit for this research goes to the researchers of this project.</em></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/27/kimi-ai-and-kvcache-ai-open-sources-agentenv/">Kimi AI and kvcache-ai Open Sources &#8216;AgentENV&#8217;: A Distributed System that Powers Agentic Reinforcement Learning (RL) Training for Kimi K3</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81394</post-id>
	</item>
		<item>
		<title>Designing Skill-Driven Financial Analysis Agents with Claude, Python, MCP Connectors, and Automated Deliverables</title>
		<link>https://www.marktechpost.com/2026/07/27/designing-skill-driven-financial-analysis-agents-with-claude-python-mcp-connectors-and-automated-deliverables/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Mon, 27 Jul 2026 18:08:24 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Agents]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81391</guid>

					<description><![CDATA[<p>In this tutorial, we build an advanced workflow around Anthropic’s financial-services repository and reproduce its skill-driven architecture in pure Python. We begin by installing the required libraries, cloning the repository, and programmatically mapping its agents, vertical plugins, partner integrations, managed-agent cookbooks, and financial analysis skills. We then parse the repository’s SKILL.md files into a searchable [&#8230;]</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/27/designing-skill-driven-financial-analysis-agents-with-claude-python-mcp-connectors-and-automated-deliverables/">Designing Skill-Driven Financial Analysis Agents with Claude, Python, MCP Connectors, and Automated Deliverables</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this tutorial, we build an advanced workflow around<a href="https://github.com/anthropics/financial-services"> <strong>Anthropic’s financial-services</strong></a><strong> </strong>repository and reproduce its skill-driven architecture in pure Python. We begin by installing the required libraries, cloning the repository, and programmatically mapping its agents, vertical plugins, partner integrations, managed-agent cookbooks, and financial analysis skills. We then parse the repository’s SKILL.md files into a searchable registry and construct a reusable SkillAgent that injects selected financial playbooks into the Anthropic Messages API while supporting an iterative tool-use loop for Python calculations and file generation. Using this architecture, we execute a synthetic discounted cash flow valuation, generate a WACC and terminal-growth sensitivity heatmap, perform comparable-company analysis with formatted Excel output, draft a private-equity investment committee memo, and inspect a managed-agent deployment specification without sending a live deployment request.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import subprocess, sys, os, io, re, json, glob, textwrap, contextlib, pathlib
def sh(cmd):
   print(f"$ {cmd}")
   r = subprocess.run(cmd, shell=True, capture_output=True, text=True)
   if r.returncode != 0:
       print(r.stderr[-1500:])
   return r
sh(f"{sys.executable} -m pip install -q anthropic pandas openpyxl pyyaml matplotlib")
import pandas as pd
import yaml
import matplotlib.pyplot as plt
REPO_URL = "https://github.com/anthropics/financial-services.git"
REPO_DIR = "financial-services"
if not os.path.isdir(REPO_DIR):
   sh(f"git clone --depth 1 {REPO_URL} {REPO_DIR}")
else:
   print("Repo already cloned — skipping.")
def get_api_key():
   try:
       from google.colab import userdata
       k = userdata.get("ANTHROPIC_API_KEY")
       if k:
           return k
   except Exception:
       pass
   if os.environ.get("ANTHROPIC_API_KEY"):
       return os.environ["ANTHROPIC_API_KEY"]
   from getpass import getpass
   return getpass("Enter your Anthropic API key: ")
os.environ["ANTHROPIC_API_KEY"] = get_api_key()
import anthropic
client = anthropic.Anthropic()
MODEL = "claude-sonnet-4-6"
print("SDK ready. Model:", MODEL)
</code></pre></div></div>



<p class="wp-block-paragraph">We install the required Python libraries, clone Anthropic’s financial-services repository, and prepare the Google Colab runtime for execution. We retrieve the Anthropic API key from Colab secrets, environment variables, or a secure interactive prompt. We then initialize the official Anthropic SDK and select the Claude model that powers the financial-analysis workflows.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">def repo_map(root=REPO_DIR):
   rows = []
   for kind, pattern in [
       ("agent",   f"{root}/plugins/agent-plugins/*"),
       ("vertical",f"{root}/plugins/vertical-plugins/*"),
       ("partner", f"{root}/plugins/partner-built/*"),
       ("cookbook",f"{root}/managed-agent-cookbooks/*"),
   ]:
       for p in sorted(glob.glob(pattern)):
           if not os.path.isdir(p):
               continue
           skills   = glob.glob(f"{p}/**/SKILL.md", recursive=True)
           commands = glob.glob(f"{p}/commands/*.md")
           rows.append({"type": kind, "name": os.path.basename(p),
                        "skills": len(skills), "commands": len(commands)})
   return pd.DataFrame(rows)
print("\n=== REPO MAP ===")
repo_df = repo_map()
print(repo_df.to_string(index=False))
mcp_files = glob.glob(f"{REPO_DIR}/plugins/**/.mcp.json", recursive=True)
for f in mcp_files[:1]:
   print(f"\n=== MCP CONNECTORS ({f}) ===")
   try:
       cfg = json.load(open(f))
       for name, srv in cfg.get("mcpServers", cfg).items():
           print(f"  {name:&lt;14} -> {srv.get('url', srv)}")
   except Exception as e:
       print("  (could not parse:", e, ")")
FRONTMATTER = re.compile(r"^---\s*\n(.*?)\n---\s*\n", re.S)
class Skill:
   def __init__(self, path):
       self.path = path
       raw = open(path, encoding="utf-8", errors="replace").read()
       m = FRONTMATTER.match(raw)
       meta = {}
       if m:
           try:
               meta = yaml.safe_load(m.group(1)) or {}
           except Exception:
               meta = {}
       self.name = str(meta.get("name") or pathlib.Path(path).parent.name)
       self.description = str(meta.get("description", ""))[:300]
       self.body = raw[m.end():] if m else raw
   def __repr__(self):
       return f"&lt;Skill {self.name}>"
class SkillRegistry:
   def __init__(self, root=REPO_DIR):
       paths  = sorted(glob.glob(f"{root}/plugins/vertical-plugins/**/SKILL.md", recursive=True))
       paths += sorted(glob.glob(f"{root}/plugins/**/SKILL.md", recursive=True))
       self.skills = {}
       for p in paths:
           s = Skill(p)
           self.skills.setdefault(s.name.lower(), s)
   def find(self, query):
       q = query.lower()
       hits = [s for k, s in self.skills.items() if q in k]
       if not hits:
           hits = [s for s in self.skills.values() if q in s.description.lower()]
       return hits
   def get(self, query):
       hits = self.find(query)
       if not hits:
           raise KeyError(f"No skill matching '{query}'. "
                          f"Available: {sorted(self.skills)[:40]}")
       return hits[0]
registry = SkillRegistry()
print(f"\nLoaded {len(registry.skills)} unique skills.")
print("Sample:", sorted(registry.skills)[:12], "...")
</code></pre></div></div>



<p class="wp-block-paragraph">We inspect the repository structure and identify its agent plugins, vertical plugins, partner integrations, managed-agent cookbooks, and available commands. We locate MCP configuration files and display the external financial data connectors defined in the repository. We then parse each SKILL.md file, extract its YAML metadata and methodology, and register every unique skill for searchable access.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">os.makedirs("outputs", exist_ok=True)
TOOLS = [
   {
       "name": "run_python",
       "description": ("Execute Python code and return stdout. pandas as pd "
                       "and numpy as np are pre-imported. Use print() to "
                       "return results. State persists across calls."),
       "input_schema": {
           "type": "object",
           "properties": {"code": {"type": "string"}},
           "required": ["code"],
       },
   },
   {
       "name": "save_file",
       "description": "Save text content to outputs/&lt;filename>.",
       "input_schema": {
           "type": "object",
           "properties": {"filename": {"type": "string"},
                          "content":  {"type": "string"}},
           "required": ["filename", "content"],
       },
   },
]
_PY_NS = {}
def _tool_run_python(code):
   import numpy as np
   _PY_NS.setdefault("pd", pd); _PY_NS.setdefault("np", np)
   buf = io.StringIO()
   try:
       with contextlib.redirect_stdout(buf):
           exec(code, _PY_NS)
       out = buf.getvalue()
       return out[:6000] if out else "(no stdout — use print())"
   except Exception as e:
       return f"ERROR: {type(e).__name__}: {e}"
def _tool_save_file(filename, content):
   safe = os.path.basename(filename)
   path = os.path.join("outputs", safe)
   open(path, "w", encoding="utf-8").write(content)
   return f"Saved {path} ({len(content)} chars)"
DISPATCH = {"run_python": lambda i: _tool_run_python(i["code"]),
           "save_file":  lambda i: _tool_save_file(i["filename"], i["content"])}
BASE_SYSTEM = """You are a financial analyst assistant operating with the
skill playbooks provided below (from Anthropic's financial-services repo).
Follow the skill's methodology, conventions, and output format closely.
Use the run_python tool for all numerical work — never do arithmetic in
your head. Use save_file for final deliverables. All work is a DRAFT for
human review; do not present it as investment advice."""
class SkillAgent:
   """Minimal reproduction of Cowork's skill-firing: chosen skills are
   concatenated into the system prompt; the agent then runs a standard
   tool-use loop against the Messages API until the model stops."""
   def __init__(self, skill_queries, max_skill_chars=12000, verbose=True):
       self.skills = [registry.get(q) for q in skill_queries]
       blocks = []
       for s in self.skills:
           blocks.append(f"\n\n===== SKILL: {s.name} =====\n"
                         f"{s.description}\n{s.body[:max_skill_chars]}")
       self.system = BASE_SYSTEM + "".join(blocks)
       self.verbose = verbose
   def run(self, prompt, max_turns=12):
       messages = [{"role": "user", "content": prompt}]
       for turn in range(max_turns):
           resp = client.messages.create(
               model=MODEL, max_tokens=8000,
               system=self.system, tools=TOOLS, messages=messages)
           messages.append({"role": "assistant", "content": resp.content})
           if resp.stop_reason != "tool_use":
               final = "".join(b.text for b in resp.content
                               if b.type == "text")
               return final, messages
           results = []
           for block in resp.content:
               if block.type == "tool_use":
                   if self.verbose:
                       print(f"  [turn {turn}] tool: {block.name}")
                   out = DISPATCH[block.name](block.input)
                   results.append({"type": "tool_result",
                                   "tool_use_id": block.id,
                                   "content": str(out)})
           messages.append({"role": "user", "content": results})
       return "(hit max_turns)", messages
</code></pre></div></div>



<p class="wp-block-paragraph">We define tools that allow Claude to execute Python calculations and save generated deliverables inside the Colab environment. We build a persistent Python namespace so numerical models, tables, and intermediate variables remain available across multiple agent turns. We then create the SkillAgent class, inject selected financial playbooks into its system prompt, and manage the Anthropic Messages API tool-use loop.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">SAMPLE_CO = """
Target: 'Meridian Software' (synthetic). FY2025 actuals, $mm:
Revenue 850 (grew 18% y/y) | EBITDA margin 27% | D&amp;A 4% of rev
CapEx 5% of rev | NWC change 1% of rev growth | Tax rate 24%
Net debt 320 | Diluted shares 92mm
Assumptions: revenue growth fades 18% -> 6% linearly over 5 yrs,
EBITDA margin expands 100bps total, WACC 9.5%, terminal growth 2.5%.
"""
print("\n" + "="*76 + "\nDEMO A — DCF (dcf-model skill)\n" + "="*76)
try:
   dcf_agent = SkillAgent(["dcf"])
   dcf_answer, _ = dcf_agent.run(
       "Run a 5-year unlevered DCF per the skill playbook on this company:\n"
       + SAMPLE_CO +
       "\nCompute enterprise value, equity value, and implied share price "
       "with run_python. Then print a WACC (8.5%-10.5%, 50bp steps) x "
       "terminal growth (1.5%-3.5%, 50bp steps) sensitivity grid of implied "
       "share price as a JSON object under the marker SENS_JSON:, and give "
       "a concise summary.")
   print("\n--- DCF RESULT ---\n", dcf_answer[:3000])
   m = re.search(r"SENS_JSON:\s*(\{.*\})", dcf_answer, re.S)
   if m:
       grid = json.loads(m.group(1))
       sens = pd.DataFrame(grid)
       sens = sens.apply(pd.to_numeric, errors="coerce")
       fig, ax = plt.subplots(figsize=(7, 4))
       im = ax.imshow(sens.values, cmap="RdYlGn", aspect="auto")
       ax.set_xticks(range(len(sens.columns)), sens.columns)
       ax.set_yticks(range(len(sens.index)), sens.index)
       ax.set_xlabel("Terminal growth"); ax.set_ylabel("WACC")
       ax.set_title("Implied share price sensitivity ($)")
       for i in range(sens.shape[0]):
           for j in range(sens.shape[1]):
               v = sens.values[i, j]
               if pd.notna(v):
                   ax.text(j, i, f"{v:,.0f}", ha="center", va="center",
                           fontsize=8)
       fig.colorbar(im); plt.tight_layout(); plt.show()
except Exception as e:
   print("Demo A skipped:", e)
</code></pre></div></div>



<p class="wp-block-paragraph">We provide synthetic operating assumptions and instruct the DCF skill agent to construct a five-year unlevered cash-flow valuation. We use the Python execution tool to calculate enterprise value, equity value, implied share price, and a two-dimensional sensitivity matrix. We then extract the structured sensitivity results and visualize the relationship between WACC, terminal growth, and implied valuation through a heatmap.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">PEERS = """
Synthetic peer set ($mm except per-share):
Ticker  Price  Shares  NetDebt  Rev_NTM  EBITDA_NTM  EPS_NTM
ALFA    64.2   210     450      2900     820         3.10
BRVO    28.7   540     -120     4100     980         1.45
CHRL    112.5  95      760      1850     610         5.60
DLTA    41.9   330     210      2600     700         2.05
"""
print("\n" + "="*76 + "\nDEMO B — COMPS (comps-analysis skill) -> Excel\n" + "="*76)
try:
   comps_agent = SkillAgent(["comps"])
   comps_answer, _ = comps_agent.run(
       "Per the comps skill, compute EV, EV/Revenue, EV/EBITDA and P/E "
       "(all NTM) for these peers with run_python:\n" + PEERS +
       "\nThen print the full comps table plus min/25th/median/75th/max "
       "summary stats as JSON under the marker COMPS_JSON: with keys "
       "'table' (list of row dicts) and 'stats' (dict of dicts).")
   print("\n--- COMPS NARRATIVE ---\n", comps_answer[:1500])
   m = re.search(r"COMPS_JSON:\s*(\{.*\})", comps_answer, re.S)
   if m:
       payload = json.loads(m.group(1))
       table = pd.DataFrame(payload["table"])
       stats = pd.DataFrame(payload["stats"])
       xlsx = "outputs/comps_analysis.xlsx"
       with pd.ExcelWriter(xlsx, engine="openpyxl") as xl:
           table.to_excel(xl, sheet_name="Comps", index=False)
           stats.to_excel(xl, sheet_name="Summary Stats")
       from openpyxl import load_workbook
       from openpyxl.styles import Font, PatternFill
       wb = load_workbook(xlsx)
       for ws in wb.worksheets:
           for cell in ws[1]:
               cell.font = Font(bold=True, color="FFFFFF")
               cell.fill = PatternFill("solid", start_color="1F4E79")
           for col in ws.columns:
               w = max(len(str(c.value)) for c in col if c.value is not None)
               ws.column_dimensions[col[0].column_letter].width = w + 3
       wb.save(xlsx)
       print("Wrote", xlsx)
       print(table.to_string(index=False))
except Exception as e:
   print("Demo B skipped:", e)
</code></pre></div></div>



<p class="wp-block-paragraph">We supply a synthetic peer set and calculate enterprise value, EV-to-revenue, EV-to-EBITDA, and price-to-earnings multiples. We convert the agent’s structured JSON response into detailed comparable-company and summary-statistics DataFrames. We then export the analysis to a multi-sheet Excel workbook and apply professional header formatting and automatic column sizing.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n" + "="*76 + "\nDEMO C — IC MEMO (ic-memo skill)\n" + "="*76)
try:
   ic_agent = SkillAgent(["ic-memo"])
   ic_answer, _ = ic_agent.run(
       "Draft a first-round IC memo per the skill for a hypothetical "
       "buyout of Meridian Software (see facts below). Base the valuation "
       "framing on ~11x EV/EBITDA entry, 45% leverage, 5-yr hold. Use "
       "run_python for any quick math (e.g., rough MOIC/IRR math) and "
       "save the memo with save_file as ic_memo_meridian.md.\n" + SAMPLE_CO)
   print("\n--- IC MEMO (first 1500 chars of reply) ---\n", ic_answer[:1500])
   memo_path = "outputs/ic_memo_meridian.md"
   if os.path.exists(memo_path):
       print(f"\nMemo saved -> {memo_path} "
             f"({os.path.getsize(memo_path)} bytes)")
except Exception as e:
   print("Demo C skipped:", e)
print("\n" + "="*76 + "\nPART 7 — MANAGED AGENT COOKBOOK (dry run)\n" + "="*76)
yamls = sorted(glob.glob(f"{REPO_DIR}/managed-agent-cookbooks/*/agent.yaml")) \
     + sorted(glob.glob(f"{REPO_DIR}/managed-agent-cookbooks/*/*.yaml"))
if yamls:
   path = yamls[0]
   print("Inspecting:", path)
   try:
       spec = yaml.safe_load(open(path))
       print(json.dumps(spec, indent=2, default=str)[:2500])
       print("\nDeploy flow: resolve file refs -> upload skills -> create "
             "leaf-worker subagents -> POST orchestrator to /v1/agents "
             "(see scripts/orchestrate.py for the handoff_request event loop).")
   except Exception as e:
       print("Could not parse yaml:", e)
else:
   print("No cookbook yaml found on this branch — see "
         "managed-agent-cookbooks/ READMEs on GitHub.")
print("\n" + "="*76)
print("DONE. Artifacts in ./outputs/:", os.listdir("outputs"))
print("""
Where to go next:
* Swap SAMPLE_CO / PEERS for real data via the repo's MCP connectors
  (Daloopa, FactSet, S&amp;P, Morningstar, PitchBook...) — subscriptions apply.
* Load other skills: SkillAgent(["lbo"]), ["merger"], ["earnings"],
  ["rebalance"], ["tlh"], ["kyc"] ... — see `sorted(registry.skills)`.
* Stack skills: SkillAgent(["comps", "dcf"]) for a football-field workflow.
* For production, install as a Cowork plugin or deploy via Managed Agents
  instead of this Colab loop — same skills, governed runtime.
All outputs are drafts for qualified human review — not investment advice.
""")
</code></pre></div></div>



<p class="wp-block-paragraph">We apply the investment-committee memo skill to a hypothetical software buyout and calculate supporting return metrics with Python. We save the resulting memo as a Markdown deliverable and verify that the generated file exists in the output directory. We then inspect a managed-agent cookbook, display the deployment specification, and conclude by reviewing the generated artifacts and possible production extensions.</p>



<p class="wp-block-paragraph">By completing this tutorial, we implement a practical Colab-based approximation of Anthropic’s financial-services skill and agent framework while preserving the repository’s methodology-driven approach to financial analysis. We combine structured skill discovery, dynamic system-prompt construction, persistent Python execution, API-based tool orchestration, and automated deliverable generation in a single reusable workflow. We also demonstrate how the same agent architecture supports multiple finance use cases, including DCF valuation, trading-comps analysis, sensitivity testing, Excel reporting, and investment committee memo preparation. From here, we extend the system by loading additional skills, combining multiple valuation playbooks, connecting to licensed financial data providers via MCP integrations, and replacing the tutorial sandbox with a governed production runtime in Claude Code, Cowork, or Managed Agents.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Agentic%20AI%20Codes/claude_financial_services_agentic_workflows_marktechpost.py" target="_blank" rel="noreferrer noopener">Full Code here</a>. </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://www.aidevsignals.com/" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/27/designing-skill-driven-financial-analysis-agents-with-claude-python-mcp-connectors-and-automated-deliverables/">Designing Skill-Driven Financial Analysis Agents with Claude, Python, MCP Connectors, and Automated Deliverables</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81391</post-id>
	</item>
		<item>
		<title>Perplexity Releases pplx, a Single-Binary CLI That Puts Its Search API in the Terminal for Coding Agents</title>
		<link>https://www.marktechpost.com/2026/07/27/perplexity-releases-pplx/</link>
		
		<dc:creator><![CDATA[Michal Sutter]]></dc:creator>
		<pubDate>Mon, 27 Jul 2026 16:32:47 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Agents]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81383</guid>

					<description><![CDATA[<p>Perplexity has released pplx, an official command line client for its Search API. The tool exposes two commands — pplx search web and pplx content fetch — and returns exactly one JSON object on stdout. It ships as a checksum-verified single binary for macOS arm64 and Linux, alongside an Agent Skill for Claude Code, Codex CLI and any harness that can read a URL.</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/27/perplexity-releases-pplx/">Perplexity Releases pplx, a Single-Binary CLI That Puts Its Search API in the Terminal for Coding Agents</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph"><a href="https://www.perplexity.ai/">Perplexity</a> has released <code>pplx</code>, an official command line client for its Search API. The tool returns grounded search results and extracted page text, all as JSON. According to its docs, it targets humans and coding agents equally. It is not a chat client. There is no conversational mode, no model selection and no synthesized answer.</p>



<h2 id="h-two-surfaces-one-output-contract" class="wp-block-heading"><strong>Two surfaces, one output contract</strong></h2>



<p class="wp-block-paragraph">The tool exposes exactly two working surfaces. <code>pplx search web</code> runs a live web search. <code>pplx content fetch</code> pulls a URL and returns cleaned page text.</p>



<p class="wp-block-paragraph">The contract around them is the interesting part. Per the official <a href="https://github.com/perplexityai/api-platform-developers/blob/main/skills/pplx-cli/SKILL.md"><code>pplx-cli</code> Agent Skill</a>, success means exit code 0 and exactly one JSON object on stdout. Search returns <code>{hits: [{url, title, domain, snippet, ...}], total, saved_to?}</code>.</p>



<p class="wp-block-paragraph">Every failure exits 1 with an empty stdout. One JSON error object goes to stderr, shaped <code>{"error":{"code","message","command","hint"?}}</code>. Documented codes include <code>AUTHENTICATION</code>, <code>UNKNOWN_ARGUMENT</code>, <code>ARGUMENT_ERROR</code> and <code>BAD_REQUEST</code>. The skill notes that list is not exhaustive, so callers should branch on <code>error.code</code>.</p>



<div id="mtp-pplx-explainer" style="margin:0 0 24px;">
<iframe id="mtpPplxExplainerFrame" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;style&gt;
  *{box-sizing:border-box;margin:0;padding:0}
  html,body{background:transparent}
  body{font-family:ui-monospace,SFMono-Regular,&quot;SF Mono&quot;,Menlo,Consolas,&quot;Liberation Mono&quot;,monospace;-webkit-font-smoothing:antialiased}
  #pplxRoot{background:#0a0a0a;color:#f2f2f2;border:1px solid #2a2a2a;border-radius:10px;overflow:hidden;max-width:960px;margin:0 auto}
  .bar{display:flex;align-items:center;gap:8px;padding:10px 14px;background:#141414;border-bottom:1px solid #262626}
  .dot{width:11px;height:11px;border-radius:50%}
  .d1{background:#ff6b1a}.d2{background:#7a3d12}.d3{background:#3a2412}
  .bartitle{margin-left:8px;font-size:12px;color:#8a8a8a;letter-spacing:.04em}
  .barver{margin-left:auto;font-size:11px;color:#ff8a3d;letter-spacing:.03em}
  .lede{padding:16px 18px 4px;font-size:13px;line-height:1.6;color:#bdbdbd}
  .lede b{color:#ff8a3d;font-weight:600}
  .wrap{display:flex;flex-wrap:wrap;gap:0}
  .side{flex:0 0 232px;min-width:200px;border-right:1px solid #222;padding:14px 12px}
  .sidelabel{font-size:10px;letter-spacing:.16em;color:#6a6a6a;padding:0 8px 9px}
  .cmd{display:block;width:100%;text-align:left;background:transparent;border:0;border-left:2px solid transparent;color:#c9c9c9;font:inherit;font-size:12.5px;padding:9px 10px;cursor:pointer;border-radius:0 5px 5px 0;transition:background .13s,color .13s}
  .cmd:hover{background:#161616;color:#fff}
  .cmd.on{background:#1b1108;border-left-color:#ff6b1a;color:#ff8a3d}
  .cmd span{color:#5f5f5f;margin-right:8px;font-size:11px}
  .cmd.on span{color:#c0651f}
  .main{flex:1 1 380px;min-width:270px;padding:14px 16px 16px;overflow:hidden}
  .screen{background:#050505;border:1px solid #1f1f1f;border-radius:7px;padding:13px 14px;overflow-x:auto}
  pre{font:inherit;font-size:12.5px;line-height:1.72;white-space:pre;color:#e8e8e8}
  .p{color:#ff6b1a}
  .k{color:#ff8a3d}
  .s{color:#ffffff}
  .c{color:#6b6b6b}
  .g{color:#9ad06a}
  .r{color:#ff5f56}
  .note{margin-top:12px;font-size:12px;line-height:1.65;color:#a8a8a8;border-left:2px solid #ff6b1a;padding-left:11px}
  .note b{color:#fff;font-weight:600}
  .meter{border-top:1px solid #222;padding:16px 18px}
  .mrow{display:flex;flex-wrap:wrap;align-items:center;gap:12px}
  .mlab{font-size:11px;letter-spacing:.14em;color:#6a6a6a;flex:0 0 100%}
  input[type=range]{-webkit-appearance:none;appearance:none;flex:1 1 200px;min-width:150px;height:3px;background:#2e2e2e;border-radius:3px;outline:none}
  input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;width:15px;height:15px;border-radius:50%;background:#ff6b1a;cursor:pointer;border:2px solid #0a0a0a}
  input[type=range]::-moz-range-thumb{width:15px;height:15px;border-radius:50%;background:#ff6b1a;cursor:pointer;border:2px solid #0a0a0a}
  input[type=range]:focus-visible{box-shadow:0 0 0 2px #ff6b1a}
  .out{font-size:12.5px;color:#bdbdbd;white-space:nowrap}
  .out b{color:#ff8a3d;font-size:15px;font-weight:600}
  .facts{display:flex;flex-wrap:wrap;gap:1px;background:#222;border-top:1px solid #222}
  .fact{flex:1 1 150px;background:#0d0d0d;padding:13px 14px}
  .fv{font-size:16px;color:#ff8a3d;font-weight:600;line-height:1.25}
  .fl{font-size:10.5px;color:#7a7a7a;margin-top:4px;line-height:1.45;letter-spacing:.03em}
  .foot{padding:11px 18px;background:#0d0d0d;border-top:1px solid #222;font-size:10.5px;color:#666;display:flex;flex-wrap:wrap;gap:8px;justify-content:space-between}
  .foot a{color:#ff6b1a;text-decoration:none}
  .foot a:hover{text-decoration:underline}
  .cmd:focus-visible,.foot a:focus-visible{outline:2px solid #ff6b1a;outline-offset:2px}
  @media (max-width:640px){
    .side{flex:1 1 100%;border-right:0;border-bottom:1px solid #222;display:flex;flex-wrap:wrap;gap:6px;padding:12px}
    .sidelabel{flex:1 1 100%;padding:0 0 4px}
    .cmd{width:auto;flex:0 0 auto;border-left:0;border-bottom:2px solid transparent;border-radius:5px;padding:7px 10px;font-size:12px}
    .cmd.on{border-left:0;border-bottom-color:#ff6b1a}
    .cmd span{display:none}
    pre{font-size:11px}
    .lede{font-size:12.5px;padding:14px 14px 2px}
    .main{padding:12px 12px 14px}
  }
  @media (prefers-reduced-motion:reduce){*{transition:none!important}}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;pplxRoot&quot;&gt;
  &lt;div class=&quot;bar&quot;&gt;
    &lt;span class=&quot;dot d1&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot d2&quot;&gt;&lt;/span&gt;&lt;span class=&quot;dot d3&quot;&gt;&lt;/span&gt;
    &lt;span class=&quot;bartitle&quot;&gt;pplx — the Perplexity Search API in your terminal&lt;/span&gt;
    &lt;span class=&quot;barver&quot;&gt;v0.2.2&lt;/span&gt;
  &lt;/div&gt;

  &lt;div class=&quot;lede&quot;&gt;
    Every &lt;b&gt;pplx&lt;/b&gt; command prints &lt;b&gt;exactly one JSON object to stdout&lt;/b&gt; and exits 0. Failures exit 1 with an empty stdout and one JSON error object on stderr. Pick a command to see the contract.
  &lt;/div&gt;

  &lt;div class=&quot;wrap&quot;&gt;
    &lt;div class=&quot;side&quot;&gt;
      &lt;div class=&quot;sidelabel&quot;&gt;COMMANDS&lt;/div&gt;
      &lt;div id=&quot;pplxNav&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;main&quot;&gt;
      &lt;div class=&quot;screen&quot;&gt;&lt;pre id=&quot;pplxOut&quot;&gt;&lt;/pre&gt;&lt;/div&gt;
      &lt;div class=&quot;note&quot; id=&quot;pplxNote&quot;&gt;&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;meter&quot;&gt;
    &lt;div class=&quot;mrow&quot;&gt;
      &lt;div class=&quot;mlab&quot;&gt;SEARCH API COST — $5.00 PER 1,000 REQUESTS, NO TOKEN CHARGES&lt;/div&gt;
      &lt;input type=&quot;range&quot; id=&quot;pplxSlide&quot; min=&quot;0&quot; max=&quot;24&quot; value=&quot;8&quot; step=&quot;1&quot; aria-label=&quot;Searches per day&quot;&gt;
      &lt;div class=&quot;out&quot;&gt;&lt;span id=&quot;pplxQ&quot;&gt;1,000&lt;/span&gt; searches/day &amp;nbsp;&amp;rarr;&amp;nbsp; &lt;b id=&quot;pplxC&quot;&gt;$150&lt;/b&gt; /month&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;facts&quot;&gt;
    &lt;div class=&quot;fact&quot;&gt;&lt;div class=&quot;fv&quot;&gt;50 req/s&lt;/div&gt;&lt;div class=&quot;fl&quot;&gt;POST /search rate limit, burst 50, same on every usage tier&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;fact&quot;&gt;&lt;div class=&quot;fv&quot;&gt;1&amp;ndash;20&lt;/div&gt;&lt;div class=&quot;fl&quot;&gt;results per search; the CLI default is 10 via -n&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;fact&quot;&gt;&lt;div class=&quot;fv&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;fl&quot;&gt;supported platforms: macOS arm64, Linux x86_64, Linux arm64&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;fact&quot;&gt;&lt;div class=&quot;fv&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;fl&quot;&gt;surfaces: pplx search web and pplx content fetch&lt;/div&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;foot&quot;&gt;
    &lt;span&gt;Facts verified against the perplexity-cli README, pplx-cli SKILL.md and docs.perplexity.ai on July 27, 2026.&lt;/span&gt;
    &lt;span&gt;Built by &lt;a href=&quot;https://www.marktechpost.com/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Marktechpost&lt;/a&gt;&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var D=[
    {id:&quot;install&quot;,label:&quot;install.sh&quot;,
     out:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;curl -fsSL https://github.com/perplexityai/perplexity-cli/\\&lt;/span&gt;\n  &lt;span class=&quot;s&quot;&gt;releases/latest/download/install.sh | sh&lt;/span&gt;\n\n&lt;span class=&quot;k&quot;&gt;&amp;gt;&amp;gt;&lt;/span&gt; Resolving latest release\n&lt;span class=&quot;k&quot;&gt;&amp;gt;&amp;gt;&lt;/span&gt; Downloading pplx-aarch64-apple-darwin.bin (2026.07.23.1784814721+8fdc4c2)\n&lt;span class=&quot;k&quot;&gt;&amp;gt;&amp;gt;&lt;/span&gt; Verifying checksum\n&lt;span class=&quot;g&quot;&gt;&amp;gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;g&quot;&gt;Installed 2026.07.23.1784814721+8fdc4c2 to ~/.local/bin/pplx&lt;/span&gt;\n\n&lt;span class=&quot;c&quot;&gt;# no sudo. override the target with PPLX_INSTALL_PATH&lt;/span&gt;',
     note:'The installer reads &lt;b&gt;manifest.json&lt;/b&gt; from the latest release, then pins every remaining download to that release tag. It verifies the binary against &lt;b&gt;SHA256SUMS&lt;/b&gt; before installing, and writes a receipt to &lt;b&gt;~/.config/pplx/pplx-receipt.json&lt;/b&gt; only after the binary runs. Unsupported platforms are rejected outright &amp;mdash; there is no Windows or Intel macOS build.'},

    {id:&quot;auth&quot;,label:&quot;auth login&quot;,
     out:'&lt;span class=&quot;c&quot;&gt;# option A &amp;mdash; environment variable (agents, CI)&lt;/span&gt;\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;export PERPLEXITY_API_KEY=pplx-...&lt;/span&gt;\n\n&lt;span class=&quot;c&quot;&gt;# option B &amp;mdash; store it once (interactive terminal only)&lt;/span&gt;\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;pplx auth login&lt;/span&gt;\n&lt;span class=&quot;k&quot;&gt;?&lt;/span&gt; API key &lt;span class=&quot;c&quot;&gt;&amp;lt;hidden prompt&amp;gt;&lt;/span&gt;\n\n&lt;span class=&quot;c&quot;&gt;# no key configured:&lt;/span&gt;\n&lt;span class=&quot;r&quot;&gt;{&quot;error&quot;:{&quot;code&quot;:&quot;AUTHENTICATION&quot;,&quot;message&quot;:&quot;...&quot;}}&lt;/span&gt;',
     note:'&lt;b&gt;PERPLEXITY_API_KEY takes precedence&lt;/b&gt; over the key stored by &lt;b&gt;pplx auth login&lt;/b&gt;. The stored key lives at &lt;b&gt;~/Library/Application Support/perplexity/credentials.json&lt;/b&gt; on macOS and under &lt;b&gt;$XDG_CONFIG_HOME&lt;/b&gt; or &lt;b&gt;~/.config&lt;/b&gt; on Linux. &lt;b&gt;auth login is TTY-only&lt;/b&gt; and hard-rejects piped input, so agents must use the environment variable. &lt;b&gt;auth logout&lt;/b&gt; and &lt;b&gt;auth login &amp;lt;key&amp;gt;&lt;/b&gt; do not exist. Keys are redacted from all CLI output.'},

    {id:&quot;search&quot;,label:&quot;search web&quot;,
     out:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;pplx search web &quot;rust async runtimes&quot; -n 5 \\&lt;/span&gt;\n    &lt;span class=&quot;s&quot;&gt;--domains docs.rs,rust-lang.org&lt;/span&gt;\n\n{\n  &lt;span class=&quot;k&quot;&gt;&quot;hits&quot;&lt;/span&gt;: [\n    {\n      &lt;span class=&quot;k&quot;&gt;&quot;url&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;&quot;https://docs.rs/tokio/latest/tokio/&quot;&lt;/span&gt;,\n      &lt;span class=&quot;k&quot;&gt;&quot;title&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;&quot;tokio - Rust&quot;&lt;/span&gt;,\n      &lt;span class=&quot;k&quot;&gt;&quot;domain&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;&quot;docs.rs&quot;&lt;/span&gt;,\n      &lt;span class=&quot;k&quot;&gt;&quot;snippet&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;&quot;...&quot;&lt;/span&gt;\n    }\n  ],\n  &lt;span class=&quot;k&quot;&gt;&quot;total&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;5&lt;/span&gt;\n}',
     note:'Flags: &lt;b&gt;-n / --limit&lt;/b&gt; (default 10), &lt;b&gt;--country&lt;/b&gt; (default US), &lt;b&gt;--domains&lt;/b&gt; and &lt;b&gt;--excluded-domains&lt;/b&gt;, &lt;b&gt;--recency-filter&lt;/b&gt; (hour, day, week, month, year), &lt;b&gt;--published-after-date&lt;/b&gt; and &lt;b&gt;--published-before-date&lt;/b&gt;, &lt;b&gt;--search-context-size&lt;/b&gt; (low, medium, high). Date flags use &lt;b&gt;MM/DD/YYYY&lt;/b&gt;, not ISO dates. Extra positional queries are &lt;b&gt;rewordings of the same question&lt;/b&gt;, not separate searches &amp;mdash; N distinct topics still need N invocations.'},

    {id:&quot;fetch&quot;,label:&quot;content fetch&quot;,
     out:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;pplx content fetch https://example.com/article --no-cache&lt;/span&gt;\n\n{\n  &lt;span class=&quot;k&quot;&gt;&quot;title&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;&quot;...&quot;&lt;/span&gt;,\n  &lt;span class=&quot;k&quot;&gt;&quot;description&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;&quot;...&quot;&lt;/span&gt;,\n  &lt;span class=&quot;k&quot;&gt;&quot;authors&quot;&lt;/span&gt;: [&lt;span class=&quot;g&quot;&gt;&quot;...&quot;&lt;/span&gt;],\n  &lt;span class=&quot;k&quot;&gt;&quot;published_date&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;&quot;...&quot;&lt;/span&gt;,\n  &lt;span class=&quot;k&quot;&gt;&quot;domain&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;&quot;example.com&quot;&lt;/span&gt;,\n  &lt;span class=&quot;k&quot;&gt;&quot;is_paywall&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;false&lt;/span&gt;,\n  &lt;span class=&quot;k&quot;&gt;&quot;content&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;&quot;cleaned page text ...&quot;&lt;/span&gt;\n}',
     note:'Accepts &lt;b&gt;http(s) URLs only&lt;/b&gt;. Check &lt;b&gt;error&lt;/b&gt; and &lt;b&gt;is_paywall&lt;/b&gt; in the output &lt;b&gt;before trusting content&lt;/b&gt;. &lt;b&gt;--html&lt;/b&gt; adds a &lt;b&gt;raw_html&lt;/b&gt; field fetched live via crawler &amp;mdash; large, so pair it with a save directory and a preview. &lt;b&gt;--no-cache&lt;/b&gt; forces a live fetch; the cache is on by default. Saved results land at &lt;b&gt;{dir}/fetch/{rand}.json&lt;/b&gt;.'},

    {id:&quot;budget&quot;,label:&quot;token budget&quot;,
     out:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;pplx search web &quot;kubernetes pod OOMKilled causes&quot; \\&lt;/span&gt;\n    &lt;span class=&quot;s&quot;&gt;&quot;why does k8s keep killing my pod with OOMKilled&quot; \\&lt;/span&gt;\n    &lt;span class=&quot;s&quot;&gt;-n 5 --output-dir out --stdout-preview=200&lt;/span&gt;\n\n{\n  &lt;span class=&quot;k&quot;&gt;&quot;hits&quot;&lt;/span&gt;: [ { ... &lt;span class=&quot;g&quot;&gt;&quot;...&amp;lt;truncated&amp;gt;&quot;&lt;/span&gt; } ],\n  &lt;span class=&quot;k&quot;&gt;&quot;total&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;5&lt;/span&gt;,\n  &lt;span class=&quot;k&quot;&gt;&quot;saved_to&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;&quot;out/web/8f3c1a.json&quot;&lt;/span&gt;,\n  &lt;span class=&quot;k&quot;&gt;&quot;truncated&quot;&lt;/span&gt;: &lt;span class=&quot;g&quot;&gt;true&lt;/span&gt;\n}',
     note:'This is the save-and-preview pattern the official skill recommends. &lt;b&gt;--stdout-preview is a no-op without a save directory&lt;/b&gt; &amp;mdash; it only truncates when the result is also written via &lt;b&gt;--output-dir&lt;/b&gt; or &lt;b&gt;$PPLX_OUTPUT_DIR&lt;/b&gt;. Alone, you get full-size output, and hits can be multiple KB each. The file is written only after a successful request.'},

    {id:&quot;errors&quot;,label:&quot;error contract&quot;,
     out:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;pplx search web &quot;openai news&quot; --recency-filter week \\&lt;/span&gt;\n    &lt;span class=&quot;s&quot;&gt;--published-after-date 07/01/2026&lt;/span&gt;\n\n&lt;span class=&quot;c&quot;&gt;# stdout: empty&lt;/span&gt;\n&lt;span class=&quot;c&quot;&gt;# stderr:&lt;/span&gt;\n&lt;span class=&quot;r&quot;&gt;{&quot;error&quot;:{&quot;code&quot;:&quot;BAD_REQUEST&quot;,&lt;/span&gt;\n&lt;span class=&quot;r&quot;&gt;  &quot;message&quot;:&quot;recency cannot be used together with&lt;/span&gt;\n&lt;span class=&quot;r&quot;&gt;    published_after or published_before&quot;,&lt;/span&gt;\n&lt;span class=&quot;r&quot;&gt;  &quot;command&quot;:&quot;search web&quot;}}&lt;/span&gt;\n\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;echo $?&lt;/span&gt;\n&lt;span class=&quot;r&quot;&gt;1&lt;/span&gt;',
     note:'Every failure exits 1 with an &lt;b&gt;empty stdout&lt;/b&gt; and exactly one JSON error object on &lt;b&gt;stderr&lt;/b&gt; &amp;mdash; parse stderr, not stdout, when the exit code is non-zero. Documented codes include &lt;b&gt;AUTHENTICATION&lt;/b&gt;, &lt;b&gt;UNKNOWN_ARGUMENT&lt;/b&gt;, &lt;b&gt;ARGUMENT_ERROR&lt;/b&gt; and &lt;b&gt;BAD_REQUEST&lt;/b&gt;, and the list is not exhaustive, so branch on &lt;b&gt;error.code&lt;/b&gt;. Mixing &lt;b&gt;--recency-filter&lt;/b&gt; with date bounds is rejected server-side, not client-side.'},

    {id:&quot;update&quot;,label:&quot;update&quot;,
     out:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;pplx update --check&lt;/span&gt;\n&lt;span class=&quot;k&quot;&gt;&amp;gt;&amp;gt;&lt;/span&gt; newer release available: v0.2.2\n\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;pplx update&lt;/span&gt;\n&lt;span class=&quot;k&quot;&gt;&amp;gt;&amp;gt;&lt;/span&gt; Verifying checksum\n&lt;span class=&quot;g&quot;&gt;&amp;gt;&amp;gt;&lt;/span&gt; &lt;span class=&quot;g&quot;&gt;Updated to 2026.07.23.1784814721+8fdc4c2&lt;/span&gt;\n\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;pplx --version&lt;/span&gt;\n&lt;span class=&quot;g&quot;&gt;2026.07.23.1784814721+8fdc4c2&lt;/span&gt;',
     note:'&lt;b&gt;pplx update&lt;/b&gt; (v0.2.2 and later) downloads the latest release for your platform, verifies its SHA-256 checksum against the release manifest, and atomically replaces the installed binary. &lt;b&gt;Neither update nor --check needs an API key.&lt;/b&gt; Releases are semver-tagged; &lt;b&gt;pplx --version&lt;/b&gt; prints a date-stamped build string in the form &lt;b&gt;YYYY.MM.DD.&amp;lt;build&amp;gt;+&amp;lt;sha&amp;gt;&lt;/b&gt;, and each release manifest ties the two together.'}
  ];

  var nav=document.getElementById(&quot;pplxNav&quot;),
      out=document.getElementById(&quot;pplxOut&quot;),
      note=document.getElementById(&quot;pplxNote&quot;),
      btns=[];

  function pick(i){
    for(var j=0;j&lt;btns.length;j++){btns[j].className=&quot;cmd&quot;+(j===i?&quot; on&quot;:&quot;&quot;);btns[j].setAttribute(&quot;aria-pressed&quot;,j===i?&quot;true&quot;:&quot;false&quot;);}
    out.innerHTML=D[i].out; note.innerHTML=D[i].note; ping();
  }

  for(var i=0;i&lt;D.length;i++){
    (function(i){
      var b=document.createElement(&quot;button&quot;);
      b.type=&quot;button&quot;; b.className=&quot;cmd&quot;;
      b.innerHTML='&lt;span&gt;'+String(i+1).padStart(2,&quot;0&quot;)+'&lt;/span&gt;pplx '+D[i].label;
      b.onclick=function(){pick(i);};
      nav.appendChild(b); btns.push(b);
    })(i);
  }

  var STEPS=[50,100,200,300,500,750,1000,1500,2000,3000,5000,7500,10000,15000,20000,30000,50000,75000,100000,150000,200000,300000,500000,750000,1000000];
  var sl=document.getElementById(&quot;pplxSlide&quot;), q=document.getElementById(&quot;pplxQ&quot;), c=document.getElementById(&quot;pplxC&quot;);
  sl.max=String(STEPS.length-1);
  function money(v){
    if(v&gt;=1000) return &quot;$&quot;+Math.round(v).toLocaleString(&quot;en-US&quot;);
    if(v&gt;=1) return &quot;$&quot;+v.toFixed(2).replace(/\.00$/,&quot;&quot;);
    return &quot;$&quot;+v.toFixed(2);
  }
  function calc(){
    var n=STEPS[+sl.value];
    q.textContent=n.toLocaleString(&quot;en-US&quot;);
    c.textContent=money(n*30/1000*5);
  }
  sl.addEventListener(&quot;input&quot;,calc);
  calc();

  function ping(){
    try{
      var h=document.getElementById(&quot;pplxRoot&quot;).offsetHeight+40;
      parent.postMessage({pplxEmbed:&quot;pplx-explainer&quot;,height:h},&quot;*&quot;);
    }catch(e){}
  }
  window.addEventListener(&quot;load&quot;,ping);
  window.addEventListener(&quot;resize&quot;,ping);
  pick(0);
  setTimeout(ping,120); setTimeout(ping,600);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
" style="width:100%;border:0;display:block;height:620px;overflow:hidden;" scrolling="no" loading="lazy" title="Interactive explainer: how the pplx CLI works"></iframe>
</div>
<script>
(function(){
  var f=document.getElementById("mtpPplxExplainerFrame");
  window.addEventListener("message",function(e){
    if(!e.data||e.data.pplxEmbed!=="pplx-explainer")return;
    var h=parseInt(e.data.height,10);
    if(h>0)f.style.height=h+"px";
  });
})();
</script>
<style>
#mtp-pplx-explainer hr,#mtp-pplx-explainer p:empty,#mtp-pplx-explainer del,#mtp-pplx-explainer s{display:none!important}
</style>




<p class="wp-block-paragraph"></p>



<h2 id="h-install-path-and-platform-support" class="wp-block-heading"><strong>Install path and platform support</strong></h2>



<p class="wp-block-paragraph">Installation is a single shell command that pipes a script into <code>sh</code>:</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">curl -fsSL https://github.com/perplexityai/perplexity-cli/releases/latest/download/install.sh | sh</code></pre></div></div>



<p class="wp-block-paragraph">Reading <a href="https://github.com/perplexityai/perplexity-cli/blob/main/install.sh"><code>install.sh</code></a> shows what it actually does. It downloads <code>manifest.json</code> from the latest release and extracts the tag and version. It then pins every remaining download to that tag, explicitly to avoid racing a concurrent publish.</p>



<p class="wp-block-paragraph">It fetches <code>SHA256SUMS</code> and the platform binary, verifies the checksum, and installs to <code>~/.local/bin/pplx</code>. No sudo is required. A receipt is written to <code>~/.config/pplx/pplx-receipt.json</code>, but only after the installed binary runs successfully.</p>



<p class="wp-block-paragraph">Platform coverage is limited to three targets: macOS on Apple Silicon, Linux x86_64 and Linux arm64. Anything else exits with an error. There is no Windows build and no Intel macOS build.</p>



<h2 id="h-context-window-economics-are-a-first-class-design-concern" class="wp-block-heading"><strong>Context-window economics are a first-class design concern</strong></h2>



<p class="wp-block-paragraph">The most agent-specific feature is token budgeting. <code>--output-dir</code> writes the full result set to a JSON file. <code>--stdout-preview[=&lt;CHARS&gt;]</code> truncates long string fields in stdout, adding <code>...&lt;truncated&gt;</code> markers.</p>



<p class="wp-block-paragraph">The skill is blunt about the trap: <code>--stdout-preview</code> is a no-op without a save directory. It truncates only when the result is also saved via <code>--output-dir</code> or <code>$PPLX_OUTPUT_DIR</code>. Used alone it returns full-size output, and hits can be multiple KB each.</p>



<p class="wp-block-paragraph">Saved search results land at <code>{dir}/web/{rand}.json</code> and fetches at <code>{dir}/fetch/{rand}.json</code>. Files are written only after a successful request. <code>PPLX_OUTPUT_DIR</code> sets a workspace default so the flag need not be repeated.</p>



<p class="wp-block-paragraph">For content fetch, the skill adds a correctness check rather than a cost one. Verify <code>error</code> and <code>is_paywall</code> in the output before trusting <code>content</code>. <code>--html</code> adds a <code>raw_html</code> field fetched live via crawler, and <code>--no-cache</code> forces a live fetch. </p>



<div id="mtp-pplx-deploy" style="margin:0 0 24px;">
<iframe id="mtpPplxDeployFrame" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;style&gt;
  *{box-sizing:border-box;margin:0;padding:0}
  html,body{background:transparent}
  body{font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Inter,Roboto,Helvetica,Arial,sans-serif;-webkit-font-smoothing:antialiased}
  code,pre{font-family:ui-monospace,SFMono-Regular,&quot;SF Mono&quot;,Menlo,Consolas,&quot;Liberation Mono&quot;,monospace}
  #pxRoot{background:#121516;color:#F7F7F8;border:1px solid #253032;border-radius:12px;overflow:hidden;max-width:960px;margin:0 auto}
  .hd{padding:18px 20px 16px;border-bottom:1px solid #1e2628;background:linear-gradient(180deg,#141a1b,#121516)}
  .eyebrow{font-size:10.5px;letter-spacing:.18em;color:#4FC4CF;font-weight:600}
  .h{font-size:19px;line-height:1.3;margin-top:7px;font-weight:600;letter-spacing:-.01em}
  .sub{font-size:13px;line-height:1.6;color:#9AA6A8;margin-top:7px}
  .sub code{color:#4FC4CF;background:#182022;padding:1px 5px;border-radius:4px;font-size:12px}

  .steps{display:flex;flex-wrap:wrap;gap:1px;background:#1e2628;border-bottom:1px solid #1e2628}
  .st{flex:1 1 120px;background:#121516;border:0;border-top:2px solid transparent;color:#8B999B;font:inherit;font-size:12px;text-align:left;padding:12px 14px;cursor:pointer;transition:background .13s,color .13s}
  .st:hover{background:#171d1f;color:#F7F7F8}
  .st.on{background:#101819;border-top-color:#20808D;color:#F7F7F8}
  .st .n{display:block;font-size:10px;letter-spacing:.14em;color:#5C6A6C;margin-bottom:4px}
  .st.on .n{color:#4FC4CF}

  .body{padding:18px 20px}
  .stephead{font-size:15px;font-weight:600;margin-bottom:6px}
  .stepwhy{font-size:13px;line-height:1.65;color:#9AA6A8;margin-bottom:13px}
  .stepwhy b{color:#F7F7F8;font-weight:600}
  .stepwhy a{color:#4FC4CF}
  pre.sh{background:#0B0F10;border:1px solid #1e2628;border-left:2px solid #20808D;border-radius:7px;padding:12px 14px;font-size:12.5px;line-height:1.7;color:#E4E9EA;overflow-x:auto;white-space:pre}
  pre.sh .p{color:#20808D;font-weight:600}
  pre.sh .c{color:#5C6A6C}
  pre.sh .v{color:#4FC4CF}
  .gotcha{margin-top:12px;background:#1a1512;border:1px solid #33291f;border-radius:7px;padding:10px 13px;font-size:12px;line-height:1.6;color:#C9BCA8}
  .gotcha b{color:#E8A33D}

  .dsec{border-top:1px solid #1e2628;padding:18px 20px 6px}
  .dlabel{font-size:10.5px;letter-spacing:.18em;color:#4FC4CF;font-weight:600;margin-bottom:4px}
  .dintro{font-size:13px;line-height:1.6;color:#9AA6A8;margin-bottom:14px}
  .cards{display:flex;flex-wrap:wrap;gap:10px}
  .card{flex:1 1 210px;min-width:200px;background:#151b1c;border:1px solid #253032;border-radius:9px;overflow:hidden;transition:border-color .13s}
  .card:hover{border-color:#2f4247}
  .card.on{border-color:#20808D}
  .cbtn{width:100%;text-align:left;background:transparent;border:0;color:#F7F7F8;font:inherit;padding:13px 14px;cursor:pointer;display:block}
  .ct{font-size:13.5px;font-weight:600}
  .cd{font-size:11.5px;color:#8B999B;margin-top:4px;line-height:1.55}
  .cwho{font-size:10px;letter-spacing:.12em;color:#20808D;font-weight:600;margin-bottom:6px}
  .card.on .cwho{color:#4FC4CF}
  .cbody{display:none;padding:0 14px 14px}
  .card.on .cbody{display:block}
  .cbody pre{background:#0B0F10;border:1px solid #1e2628;border-radius:6px;padding:10px 12px;font-size:11.5px;line-height:1.7;color:#E4E9EA;overflow-x:auto;white-space:pre}
  .cnote{font-size:11.5px;line-height:1.6;color:#8B999B;margin-top:9px}
  .cnote b{color:#C4CFD1}
  .chev{float:right;color:#5C6A6C;font-size:0;line-height:1}
  .chev::after{content:&quot;+&quot;;font-size:13px}
  .card.on .chev{color:#4FC4CF}
  .card.on .chev::after{content:&quot;\2013&quot;}

  .foot{margin-top:16px;padding:12px 20px;background:#0E1213;border-top:1px solid #1e2628;font-size:10.5px;color:#6B7779;display:flex;flex-wrap:wrap;gap:8px;justify-content:space-between}
  .foot a{color:#20808D;text-decoration:none}
  .foot a:hover{text-decoration:underline}
  .st:focus-visible,.cbtn:focus-visible,.foot a:focus-visible{outline:2px solid #4FC4CF;outline-offset:-2px}
  @media (max-width:640px){
    .st{flex:1 1 33%;font-size:11px;padding:10px}
    .h{font-size:17px}
    pre.sh{font-size:11px}
    .body,.dsec,.hd{padding-left:14px;padding-right:14px}
    .card{flex:1 1 100%}
  }
  @media (prefers-reduced-motion:reduce){*{transition:none!important}}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;pxRoot&quot;&gt;
  &lt;div class=&quot;hd&quot;&gt;
    &lt;div class=&quot;eyebrow&quot;&gt;HOW TO USE IT&lt;/div&gt;
    &lt;div class=&quot;h&quot;&gt;Setting up &lt;code style=&quot;color:#4FC4CF&quot;&gt;pplx&lt;/code&gt; &amp;mdash; five steps, then four ways to deploy it&lt;/div&gt;
    &lt;div class=&quot;sub&quot;&gt;The CLI talks to &lt;code&gt;https://api.perplexity.ai&lt;/code&gt;. Nothing here needs sudo, Node.js or Python.&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;steps&quot; id=&quot;pxSteps&quot;&gt;&lt;/div&gt;
  &lt;div class=&quot;body&quot;&gt;
    &lt;div class=&quot;stephead&quot; id=&quot;pxHead&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;stepwhy&quot; id=&quot;pxWhy&quot;&gt;&lt;/div&gt;
    &lt;pre class=&quot;sh&quot; id=&quot;pxCode&quot;&gt;&lt;/pre&gt;
    &lt;div class=&quot;gotcha&quot; id=&quot;pxGot&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;dsec&quot;&gt;
    &lt;div class=&quot;dlabel&quot;&gt;DEPLOYMENT OPTIONS&lt;/div&gt;
    &lt;div class=&quot;dintro&quot;&gt;The same binary ships four ways. Pick by who is typing the command &amp;mdash; a person, a coding agent, or a build server.&lt;/div&gt;
    &lt;div class=&quot;cards&quot; id=&quot;pxCards&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;foot&quot;&gt;
    &lt;span&gt;Commands transcribed from the perplexity-cli README, the pplx-cli Agent Skill and the api-platform-developers README, checked July 27, 2026.&lt;/span&gt;
    &lt;span&gt;Built by &lt;a href=&quot;https://www.marktechpost.com/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Marktechpost&lt;/a&gt;&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var S=[
    {t:&quot;Install&quot;,h:&quot;Install the binary&quot;,
     why:'One command. The installer resolves the latest release, verifies the SHA-256 checksum, and drops a single binary into &lt;b&gt;~/.local/bin/pplx&lt;/b&gt;. Set &lt;b&gt;PPLX_INSTALL_PATH&lt;/b&gt; to install elsewhere.',
     code:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; curl -fsSL https://github.com/perplexityai/perplexity-cli/\\\n    releases/latest/download/install.sh | sh\n\n&lt;span class=&quot;c&quot;&gt;# confirm it is on PATH&lt;/span&gt;\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx --version\n&lt;span class=&quot;v&quot;&gt;2026.07.23.1784814721+8fdc4c2&lt;/span&gt;',
     got:'&lt;b&gt;Platform check first.&lt;/b&gt; Only macOS arm64, Linux x86_64 and Linux arm64 have builds. If &lt;b&gt;~/.local/bin&lt;/b&gt; is not on your PATH, the installer prints a note but does not edit your shell profile &amp;mdash; add it yourself.'},

    {t:&quot;Authenticate&quot;,h:&quot;Give it an API key&quot;,
     why:'Create a key at &lt;a href=&quot;https://www.perplexity.ai/account/api&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;perplexity.ai/account/api&lt;/a&gt;. Use the environment variable for anything scripted, and &lt;b&gt;pplx auth login&lt;/b&gt; only at a real terminal.',
     code:'&lt;span class=&quot;c&quot;&gt;# agents, CI, containers&lt;/span&gt;\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; export PERPLEXITY_API_KEY=&lt;span class=&quot;v&quot;&gt;pplx-...&lt;/span&gt;\n\n&lt;span class=&quot;c&quot;&gt;# humans, stores the key once&lt;/span&gt;\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx auth login',
     got:'&lt;b&gt;pplx auth login is TTY-only.&lt;/b&gt; It hard-rejects piped input, so it cannot run inside an agent loop. There is no &lt;b&gt;auth logout&lt;/b&gt; and no &lt;b&gt;auth login &amp;lt;key&amp;gt;&lt;/b&gt;. When both are set, the environment variable wins.'},

    {t:&quot;Search&quot;,h:&quot;Run your first search&quot;,
     why:'Results come back as one JSON object, so &lt;b&gt;jq&lt;/b&gt; works immediately and so does an agent tool call. Scope the query with domain, country, recency or date filters.',
     code:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx search web &lt;span class=&quot;v&quot;&gt;&quot;rust async runtimes&quot;&lt;/span&gt; -n 5\n\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx search web &lt;span class=&quot;v&quot;&gt;&quot;openai news&quot;&lt;/span&gt; --recency-filter week\n\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx search web &lt;span class=&quot;v&quot;&gt;&quot;model releases&quot;&lt;/span&gt; \\\n    --domains wikipedia.org,arxiv.org | jq -r &lt;span class=&quot;v&quot;&gt;\'.hits[].url\'&lt;/span&gt;',
     got:'&lt;b&gt;Date flags are MM/DD/YYYY&lt;/b&gt;, not ISO. And do not combine &lt;b&gt;--recency-filter&lt;/b&gt; with &lt;b&gt;--published-after-date&lt;/b&gt; or &lt;b&gt;--published-before-date&lt;/b&gt; &amp;mdash; the server rejects that pair as &lt;b&gt;BAD_REQUEST&lt;/b&gt;, wasting a round trip.'},

    {t:&quot;Fetch&quot;,h:&quot;Pull a page as clean text&quot;,
     why:'&lt;b&gt;pplx content fetch&lt;/b&gt; returns cleaned page content plus metadata: title, description, authors, published date, domain, and paywall and cache flags.',
     code:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx content fetch https://example.com/article\n\n&lt;span class=&quot;c&quot;&gt;# force a live fetch instead of a cache lookup&lt;/span&gt;\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx content fetch https://example.com --no-cache\n\n&lt;span class=&quot;c&quot;&gt;# add raw page source (large)&lt;/span&gt;\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx content fetch https://example.com --html \\\n    --output-dir out --stdout-preview=200',
     got:'&lt;b&gt;Check error and is_paywall before trusting content.&lt;/b&gt; A paywalled page still returns a response. http(s) URLs only.'},

    {t:&quot;Budget&quot;,h:&quot;Keep stdout small&quot;,
     why:'Default search output can run to tens of KB, which is real money in an agent context window. Save the full result to disk and print a truncated preview.',
     code:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; export PPLX_OUTPUT_DIR=./out\n\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx search web &lt;span class=&quot;v&quot;&gt;&quot;kubernetes pod OOMKilled causes&quot;&lt;/span&gt; \\\n    -n 5 --stdout-preview=200\n\n&lt;span class=&quot;c&quot;&gt;# { ..., &quot;saved_to&quot;: &quot;out/web/8f3c1a.json&quot;, &quot;truncated&quot;: true }&lt;/span&gt;',
     got:'&lt;b&gt;--stdout-preview does nothing on its own.&lt;/b&gt; It only truncates when the result is also being saved, via &lt;b&gt;--output-dir&lt;/b&gt; or &lt;b&gt;$PPLX_OUTPUT_DIR&lt;/b&gt;. Without a save directory you get full-size output.'},

    {t:&quot;Update&quot;,h:&quot;Stay current&quot;,
     why:'&lt;b&gt;pplx update&lt;/b&gt; replaces the installed binary in place after verifying its checksum against the release manifest. Neither update nor the check needs an API key.',
     code:'&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx update --check\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; pplx update\n\n&lt;span class=&quot;c&quot;&gt;# uninstall&lt;/span&gt;\n&lt;span class=&quot;p&quot;&gt;$&lt;/span&gt; rm ~/.local/bin/pplx ~/.config/pplx/pplx-receipt.json',
     got:'&lt;b&gt;Also remove the stored key&lt;/b&gt; at &lt;b&gt;~/.config/perplexity/credentials.json&lt;/b&gt; on Linux or &lt;b&gt;~/Library/Application Support/perplexity/credentials.json&lt;/b&gt; on macOS. Re-running the install one-liner replaces the binary in place too.'}
  ];

  var C=[
    {who:&quot;HUMANS&quot;,t:&quot;Straight shell&quot;,d:&quot;Install, log in once, pipe JSON into jq.&quot;,
     code:'$ curl -fsSL https://github.com/perplexityai/perplexity-cli/\n    releases/latest/download/install.sh | sh\n$ pplx auth login\n$ pplx search web &quot;query&quot; | jq \'.hits[0]\'',
     note:'The lightest path. &lt;b&gt;PPLX_OUTPUT_DIR&lt;/b&gt; gives every command a default save directory so you stop repeating &lt;b&gt;--output-dir&lt;/b&gt;.'},

    {who:&quot;CLAUDE CODE&quot;,t:&quot;Plugin marketplace&quot;,d:&quot;Installs the skill and the docs MCP server together.&quot;,
     code:'/plugin marketplace add perplexityai/api-platform-developers\n/plugin install perplexity-platform@api-platform-developers\n/reload-plugins\n\n/perplexity-platform:pplx-cli [query or URL]',
     note:'The plugin ships the docs MCP server in &lt;b&gt;.mcp.json&lt;/b&gt;, so installing the plugin is enough. Standalone: &lt;b&gt;claude mcp add --transport http perplexity-docs https://docs.perplexity.ai/mcp&lt;/b&gt;.'},

    {who:&quot;CODEX &amp; OTHERS&quot;,t:&quot;Agent Skills clients&quot;,d:&quot;Portable skill folder, or one of the ecosystem installers.&quot;,
     code:'codex plugin marketplace add perplexityai/api-platform-developers\n\n# or skills only:\n# project: &lt;repo&gt;/.agents/skills/pplx-cli\n# user:    ~/.agents/skills/pplx-cli\n\nnpx skills add perplexityai/api-platform-developers',
     note:'Invoke implicitly by description match, or explicitly with &lt;b&gt;$pplx-cli&lt;/b&gt;. &lt;b&gt;gh skill add&lt;/b&gt; supports version pinning, which matters if you want a fixed skill revision.'},

    {who:&quot;ANY AGENT / CI&quot;,t:&quot;Point at the raw SKILL.md&quot;,d:&quot;No plugin system required. Works headless.&quot;,
     code:'Read https://raw.githubusercontent.com/perplexityai/\napi-platform-developers/main/skills/pplx-cli/SKILL.md,\ninstall the skill, and use it for search.\n\n# non-interactive prerequisites\nexport PERPLEXITY_API_KEY=pplx-...\nexport PPLX_OUTPUT_DIR=/workspace/out',
     note:'Parse &lt;b&gt;stderr&lt;/b&gt;, not stdout, whenever the exit code is 1 &amp;mdash; that is where the single JSON error object lands. Branch on &lt;b&gt;error.code&lt;/b&gt;, since the documented list is not exhaustive.'}
  ];

  var nav=document.getElementById(&quot;pxSteps&quot;), head=document.getElementById(&quot;pxHead&quot;),
      why=document.getElementById(&quot;pxWhy&quot;), code=document.getElementById(&quot;pxCode&quot;),
      got=document.getElementById(&quot;pxGot&quot;), sbtn=[];

  function step(i){
    for(var j=0;j&lt;sbtn.length;j++){sbtn[j].className=&quot;st&quot;+(j===i?&quot; on&quot;:&quot;&quot;);}
    head.textContent=S[i].h; why.innerHTML=S[i].why; code.innerHTML=S[i].code; got.innerHTML=S[i].got; ping();
  }
  for(var i=0;i&lt;S.length;i++){(function(i){
    var b=document.createElement(&quot;button&quot;); b.type=&quot;button&quot;; b.className=&quot;st&quot;;
    b.innerHTML='&lt;span class=&quot;n&quot;&gt;STEP '+(i+1)+'&lt;/span&gt;'+S[i].t;
    b.onclick=function(){step(i);}; nav.appendChild(b); sbtn.push(b);
  })(i);}

  var wrap=document.getElementById(&quot;pxCards&quot;), cards=[];
  function card(i){
    for(var j=0;j&lt;cards.length;j++){cards[j].className=&quot;card&quot;+(j===i?&quot; on&quot;:&quot;&quot;);}
    ping();
  }
  for(var i=0;i&lt;C.length;i++){(function(i){
    var d=document.createElement(&quot;div&quot;); d.className=&quot;card&quot;;
    d.innerHTML='&lt;button type=&quot;button&quot; class=&quot;cbtn&quot;&gt;&lt;span class=&quot;chev&quot;&gt;+&lt;/span&gt;'+
      '&lt;div class=&quot;cwho&quot;&gt;'+C[i].who+'&lt;/div&gt;&lt;div class=&quot;ct&quot;&gt;'+C[i].t+'&lt;/div&gt;'+
      '&lt;div class=&quot;cd&quot;&gt;'+C[i].d+'&lt;/div&gt;&lt;/button&gt;'+
      '&lt;div class=&quot;cbody&quot;&gt;&lt;pre&gt;'+C[i].code+'&lt;/pre&gt;&lt;div class=&quot;cnote&quot;&gt;'+C[i].note+'&lt;/div&gt;&lt;/div&gt;';
    d.querySelector(&quot;.cbtn&quot;).onclick=function(){card(cards[i].className.indexOf(&quot;on&quot;)&gt;-1?-1:i);};
    wrap.appendChild(d); cards.push(d);
  })(i);}

  function ping(){
    try{
      var h=document.getElementById(&quot;pxRoot&quot;).offsetHeight+40;
      parent.postMessage({pplxEmbed:&quot;pplx-deploy&quot;,height:h},&quot;*&quot;);
    }catch(e){}
  }
  window.addEventListener(&quot;load&quot;,ping);
  window.addEventListener(&quot;resize&quot;,ping);
  step(0); card(0);
  setTimeout(ping,120); setTimeout(ping,600);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
" style="width:100%;border:0;display:block;height:640px;overflow:hidden;" scrolling="no" loading="lazy" title="Step-by-step setup and deployment options for the pplx CLI"></iframe>
</div>
<script>
(function(){
  var f=document.getElementById("mtpPplxDeployFrame");
  window.addEventListener("message",function(e){
    if(!e.data||e.data.pplxEmbed!=="pplx-deploy")return;
    var h=parseInt(e.data.height,10);
    if(h>0)f.style.height=h+"px";
  });
})();
</script>
<style>
#mtp-pplx-deploy hr,#mtp-pplx-deploy p:empty,#mtp-pplx-deploy del,#mtp-pplx-deploy s{display:none!important}
</style>




<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li><code>pplx</code> is a single verified binary exposing two commands: <code>pplx search web</code> and <code>pplx content fetch</code>.</li>



<li>Success is exit 0 plus one JSON object on stdout; failures put one JSON error object on stderr.</li>



<li><code>pplx auth login</code> is TTY-only, so agents and CI must export <code>PERPLEXITY_API_KEY</code>.</li>



<li><code>--stdout-preview</code> only truncates when paired with <code>--output-dir</code> or <code>$PPLX_OUTPUT_DIR</code>.</li>



<li>Search API billing is $5.00 per 1,000 requests, capped at 50 QPS on every usage tier.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"><strong>Sources:</strong> <a href="https://github.com/perplexityai/perplexity-cli">perplexityai/perplexity-cli</a>, <a href="https://github.com/perplexityai/api-platform-developers/blob/main/skills/pplx-cli/SKILL.md">pplx-cli SKILL.md</a>, <a href="https://github.com/perplexityai/api-platform-developers">api-platform-developers</a>, <a href="https://docs.perplexity.ai/docs/getting-started/pricing">Perplexity API pricing</a>, <a href="https://docs.perplexity.ai/docs/admin/rate-limits-usage-tiers">Rate limits and usage tiers</a>, and <a href="https://docs.perplexity.ai/docs/search/quickstart">Search API quickstart</a></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/27/perplexity-releases-pplx/">Perplexity Releases pplx, a Single-Binary CLI That Puts Its Search API in the Terminal for Coding Agents</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81383</post-id>
	</item>
		<item>
		<title>Black Forest Labs Releases FLUX 3: A Multimodal Flow Model for Image, Video, Audio and Robot Action Prediction</title>
		<link>https://www.marktechpost.com/2026/07/26/black-forest-labs-releases-flux-3-a-multimodal-flow-model-for-image-video-audio-and-robot-action-prediction/</link>
		
		<dc:creator><![CDATA[Michal Sutter]]></dc:creator>
		<pubDate>Sun, 26 Jul 2026 17:50:23 +0000</pubDate>
				<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Computer Vision]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Physical AI]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81375</guid>

					<description><![CDATA[<p>Black Forest Labs (BFL) has released FLUX 3, a multimodal foundation model that learns from images, videos and audio inside a single architecture. It is also the first FLUX model to ship video, audio and action prediction from one set of weights. The Black Forest Labs (BFL) research team argues that no single modality gives [&#8230;]</p>
<p>The post <a href="https://www.marktechpost.com/2026/07/26/black-forest-labs-releases-flux-3-a-multimodal-flow-model-for-image-video-audio-and-robot-action-prediction/">Black Forest Labs Releases FLUX 3: A Multimodal Flow Model for Image, Video, Audio and Robot Action Prediction</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Black Forest Labs (BFL) has released <a href="https://bfl.ai/blog/flux-3">FLUX 3</a>, a multimodal foundation model that learns from images, videos and audio inside a single architecture. It is also the first FLUX model to ship video, audio and action prediction from one set of weights.</p>



<p class="wp-block-paragraph">The Black Forest Labs (BFL) research team argues that no single modality gives a complete description of the world. Images capture spatial structure at one instant. Video restores time and exposes physical dynamics. Audio reveals causal relationships between mechanical events and sound. Each is treated as a lossy projection of the same underlying reality.</p>



<p class="wp-block-paragraph">Training on all of them at once means the modalities constrain each other. The sound has to match the impact. The motion has to obey the mass. The research team calls FLUX 3 its first model built entirely on that principle.</p>



<h2 id="h-the-method-underneath-self-flow" class="wp-block-heading"><strong>The method underneath: Self-Flow</strong></h2>



<p class="wp-block-paragraph">FLUX 3 builds on <a href="https://bfl.ai/research/self-flow">Self-Flow</a>, BFL&#8217;s method for aligning multimodal generation and understanding in one architecture. Self-Flow combines the flow matching objective with a self-supervised feature reconstruction objective. The <a href="https://github.com/black-forest-labs/Self-Flow">reference implementation on GitHub</a> is Apache-2.0 and uses SiT-XL/2 with per-token timestep conditioning. It trains with a 25% per-token mask ratio and self-distillation from an EMA teacher at layer 20 to a student at layer 8.</p>



<p class="wp-block-paragraph">That released checkpoint is an ImageNet 256×256 research model, not FLUX 3. BFL states that it &#8216;significantly scaled up compute and data resources&#8217; on the same approach to train FLUX 3 across video, images and audio simultaneously. Self-Flow itself was introduced in March 2026, so it is not new to this launch. What is new is the scale.</p>



<h2 id="h-what-flux-3-video-does" class="wp-block-heading"><strong>What FLUX 3 Video does</strong></h2>



<p class="wp-block-paragraph">FLUX 3 Video generates clips up to 20 seconds long in a single generation, with native audio. The supported modes cover text-to-video, image-to-video, video-to-video from a reference clip, keyframe-to-video for controlled transitions, and generative video-audio continuation from input video and audio.</p>



<p class="wp-block-paragraph">BFL also lists multilingual dialogue, agentic chaining of clips into multi-shot sequences, and strong typography generation with animated designs. The BFL team reports particular strength in human facial expressions and in associating sounds with physical events.</p>



<h2 id="h-performance" class="wp-block-heading"><strong>Performance</strong></h2>



<p class="wp-block-paragraph">BFL team published preliminary human preference results. The setup was 10-second text-to-video clips at 720p with audio. FLUX 3 was preferred over Luma Ray 3.2 in 93% of comparisons and over Runway Gen-4.5 in 77%. Against Grok Imagine Video the figure is up to 69%, then Kling v3 Pro at 60%, Happy Horse v1 at 59% and Happy Horse 1.1 at 57%. Against Seedance 2.0 and Gemini Omni Flash the result is 52%, close to a coin flip.</p>



<h2 id="h-interactive-explorer" class="wp-block-heading"><strong>Interactive</strong> <strong>Explorer</strong></h2>



<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
  :root{
    --bg:#0a0a0a; --panel:#121212; --panel2:#181818; --line:#282828;
    --fg:#f4f4f4; --dim:#8b8b8b; --orange:#ff7a18; --orange2:#ffab5e; --orange-dim:#7a3a08;
    --mono: ui-monospace, "JetBrains Mono", "SFMono-Regular", Menlo, Consolas, "Liberation Mono", monospace;
  }
  *{box-sizing:border-box;}
  html,body{margin:0;padding:0;background:transparent;}
  body{font-family:var(--mono);}

  #mtp-flux3-term{
    background:var(--bg); color:var(--fg); border:1px solid var(--line);
    border-radius:10px; overflow:hidden; font-family:var(--mono);
    font-size:13.5px; line-height:1.55; max-width:100%;
  }

  /* title bar */
  .tbar{display:flex;align-items:center;gap:10px;padding:9px 14px;background:var(--panel2);border-bottom:1px solid var(--line);}
  .dots{display:flex;gap:6px;}
  .dot{width:10px;height:10px;border-radius:50%;background:#333;}
  .dot.a{background:var(--orange);}
  .tbar .path{color:var(--dim);font-size:12px;letter-spacing:.02em;}
  .tbar .path b{color:var(--fg);font-weight:600;}
  .tbar .tag{margin-left:auto;font-size:10.5px;letter-spacing:.12em;text-transform:uppercase;color:var(--orange);border:1px solid var(--orange-dim);padding:2px 7px;border-radius:3px;}

  .wrap{display:grid;grid-template-columns:206px 1fr;min-height:430px;}

  /* sidebar */
  .side{background:var(--panel);border-right:1px solid var(--line);padding:12px 10px;}
  .side h4{margin:0 0 8px 4px;font-size:10px;letter-spacing:.16em;text-transform:uppercase;color:var(--dim);font-weight:600;}
  .cmd{display:block;width:100%;text-align:left;background:transparent;border:1px solid transparent;color:var(--fg);
       font-family:var(--mono);font-size:12.5px;padding:6px 8px;border-radius:4px;cursor:pointer;margin-bottom:2px;}
  .cmd span{color:var(--orange);margin-right:6px;}
  .cmd:hover{background:var(--panel2);border-color:var(--line);}
  .cmd[aria-current="true"]{background:#1d1207;border-color:var(--orange-dim);}
  .cmd:focus-visible{outline:2px solid var(--orange);outline-offset:1px;}
  .side .hintbox{margin-top:12px;padding:8px;border:1px dashed #2e2e2e;border-radius:4px;color:var(--dim);font-size:11px;line-height:1.5;}

  /* output */
  .main{padding:14px 16px 12px;overflow:auto;}
  .line{white-space:pre-wrap;word-break:break-word;}
  .c-o{color:var(--orange);} .c-d{color:var(--dim);} .c-w{color:var(--fg);}
  .cmdline{margin:0 0 10px;color:var(--dim);}
  .cmdline b{color:var(--orange2);font-weight:600;}
  .cur{display:inline-block;width:7px;height:14px;background:var(--orange);vertical-align:-2px;animation:bl 1s steps(1) infinite;}
  @keyframes bl{50%{opacity:0;}}

  h5.sec{margin:14px 0 6px;font-size:10.5px;letter-spacing:.14em;text-transform:uppercase;color:var(--orange);font-weight:600;}
  h5.sec:first-child{margin-top:0;}
  .note{color:var(--dim);font-size:11.5px;margin:8px 0 0;line-height:1.5;}

  /* key-value rows */
  .kv{display:grid;grid-template-columns:minmax(120px,190px) 1fr;gap:2px 14px;}
  .kv dt{color:var(--dim);}
  .kv dd{margin:0;color:var(--fg);}
  .kv dd em{font-style:normal;color:var(--orange2);}

  /* bars */
  .bar{margin:0 0 7px;}
  .bar .top{display:flex;justify-content:space-between;gap:10px;font-size:12.5px;}
  .bar .top .val{color:var(--orange2);font-variant-numeric:tabular-nums;}
  .track{height:9px;background:#1c1c1c;border:1px solid var(--line);border-radius:2px;overflow:hidden;margin-top:3px;}
  .fill{height:100%;width:0;background:repeating-linear-gradient(90deg,var(--orange) 0 6px,#e06a10 6px 8px);transition:width .7s cubic-bezier(.2,.8,.2,1);}
  .fill.mute{background:repeating-linear-gradient(90deg,#4a4a4a 0 6px,#3a3a3a 6px 8px);}
  button.bar{display:block;width:100%;text-align:left;background:transparent;border:0;padding:2px 0;cursor:pointer;font-family:var(--mono);color:var(--fg);}
  button.bar:hover .top .lbl{color:var(--orange2);}
  button.bar:focus-visible{outline:2px solid var(--orange);outline-offset:2px;}
  .readout{margin-top:8px;border-left:2px solid var(--orange);padding:6px 10px;background:var(--panel);color:var(--dim);font-size:12px;min-height:32px;}
  .readout b{color:var(--fg);font-weight:600;}

  .toolbar{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:10px;}
  .chip{background:var(--panel2);border:1px solid var(--line);color:var(--dim);font-family:var(--mono);font-size:11.5px;
        padding:3px 9px;border-radius:3px;cursor:pointer;}
  .chip[aria-pressed="true"]{border-color:var(--orange);color:var(--orange);background:#1d1207;}
  .chip:focus-visible{outline:2px solid var(--orange);outline-offset:1px;}

  /* status table */
  table.st{width:100%;border-collapse:collapse;font-size:12.5px;}
  table.st th,table.st td{text-align:left;padding:6px 8px;border-bottom:1px solid var(--line);vertical-align:top;}
  table.st th{color:var(--dim);font-weight:600;font-size:10.5px;letter-spacing:.1em;text-transform:uppercase;}
  .pill{display:inline-block;padding:1px 7px;border-radius:3px;font-size:11px;border:1px solid;}
  .pill.live{color:#0a0a0a;background:var(--orange);border-color:var(--orange);font-weight:600;}
  .pill.soon{color:var(--orange2);border-color:var(--orange-dim);background:#1d1207;}
  .pill.later{color:var(--dim);border-color:var(--line);background:var(--panel2);}

  /* slider */
  .slab{display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin:6px 0 2px;}
  input[type=range]{-webkit-appearance:none;appearance:none;flex:1 1 200px;height:22px;background:transparent;cursor:pointer;}
  input[type=range]::-webkit-slider-runnable-track{height:6px;background:#1c1c1c;border:1px solid var(--line);border-radius:3px;}
  input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;width:16px;height:16px;margin-top:-6px;border-radius:2px;background:var(--orange);border:0;}
  input[type=range]::-moz-range-track{height:6px;background:#1c1c1c;border:1px solid var(--line);border-radius:3px;}
  input[type=range]::-moz-range-thumb{width:16px;height:16px;border-radius:2px;background:var(--orange);border:0;}
  input[type=range]:focus-visible{outline:2px solid var(--orange);outline-offset:3px;}
  .bignum{font-size:26px;color:var(--orange);font-variant-numeric:tabular-nums;letter-spacing:-.01em;}
  .bignum small{font-size:12px;color:var(--dim);letter-spacing:.08em;text-transform:uppercase;margin-left:6px;}

  /* prompt */
  .promptrow{display:flex;align-items:center;gap:8px;border-top:1px solid var(--line);padding:9px 16px;background:var(--panel);}
  .promptrow .ps1{color:var(--orange);}
  .promptrow input[type=text]{flex:1;background:transparent;border:0;color:var(--fg);font-family:var(--mono);font-size:13px;padding:2px 0;}
  .promptrow input[type=text]:focus{outline:none;}
  .promptrow .kbd{color:var(--dim);font-size:11px;border:1px solid var(--line);border-radius:3px;padding:1px 5px;}

  footer.ft{display:flex;justify-content:space-between;gap:10px;flex-wrap:wrap;
            padding:8px 16px;background:var(--panel2);border-top:1px solid var(--line);color:var(--dim);font-size:11px;}
  footer.ft a{color:var(--orange);text-decoration:none;}
  footer.ft a:hover{text-decoration:underline;}
  footer.ft .brand{color:var(--orange);font-weight:600;}

  @media (max-width:640px){
    #mtp-flux3-term{font-size:12.5px;}
    .wrap{grid-template-columns:1fr;min-height:0;}
    .side{border-right:0;border-bottom:1px solid var(--line);display:flex;flex-wrap:wrap;gap:4px;padding:10px;}
    .side h4{width:100%;}
    .cmd{width:auto;flex:1 1 44%;font-size:11.5px;padding:5px 6px;}
    .side .hintbox{display:none;}
    .main{padding:12px;}
    .kv{grid-template-columns:1fr;gap:0 0;}
    .kv dt{margin-top:6px;}
    table.st th:nth-child(3),table.st td:nth-child(3){display:none;}
  }
  @media (prefers-reduced-motion:reduce){
    .fill{transition:none;} .cur{animation:none;}
  }
</style>
</head>
<body>
<div id="mtp-flux3-term">

  <div class="tbar">
    <div class="dots"><i class="dot a"></i><i class="dot"></i><i class="dot"></i></div>
    <div class="path">bfl@<b>flux-3</b>:~/real-world-models</div>
    <div class="tag">Early Access</div>
  </div>

  <div class="wrap">
    <nav class="side" aria-label="Commands">
      <h4>Commands</h4>
      <button class="cmd" data-k="spec"><span>&gt;</span>spec</button>
      <button class="cmd" data-k="bench"><span>&gt;</span>bench</button>
      <button class="cmd" data-k="compute"><span>&gt;</span>compute</button>
      <button class="cmd" data-k="action"><span>&gt;</span>action</button>
      <button class="cmd" data-k="mimic"><span>&gt;</span>mimic</button>
      <button class="cmd" data-k="rollout"><span>&gt;</span>rollout</button>
      <button class="cmd" data-k="selfflow"><span>&gt;</span>selfflow</button>
      <div class="hintbox">Type a command below, or press <b>&uarr;</b> / <b>&darr;</b> in the prompt to cycle history. Every figure is sourced from Black Forest Labs or mimic robotics.</div>
    </nav>

    <main class="main" id="out" aria-live="polite"></main>
  </div>

  <div class="promptrow">
    <span class="ps1" aria-hidden="true">flux3 $</span>
    <label for="cmdin" style="position:absolute;left:-9999px;">Enter a command</label>
    <input id="cmdin" type="text" autocomplete="off" spellcheck="false" placeholder="try: bench --sort  |  mimic  |  help">
    <span class="kbd">enter</span>
  </div>

  <footer class="ft">
    <span>Sources: bfl.ai/blog/flux-3 &middot; bfl.ai/blog/flux-3-mimic &middot; mimicrobotics.com &middot; figures dated 23 Jul 2026</span>
    <span>Built by <span class="brand">Marktechpost</span></span>
  </footer>
</div>

<script>
(function(){
  var out = document.getElementById('out');
  var input = document.getElementById('cmdin');
  var hist = [], hp = -1;
  var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  var BENCH = [
    {n:'Luma Ray 3.2',        v:93, note:'Highest reported preference margin in BFL&rsquo;s early evaluation.'},
    {n:'Runway Gen-4.5',      v:77, note:'Second-highest margin reported by BFL.'},
    {n:'Grok Imagine Video',  v:69, note:'BFL reports &ldquo;up to&rdquo; 69% &mdash; the phrasing marks an upper bound, not a mean.'},
    {n:'Kling v3 Pro',        v:60, note:'Modest but consistent preference margin.'},
    {n:'Happy Horse v1',      v:59, note:'Near the middle of the reported field.'},
    {n:'Happy Horse 1.1',     v:57, note:'Near the middle of the reported field.'},
    {n:'Seedance 2.0',        v:52, note:'Statistically close to a coin flip.'},
    {n:'Gemini Omni Flash',   v:52, note:'Statistically close to a coin flip.'}
  ];

  function esc(s){return String(s);}

  function bars(list, mode){
    var arr = list.slice();
    if(mode === 'sort') arr.sort(function(a,b){return a.v - b.v;});
    var h = '';
    for(var i=0;i<arr.length;i++){
      var b = arr[i];
      h += '<button class="bar" type="button" data-note="'+b.note+'" data-n="'+b.n+'" data-v="'+b.v+'">'
        +   '<span class="top"><span class="lbl">'+b.n+'</span><span class="val">'+b.v+'%</span></span>'
        +   '<span class="track"><span class="fill'+(b.v<55?' mute':'')+'" data-w="'+b.v+'"></span></span>'
        + '</button>';
    }
    return h;
  }

  var VIEWS = {
    help: {cmd:'help', body:function(){
      return '<h5 class="sec">Available commands</h5>'
        + '<dl class="kv">'
        + '<dt>spec</dt><dd>Model shape, modalities and video limits</dd>'
        + '<dt>bench</dt><dd>Preliminary human-preference win rates</dd>'
        + '<dt>compute</dt><dd>Where the training budget actually goes</dd>'
        + '<dt>action</dt><dd>Cost of adding action prediction to the backbone</dd>'
        + '<dt>mimic</dt><dd>FLUX-mimic latency and success rates</dd>'
        + '<dt>rollout</dt><dd>What ships when, and to whom</dd>'
        + '<dt>selfflow</dt><dd>The training method underneath FLUX 3</dd>'
        + '</dl>';
    }},

    spec: {cmd:'flux3 --spec', body:function(){
      return '<h5 class="sec">One backbone, four output types</h5>'
        + '<dl class="kv">'
        + '<dt>Model class</dt><dd>Multimodal flow matching foundation model</dd>'
        + '<dt>Joint training modalities</dt><dd><em>image + video + audio</em>, one architecture</dd>'
        + '<dt>Extended output</dt><dd>Action prediction (robot state)</dd>'
        + '<dt>Max video length</dt><dd><em>20 seconds</em> in a single generation</dd>'
        + '<dt>Audio</dt><dd>Native, generated jointly with video</dd>'
        + '<dt>Video modes</dt><dd>text-to-video, image-to-video, video-to-video, keyframe-to-video, video+audio continuation</dd>'
        + '<dt>Other capabilities</dt><dd>multilingual dialogue, agentic multi-shot chaining, typography and animated design</dd>'
        + '<dt>Announced</dt><dd>23 July 2026, Black Forest Labs</dd>'
        + '<dt>Pricing</dt><dd class="c-d">not published</dd>'
        + '</dl>'
        + '<p class="note">BFL describes each modality as a lossy projection of one underlying reality. Training on all of them at once lets their mutual constraints supervise each other.</p>';
    }},

    bench: {cmd:'flux3 bench --video', body:function(state){
      var mode = (state && state.mode) || 'raw';
      return '<h5 class="sec">Human preference win rate &mdash; FLUX 3 vs. baseline</h5>'
        + '<div class="toolbar">'
        + '<button class="chip" type="button" data-mode="raw" aria-pressed="'+(mode==='raw')+'">as published</button>'
        + '<button class="chip" type="button" data-mode="sort" aria-pressed="'+(mode==='sort')+'">sort ascending</button>'
        + '</div>'
        + '<div id="barwrap">'+bars(BENCH, mode)+'</div>'
        + '<div class="readout" id="readout">Select a bar to read the caveat attached to it. Bars at or below 55% are greyed: those are near coin flips.</div>'
        + '<p class="note">Setup: 10-second text-to-video clips at 720p with audio. BFL labels these results preliminary and notes the model and evaluation harness were still in development. Win rates are vendor-run and not independently reproduced.</p>';
    }},

    compute: {cmd:'flux3 stack --compute', body:function(){
      return '<h5 class="sec">Where the training budget goes</h5>'
        + '<div class="bar"><span class="top"><span class="lbl">Video prediction, share of total compute cost</span><span class="val">&gt;95%</span></span>'
        + '<span class="track"><span class="fill" data-w="95"></span></span></div>'
        + '<div class="bar"><span class="top"><span class="lbl">Audio, share of tokens in a 720p video+audio clip</span><span class="val">&lt;0.5%</span></span>'
        + '<span class="track"><span class="fill mute" data-w="2"></span></span></div>'
        + '<h5 class="sec">Training corpus</h5>'
        + '<dl class="kv">'
        + '<dt>General video</dt><dd><em>tens of millions of hours</em></dd>'
        + '<dt>Manipulation-focused video</dt><dd><em>hundreds of thousands of hours</em>, human and robot</dd>'
        + '</dl>'
        + '<p class="note">BFL&rsquo;s argument: video is the expensive modality because rendering the world correctly forces the model to learn contact, motion, weight and cause. Audio and actions are low-dimensional signals that attach to a world model already paid for.</p>';
    }},

    action: {cmd:'flux3 action --transfer', body:function(){
      return '<h5 class="sec">Adding action prediction to a video backbone</h5>'
        + '<div class="slab">'
        + '<div><span class="bignum" id="qval">90</span><small id="qlab">% of prior video quality</small></div>'
        + '</div>'
        + '<div class="slab"><input type="range" id="steps" min="0" max="3500" step="50" value="0" aria-label="Training steps after action prediction was added">'
        + '<span class="c-d" style="min-width:112px;">step <b id="stepv" style="color:var(--orange2)">0</b> / 3500</span></div>'
        + '<div class="readout" id="areadout">At the moment action prediction enters the curriculum, human ratings on text-to-video and image-to-video fall by up to <b>10%</b>.</div>'
        + '<p class="note">Two reported endpoints: an initial drop of up to 10% in human ratings, and full recovery of prior video quality after 3,500 steps. The slider interpolates linearly between those two points &mdash; it is an illustration of the reported result, not BFL&rsquo;s published curve.</p>';
    }},

    mimic: {cmd:'flux3 mimic --deploy', body:function(){
      return '<h5 class="sec">Latency budget on-robot</h5>'
        + '<dl class="kv">'
        + '<dt>Backbone, input &rarr; world representation</dt><dd><em>&lt; 80 ms</em> on a single NVIDIA RTX 5090</dd>'
        + '<dt>Full self-contained robot system</dt><dd><em>101 ms</em> reaction time</dd>'
        + '<dt>Inference trick</dt><dd>action decoder reads latent features, so no video rollout is generated at inference</dd>'
        + '</dl>'
        + '<h5 class="sec">Soft-body kitting, autonomous success rate</h5>'
        + '<div class="bar"><span class="top"><span class="lbl">FLUX-mimic preview, no task-specific fine-tuning</span><span class="val">95%</span></span>'
        + '<span class="track"><span class="fill" data-w="95"></span></span></div>'
        + '<div class="bar"><span class="top"><span class="lbl">Flow matching baseline, heavily post-trained on this task</span><span class="val">70%</span></span>'
        + '<span class="track"><span class="fill mute" data-w="70"></span></span></div>'
        + '<div class="bar"><span class="top"><span class="lbl">Adapted pi0.5 trained on mimic&rsquo;s data mix</span><span class="val">55%</span></span>'
        + '<span class="track"><span class="fill mute" data-w="55"></span></span></div>'
        + '<p class="note">Numbers reported by mimic robotics for a preview build on their own robot. With the backbone frozen, mimic reports FLUX-mimic stayed competitive while the pi0.5 action decoder dropped to 0%. Deployed tasks include kitting into structured trays, inserting electronic control units, assembly, and handling seals and cables. Audi is testing and deploying the system on production lines.</p>';
    }},

    rollout: {cmd:'flux3 rollout --status', body:function(){
      return '<h5 class="sec">Launch plan</h5>'
        + '<table class="st"><thead><tr><th>Capability</th><th>Access</th><th>Channel</th></tr></thead><tbody>'
        + '<tr><td>FLUX 3 Video</td><td><span class="pill live">early access</span></td><td>API + private weights</td></tr>'
        + '<tr><td>FLUX-mimic / FLUX 3 Action</td><td><span class="pill live">early access</span></td><td>selected research and commercial partners</td></tr>'
        + '<tr><td>FLUX 3 Image</td><td><span class="pill soon">weeks</span></td><td>API + private weights</td></tr>'
        + '<tr><td>FLUX 3 Dev</td><td><span class="pill later">later</span></td><td>open-weight multimodal backbone</td></tr>'
        + '</tbody></table>'
        + '<p class="note">Each tier goes through an early access phase before wider release. BFL has said it will publish more technical detail on the underlying approach. No pricing has been announced for any tier.</p>';
    }},

    selfflow: {cmd:'flux3 selfflow --paper', body:function(){
      return '<h5 class="sec">The method underneath</h5>'
        + '<dl class="kv">'
        + '<dt>Paper</dt><dd>Self-Supervised Flow Matching for Scalable Multi-Modal Synthesis (ICML 2026)</dd>'
        + '<dt>arXiv</dt><dd>2603.06507</dd>'
        + '<dt>Authors</dt><dd>Chefer, Esser, Lorenz, Podell, Raja, Tong, Torralba, Rombach</dd>'
        + '<dt>Idea</dt><dd>flow matching objective + self-supervised feature reconstruction, in one framework</dd>'
        + '<dt>Why it matters here</dt><dd>generation quality and representation quality improve together, so actions are decodable from the backbone</dd>'
        + '<dt>Reference code</dt><dd>Apache-2.0, SiT-XL/2 with per-token timestep conditioning, 25% per-token mask ratio, EMA teacher at layer 20 and student at layer 8</dd>'
        + '</dl>'
        + '<p class="note">The released checkpoint and configuration are the ImageNet 256&times;256 research model, not FLUX 3 itself. BFL says FLUX 3 is the same method with compute and data scaled up. In Self-Flow experiments, action prediction reached a given success rate in half the training steps of a video model without Self-Flow.</p>';
    }}
  };

  var benchState = {mode:'raw'};

  function animateFills(){
    var f = out.querySelectorAll('.fill');
    requestAnimationFrame(function(){
      for(var i=0;i<f.length;i++){ f[i].style.width = f[i].getAttribute('data-w') + '%'; }
      setTimeout(sendHeight, 780);
    });
  }

  function render(key, opts){
    var v = VIEWS[key];
    if(!v){
      out.innerHTML = '<p class="line c-d">command not found: <span class="c-o">'+key+'</span> &mdash; type <span class="c-o">help</span> for the list.</p>';
      sendHeight(); return;
    }
    var head = '<p class="cmdline">flux3 $ <b></b><i class="cur"></i></p>';
    out.innerHTML = head + '<div id="vbody" style="opacity:0"></div>';
    var target = out.querySelector('.cmdline b');
    var bodyEl = out.querySelector('#vbody');
    var txt = v.cmd, i = 0;

    function finish(){
      target.textContent = txt;
      bodyEl.innerHTML = v.body(opts || benchState);
      bodyEl.style.opacity = '1';
      bind(key);
      animateFills();
      sendHeight();
    }
    if(reduce){ finish(); return; }
    (function type(){
      target.textContent = txt.slice(0, i);
      i++;
      if(i <= txt.length){ setTimeout(type, 16); } else { finish(); }
    })();

    var btns = document.querySelectorAll('.cmd');
    for(var b=0;b<btns.length;b++){
      btns[b].setAttribute('aria-current', btns[b].getAttribute('data-k') === key ? 'true' : 'false');
    }
  }

  function bind(key){
    if(key === 'bench'){
      var chips = out.querySelectorAll('.chip');
      for(var c=0;c<chips.length;c++){
        chips[c].addEventListener('click', function(){
          benchState.mode = this.getAttribute('data-mode');
          render('bench', benchState);
        });
      }
      var bs = out.querySelectorAll('button.bar');
      var ro = out.querySelector('#readout');
      for(var j=0;j<bs.length;j++){
        bs[j].addEventListener('click', function(){
          ro.innerHTML = '<b>FLUX 3 vs. ' + this.getAttribute('data-n') + '</b> &mdash; preferred in '
            + this.getAttribute('data-v') + '% of comparisons. ' + this.getAttribute('data-note');
          sendHeight();
        });
      }
    }
    if(key === 'action'){
      var s = out.querySelector('#steps');
      var qv = out.querySelector('#qval');
      var sv = out.querySelector('#stepv');
      var ar = out.querySelector('#areadout');
      s.addEventListener('input', function(){
        var st = parseInt(this.value, 10);
        var q = 90 + (st / 3500) * 10;
        qv.textContent = q.toFixed(1);
        sv.textContent = st.toLocaleString('en-US');
        if(st === 0){
          ar.innerHTML = 'At the moment action prediction enters the curriculum, human ratings on text-to-video and image-to-video fall by up to <b>10%</b>.';
        } else if(st >= 3500){
          ar.innerHTML = 'After <b>3,500 steps</b> the model has regained its full previous video quality &mdash; and now also predicts actions. BFL&rsquo;s reading: the same backbone carries both.';
        } else {
          ar.innerHTML = 'Recovery phase. The model is learning the structure of the action space and aligning its world representation to it.';
        }
      });
    }
  }

  var buttons = document.querySelectorAll('.cmd');
  for(var i=0;i<buttons.length;i++){
    buttons[i].addEventListener('click', function(){ render(this.getAttribute('data-k')); });
  }

  input.addEventListener('keydown', function(e){
    if(e.key === 'Enter'){
      var raw = input.value.trim().toLowerCase();
      if(!raw) return;
      hist.push(raw); hp = hist.length;
      var word = raw.replace(/^flux3\s+/, '').split(/[\s-]+/)[0];
      if(word === 'clear'){ out.innerHTML = '<p class="line c-d">cleared. type <span class="c-o">help</span> for the command list.</p>'; input.value=''; sendHeight(); return; }
      if(raw.indexOf('--sort') > -1){ benchState.mode = 'sort'; }
      render(VIEWS[word] ? word : (word || 'help'), benchState);
      input.value = '';
    } else if(e.key === 'ArrowUp'){
      if(hp > 0){ hp--; input.value = hist[hp]; } e.preventDefault();
    } else if(e.key === 'ArrowDown'){
      if(hp < hist.length - 1){ hp++; input.value = hist[hp]; } else { hp = hist.length; input.value=''; } e.preventDefault();
    }
  });

  function sendHeight(){
    var el = document.getElementById('mtp-flux3-term');
    if(!el) return;
    var h = el.offsetHeight + 40;
    if(window.parent !== window){
      window.parent.postMessage({mtpFlux3Height: h}, '*');
    }
  }

  window.addEventListener('resize', sendHeight);
  render('spec');
  setTimeout(sendHeight, 120);
  setTimeout(sendHeight, 900);
})();
</script>
</body>
</html>




<p class="wp-block-paragraph" id="h-"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>FLUX 3 is one flow matching backbone trained jointly on image, video and audio.</li>



<li>FLUX 3 Video generates up to 20 seconds with native audio in a single generation.</li>



<li>Video prediction consumes over 95% of the training compute; audio is under 0.5% of tokens.</li>



<li>The same backbone drives FLUX-mimic, a robot policy running under 80 ms on one RTX 5090.</li>



<li>Access is gated: Video and Action are in early access, Image follows, open weights come last.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"><em>Check out the <a href="https://bfl.ai/blog/flux-3">FLUX 3 announcement</a>, the <a href="https://bfl.ai/blog/flux-3-mimic">FLUX 3 x mimic technical post</a> and the <a href="https://arxiv.org/abs/2603.06507">Self-Flow paper</a>. All credit for this research goes to the researchers of this project.</em></p>
<p>The post <a href="https://www.marktechpost.com/2026/07/26/black-forest-labs-releases-flux-3-a-multimodal-flow-model-for-image-video-audio-and-robot-action-prediction/">Black Forest Labs Releases FLUX 3: A Multimodal Flow Model for Image, Video, Audio and Robot Action Prediction</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81375</post-id>
	</item>
	</channel>
</rss>
