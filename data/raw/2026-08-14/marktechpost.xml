<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	>

<channel>
	<title>MarkTechPost</title>
	<atom:link href="https://www.marktechpost.com/feed/" rel="self" type="application/rss+xml" />
	<link>https://www.marktechpost.com/</link>
	<description>An Artificial Intelligence News Platform</description>
	<lastBuildDate>Thu, 13 Aug 2026 17:47:43 +0000</lastBuildDate>
	<language>en-US</language>
	<sy:updatePeriod>
	hourly	</sy:updatePeriod>
	<sy:updateFrequency>
	1	</sy:updateFrequency>
	<generator>https://wordpress.org/?v=7.0.4</generator>

<image>
	<url>https://www.marktechpost.com/wp-content/uploads/2022/04/cropped-Favicon-512-x-512-1-1-32x32.png</url>
	<title>MarkTechPost</title>
	<link>https://www.marktechpost.com/</link>
	<width>32</width>
	<height>32</height>
</image> 
<site xmlns="com-wordpress:feed-additions:1">127842392</site>	<item>
		<title>Google AI Just Released Gemini 3.7 Flash: A Coding and Agent Model at $0.75/1M Input Tokens</title>
		<link>https://www.marktechpost.com/2026/08/13/google-ai-just-released-gemini-3-7-flash/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Thu, 13 Aug 2026 17:47:41 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Uncategorized]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81710</guid>

					<description><![CDATA[<p>Google has released Gemini 3.7 Flash, a refinement of Gemini 3.6 Flash with algorithmic improvements to its reasoning core. It handles text, images, audio, and video across a 1M-token context window with 64K-token output, and supports customizable thinking configurations. Coding results move notably: 43.6% on FrontierCode 1.1 Main versus 34.4%, 65.3% on DeepSWE v1.1, and 1588 Elo on WebDev Arena. Document and workflow evals gain more — GDP.pdf goes from 22.0% to 34.0%, AutomationBench from 17.0% to 30.4%. GPT-5.6 Terra still leads on terminal and computer-use agents. The model is API and enterprise only, with no open weights, at an introductory $0.75/$3.75 per 1M tokens through December 31, 2026.</p>
<p>The post <a href="https://www.marktechpost.com/2026/08/13/google-ai-just-released-gemini-3-7-flash/">Google AI Just Released Gemini 3.7 Flash: A Coding and Agent Model at $0.75/1M Input Tokens</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Google has released <a href="https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/">Gemini 3.7 Flash</a>, the newest model in its Flash tier, three weeks after Gemini 3.6 Flash. The <a href="https://deepmind.google/models/model-cards/gemini-3-7-flash">model card</a> describes it as a refinement of 3.6 Flash with algorithmic improvements to the core reasoning foundation — not a new pretraining run. It accepts text, images, audio, and video across a 1M-token context window, returns up to 64K output tokens, and supports customizable thinking configurations that trade quality against cost and latency. The knowledge cutoff stays at March 2026. The gains concentrate in three places: software engineering, document-heavy knowledge work, and web development. The sharper argument is price. Gemini 3.7 Flash ships at $0.75 per 1M input tokens and $3.75 per 1M output tokens — half the original 3.6 Flash list rate, and roughly a third the blended cost of Claude Sonnet 5 or GPT-5.6 Terra.</p>



<h2 id="h-is-it-deployable" class="wp-block-heading"><strong>Is it Deployable?</strong></h2>



<p class="wp-block-paragraph"><strong>Yes, API and enterprise only.</strong> There are no open weights. Access runs through hosted surfaces: the <a href="https://ai.google.dev/gemini-api/docs/latest-model">Gemini API</a> and <a href="https://ai.dev/prompts/new_chat?model=gemini-3.7-flash">Google AI Studio</a>, <a href="https://antigravity.google/">Google Antigravity</a>, <a href="https://developer.android.com/studio">Android Studio</a>, the <a href="https://console.cloud.google.com/agent-platform/publishers/google/model-garden/gemini-3.7-flash">Gemini Enterprise Agent Platform</a>, and the <a href="https://cloud.google.com/gemini-enterprise">Gemini Enterprise</a> app. Consumers reach it through Gemini Spark on Google AI Pro and Ultra plans.</p>



<ul class="wp-block-list">
<li><strong>Company fit</strong>: Startups and mid-market teams gain the most, because the introductory price makes always-on agents affordable without a Pro-tier budget. Regulated enterprises get a governed path through Gemini Enterprise. Teams with data-residency or air-gap requirements are excluded — there is nothing to self-host.</li>



<li><strong>Industries</strong>: Google&#8217;s own eval set points at legal, financial services, biosciences, and enterprise operations. The Harvey LAB-AA, GDP.pdf, and AutomationBench results are the tells.</li>



<li><strong>Applications</strong>: Long-running coding agents, document-heavy back-office automation, UI generation from screenshots or design systems, and PDF-to-structured-data pipelines.</li>
</ul>



<h2 id="h-the-benchmark-picture" class="wp-block-heading"><strong>The Benchmark Picture</strong></h2>



<p class="wp-block-paragraph">On <a href="https://cognition.com/frontiercode">FrontierCode 1.1</a> Main, which measures production code quality, Gemini 3.7 Flash scores 43.6% against 34.4% for 3.6 Flash. On <a href="https://deepswe.datacurve.ai/">DeepSWE v1.1</a>, a long-horizon software engineering eval, it reaches 65.3%. On <a href="https://arena.ai/leaderboard/code/webdev">WebDev Arena</a> it posts an Elo of 1588 versus 1538, the top score in Google&#8217;s comparison table.</p>



<p class="wp-block-paragraph">Document and workflow results move further. GDP.pdf, an expert PDF comprehension eval, goes from 22.0% to 34.0%. AutomationBench, a private enterprise workflow set, goes from 17.0% to 30.4% — ahead of both Claude Sonnet 5 at 10.7% and GPT-5.6 Terra at 23.6%. Long-context retrieval on GDM-MRCR v2 at 128k reaches 97.0%.</p>



<p class="wp-block-paragraph">GPT-5.6 Terra is ahead on DeepSWE (69.6%), Terminal-bench 2.1 (87.4%), Terminal-bench 3.0 (20.8%), and OSWorld-2.0 (50.2%). On GDPval-AA v2 knowledge work, 3.7 Flash scores 1525 Elo against 1598 for Sonnet 5 and 1628 for Muse Spark 1.2. CharXiv Reasoning is a regression: 84.5% without tools, down from 85.2% for 3.6 Flash. On the Artificial Analysis Intelligence Index, 3.7 Flash scores 56, against 57 for both GPT-5.6 Terra and Muse Spark 1.2.</p>



<div id="mtp-g37-x7k2" style="margin:0!important;padding:0!important;background:transparent!important">
<iframe id="mtp-g37-x7k2-f" title="Gemini 3.7 Flash interactive explainer" loading="lazy" style="width:100%!important;height:600px;border:0!important;display:block!important;background:transparent!important;overflow:hidden" scrolling="no" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;style&gt;
*{box-sizing:border-box;margin:0;padding:0}
body{background:#F0F4F9;color:#1F1F1F;font-family:'Google Sans',Roboto,-apple-system,'Segoe UI',Arial,sans-serif;padding:0}
#app{max-width:900px;margin:0 auto;padding:16px}
.hd{background:#fff;border:1px solid #DDE3EA;border-radius:16px;padding:16px 18px;margin-bottom:12px;position:relative;overflow:hidden}
.hd:before{content:&quot;&quot;;position:absolute;left:0;top:0;height:4px;width:100%;background:linear-gradient(90deg,#4796E3,#9177C7,#D56C82)}
.eyebrow{font-size:10px;letter-spacing:.13em;text-transform:uppercase;color:#5F6368;font-weight:700}
h1{font-size:clamp(18px,3.6vw,25px);line-height:1.2;margin:6px 0 5px;font-weight:600}
.grad{background:linear-gradient(90deg,#4796E3,#9177C7,#D56C82);-webkit-background-clip:text;background-clip:text;color:transparent}
.sub{font-size:12.5px;color:#5F6368;line-height:1.5}
.tabs{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:12px}
.tab{flex:1;min-width:88px;background:#fff;border:1px solid #DDE3EA;border-radius:11px;padding:9px 6px;font:600 12px/1.2 inherit;color:#5F6368;cursor:pointer;transition:.18s;text-align:center}
.tab:hover{border-color:#4285F4;color:#1F1F1F}
.tab.on{background:#1F1F1F;border-color:#1F1F1F;color:#fff}
.panel{background:#fff;border:1px solid #DDE3EA;border-radius:16px;padding:16px;display:none;animation:fade .32s ease}
.panel.on{display:block}
@keyframes fade{from{opacity:0;transform:translateY(7px)}to{opacity:1;transform:none}}
h2{font-size:14px;font-weight:600;margin-bottom:3px}
.hint{font-size:11.5px;color:#5F6368;line-height:1.5;margin-bottom:12px}
.chips{display:flex;gap:5px;flex-wrap:wrap;margin-bottom:13px}
.chip{background:#F0F4F9;border:1px solid #DDE3EA;border-radius:20px;padding:6px 11px;font:600 11px/1 inherit;color:#5F6368;cursor:pointer;transition:.16s}
.chip:hover{border-color:#9177C7}
.chip.on{background:#E8F0FE;border-color:#4285F4;color:#1046a3}
.row{margin-bottom:11px}
.rowtop{display:flex;justify-content:space-between;align-items:baseline;gap:8px;margin-bottom:5px}
.nm{font-size:12px;font-weight:600;display:flex;align-items:center;gap:6px}
.dot{width:8px;height:8px;border-radius:50%;flex:none}
.val{font-size:12.5px;font-weight:700;font-variant-numeric:tabular-nums}
.track{height:15px;background:#F0F4F9;border-radius:8px;overflow:hidden}
.fill{height:100%;border-radius:8px;width:0;transition:width .9s cubic-bezier(.22,1,.36,1)}
.delta{font-size:10.5px;font-weight:700;padding:2px 6px;border-radius:6px;background:#E6F4EA;color:#137333}
.note{font-size:11px;color:#5F6368;line-height:1.55;border-top:1px solid #EDF1F6;padding-top:10px;margin-top:12px}
.ctrl{background:#F0F4F9;border-radius:12px;padding:13px;margin-bottom:14px}
.lab{display:flex;justify-content:space-between;font-size:11.5px;font-weight:600;margin-bottom:7px}
.lab span:last-child{color:#1046a3;font-variant-numeric:tabular-nums}
input[type=range]{width:100%;-webkit-appearance:none;appearance:none;height:5px;border-radius:4px;background:#DDE3EA;outline:none}
input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;width:19px;height:19px;border-radius:50%;background:#4285F4;cursor:pointer;border:3px solid #fff;box-shadow:0 1px 4px rgba(0,0,0,.28)}
input[type=range]::-moz-range-thumb{width:19px;height:19px;border-radius:50%;background:#4285F4;cursor:pointer;border:3px solid #fff}
.big{display:flex;gap:9px;flex-wrap:wrap;margin-bottom:14px}
.stat{flex:1;min-width:120px;background:#F0F4F9;border-radius:12px;padding:11px 12px}
.stat b{display:block;font-size:clamp(17px,3.4vw,22px);font-weight:700;font-variant-numeric:tabular-nums;line-height:1.15}
.stat i{display:block;font-style:normal;font-size:10px;letter-spacing:.07em;text-transform:uppercase;color:#5F6368;font-weight:700;margin-bottom:3px}
.stat em{display:block;font-style:normal;font-size:10.5px;color:#5F6368;margin-top:3px}
.quad{position:relative;width:100%;height:290px;background:#F0F4F9;border-radius:12px;margin-bottom:12px;overflow:hidden}
.gl{position:absolute;background:#DDE3EA}
.pt{position:absolute;width:15px;height:15px;border-radius:50%;transform:translate(-50%,-50%) scale(0);transition:transform .55s cubic-bezier(.34,1.56,.64,1);border:2.5px solid #fff;box-shadow:0 1px 4px rgba(0,0,0,.2);cursor:pointer}
.pt.show{transform:translate(-50%,-50%) scale(1)}
.ptl{position:absolute;font-size:10px;font-weight:700;color:#1F1F1F;transform:translate(-50%,0);opacity:0;transition:opacity .5s .35s;background:rgba(255,255,255,.92);padding:1px 4px;border-radius:4px;max-width:44%;text-align:center;line-height:1.25}
.ptl.show{opacity:1}
.axl{position:absolute;font-size:9.5px;font-weight:700;color:#5F6368;text-transform:uppercase;letter-spacing:.07em}
.tl{position:relative;padding-left:26px}
.tl:before{content:&quot;&quot;;position:absolute;left:6px;top:6px;bottom:16px;width:2px;background:linear-gradient(180deg,#4796E3,#9177C7,#D56C82)}
.ev{position:relative;margin-bottom:16px;opacity:0;transform:translateX(-11px);transition:.5s}
.ev.show{opacity:1;transform:none}
.ev:before{content:&quot;&quot;;position:absolute;left:-25px;top:4px;width:12px;height:12px;border-radius:50%;background:#fff;border:2.5px solid #9177C7}
.ev.now:before{background:#4285F4;border-color:#4285F4;box-shadow:0 0 0 4px rgba(66,133,244,.19)}
.evd{font-size:10px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:#5F6368}
.evt{font-size:13px;font-weight:600;margin:2px 0 2px}
.evb{font-size:11.5px;color:#5F6368;line-height:1.5}
table{width:100%;border-collapse:collapse;font-size:11.5px}
th{text-align:left;font-size:9.5px;letter-spacing:.07em;text-transform:uppercase;color:#5F6368;padding:0 6px 7px;border-bottom:1px solid #DDE3EA}
td{padding:8px 6px;border-bottom:1px solid #EDF1F6;font-variant-numeric:tabular-nums}
td:first-child{font-weight:600}
.win{color:#1046a3;font-weight:700}
.wrap{overflow-x:auto}
.ft{text-align:center;font-size:10.5px;color:#5F6368;padding:13px 4px 4px;line-height:1.6}
.ft b{color:#4285F4}
a{color:#1046a3}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;app&quot;&gt;
  &lt;div class=&quot;hd&quot;&gt;
    &lt;div class=&quot;eyebrow&quot;&gt;Interactive Explainer&lt;/div&gt;
    &lt;h1&gt;Gemini 3.7 Flash, &lt;span class=&quot;grad&quot;&gt;by the numbers&lt;/span&gt;&lt;/h1&gt;
    &lt;div class=&quot;sub&quot;&gt;Every figure below comes from the Gemini 3.7 Flash model card (Google DeepMind, August 2026) and Google's launch post. Tap the tabs, drag the sliders.&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;tabs&quot;&gt;
    &lt;button class=&quot;tab on&quot; data-t=&quot;b&quot;&gt;Benchmarks&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-t=&quot;c&quot;&gt;Cost&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-t=&quot;v&quot;&gt;Value map&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-t=&quot;t&quot;&gt;Cadence&lt;/button&gt;
  &lt;/div&gt;

  &lt;!-- BENCHMARKS --&gt;
  &lt;div class=&quot;panel on&quot; id=&quot;p-b&quot;&gt;
    &lt;h2&gt;3.7 Flash vs 3.6 Flash vs the field&lt;/h2&gt;
    &lt;div class=&quot;hint&quot;&gt;Pick a benchmark. Bars animate to the scores reported in the model card. Toggle rivals on and off.&lt;/div&gt;
    &lt;div class=&quot;chips&quot; id=&quot;bchips&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;chips&quot; id=&quot;rchips&quot;&gt;&lt;/div&gt;
    &lt;div id=&quot;bars&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;note&quot; id=&quot;bnote&quot;&gt;&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- COST --&gt;
  &lt;div class=&quot;panel&quot; id=&quot;p-c&quot;&gt;
    &lt;h2&gt;What a month actually costs&lt;/h2&gt;
    &lt;div class=&quot;hint&quot;&gt;Drag your monthly token volume and output share. Prices are list API prices from the model card; Gemini Flash rates are introductory and expire December 31, 2026.&lt;/div&gt;
    &lt;div class=&quot;ctrl&quot;&gt;
      &lt;div class=&quot;lab&quot;&gt;&lt;span&gt;Monthly tokens&lt;/span&gt;&lt;span id=&quot;tv&quot;&gt;100M&lt;/span&gt;&lt;/div&gt;
      &lt;input type=&quot;range&quot; id=&quot;tok&quot; min=&quot;1&quot; max=&quot;100&quot; value=&quot;20&quot;&gt;
    &lt;/div&gt;
    &lt;div class=&quot;ctrl&quot;&gt;
      &lt;div class=&quot;lab&quot;&gt;&lt;span&gt;Share that is output tokens&lt;/span&gt;&lt;span id=&quot;ov&quot;&gt;20%&lt;/span&gt;&lt;/div&gt;
      &lt;input type=&quot;range&quot; id=&quot;out&quot; min=&quot;5&quot; max=&quot;60&quot; value=&quot;20&quot;&gt;
    &lt;/div&gt;
    &lt;div class=&quot;big&quot;&gt;
      &lt;div class=&quot;stat&quot;&gt;&lt;i&gt;Gemini 3.7 Flash today&lt;/i&gt;&lt;b id=&quot;c1&quot;&gt;&amp;mdash;&lt;/b&gt;&lt;em&gt;$0.75 in / $3.75 out per 1M&lt;/em&gt;&lt;/div&gt;
      &lt;div class=&quot;stat&quot;&gt;&lt;i&gt;Same model, Jan 1 2027&lt;/i&gt;&lt;b id=&quot;c2&quot;&gt;&amp;mdash;&lt;/b&gt;&lt;em&gt;$1.50 in / $7.50 out per 1M&lt;/em&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div id=&quot;cbars&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;note&quot;&gt;Introductory pricing expires December 31, 2026. From January 1, 2027 Gemini 3.7 Flash lists at $1.50/1M input and $7.50/1M output. Comparison prices: Claude Sonnet 5 $2.00/$10.00, GPT&amp;#8209;5.6 Terra $2.00/$12.00, Muse Spark 1.2 $1.25/$4.25 per 1M tokens, as tabulated in Google's model card. Cached input, batch and thinking-token effects are excluded.&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- VALUE --&gt;
  &lt;div class=&quot;panel&quot; id=&quot;p-v&quot;&gt;
    &lt;h2&gt;Intelligence against blended price&lt;/h2&gt;
    &lt;div class=&quot;hint&quot;&gt;Horizontal axis: blended cost per 1M tokens at an 80/20 input/output mix. Vertical axis: Artificial Analysis Intelligence Index as tabulated by Google. Up and to the left is the good corner.&lt;/div&gt;
    &lt;div class=&quot;quad&quot; id=&quot;quad&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;wrap&quot;&gt;&lt;table id=&quot;vt&quot;&gt;&lt;/table&gt;&lt;/div&gt;
    &lt;div class=&quot;note&quot;&gt;Blended price is calculated here, not quoted: 0.8 &amp;times; input price + 0.2 &amp;times; output price. Index values and list prices are from the Gemini 3.7 Flash model card. Gemini figures use introductory pricing.&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- TIMELINE --&gt;
  &lt;div class=&quot;panel&quot; id=&quot;p-t&quot;&gt;
    &lt;h2&gt;Three Flash releases in four months&lt;/h2&gt;
    &lt;div class=&quot;hint&quot;&gt;Google is shipping the workhorse tier faster than the Pro tier. 3.7 Flash landed three weeks after 3.6 Flash.&lt;/div&gt;
    &lt;div class=&quot;tl&quot; id=&quot;tl&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;note&quot;&gt;Dates from Google's launch posts. Google states 3.7 Flash arrived &quot;just three weeks after Gemini 3.6 Flash.&quot; The knowledge cutoff for 3.7 Flash is March 2026, unchanged from 3.6 Flash.&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;Sources: &lt;b&gt;Google DeepMind&lt;/b&gt; Gemini 3.7 Flash model card &amp;middot; &lt;b&gt;Google&lt;/b&gt; The Keyword, Aug 13, 2026&lt;br&gt;Built for &lt;b&gt;Marktechpost&lt;/b&gt;&lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
var C={g37:&quot;#4285F4&quot;,g36:&quot;#9AA0A6&quot;,son:&quot;#D56C82&quot;,gpt:&quot;#34A853&quot;,mus:&quot;#FBBC04&quot;};
var RIVALS=[{k:&quot;son&quot;,n:&quot;Claude Sonnet 5&quot;},{k:&quot;gpt&quot;,n:&quot;GPT-5.6 Terra&quot;},{k:&quot;mus&quot;,n:&quot;Muse Spark 1.2&quot;}];
var B=[
 {id:&quot;fc&quot;,n:&quot;FrontierCode 1.1&quot;,d:&quot;Production code quality&quot;,u:&quot;%&quot;,max:100,g37:43.6,g36:34.4,son:42.7,gpt:41.3,mus:null,note:&quot;Highest score in the table. Google's launch post frames this as production-ready code generation.&quot;},
 {id:&quot;ds&quot;,n:&quot;DeepSWE v1.1&quot;,d:&quot;Long-horizon software engineering&quot;,u:&quot;%&quot;,max:100,g37:65.3,g36:48.6,son:53.8,gpt:69.6,mus:54.9,note:&quot;The largest jump over 3.6 Flash of any coding eval here. Google's blog post cites 49.0% for 3.6 Flash; the model card table says 48.6%.&quot;},
 {id:&quot;ca&quot;,n:&quot;Code Arena&quot;,d:&quot;Web development, Elo&quot;,u:&quot;&quot;,max:1700,base:1450,g37:1588,g36:1538,son:1541,gpt:1523,mus:1535,note:&quot;Top of the table on Elo. Google also publishes this as WebDev Arena on arena.ai.&quot;},
 {id:&quot;tb&quot;,n:&quot;Terminal-bench 2.1&quot;,d:&quot;Agentic terminal coding&quot;,u:&quot;%&quot;,max:100,g37:85.8,g36:78.0,son:80.4,gpt:87.4,mus:82.9,note:&quot;Second to GPT-5.6 Terra, but ahead of Claude Sonnet 5 at a quarter of the input price.&quot;},
 {id:&quot;t3&quot;,n:&quot;Terminal-bench 3.0&quot;,d:&quot;General agent capabilities&quot;,u:&quot;%&quot;,max:100,g37:14.9,g36:5.4,son:14.6,gpt:20.8,mus:null,note:&quot;Absolute scores stay low across every model. This is the eval that shows how far general agents still have to go.&quot;},
 {id:&quot;ab&quot;,n:&quot;AutomationBench&quot;,d:&quot;Enterprise workflow automation&quot;,u:&quot;%&quot;,max:100,g37:30.4,g36:17.0,son:10.7,gpt:23.6,mus:null,note:&quot;Private eval set. 3.7 Flash leads the table and nearly doubles 3.6 Flash.&quot;},
 {id:&quot;gp&quot;,n:&quot;GDP.pdf&quot;,d:&quot;Expert PDF comprehension&quot;,u:&quot;%&quot;,max:100,g37:34.0,g36:22.0,son:28.0,gpt:24.7,mus:16.0,note:&quot;Document-heavy work in finance, law and biosciences is where Google is aiming this release.&quot;},
 {id:&quot;hv&quot;,n:&quot;Harvey LAB-AA&quot;,d:&quot;Complex legal workflows&quot;,u:&quot;%&quot;,max:100,g37:90.7,g36:85.1,son:90.1,gpt:85.2,mus:null,note:&quot;Harvey is also quoted as a launch customer in Google's post.&quot;},
 {id:&quot;mr&quot;,n:&quot;GDM-MRCR v2&quot;,d:&quot;Long context, 8-needle at 128k&quot;,u:&quot;%&quot;,max:100,g37:97.0,g36:91.8,son:81.5,gpt:93.5,mus:null,note:&quot;Retrieval accuracy inside the 1M-token context window. Highest in the table.&quot;},
 {id:&quot;os&quot;,n:&quot;OSWorld-2.0&quot;,d:&quot;Agentic computer use&quot;,u:&quot;%&quot;,max:100,g37:47.9,g36:33.8,son:null,gpt:50.2,mus:null,note:&quot;Computer use jumps 14 points over 3.6 Flash but still trails GPT-5.6 Terra.&quot;},
 {id:&quot;hl&quot;,n:&quot;HLE-Verified&quot;,d:&quot;Multidisciplinary expert reasoning&quot;,u:&quot;%&quot;,max:100,g37:53.6,g36:51.2,son:31.0,gpt:51.1,mus:null,note:&quot;Leads the table, though the gain over 3.6 Flash is a modest 2.4 points.&quot;},
 {id:&quot;cx&quot;,n:&quot;CharXiv Reasoning&quot;,d:&quot;Complex charts, no tools&quot;,u:&quot;%&quot;,max:100,g37:84.5,g36:85.2,son:77.0,gpt:85.9,mus:null,note:&quot;A regression. 3.7 Flash scores below 3.6 Flash here, one of the few places it does.&quot;}
];
var PR={g37:{n:&quot;Gemini 3.7 Flash&quot;,i:0.75,o:3.75,x:56},g37f:{n:&quot;Gemini 3.7 Flash (2027)&quot;,i:1.50,o:7.50,x:56},son:{n:&quot;Claude Sonnet 5&quot;,i:2.00,o:10.00,x:55},gpt:{n:&quot;GPT-5.6 Terra&quot;,i:2.00,o:12.00,x:57},mus:{n:&quot;Muse Spark 1.2&quot;,i:1.25,o:4.25,x:57}};
var TL=[
 {d:&quot;May 2026 &amp;middot; Google I/O&quot;,t:&quot;Gemini 3.5 Flash&quot;,b:&quot;Flash becomes the sustained-performance tier for agentic and coding work. Gemini Spark launches as a 24/7 personal agent.&quot;},
 {d:&quot;July 21, 2026&quot;,t:&quot;Gemini 3.6 Flash&quot;,b:&quot;Token efficiency release. Knowledge cutoff moves to March 2026. Ships alongside 3.5 Flash-Lite and 3.5 Flash Cyber.&quot;},
 {d:&quot;August 13, 2026&quot;,t:&quot;Gemini 3.7 Flash&quot;,b:&quot;Algorithmic changes to the reasoning core, customizable thinking configurations, and an introductory price of $0.75/$3.75 per 1M tokens.&quot;,now:1}
];

var cur=&quot;fc&quot;, on={son:true,gpt:true,mus:false};
function $(s){return document.querySelector(s)}
function fmt(v){return v&gt;=1000?&quot;$&quot;+v.toLocaleString(undefined,{maximumFractionDigits:0}):&quot;$&quot;+v.toFixed(2)}

function chips(){
  var h=&quot;&quot;;
  for(var i=0;i&lt;B.length;i++){h+='&lt;button class=&quot;chip'+(B[i].id===cur?&quot; on&quot;:&quot;&quot;)+'&quot; data-b=&quot;'+B[i].id+'&quot;&gt;'+B[i].n+'&lt;/button&gt;'}
  $(&quot;#bchips&quot;).innerHTML=h;
  var r='&lt;span class=&quot;chip&quot; style=&quot;background:none;border:none;cursor:default;padding-left:0&quot;&gt;Compare with:&lt;/span&gt;';
  for(var j=0;j&lt;RIVALS.length;j++){r+='&lt;button class=&quot;chip'+(on[RIVALS[j].k]?&quot; on&quot;:&quot;&quot;)+'&quot; data-r=&quot;'+RIVALS[j].k+'&quot;&gt;'+RIVALS[j].n+'&lt;/button&gt;'}
  $(&quot;#rchips&quot;).innerHTML=r;
}
function bars(){
  var b=null;for(var i=0;i&lt;B.length;i++){if(B[i].id===cur)b=B[i]}
  var base=b.base||0,span=b.max-base;
  var list=[{k:&quot;g37&quot;,n:&quot;Gemini 3.7 Flash&quot;},{k:&quot;g36&quot;,n:&quot;Gemini 3.6 Flash&quot;}];
  for(var j=0;j&lt;RIVALS.length;j++){if(on[RIVALS[j].k])list.push({k:RIVALS[j].k,n:RIVALS[j].n})}
  var h=&quot;&quot;;
  for(var m=0;m&lt;list.length;m++){
    var v=b[list[m].k];
    var pct=(v===null||v===undefined)?0:Math.max(2,((v-base)/span)*100);
    var txt=(v===null||v===undefined)?&quot;not reported&quot;:(b.u===&quot;%&quot;?v.toFixed(1)+&quot;%&quot;:v);
    var dl=&quot;&quot;;
    if(list[m].k===&quot;g37&quot;&amp;&amp;b.g36!==null){var d=(b.g37-b.g36);dl=' &lt;span class=&quot;delta&quot; style=&quot;'+(d&lt;0?&quot;background:#FCE8E6;color:#c5221f&quot;:&quot;&quot;)+'&quot;&gt;'+(d&gt;0?&quot;+&quot;:&quot;&quot;)+(b.u===&quot;%&quot;?d.toFixed(1)+&quot; pts&quot;:d+&quot; Elo&quot;)+' vs 3.6&lt;/span&gt;'}
    h+='&lt;div class=&quot;row&quot;&gt;&lt;div class=&quot;rowtop&quot;&gt;&lt;div class=&quot;nm&quot;&gt;&lt;span class=&quot;dot&quot; style=&quot;background:'+C[list[m].k]+'&quot;&gt;&lt;/span&gt;'+list[m].n+dl+'&lt;/div&gt;&lt;div class=&quot;val&quot;&gt;'+txt+'&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; data-w=&quot;'+pct+'&quot; style=&quot;background:'+C[list[m].k]+'&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;';
  }
  $(&quot;#bars&quot;).innerHTML=h;
  $(&quot;#bnote&quot;).innerHTML=&quot;&lt;b&gt;&quot;+b.n+&quot;&lt;/b&gt; &amp;mdash; &quot;+b.d+&quot;. &quot;+b.note;
  setTimeout(function(){
    var f=document.querySelectorAll(&quot;#bars .fill&quot;);
    for(var i=0;i&lt;f.length;i++){f[i].style.width=f[i].getAttribute(&quot;data-w&quot;)+&quot;%&quot;}
    ping();
  },40);
}
function cost(){
  var tk=+$(&quot;#tok&quot;).value*5, sh=+$(&quot;#out&quot;).value;
  $(&quot;#tv&quot;).textContent=tk&gt;=1000?(tk/1000).toFixed(2).replace(/\.?0+$/,&quot;&quot;)+&quot;B&quot;:tk+&quot;M&quot;;
  $(&quot;#ov&quot;).textContent=sh+&quot;%&quot;;
  var oT=tk*sh/100, iT=tk-oT;
  var keys=[&quot;g37&quot;,&quot;g37f&quot;,&quot;son&quot;,&quot;gpt&quot;,&quot;mus&quot;],vals=[],mx=0;
  for(var i=0;i&lt;keys.length;i++){var p=PR[keys[i]];var c=(iT/1)*p.i+(oT/1)*p.o;vals.push(c);if(c&gt;mx)mx=c}
  $(&quot;#c1&quot;).textContent=fmt(vals[0]);
  $(&quot;#c2&quot;).textContent=fmt(vals[1]);
  var col={g37:&quot;#4285F4&quot;,g37f:&quot;#9177C7&quot;,son:&quot;#D56C82&quot;,gpt:&quot;#34A853&quot;,mus:&quot;#FBBC04&quot;};
  var h=&quot;&quot;;
  for(var j=0;j&lt;keys.length;j++){
    var mult=vals[0]&gt;0?(vals[j]/vals[0]):1;
    h+='&lt;div class=&quot;row&quot;&gt;&lt;div class=&quot;rowtop&quot;&gt;&lt;div class=&quot;nm&quot;&gt;&lt;span class=&quot;dot&quot; style=&quot;background:'+col[keys[j]]+'&quot;&gt;&lt;/span&gt;'+PR[keys[j]].n+(j&gt;0?' &lt;span class=&quot;delta&quot; style=&quot;background:#F0F4F9;color:#5F6368&quot;&gt;'+mult.toFixed(1)+'&amp;times;&lt;/span&gt;':'')+'&lt;/div&gt;&lt;div class=&quot;val&quot;&gt;'+fmt(vals[j])+'&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; style=&quot;width:'+Math.max(2,vals[j]/mx*100)+'%;background:'+col[keys[j]]+'&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;';
  }
  $(&quot;#cbars&quot;).innerHTML=h;
  ping();
}
function quad(){
  var q=$(&quot;#quad&quot;);
  var keys=[&quot;g37&quot;,&quot;son&quot;,&quot;gpt&quot;,&quot;mus&quot;];
  var pts=[];var minX=99,maxX=0,minY=99,maxY=0;
  for(var i=0;i&lt;keys.length;i++){var p=PR[keys[i]];var bp=0.8*p.i+0.2*p.o;pts.push({k:keys[i],n:p.n,x:bp,y:p.x});
    if(bp&lt;minX)minX=bp;if(bp&gt;maxX)maxX=bp;if(p.x&lt;minY)minY=p.x;if(p.x&gt;maxY)maxY=p.x}
  var h='&lt;div class=&quot;gl&quot; style=&quot;left:20%;top:0;bottom:26px;width:1px&quot;&gt;&lt;/div&gt;&lt;div class=&quot;gl&quot; style=&quot;left:55%;top:0;bottom:26px;width:1px&quot;&gt;&lt;/div&gt;&lt;div class=&quot;gl&quot; style=&quot;left:0;right:0;top:32%;height:1px&quot;&gt;&lt;/div&gt;&lt;div class=&quot;gl&quot; style=&quot;left:0;right:0;top:60%;height:1px&quot;&gt;&lt;/div&gt;&lt;div class=&quot;gl&quot; style=&quot;left:0;right:0;bottom:26px;height:1px&quot;&gt;&lt;/div&gt;';
  h+='&lt;div class=&quot;axl&quot; style=&quot;left:10px;top:9px&quot;&gt;More intelligent &amp;uarr;&lt;/div&gt;&lt;div class=&quot;axl&quot; style=&quot;right:10px;bottom:9px&quot;&gt;Costlier &amp;rarr;&lt;/div&gt;&lt;div class=&quot;axl&quot; style=&quot;left:10px;bottom:9px&quot;&gt;Cheaper&lt;/div&gt;';
  var col={g37:&quot;#4285F4&quot;,son:&quot;#D56C82&quot;,gpt:&quot;#34A853&quot;,mus:&quot;#FBBC04&quot;};
  for(var j=0;j&lt;pts.length;j++){
    var px=25+((pts[j].x-minX)/(maxX-minX||1))*52;
    var py=26+(1-((pts[j].y-minY)/(maxY-minY||1)))*44;
    h+='&lt;div class=&quot;pt&quot; style=&quot;left:'+px+'%;top:'+py+'%;background:'+col[pts[j].k]+'&quot;&gt;&lt;/div&gt;';
    h+='&lt;div class=&quot;ptl&quot; style=&quot;left:'+px+'%;top:calc('+py+'% + 12px)&quot;&gt;'+pts[j].n+'&lt;/div&gt;';
  }
  q.innerHTML=h;
  var t=document.querySelectorAll(&quot;#quad .pt&quot;),l=document.querySelectorAll(&quot;#quad .ptl&quot;);
  for(var m=0;m&lt;t.length;m++){(function(i){setTimeout(function(){t[i].classList.add(&quot;show&quot;);l[i].classList.add(&quot;show&quot;)},110*i+60)})(m)}
  var tb=&quot;&lt;tr&gt;&lt;th&gt;Model&lt;/th&gt;&lt;th&gt;Blended $/1M&lt;/th&gt;&lt;th&gt;Intelligence Index&lt;/th&gt;&lt;th&gt;Index per $&lt;/th&gt;&lt;/tr&gt;&quot;;
  var best=0;for(var n=0;n&lt;pts.length;n++){var r=pts[n].y/pts[n].x;if(r&gt;best)best=r}
  for(var o=0;o&lt;pts.length;o++){var rr=pts[o].y/pts[o].x;
    tb+=&quot;&lt;tr&gt;&lt;td&gt;&quot;+pts[o].n+&quot;&lt;/td&gt;&lt;td&gt;$&quot;+pts[o].x.toFixed(2)+&quot;&lt;/td&gt;&lt;td&gt;&quot;+pts[o].y+'&lt;/td&gt;&lt;td class=&quot;'+(rr===best?&quot;win&quot;:&quot;&quot;)+'&quot;&gt;'+rr.toFixed(1)+&quot;&lt;/td&gt;&lt;/tr&gt;&quot;}
  $(&quot;#vt&quot;).innerHTML=tb;
  ping();
}
function tl(){
  var h=&quot;&quot;;
  for(var i=0;i&lt;TL.length;i++){h+='&lt;div class=&quot;ev'+(TL[i].now?&quot; now&quot;:&quot;&quot;)+'&quot;&gt;&lt;div class=&quot;evd&quot;&gt;'+TL[i].d+'&lt;/div&gt;&lt;div class=&quot;evt&quot;&gt;'+TL[i].t+'&lt;/div&gt;&lt;div class=&quot;evb&quot;&gt;'+TL[i].b+'&lt;/div&gt;&lt;/div&gt;'}
  $(&quot;#tl&quot;).innerHTML=h;
  var e=document.querySelectorAll(&quot;#tl .ev&quot;);
  for(var j=0;j&lt;e.length;j++){(function(i){setTimeout(function(){e[i].classList.add(&quot;show&quot;)},150*i+70)})(j)}
  ping();
}
function ping(){
  setTimeout(function(){
    var el=document.getElementById(&quot;app&quot;);
    var hh=el.offsetHeight+40;
    if(window.parent)window.parent.postMessage({mtpFrame:&quot;g37flash&quot;,height:hh},&quot;*&quot;);
  },60);
}
document.addEventListener(&quot;click&quot;,function(e){
  var t=e.target;
  if(t.dataset&amp;&amp;t.dataset.t){
    var tabs=document.querySelectorAll(&quot;.tab&quot;);for(var i=0;i&lt;tabs.length;i++)tabs[i].classList.remove(&quot;on&quot;);
    t.classList.add(&quot;on&quot;);
    var ps=document.querySelectorAll(&quot;.panel&quot;);for(var j=0;j&lt;ps.length;j++)ps[j].classList.remove(&quot;on&quot;);
    document.getElementById(&quot;p-&quot;+t.dataset.t).classList.add(&quot;on&quot;);
    if(t.dataset.t===&quot;b&quot;)bars();
    if(t.dataset.t===&quot;c&quot;)cost();
    if(t.dataset.t===&quot;v&quot;)quad();
    if(t.dataset.t===&quot;t&quot;)tl();
    ping();
  }
  if(t.dataset&amp;&amp;t.dataset.b){cur=t.dataset.b;chips();bars()}
  if(t.dataset&amp;&amp;t.dataset.r){on[t.dataset.r]=!on[t.dataset.r];chips();bars()}
});
$(&quot;#tok&quot;).addEventListener(&quot;input&quot;,cost);
$(&quot;#out&quot;).addEventListener(&quot;input&quot;,cost);
chips();bars();cost();
window.addEventListener(&quot;load&quot;,ping);
setTimeout(ping,400);setTimeout(ping,1200);
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
</div>
<script>
(function(){
  var f=document.getElementById("mtp-g37-x7k2-f");
  window.addEventListener("message",function(e){
    if(!e.data||e.data.mtpFrame!=="g37flash")return;
    var h=parseInt(e.data.height,10);
    if(h&&h>200&&h<4000)f.style.height=h+"px";
  },false);
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-pricing-is-the-real-argument" class="wp-block-heading"><strong>Pricing is the real argument</strong></h2>



<p class="wp-block-paragraph">Gemini 3.7 Flash lists at $0.75 per 1M input tokens and $3.75 per 1M output tokens. That rate is introductory and expires December 31, 2026; from January 1, 2027 it becomes $1.50 and $7.50. In Google's own table, Claude Sonnet 5 sits at $2.00/$10.00 and GPT-5.6 Terra at $2.00/$12.00.</p>



<p class="wp-block-paragraph">At an 80/20 input-output mix, that is a blended $1.35 per 1M tokens today against $3.60 for Sonnet 5 and $4.00 for GPT-5.6 Terra. For teams running agents at volume, the intelligence-per-dollar gap is the reason to evaluate, not the individual eval wins.</p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>Gemini 3.7 Flash is a refinement of 3.6 Flash, not a new base model, shipped just three weeks later.</li>



<li>Coding gains are real: FrontierCode 43.6% vs 34.4%, DeepSWE 65.3% vs 48.6%, WebDev Arena 1588 Elo.</li>



<li>Price is the strongest claim — $0.75/$3.75 per 1M until December 31, 2026, then it doubles.</li>



<li>GPT-5.6 Terra still leads on terminal and computer-use agents; CharXiv is a small regression.</li>



<li>API and enterprise only. No open weights, so no self-hosting or air-gapped deployment.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/" target="_blank" rel="noreferrer noopener">Technical Details</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://magic.beehiiv.com/v1/f5e63dd4-5653-4f09-83e2-321a8b1ba526?email={{email}}" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/08/13/google-ai-just-released-gemini-3-7-flash/">Google AI Just Released Gemini 3.7 Flash: A Coding and Agent Model at $0.75/1M Input Tokens</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81710</post-id>	</item>
		<item>
		<title>Liquid AI Releases LFM2.5-VL-3B: A 3B Vision-Language Model That Reads Screens, Grounds Objects, and Calls Tools On-Device</title>
		<link>https://www.marktechpost.com/2026/08/13/liquid-ai-lfm2-5-vl-3b-on-device-vision-language-model/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Thu, 13 Aug 2026 15:56:03 +0000</pubDate>
				<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Computer Vision]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[OCR]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Uncategorized]]></category>
		<category><![CDATA[Vision Language Model]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81705</guid>

					<description><![CDATA[<p>Liquid AI released LFM2.5-VL-3B, a 3.1B-parameter vision-language model built for on-device deployment. It averages 80.7 on ScreenSpot-v2 and lifts RefCOCO grounding from 57.1 to 87.9. Function calling is new to the VL line, with ToolSandbox moving from 26.4 to 59.5. The model fits in roughly 3 GB and decodes 228 tokens/s on an Apple M5 Max.</p>
<p>The post <a href="https://www.marktechpost.com/2026/08/13/liquid-ai-lfm2-5-vl-3b-on-device-vision-language-model/">Liquid AI Releases LFM2.5-VL-3B: A 3B Vision-Language Model That Reads Screens, Grounds Objects, and Calls Tools On-Device</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph" id="h-">Yesterday,<a href="https://www.liquid.ai/"> Liquid AI</a> released <a href="https://www.liquid.ai/blog/lfm2-5-vl-3b">LFM2.5-VL-3B</a>. It is a 3.1B-parameter vision-language model built for on-device deployment. The model reads digital screens across mobile, web, and desktop. It grounds objects to coordinates, parses documents and charts, and calls tools from text or image input. Liquid AI reports an average of 69.4 across 28 vision benchmarks. That matches InternVL-3.5-4B and sits 0.7 points behind Qwen3.5-4B, both 4.7B models. The model is non-reasoning, so it answers directly and keeps latency low. It fits in roughly 3 GB of memory and decodes 228 tokens/s on an Apple M5 Max. </p>



<h2 id="h-is-it-deployable" class="wp-block-heading"><strong>Is it deployable?</strong></h2>



<p class="wp-block-paragraph"><strong>Yes, </strong>the checkpoint ships in four formats: <a href="https://huggingface.co/LiquidAI/LFM2.5-VL-3B">native</a>, <a href="https://huggingface.co/LiquidAI/LFM2.5-VL-3B-GGUF">GGUF</a>, <a href="https://huggingface.co/LiquidAI/LFM2.5-VL-3B-ONNX">ONNX</a>, and <a href="https://huggingface.co/LiquidAI/LFM2.5-VL-3B-MLX-8bit">MLX</a>. Day-one runtimes include llama.cpp, MLX, vLLM, SGLang, and ONNX. It fits in roughly 3 GB of memory.</p>



<ul class="wp-block-list">
<li><strong>Which company levels</strong>: The <a href="https://www.liquid.ai/lfm-license">LFM Open License v1.0</a> is Apache-2.0-based with one change: free commercial use ends once a company&#8217;s annual revenue reaches $10M USD. So indie developers, startups, and SMBs under that line can ship commercially at no cost. Enterprises above it must negotiate a commercial license with Liquid AI. Research, education, and non-profit use carry no revenue limit.</li>



<li><strong>Industries</strong>: Consumer electronics, <a href="https://www.liquid.ai/automotive">automotive</a>, <a href="https://www.liquid.ai/industrial-robotics">industrial and robotics</a>, <a href="https://www.liquid.ai/finance">financial services</a>, <a href="https://www.liquid.ai/healthcare">healthcare</a>, and <a href="https://www.liquid.ai/e-commerce">e-commerce</a>. Also QA and RPA vendors that automate GUIs.</li>



<li><strong>Applications</strong>: On-device screen agents, GUI test automation, PDF-to-structured-text with layout labels, invoice and receipt OCR, near-real-time object detection in vehicles, offline translation of menus and road signs, and multi-image comparison.</li>
</ul>



<h2 id="h-so-what-is-new" class="wp-block-heading"><strong>So, What is new?</strong></h2>



<p class="wp-block-paragraph">LFM2.5-VL-3B extends <a href="https://www.liquid.ai/blog/lfm2-vl-3b-a-new-efficient-vision-language-for-the-edge">LFM2-VL-3B</a> along four axes.</p>



<ul class="wp-block-list">
<li><strong>Screen and UI understanding</strong>: The model averages 80.7 on ScreenSpot-v2 across desktop (78.7), mobile (81.2), and web (82.2). Liquid AI reports Gemma-4-E4B at 51.2 and Qwen3.5-4B at 78.5, with the larger InternVL-3.5-4B ahead at 84.1.</li>



<li><strong>Function calling</strong>: This is new to the VL line. ToolSandbox moves from 26.4 to 59.5. BFCL v4 moves from 20.5 to 32.5. Tool calls are emitted as Pythonic calls between <code>&lt;|tool_call_start|></code> and <code>&lt;|tool_call_end|></code> tokens.</li>



<li><strong>Grounding</strong>: RefCOCO-avg precision@1 rises from 57.1 to 87.9, a 30-point gain driven by scaled synthetic grounding data.</li>



<li><strong>Multi-image input</strong>: BLINK improves from 50.2 to 61.5, and MuirBench from 34.9 to 58.3.</li>
</ul>



<h2 id="h-architecture-and-training" class="wp-block-heading"><strong>Architecture and training</strong></h2>



<p class="wp-block-paragraph">The language backbone is <a href="https://www.liquid.ai/blog/lfm2-5-2-6b">LFM2.5-2.6B</a>. The vision tower is a SigLIP2 NaFlex shape-optimized 400M encoder. NaFlex handles native resolution by splitting large images into non-overlapping 512×512 patches plus a resized whole-image thumbnail. Context length is 32,768 tokens, and 16 languages are supported.</p>



<p class="wp-block-paragraph">Pre-training used approximately 34T tokens. Vocabulary was doubled to 128K by <a href="https://www.liquid.ai/blog/tokenizer-expansion">extending the existing tokenizer in place</a>, which improves non-Latin script coverage. Vision pre-training was scaled 4× in tokens with curated and synthetic caption, OCR, grounding, and instruction-following data.</p>



<p class="wp-block-paragraph">Post-training is SFT with knowledge distillation from a larger teacher and <a href="https://www.liquid.ai/blog/antidoom">Antidoom training</a>, followed by multi-reward reinforcement learning.</p>



<p class="wp-block-paragraph">The model is non-reasoning. It answers directly, which is the design choice behind its latency profile.</p>



<h2 id="h-benchmarks" class="wp-block-heading"><strong>Benchmarks</strong></h2>



<p class="wp-block-paragraph">Liquid AI evaluated across 28 vision benchmarks using vLLM 0.26.0 in non-reasoning mode. LFM2.5-VL-3B averages 69.4, matching InternVL-3.5-4B (69.4) and landing 0.7 points behind Qwen3.5-4B (70.1). Both comparison models are 4.7B parameters.</p>



<p class="wp-block-paragraph">Notable individual results: RealWorldQA 73.1 against InternVL-3.5-4B at 67.7, TextVQA 84.3 against Qwen3.5-4B at 81.2, MMStar 63.3, MathVista-mini 68.5, ChartQA 81.3, DocVQA 91.1, and OCRBench v1 84.2. CountBenchQA regressed to 87.3 from 92.2 in the prior release.</p>



<p class="wp-block-paragraph">On text-only evaluation, IFEval reaches 82.3, up from 72.9. Gemma-4-E4B still leads there at 87.9.</p>



<!-- LFM2.5-VL-3B Interactive Explainer | Marktechpost | paste into a Custom HTML block -->
<div id="lfmx-wrap" style="max-width:100%;margin:28px 0;">
<iframe id="lfmx-frame" title="LFM2.5-VL-3B Interactive Explainer"
  style="width:100%;height:720px;border:0;display:block;overflow:hidden;"
  scrolling="no" loading="lazy"
  srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;LFM2.5-VL-3B Interactive Explainer&lt;/title&gt;
&lt;style&gt;
*{box-sizing:border-box;margin:0;padding:0}
body{background:#FFFFFF;color:#101418;font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Inter,Roboto,sans-serif;-webkit-font-smoothing:antialiased}
#lfmx{max-width:940px;margin:0 auto;padding:18px}
#lfmx .hd{border:1px solid #E4E8EE;border-radius:14px;padding:20px 22px;background:
 radial-gradient(900px 240px at 8% -50%,rgba(47,107,255,.10),transparent 62%),
 linear-gradient(180deg,#FFFFFF,#F7F9FC)}
#lfmx .eyebrow{font:600 10.5px/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.20em;color:#2F6BFF;text-transform:uppercase}
#lfmx h1{font-size:21px;line-height:1.28;margin:10px 0 7px;color:#0B0E12;font-weight:700;letter-spacing:-.01em}
#lfmx .sub{font-size:13px;line-height:1.58;color:#5D6875}
#lfmx .chips{display:flex;flex-wrap:wrap;gap:7px;margin-top:13px}
#lfmx .chip{font:600 11px/1 ui-monospace,SFMono-Regular,Menlo,monospace;padding:6px 10px;border-radius:999px;border:1px solid #E1E6ED;background:#F4F6FA;color:#3E4956}
#lfmx .tabs{display:flex;flex-wrap:wrap;gap:6px;margin:18px 0 14px}
#lfmx .tab{flex:1 1 150px;cursor:pointer;border:1px solid #E1E6ED;background:#FFFFFF;color:#69747F;
 border-radius:10px;padding:10px 8px;font:600 11.5px/1.35 ui-monospace,SFMono-Regular,Menlo,monospace;
 text-align:center;transition:.18s;letter-spacing:.04em}
#lfmx .tab:hover{border-color:#B9C4D2;color:#20262E;background:#F7F9FC}
#lfmx .tab.on{background:#0B0E12;border-color:#0B0E12;color:#FFFFFF;
 box-shadow:0 6px 18px -10px rgba(11,14,18,.65)}
#lfmx .panel{display:none;border:1px solid #E4E8EE;border-radius:14px;background:#FFFFFF;padding:20px}
#lfmx .panel.on{display:block;animation:lfmfade .32s ease}
@keyframes lfmfade{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
#lfmx h2{font-size:15px;color:#0B0E12;margin-bottom:7px;font-weight:700;letter-spacing:-.005em}
#lfmx p.exp{font-size:12.8px;line-height:1.65;color:#5D6875;margin-bottom:15px}
#lfmx .stage{border:1px solid #E9EDF3;border-radius:11px;background:#FAFBFD;padding:15px;position:relative;overflow:hidden}
#lfmx .btn{cursor:pointer;border:1px solid #D5DCE6;background:#FFFFFF;
 color:#20262E;border-radius:8px;padding:8px 14px;font:600 11.5px/1 ui-monospace,Menlo,monospace;letter-spacing:.05em;transition:.16s}
#lfmx .btn:hover{border-color:#0B0E12;background:#0B0E12;color:#FFFFFF}
#lfmx .row{display:flex;flex-wrap:wrap;gap:9px;align-items:center;margin-top:14px}
#lfmx .nums{display:grid;grid-template-columns:repeat(3,1fr);gap:9px;margin-top:15px}
#lfmx .num{border:1px solid #E9EDF3;border-radius:9px;background:#FAFBFD;padding:11px}
#lfmx .num i{display:block;font:600 9.5px/1 ui-monospace,Menlo,monospace;color:#818C99;letter-spacing:.13em;text-transform:uppercase;font-style:normal}
#lfmx .num b{display:block;margin-top:7px;font:700 19px/1 ui-monospace,Menlo,monospace;color:#0B0E12}
#lfmx .num s{display:block;margin-top:5px;font-size:10.5px;color:#818C99;text-decoration:none}
#lfmx .note{margin-top:13px;font:400 11px/1.6 ui-monospace,Menlo,monospace;color:#98A2AE}
#lfmx .foot{margin-top:15px;text-align:center;font:600 10.5px/1.5 ui-monospace,Menlo,monospace;color:#98A2AE;letter-spacing:.1em}
#lfmx .foot span{color:#2F6BFF}
/* naflex */
#lfmx .nf{display:flex;gap:16px;align-items:center;justify-content:center;flex-wrap:wrap;min-height:170px}
#lfmx .img{width:150px;height:112px;border:1px solid #C9D3E0;border-radius:7px;position:relative;overflow:hidden;
 background:linear-gradient(135deg,#E8EEF8,#D8E2F0)}
#lfmx .tile{position:absolute;border:1px solid #2F6BFF;background:rgba(47,107,255,.12);opacity:0;transition:opacity .3s}
#lfmx .tile.on{opacity:1}
#lfmx .arrow{color:#2F6BFF;font-size:20px}
#lfmx .toks{display:flex;flex-wrap:wrap;gap:4px;width:170px}
#lfmx .tok{width:13px;height:13px;border-radius:3px;background:#E3E8F0;transform:scale(.2);transition:.25s}
#lfmx .tok.on{background:#2F6BFF;transform:scale(1)}
/* grounding + screen canvas */
#lfmx .scene{position:relative;width:100%;max-width:430px;margin:0 auto;height:210px;border-radius:8px;
 background:#EEF2F7;border:1px solid #DDE3EC}
#lfmx .obj{position:absolute;border-radius:5px;background:#D3DBE5}
#lfmx .bbox{position:absolute;border:2px solid #2F6BFF;border-radius:4px;opacity:0;transform:scale(1.25);
 transition:opacity .35s,transform .35s;box-shadow:0 0 0 3px rgba(47,107,255,.12)}
#lfmx .bbox.on{opacity:1;transform:scale(1)}
#lfmx .bbox em{position:absolute;top:-19px;left:-2px;font:600 9.5px/1 ui-monospace,Menlo,monospace;
 background:#2F6BFF;color:#FFFFFF;padding:3px 5px;border-radius:3px;font-style:normal;white-space:nowrap}
#lfmx .cross{position:absolute;width:20px;height:20px;margin:-10px 0 0 -10px;pointer-events:none;
 transition:left .55s cubic-bezier(.5,0,.2,1),top .55s cubic-bezier(.5,0,.2,1)}
#lfmx .cross:before,#lfmx .cross:after{content:&quot;&quot;;position:absolute;background:#0B0E12}
#lfmx .cross:before{left:9px;top:0;width:2px;height:20px}
#lfmx .cross:after{top:9px;left:0;height:2px;width:20px}
#lfmx .ring{position:absolute;inset:-9px;border:2px solid #0B0E12;border-radius:50%;animation:lfmping 1.3s infinite}
@keyframes lfmping{0%{transform:scale(.55);opacity:.85}100%{transform:scale(1.5);opacity:0}}
/* bars */
#lfmx .bar{margin-top:11px}
#lfmx .bar u{display:flex;justify-content:space-between;font:600 11px/1 ui-monospace,Menlo,monospace;
 color:#5D6875;text-decoration:none;margin-bottom:5px}
#lfmx .bar u b{color:#0B0E12}
#lfmx .track{height:9px;border-radius:6px;background:#EDF0F5;overflow:hidden}
#lfmx .fill{height:100%;width:0;border-radius:6px;background:linear-gradient(90deg,#2F6BFF,#5B92FF);
 transition:width 1.05s cubic-bezier(.3,1,.4,1)}
#lfmx .fill.alt{background:#C4CDD9}
/* tools terminal */
#lfmx .term{font:400 12px/1.75 ui-monospace,SFMono-Regular,Menlo,monospace;min-height:186px;color:#2A313A}
#lfmx .term div{opacity:0;transform:translateY(5px);transition:.3s;overflow-wrap:anywhere;word-break:break-word}
#lfmx .term div.on{opacity:1;transform:none}
#lfmx .k{color:#2F6BFF}#lfmx .g{color:#0E8A5F}#lfmx .o{color:#0B0E12;font-weight:600}#lfmx .d{color:#98A2AE}
@media(max-width:640px){
 #lfmx{padding:12px}
 #lfmx h1{font-size:17px}
 #lfmx .nums{grid-template-columns:1fr}
 #lfmx .tab{flex:1 1 100%}
 #lfmx .nf{flex-direction:column}
 #lfmx .arrow{transform:rotate(90deg)}
 #lfmx .scene{height:180px}
 #lfmx .bbox em{font-size:9px;padding:2px 4px;top:-17px}
 #lfmx .co{display:none}
}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;lfmx&quot;&gt;

  &lt;div class=&quot;hd&quot;&gt;
    &lt;div class=&quot;eyebrow&quot;&gt;Liquid AI &amp;middot; Aug 12, 2026 &amp;middot; Interactive Explainer&lt;/div&gt;
    &lt;h1&gt;LFM2.5-VL-3B: how a 3.1B model reads screens, grounds objects, and calls tools&lt;/h1&gt;
    &lt;p class=&quot;sub&quot;&gt;Five interactive panels. Every number below is taken from Liquid AI's release post and model card.&lt;/p&gt;
    &lt;div class=&quot;chips&quot;&gt;
      &lt;span class=&quot;chip&quot;&gt;3.1B params&lt;/span&gt;
      &lt;span class=&quot;chip&quot;&gt;SigLIP2 NaFlex 400M&lt;/span&gt;
      &lt;span class=&quot;chip&quot;&gt;128K vocab&lt;/span&gt;
      &lt;span class=&quot;chip&quot;&gt;32,768 ctx&lt;/span&gt;
      &lt;span class=&quot;chip&quot;&gt;~3 GB memory&lt;/span&gt;
      &lt;span class=&quot;chip&quot;&gt;non-reasoning&lt;/span&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;tabs&quot;&gt;
    &lt;div class=&quot;tab on&quot; data-p=&quot;0&quot;&gt;01 &amp;middot; NaFlex Vision&lt;/div&gt;
    &lt;div class=&quot;tab&quot; data-p=&quot;1&quot;&gt;02 &amp;middot; Grounding&lt;/div&gt;
    &lt;div class=&quot;tab&quot; data-p=&quot;2&quot;&gt;03 &amp;middot; Screen / UI&lt;/div&gt;
    &lt;div class=&quot;tab&quot; data-p=&quot;3&quot;&gt;04 &amp;middot; Tool Calling&lt;/div&gt;
    &lt;div class=&quot;tab&quot; data-p=&quot;4&quot;&gt;05 &amp;middot; Speed&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- 01 --&gt;
  &lt;div class=&quot;panel on&quot; data-i=&quot;0&quot;&gt;
    &lt;h2&gt;01 &amp;middot; Native-resolution vision with SigLIP2 NaFlex&lt;/h2&gt;
    &lt;p class=&quot;exp&quot;&gt;Large images are split into non-overlapping 512&amp;times;512 patches, plus one resized whole-image thumbnail. The encoder turns those patches into visual tokens that the LFM2.5-2.6B backbone reads alongside text. More patches means more tokens, and more tokens means slower time to first token.&lt;/p&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;nf&quot;&gt;
        &lt;div&gt;
          &lt;div class=&quot;img&quot; id=&quot;nfImg&quot;&gt;&lt;/div&gt;
          &lt;div style=&quot;text-align:center;font:600 9.5px/1 ui-monospace,Menlo,monospace;color:#818C99;margin-top:7px&quot; id=&quot;nfDim&quot;&gt;1024 &amp;times; 768&lt;/div&gt;
        &lt;/div&gt;
        &lt;div class=&quot;arrow&quot;&gt;&amp;#10230;&lt;/div&gt;
        &lt;div class=&quot;toks&quot; id=&quot;nfToks&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;
        &lt;button class=&quot;btn&quot; id=&quot;nfA&quot;&gt;Small image&lt;/button&gt;
        &lt;button class=&quot;btn&quot; id=&quot;nfB&quot;&gt;Large image&lt;/button&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;nums&quot;&gt;
      &lt;div class=&quot;num&quot;&gt;&lt;i&gt;Patch grid&lt;/i&gt;&lt;b id=&quot;nfGrid&quot;&gt;2 &amp;times; 2&lt;/b&gt;&lt;s&gt;512&amp;times;512 non-overlapping&lt;/s&gt;&lt;/div&gt;
      &lt;div class=&quot;num&quot;&gt;&lt;i&gt;+ thumbnail&lt;/i&gt;&lt;b&gt;1&lt;/b&gt;&lt;s&gt;resized whole image&lt;/s&gt;&lt;/div&gt;
      &lt;div class=&quot;num&quot;&gt;&lt;i&gt;Context budget&lt;/i&gt;&lt;b&gt;32,768&lt;/b&gt;&lt;s&gt;tokens, text + vision&lt;/s&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;note&quot;&gt;Tile count is computed from the selected dimensions. Token squares are illustrative, not the model's real token count.&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- 02 --&gt;
  &lt;div class=&quot;panel&quot; data-i=&quot;1&quot;&gt;
    &lt;h2&gt;02 &amp;middot; Grounding: from 57.1 to 87.9 on RefCOCO&lt;/h2&gt;
    &lt;p class=&quot;exp&quot;&gt;Grounding means returning a bounding box for an object named in plain language. Liquid AI scaled synthetic grounding data, and RefCOCO-avg precision@1 moved from 57.1 to 87.9 &amp;mdash; a 30-point gain over LFM2-VL-3B. Coordinates are normalized integers in the 0&amp;ndash;1000 range.&lt;/p&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;scene&quot; id=&quot;gScene&quot;&gt;
        &lt;div class=&quot;obj&quot; style=&quot;left:8%;top:52%;width:22%;height:32%&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;obj&quot; style=&quot;left:40%;top:22%;width:18%;height:26%&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;obj&quot; style=&quot;left:67%;top:46%;width:24%;height:38%&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;bbox&quot; data-b=&quot;1&quot; style=&quot;left:8%;top:52%;width:22%;height:32%&quot;&gt;&lt;em&gt;cup&lt;span class=&quot;co&quot;&gt; [080,520,300,840]&lt;/span&gt;&lt;/em&gt;&lt;/div&gt;
        &lt;div class=&quot;bbox&quot; data-b=&quot;1&quot; style=&quot;left:40%;top:22%;width:18%;height:26%&quot;&gt;&lt;em&gt;sign&lt;span class=&quot;co&quot;&gt; [400,220,580,480]&lt;/span&gt;&lt;/em&gt;&lt;/div&gt;
        &lt;div class=&quot;bbox&quot; data-b=&quot;1&quot; style=&quot;left:67%;top:46%;width:24%;height:38%&quot;&gt;&lt;em&gt;bag&lt;span class=&quot;co&quot;&gt; [670,460,910,840]&lt;/span&gt;&lt;/em&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;button class=&quot;btn&quot; id=&quot;gRun&quot;&gt;Run grounding query&lt;/button&gt;
      &lt;span style=&quot;font:400 11px/1 ui-monospace,Menlo,monospace;color:#818C99&quot;&gt;&quot;find the cup, the sign, and the bag&quot;&lt;/span&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;bar&quot;&gt;&lt;u&gt;LFM2.5-VL-3B &lt;b id=&quot;gA&quot;&gt;87.9&lt;/b&gt;&lt;/u&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; data-w=&quot;87.9&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;bar&quot;&gt;&lt;u&gt;LFM2-VL-3B (previous) &lt;b&gt;57.1&lt;/b&gt;&lt;/u&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill alt&quot; data-w=&quot;57.1&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;bar&quot;&gt;&lt;u&gt;InternVL 3.5 4B &lt;b&gt;88.8&lt;/b&gt;&lt;/u&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill alt&quot; data-w=&quot;88.8&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;note&quot;&gt;Scene is a schematic. Box coordinates shown are illustrative examples of the model's 0&amp;ndash;1000 output format.&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- 03 --&gt;
  &lt;div class=&quot;panel&quot; data-i=&quot;2&quot;&gt;
    &lt;h2&gt;03 &amp;middot; Screen understanding across mobile, web, desktop&lt;/h2&gt;
    &lt;p class=&quot;exp&quot;&gt;ScreenSpot-v2 asks a model to point at the UI element that satisfies an instruction. LFM2.5-VL-3B averages 80.7, splitting into 78.7 desktop, 81.2 mobile, and 82.2 web. That is far ahead of Gemma-4-E4B at 51.2 and slightly ahead of Qwen3.5-4B at 78.5, while InternVL-3.5-4B leads at 84.1.&lt;/p&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;scene&quot; id=&quot;sScene&quot; style=&quot;background:#F2F5F9&quot;&gt;
        &lt;div class=&quot;obj&quot; style=&quot;left:5%;top:8%;width:90%;height:14%;background:#C3CDDA&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;obj&quot; style=&quot;left:5%;top:30%;width:42%;height:26%&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;obj&quot; style=&quot;left:53%;top:30%;width:42%;height:26%&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;obj&quot; style=&quot;left:5%;top:64%;width:28%;height:22%;background:#2F6BFF&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;obj&quot; style=&quot;left:37%;top:64%;width:28%;height:22%&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;obj&quot; style=&quot;left:69%;top:64%;width:26%;height:22%&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;cross&quot; id=&quot;sCross&quot; style=&quot;left:50%;top:50%&quot;&gt;&lt;span class=&quot;ring&quot;&gt;&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;row&quot; id=&quot;sBtns&quot;&gt;
        &lt;button class=&quot;btn&quot; data-x=&quot;50&quot; data-y=&quot;15&quot;&gt;&quot;open the top bar&quot;&lt;/button&gt;
        &lt;button class=&quot;btn&quot; data-x=&quot;19&quot; data-y=&quot;75&quot;&gt;&quot;tap Submit&quot;&lt;/button&gt;
        &lt;button class=&quot;btn&quot; data-x=&quot;74&quot; data-y=&quot;43&quot;&gt;&quot;select right card&quot;&lt;/button&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;bar&quot;&gt;&lt;u&gt;LFM2.5-VL-3B (3.1B) &lt;b&gt;80.7&lt;/b&gt;&lt;/u&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; data-w=&quot;80.7&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;bar&quot;&gt;&lt;u&gt;InternVL 3.5 4B (4.7B) &lt;b&gt;84.1&lt;/b&gt;&lt;/u&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill alt&quot; data-w=&quot;84.1&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;bar&quot;&gt;&lt;u&gt;Qwen3.5-4B (4.7B) &lt;b&gt;78.5&lt;/b&gt;&lt;/u&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill alt&quot; data-w=&quot;78.5&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;bar&quot;&gt;&lt;u&gt;Gemma-4-E4B (8B) &lt;b&gt;51.2&lt;/b&gt;&lt;/u&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill alt&quot; data-w=&quot;51.2&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;div class=&quot;note&quot;&gt;Wireframe is schematic. Crosshair positions are hand-placed to show what a grounded click looks like.&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- 04 --&gt;
  &lt;div class=&quot;panel&quot; data-i=&quot;3&quot;&gt;
    &lt;h2&gt;04 &amp;middot; Function calling from text or image input&lt;/h2&gt;
    &lt;p class=&quot;exp&quot;&gt;Function calling is new to Liquid AI's VL line. The model writes Pythonic calls between special tokens, the host executes them, and the result comes back with the tool role. ToolSandbox more than doubled from 26.4 to 59.5, and BFCL v4 moved from 20.5 to 32.5.&lt;/p&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;term&quot; id=&quot;tTerm&quot;&gt;
        &lt;div&gt;&lt;span class=&quot;d&quot;&gt;user &amp;rsaquo;&lt;/span&gt; [image of a dish] what is the recipe for this?&lt;/div&gt;
        &lt;div&gt;&lt;span class=&quot;d&quot;&gt;assistant &amp;rsaquo;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;&amp;lt;|tool_call_start|&amp;gt;&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;&lt;span class=&quot;k&quot;&gt;&amp;nbsp;&amp;nbsp;[search_recipe(dish=&quot;shakshuka&quot;)]&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;&lt;span class=&quot;k&quot;&gt;&amp;lt;|tool_call_end|&amp;gt;&lt;/span&gt; &lt;span class=&quot;d&quot;&gt;Looking up the recipe.&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;&lt;span class=&quot;d&quot;&gt;tool &amp;rsaquo;&lt;/span&gt; &lt;span class=&quot;g&quot;&gt;[{&quot;title&quot;:&quot;Shakshuka&quot;,&quot;steps&quot;:6,&quot;time&quot;:&quot;25m&quot;}]&lt;/span&gt;&lt;/div&gt;
        &lt;div&gt;&lt;span class=&quot;d&quot;&gt;assistant &amp;rsaquo;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;Shakshuka. Six steps, about 25 minutes.&lt;/span&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;button class=&quot;btn&quot; id=&quot;tRun&quot;&gt;Replay tool call&lt;/button&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;nums&quot;&gt;
      &lt;div class=&quot;num&quot;&gt;&lt;i&gt;ToolSandbox&lt;/i&gt;&lt;b id=&quot;tS&quot;&gt;59.5&lt;/b&gt;&lt;s&gt;from 26.4&lt;/s&gt;&lt;/div&gt;
      &lt;div class=&quot;num&quot;&gt;&lt;i&gt;BFCL v4&lt;/i&gt;&lt;b id=&quot;tB&quot;&gt;32.5&lt;/b&gt;&lt;s&gt;from 20.5&lt;/s&gt;&lt;/div&gt;
      &lt;div class=&quot;num&quot;&gt;&lt;i&gt;IFEval (text)&lt;/i&gt;&lt;b id=&quot;tI&quot;&gt;82.3&lt;/b&gt;&lt;s&gt;from 72.9&lt;/s&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;note&quot;&gt;Transcript is a representative example of the documented call format, not a captured model output.&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- 05 --&gt;
  &lt;div class=&quot;panel&quot; data-i=&quot;4&quot;&gt;
    &lt;h2&gt;05 &amp;middot; Why it is fast: no reasoning trace, light encoder&lt;/h2&gt;
    &lt;p class=&quot;exp&quot;&gt;The model answers directly instead of producing a reasoning trace, and its vision encoder is small. Both shorten time to first token. On a single NVIDIA H100 SXM5, TTFT is about 34 ms on a 5-frame 256&amp;times;256 clip, where Liquid AI measured the Gemma models near 200 ms.&lt;/p&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;bar&quot;&gt;&lt;u&gt;Apple M5 Max &lt;b&gt;&lt;span id=&quot;rA&quot;&gt;0&lt;/span&gt; tok/s&lt;/b&gt;&lt;/u&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; data-w=&quot;100&quot; data-c=&quot;228&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;bar&quot;&gt;&lt;u&gt;AMD Ryzen AI Max+ 395 &lt;b&gt;&lt;span id=&quot;rB&quot;&gt;0&lt;/span&gt; tok/s&lt;/b&gt;&lt;/u&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; data-w=&quot;50.9&quot; data-c=&quot;116&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;bar&quot;&gt;&lt;u&gt;Galaxy S26 Ultra &lt;b&gt;&lt;span id=&quot;rC&quot;&gt;0&lt;/span&gt; tok/s&lt;/b&gt;&lt;/u&gt;&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill&quot; data-w=&quot;8.8&quot; data-c=&quot;20&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;button class=&quot;btn&quot; id=&quot;rRun&quot;&gt;Replay decode race&lt;/button&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;nums&quot;&gt;
      &lt;div class=&quot;num&quot;&gt;&lt;i&gt;TTFT (5-frame clip)&lt;/i&gt;&lt;b&gt;~34 ms&lt;/b&gt;&lt;s&gt;H100 SXM5, one request&lt;/s&gt;&lt;/div&gt;
      &lt;div class=&quot;num&quot;&gt;&lt;i&gt;Peak throughput&lt;/i&gt;&lt;b&gt;~11K tok/s&lt;/b&gt;&lt;s&gt;high concurrency, vLLM 0.26&lt;/s&gt;&lt;/div&gt;
      &lt;div class=&quot;num&quot;&gt;&lt;i&gt;Memory footprint&lt;/i&gt;&lt;b&gt;~3 GB&lt;/b&gt;&lt;s&gt;on-device&lt;/s&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;note&quot;&gt;Bars are scaled to the fastest device shown. All figures are Liquid AI's published measurements.&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;foot&quot;&gt;MARKTECHPOST &lt;span&gt;&amp;#9670;&lt;/span&gt; SOURCE: LIQUID AI RELEASE POST &amp;amp; HUGGING FACE MODEL CARD&lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var R=document.getElementById('lfmx');
  var tabs=[].slice.call(R.querySelectorAll('.tab')),panels=[].slice.call(R.querySelectorAll('.panel'));

  function fillBars(p){
    [].slice.call(p.querySelectorAll('.fill')).forEach(function(f,i){
      f.style.width='0%';
      setTimeout(function(){f.style.width=f.getAttribute('data-w')+'%';},80+i*110);
    });
  }
  function count(el,to,ms){
    var st=null,dec=(to%1!==0);
    function s(t){if(!st)st=t;var k=Math.min(1,(t-st)/ms);
      el.textContent=dec?(to*k).toFixed(1):Math.round(to*k);
      if(k&lt;1)requestAnimationFrame(s);}
    requestAnimationFrame(s);
  }

  /* 01 NaFlex */
  var nfImg=document.getElementById('nfImg'),nfToks=document.getElementById('nfToks');
  for(var i=0;i&lt;24;i++){var s=document.createElement('span');s.className='tok';nfToks.appendChild(s);}
  var toks=[].slice.call(nfToks.children);
  function naflex(w,h){
    document.getElementById('nfDim').innerHTML=w+' &amp;times; '+h;
    var cx=Math.ceil(w/512),cy=Math.ceil(h/512),n=cx*cy;
    document.getElementById('nfGrid').innerHTML=cx+' &amp;times; '+cy;
    nfImg.innerHTML='';
    for(var y=0;y&lt;cy;y++)for(var x=0;x&lt;cx;x++){
      var d=document.createElement('div');d.className='tile';
      d.style.left=(x*100/cx)+'%';d.style.top=(y*100/cy)+'%';
      d.style.width=(100/cx)+'%';d.style.height=(100/cy)+'%';
      nfImg.appendChild(d);
    }
    var ts=[].slice.call(nfImg.children);
    ts.forEach(function(t,k){setTimeout(function(){t.className='tile on';},120+k*130);});
    var lit=Math.min(24,n*5+3);
    toks.forEach(function(t){t.className='tok';});
    for(var j=0;j&lt;lit;j++)(function(j){setTimeout(function(){toks[j].className='tok on';},400+j*45);})(j);
  }
  document.getElementById('nfA').onclick=function(){naflex(640,480);};
  document.getElementById('nfB').onclick=function(){naflex(1600,1200);};

  /* 02 grounding */
  var gBoxes=[].slice.call(document.querySelectorAll('#lfmx .bbox'));
  function ground(){
    gBoxes.forEach(function(b){b.className='bbox';});
    gBoxes.forEach(function(b,k){setTimeout(function(){b.className='bbox on';},220+k*300);});
    count(document.getElementById('gA'),87.9,1000);
    fillBars(panels[1]);
  }
  document.getElementById('gRun').onclick=ground;

  /* 03 screen */
  var cross=document.getElementById('sCross');
  [].slice.call(document.querySelectorAll('#sBtns .btn')).forEach(function(b){
    b.onclick=function(){cross.style.left=b.getAttribute('data-x')+'%';cross.style.top=b.getAttribute('data-y')+'%';};
  });

  /* 04 tools */
  var lines=[].slice.call(document.querySelectorAll('#tTerm div'));
  function term(){
    lines.forEach(function(l){l.className='';});
    lines.forEach(function(l,k){setTimeout(function(){l.className='on';},180+k*420);});
    count(document.getElementById('tS'),59.5,900);
    count(document.getElementById('tB'),32.5,900);
    count(document.getElementById('tI'),82.3,900);
  }
  document.getElementById('tRun').onclick=term;

  /* 05 speed */
  function race(){
    fillBars(panels[4]);
    count(document.getElementById('rA'),228,1200);
    count(document.getElementById('rB'),116,1200);
    count(document.getElementById('rC'),20,1200);
  }
  document.getElementById('rRun').onclick=race;

  /* tabs */
  tabs.forEach(function(t){
    t.onclick=function(){
      tabs.forEach(function(x){x.className='tab';});
      panels.forEach(function(x){x.className='panel';});
      t.className='tab on';
      var idx=+t.getAttribute('data-p');
      panels[idx].className='panel on';
      if(idx===1)ground();
      if(idx===2)fillBars(panels[2]);
      if(idx===3)term();
      if(idx===4)race();
      resize();
    };
  });

  /* seed everything on load */
  naflex(1024,768);
  gBoxes.forEach(function(b){b.className='bbox on';});
  [].slice.call(panels[1].querySelectorAll('.fill')).forEach(function(f){f.style.width=f.getAttribute('data-w')+'%';});
  [].slice.call(panels[2].querySelectorAll('.fill')).forEach(function(f){f.style.width=f.getAttribute('data-w')+'%';});
  [].slice.call(panels[4].querySelectorAll('.fill')).forEach(function(f){f.style.width=f.getAttribute('data-w')+'%';});
  lines.forEach(function(l){l.className='on';});
  document.getElementById('rA').textContent='228';
  document.getElementById('rB').textContent='116';
  document.getElementById('rC').textContent='20';
  fillBars(panels[0]);

  /* auto-resize for iframe host */
  function resize(){
    try{parent.postMessage({lfmxHeight:R.offsetHeight+40},'*');}catch(e){}
  }
  window.addEventListener('load',resize);
  setTimeout(resize,120);setTimeout(resize,600);
  window.addEventListener('resize',resize);
  resize();
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
</div>
<script>
(function(){
  var f=document.getElementById('lfmx-frame');
  window.addEventListener('message',function(e){
    if(e&&e.data&&e.data.lfmxHeight){ f.style.height=e.data.lfmxHeight+'px'; }
  },false);
})();
</script>




<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>LFM2.5-VL-3B hits a 69.4 average across 28 vision benchmarks, matching 4.7B-class models.</li>



<li>ScreenSpot-v2 jumps to 80.7 and RefCOCO-avg to 87.9, from 57.1 in the prior release.</li>



<li>Function calling is new to the VL line: ToolSandbox 26.4 → 59.5, BFCL v4 20.5 → 32.5.</li>



<li>Runs in ~3 GB, decoding 228 tok/s on M5 Max and 20 tok/s on a Galaxy S26 Ultra.</li>



<li>LFM Open License v1.0 is free commercially only under $10M annual revenue.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://www.liquid.ai/blog/lfm2-5-vl-3b" target="_blank" rel="noreferrer noopener">Technical Details</a></strong> and <strong><a href="https://huggingface.co/LiquidAI/LFM2.5-VL-3B" target="_blank" rel="noreferrer noopener">Model Weights</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://magic.beehiiv.com/v1/f5e63dd4-5653-4f09-83e2-321a8b1ba526?email={{email}}" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/08/13/liquid-ai-lfm2-5-vl-3b-on-device-vision-language-model/">Liquid AI Releases LFM2.5-VL-3B: A 3B Vision-Language Model That Reads Screens, Grounds Objects, and Calls Tools On-Device</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81705</post-id>	</item>
		<item>
		<title>Dyna Robotics Introduces Dyna-2: A World-Action Model Pre-Trained on 1 Million Hours of Human Video</title>
		<link>https://www.marktechpost.com/2026/08/13/dyna-robotics-introduces-dyna-2-a-world-action-model-pre-trained-on-1-million-hours-of-human-video/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Thu, 13 Aug 2026 07:42:02 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Embedding Model]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Physical AI]]></category>
		<category><![CDATA[Robotics]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Vision Language Model]]></category>
		<category><![CDATA[World Model]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81697</guid>

					<description><![CDATA[<p>Dyna Robotics has released Dyna-2, a world-action model pre-trained on more than one million hours of egocentric human video. The technical report establishes three results: a scaling law on human data to 1M hours, the first transfer of that law to unseen robot data, and evidence that video co-training drives cross-embodiment generalization.</p>
<p>The post <a href="https://www.marktechpost.com/2026/08/13/dyna-robotics-introduces-dyna-2-a-world-action-model-pre-trained-on-1-million-hours-of-human-video/">Dyna Robotics Introduces Dyna-2: A World-Action Model Pre-Trained on 1 Million Hours of Human Video</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph" id="h-">Dyna Robotics has released <strong>Dyna-2</strong>, a world-action model for robot manipulation. It was pre-trained on more than one million hours of egocentric human video. That is roughly 170 years of continuous waking experience. Robot learning has been bottlenecked by action-labelled data, which teleoperation must deliberately produce. Dyna-2 tests whether ordinary human video can substitute. The research team trained a data ladder from 1,000 to 1,000,000 hours and measured what scales. Three results follow: a scaling law on human data, the first transfer of that law to unseen robot data, and evidence that video prediction drives the transfer.</p>



<h2 id="h-is-it-deployable" class="wp-block-heading"><strong>Is it deployable?</strong></h2>



<p class="wp-block-paragraph"><strong>Yes, but as a vendor-operated system, not as downloadable weights.</strong> <a href="https://www.dyna.co/">Dyna Robotics</a> has announced no public checkpoint, API, or license for Dyna-2. Deployment today means buying a Dyna robot cell, not self-hosting a model.</p>



<ul class="wp-block-list">
<li><strong>Which companies</strong>: Dyna-1 robots already run in production in hotels, restaurants, and laundromats, per the company&#8217;s <a href="https://www.morningstar.com/news/pr-newswire/20260810sf22606/dyna-robotics-unveils-dyna-2-world-action-model-demonstrating-first-true-scaling-law-in-robotics-powered-entirely-by-human-data">August 10, 2026 announcement</a>. That points at mid-market service operators and multi-site enterprises with repetitive, stationary manipulation work. It is not a fit for solo builders or research labs wanting local inference.</li>



<li><strong>Industries</strong>: Hospitality, commercial laundry, food service, light assembly and kitting, and facilities cleaning.</li>



<li><strong>Applications</strong>: The 14 post-training tasks map cleanly to real work: trash tray clearing, first-aid kitting, tote construction, food scooping, rope tying, hanger preparation, and targeted drink retrieval from a fridge.</li>
</ul>



<h2 id="h-what-is-dyna-2" class="wp-block-heading">What is Dyna-2 </h2>



<p class="wp-block-paragraph">Dyna-2 is a <strong>world-action model (WAM)</strong>: one generative model that denoises future video and a future action chunk, jointly or separately, on a video-diffusion backbone. It was pre-trained on <strong>more than one million hours of egocentric human video</strong>, roughly 170 years of continuous waking experience.</p>



<p class="wp-block-paragraph">Architecturally it is a mixture of transformers. Video and action are tokenized separately and get distinct DiT layer stacks that attend to each other. Proprioception feeds directly into the action transformer. Video tokens use causal masking; action tokens use bidirectional self-attention and attend to context video tokens. Video tokens cross-attend to text, but text does not directly influence action tokens.</p>



<p class="wp-block-paragraph">Training uses <strong>flow matching</strong>. A video loss and an action loss share a trunk as two separate marginal velocity fields. Because the action network never takes the noised video latent as an argument, the policy stays reactive at inference — it neither generates nor attends to predicted future video. The action transformer is deliberately shallower and joins the video stream early, which the team says improves real-time latency without costing performance.</p>



<h2 id="h-the-three-scaling-results" class="wp-block-heading"><strong>The three scaling results</strong></h2>



<p class="wp-block-paragraph">Dyna Robotics cut <strong>nested</strong> subsets of exactly 1,000, 10,000, 100,000, and 1,000,000 hours, keeping identical proportions from each source. A larger budget only adds data, so curve differences cannot be attributed to distribution shift. A fixed, disjoint 100-hour validation set scores every rung.</p>



<ol class="wp-block-list">
<li><strong>A scaling law holds on human data to one million hours</strong>: All four metrics improve monotonically and fit power laws: held-out MSE = 0.0691·D^-0.0184 (R²=0.919), accuracy@0.5 = 0.357·D^+0.0203 (R²=0.865). Across the ladder, accuracy@0.1 rises 51% against 12% for MSE.</li>



<li><strong>That law transfers to robot data the model never saw</strong>: The same checkpoints were scored zero-shot on 39 tasks across two stationary bimanual YAM platforms — 12 internal, 27 from <a href="https://arxiv.org/abs/2606.27375">xdof ABC</a>. Zero-shot action MSE = 0.306·D^-0.0713 (R²=0.884). The team reports an inflection between 10k and 100k hours.</li>



<li><strong>The objective matters, and video is a separate axis</strong>: Joint denoising beat action-only on <strong>39 of 39 tasks at every action scale</strong>. Holding action-labelled data fixed at 50,000 hours and adding video-only hours drops zero-shot robot MSE from 0.340 to 0.120. Notably, held-out <em>human</em> error does not improve — the benefit of video is specifically cross-embodiment generalization.</li>
</ol>



<h2 id="h-on-robot-results" class="wp-block-heading"><strong>On-robot results</strong></h2>



<p class="wp-block-paragraph">Each rung was post-trained on 14 tasks, at most 10 hours of robot data each, across three embodiments: 6-DOF YAM arms with parallel-jaw grippers, the same arms with WUJI-2 20-DOF dexterous hands, and a semi-humanoid prototype. Post-training used robot data only — no human-robot alignment, no co-training.</p>



<p class="wp-block-paragraph">Mean normalized score rose <strong>20% → 28% → 45% → 53%</strong> across the ladder, best on 9 of 14 tasks at one million hours. Lockbox Key Turning is the threshold case: 0% up to 100,000 hours, then 90%. Bottle Cap Untwisting was post-trained on roughly <strong>10 minutes</strong> of demonstrations and still climbed to 50%.</p>



<p class="wp-block-paragraph">Against Dyna-1 — the company&#8217;s production VLA initialized from Qwen3-VL-4B — an early Dyna-2 reached 1.55× success rate and 1.12× grade, pooled over 7 tasks and 3 checkpoints. At unseen customer sites, Dyna-2 passed production criteria 87% versus Dyna-1&#8217;s 46%, though both pass near 100% in house. A distillation pipeline also cuts video sampling from 10,203 ms to 110 ms on one H100.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive explainer</strong></h2>



<div id="mtp-dyna2-wrap" style="margin:28px 0;">
<iframe id="mtp-dyna2-frame" style="width:100%;height:1180px;border:1px solid #1E2226;border-radius:4px;background:#08090A;display:block;" scrolling="no" loading="lazy" title="Dyna-2 interactive explainer" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;Dyna-2 Interactive Explainer&lt;/title&gt;
&lt;style&gt;
  *{box-sizing:border-box;margin:0;padding:0}
  body{background:#08090A;color:#EDEAE3;font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;padding:20px 16px 26px;-webkit-font-smoothing:antialiased}
  .wrap{max-width:940px;margin:0 auto}
  .eyebrow{font-size:10px;letter-spacing:.22em;color:#7E8489;text-transform:uppercase;margin-bottom:10px}
  h1{font-size:19px;font-weight:600;letter-spacing:-.01em;line-height:1.35;margin-bottom:6px}
  .sub{font-size:12.5px;color:#8A8F94;line-height:1.6;margin-bottom:18px;max-width:660px}
  .tabs{display:flex;flex-wrap:wrap;gap:6px;border-bottom:1px solid #1E2226;padding-bottom:12px;margin-bottom:18px}
  .tab{background:transparent;border:1px solid #1E2226;color:#8A8F94;font-family:inherit;font-size:11px;letter-spacing:.06em;padding:7px 11px;cursor:pointer;transition:all .22s;border-radius:2px;white-space:nowrap}
  .tab:hover{color:#EDEAE3;border-color:#3A4046}
  .tab.on{background:#EDEAE3;color:#08090A;border-color:#EDEAE3;font-weight:600}
  .panel{display:none;animation:fade .35s ease}
  .panel.on{display:block}
  @keyframes fade{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
  .lead{font-size:12.5px;line-height:1.75;color:#B9B5AE;margin-bottom:16px;max-width:700px}
  .lead b{color:#EDEAE3;font-weight:600}
  .card{background:#0D0F11;border:1px solid #1E2226;border-radius:3px;padding:16px 15px;margin-bottom:14px}
  .cardhd{font-size:10px;letter-spacing:.18em;color:#7E8489;text-transform:uppercase;margin-bottom:14px}
  /* ladder control */
  .ladder{display:flex;gap:6px;margin-bottom:16px;flex-wrap:wrap}
  .rung{flex:1;min-width:64px;background:transparent;border:1px solid #1E2226;color:#7E8489;font-family:inherit;font-size:11px;padding:9px 4px;cursor:pointer;transition:all .22s;border-radius:2px}
  .rung:hover{border-color:#3A4046;color:#EDEAE3}
  .rung.on{background:rgba(232,184,75,.1);border-color:#E8B84B;color:#E8B84B;font-weight:600}
  .play{background:transparent;border:1px solid #1E2226;color:#7E8489;font-family:inherit;font-size:10.5px;letter-spacing:.1em;padding:9px 14px;cursor:pointer;border-radius:2px;transition:all .22s}
  .play:hover{border-color:#E8B84B;color:#E8B84B}
  /* metric grid */
  .mgrid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}
  .metric{background:#08090A;border:1px solid #1E2226;border-radius:2px;padding:11px 10px}
  .mlab{font-size:9.5px;letter-spacing:.1em;color:#7E8489;text-transform:uppercase;margin-bottom:7px;line-height:1.4;min-height:24px}
  .mval{font-size:20px;font-weight:600;color:#EDEAE3;font-variant-numeric:tabular-nums;transition:color .3s}
  .mval.good{color:#E8B84B}
  .mbar{height:3px;background:#1E2226;margin-top:9px;border-radius:2px;overflow:hidden}
  .mfill{height:100%;background:#E8B84B;width:0;transition:width .7s cubic-bezier(.4,0,.2,1)}
  .mnote{font-size:9.5px;color:#5F6469;margin-top:7px;letter-spacing:.03em}
  /* bars */
  .brow{display:flex;align-items:center;gap:10px;margin-bottom:9px}
  .blab{width:112px;flex-shrink:0;font-size:10.5px;color:#8A8F94;text-align:right;letter-spacing:.02em}
  .btrack{flex:1;height:20px;background:#08090A;border:1px solid #1E2226;border-radius:2px;position:relative;overflow:hidden}
  .bfill{height:100%;width:0;transition:width .8s cubic-bezier(.4,0,.2,1);border-radius:1px}
  .bnum{width:56px;flex-shrink:0;font-size:11px;color:#EDEAE3;font-variant-numeric:tabular-nums;text-align:right;font-weight:600}
  /* svg arch */
  .archwrap{overflow-x:auto;-webkit-overflow-scrolling:touch}
  svg{display:block;min-width:560px}
  .flow{stroke-dasharray:5 5;animation:dash 1.4s linear infinite}
  @keyframes dash{to{stroke-dashoffset:-20}}
  .dim{opacity:.16;transition:opacity .4s}
  .lit{opacity:1;transition:opacity .4s}
  .toggle{display:flex;gap:6px;margin-bottom:14px}
  /* callout */
  .callout{border-left:2px solid #E8B84B;padding:10px 0 10px 13px;font-size:11.5px;line-height:1.7;color:#B9B5AE;margin-top:14px}
  .callout b{color:#E8B84B;font-weight:600}
  .legend{display:flex;gap:16px;flex-wrap:wrap;font-size:10px;color:#7E8489;margin-top:12px}
  .dot{display:inline-block;width:8px;height:8px;border-radius:1px;margin-right:6px;vertical-align:middle}
  .src{font-size:9.5px;color:#5F6469;line-height:1.6;border-top:1px solid #1E2226;padding-top:12px;margin-top:18px}
  .src a{color:#8A8F94;text-decoration:none;border-bottom:1px solid #2A2F34}
  .src a:hover{color:#E8B84B;border-color:#E8B84B}
  @media(max-width:640px){
    body{padding:16px 12px 22px}
    h1{font-size:16px}
    .mgrid{grid-template-columns:repeat(2,1fr)}
    .blab{width:82px;font-size:9.5px}
    .bnum{width:46px;font-size:10px}
    .tab{font-size:10px;padding:6px 9px}
    .mval{font-size:17px}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot;&gt;
  &lt;div class=&quot;eyebrow&quot;&gt;[ Interactive Explainer ]&lt;/div&gt;
  &lt;h1&gt;Dyna-2: what a 1,000,000-hour world-action model actually changes&lt;/h1&gt;
  &lt;p class=&quot;sub&quot;&gt;Move the controls. Every number below is read directly from the Dyna Robotics technical report.&lt;/p&gt;

  &lt;div class=&quot;tabs&quot;&gt;
    &lt;button class=&quot;tab on&quot; data-p=&quot;0&quot;&gt;01 · Architecture&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-p=&quot;1&quot;&gt;02 · Human scaling law&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-p=&quot;2&quot;&gt;03 · Human → robot&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-p=&quot;3&quot;&gt;04 · Video as an axis&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-p=&quot;4&quot;&gt;05 · WAM vs VLA&lt;/button&gt;
  &lt;/div&gt;

  &lt;!-- 01 ARCHITECTURE --&gt;
  &lt;div class=&quot;panel on&quot; id=&quot;p0&quot;&gt;
    &lt;p class=&quot;lead&quot;&gt;Dyna-2 is a &lt;b&gt;world-action model (WAM)&lt;/b&gt;: one generative model that denoises future video and a future action chunk, built on a video-diffusion backbone. Video and action each get their own DiT stack. During training the video loss shapes the shared trunk. At inference the action stream never reads the noised video latent, so the policy stays &lt;b&gt;reactive&lt;/b&gt;.&lt;/p&gt;
    &lt;div class=&quot;toggle&quot;&gt;
      &lt;button class=&quot;rung on&quot; id=&quot;tTrain&quot;&gt;TRAINING&lt;/button&gt;
      &lt;button class=&quot;rung&quot; id=&quot;tInfer&quot;&gt;INFERENCE&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;card&quot;&gt;
      &lt;div class=&quot;cardhd&quot; id=&quot;archHd&quot;&gt;Training — joint video + action denoising&lt;/div&gt;
      &lt;div class=&quot;archwrap&quot;&gt;
      &lt;svg viewBox=&quot;0 0 620 250&quot; xmlns=&quot;http://www.w3.org/2000/svg&quot;&gt;
        &lt;defs&gt;
          &lt;marker id=&quot;ah&quot; markerWidth=&quot;7&quot; markerHeight=&quot;7&quot; refX=&quot;6&quot; refY=&quot;3&quot; orient=&quot;auto&quot;&gt;
            &lt;path d=&quot;M0,0 L6,3 L0,6 z&quot; fill=&quot;#5F6469&quot;/&gt;
          &lt;/marker&gt;
          &lt;marker id=&quot;ahg&quot; markerWidth=&quot;7&quot; markerHeight=&quot;7&quot; refX=&quot;6&quot; refY=&quot;3&quot; orient=&quot;auto&quot;&gt;
            &lt;path d=&quot;M0,0 L6,3 L0,6 z&quot; fill=&quot;#E8B84B&quot;/&gt;
          &lt;/marker&gt;
        &lt;/defs&gt;
        &lt;!-- inputs --&gt;
        &lt;rect x=&quot;8&quot; y=&quot;24&quot; width=&quot;104&quot; height=&quot;30&quot; fill=&quot;#0D0F11&quot; stroke=&quot;#2A2F34&quot; rx=&quot;2&quot;/&gt;
        &lt;text x=&quot;60&quot; y=&quot;43&quot; fill=&quot;#B9B5AE&quot; font-size=&quot;10&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;Context frames&lt;/text&gt;
        &lt;rect x=&quot;8&quot; y=&quot;66&quot; width=&quot;104&quot; height=&quot;30&quot; fill=&quot;#0D0F11&quot; stroke=&quot;#2A2F34&quot; rx=&quot;2&quot;/&gt;
        &lt;text x=&quot;60&quot; y=&quot;85&quot; fill=&quot;#B9B5AE&quot; font-size=&quot;10&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;Instruction&lt;/text&gt;
        &lt;rect x=&quot;8&quot; y=&quot;150&quot; width=&quot;104&quot; height=&quot;30&quot; fill=&quot;#0D0F11&quot; stroke=&quot;#2A2F34&quot; rx=&quot;2&quot;/&gt;
        &lt;text x=&quot;60&quot; y=&quot;169&quot; fill=&quot;#B9B5AE&quot; font-size=&quot;10&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;Proprioception&lt;/text&gt;
        &lt;rect x=&quot;8&quot; y=&quot;192&quot; width=&quot;104&quot; height=&quot;30&quot; fill=&quot;#0D0F11&quot; stroke=&quot;#2A2F34&quot; rx=&quot;2&quot;/&gt;
        &lt;text x=&quot;60&quot; y=&quot;211&quot; fill=&quot;#B9B5AE&quot; font-size=&quot;10&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;Noised action a&lt;/text&gt;
        &lt;g id=&quot;gVidIn&quot;&gt;&lt;rect x=&quot;8&quot; y=&quot;108&quot; width=&quot;104&quot; height=&quot;30&quot; fill=&quot;#0D0F11&quot; stroke=&quot;#2A2F34&quot; rx=&quot;2&quot;/&gt;
        &lt;text x=&quot;60&quot; y=&quot;127&quot; fill=&quot;#B9B5AE&quot; font-size=&quot;10&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;Noised video z&lt;/text&gt;&lt;/g&gt;

        &lt;!-- video transformer --&gt;
        &lt;g id=&quot;gVidT&quot;&gt;
          &lt;rect x=&quot;196&quot; y=&quot;24&quot; width=&quot;150&quot; height=&quot;114&quot; fill=&quot;#0D0F11&quot; stroke=&quot;#E8B84B&quot; rx=&quot;2&quot;/&gt;
          &lt;text x=&quot;271&quot; y=&quot;72&quot; fill=&quot;#E8B84B&quot; font-size=&quot;11&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot; font-weight=&quot;600&quot;&gt;VIDEO DiT&lt;/text&gt;
          &lt;text x=&quot;271&quot; y=&quot;90&quot; fill=&quot;#7E8489&quot; font-size=&quot;9&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;causal mask&lt;/text&gt;
          &lt;text x=&quot;271&quot; y=&quot;105&quot; fill=&quot;#7E8489&quot; font-size=&quot;9&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;cross-attn to text&lt;/text&gt;
        &lt;/g&gt;
        &lt;!-- action transformer --&gt;
        &lt;rect x=&quot;196&quot; y=&quot;150&quot; width=&quot;150&quot; height=&quot;72&quot; fill=&quot;#0D0F11&quot; stroke=&quot;#EDEAE3&quot; rx=&quot;2&quot;/&gt;
        &lt;text x=&quot;271&quot; y=&quot;180&quot; fill=&quot;#EDEAE3&quot; font-size=&quot;11&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot; font-weight=&quot;600&quot;&gt;ACTION DiT&lt;/text&gt;
        &lt;text x=&quot;271&quot; y=&quot;196&quot; fill=&quot;#7E8489&quot; font-size=&quot;9&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;bidirectional attn&lt;/text&gt;
        &lt;text x=&quot;271&quot; y=&quot;210&quot; fill=&quot;#7E8489&quot; font-size=&quot;9&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;shallower · early join&lt;/text&gt;

        &lt;!-- input arrows --&gt;
        &lt;line x1=&quot;112&quot; y1=&quot;39&quot; x2=&quot;192&quot; y2=&quot;60&quot; stroke=&quot;#5F6469&quot; stroke-width=&quot;1&quot; marker-end=&quot;url(#ah)&quot;/&gt;
        &lt;line x1=&quot;112&quot; y1=&quot;81&quot; x2=&quot;192&quot; y2=&quot;76&quot; stroke=&quot;#5F6469&quot; stroke-width=&quot;1&quot; marker-end=&quot;url(#ah)&quot;/&gt;
        &lt;g id=&quot;gVidArrow&quot;&gt;&lt;line x1=&quot;112&quot; y1=&quot;123&quot; x2=&quot;192&quot; y2=&quot;100&quot; stroke=&quot;#5F6469&quot; stroke-width=&quot;1&quot; marker-end=&quot;url(#ah)&quot;/&gt;&lt;/g&gt;
        &lt;line x1=&quot;112&quot; y1=&quot;165&quot; x2=&quot;192&quot; y2=&quot;176&quot; stroke=&quot;#5F6469&quot; stroke-width=&quot;1&quot; marker-end=&quot;url(#ah)&quot;/&gt;
        &lt;line x1=&quot;112&quot; y1=&quot;207&quot; x2=&quot;192&quot; y2=&quot;196&quot; stroke=&quot;#5F6469&quot; stroke-width=&quot;1&quot; marker-end=&quot;url(#ah)&quot;/&gt;

        &lt;!-- shared trunk link --&gt;
        &lt;g id=&quot;gTrunk&quot;&gt;
          &lt;line class=&quot;flow&quot; x1=&quot;271&quot; y1=&quot;138&quot; x2=&quot;271&quot; y2=&quot;148&quot; stroke=&quot;#E8B84B&quot; stroke-width=&quot;1.4&quot; marker-end=&quot;url(#ahg)&quot;/&gt;
          &lt;text x=&quot;286&quot; y=&quot;147&quot; fill=&quot;#E8B84B&quot; font-size=&quot;8.5&quot; font-family=&quot;monospace&quot;&gt;shared trunk&lt;/text&gt;
        &lt;/g&gt;
        &lt;!-- context video tokens always feed action --&gt;
        &lt;path id=&quot;ctxPath&quot; class=&quot;flow&quot; d=&quot;M196,54 C170,54 170,164 196,164&quot; fill=&quot;none&quot; stroke=&quot;#8A8F94&quot; stroke-width=&quot;1.2&quot; marker-end=&quot;url(#ah)&quot;/&gt;
        &lt;text x=&quot;118&quot; y=&quot;115&quot; fill=&quot;#8A8F94&quot; font-size=&quot;8.5&quot; font-family=&quot;monospace&quot; id=&quot;ctxLbl&quot;&gt;observed context&lt;/text&gt;

        &lt;!-- outputs --&gt;
        &lt;g id=&quot;gVidOut&quot;&gt;
          &lt;rect x=&quot;404&quot; y=&quot;52&quot; width=&quot;140&quot; height=&quot;34&quot; fill=&quot;#0D0F11&quot; stroke=&quot;#E8B84B&quot; rx=&quot;2&quot;/&gt;
          &lt;text x=&quot;474&quot; y=&quot;67&quot; fill=&quot;#E8B84B&quot; font-size=&quot;10&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;Predicted future&lt;/text&gt;
          &lt;text x=&quot;474&quot; y=&quot;79&quot; fill=&quot;#E8B84B&quot; font-size=&quot;10&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot;&gt;video&lt;/text&gt;
          &lt;line class=&quot;flow&quot; x1=&quot;346&quot; y1=&quot;69&quot; x2=&quot;400&quot; y2=&quot;69&quot; stroke=&quot;#E8B84B&quot; stroke-width=&quot;1.4&quot; marker-end=&quot;url(#ahg)&quot;/&gt;
        &lt;/g&gt;
        &lt;rect x=&quot;404&quot; y=&quot;169&quot; width=&quot;140&quot; height=&quot;34&quot; fill=&quot;#0D0F11&quot; stroke=&quot;#EDEAE3&quot; rx=&quot;2&quot;/&gt;
        &lt;text x=&quot;474&quot; y=&quot;191&quot; fill=&quot;#EDEAE3&quot; font-size=&quot;10&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot; font-weight=&quot;600&quot;&gt;Action chunk&lt;/text&gt;
        &lt;line class=&quot;flow&quot; x1=&quot;346&quot; y1=&quot;186&quot; x2=&quot;400&quot; y2=&quot;186&quot; stroke=&quot;#EDEAE3&quot; stroke-width=&quot;1.4&quot; marker-end=&quot;url(#ah)&quot;/&gt;
        &lt;text x=&quot;474&quot; y=&quot;222&quot; fill=&quot;#7E8489&quot; font-size=&quot;8.5&quot; font-family=&quot;monospace&quot; text-anchor=&quot;middle&quot; id=&quot;outNote&quot;&gt;flow matching · velocity prediction&lt;/text&gt;
      &lt;/svg&gt;
      &lt;/div&gt;
      &lt;div class=&quot;callout&quot; id=&quot;archNote&quot;&gt;&lt;b&gt;Training.&lt;/b&gt; A video loss and an action loss share one trunk, fit as two separate marginal velocity fields under flow matching.&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- 02 HUMAN SCALING --&gt;
  &lt;div class=&quot;panel&quot; id=&quot;p1&quot;&gt;
    &lt;p class=&quot;lead&quot;&gt;Nested subsets of exactly &lt;b&gt;1,000 / 10,000 / 100,000 / 1,000,000 hours&lt;/b&gt; were cut from the corpus, so a bigger budget only ever &lt;b&gt;adds&lt;/b&gt; data. A fixed, disjoint 100-hour validation set scores every rung. All four metrics improve monotonically and fit a power law in hours.&lt;/p&gt;
    &lt;div class=&quot;ladder&quot; id=&quot;lad1&quot;&gt;&lt;/div&gt;
    &lt;button class=&quot;play&quot; id=&quot;play1&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/25b6.png" alt="▶" class="wp-smiley" style="height: 1em; max-height: 1em;" /> ANIMATE THE LADDER&lt;/button&gt;
    &lt;div class=&quot;card&quot; style=&quot;margin-top:14px&quot;&gt;
      &lt;div class=&quot;cardhd&quot;&gt;Held-out human data · rung &lt;span id=&quot;r1lbl&quot;&gt;1k&lt;/span&gt; hours&lt;/div&gt;
      &lt;div class=&quot;mgrid&quot;&gt;
        &lt;div class=&quot;metric&quot;&gt;&lt;div class=&quot;mlab&quot;&gt;Held-out MSE ↓&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;m1a&quot;&gt;0.062&lt;/div&gt;&lt;div class=&quot;mbar&quot;&gt;&lt;div class=&quot;mfill&quot; id=&quot;f1a&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mnote&quot;&gt;D&lt;sup&gt;-0.0184&lt;/sup&gt; · R²=0.919&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;metric&quot;&gt;&lt;div class=&quot;mlab&quot;&gt;Held-out L1 ↓&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;m1b&quot;&gt;0.140&lt;/div&gt;&lt;div class=&quot;mbar&quot;&gt;&lt;div class=&quot;mfill&quot; id=&quot;f1b&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mnote&quot;&gt;D&lt;sup&gt;-0.0132&lt;/sup&gt; · R²=0.879&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;metric&quot;&gt;&lt;div class=&quot;mlab&quot;&gt;Accuracy @0.1 ↑&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;m1c&quot;&gt;0.017&lt;/div&gt;&lt;div class=&quot;mbar&quot;&gt;&lt;div class=&quot;mfill&quot; id=&quot;f1c&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mnote&quot;&gt;D&lt;sup&gt;+0.0606&lt;/sup&gt; · R²=0.926&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;metric&quot;&gt;&lt;div class=&quot;mlab&quot;&gt;Accuracy @0.5 ↑&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;m1d&quot;&gt;0.40&lt;/div&gt;&lt;div class=&quot;mbar&quot;&gt;&lt;div class=&quot;mfill&quot; id=&quot;f1d&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mnote&quot;&gt;D&lt;sup&gt;+0.0203&lt;/sup&gt; · R²=0.865&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;callout&quot;&gt;Across the full ladder &lt;b&gt;accuracy@0.1 rises 51%&lt;/b&gt; while MSE improves 12%. Tight-threshold accuracy — a proxy for movement precision — is where scale pays most.&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- 03 HUMAN TO ROBOT --&gt;
  &lt;div class=&quot;panel&quot; id=&quot;p2&quot;&gt;
    &lt;p class=&quot;lead&quot;&gt;The same checkpoints were scored on &lt;b&gt;39 robot tasks&lt;/b&gt; across two stationary bimanual YAM platforms — 12 internal, 27 from xdof ABC — with &lt;b&gt;zero robot trajectories in pre-training&lt;/b&gt;. Then each rung was post-trained on 14 tasks, at most 10 hours of robot data each, robot data only, no human–robot alignment.&lt;/p&gt;
    &lt;div class=&quot;ladder&quot; id=&quot;lad2&quot;&gt;&lt;/div&gt;
    &lt;button class=&quot;play&quot; id=&quot;play2&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/25b6.png" alt="▶" class="wp-smiley" style="height: 1em; max-height: 1em;" /> ANIMATE THE LADDER&lt;/button&gt;
    &lt;div class=&quot;card&quot; style=&quot;margin-top:14px&quot;&gt;
      &lt;div class=&quot;cardhd&quot;&gt;Zero-shot on unseen robot data · rung &lt;span id=&quot;r2lbl&quot;&gt;1k&lt;/span&gt; hours&lt;/div&gt;
      &lt;div class=&quot;mgrid&quot; style=&quot;grid-template-columns:repeat(2,1fr)&quot;&gt;
        &lt;div class=&quot;metric&quot;&gt;&lt;div class=&quot;mlab&quot;&gt;Zero-shot action MSE ↓&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;m2a&quot;&gt;0.180&lt;/div&gt;&lt;div class=&quot;mbar&quot;&gt;&lt;div class=&quot;mfill&quot; id=&quot;f2a&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mnote&quot;&gt;D&lt;sup&gt;-0.0713&lt;/sup&gt; · R²=0.884&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;metric&quot;&gt;&lt;div class=&quot;mlab&quot;&gt;Zero-shot accuracy@0.5 ↑&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;m2b&quot;&gt;0.067&lt;/div&gt;&lt;div class=&quot;mbar&quot;&gt;&lt;div class=&quot;mfill&quot; id=&quot;f2b&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mnote&quot;&gt;D&lt;sup&gt;+0.139&lt;/sup&gt; · R²=0.918&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;card&quot;&gt;
      &lt;div class=&quot;cardhd&quot;&gt;Post-trained on the physical robot · 14 tasks, 3 embodiments&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Mean normalized&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b2m&quot; style=&quot;background:#E8B84B&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n2m&quot;&gt;20%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Lockbox key turn&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b2l&quot; style=&quot;background:#8A8F94&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n2l&quot;&gt;0%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Bottle cap untwist&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b2b&quot; style=&quot;background:#8A8F94&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n2b&quot;&gt;10%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Drink retrieval&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b2d&quot; style=&quot;background:#8A8F94&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n2d&quot;&gt;58%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;callout&quot;&gt;&lt;b&gt;Lockbox Key Turning&lt;/b&gt; is the threshold case: no checkpoint up to 100,000 hours ever turned the key. At one million hours it turned 90% of the time. &lt;b&gt;Bottle Cap Untwisting&lt;/b&gt; was post-trained on roughly 10 minutes of robot demonstrations, on two 20-DOF dexterous hands.&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- 04 VIDEO AXIS --&gt;
  &lt;div class=&quot;panel&quot; id=&quot;p3&quot;&gt;
    &lt;p class=&quot;lead&quot;&gt;Hold action-labelled human data &lt;b&gt;fixed at 50,000 hours&lt;/b&gt; and add only video-prediction data with no action labels. Zero-shot robot error keeps falling. Held-out &lt;i&gt;human&lt;/i&gt; error does not improve — the gain from video is specifically &lt;b&gt;cross-embodiment generalization&lt;/b&gt;.&lt;/p&gt;
    &lt;div class=&quot;ladder&quot; id=&quot;lad3&quot;&gt;&lt;/div&gt;
    &lt;button class=&quot;play&quot; id=&quot;play3&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/25b6.png" alt="▶" class="wp-smiley" style="height: 1em; max-height: 1em;" /> ANIMATE THE VIDEO AXIS&lt;/button&gt;
    &lt;div class=&quot;card&quot; style=&quot;margin-top:14px&quot;&gt;
      &lt;div class=&quot;cardhd&quot;&gt;Video-only hours added · action data fixed at 50k hr&lt;/div&gt;
      &lt;div class=&quot;metric&quot; style=&quot;margin-bottom:14px&quot;&gt;&lt;div class=&quot;mlab&quot;&gt;Zero-shot robot action MSE ↓&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;m3a&quot;&gt;0.340&lt;/div&gt;&lt;div class=&quot;mbar&quot;&gt;&lt;div class=&quot;mfill&quot; id=&quot;f3a&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mnote&quot;&gt;0 → 50k video hours drives 0.340 → 0.120&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Robot (unseen)&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b3r&quot; style=&quot;background:#E8B84B&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n3r&quot;&gt;100%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Human (in-domain)&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b3h&quot; style=&quot;background:#8A8F94&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n3h&quot;&gt;100%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;legend&quot;&gt;&lt;span&gt;&lt;span class=&quot;dot&quot; style=&quot;background:#E8B84B&quot;&gt;&lt;/span&gt;Robot error, % of each domain's own 0-video arm (lower = better)&lt;/span&gt;&lt;span&gt;&lt;span class=&quot;dot&quot; style=&quot;background:#8A8F94&quot;&gt;&lt;/span&gt;Human error, same normalization&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;callout&quot;&gt;At 50k video hours the robot arm falls to &lt;b&gt;34%&lt;/b&gt; of its no-video baseline while the human arm sits at &lt;b&gt;104%&lt;/b&gt; — slightly worse. Repeating the experiment at 250k action hours with 0 → 750k video hours moves MSE from 0.10 to 0.084.&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;card&quot;&gt;
      &lt;div class=&quot;cardhd&quot;&gt;Objective ablation · zero-shot on all 39 robot tasks&lt;/div&gt;
      &lt;p class=&quot;lead&quot; style=&quot;margin-bottom:12px;font-size:11.5px&quot;&gt;Joint denoising beat action-only on &lt;b&gt;39 of 39 tasks at every action scale&lt;/b&gt;. Action-only overfits badly as data grows. Only the video co-training recipe keeps improving as action data scales.&lt;/p&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- 05 WAM VS VLA --&gt;
  &lt;div class=&quot;panel&quot; id=&quot;p4&quot;&gt;
    &lt;p class=&quot;lead&quot;&gt;An early Dyna-2 was compared against &lt;b&gt;Dyna-1&lt;/b&gt;, Dyna's production VLA initialized from Qwen3-VL-4B, under matched pre-training data, post-training data and hyperparameters, from three distinct pre-training checkpoints each. Dyna Robotics frames this as a &lt;b&gt;lower bound&lt;/b&gt; for the WAM: the pipeline was tuned for the VLA.&lt;/p&gt;
    &lt;button class=&quot;play&quot; id=&quot;play4&quot;&gt;<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/25b6.png" alt="▶" class="wp-smiley" style="height: 1em; max-height: 1em;" /> RUN THE COMPARISON&lt;/button&gt;
    &lt;div class=&quot;card&quot; style=&quot;margin-top:14px&quot;&gt;
      &lt;div class=&quot;cardhd&quot;&gt;Pooled over 7 tasks × 3 checkpoints · VLA normalized to 1.00×&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;VLA success&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b4a&quot; style=&quot;background:#4A4F54&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n4a&quot;&gt;1.00×&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;WAM success&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b4b&quot; style=&quot;background:#E8B84B&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n4b&quot;&gt;1.55×&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;VLA grade&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b4c&quot; style=&quot;background:#4A4F54&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n4c&quot;&gt;1.00×&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;WAM grade&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b4d&quot; style=&quot;background:#E8B84B&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n4d&quot;&gt;1.12×&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;callout&quot;&gt;Head-to-head, fixing checkpoint step and task, the early Dyna-2 won &lt;b&gt;65%&lt;/b&gt;, Dyna-1 won 29%, 6% tied.&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;card&quot;&gt;
      &lt;div class=&quot;cardhd&quot;&gt;Zero-shot at real customer sites · production pass criteria&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Dyna-1 in-house&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b5a&quot; style=&quot;background:#4A4F54&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n5a&quot;&gt;100%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Dyna-2 in-house&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b5b&quot; style=&quot;background:#4A4F54&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n5b&quot;&gt;100%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Dyna-1 on site&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b5c&quot; style=&quot;background:#8A8F94&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n5c&quot;&gt;46%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Dyna-2 on site&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b5d&quot; style=&quot;background:#E8B84B&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n5d&quot;&gt;87%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;callout&quot;&gt;Both models pass near 100% in house. At customer sites neither had seen, the gap is &lt;b&gt;41 points&lt;/b&gt; at identical post-training budget. Graded by on-site operators not involved in model development.&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;card&quot;&gt;
      &lt;div class=&quot;cardhd&quot;&gt;One-step video generation · single H100&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Teacher, 100 NFE&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b6a&quot; style=&quot;background:#4A4F54&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n6a&quot;&gt;10203ms&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;brow&quot;&gt;&lt;div class=&quot;blab&quot;&gt;Distilled, 1 NFE&lt;/div&gt;&lt;div class=&quot;btrack&quot;&gt;&lt;div class=&quot;bfill&quot; id=&quot;b6b&quot; style=&quot;background:#E8B84B&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;bnum&quot; id=&quot;n6b&quot;&gt;110ms&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;callout&quot;&gt;About &lt;b&gt;90× faster&lt;/b&gt; for a three-second, three-view manipulation video. The one-step student holds FVD at 121 and flicker at 1.94 against 2.37 for real recorded footage; motion sits at 75% of real, where it still trails the full teacher.&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;src&quot;&gt;
    Every figure above is read from the Dyna Robotics technical report, &lt;a href=&quot;https://www.dyna.co/dyna-2&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Dyna-2: A 1-Million-Hour Scaling Law for World-Action Models&lt;/a&gt; (August 2026). Interactive explainer built by &lt;a href=&quot;https://www.marktechpost.com&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Marktechpost&lt;/a&gt;. No values are estimated or interpolated.
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var LAD=['1k','10k','100k','1M'];
  var D={
    h:{mse:[0.062,0.057,0.056,0.054],l1:[0.140,0.131,0.129,0.127],a1:[0.017,0.021,0.024,0.026],a5:[0.40,0.44,0.45,0.47]},
    r:{mse:[0.180,0.174,0.124,0.117],a5:[0.067,0.074,0.136,0.159]},
    p:{mean:[20,28,45,53],lock:[0,0,0,90],cap:[10,10,40,50],drink:[58,75,83,83]}
  };
  var VID=['0','1k','10k','50k'];
  var V={mse:[0.340,0.245,0.170,0.120],robot:[100,72,50,34],human:[100,101,103,104]};

  function $(id){return document.getElementById(id);}
  function pct(v,lo,hi){var p=(v-lo)/(hi-lo)*100;return Math.max(3,Math.min(100,p));}
  function inv(v,lo,hi){return pct(hi-v+lo,lo,hi);}

  // tabs
  var tabs=document.querySelectorAll('.tab');
  for(var i=0;i&lt;tabs.length;i++){
    tabs[i].addEventListener('click',function(){
      for(var j=0;j&lt;tabs.length;j++){tabs[j].classList.remove('on');document.getElementById('p'+j).classList.remove('on');}
      this.classList.add('on');$('p'+this.getAttribute('data-p')).classList.add('on');
      setTimeout(resize,60);
    });
  }

  // architecture toggle
  var vidIds=['gVidT','gVidIn','gVidArrow','gVidOut','gTrunk'];
  function setArch(train){
    for(var i=0;i&lt;vidIds.length;i++){
      var el=$(vidIds[i]); if(!el) continue;
      el.setAttribute('class', train?'lit':'dim');
    }
    $('tTrain').classList.toggle('on',train);
    $('tInfer').classList.toggle('on',!train);
    $('archHd').textContent = train ? 'Training — joint video + action denoising' : 'Inference — reactive policy, video branch unused';
    $('outNote').textContent = train ? 'flow matching · velocity prediction' : 'no future video generated or attended';
    $('archNote').innerHTML = train
      ? '&lt;b&gt;Training.&lt;/b&gt; A video loss and an action loss share one trunk, fit as two separate marginal velocity fields under flow matching.'
      : '&lt;b&gt;Inference.&lt;/b&gt; The action network never takes the noised video latent as an argument. It still attends to &lt;b&gt;context&lt;/b&gt; video tokens, so the policy stays reactive and real-time.';
    setTimeout(resize,60);
  }
  $('tTrain').addEventListener('click',function(){setArch(true);});
  $('tInfer').addEventListener('click',function(){setArch(false);});

  // ladder builders
  function buildLadder(host,labels,onPick){
    var h=$(host); h.innerHTML='';
    labels.forEach(function(l,i){
      var b=document.createElement('button');
      b.className='rung'+(i===0?' on':''); b.textContent=l;
      b.addEventListener('click',function(){
        var kids=h.querySelectorAll('.rung');
        for(var k=0;k&lt;kids.length;k++)kids[k].classList.remove('on');
        b.classList.add('on'); onPick(i);
      });
      h.appendChild(b);
    });
  }
  function selectRung(host,i){
    var kids=$(host).querySelectorAll('.rung');
    for(var k=0;k&lt;kids.length;k++)kids[k].classList.toggle('on',k===i);
  }

  // panel 1
  function render1(i){
    $('r1lbl').textContent=LAD[i];
    $('m1a').textContent=D.h.mse[i].toFixed(3); $('f1a').style.width=inv(D.h.mse[i],0.050,0.064)+'%';
    $('m1b').textContent=D.h.l1[i].toFixed(3);  $('f1b').style.width=inv(D.h.l1[i],0.124,0.142)+'%';
    $('m1c').textContent=D.h.a1[i].toFixed(3);  $('f1c').style.width=pct(D.h.a1[i],0.014,0.028)+'%';
    $('m1d').textContent=D.h.a5[i].toFixed(2);  $('f1d').style.width=pct(D.h.a5[i],0.38,0.48)+'%';
    ['m1a','m1b','m1c','m1d'].forEach(function(id){$(id).classList.toggle('good',i===3);});
  }
  buildLadder('lad1',LAD,render1); render1(0);

  // panel 2
  function render2(i){
    $('r2lbl').textContent=LAD[i];
    $('m2a').textContent=D.r.mse[i].toFixed(3); $('f2a').style.width=inv(D.r.mse[i],0.110,0.190)+'%';
    $('m2b').textContent=D.r.a5[i].toFixed(3);  $('f2b').style.width=pct(D.r.a5[i],0.050,0.170)+'%';
    ['m2a','m2b'].forEach(function(id){$(id).classList.toggle('good',i===3);});
    $('b2m').style.width=D.p.mean[i]+'%'; $('n2m').textContent=D.p.mean[i]+'%';
    $('b2l').style.width=Math.max(1,D.p.lock[i])+'%'; $('n2l').textContent=D.p.lock[i]+'%';
    $('b2b').style.width=D.p.cap[i]+'%'; $('n2b').textContent=D.p.cap[i]+'%';
    $('b2d').style.width=D.p.drink[i]+'%'; $('n2d').textContent=D.p.drink[i]+'%';
    var hot=(i===3)?'#E8B84B':'#8A8F94';
    $('b2l').style.background=hot; $('b2b').style.background=(i&gt;=2)?'#E8B84B':'#8A8F94';
    $('b2d').style.background=(i&gt;=2)?'#E8B84B':'#8A8F94';
  }
  buildLadder('lad2',LAD,render2); render2(0);

  // panel 3
  function render3(i){
    $('m3a').textContent=V.mse[i].toFixed(3); $('f3a').style.width=inv(V.mse[i],0.100,0.350)+'%';
    $('m3a').classList.toggle('good',i===3);
    $('b3r').style.width=V.robot[i]+'%'; $('n3r').textContent=V.robot[i]+'%';
    $('b3h').style.width=V.human[i]+'%'; $('n3h').textContent=V.human[i]+'%';
  }
  buildLadder('lad3',VID,render3); render3(0);

  // panel 4/5 comparison
  function render4(on){
    $('b4a').style.width=on?'50%':'0'; $('b4b').style.width=on?'77.5%':'0';
    $('b4c').style.width=on?'50%':'0'; $('b4d').style.width=on?'56%':'0';
    $('b5a').style.width=on?'100%':'0'; $('b5b').style.width=on?'100%':'0';
    $('b5c').style.width=on?'46%':'0';  $('b5d').style.width=on?'87%':'0';
    $('b6a').style.width=on?'100%':'0'; $('b6b').style.width=on?'1.1%':'0';
  }
  render4(true);

  // autoplay helpers
  function autoplay(host,render,btn,n){
    var busy=false;
    $(btn).addEventListener('click',function(){
      if(busy)return; busy=true; var i=0;
      selectRung(host,0); render(0);
      var t=setInterval(function(){
        i++; if(i&gt;=n){clearInterval(t);busy=false;return;}
        selectRung(host,i); render(i);
      },900);
    });
  }
  autoplay('lad1',render1,'play1',4);
  autoplay('lad2',render2,'play2',4);
  autoplay('lad3',render3,'play3',4);
  $('play4').addEventListener('click',function(){render4(false);setTimeout(function(){render4(true);},260);});

  // resize
  function resize(){
    try{
      var h=document.body.offsetHeight+40;
      if(window.parent&amp;&amp;window.parent!==window){
        window.parent.postMessage({mtpFrame:'dyna2',height:h},'*');
      }
    }catch(e){}
  }
  window.addEventListener('load',function(){resize();setTimeout(resize,400);});
  window.addEventListener('resize',resize);
  setTimeout(resize,120);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
</div>
<script>
(function(){
  window.addEventListener("message",function(e){
    if(e.data&&e.data.mtpFrame==="dyna2"&&e.data.height){
      var f=document.getElementById("mtp-dyna2-frame");
      if(f){f.style.height=e.data.height+"px";}
    }
  });
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>Dyna-2 is a world-action model pre-trained on 1M+ hours of egocentric human video.</li>



<li>Scaling laws hold on held-out human data across four orders of magnitude.</li>



<li>For the first time, that law transfers zero-shot to robot data never seen in pre-training.</li>



<li>Video co-training — not action data — is what drives cross-embodiment generalization.</li>



<li>No weights, no API: deployment today means a vendor-operated Dyna robot cell.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"><strong>Read the full technical report:</strong> <a href="https://www.dyna.co/dyna-2" target="_blank" rel="noreferrer noopener"><strong>Dyna-2: A 1-Million-Hour Scaling Law for World-Action Models</strong></a> and <a href="https://x.com/DynaRobotics/status/2086856327150858298" target="_blank" rel="noreferrer noopener"><strong>Announcement</strong></a>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://magic.beehiiv.com/v1/f5e63dd4-5653-4f09-83e2-321a8b1ba526?email={{email}}" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/08/13/dyna-robotics-introduces-dyna-2-a-world-action-model-pre-trained-on-1-million-hours-of-human-video/">Dyna Robotics Introduces Dyna-2: A World-Action Model Pre-Trained on 1 Million Hours of Human Video</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81697</post-id>	</item>
		<item>
		<title>SpaceXAI Releases Grok 4.6: A 500K-Context Frontier Model Tuned for Long-Running Agents, Coding, and Knowledge Work</title>
		<link>https://www.marktechpost.com/2026/08/12/spacexai-releases-grok-4-6/</link>
		
		<dc:creator><![CDATA[Michal Sutter]]></dc:creator>
		<pubDate>Thu, 13 Aug 2026 06:18:14 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81694</guid>

					<description><![CDATA[<p>SpaceXAI released Grok 4.6 on August 12, 2026 — a post-training upgrade over Grok 4.5, not a larger base model. It ties GPT-5.6 Sol Max at 61 on the Artificial Analysis Intelligence Index, ships 500K context and a new xhigh reasoning level, and holds pricing at $2/$6 per million tokens. The coding benchmarks are where it still loses.</p>
<p>The post <a href="https://www.marktechpost.com/2026/08/12/spacexai-releases-grok-4-6/">SpaceXAI Releases Grok 4.6: A 500K-Context Frontier Model Tuned for Long-Running Agents, Coding, and Knowledge Work</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph"><a href="https://x.ai/">SpaceXAI</a> just released <a href="https://docs.x.ai/developers/grok-4-6">Grok 4.6</a>. The release is a post-training upgrade over <a href="https://docs.x.ai/developers/grok-4-5">Grok 4.5</a> rather than a larger base model. SpaceXAI held the foundation constant and spent the improvement on a longer supplemental training run, regenerated supervised fine-tuning trajectories, and reinforcement learning in agentic environments. Agents that stay on a task across many steps without drifting. Grok 4.6 scores 61 on the Artificial Analysis Intelligence Index, up five points from Grok 4.5 and tied with GPT-5.6 Sol Max. The model takes 500,000 context tokens, is live today in <a href="https://cursor.com/">Cursor</a> and <a href="https://docs.x.ai/build/overview">Grok Build</a>, and adds a new <code>xhigh</code> reasoning-effort level above the ladder Grok 4.5 shipped with.</p>



<h2 id="h-is-it-deployable" class="wp-block-heading"><strong>Is it deployable?</strong></h2>



<p class="wp-block-paragraph"><strong>Yes, in production, with a bounded set of workloads.</strong> The model is generally available through the <a href="https://console.x.ai/">xAI API</a> as <code>grok-4.6</code>, is the default model in <a href="https://docs.x.ai/build/overview">Grok Build</a>, ships in <a href="https://cursor.com/">Cursor</a> on all plans, and is routable via OpenRouter, Vercel, and Cloudflare. There is no open-weights release and no self-hosting path, so air-gapped deployments are out.</p>



<ul class="wp-block-list">
<li><strong>Company stage</strong>: Seed-stage teams and indie developers can adopt it immediately, since Cursor and Grok Build need no harness work. Mid-market engineering orgs are the strongest fit: API-only integration, mTLS authentication, batch and priority processing are documented. Regulated enterprises should stage a pilot first — the vendor&#8217;s brand history is a live procurement question in several buying committees.</li>



<li><strong>Industries</strong>: Software and developer tooling, semiconductor and kernel engineering, hardware and CAD-adjacent design, financial research, and legal analysis. The training mix explicitly targeted several of these.</li>



<li><strong>Applications</strong>: Repository-wide refactors, migration agents, research-and-synthesis pipelines over 500K-token corpora, first-pass application scaffolding from a product brief, GPU kernel optimization, and document-heavy knowledge work.</li>
</ul>



<h2 id="h-what-actually-changed" class="wp-block-heading">What actually changed</h2>



<p class="wp-block-paragraph">Grok 4.6 is not a larger base model. SpaceXAI describes a longer supplemental training run than Grok 4.5 received, using curated model-generated data for reasoning and advanced technical concepts, high-quality engineering data, and an improved optimizer and training recipe.</p>



<p class="wp-block-paragraph">Grok 4.5 was then used to regenerate supervised fine-tuning trajectories across reasoning-effort levels, agent harnesses, and domains spanning STEM, software engineering, and knowledge work, with problematic traces filtered by model-based checks. Reinforcement learning followed in agentic environments covering knowledge work, general coding, web development, computer-aided design, and kernel optimization.</p>



<p class="wp-block-paragraph">The behavioral insight is an important one: on longer trajectories, SpaceXAI reports more self-testing and verification, with the model checking its own work before moving on. That is a vendor observation from internal testing, not an independently measured result.</p>



<p class="wp-block-paragraph">The model takes 500,000 context tokens, accepts text and image input with text-only output, has no stated text output limit, and carries a February 1, 2026 knowledge cutoff. <code>reasoning_effort</code> now supports <code>low</code>, <code>medium</code>, <code>high</code> (default), and a new <code>xhigh</code> level. SpaceXAI did not publish a parameter count for Grok 4.6.</p>



<h2 id="h-benchmarks-read-the-losses-first" class="wp-block-heading"><strong>Benchmarks: read the losses first</strong></h2>



<p class="wp-block-paragraph">On xAI&#8217;s launch table, Grok 4.6 (High) scores <strong>61</strong> on the Artificial Analysis Intelligence Index, up from 56 for Grok 4.5 and tied with GPT-5.6 Sol Max. It leads the table on GDPval-AA v2 (1753 Elo, versus 1526 for Grok 4.5), AA-Briefcase (1577, versus 1313), and Harvey LAB.</p>



<p class="wp-block-paragraph">It trails on the coding rows that matter most to engineering teams. DeepSWE v1.1 lands at 65.9%, up 11.9 points generationally but behind GPT-5.6 Sol Max at 73%. Terminal-Bench v3.0 reaches 26%, nearly double Grok 4.5&#8217;s 15.7% and still last of the four listed models. CursorBench v3.2 is 69.9%, FrontierCode v1.1 Extended is 61.3%, and APEX-Agents is 57.5%.</p>



<p class="wp-block-paragraph">Two things to note while evaluating. First, the table&#8217;s bolded wins on GDPval-AA v2 and AA-Briefcase sit inside <a href="https://artificialanalysis.ai/">Artificial Analysis</a>&#8216; published confidence intervals — they are statistical ties, not leads. Second, the comparison set excludes Anthropic&#8217;s Claude Opus 5, which currently tops that index. The disclosed losses are the more reliable signal.</p>



<h2 id="h-pricing-and-access" class="wp-block-heading"><strong>Pricing and access</strong></h2>



<p class="wp-block-paragraph">Per the <a href="https://docs.x.ai/developers/release-notes">release notes</a>, Grok 4.6 bills $2 / $0.50 / $6 per 1M tokens (input / cached input / output) below 200K prompt tokens, and $4 / $1 / $12 above that threshold. The launch page also references a faster variant at double the price, with no separate model ID published. Grok Build and Cursor are offering 2× included usage for the first week.</p>



<p class="wp-block-paragraph">Teams should set a <code>prompt_cache_key</code> (or the <code>x-grok-conv-id</code> header on Chat Completions). Without it, requests scatter across servers and cache hits become unreliable, so full input price applies.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive explainer</strong></h2>



<iframe srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;Grok 4.6 Interactive Explainer&lt;/title&gt;
&lt;style&gt;
  :root{
    --bg:#000000; --panel:#0a0a0a; --panel2:#111111;
    --line:#242424; --line2:#333333;
    --ink:#f5f5f5; --mid:#a3a3a3; --muted:#6e6e6e;
    --hero:#ffffff; --g45:#4a4a4a; --rival:#6f6f6f; --rival2:#8f8f8f;
    --mono:ui-monospace,SFMono-Regular,Menlo,Consolas,&quot;Liberation Mono&quot;,monospace;
    --sans:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Inter,Helvetica,Arial,sans-serif;
  }
  *{box-sizing:border-box}
  html,body{margin:0;padding:0;background:var(--bg);}
  body{font-family:var(--sans);color:var(--ink);-webkit-font-smoothing:antialiased;}
  .wrap{max-width:920px;margin:0 auto;padding:0 0 4px;}

  /* ---------- header ---------- */
  .hd{border:1px solid var(--line);border-radius:14px;background:
      radial-gradient(120% 140% at 0% 0%, #161616 0%, #000 58%);padding:22px 22px 20px;}
  .eyebrow{font-family:var(--mono);font-size:10.5px;letter-spacing:.22em;text-transform:uppercase;
      color:var(--muted);display:flex;gap:10px;align-items:center;flex-wrap:wrap;}
  .dot{width:5px;height:5px;border-radius:50%;background:var(--hero);display:inline-block;
      animation:blip 2.6s ease-in-out infinite;}
  @keyframes blip{0%,100%{opacity:.25}50%{opacity:1}}
  h1{font-size:27px;line-height:1.15;margin:12px 0 8px;letter-spacing:-.028em;font-weight:640;}
  .sub{color:var(--mid);font-size:14px;line-height:1.55;margin:0;max-width:64ch;}
  .specs{display:grid;grid-template-columns:repeat(4,1fr);gap:1px;background:var(--line);
      border:1px solid var(--line);border-radius:10px;overflow:hidden;margin-top:18px;}
  .spec{background:var(--panel);padding:11px 12px;}
  .spec b{display:block;font-family:var(--mono);font-size:15px;letter-spacing:-.01em;}
  .spec span{display:block;font-family:var(--mono);font-size:9.5px;letter-spacing:.14em;
      text-transform:uppercase;color:var(--muted);margin-top:5px;}

  /* ---------- tabs ---------- */
  .tabs{display:flex;gap:6px;margin:16px 0 12px;flex-wrap:wrap;}
  .tab{font-family:var(--mono);font-size:11px;letter-spacing:.08em;text-transform:uppercase;
      background:var(--panel);color:var(--mid);border:1px solid var(--line);border-radius:999px;
      padding:8px 14px;cursor:pointer;transition:all .18s ease;}
  .tab:hover{border-color:var(--line2);color:var(--ink);}
  .tab[aria-selected=&quot;true&quot;]{background:var(--hero);color:#000;border-color:var(--hero);}
  .tab:focus-visible{outline:2px solid #fff;outline-offset:2px;}

  .panel{border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:20px;}
  .panel[hidden]{display:none;}
  .ttl{font-family:var(--mono);font-size:10.5px;letter-spacing:.2em;text-transform:uppercase;
      color:var(--muted);margin:0 0 4px;}
  .lede{font-size:13.5px;line-height:1.6;color:var(--mid);margin:0 0 16px;max-width:70ch;}
  .lede b{color:var(--ink);font-weight:600;}

  /* ---------- bench ---------- */
  .chips{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:16px;}
  .chip{font-family:var(--mono);font-size:10.5px;background:#0f0f0f;border:1px solid var(--line);
      color:var(--mid);padding:6px 10px;border-radius:6px;cursor:pointer;transition:.16s;}
  .chip:hover{color:var(--ink);border-color:var(--line2);}
  .chip[aria-pressed=&quot;true&quot;]{background:#1c1c1c;color:#fff;border-color:#555;}
  .chip:focus-visible{outline:2px solid #fff;outline-offset:2px;}
  .rows{display:flex;flex-direction:column;gap:11px;}
  .row{display:grid;grid-template-columns:132px 1fr 74px;align-items:center;gap:12px;}
  .rl{font-family:var(--mono);font-size:11px;color:var(--mid);white-space:nowrap;overflow:hidden;
      text-overflow:ellipsis;}
  .rl.hero{color:#fff;}
  .track{height:16px;background:#0f0f0f;border:1px solid var(--line);border-radius:4px;overflow:hidden;}
  .fill{height:100%;width:0;background:var(--rival);border-radius:3px 0 0 3px;
      transition:width .75s cubic-bezier(.22,1,.36,1);}
  .fill.hero{background:repeating-linear-gradient(135deg,#fff 0 6px,#dcdcdc 6px 12px);}
  .fill.prev{background:var(--g45);}
  .rv{font-family:var(--mono);font-size:12px;text-align:right;color:var(--mid);}
  .rv.hero{color:#fff;}
  .note{font-family:var(--mono);font-size:10.5px;line-height:1.65;color:var(--muted);
      border-top:1px solid var(--line);margin-top:16px;padding-top:12px;}

  /* ---------- agent run ---------- */
  .runbar{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-bottom:16px;}
  .btn{font-family:var(--mono);font-size:11px;letter-spacing:.06em;text-transform:uppercase;
      background:var(--hero);color:#000;border:1px solid var(--hero);border-radius:7px;
      padding:9px 15px;cursor:pointer;font-weight:600;}
  .btn.ghost{background:transparent;color:var(--mid);border-color:var(--line2);}
  .btn:focus-visible{outline:2px solid #fff;outline-offset:2px;}
  .lane{border:1px solid var(--line);border-radius:10px;background:var(--panel2);padding:14px;
      margin-bottom:10px;}
  .lanehd{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:10px;
      font-family:var(--mono);font-size:11px;color:var(--mid);gap:10px;}
  .lanehd .who{color:#fff;letter-spacing:.04em;}
  .tape{display:flex;gap:3px;height:26px;align-items:flex-end;}
  .st{flex:1;height:8px;background:#1a1a1a;border-radius:2px;transition:.28s ease;}
  .st.on{height:18px;background:var(--g45);}
  .st.on.hero{background:#fff;}
  .st.ver{height:26px;background:#fff;box-shadow:0 0 0 1px #000, 0 0 10px rgba(255,255,255,.55);}
  .st.dead{height:12px;background:#2b2b2b;}
  .stat{font-family:var(--mono);font-size:10.5px;color:var(--muted);margin-top:9px;
      display:flex;gap:14px;flex-wrap:wrap;}
  .stat b{color:var(--ink);font-weight:500;}

  /* ---------- cost ---------- */
  .ctrl{margin-bottom:15px;}
  .ctrl label{font-family:var(--mono);font-size:10.5px;letter-spacing:.14em;text-transform:uppercase;
      color:var(--muted);display:flex;justify-content:space-between;margin-bottom:7px;gap:10px;}
  .ctrl label b{color:#fff;letter-spacing:0;text-transform:none;font-size:12px;font-weight:500;}
  input[type=range]{width:100%;-webkit-appearance:none;appearance:none;height:3px;background:#2a2a2a;
      border-radius:2px;outline:none;}
  input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;width:16px;height:16px;
      border-radius:50%;background:#fff;cursor:pointer;border:2px solid #000;
      box-shadow:0 0 0 1px #fff;}
  input[type=range]::-moz-range-thumb{width:14px;height:14px;border-radius:50%;background:#fff;
      cursor:pointer;border:2px solid #000;}
  input[type=range]:focus-visible{outline:2px solid #fff;outline-offset:4px;}
  .tier{font-family:var(--mono);font-size:10.5px;padding:9px 12px;border:1px solid var(--line);
      border-radius:8px;background:#0f0f0f;color:var(--mid);margin:4px 0 16px;transition:.25s;}
  .tier.hi{border-color:#fff;color:#fff;background:#161616;}
  .out{display:grid;grid-template-columns:repeat(3,1fr);gap:1px;background:var(--line);
      border:1px solid var(--line);border-radius:10px;overflow:hidden;}
  .oc{background:var(--panel2);padding:14px 12px;}
  .oc b{display:block;font-family:var(--mono);font-size:19px;letter-spacing:-.02em;}
  .oc span{display:block;font-family:var(--mono);font-size:9.5px;letter-spacing:.14em;
      text-transform:uppercase;color:var(--muted);margin-top:6px;}

  /* ---------- effort ---------- */
  .steps{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;margin-bottom:16px;}
  .sbtn{font-family:var(--mono);font-size:11px;text-transform:uppercase;letter-spacing:.08em;
      background:#0f0f0f;border:1px solid var(--line);color:var(--mid);padding:12px 6px;
      border-radius:8px;cursor:pointer;transition:.18s;}
  .sbtn[aria-pressed=&quot;true&quot;]{background:#fff;color:#000;border-color:#fff;font-weight:600;}
  .sbtn:focus-visible{outline:2px solid #fff;outline-offset:2px;}
  .meter{height:6px;background:#151515;border-radius:3px;overflow:hidden;margin:6px 0 14px;}
  .meter i{display:block;height:100%;width:0;background:#fff;transition:width .5s cubic-bezier(.22,1,.36,1);}
  .mlab{font-family:var(--mono);font-size:10.5px;color:var(--muted);display:flex;
      justify-content:space-between;}
  #effortText{font-size:13.5px;line-height:1.6;color:var(--mid);margin:0;}

  .ft{font-family:var(--mono);font-size:10px;letter-spacing:.06em;color:var(--muted);
      text-align:center;padding:16px 8px 6px;line-height:1.7;}
  .ft a{color:var(--mid);text-decoration:none;border-bottom:1px solid #333;}
  .ft a:hover{color:#fff;}

  @media (max-width:640px){
    .hd{padding:18px 15px}
    h1{font-size:21px}
    .specs{grid-template-columns:repeat(2,1fr)}
    .panel{padding:15px}
    .row{grid-template-columns:96px 1fr 58px;gap:8px}
    .rl{font-size:10px}
    .out{grid-template-columns:1fr}
    .steps{grid-template-columns:repeat(2,1fr)}
    .tape{gap:2px}
  }
  @media (prefers-reduced-motion:reduce){
    *{animation:none!important;transition:none!important}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot;&gt;

  &lt;div class=&quot;hd&quot;&gt;
    &lt;div class=&quot;eyebrow&quot;&gt;&lt;span class=&quot;dot&quot;&gt;&lt;/span&gt; SpaceXAI &amp;middot; grok-4.6 &amp;middot; released August 12, 2026&lt;/div&gt;
    &lt;h1&gt;Grok 4.6, taken apart&lt;/h1&gt;
    &lt;p class=&quot;sub&quot;&gt;A post-training upgrade aimed at agents that keep going. Four views below: where it wins and loses, what &quot;long-running&quot; looks like step by step, what a run costs at each context tier, and what the reasoning dial does.&lt;/p&gt;
    &lt;div class=&quot;specs&quot;&gt;
      &lt;div class=&quot;spec&quot;&gt;&lt;b&gt;500K&lt;/b&gt;&lt;span&gt;context tokens&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;spec&quot;&gt;&lt;b&gt;$2 / $6&lt;/b&gt;&lt;span&gt;in / out per 1M&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;spec&quot;&gt;&lt;b&gt;Feb 1, 2026&lt;/b&gt;&lt;span&gt;knowledge cutoff&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;spec&quot;&gt;&lt;b&gt;4 levels&lt;/b&gt;&lt;span&gt;reasoning effort&lt;/span&gt;&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;tabs&quot; role=&quot;tablist&quot; aria-label=&quot;Grok 4.6 explainer views&quot;&gt;
    &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;true&quot;  aria-controls=&quot;p1&quot; id=&quot;t1&quot;&gt;01 &amp;nbsp;Benchmarks&lt;/button&gt;
    &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; aria-controls=&quot;p2&quot; id=&quot;t2&quot;&gt;02 &amp;nbsp;Agent run&lt;/button&gt;
    &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; aria-controls=&quot;p3&quot; id=&quot;t3&quot;&gt;03 &amp;nbsp;Cost&lt;/button&gt;
    &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; aria-controls=&quot;p4&quot; id=&quot;t4&quot;&gt;04 &amp;nbsp;Effort dial&lt;/button&gt;
  &lt;/div&gt;

  &lt;!-- 01 --&gt;
  &lt;section class=&quot;panel&quot; id=&quot;p1&quot; role=&quot;tabpanel&quot; aria-labelledby=&quot;t1&quot;&gt;
    &lt;p class=&quot;ttl&quot;&gt;01 / the scoreboard, wins and losses&lt;/p&gt;
    &lt;p class=&quot;lede&quot;&gt;Pick a benchmark. Bars animate to xAI's published launch-table figures. &lt;b&gt;Hatched white is Grok 4.6.&lt;/b&gt; Grok 4.5 is the dark bar behind it, so the generational jump is the part you can actually read without a confidence interval.&lt;/p&gt;
    &lt;div class=&quot;chips&quot; id=&quot;chips&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;rows&quot; id=&quot;rows&quot;&gt;&lt;/div&gt;
    &lt;p class=&quot;note&quot; id=&quot;bnote&quot;&gt;&lt;/p&gt;
  &lt;/section&gt;

  &lt;!-- 02 --&gt;
  &lt;section class=&quot;panel&quot; id=&quot;p2&quot; role=&quot;tabpanel&quot; aria-labelledby=&quot;t2&quot; hidden&gt;
    &lt;p class=&quot;ttl&quot;&gt;02 / what &quot;long-running&quot; means&lt;/p&gt;
    &lt;p class=&quot;lede&quot;&gt;Press run. Each tick is one step in an agent trajectory. &lt;b&gt;Tall white spikes are self-verification&lt;/b&gt; &amp;mdash; the model re-checking its own work before moving on. Grey stubs are dead ends. This is an illustration of xAI's described behaviour, not measured telemetry.&lt;/p&gt;
    &lt;div class=&quot;runbar&quot;&gt;
      &lt;button class=&quot;btn&quot; id=&quot;runBtn&quot;&gt;Run trajectory&lt;/button&gt;
      &lt;button class=&quot;btn ghost&quot; id=&quot;resetBtn&quot;&gt;Reset&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;lane&quot;&gt;
      &lt;div class=&quot;lanehd&quot;&gt;&lt;span class=&quot;who&quot;&gt;GROK 4.6&lt;/span&gt;&lt;span id=&quot;l46&quot;&gt;idle&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;tape&quot; id=&quot;tape46&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;stat&quot; id=&quot;s46&quot;&gt;&lt;span&gt;steps &lt;b&gt;0&lt;/b&gt;&lt;/span&gt;&lt;span&gt;verifications &lt;b&gt;0&lt;/b&gt;&lt;/span&gt;&lt;span&gt;recovered &lt;b&gt;0&lt;/b&gt;&lt;/span&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;lane&quot;&gt;
      &lt;div class=&quot;lanehd&quot;&gt;&lt;span class=&quot;who&quot; style=&quot;color:#8a8a8a&quot;&gt;GROK 4.5&lt;/span&gt;&lt;span id=&quot;l45&quot;&gt;idle&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;tape&quot; id=&quot;tape45&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;stat&quot; id=&quot;s45&quot;&gt;&lt;span&gt;steps &lt;b&gt;0&lt;/b&gt;&lt;/span&gt;&lt;span&gt;verifications &lt;b&gt;0&lt;/b&gt;&lt;/span&gt;&lt;span&gt;recovered &lt;b&gt;0&lt;/b&gt;&lt;/span&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;p class=&quot;note&quot;&gt;Measured signal behind the illustration: Artificial Analysis reports Grok 4.6 finished AA-Briefcase workloads in roughly 53 turns and about 0.5B input tokens on average, against roughly 103 turns and about 2B input tokens for Claude Opus 5 Max. Fewer turns is not the same as better work.&lt;/p&gt;
  &lt;/section&gt;

  &lt;!-- 03 --&gt;
  &lt;section class=&quot;panel&quot; id=&quot;p3&quot; role=&quot;tabpanel&quot; aria-labelledby=&quot;t3&quot; hidden&gt;
    &lt;p class=&quot;ttl&quot;&gt;03 / the 200K price cliff&lt;/p&gt;
    &lt;p class=&quot;lede&quot;&gt;Grok 4.6 bills two tiers. Below 200K prompt tokens it is &lt;b&gt;$2 / $0.50 / $6&lt;/b&gt; per 1M (input / cached input / output). Above 200K, every rate doubles to &lt;b&gt;$4 / $1 / $12&lt;/b&gt;. Drag the prompt slider past 200K and watch the whole bill re-rate.&lt;/p&gt;
    &lt;div class=&quot;ctrl&quot;&gt;
      &lt;label for=&quot;pin&quot;&gt;Prompt tokens per call &lt;b id=&quot;pinV&quot;&gt;50,000&lt;/b&gt;&lt;/label&gt;
      &lt;input type=&quot;range&quot; id=&quot;pin&quot; min=&quot;5000&quot; max=&quot;480000&quot; step=&quot;5000&quot; value=&quot;50000&quot;&gt;
    &lt;/div&gt;
    &lt;div class=&quot;tier&quot; id=&quot;tierBox&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;ctrl&quot;&gt;
      &lt;label for=&quot;pout&quot;&gt;Output tokens per call &lt;b id=&quot;poutV&quot;&gt;4,000&lt;/b&gt;&lt;/label&gt;
      &lt;input type=&quot;range&quot; id=&quot;pout&quot; min=&quot;500&quot; max=&quot;40000&quot; step=&quot;500&quot; value=&quot;4000&quot;&gt;
    &lt;/div&gt;
    &lt;div class=&quot;ctrl&quot;&gt;
      &lt;label for=&quot;pcache&quot;&gt;Share of prompt served from cache &lt;b id=&quot;pcacheV&quot;&gt;0%&lt;/b&gt;&lt;/label&gt;
      &lt;input type=&quot;range&quot; id=&quot;pcache&quot; min=&quot;0&quot; max=&quot;90&quot; step=&quot;5&quot; value=&quot;0&quot;&gt;
    &lt;/div&gt;
    &lt;div class=&quot;ctrl&quot;&gt;
      &lt;label for=&quot;pcalls&quot;&gt;Calls per day &lt;b id=&quot;pcallsV&quot;&gt;2,000&lt;/b&gt;&lt;/label&gt;
      &lt;input type=&quot;range&quot; id=&quot;pcalls&quot; min=&quot;100&quot; max=&quot;50000&quot; step=&quot;100&quot; value=&quot;2000&quot;&gt;
    &lt;/div&gt;
    &lt;div class=&quot;out&quot;&gt;
      &lt;div class=&quot;oc&quot;&gt;&lt;b id=&quot;oCall&quot;&gt;$0.00&lt;/b&gt;&lt;span&gt;per call&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;oc&quot;&gt;&lt;b id=&quot;oDay&quot;&gt;$0.00&lt;/b&gt;&lt;span&gt;per day&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;oc&quot;&gt;&lt;b id=&quot;oMonth&quot;&gt;$0.00&lt;/b&gt;&lt;span&gt;per 30 days&lt;/span&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;p class=&quot;note&quot;&gt;Arithmetic on xAI's published list rates only. It excludes the doubled-price fast variant, server-side tool call fees, priority processing, and retries &amp;mdash; all of which move a real bill. Set a prompt_cache_key or the cached rate will not apply.&lt;/p&gt;
  &lt;/section&gt;

  &lt;!-- 04 --&gt;
  &lt;section class=&quot;panel&quot; id=&quot;p4&quot; role=&quot;tabpanel&quot; aria-labelledby=&quot;t4&quot; hidden&gt;
    &lt;p class=&quot;ttl&quot;&gt;04 / reasoning_effort&lt;/p&gt;
    &lt;p class=&quot;lede&quot;&gt;Grok 4.6 adds &lt;b&gt;xhigh&lt;/b&gt; above the low / medium / high ladder Grok 4.5 shipped with. The parameter trades latency and output tokens for depth on hard steps. Tap a level.&lt;/p&gt;
    &lt;div class=&quot;steps&quot; id=&quot;steps&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;mlab&quot;&gt;&lt;span&gt;reasoning depth&lt;/span&gt;&lt;span id=&quot;dLab&quot;&gt;high&lt;/span&gt;&lt;/div&gt;
    &lt;div class=&quot;meter&quot;&gt;&lt;i id=&quot;mDepth&quot;&gt;&lt;/i&gt;&lt;/div&gt;
    &lt;div class=&quot;mlab&quot;&gt;&lt;span&gt;latency &amp;amp; output tokens&lt;/span&gt;&lt;span id=&quot;cLab&quot;&gt;baseline&lt;/span&gt;&lt;/div&gt;
    &lt;div class=&quot;meter&quot;&gt;&lt;i id=&quot;mCost&quot;&gt;&lt;/i&gt;&lt;/div&gt;
    &lt;p id=&quot;effortText&quot;&gt;&lt;/p&gt;
    &lt;p class=&quot;note&quot;&gt;high is the API default. Levels are a documented parameter; the depth and cost bars are a qualitative illustration, not published per-level measurements.&lt;/p&gt;
  &lt;/section&gt;

  &lt;p class=&quot;ft&quot;&gt;Figures from xAI's Grok 4.6 launch table and SpaceXAI developer docs, August 12, 2026.&lt;br&gt;Vendor-reported benchmarks unless noted. Built for &lt;a href=&quot;https://www.marktechpost.com&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Marktechpost&lt;/a&gt;.&lt;/p&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  &quot;use strict&quot;;

  /* ---------------- tabs ---------------- */
  var tabs = [].slice.call(document.querySelectorAll('.tab'));
  tabs.forEach(function(t){
    t.addEventListener('click', function(){
      tabs.forEach(function(x){
        x.setAttribute('aria-selected','false');
        document.getElementById(x.getAttribute('aria-controls')).hidden = true;
      });
      t.setAttribute('aria-selected','true');
      document.getElementById(t.getAttribute('aria-controls')).hidden = false;
      resize();
    });
  });

  /* ---------------- 01 benchmarks ---------------- */
  var B = [
    { k:'AA Intelligence Index', max:70, unit:'', dec:0,
      d:[['Grok 4.6',61,'hero'],['Grok 4.5',56,'prev'],['GPT-5.6 Sol Max',61,''],['Fable 5 Max',62,'']],
      n:'A composite average across nine evaluations. Grok 4.6 ties GPT-5.6 Sol Max at 61 and adds five points over Grok 4.5. xAI\u2019s own comparison table omits Claude Opus 5, which leads this index.' },
    { k:'GDPval-AA v2', max:1900, unit:' Elo', dec:0,
      d:[['Grok 4.6',1753,'hero'],['Grok 4.5',1526,'prev'],['GPT-5.6 Sol Max',1728,''],['Fable 5 Max',1741,'']],
      n:'Real-world knowledge-work tasks, scored as a Bradley-Terry Elo rating. The 227-point jump over Grok 4.5 is large. The lead over Fable 5 Max is inside Artificial Analysis\u2019 published confidence intervals \u2014 read it as a tie.' },
    { k:'AA-Briefcase', max:1750, unit:' Elo', dec:0,
      d:[['Grok 4.6',1577,'hero'],['Grok 4.5',1313,'prev'],['GPT-5.6 Sol Max',1502,''],['Fable 5 Max',1574,'']],
      n:'Long-horizon agentic office work. The 3-point gap over Fable 5 Max is statistically indistinguishable; the 264-point gain over Grok 4.5 is the number that matters here.' },
    { k:'CursorBench v3.2', max:80, unit:'%', dec:1,
      d:[['Grok 4.6',69.9,'hero'],['Grok 4.5',66.7,'prev'],['GPT-5.6 Sol Max',67.2,''],['Fable 5 Max',70.5,'']],
      n:'Cursor\u2019s in-editor coding evaluation. Grok 4.6 passes GPT-5.6 Sol Max and lands just under Fable 5 Max.' },
    { k:'DeepSWE v1.1', max:80, unit:'%', dec:1,
      d:[['Grok 4.6',65.9,'hero'],['Grok 4.5',54.0,'prev'],['GPT-5.6 Sol Max',73.0,''],['Fable 5 Max',70.0,'']],
      n:'The clearest disclosed loss. An 11.9-point generational gain still leaves Grok 4.6 more than seven points behind GPT-5.6 Sol Max on repository-level software engineering.' },
    { k:'FrontierCode v1.1 Ext.', max:75, unit:'%', dec:1,
      d:[['Grok 4.6',61.3,'hero'],['Grok 4.5',56.6,'prev'],['GPT-5.6 Sol Max',60.6,''],['Fable 5 Max',63.6,'']],
      n:'Narrow edge over GPT-5.6 Sol Max, behind Fable 5 Max. A sub-one-point margin on a coding benchmark is not a procurement signal on its own.' },
    { k:'APEX-Agents', max:70, unit:'%', dec:1,
      d:[['Grok 4.6',57.5,'hero'],['Grok 4.5',47.1,'prev'],['GPT-5.6 Sol Max',56.7,''],['Fable 5 Max',59.2,'']],
      n:'Mercor\u2019s agent evaluation. A 10.4-point generational gain, and the row where the long-agent training story shows up most cleanly.' },
    { k:'Terminal-Bench v3.0', max:45, unit:'%', dec:1,
      d:[['Grok 4.6',26.0,'hero'],['Grok 4.5',15.7,'prev'],['GPT-5.6 Sol Max',34.6,''],['Fable 5 Max',34.1,'']],
      n:'The weakest row. Grok 4.6 nearly doubles Grok 4.5 and is still last of the four on terminal-driven tasks. If your agent lives in a shell, test before you switch.' },
    { k:'APEX-SWE', max:70, unit:'%', dec:1,
      d:[['Grok 4.6',56.4,'hero'],['Fable 5 Max',58.8,'']],
      n:'xAI published no comparable GPT-5.6 Sol Max figure for this row, and no Grok 4.5 baseline.' }
  ];

  var chips = document.getElementById('chips');
  var rows  = document.getElementById('rows');
  var bnote = document.getElementById('bnote');
  var bi = 0;

  B.forEach(function(b, i){
    var c = document.createElement('button');
    c.className = 'chip'; c.type = 'button'; c.textContent = b.k;
    c.setAttribute('aria-pressed', i === 0 ? 'true' : 'false');
    c.addEventListener('click', function(){ bi = i; drawBench(); });
    chips.appendChild(c);
  });

  function fmt(v, dec, unit){
    var s = dec ? v.toFixed(dec) : String(Math.round(v));
    if (!dec &amp;&amp; v &gt;= 1000) s = v.toLocaleString('en-US');
    return s + unit;
  }

  function drawBench(){
    [].slice.call(chips.children).forEach(function(c,i){
      c.setAttribute('aria-pressed', i === bi ? 'true' : 'false');
    });
    var b = B[bi];
    rows.innerHTML = '';
    b.d.forEach(function(d){
      var r = document.createElement('div'); r.className = 'row';
      var isHero = d[2] === 'hero';
      r.innerHTML =
        '&lt;div class=&quot;rl' + (isHero ? ' hero' : '') + '&quot;&gt;' + d[0] + '&lt;/div&gt;' +
        '&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;fill ' + d[2] + '&quot;&gt;&lt;/div&gt;&lt;/div&gt;' +
        '&lt;div class=&quot;rv' + (isHero ? ' hero' : '') + '&quot;&gt;' + fmt(d[1], b.dec, b.unit) + '&lt;/div&gt;';
      rows.appendChild(r);
      var f = r.querySelector('.fill');
      requestAnimationFrame(function(){
        requestAnimationFrame(function(){ f.style.width = (d[1] / b.max * 100) + '%'; });
      });
    });
    bnote.textContent = b.n;
    resize();
  }
  drawBench();

  /* ---------------- 02 agent run ---------------- */
  var N46 = 40, N45 = 40;
  var tape46 = document.getElementById('tape46');
  var tape45 = document.getElementById('tape45');
  var timer = null;

  function buildTapes(){
    tape46.innerHTML = ''; tape45.innerHTML = '';
    for (var i = 0; i &lt; N46; i++){
      var a = document.createElement('div'); a.className = 'st'; tape46.appendChild(a);
      var b = document.createElement('div'); b.className = 'st'; tape45.appendChild(b);
    }
    document.getElementById('l46').textContent = 'idle';
    document.getElementById('l45').textContent = 'idle';
    setStat('s46', 0, 0, 0); setStat('s45', 0, 0, 0);
  }
  function setStat(id, s, v, r){
    document.getElementById(id).innerHTML =
      '&lt;span&gt;steps &lt;b&gt;' + s + '&lt;/b&gt;&lt;/span&gt;&lt;span&gt;verifications &lt;b&gt;' + v + '&lt;/b&gt;&lt;/span&gt;&lt;span&gt;recovered &lt;b&gt;' + r + '&lt;/b&gt;&lt;/span&gt;';
  }
  buildTapes();

  function runIt(){
    if (timer) clearInterval(timer);
    buildTapes();
    var i = 0, v46 = 0, v45 = 0, r46 = 0, r45 = 0, stop45 = 0;
    var c46 = tape46.children, c45 = tape45.children;
    timer = setInterval(function(){
      if (i &gt;= N46){
        clearInterval(timer); timer = null;
        document.getElementById('l46').textContent = 'task complete';
        if (!stop45) document.getElementById('l45').textContent = 'ran out of steps';
        return;
      }
      // 4.6 lane: verifies every 5th step, recovers from dead ends
      var isVer = (i % 5 === 4);
      var isDead = (i === 11 || i === 24);
      c46[i].className = 'st on hero' + (isVer ? ' ver' : '');
      if (isVer) v46++;
      if (isDead){ c46[i].className = 'st dead'; r46++; }
      // 4.5 lane: drifts, then stalls
      if (!stop45){
        var d45 = (i === 9 || i === 13 || i === 17 || i === 19);
        c45[i].className = 'st on' + (i % 11 === 10 ? ' ver' : '');
        if (i % 11 === 10) v45++;
        if (d45){ c45[i].className = 'st dead'; r45++; }
        if (i === 21){
          stop45 = 1;
          document.getElementById('l45').textContent = 'stalled at step 22';
        }
      }
      i++;
      document.getElementById('l46').textContent = 'step ' + i + ' / ' + N46;
      setStat('s46', i, v46, r46);
      setStat('s45', stop45 ? 22 : i, v45, r45);
    }, 95);
  }
  document.getElementById('runBtn').addEventListener('click', runIt);
  document.getElementById('resetBtn').addEventListener('click', function(){
    if (timer){ clearInterval(timer); timer = null; }
    buildTapes();
  });

  /* ---------------- 03 cost ---------------- */
  var pin = document.getElementById('pin'), pout = document.getElementById('pout'),
      pcache = document.getElementById('pcache'), pcalls = document.getElementById('pcalls');

  function money(x){
    if (x &gt;= 1000) return '$' + x.toLocaleString('en-US', {maximumFractionDigits:0});
    if (x &gt;= 1) return '$' + x.toFixed(2);
    return '$' + x.toFixed(4);
  }
  function calcCost(){
    var inTok = +pin.value, outTok = +pout.value, cache = +pcache.value / 100, calls = +pcalls.value;
    var over = inTok &gt; 200000;
    var rIn = over ? 4 : 2, rCache = over ? 1 : 0.5, rOut = over ? 12 : 6;
    var cached = inTok * cache, fresh = inTok - cached;
    var per = (fresh / 1e6) * rIn + (cached / 1e6) * rCache + (outTok / 1e6) * rOut;

    document.getElementById('pinV').textContent   = inTok.toLocaleString('en-US');
    document.getElementById('poutV').textContent  = outTok.toLocaleString('en-US');
    document.getElementById('pcacheV').textContent = (cache * 100).toFixed(0) + '%';
    document.getElementById('pcallsV').textContent = calls.toLocaleString('en-US');

    var tb = document.getElementById('tierBox');
    tb.className = 'tier' + (over ? ' hi' : '');
    tb.textContent = over
      ? 'LONG-CONTEXT TIER \u2014 prompt above 200K tokens. Billing $4 / $1 / $12 per 1M.'
      : 'STANDARD TIER \u2014 prompt at or below 200K tokens. Billing $2 / $0.50 / $6 per 1M.';

    document.getElementById('oCall').textContent  = money(per);
    document.getElementById('oDay').textContent   = money(per * calls);
    document.getElementById('oMonth').textContent = money(per * calls * 30);
  }
  [pin, pout, pcache, pcalls].forEach(function(el){ el.addEventListener('input', calcCost); });
  calcCost();

  /* ---------------- 04 effort ---------------- */
  var E = [
    ['low',    22, 12, 'Shallowest pass. Suited to classification, extraction, routing, and short tool calls where the answer is close to the prompt.'],
    ['medium', 48, 34, 'Middle setting. A reasonable default for retrieval-grounded answers and routine code edits with a verification step downstream.'],
    ['high',   74, 62, 'The API default, and the level xAI reports its launch benchmarks at. Multi-step coding, research, and agent loops sit here.'],
    ['xhigh',  96, 94, 'New in Grok 4.6. Reserve it for the hardest steps in a trajectory \u2014 thorny debugging, plans that must hold over many turns \u2014 and route the rest lower.']
  ];
  var stepsEl = document.getElementById('steps');
  var ei = 2;
  E.forEach(function(e, i){
    var b = document.createElement('button');
    b.className = 'sbtn'; b.type = 'button'; b.textContent = e[0];
    b.addEventListener('click', function(){ ei = i; drawEffort(); });
    stepsEl.appendChild(b);
  });
  function drawEffort(){
    [].slice.call(stepsEl.children).forEach(function(b,i){
      b.setAttribute('aria-pressed', i === ei ? 'true' : 'false');
    });
    var e = E[ei];
    document.getElementById('mDepth').style.width = e[1] + '%';
    document.getElementById('mCost').style.width  = e[2] + '%';
    document.getElementById('dLab').textContent = e[0] + (ei === 2 ? ' (default)' : '');
    document.getElementById('cLab').textContent = ei === 0 ? 'lowest' : ei === 3 ? 'highest' : 'moderate';
    document.getElementById('effortText').textContent = e[3];
    resize();
  }
  drawEffort();

  /* ---------------- auto-resize ---------------- */
  function resize(){
    try {
      var h = document.body.offsetHeight + 40;
      if (window.parent &amp;&amp; window.parent !== window){
        window.parent.postMessage({ mtpGrok46Height: h }, '*');
      }
    } catch(e){}
  }
  window.addEventListener('load', resize);
  window.addEventListener('resize', resize);
  setTimeout(resize, 300);
  setTimeout(resize, 1200);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
" style="width:100%;height:1500px;border:0;display:block;background:#000;" id="mtp-grok46-frame" loading="lazy" title="Grok 4.6 interactive explainer"></iframe>
<script>
window.addEventListener("message",function(e){if(e.data&&e.data.mtpGrok46Height){var f=document.getElementById("mtp-grok46-frame");if(f){f.style.height=e.data.mtpGrok46Height+"px";}}});
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading">Key Takeaways</h2>



<ul class="wp-block-list">
<li>Grok 4.6 is a post-training upgrade on Grok 4.5, not a bigger base model.</li>



<li>500K context, text and image input, and a new <code>xhigh</code> reasoning-effort level.</li>



<li>Ties GPT-5.6 Sol Max at 61 on the AA Intelligence Index; trails on DeepSWE and Terminal-Bench.</li>



<li>Same $2 / $6 headline price, with rates doubling above 200K prompt tokens.</li>



<li>Available today via API, Cursor, and Grok Build — no open weights, no self-hosting.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://x.ai/news/grok-4-6" target="_blank" rel="noreferrer noopener">FULL TECHNICAL DETAILS here</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://magic.beehiiv.com/v1/f5e63dd4-5653-4f09-83e2-321a8b1ba526?email={{email}}" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/08/12/spacexai-releases-grok-4-6/">SpaceXAI Releases Grok 4.6: A 500K-Context Frontier Model Tuned for Long-Running Agents, Coding, and Knowledge Work</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81694</post-id>	</item>
		<item>
		<title>AllenAI Open Instruct Tulu 3 Post-Training with SFT, DPO, RLVR, GRPO, and Verifier-Based Evaluation</title>
		<link>https://www.marktechpost.com/2026/08/12/allenai-open-instruct-tulu-3-post-training-with-sft-dpo-rlvr-grpo-and-verifier-based-evaluation/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Wed, 12 Aug 2026 17:37:37 +0000</pubDate>
				<category><![CDATA[Uncategorized]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81688</guid>

					<description><![CDATA[<p>Build a custom LLM post-training pipeline using AllenAI’s Open Instruct framework. This comprehensive guide walks through Supervised Fine-Tuning (SFT), Direct Preference Optimization (DPO), and Reinforcement Learning with Verifiable Rewards (GRPO), optimized to run efficiently on 16GB hardware without needing heavy distributed computing infrastructure.</p>
<p>The post <a href="https://www.marktechpost.com/2026/08/12/allenai-open-instruct-tulu-3-post-training-with-sft-dpo-rlvr-grpo-and-verifier-based-evaluation/">AllenAI Open Instruct Tulu 3 Post-Training with SFT, DPO, RLVR, GRPO, and Verifier-Based Evaluation</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this tutorial, we build an end-to-end post-training pipeline for a compact instruction-tuned language model using<a href="https://github.com/allenai/open-instruct"> <strong>AllenAI’s Open Instruct</strong></a> framework. We move through three major training stages: Supervised Fine-Tuning, Direct Preference Optimization, and Reinforcement Learning with Verifiable Rewards using GRPO, while adapting the original multi-GPU Tulu 3 stack to fit within a 16 GB runtime. We clone the Open Instruct repository, selectively load its native loss and utility functions, configure LoRA adapters, prepare GSM8K data for each training stage, and use deterministic verifiers to evaluate generated mathematical answers. Throughout the workflow, we preserve the core optimization logic of Open Instruct while replacing distributed components such as vLLM, Ray actors, DeepSpeed, and asynchronous rollout queues with lightweight Hugging Face and PyTorch implementations suitable for Colab.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import os, sys, subprocess, textwrap, json, math, random, re, ast, types, dataclasses, gc, contextlib
REPO_URL = "https://github.com/allenai/open-instruct.git"
REPO_DIR = "/content/open-instruct" if os.path.isdir("/content") else "./open-instruct"
PIP_PKGS = [
   "peft", "accelerate",
   "ray", "wandb", "beaker-py",
   "langdetect==1.0.9", "immutabledict==1.2.0", "nltk",
   "absl-py", "sympy", "antlr4-python3-runtime==4.11",
   "tiktoken",
]
def sh(*args):
   print("$", " ".join(args))
   subprocess.run(args, check=False)
def setup():
   sh(sys.executable, "-m", "pip", "install", "-q", *PIP_PKGS)
   if not os.path.isdir(REPO_DIR):
       sh("git", "clone", "--depth", "1", REPO_URL, REPO_DIR)
   if REPO_DIR not in sys.path:
       sys.path.insert(0, REPO_DIR)
   os.environ.setdefault("WANDB_MODE", "disabled")
   os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
   os.environ.setdefault("RAY_DISABLE_IMPORT_WARNING", "1")
setup()
import numpy as np
import torch
import torch.nn.functional as F
from torch.utils.data import DataLoader
from datasets import load_dataset, Dataset
from transformers import AutoModelForCausalLM, DataCollatorForSeq2Seq, get_cosine_schedule_with_warmup
from peft import LoraConfig, get_peft_model
DEV = "cuda" if torch.cuda.is_available() else "cpu"
try:
   _bf16 = DEV == "cuda" and torch.cuda.is_bf16_supported(including_emulation=False)
except TypeError:
   _bf16 = DEV == "cuda" and torch.cuda.get_device_properties(0).major >= 8
AMP_DTYPE = torch.bfloat16 if _bf16 else torch.float16
USE_SCALER = AMP_DTYPE is torch.float16
print(f"device={DEV}  autocast dtype={AMP_DTYPE}  gpu={torch.cuda.get_device_name(0) if DEV=='cuda' else '-'}")
def oi_load(relpath, names, ns=None):
   src = open(os.path.join(REPO_DIR, relpath)).read()
   tree = ast.parse(src)
   found = {n.name: n for n in tree.body
            if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef)) and n.name in names}
   missing = set(names) - set(found)
   if missing:
       raise KeyError(f"{relpath}: could not find {missing} (upstream may have renamed them)")
   ns = {} if ns is None else dict(ns)
   ns.update({"torch": torch, "F": F, "np": np, "enum": __import__("enum"),
              "dataclasses": dataclasses, "math": math, "os": os})
   future = ast.parse("from __future__ import annotations").body
   mod = ast.Module(body=future + [found[n] for n in names], type_ignores=[])
   exec(compile(ast.fix_missing_locations(mod), f"&lt;open_instruct:{relpath}>", "exec"), ns)
   return {n: ns[n] for n in names}
_dpo  = oi_load("open_instruct/dpo_utils.py", ["dpo_loss", "_get_batch_logps"])
_pf   = oi_load("open_instruct/padding_free_collator.py", ["calculate_per_token_logps"])
_rl   = oi_load("open_instruct/rl_utils.py", ["masked_mean"])
_mu   = oi_load("open_instruct/model_utils.py", ["estimate_kl"])
_grpo = oi_load("open_instruct/grpo_utils.py", ["GRPOLossType", "compute_grpo_loss"],
               ns={"model_utils": types.SimpleNamespace(**_mu)})
dpo_loss           = _dpo["dpo_loss"]
get_batch_logps    = _dpo["_get_batch_logps"]
per_token_logps_fn = _pf["calculate_per_token_logps"]
masked_mean        = _rl["masked_mean"]
compute_grpo_loss  = _grpo["compute_grpo_loss"]
GRPOLossType       = _grpo["GRPOLossType"]
print("lifted from repo:", [f.__name__ for f in (dpo_loss, get_batch_logps, per_token_logps_fn,
                                                masked_mean, compute_grpo_loss)])
from open_instruct.dataset_transformation import (
   CHAT_TEMPLATES, TokenizerConfig,
   sft_tulu_tokenize_and_truncate_v1, sft_tulu_filter_v1,
   preference_tulu_tokenize_and_truncate_v1_2,
   rlvr_tokenize_v1, visualize_token_role,
)
from open_instruct.ground_truth_utils import GSM8KVerifier, MathVerifier, IFEvalVerifierOld
</code></pre></div></div>



<p class="wp-block-paragraph">We install the required lightweight dependencies, clone the Open Instruct repository, and configure the Colab environment for stable execution. We detect the available GPU precision mode and select either FP16 or BF16 autocasting based on the hardware capabilities. We also extract the original DPO, GRPO, masking, and log-probability functions directly from the repository without importing its full distributed training stack.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">@dataclasses.dataclass
class CFG:
   model: str = "Qwen/Qwen2.5-0.5B-Instruct"
   max_seq_len: int = 640
   seed: int = 42
   n_sft: int = 192
   sft_steps: int = 40
   sft_micro_bs: int = 2
   sft_accum: int = 4
   sft_lr: float = 1e-4
   n_dpo: int = 96
   dpo_steps: int = 24
   dpo_micro_bs: int = 1
   dpo_accum: int = 4
   dpo_lr: float = 5e-5
   dpo_beta: float = 0.1
   dpo_norm: bool = True
   grpo_iters: int = 6
   prompts_per_iter: int = 4
   samples_per_prompt: int = 4
   grpo_micro_bs: int = 1
   grpo_inner_epochs: int = 2
   grpo_lr: float = 2e-5
   grpo_temperature: float = 1.0
   grpo_max_new: int = 200
   grpo_kl_beta: float = 0.02
   clip_lower: float = 0.2
   clip_higher: float = 0.272
   kl_estimator: int = 2
   adv_norm: str = "centered"
   n_eval: int = 24
cfg = CFG()
random.seed(cfg.seed); np.random.seed(cfg.seed); torch.manual_seed(cfg.seed)
tc = TokenizerConfig(tokenizer_name_or_path=cfg.model, chat_template_name=None, use_fast=True)
tok = tc.tokenizer
print(f"\navailable CHAT_TEMPLATES: {list(CHAT_TEMPLATES)[:12]} ... ({len(CHAT_TEMPLATES)} total)")
print(f"pad={tok.pad_token!r}({tok.pad_token_id})  eos={tok.eos_token!r}({tok.eos_token_id})")
_demo = {"messages": [
   {"role": "user", "content": "What is 12 * 3?"},
   {"role": "assistant", "content": "12 * 3 = 36. The answer is 36."},
   {"role": "user", "content": "And minus 6?"},
   {"role": "assistant", "content": "36 - 6 = 30. The answer is 30."},
]}
_enc = sft_tulu_tokenize_and_truncate_v1(dict(_demo), tok, cfg.max_seq_len)
print("\n[SFT label masking — colour 0 = masked out of the loss, colour 1 = trained on]")
visualize_token_role(_enc["input_ids"].tolist(), (_enc["labels"] != -100).long().tolist(), tok)
print(f"trainable tokens: {(_enc['labels'] != -100).sum().item()}/{_enc['labels'].numel()}")
</code></pre></div></div>



<p class="wp-block-paragraph">We define a centralized configuration class that controls the model, dataset sizes, learning rates, batch settings, and optimization parameters for every training stage. We initialize the Open Instruct tokenizer while preserving the model’s chat template and ensuring that padding and end-of-sequence tokens remain correctly separated. We then tokenize a sample conversation and visualize which assistant tokens contribute to the supervised training loss.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">gsm = load_dataset("openai/gsm8k", "main")
SYS = "You are a careful math assistant. Reason step by step, then finish with 'The answer is N.'"
def gsm_answer(a):
   return a.split("####")[-1].strip().replace(",", "")
def gsm_solution(a):
   body = a.split("####")[0].strip()
   body = re.sub(r"&lt;&lt;.*?>>", "", body)
   return f"{body}\nThe answer is {gsm_answer(a)}."
def as_messages(row):
   return [{"role": "system", "content": SYS},
           {"role": "user", "content": row["question"]},
           {"role": "assistant", "content": gsm_solution(row["answer"])}]
train_rows = [gsm["train"][i] for i in range(cfg.n_sft + cfg.n_dpo)]
eval_rows = [gsm["test"][i] for i in range(cfg.n_eval)]
def to_lists(row):
   for k in ("input_ids", "labels", "attention_mask"):
       row[k] = row[k].tolist()
   return row
sft_ds = Dataset.from_list([{"messages": as_messages(r)} for r in train_rows[: cfg.n_sft]])
sft_ds = sft_ds.map(lambda r: to_lists(sft_tulu_tokenize_and_truncate_v1(r, tok, cfg.max_seq_len)),
                   remove_columns=["messages"], desc="sft tokenize")
sft_ds = sft_ds.filter(sft_tulu_filter_v1, fn_kwargs={"tokenizer": tok}, desc="drop all-masked")
def make_pair(r):
   gold = gsm_answer(r["answer"])
   bad = (str(int(float(gold)) + random.choice([-10, -3, -1, 1, 2, 7]))
          if gold.replace('.', '', 1).lstrip('-').isdigit() else gold + "0")
   prompt = [{"role": "system", "content": SYS}, {"role": "user", "content": r["question"]}]
   good_txt = gsm_solution(r["answer"])
   bad_txt = good_txt.rsplit("The answer is", 1)[0] + f"The answer is {bad}."
   return {"chosen": prompt + [{"role": "assistant", "content": good_txt}],
           "rejected": prompt + [{"role": "assistant", "content": bad_txt}]}
dpo_ds = Dataset.from_list([make_pair(r) for r in train_rows[cfg.n_sft:]])
dpo_ds = dpo_ds.map(
   lambda r: {k: (v.tolist() if torch.is_tensor(v) else v) for k, v in
              preference_tulu_tokenize_and_truncate_v1_2(r, tok, cfg.max_seq_len).items()},
   remove_columns=["chosen", "rejected"], desc="dpo tokenize")
rlvr_rows = [{"messages": as_messages(r)[:2], "ground_truth": gsm_answer(r["answer"]), "dataset": "gsm8k"}
            for r in train_rows[: cfg.n_sft]]
rlvr_ds = Dataset.from_list(rlvr_rows).map(lambda r: rlvr_tokenize_v1(r, tok),
                                          remove_columns=["messages"], desc="rlvr tokenize")
print(f"\nsft={len(sft_ds)}  dpo={len(dpo_ds)}  rlvr={len(rlvr_ds)}")
VERIFIERS = {"gsm8k": GSM8KVerifier(), "math": MathVerifier(), "ifeval_old": IFEvalVerifierOld()}
print("\n[verifier smoke test]")
print(" gsm8k :", VERIFIERS["gsm8k"]([], "9 + 3 = 12. The answer is 12.", "12").score)
print(" gsm8k :", VERIFIERS["gsm8k"]([], "The answer is 11.", "12").score)
print(" math  :", VERIFIERS["math"]([], r"hence \boxed{0.5}", r"\frac{1}{2}").score)
print(" ifeval:", VERIFIERS["ifeval_old"]([], "one two three four five six seven",
                                         json.dumps({"func_name": "validate_word_constraint",
                                                     "N": 6, "quantifier": "at least"})).score)
def verify_batch(responses, ground_truths, sources, tokenized=None):
   out = []
   for i, (resp, gt, src) in enumerate(zip(responses, ground_truths, sources)):
       v = VERIFIERS.get(src, VERIFIERS["gsm8k"])
       out.append(v(tokenized[i] if tokenized else [], resp, gt).score * v.weight)
   return np.array(out, dtype=np.float32)
</code></pre></div></div>



<p class="wp-block-paragraph">We load GSM8K and transform its questions and solutions into a consistent conversational format for SFT, DPO, and RLVR training. We create supervised examples, preference pairs with deliberately incorrect final answers, and verifier-ready prompts with structured ground-truth labels. We also initialize Open Instruct’s GSM8K, mathematical, and instruction-following verifiers and use them to score generated responses deterministically.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">model = AutoModelForCausalLM.from_pretrained(cfg.model, dtype=torch.float32).to(DEV)
model.config.use_cache = False
if len(tok) > model.get_input_embeddings().weight.shape[0]:
   model.resize_token_embeddings(len(tok))
def _patch_peft_torchao():
   import importlib
   for mod in ("peft.import_utils", "peft.tuners.lora.torchao",
               "peft.tuners.lora.model", "peft.tuners.lora.layer"):
       try:
           m = importlib.import_module(mod)
       except Exception:
           continue
       if hasattr(m, "is_torchao_available"):
           m.is_torchao_available = lambda: False
_patch_peft_torchao()
model = get_peft_model(model, LoraConfig(
   r=32, lora_alpha=64, lora_dropout=0.05, bias="none", task_type="CAUSAL_LM",
model.print_trainable_parameters()
TRAINABLE = [p for p in model.parameters() if p.requires_grad]
@contextlib.contextmanager
def with_cache():
   old = model.config.use_cache
   model.config.use_cache = True
   try:
       yield
   finally:
       model.config.use_cache = old
def amp():
   return torch.autocast(device_type="cuda", dtype=AMP_DTYPE) if DEV == "cuda" \
       else torch.autocast(device_type="cpu", enabled=False)
def new_opt(lr, steps):
   opt = torch.optim.AdamW(TRAINABLE, lr=lr, weight_decay=0.0, betas=(0.9, 0.999))
   sched = get_cosine_schedule_with_warmup(opt, int(0.05 * steps) + 1, steps)
   scaler = torch.amp.GradScaler("cuda", enabled=USE_SCALER)
   return opt, sched, scaler
def step_opt(opt, sched, scaler):
   scaler.unscale_(opt)
   torch.nn.utils.clip_grad_norm_(TRAINABLE, 1.0)
   scaler.step(opt); scaler.update(); sched.step(); opt.zero_grad(set_to_none=True)
@torch.no_grad()
def evaluate(tag, rows, max_new=256):
   model.eval()
   tok.padding_side = "left"
   correct, bs = 0.0, 4
   for i in range(0, len(rows), bs):
       chunk = rows[i:i + bs]
       prompts = [tok.apply_chat_template(
           [{"role": "system", "content": SYS}, {"role": "user", "content": r["question"]}],
           add_generation_prompt=True, tokenize=False) for r in chunk]
       enc = tok(prompts, return_tensors="pt", padding=True, add_special_tokens=False).to(DEV)
       with amp(), with_cache():
           out = model.generate(**enc, max_new_tokens=max_new, do_sample=False,
                                pad_token_id=tok.pad_token_id)
       texts = tok.batch_decode(out[:, enc["input_ids"].shape[1]:], skip_special_tokens=True)
       correct += verify_batch(texts, [gsm_answer(r["answer"]) for r in chunk],
                               ["gsm8k"] * len(chunk)).sum()
   acc = correct / len(rows)
   print(f"  [eval:{tag}] verifier accuracy = {acc:.3f}  ({int(correct)}/{len(rows)})")
   model.train(); tok.padding_side = "right"
   return acc
print("\n" + "=" * 90); print("BASELINE"); print("=" * 90)
base_acc = evaluate("base", eval_rows)
</code></pre></div></div>



<p class="wp-block-paragraph">We load the Qwen instruction model, apply LoRA adapters to its attention and feed-forward projection layers, and restrict optimization to the trainable adapter parameters. We configure mixed-precision execution, gradient scaling, gradient clipping, learning-rate scheduling, and temporary KV-cache activation for generation. We then evaluate the untrained baseline on GSM8K using greedy decoding and verifier-based answer accuracy.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n" + "=" * 90); print("STAGE 1 — SFT"); print("=" * 90)
sft_collate = DataCollatorForSeq2Seq(tokenizer=tok, padding="longest", label_pad_token_id=-100)
sft_dl = DataLoader(sft_ds, batch_size=cfg.sft_micro_bs, shuffle=True, collate_fn=sft_collate, drop_last=True)
opt, sched, scaler = new_opt(cfg.sft_lr, cfg.sft_steps)
model.train(); it, step, run = iter(sft_dl), 0, 0.0
while step &lt; cfg.sft_steps:
   for _ in range(cfg.sft_accum):
       try:
           batch = next(it)
       except StopIteration:
           it = iter(sft_dl); batch = next(it)
       batch = {k: v.to(DEV) for k, v in batch.items()}
       with amp():
           loss = model(**batch).loss / cfg.sft_accum
       scaler.scale(loss).backward()
       run += loss.item()
   step_opt(opt, sched, scaler); step += 1
   if step % 10 == 0 or step == 1:
       print(f"  sft step {step:>3}/{cfg.sft_steps}  loss {run:.4f}  lr {sched.get_last_lr()[0]:.2e}")
   run = 0.0
sft_acc = evaluate("after-sft", eval_rows)
</code></pre></div></div>



<p class="wp-block-paragraph">We construct a padded SFT DataLoader and train the LoRA adapters on tokenized GSM8K conversations using gradient accumulation. We optimize the model with cross-entropy loss calculated only over the unmasked assistant response tokens. We track the training loss and learning rate throughout the stage and evaluate the updated model after supervised fine-tuning.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n" + "=" * 90); print("STAGE 2 — DPO (dpo_norm)"); print("=" * 90)
def pad_side(seqs, pad, maxlen):
   return torch.tensor([s + [pad] * (maxlen - len(s)) for s in seqs], dtype=torch.long)
def dpo_collate(features):
   out = {}
   for pfx in ("chosen", "rejected"):
       L = max(len(f[f"{pfx}_input_ids"]) for f in features)
       out[f"{pfx}_input_ids"] = pad_side([f[f"{pfx}_input_ids"] for f in features], tok.pad_token_id, L)
       out[f"{pfx}_labels"] = pad_side([f[f"{pfx}_labels"] for f in features], -100, L)
       out[f"{pfx}_attention_mask"] = pad_side([f[f"{pfx}_attention_mask"] for f in features], 0, L)
   return out
def seq_logps(input_ids, attn, labels):
   with amp():
       logits = model(input_ids=input_ids, attention_mask=attn).logits
   ptl = per_token_logps_fn(logits, labels)
   return get_batch_logps(ptl, labels, average_log_prob=cfg.dpo_norm)
dpo_dl = DataLoader(dpo_ds, batch_size=cfg.dpo_micro_bs, shuffle=True, collate_fn=dpo_collate, drop_last=True)
opt, sched, scaler = new_opt(cfg.dpo_lr, cfg.dpo_steps)
it, step = iter(dpo_dl), 0
while step &lt; cfg.dpo_steps:
   agg = {"loss": 0.0, "acc": 0.0, "margin": 0.0}
   for _ in range(cfg.dpo_accum):
       try:
           b = next(it)
       except StopIteration:
           it = iter(dpo_dl); b = next(it)
       b = {k: v.to(DEV) for k, v in b.items()}
       with torch.no_grad(), model.disable_adapter():
           ref_c = seq_logps(b["chosen_input_ids"], b["chosen_attention_mask"], b["chosen_labels"])
           ref_r = seq_logps(b["rejected_input_ids"], b["rejected_attention_mask"], b["rejected_labels"])
       pol_c = seq_logps(b["chosen_input_ids"], b["chosen_attention_mask"], b["chosen_labels"])
       pol_r = seq_logps(b["rejected_input_ids"], b["rejected_attention_mask"], b["rejected_labels"])
       losses, r_c, r_r = dpo_loss(pol_c, pol_r, ref_c, ref_r, beta=cfg.dpo_beta, label_smoothing=0.0)
       loss = losses.mean() / cfg.dpo_accum
       scaler.scale(loss).backward()
       agg["loss"] += loss.item()
       agg["acc"] += (r_c > r_r).float().mean().item() / cfg.dpo_accum
       agg["margin"] += (r_c - r_r).mean().item() / cfg.dpo_accum
   step_opt(opt, sched, scaler); step += 1
   if step % 8 == 0 or step == 1:
       print(f"  dpo step {step:>3}/{cfg.dpo_steps}  loss {agg['loss']:.4f} "
             f"reward_acc {agg['acc']:.2f}  margin {agg['margin']:+.3f}")
dpo_acc = evaluate("after-dpo", eval_rows)
</code></pre></div></div>



<p class="wp-block-paragraph">We batch the chosen and rejected responses separately and calculate their length-normalized sequence log probabilities with Open Instruct’s native utilities. We compare the active LoRA policy against the frozen base reference policy and optimize the model using the repository’s DPO loss. We monitor preference accuracy, reward margins, and training loss before measuring the model’s post-DPO verifier performance.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">print("\n" + "=" * 90); print("STAGE 3 — RLVR / GRPO"); print("=" * 90)
grpo_cfg = types.SimpleNamespace(loss_fn=GRPOLossType.dapo, clip_lower=cfg.clip_lower,
                                clip_higher=cfg.clip_higher, kl_estimator=cfg.kl_estimator)
_gen_eos = getattr(getattr(model, "generation_config", None), "eos_token_id", None)
_terms = {tok.eos_token_id, tok.pad_token_id}
_terms |= set(_gen_eos) if isinstance(_gen_eos, (list, tuple)) else {_gen_eos}
TERMINATORS = torch.tensor(sorted(t for t in _terms if t is not None), device=DEV)
def token_logps(seq, attn, temperature, grad=True):
   pos = (attn.cumsum(-1) - 1).clamp(min=0)
   ctx = torch.enable_grad() if grad else torch.no_grad()
   with ctx, amp():
       logits = model(input_ids=seq, attention_mask=attn, position_ids=pos).logits
   return per_token_logps_fn(logits / temperature, seq)
def rollout(batch_rows):
   G = cfg.samples_per_prompt
   ids = [r["input_ids_prompt"] for r in batch_rows]
   P = max(len(x) for x in ids)
   pin = torch.tensor([[tok.pad_token_id] * (P - len(x)) + x for x in ids], device=DEV)
   pmask = torch.tensor([[0] * (P - len(x)) + [1] * len(x) for x in ids], device=DEV)
   model.eval()
   with torch.no_grad(), amp(), with_cache():
       seq = model.generate(input_ids=pin, attention_mask=pmask, do_sample=True,
                            temperature=cfg.grpo_temperature, top_p=1.0, top_k=0,
                            max_new_tokens=cfg.grpo_max_new, num_return_sequences=G,
                            pad_token_id=tok.pad_token_id)
   model.train()
   resp = seq[:, P:]
   is_term = torch.isin(resp, TERMINATORS)
   first = torch.where(is_term.any(1), is_term.float().argmax(1),
                       torch.full((resp.shape[0],), resp.shape[1] - 1, device=DEV))
   idx = torch.arange(resp.shape[1], device=DEV).unsqueeze(0)
   resp_mask = (idx &lt;= first.unsqueeze(1)).long()
   full_mask = torch.cat([torch.zeros(seq.shape[0], P, dtype=torch.long, device=DEV), resp_mask], 1)
   attn = torch.cat([pmask.repeat_interleave(G, 0), resp_mask], 1)
   texts = tok.batch_decode(resp, skip_special_tokens=True)
   gts = [r["ground_truth"] for r in batch_rows for _ in range(G)]
   srcs = [r["dataset"] for r in batch_rows for _ in range(G)]
   scores = verify_batch(texts, gts, srcs)
   per_prompt = scores.reshape(-1, G)
   mean_g = np.repeat(per_prompt.mean(-1), G, 0)
   if cfg.adv_norm == "standard":
       adv = (scores - mean_g) / (np.repeat(per_prompt.std(-1), G, 0) + 1e-8)
   else:
       adv = scores - mean_g
   adv_t = torch.tensor(adv, device=DEV, dtype=torch.float32).unsqueeze(1).expand_as(full_mask.float())
   return seq, attn, full_mask, adv_t, scores, texts
opt, sched, scaler = new_opt(cfg.grpo_lr, cfg.grpo_iters * cfg.grpo_inner_epochs)
order = list(range(len(rlvr_ds))); random.shuffle(order)
for it_i in range(cfg.grpo_iters):
   rows = [rlvr_ds[order[(it_i * cfg.prompts_per_iter + j) % len(rlvr_ds)]]
           for j in range(cfg.prompts_per_iter)]
   seq, attn, mask, adv, scores, texts = rollout(rows)
   with torch.no_grad():
       old_lp = torch.cat([token_logps(seq[i:i + cfg.grpo_micro_bs], attn[i:i + cfg.grpo_micro_bs],
                                       cfg.grpo_temperature, grad=False)
                           for i in range(0, seq.shape[0], cfg.grpo_micro_bs)])
       with model.disable_adapter():
           ref_lp = torch.cat([token_logps(seq[i:i + cfg.grpo_micro_bs], attn[i:i + cfg.grpo_micro_bs],
                                           cfg.grpo_temperature, grad=False)
                               for i in range(0, seq.shape[0], cfg.grpo_micro_bs)])
   n_chunks = math.ceil(seq.shape[0] / cfg.grpo_micro_bs)
   for ep in range(cfg.grpo_inner_epochs):
       stats = {"pg": 0.0, "kl": 0.0, "clip": 0.0}
       for i in range(0, seq.shape[0], cfg.grpo_micro_bs):
           sl = slice(i, i + cfg.grpo_micro_bs)
           new_lp = token_logps(seq[sl], attn[sl], cfg.grpo_temperature, grad=True)
           new_lp_, old_lp_, ref_lp_ = new_lp[:, :-1], old_lp[sl][:, :-1], ref_lp[sl][:, :-1]
           m_, a_ = mask[sl][:, 1:], adv[sl][:, 1:]
           ratio = torch.exp((new_lp_ - old_lp_).clamp(-20, 20))
           pg, clipfrac, kl = compute_grpo_loss(new_lp_, ratio, a_, ref_lp_, grpo_cfg,
                                                torch.ones_like(ratio))
           loss = masked_mean(pg + cfg.grpo_kl_beta * kl, m_) / n_chunks
           scaler.scale(loss).backward()
           with torch.no_grad():
               stats["pg"] += masked_mean(pg.detach(), m_).item() / n_chunks
               stats["kl"] += masked_mean(kl.detach(), m_).item() / n_chunks
               stats["clip"] += masked_mean(clipfrac.detach(), m_).item() / n_chunks
           del new_lp, ratio, pg, kl
       step_opt(opt, sched, scaler)
       if DEV == "cuda":
           torch.cuda.empty_cache()
       print(f"  grpo iter {it_i+1}/{cfg.grpo_iters} ep{ep+1}  reward {scores.mean():.3f} "
             f"(solved {int(scores.sum())}/{len(scores)})  pg {stats['pg']:+.4f}  "
             f"kl {stats['kl']:.4f}  clipfrac {stats['clip']:.3f}")
print("\n  sample rollout ->", textwrap.shorten(texts[0].replace("\n", " "), 220))
rlvr_acc = evaluate("after-rlvr", eval_rows)
print("\n" + "=" * 90)
print(f"{'stage':&lt;14}{'verifier acc':>14}")
for name, val in [("base", f"{base_acc:.3f}"), ("sft", f"{sft_acc:.3f}"),
                 ("dpo", f"{dpo_acc:.3f}"), ("rlvr", f"{rlvr_acc:.3f}")]:
   print(f"{name:&lt;14}{val:>14}")
print("=" * 90)
OUT = "/content/tulu-mini" if os.path.isdir("/content") else "./tulu-mini"
merged = model.merge_and_unload()
merged.save_pretrained(OUT); tok.save_pretrained(OUT)
print(f"merged checkpoint -> {OUT}  (equivalent to `python open_instruct/merge_lora.py`)")
</code></pre></div></div>



<p class="wp-block-paragraph">We generate multiple sampled responses for each prompt, score them with deterministic verifiers, and calculate group-relative advantages from their reward distributions. We optimize the policy with Open Instruct’s GRPO and DAPO-style clipping logic while applying response masks, importance ratios, and KL regularization against the reference model. We finally compare accuracy across the baseline, SFT, DPO, and RLVR stages before merging the LoRA adapters and saving the completed checkpoint.</p>



<p class="wp-block-paragraph">In conclusion, we implemented a practical miniature version of the Tulu 3 post-training stack and observed how each training stage changes model performance on verifier-scored mathematical reasoning tasks. We first established a baseline, improved instruction-following through supervised fine-tuning, refined response preferences through length-normalized DPO, and finally optimized verified task rewards using group-relative advantages and the repository’s GRPO loss implementation. We also used LoRA to maintain an accessible reference policy, apply response masking and KL regularization during reinforcement learning, compare accuracy across all training stages, and export a merged checkpoint for later inference or evaluation.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/LLM%20Projects/allenai_open_instruct_tulu3_post_training_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">FULL CODES here</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://magic.beehiiv.com/v1/f5e63dd4-5653-4f09-83e2-321a8b1ba526?email={{email}}" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/08/12/allenai-open-instruct-tulu-3-post-training-with-sft-dpo-rlvr-grpo-and-verifier-based-evaluation/">AllenAI Open Instruct Tulu 3 Post-Training with SFT, DPO, RLVR, GRPO, and Verifier-Based Evaluation</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81688</post-id>	</item>
		<item>
		<title>NVIDIA AI Releases Nemotron 3.5 Lightning: A 30B Open MoE with 3B Active Parameters, and NeMo Switchyard Model Router</title>
		<link>https://www.marktechpost.com/2026/08/11/nvidia-ai-releases-nemotron-3-5-lightning-and-nemo-switchyard/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Wed, 12 Aug 2026 06:59:40 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Deep Learning]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Embedding Model]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Uncategorized]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81682</guid>

					<description><![CDATA[<p>NVIDIA's open 30B MoE targets the agent execution layer, with Switchyard routing each step to the cheapest capable model.</p>
<p>The post <a href="https://www.marktechpost.com/2026/08/11/nvidia-ai-releases-nemotron-3-5-lightning-and-nemo-switchyard/">NVIDIA AI Releases Nemotron 3.5 Lightning: A 30B Open MoE with 3B Active Parameters, and NeMo Switchyard Model Router</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph" id="h-">NVIDIA introduced open technologies for building always-on AI agents from systems of specialized models. Two artifacts shipped together. <a href="https://huggingface.co/nvidia/NVIDIA-Nemotron-3.5-Lightning-30B-A3B-NVFP4">Nemotron 3.5 Lightning</a> is a lightweight, customizable open model built for high-volume agentic tasks, and <a href="https://github.com/NVIDIA-NeMo/Switchyard">NeMo Switchyard</a> is an open source routing library that directs each step of an agent workflow to the most capable and efficient model available. The problem both address is structural: long-running agents spend most of their time on tool calls, result validation, and subagent delegation, and sending every one of those steps to a frontier reasoning model adds cost and latency. Lightning is a 30B mixture-of-experts model with 3B active parameters, built on a hybrid Mamba-2 + MoE + Attention architecture with a 1M-token context window. NVIDIA reports up to 4x faster output speed than similar-sized models, and 30% faster completion of 10,000 PinchBench tasks than Qwen3.6 35B at comparable accuracy. Many industry players like CrowdStrike, Harvey, CodeRabbit, Fastino Labs, and Lila Sciences are already customizing it for cybersecurity, legal, coding, finance, and healthcare workloads.</p>



<h2 id="h-is-it-deployable" class="wp-block-heading"><strong>Is it deployable?</strong></h2>



<p class="wp-block-paragraph"><strong>Yes</strong>. <a href="https://huggingface.co/nvidia/NVIDIA-Nemotron-3.5-Lightning-30B-A3B-NVFP4">Nemotron 3.5 Lightning</a> is generally available under the permissive <a href="https://openmdw.ai/license/1-1/">OpenMDW-1.1</a> license, with open weights, training data, and recipes. NVIDIA states the model is ready for commercial use.</p>



<ul class="wp-block-list">
<li><strong>Which companies</strong>: Anyone with a single modern GPU. NVIDIA lists single-GPU deployment on 1x DGX Spark (GB10) or 1x H100. That puts solo developers and seed-stage startups on the same footing as enterprises. Mid-market teams can serve it from <a href="https://www.baseten.co/blog/introducing-nemotron-35-lightning/">Baseten</a>, <a href="https://www.together.ai/models/nemotron-3-5-lightning">Together AI</a>, or <a href="https://tokenfactory.nebius.com/endpoints?modals=endpoint-details&amp;model-id=nvidia/Nemotron-3_5-Lightning">Nebius</a>; regulated enterprises can keep it fully on-premises.</li>



<li><strong>Industries</strong>: Cybersecurity, legal services, software engineering, financial services, healthcare, and life sciences all appear in NVIDIA&#8217;s named customer set.</li>



<li><strong>Applications</strong>: Tool calling, result validation, subagent delegation, code review routing, log triage, contract parsing, and long-context retrieval across a 1M-token window.</li>
</ul>



<h2 id="h-the-execution-layer-not-the-planning-layer" class="wp-block-heading"><strong>The execution layer, not the planning layer</strong></h2>



<p class="wp-block-paragraph">Long-running agents spend most of their time on high-volume execution. Tool calls, result validation, and subagent delegation dominate the token budget. Routing every one of those steps to a frontier reasoning model adds cost and latency.</p>



<p class="wp-block-paragraph"><a href="https://developer.nvidia.com/blog/nvidia-nemotron-3-5-lightning-delivers-fast-accurate-specialized-task-execution-for-long-running-agents/">Nemotron 3.5 Lightning</a> targets that execution layer. It is a 30B mixture-of-experts model with 3B active parameters, built on a hybrid Mamba-2 + MoE + Attention architecture. Context length reaches 1M tokens. Pre-training covered more than 20 trillion tokens using an NVFP4 recipe.</p>



<p class="wp-block-paragraph">The model is the smallest member of the Nemotron 3 family. Frontier models such as <a href="https://developer.nvidia.com/blog/nvidia-nemotron-3-ultra-powers-faster-more-efficient-reasoning-for-long-running-agents/">Nemotron 3 Ultra</a> handle orchestration and planning, while Lightning handles the routine calls beneath them.</p>



<h2 id="h-where-the-speed-comes-from" class="wp-block-heading"><strong>Where the speed comes from</strong></h2>



<p class="wp-block-paragraph"><strong>Two mechanisms:</strong></p>



<ul class="wp-block-list">
<li><strong>First, Speculative Decoding:</strong> Multi-token prediction was baked in during a dedicated pre-training stage, then improved with an MTP-boosting phase. NVIDIA also ships two external draft models: <strong>DSpark</strong>, a semi-autoregressive drafter recommended for DGX Spark and low-concurrency data center workloads, and <a href="https://developer.nvidia.com/blog/boost-inference-performance-up-to-15x-on-nvidia-blackwell-using-dflash-speculative-decoding/"><strong>DFlash</strong></a>, which uses a lightweight block-diffusion model.</li>



<li><strong>Second, Quantization:</strong> An <a href="https://developer.nvidia.com/blog/introducing-nvfp4-for-efficient-and-accurate-low-precision-inference/">NVFP4</a> checkpoint ships alongside BF16. The same checkpoint serves Blackwell and Hopper natively, and extends to Ampere through W4A16 kernels.</li>
</ul>



<p class="wp-block-paragraph">NVIDIA reports up to 4x output speed versus similar-sized models. On PinchBench, it reports 86% accuracy while completing 10,000 tasks 30% faster than Qwen3.6 35B at comparable accuracy.</p>



<p class="wp-block-paragraph">Published model card results (BF16 / NVFP4): MMLU Pro 81.94 / 81.62, GPQA Diamond 75.44 / 75.57, SWE-bench Verified 51.56 / 52.80, Terminal-Bench 2.1 24.58 / 23.46, AA-LCR 52.00 / 49.19. Recommended sampling is temperature 1.0 and top_p 0.95.</p>



<h2 id="h-nemo-switchyard" class="wp-block-heading"><strong>NeMo Switchyard</strong></h2>



<p class="wp-block-paragraph"><a href="https://github.com/NVIDIA-NeMo/Switchyard">NeMo Switchyard</a> is an open source library that routes each step of an agent workflow to the most capable and efficient model available.</p>



<p class="wp-block-paragraph">It offers tuning-free routers, including an LLM classifier with session affinity, a stage router that reads recent tool activity, and an escalation router that starts cheap and promotes on sustained difficulty. A tunable prefill router learns from the model&#8217;s residual stream to predict which candidate will succeed. The reference server accepts OpenAI, Anthropic, and Responses API requests.</p>



<p class="wp-block-paragraph">Two published results: <a href="https://www.langchain.com/blog/switchyard-agent-routing-benchmark">LangChain</a> benchmarked 145 multi-turn agentic tasks. Routing between Lightning and Claude Opus 4.8 with the escalation router cut cost 74% versus a frontier-only baseline, sending 7% of calls to the frontier model, at a roughly 6-point accuracy tradeoff. Cognition implemented staged routing in Devin Desktop. On FrontierCode Main, routing between Opus 5 and Kimi K2.7 reached 50.6% at a $3.11 mean cost, within 2.8 points of Opus 5 accuracy at approximately 28% lower mean cost.</p>



<h2 id="h-interactive-explainer" class="wp-block-heading"><strong>Interactive explainer</strong></h2>



<div id="mtp-nemotron-lightning-wrap" style="margin:22px 0;">
<iframe id="mtp-nemotron-lightning" title="Nemotron 3.5 Lightning interactive explainer" scrolling="no" frameborder="0" style="width:100%;border:0;overflow:hidden;display:block;height:600px;" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;Nemotron 3.5 Lightning Explainer&lt;/title&gt;
&lt;style&gt;
  *{box-sizing:border-box;margin:0;padding:0}
  body{background:#111;color:#e6e6e6;font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;padding:0}
  .wrap{max-width:900px;margin:0 auto;padding:20px 16px 14px}
  .hd{border-left:3px solid #76B900;padding-left:12px;margin-bottom:16px}
  .hd h2{font-size:19px;font-weight:700;letter-spacing:-.2px;color:#fff;line-height:1.3}
  .hd p{font-size:13px;color:#9a9a9a;margin-top:5px;line-height:1.5}
  .tabs{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:14px}
  .tab{flex:1 1 150px;background:#1a1a1a;border:1px solid #2a2a2a;color:#a5a5a5;font-size:12px;font-weight:600;
       padding:10px 8px;border-radius:6px;cursor:pointer;transition:all .18s;text-align:center;line-height:1.3}
  .tab:hover{border-color:#3d3d3d;color:#ddd}
  .tab.on{background:#76B900;border-color:#76B900;color:#0a0a0a}
  .panel{display:none;background:#161616;border:1px solid #262626;border-radius:8px;padding:16px}
  .panel.on{display:block}
  .lede{font-size:13px;color:#b8b8b8;line-height:1.6;margin-bottom:14px}
  .lede b{color:#76B900;font-weight:600}
  .stage{background:#0d0d0d;border:1px solid #222;border-radius:6px;padding:14px;margin-bottom:12px}
  .row{display:flex;align-items:center;gap:10px;flex-wrap:wrap}
  .btn{background:#76B900;color:#0a0a0a;border:0;border-radius:5px;padding:9px 16px;font-size:12px;
       font-weight:700;cursor:pointer;transition:opacity .15s}
  .btn:hover{opacity:.85}
  .btn.alt{background:#1f1f1f;color:#9a9a9a;border:1px solid #2f2f2f}
  .btn:disabled{opacity:.4;cursor:not-allowed}
  .lbl{font-size:10px;color:#6d6d6d;text-transform:uppercase;letter-spacing:1.1px;font-weight:700;margin-bottom:8px}
  /* MoE */
  .router{text-align:center;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:11px;color:#76B900;
          border:1px dashed #3a5c00;border-radius:5px;padding:7px;margin:10px auto;max-width:230px}
  .experts{display:grid;grid-template-columns:repeat(8,1fr);gap:5px;margin-top:10px}
  .ex{height:46px;background:#1c1c1c;border:1px solid #292929;border-radius:4px;display:flex;
      align-items:center;justify-content:center;font-size:9px;color:#4d4d4d;transition:all .3s;
      font-family:ui-monospace,SFMono-Regular,Menlo,monospace}
  .ex.hot{background:#76B900;border-color:#9ae82a;color:#0a0a0a;font-weight:700;
          box-shadow:0 0 14px rgba(118,185,0,.55);transform:scale(1.08)}
  .meters{display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-top:12px}
  .meter{background:#131313;border:1px solid #242424;border-radius:5px;padding:9px;text-align:center}
  .meter .v{font-size:17px;font-weight:800;color:#76B900;font-family:ui-monospace,SFMono-Regular,Menlo,monospace}
  .meter .k{font-size:9px;color:#767676;text-transform:uppercase;letter-spacing:.7px;margin-top:3px;line-height:1.3}
  /* spec dec */
  .track{margin-bottom:12px}
  .tname{font-size:11px;color:#a5a5a5;margin-bottom:6px;display:flex;justify-content:space-between;font-weight:600}
  .tname span{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;color:#76B900}
  .cells{display:flex;gap:3px;flex-wrap:wrap}
  .c{width:20px;height:20px;border-radius:3px;background:#1c1c1c;border:1px solid #292929;transition:all .2s}
  .c.f{background:#3d3d3d;border-color:#4a4a4a}
  .c.g{background:#76B900;border-color:#9ae82a;box-shadow:0 0 8px rgba(118,185,0,.4)}
  .c.d{background:#2f4a00;border-color:#4f7a00}
  /* switchyard */
  .dots{display:grid;grid-template-columns:repeat(20,1fr);gap:3px;margin-top:8px}
  .d{aspect-ratio:1;border-radius:2px;background:#1c1c1c;transition:background .25s}
  .d.sm{background:#76B900}
  .d.fr{background:#8b5cf6}
  .bar{height:24px;background:#1a1a1a;border:1px solid #262626;border-radius:4px;overflow:hidden;position:relative;margin-top:6px}
  .fill{height:100%;width:100%;background:linear-gradient(90deg,#76B900,#a3e635);transition:width 1.1s cubic-bezier(.4,0,.2,1)}
  .fill.pv{background:linear-gradient(90deg,#8b5cf6,#a78bfa)}
  .bl{position:absolute;right:8px;top:4px;font-size:11px;font-weight:700;color:#fff;
      font-family:ui-monospace,SFMono-Regular,Menlo,monospace}
  .legend{display:flex;gap:14px;font-size:10px;color:#8a8a8a;margin-top:9px;flex-wrap:wrap}
  .legend i{display:inline-block;width:9px;height:9px;border-radius:2px;margin-right:5px;vertical-align:middle}
  .note{font-size:11px;color:#7d7d7d;line-height:1.55;margin-top:11px;border-top:1px solid #232323;padding-top:10px}
  .note b{color:#a5a5a5;font-weight:600}
  .ft{margin-top:14px;padding-top:11px;border-top:1px solid #242424;display:flex;justify-content:space-between;
      align-items:center;flex-wrap:wrap;gap:8px}
  .ft span{font-size:10px;color:#5f5f5f}
  .ft b{color:#76B900;font-weight:700}
  @media(max-width:640px){
    .wrap{padding:14px 10px 10px}
    .hd h2{font-size:16px}
    .experts{grid-template-columns:repeat(4,1fr)}
    .meters{grid-template-columns:1fr}
    .dots{grid-template-columns:repeat(10,1fr)}
    .tab{flex:1 1 100%}
    .c{width:16px;height:16px}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot;&gt;
  &lt;div class=&quot;hd&quot;&gt;
    &lt;h2&gt;How Nemotron 3.5 Lightning Runs 30B Worth of Model at 3B Worth of Compute&lt;/h2&gt;
    &lt;p&gt;Three mechanisms do the work: sparse expert activation, speculative decoding, and model routing. Click through each one.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;tabs&quot;&gt;
    &lt;button class=&quot;tab on&quot; data-t=&quot;0&quot;&gt;1. Sparse MoE: 30B to 3B&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-t=&quot;1&quot;&gt;2. Speculative decoding&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-t=&quot;2&quot;&gt;3. Switchyard routing&lt;/button&gt;
  &lt;/div&gt;

  &lt;!-- PANEL 1 --&gt;
  &lt;div class=&quot;panel on&quot; id=&quot;p0&quot;&gt;
    &lt;p class=&quot;lede&quot;&gt;Nemotron 3.5 Lightning is a hybrid &lt;b&gt;Mamba-2 + MoE + Attention&lt;/b&gt; architecture with &lt;b&gt;30B total parameters&lt;/b&gt; and &lt;b&gt;3B active&lt;/b&gt;. A learned router sends each token to a small subset of experts, so only a fraction of the network runs per token.&lt;/p&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;lbl&quot;&gt;Token stream&lt;/div&gt;
      &lt;div class=&quot;router&quot; id=&quot;tok&quot;&gt;waiting for token...&lt;/div&gt;
      &lt;div class=&quot;lbl&quot; style=&quot;margin-top:12px&quot;&gt;Expert pool (illustrative, 16 shown)&lt;/div&gt;
      &lt;div class=&quot;experts&quot; id=&quot;exs&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;meters&quot;&gt;
        &lt;div class=&quot;meter&quot;&gt;&lt;div class=&quot;v&quot; id=&quot;m1&quot;&gt;0&lt;/div&gt;&lt;div class=&quot;k&quot;&gt;Tokens routed&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;meter&quot;&gt;&lt;div class=&quot;v&quot;&gt;3B&lt;/div&gt;&lt;div class=&quot;k&quot;&gt;Active params / token&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;meter&quot;&gt;&lt;div class=&quot;v&quot;&gt;30B&lt;/div&gt;&lt;div class=&quot;k&quot;&gt;Total params&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;row&quot;&gt;
      &lt;button class=&quot;btn&quot; id=&quot;b1&quot;&gt;Route a token&lt;/button&gt;
      &lt;button class=&quot;btn alt&quot; id=&quot;b1a&quot;&gt;Auto-run&lt;/button&gt;
    &lt;/div&gt;
    &lt;p class=&quot;note&quot;&gt;&lt;b&gt;Why it matters:&lt;/b&gt; capacity of a larger dense model at the compute cost of a small one. NVIDIA reports the model deploys on a single DGX Spark (GB10) or a single H100, with an NVFP4 checkpoint alongside BF16 and support spanning Blackwell, Hopper, and Ampere (via W4A16).&lt;/p&gt;
  &lt;/div&gt;

  &lt;!-- PANEL 2 --&gt;
  &lt;div class=&quot;panel&quot; id=&quot;p1&quot;&gt;
    &lt;p class=&quot;lede&quot;&gt;Standard decoding emits one token per forward pass. Lightning bakes in &lt;b&gt;multi-token prediction&lt;/b&gt; during pretraining and ships two draft models, &lt;b&gt;DSpark&lt;/b&gt; and &lt;b&gt;DFlash&lt;/b&gt;, which propose a whole block of tokens that the main model then verifies in one pass.&lt;/p&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;track&quot;&gt;
        &lt;div class=&quot;tname&quot;&gt;Standard decoding &lt;span id=&quot;s1&quot;&gt;0 tokens&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;cells&quot; id=&quot;r1&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;track&quot; style=&quot;margin-bottom:0&quot;&gt;
        &lt;div class=&quot;tname&quot;&gt;Speculative decoding (draft block then verify) &lt;span id=&quot;s2&quot;&gt;0 tokens&lt;/span&gt;&lt;/div&gt;
        &lt;div class=&quot;cells&quot; id=&quot;r2&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;legend&quot;&gt;
        &lt;div&gt;&lt;i style=&quot;background:#3d3d3d&quot;&gt;&lt;/i&gt;Pending&lt;/div&gt;
        &lt;div&gt;&lt;i style=&quot;background:#2f4a00&quot;&gt;&lt;/i&gt;Drafted&lt;/div&gt;
        &lt;div&gt;&lt;i style=&quot;background:#76B900&quot;&gt;&lt;/i&gt;Accepted&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;row&quot;&gt;&lt;button class=&quot;btn&quot; id=&quot;b2&quot;&gt;Run the race&lt;/button&gt;&lt;/div&gt;
    &lt;p class=&quot;note&quot;&gt;&lt;b&gt;Reported result:&lt;/b&gt; NVIDIA states up to &lt;b&gt;4x output speed&lt;/b&gt; versus similar-sized models, placing Lightning on the accuracy-speed Pareto frontier of the Artificial Analysis Intelligence Index. On PinchBench, NVIDIA reports 86 percent accuracy while completing 10,000 tasks 30 percent faster than Qwen3.6 35B at comparable accuracy. Animation is illustrative, not a timing benchmark.&lt;/p&gt;
  &lt;/div&gt;

  &lt;!-- PANEL 3 --&gt;
  &lt;div class=&quot;panel&quot; id=&quot;p2&quot;&gt;
    &lt;p class=&quot;lede&quot;&gt;NeMo Switchyard is an open source routing library. Its &lt;b&gt;escalation router&lt;/b&gt; starts every session on the cheaper model and promotes it to a frontier model only when an LLM judge detects sustained difficulty.&lt;/p&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;lbl&quot;&gt;Agent calls (100 sampled)&lt;/div&gt;
      &lt;div class=&quot;dots&quot; id=&quot;dots&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;legend&quot;&gt;
        &lt;div&gt;&lt;i style=&quot;background:#76B900&quot;&gt;&lt;/i&gt;Nemotron 3.5 Lightning&lt;/div&gt;
        &lt;div&gt;&lt;i style=&quot;background:#8b5cf6&quot;&gt;&lt;/i&gt;Frontier model&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;lbl&quot; style=&quot;margin-top:14px&quot;&gt;Relative cost vs frontier-only baseline&lt;/div&gt;
      &lt;div class=&quot;bar&quot;&gt;&lt;div class=&quot;fill&quot; id=&quot;fill&quot;&gt;&lt;/div&gt;&lt;div class=&quot;bl&quot; id=&quot;bl&quot;&gt;100%&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;meters&quot;&gt;
        &lt;div class=&quot;meter&quot;&gt;&lt;div class=&quot;v&quot; id=&quot;mf&quot;&gt;0%&lt;/div&gt;&lt;div class=&quot;k&quot;&gt;Calls to frontier&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;meter&quot;&gt;&lt;div class=&quot;v&quot; id=&quot;mc&quot;&gt;0%&lt;/div&gt;&lt;div class=&quot;k&quot;&gt;Cost reduction&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;meter&quot;&gt;&lt;div class=&quot;v&quot;&gt;~6 pt&lt;/div&gt;&lt;div class=&quot;k&quot;&gt;Accuracy tradeoff&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;row&quot;&gt;&lt;button class=&quot;btn&quot; id=&quot;b3&quot;&gt;Run escalation routing&lt;/button&gt;&lt;/div&gt;
    &lt;p class=&quot;note&quot;&gt;&lt;b&gt;Source:&lt;/b&gt; LangChain benchmarked Switchyard on 145 multi-turn agentic tasks from its internal deep agents suite. Across five runs, routing between Nemotron 3.5 Lightning and Claude Opus 4.8 with the escalation router delivered a &lt;b&gt;74 percent cost reduction&lt;/b&gt; versus a frontier-only baseline, sending just &lt;b&gt;7 percent&lt;/b&gt; of calls to the frontier model, at a measured roughly 6-point accuracy tradeoff.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;All figures as published by NVIDIA and LangChain on August 11, 2026.&lt;/span&gt;
    &lt;span&gt;&lt;b&gt;Marktechpost&lt;/b&gt; | marktechpost.com&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var tabs=document.querySelectorAll('.tab'),pans=document.querySelectorAll('.panel');
  for(var i=0;i&lt;tabs.length;i++){
    tabs[i].addEventListener('click',function(){
      var n=this.getAttribute('data-t');
      for(var j=0;j&lt;tabs.length;j++){tabs[j].className='tab';pans[j].className='panel';}
      this.className='tab on';document.getElementById('p'+n).className='panel on';
      setTimeout(res,60);
    });
  }

  // ---- Panel 1
  var exs=document.getElementById('exs'),n=0;
  for(var k=0;k&lt;16;k++){var e=document.createElement('div');e.className='ex';e.textContent='E'+(k+1);exs.appendChild(e);}
  var cells=exs.children;
  var words=['git','pull','validate','tool','output','format','result','call','api','parse','json','retry','commit','diff','patch','done'];
  function route(){
    for(var a=0;a&lt;cells.length;a++)cells[a].className='ex';
    var p=[];while(p.length&lt;2){var r=Math.floor(Math.random()*16);if(p.indexOf(r)&lt;0)p.push(r);}
    for(var b=0;b&lt;p.length;b++)cells[p[b]].className='ex hot';
    document.getElementById('tok').textContent='token: &quot;'+words[n%words.length]+'&quot;  -&gt;  E'+(p[0]+1)+', E'+(p[1]+1);
    n++;document.getElementById('m1').textContent=n;
  }
  document.getElementById('b1').addEventListener('click',route);
  var iv=null;
  document.getElementById('b1a').addEventListener('click',function(){
    if(iv){clearInterval(iv);iv=null;this.textContent='Auto-run';}
    else{route();iv=setInterval(route,750);this.textContent='Stop';}
  });

  // ---- Panel 2
  var r1=document.getElementById('r1'),r2=document.getElementById('r2'),N=24;
  function build(){
    r1.innerHTML='';r2.innerHTML='';
    for(var i=0;i&lt;N;i++){
      var a=document.createElement('div');a.className='c f';r1.appendChild(a);
      var b=document.createElement('div');b.className='c f';r2.appendChild(b);
    }
  }
  build();
  var run2=false;
  document.getElementById('b2').addEventListener('click',function(){
    if(run2)return;run2=true;var btn=this;btn.disabled=true;build();
    var i1=0,i2=0,t1,t2;
    t1=setInterval(function(){
      if(i1&gt;=N){clearInterval(t1);return;}
      r1.children[i1].className='c g';i1++;
      document.getElementById('s1').textContent=i1+' tokens';
    },160);
    t2=setInterval(function(){
      if(i2&gt;=N){clearInterval(t2);run2=false;btn.disabled=false;return;}
      var blk=Math.min(4,N-i2);
      for(var q=0;q&lt;blk;q++)r2.children[i2+q].className='c d';
      (function(s,l){setTimeout(function(){
        for(var q=0;q&lt;l;q++)r2.children[s+q].className='c g';
        document.getElementById('s2').textContent=(s+l)+' tokens';
      },90);})(i2,blk);
      i2+=blk;
    },160);
  });

  // ---- Panel 3
  var dots=document.getElementById('dots');
  for(var d=0;d&lt;100;d++){var dv=document.createElement('div');dv.className='d';dots.appendChild(dv);}
  var run3=false;
  document.getElementById('b3').addEventListener('click',function(){
    if(run3)return;run3=true;var btn=this;btn.disabled=true;
    var ch=dots.children;
    for(var i=0;i&lt;100;i++)ch[i].className='d';
    document.getElementById('fill').className='fill pv';
    document.getElementById('fill').style.width='100%';
    document.getElementById('bl').textContent='100%';
    document.getElementById('mf').textContent='0%';
    document.getElementById('mc').textContent='0%';
    var fr=[];while(fr.length&lt;7){var r=Math.floor(Math.random()*100);if(fr.indexOf(r)&lt;0)fr.push(r);}
    var i=0;
    var t=setInterval(function(){
      if(i&gt;=100){
        clearInterval(t);
        document.getElementById('fill').className='fill';
        document.getElementById('fill').style.width='26%';
        document.getElementById('bl').textContent='26%';
        document.getElementById('mf').textContent='7%';
        document.getElementById('mc').textContent='74%';
        run3=false;btn.disabled=false;return;
      }
      ch[i].className=fr.indexOf(i)&gt;=0?'d fr':'d sm';
      i++;
    },18);
  });

  // ---- auto-resize
  function res(){
    var h=document.body.offsetHeight+40;
    if(window.parent)window.parent.postMessage({mtpFrame:'nemotron-lightning',height:h},'*');
  }
  window.addEventListener('load',res);
  setTimeout(res,300);setTimeout(res,900);
  window.addEventListener('resize',res);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
</div>
<script>
(function(){
  window.addEventListener('message', function(e){
    var d = e.data;
    if(!d || d.mtpFrame !== 'nemotron-lightning') return;
    var f = document.getElementById('mtp-nemotron-lightning');
    if(f && d.height) f.style.height = d.height + 'px';
  }, false);
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>30B open MoE with 3B active parameters, 1M context, OpenMDW-1.1 license, commercial use permitted.</li>



<li>Up to 4x output speed; PinchBench 10,000 tasks completed 30% faster than Qwen3.6 35B.</li>



<li>Speed comes from multi-token prediction plus DSpark and DFlash drafters, and an NVFP4 checkpoint.</li>



<li>Runs on 1x DGX Spark or 1x H100, and locally via Ollama, LM Studio, llama.cpp, and Unsloth.</li>



<li>NeMo Switchyard cut cost 74% in LangChain&#8217;s 145-task benchmark at a ~6-point accuracy tradeoff.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph">Try it on <a href="https://build.nvidia.com/nvidia/nemotron-3.5-lightning-30b-a3b" target="_blank" rel="noreferrer noopener"><strong>build.nvidia.com</strong></a> or <a href="https://openrouter.ai/nvidia/nemotron-3.5-lightning:free" target="_blank" rel="noreferrer noopener"><strong>OpenRouter</strong></a>, and download weights from <a href="https://huggingface.co/nvidia/NVIDIA-Nemotron-3.5-Lightning-30B-A3B-NVFP4" target="_blank" rel="noreferrer noopener"><strong>Hugging Face</strong></a> or <a href="https://modelscope.ai/collections/nv-community/Nemotron-35-Lightning" target="_blank" rel="noreferrer noopener"><strong>ModelScope</strong></a>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://magic.beehiiv.com/v1/f5e63dd4-5653-4f09-83e2-321a8b1ba526?email={{email}}" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/08/11/nvidia-ai-releases-nemotron-3-5-lightning-and-nemo-switchyard/">NVIDIA AI Releases Nemotron 3.5 Lightning: A 30B Open MoE with 3B Active Parameters, and NeMo Switchyard Model Router</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81682</post-id>	</item>
		<item>
		<title>Xiaomi&#8217;s MiLM Plus Releases PROVE: Perception-Aligned Object Removal Metrics RC-S and RC-T With a Real-World Video Benchmark</title>
		<link>https://www.marktechpost.com/2026/08/11/xiaomis-milm-plus-releases-prove-perception-aligned-object-removal-metrics-rc-s-and-rc-t-with-a-real-world-video-benchmark/</link>
		
		<dc:creator><![CDATA[Michal Sutter]]></dc:creator>
		<pubDate>Wed, 12 Aug 2026 05:05:52 +0000</pubDate>
				<category><![CDATA[AI Paper Summary]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Computer Vision]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81679</guid>

					<description><![CDATA[<p>Object removal models have improved faster than the metrics used to judge them. Diffusion erasers now reconstruct shadows, reflections and occluded structure convincingly, yet PSNR, SSIM, LPIPS, ReMOVE and CFD frequently rank their outputs the wrong way. The root cause is structural: erasure is an ill-posed, one-to-many task, so no single ground truth exists to [&#8230;]</p>
<p>The post <a href="https://www.marktechpost.com/2026/08/11/xiaomis-milm-plus-releases-prove-perception-aligned-object-removal-metrics-rc-s-and-rc-t-with-a-real-world-video-benchmark/">Xiaomi&#8217;s MiLM Plus Releases PROVE: Perception-Aligned Object Removal Metrics RC-S and RC-T With a Real-World Video Benchmark</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Object removal models have improved faster than the metrics used to judge them. Diffusion erasers now reconstruct shadows, reflections and occluded structure convincingly, yet PSNR, SSIM, LPIPS, ReMOVE and CFD frequently rank their outputs the wrong way. The root cause is structural: erasure is an ill-posed, one-to-many task, so no single ground truth exists to compare against. A team from <strong>MiLM Plus, Xiaomi Inc.</strong> has released <a href="https://xiaomi-research.github.io/prove/" target="_blank" rel="noreferrer noopener"><strong>PROVE</strong> (Perceptual RemOVal cohErence),</a> accepted at <strong>ACM MM 2026</strong>, to close that gap. PROVE pairs two perception-aligned metrics — <strong>RC-S</strong> for spatial coherence and <strong>RC-T</strong> for temporal consistency — with <strong>PROVE-Bench</strong>, a two-tier real-world video benchmark. Both metrics score the edited region locally, using sliding-window Maximum Mean Discrepancy over <a href="https://github.com/facebookresearch/dinov2">DINOv2</a> features, and neither requires a reference video.</p>



<h2 id="h-is-it-deployable" class="wp-block-heading"><strong>Is it deployable?</strong></h2>



<p class="wp-block-paragraph"><strong>Yes — as an evaluation harness, not a product feature.</strong> <a href="https://github.com/xiaomi-research/prove">PROVE</a> ships as an Apache 2.0 PyTorch repo with one CLI entry point (<code>run_prove_metrics.py</code>). It needs Python 3.10+, PyTorch 2.6+, Transformers 4.51+, and <a href="https://huggingface.co/facebook/dinov2-giant">DINOv2-giant</a> weights. Masks are mandatory; white pixels mark the removed object.</p>



<ul class="wp-block-list">
<li><strong>Company level</strong>: Any team with one GPU and a mask pipeline. RC-S runs at 134.6 ms/frame on a single RTX 4090, so nightly CI gates are practical for startups; enterprises with large editing catalogs gain most, since no paired ground truth is needed.</li>



<li><strong>Industries</strong>: Smartphone gallery and camera apps, short-video editing, e-commerce catalog cleanup, advertising and stock media, film/VFX post-production, real estate imagery, and privacy redaction in mapping archives.</li>



<li><strong>Applications</strong>: Model bake-offs, CI gates on RC-S/RC-T, tuning inference steps or quantization without a reference video, filtering training data, and shaping reward models.</li>



<li><strong>Who buys in</strong>: CV researchers and applied AI/ML engineers on editing teams first; MLOps engineers wiring the score into CI second; product managers running vendor comparisons third.</li>



<li><strong>Where it does not fit</strong>: Real-time on-device scoring, and side effects such as large shadows or reflections extending beyond the cropped evaluation region.</li>
</ul>



<h2 id="h-three-documented-failure-modes" class="wp-block-heading"><strong>Three documented failure modes</strong></h2>



<p class="wp-block-paragraph">Object removal is <strong>ill-posed and one-to-many</strong>: many restorations are plausible for the same hole, so no unique ground truth exists.</p>



<ul class="wp-block-list">
<li><strong>Full-reference bias</strong>: PSNR, SSIM and LPIPS assume point-to-point correspondence, so they reward copy-paste over genuine erasure. Residual shadows occupy few pixels and incur almost no penalty. Worse, cutting diffusion inference steps <em>improves</em> PSNR and SSIM while visual quality collapses — regression to the mean.</li>



<li><strong>No-reference blind spots</strong>: On ROSE-Bench, the authors progressively blur the masked region. Neither <a href="https://github.com/chandrasekaraditya/ReMOVE">ReMOVE</a> nor <a href="https://github.com/yeates/OmniPaint">CFD</a> degrades; both eventually surpass their unblurred baselines. CFD&#8217;s SAM-based hallucination term also misfires: a legitimately restored bicycle seat is flagged as a hallucination, scoring worse than the unedited input.</li>



<li><strong>Temporal insensitivity</strong>: Temporal Consistency and Temporal Flickering operate on full-frame features. Since the edited area is a small fraction of the frame, injected Random Drop and Random Replace corruptions on DAVIS barely move them, and sometimes move them the wrong way.</li>
</ul>



<h2 id="h-rc-s-and-rc-t" class="wp-block-heading"><strong>RC-S and RC-T</strong></h2>



<p class="wp-block-paragraph">Both share one idea: <strong>local distribution matching in deep feature space</strong> instead of global aggregation.</p>



<p class="wp-block-paragraph"><strong>RC-S (spatial)</strong>: Connected-component analysis splits the mask into independent targets. Each bounding box is expanded by one third of its side length, the crop goes to DINOv2, and the mask is downsampled to feature resolution. A w×w window slides across the feature map, computing squared <strong>Maximum Mean Discrepancy</strong> with a Gaussian RBF kernel between masked and local background features. Scores are averaged per target, then across targets.</p>



<p class="wp-block-paragraph"><strong>RC-T (temporal)</strong>: Adjacent frames are cropped jointly under the <em>union</em> of their masks to avoid misalignment, then MMD is computed only inside the <em>intersection</em> — the region restored in both frames. An ablation shows that removing the crop makes RC-T blind to injected corruption.</p>



<h2 id="h-results" class="wp-block-heading"><strong>Results</strong></h2>



<p class="wp-block-paragraph">Against human rankings from 20 participants aggregated by Borda count, <strong>RC-S reaches 0.59 average Kendall&#8217;s τ and 0.66 Spearman&#8217;s ρ</strong>, versus 0.26/0.29 for ReMOVE and 0.16/0.18 for CFD. It ranks first on five of six benchmarks. Background-only FR variants correlate <em>negatively</em> on most datasets. On RORD-Val, RC-S prefers the clean image over blurred and region-swapped variants in <strong>100% of cases</strong>; ReMOVE manages 60.06% and CFD 49.27% under blur. RC-T responds monotonically to rising corruption where TC and TF do not.</p>



<p class="wp-block-paragraph">Ablations isolate each contribution: DINOv2 (0.59 avg τ) beats DINOv3 (0.51) and SAM (0.44); dropping the sliding window costs 0.11; swapping MMD for cosine costs 0.07. RC-S is also the cheapest spatial metric tested, 13.7× faster than CFD.</p>



<h2 id="h-prove-bench" class="wp-block-heading"><strong>PROVE-Bench</strong></h2>



<p class="wp-block-paragraph"><strong>PROVE-M</strong> holds 80 paired videos: tripod-captured input and target-free footage shot within two minutes, SAM3 masks refined frame by frame, three-stage quality control, then Ken Burns-style motion augmentation applied synchronously to the triplet. Each clip is 81 frames at 1080p. <strong>PROVE-H</strong> adds 100 hard videos without ground truth — crowds, flowing water, flames, textured terrain, multi-puddle reflections, fast motion — using unrefined SAM3 masks on purpose.</p>



<p class="wp-block-paragraph">On the public leaderboard, <a href="https://github.com/xiaomi-research/svor">SVOR (1.3B)</a> leads combined RC-S at 0.5197; <a href="https://github.com/FudanCVL/EffectErase">EffectErase</a> leads RC-T at 0.2525.</p>



<!-- MTP PROVE Interactive Explainer :: paste into a WordPress Custom HTML block -->
<div id="mtp-prove-wrap" style="margin:26px 0;">
<iframe id="mtp-prove-frame" title="PROVE interactive explainer" scrolling="no" frameborder="0"
 style="width:100%;border:0;display:block;overflow:hidden;height:600px;"
 srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width,initial-scale=1&quot;&gt;
&lt;style&gt;
*{box-sizing:border-box;margin:0;padding:0}
body{background:#0b0b0d;color:#e9e9ec;font-family:-apple-system,BlinkMacSystemFont,&quot;Segoe UI&quot;,Roboto,Helvetica,Arial,sans-serif;padding:0}
#pv{max-width:900px;margin:0 auto;background:#0b0b0d;border:1px solid #22222a;border-radius:14px;overflow:hidden}
.hd{padding:18px 20px 14px;border-bottom:1px solid #1c1c23;background:linear-gradient(180deg,#131318 0%,#0b0b0d 100%)}
.eb{display:inline-block;font-size:10px;letter-spacing:.14em;text-transform:uppercase;color:#FF6900;border:1px solid rgba(255,105,0,.45);padding:3px 8px;border-radius:20px;margin-bottom:9px}
.hd h2{font-size:19px;line-height:1.3;font-weight:700;color:#fff}
.hd p{font-size:12.5px;color:#8d8d99;margin-top:6px;line-height:1.55}
.tabs{display:flex;flex-wrap:wrap;gap:6px;padding:12px 20px;border-bottom:1px solid #1c1c23;background:#0e0e12}
.tab{flex:1 1 auto;min-width:120px;font-size:11.5px;font-weight:600;padding:9px 10px;border-radius:8px;border:1px solid #26262f;background:#141419;color:#9a9aa6;cursor:pointer;transition:.18s;text-align:center}
.tab:hover{border-color:#3a3a46;color:#d5d5dd}
.tab.on{background:rgba(255,105,0,.13);border-color:#FF6900;color:#FF9440}
.pane{display:none;padding:20px}
.pane.on{display:block;animation:fi .3s ease}
@keyframes fi{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
h3{font-size:14.5px;color:#fff;margin-bottom:6px}
.sub{font-size:12px;color:#8d8d99;line-height:1.6;margin-bottom:14px}
.ctl{display:flex;align-items:center;gap:12px;flex-wrap:wrap;background:#111117;border:1px solid #22222a;border-radius:10px;padding:12px 14px;margin-bottom:14px}
.ctl label{font-size:11px;color:#9a9aa6;font-weight:600;letter-spacing:.03em}
input[type=range]{-webkit-appearance:none;appearance:none;flex:1;min-width:150px;height:4px;border-radius:3px;background:#2a2a34;outline:none}
input[type=range]::-webkit-slider-thumb{-webkit-appearance:none;width:16px;height:16px;border-radius:50%;background:#FF6900;cursor:pointer;border:2px solid #0b0b0d;box-shadow:0 0 0 3px rgba(255,105,0,.22)}
input[type=range]::-moz-range-thumb{width:16px;height:16px;border-radius:50%;background:#FF6900;cursor:pointer;border:2px solid #0b0b0d}
.val{font-size:12px;font-weight:700;color:#FF9440;font-variant-numeric:tabular-nums;min-width:44px;text-align:right}
.btn{font-size:11px;font-weight:600;padding:7px 12px;border-radius:7px;border:1px solid #33333f;background:#17171d;color:#c8c8d2;cursor:pointer;transition:.18s}
.btn:hover{border-color:#FF6900;color:#FF9440}
.btn.on{background:rgba(255,105,0,.14);border-color:#FF6900;color:#FF9440}
.grid2{display:grid;grid-template-columns:1fr 1fr;gap:14px}
.card{background:#111117;border:1px solid #22222a;border-radius:10px;padding:14px}
.mrow{display:flex;align-items:center;gap:10px;margin-bottom:11px}
.mname{font-size:11.5px;font-weight:600;width:88px;flex:0 0 88px;color:#b9b9c4}
.bar{flex:1;height:9px;background:#1e1e26;border-radius:5px;overflow:hidden}
.fill{height:100%;border-radius:5px;transition:width .45s cubic-bezier(.4,0,.2,1),background .3s}
.mval{font-size:11.5px;font-weight:700;font-variant-numeric:tabular-nums;width:56px;text-align:right}
.verdict{font-size:10.5px;font-weight:700;padding:2px 7px;border-radius:5px;letter-spacing:.03em;white-space:nowrap}
.ok{background:rgba(255,105,0,.15);color:#FF9440}
.bad{background:rgba(120,120,135,.16);color:#9a9aa6}
.stage{position:relative;background:#111117;border:1px solid #22222a;border-radius:10px;padding:14px;overflow:hidden}
.fgrid{display:grid;grid-template-columns:repeat(12,1fr);gap:3px;position:relative}
.cell{aspect-ratio:1;border-radius:2px;background:#1c6b3a;opacity:.55;transition:.25s}
.cell.msk{background:#FF6900;opacity:.5}
.cell.win{opacity:1;outline:1px solid #fff;outline-offset:-1px}
.winbox{position:absolute;border:2px solid #fff;border-radius:4px;pointer-events:none;transition:.35s cubic-bezier(.4,0,.2,1);box-shadow:0 0 14px rgba(255,105,0,.5)}
.legend{display:flex;gap:14px;flex-wrap:wrap;margin-top:12px;font-size:10.5px;color:#8d8d99}
.dot{display:inline-block;width:9px;height:9px;border-radius:2px;margin-right:5px;vertical-align:-1px}
.readout{margin-top:13px;background:#0e0e12;border:1px solid #22222a;border-left:3px solid #FF6900;border-radius:8px;padding:11px 13px;font-size:12px;line-height:1.6;color:#b9b9c4}
.readout b{color:#fff}
.mono{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;color:#FF9440;font-size:11.5px}
.frames{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:12px}
.fr{background:#0e0e12;border:1px solid #22222a;border-radius:8px;padding:10px;position:relative}
.fr h4{font-size:10.5px;color:#8d8d99;font-weight:600;margin-bottom:8px;letter-spacing:.05em;text-transform:uppercase}
.blob{height:74px;border-radius:6px;position:relative;background:#16213a;overflow:hidden}
.shape{position:absolute;background:#FF6900;opacity:.62;border-radius:6px;transition:.5s cubic-bezier(.4,0,.2,1)}
.uni{position:absolute;border:1.5px dashed #FF9440;border-radius:6px;transition:.5s}
.inter{position:absolute;background:rgba(255,255,255,.28);border:1.5px solid #fff;border-radius:4px;transition:.5s}
table{width:100%;border-collapse:collapse;font-size:11.5px}
th,td{padding:7px 8px;text-align:left;border-bottom:1px solid #1e1e26}
th{color:#8d8d99;font-weight:600;font-size:10.5px;text-transform:uppercase;letter-spacing:.05em}
td{color:#c8c8d2;font-variant-numeric:tabular-nums}
tr.hl td{color:#FF9440;font-weight:700}
.note{font-size:10.5px;color:#6f6f7c;line-height:1.6;margin-top:12px;font-style:italic}
.ft{padding:13px 20px;border-top:1px solid #1c1c23;background:#0e0e12;display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap}
.ft span{font-size:10.5px;color:#6f6f7c}
.ft b{color:#FF6900}
.ft a{color:#8d8d99;text-decoration:none;font-size:10.5px;border-bottom:1px dotted #3a3a46}
.ft a:hover{color:#FF9440}
@media(max-width:640px){.grid2{grid-template-columns:1fr}.frames{grid-template-columns:1fr}.hd h2{font-size:16.5px}.tab{min-width:calc(50% - 3px)}.mname{width:72px;flex:0 0 72px}.pane{padding:15px}}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;pv&quot;&gt;
  &lt;div class=&quot;hd&quot;&gt;
    &lt;div class=&quot;eb&quot;&gt;Interactive Explainer&lt;/div&gt;
    &lt;h2&gt;PROVE: how Removal Coherence scores object erasure without a ground truth&lt;/h2&gt;
    &lt;p&gt;Four interactive views of Xiaomi MiLM Plus's ACM MM 2026 framework: why existing metrics break, how RC-S and RC-T are computed, and how well each metric tracks human ranking.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;tabs&quot;&gt;
    &lt;div class=&quot;tab on&quot; data-t=&quot;0&quot;&gt;1 &amp;middot; The blur bias&lt;/div&gt;
    &lt;div class=&quot;tab&quot; data-t=&quot;1&quot;&gt;2 &amp;middot; RC-S sliding MMD&lt;/div&gt;
    &lt;div class=&quot;tab&quot; data-t=&quot;2&quot;&gt;3 &amp;middot; RC-T across frames&lt;/div&gt;
    &lt;div class=&quot;tab&quot; data-t=&quot;3&quot;&gt;4 &amp;middot; Human alignment&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- PANE 0 --&gt;
  &lt;div class=&quot;pane on&quot; id=&quot;p0&quot;&gt;
    &lt;h3&gt;&quot;Blur is Clean&quot;: drag the blur radius inside the mask&lt;/h3&gt;
    &lt;p class=&quot;sub&quot;&gt;The paper applies progressive Gaussian blur inside the masked region on ROSE-Bench and re-scores the same image. A perception-aligned metric should get worse. Two of the three do not.&lt;/p&gt;
    &lt;div class=&quot;ctl&quot;&gt;
      &lt;label&gt;Blur radius&lt;/label&gt;
      &lt;input type=&quot;range&quot; id=&quot;bl&quot; min=&quot;0&quot; max=&quot;30&quot; value=&quot;0&quot;&gt;
      &lt;div class=&quot;val&quot; id=&quot;blv&quot;&gt;0.0&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;grid2&quot;&gt;
      &lt;div class=&quot;card&quot;&gt;
        &lt;div class=&quot;mrow&quot;&gt;&lt;div class=&quot;mname&quot;&gt;ReMOVE &amp;uarr;&lt;/div&gt;&lt;div class=&quot;bar&quot;&gt;&lt;div class=&quot;fill&quot; id=&quot;f_re&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;v_re&quot;&gt;&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;mrow&quot;&gt;&lt;div class=&quot;mname&quot;&gt;CFD &amp;darr;&lt;/div&gt;&lt;div class=&quot;bar&quot;&gt;&lt;div class=&quot;fill&quot; id=&quot;f_cf&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;v_cf&quot;&gt;&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;mrow&quot;&gt;&lt;div class=&quot;mname&quot;&gt;RC-S &amp;uarr;&lt;/div&gt;&lt;div class=&quot;bar&quot;&gt;&lt;div class=&quot;fill&quot; id=&quot;f_rc&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mval&quot; id=&quot;v_rc&quot;&gt;&lt;/div&gt;&lt;/div&gt;
        &lt;div style=&quot;display:flex;gap:6px;flex-wrap:wrap;margin-top:4px&quot;&gt;
          &lt;span class=&quot;verdict bad&quot; id=&quot;j_re&quot;&gt;&amp;mdash;&lt;/span&gt;
          &lt;span class=&quot;verdict bad&quot; id=&quot;j_cf&quot;&gt;&amp;mdash;&lt;/span&gt;
          &lt;span class=&quot;verdict ok&quot; id=&quot;j_rc&quot;&gt;&amp;mdash;&lt;/span&gt;
        &lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;card&quot;&gt;
        &lt;div class=&quot;fr&quot; style=&quot;border:0;padding:0&quot;&gt;
          &lt;h4&gt;Masked region preview&lt;/h4&gt;
          &lt;div class=&quot;blob&quot; id=&quot;prev&quot; style=&quot;height:112px;background:#1b2436&quot;&gt;
            &lt;div class=&quot;shape&quot; id=&quot;pshape&quot; style=&quot;left:26%;top:16%;width:48%;height:68%;background:#FF6900;opacity:.5&quot;&gt;&lt;/div&gt;
          &lt;/div&gt;
        &lt;/div&gt;
        &lt;div class=&quot;readout&quot; id=&quot;ro0&quot;&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;p class=&quot;note&quot;&gt;Metric directions and axis ranges follow Figure 4 of the paper (ReMOVE 0.902&amp;ndash;0.904, CFD 0.30&amp;ndash;0.33, RC-S 0.57&amp;ndash;0.60); intermediate values are interpolated for illustration.&lt;/p&gt;
  &lt;/div&gt;

  &lt;!-- PANE 1 --&gt;
  &lt;div class=&quot;pane&quot; id=&quot;p1&quot;&gt;
    &lt;h3&gt;RC-S: crop &amp;rarr; DINOv2 features &amp;rarr; sliding-window MMD&lt;/h3&gt;
    &lt;p class=&quot;sub&quot;&gt;Each erased target is cropped with its bounding box expanded by 1/3 of its side length. DINOv2 features are extracted, the mask is downsampled to feature resolution, and a w&amp;times;w window slides across it. At each position, squared MMD compares masked features against local background features.&lt;/p&gt;
    &lt;div class=&quot;ctl&quot;&gt;
      &lt;button class=&quot;btn on&quot; id=&quot;play&quot;&gt;&amp;#9658; Play sweep&lt;/button&gt;
      &lt;label&gt;Removal quality&lt;/label&gt;
      &lt;button class=&quot;btn on&quot; data-q=&quot;0&quot;&gt;Clean&lt;/button&gt;
      &lt;button class=&quot;btn&quot; data-q=&quot;1&quot;&gt;Blurry&lt;/button&gt;
      &lt;button class=&quot;btn&quot; data-q=&quot;2&quot;&gt;Residual left&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;fgrid&quot; id=&quot;fg&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;winbox&quot; id=&quot;wb&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;legend&quot;&gt;
        &lt;span&gt;&lt;i class=&quot;dot&quot; style=&quot;background:#FF6900&quot;&gt;&lt;/i&gt;Masked / restored features&lt;/span&gt;
        &lt;span&gt;&lt;i class=&quot;dot&quot; style=&quot;background:#1c6b3a&quot;&gt;&lt;/i&gt;Background features&lt;/span&gt;
        &lt;span&gt;&lt;i class=&quot;dot&quot; style=&quot;background:#fff&quot;&gt;&lt;/i&gt;Current w&amp;times;w window&lt;/span&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;readout&quot; id=&quot;ro1&quot;&gt;&lt;/div&gt;
    &lt;p class=&quot;note&quot;&gt;Window positions and per-window MMD values are illustrative; the pipeline, cropping rule and averaging order follow Equations 1&amp;ndash;6 of the paper.&lt;/p&gt;
  &lt;/div&gt;

  &lt;!-- PANE 2 --&gt;
  &lt;div class=&quot;pane&quot; id=&quot;p2&quot;&gt;
    &lt;h3&gt;RC-T: union crop, intersection evaluation&lt;/h3&gt;
    &lt;p class=&quot;sub&quot;&gt;Cropping two adjacent frames independently would misalign them. RC-T crops both with the union of their masks, then measures feature drift only inside the intersection &amp;mdash; the region restored in both frames.&lt;/p&gt;
    &lt;div class=&quot;ctl&quot;&gt;
      &lt;label&gt;Object motion between frames&lt;/label&gt;
      &lt;input type=&quot;range&quot; id=&quot;mv&quot; min=&quot;0&quot; max=&quot;100&quot; value=&quot;30&quot;&gt;
      &lt;div class=&quot;val&quot; id=&quot;mvv&quot;&gt;30%&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;frames&quot;&gt;
      &lt;div class=&quot;fr&quot;&gt;&lt;h4&gt;Frame t&lt;/h4&gt;&lt;div class=&quot;blob&quot;&gt;&lt;div class=&quot;shape&quot; id=&quot;s1&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;fr&quot;&gt;&lt;h4&gt;Frame t+1&lt;/h4&gt;&lt;div class=&quot;blob&quot;&gt;&lt;div class=&quot;shape&quot; id=&quot;s2&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;fr&quot;&gt;&lt;h4&gt;Shared evaluation region&lt;/h4&gt;
      &lt;div class=&quot;blob&quot; id=&quot;ub&quot; style=&quot;height:88px&quot;&gt;&lt;div class=&quot;uni&quot; id=&quot;un&quot;&gt;&lt;/div&gt;&lt;div class=&quot;inter&quot; id=&quot;it&quot;&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;div class=&quot;readout&quot; id=&quot;ro2&quot;&gt;&lt;/div&gt;
    &lt;p class=&quot;note&quot;&gt;Geometry is schematic. The union-crop and intersection-mask rules follow Equations 7&amp;ndash;12; the paper reports that removing the crop step makes RC-T insensitive to injected corruption.&lt;/p&gt;
  &lt;/div&gt;

  &lt;!-- PANE 3 --&gt;
  &lt;div class=&quot;pane&quot; id=&quot;p3&quot;&gt;
    &lt;h3&gt;Correlation with human rankings (Kendall's &amp;tau;)&lt;/h3&gt;
    &lt;p class=&quot;sub&quot;&gt;Twenty participants ranked four models per sample; rankings were aggregated with Borda count. Pick a benchmark to see how each metric's induced ranking tracks those human judgments.&lt;/p&gt;
    &lt;div class=&quot;ctl&quot; id=&quot;bsel&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;card&quot; id=&quot;bars&quot;&gt;&lt;/div&gt;
    &lt;table style=&quot;margin-top:14px&quot;&gt;
      &lt;tr&gt;&lt;th&gt;Metric&lt;/th&gt;&lt;th&gt;Avg &amp;tau;&lt;/th&gt;&lt;th&gt;Avg &amp;rho;&lt;/th&gt;&lt;th&gt;Cost&lt;/th&gt;&lt;/tr&gt;
      &lt;tr&gt;&lt;td&gt;ReMOVE&lt;/td&gt;&lt;td&gt;0.26&lt;/td&gt;&lt;td&gt;0.29&lt;/td&gt;&lt;td&gt;180.7 ms/frame&lt;/td&gt;&lt;/tr&gt;
      &lt;tr&gt;&lt;td&gt;CFD&lt;/td&gt;&lt;td&gt;0.16&lt;/td&gt;&lt;td&gt;0.18&lt;/td&gt;&lt;td&gt;1842.8 ms/frame&lt;/td&gt;&lt;/tr&gt;
      &lt;tr class=&quot;hl&quot;&gt;&lt;td&gt;RC-S&lt;/td&gt;&lt;td&gt;0.59&lt;/td&gt;&lt;td&gt;0.66&lt;/td&gt;&lt;td&gt;134.6 ms/frame&lt;/td&gt;&lt;/tr&gt;
    &lt;/table&gt;
    &lt;p class=&quot;note&quot;&gt;Values from Table 2 and Table 14 of the paper. &quot;&amp;mdash;&quot; means the metric is not applicable to that benchmark (no ground truth available). Timings measured on a single NVIDIA RTX 4090.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;Source: &lt;b&gt;PROVE&lt;/b&gt; &amp;middot; MiLM Plus, Xiaomi Inc. &amp;middot; ACM MM 2026 &amp;middot; Apache 2.0&lt;/span&gt;
    &lt;a href=&quot;https://www.marktechpost.com/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Built by Marktechpost&lt;/a&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
var $=function(s){return document.getElementById(s)};

/* ---------- tabs ---------- */
var tabs=document.querySelectorAll('.tab');
for(var i=0;i&lt;tabs.length;i++){(function(t){t.addEventListener('click',function(){
  for(var j=0;j&lt;tabs.length;j++){tabs[j].classList.remove('on');document.getElementById('p'+j).classList.remove('on')}
  t.classList.add('on');document.getElementById('p'+t.getAttribute('data-t')).classList.add('on');rz();
})})(tabs[i])}

/* ---------- pane 0 : blur bias ---------- */
function lerp(a,b,x){return a+(b-a)*x}
function drawBlur(){
  var r=+$('bl').value, x=r/30, rad=(x*3).toFixed(1);
  $('blv').textContent=rad;
  $('pshape').style.filter='blur('+(x*11)+'px)';
  $('pshape').style.opacity=(0.5-x*0.16).toFixed(2);
  /* ReMOVE: rises to a peak near blur 1.0 then stays above baseline (Fig.4) */
  var re=0.9020+0.0022*Math.sin(Math.min(x,1)*Math.PI*0.9)+0.0008*x;
  var cf=lerp(0.330,0.300,x);          /* CFD falls = &quot;looks better&quot; */
  var rc=lerp(0.600,0.570,x);          /* RC-S falls = correctly penalised */
  $('v_re').textContent=re.toFixed(4);
  $('v_cf').textContent=cf.toFixed(3);
  $('v_rc').textContent=rc.toFixed(3);
  $('f_re').style.width=(((re-0.9015)/0.0035)*100)+'%';
  $('f_cf').style.width=(((0.335-cf)/0.04)*100)+'%';
  $('f_rc').style.width=(((rc-0.560)/0.05)*100)+'%';
  $('f_re').style.background='#7a7a88';
  $('f_cf').style.background='#7a7a88';
  $('f_rc').style.background='#FF6900';
  var up=re&gt;=0.9020, cfb=cf&lt;=0.330;
  $('j_re').textContent=x===0?'baseline':(up?'score improved &amp;#10007;':'score dropped');
  $('j_re').innerHTML=x===0?'baseline':(up?'improved &amp;#10007;':'dropped');
  $('j_cf').innerHTML=x===0?'baseline':(cfb?'improved &amp;#10007;':'worsened');
  $('j_rc').innerHTML=x===0?'baseline':'degraded &amp;#10003;';
  $('j_re').className='verdict '+(x&gt;0&amp;&amp;up?'bad':'bad');
  $('j_cf').className='verdict '+(x&gt;0&amp;&amp;cfb?'bad':'bad');
  $('j_rc').className='verdict ok';
  $('ro0').innerHTML= x===0
    ? 'Unblurred baseline. Now drag the slider &amp;mdash; nothing about the image gets better from here.'
    : 'At blur radius &lt;b&gt;'+rad+'&lt;/b&gt; the restored patch is visibly smoother and has lost texture. &lt;b&gt;ReMOVE and CFD both move in the direction they call &quot;better.&quot;&lt;/b&gt; RC-S moves the other way, because a blurred patch is a different &lt;i&gt;distribution&lt;/i&gt; from its surrounding background, and MMD measures exactly that.';
  rz();
}
$('bl').addEventListener('input',drawBlur);

/* ---------- pane 1 : sliding window ---------- */
var COLS=12,ROWS=8,quality=0,pos=0,timer=null;
var maskSet={};
(function(){ /* irregular blob in feature grid */
  var blob=[[4,2],[5,2],[6,2],[4,3],[5,3],[6,3],[7,3],[3,4],[4,4],[5,4],[6,4],[7,4],[4,5],[5,5],[6,5],[5,6]];
  for(var i=0;i&lt;blob.length;i++) maskSet[blob[i][0]+','+blob[i][1]]=1;
})();
var fg=$('fg');
for(var r=0;r&lt;ROWS;r++){for(var c=0;c&lt;COLS;c++){
  var d=document.createElement('div');
  d.className='cell'+(maskSet[c+','+r]?' msk':'');
  d.setAttribute('data-c',c);d.setAttribute('data-r',r);
  fg.appendChild(d);
}}
var winCells=[];
function moveWin(){
  var W=3, maxc=COLS-W, maxr=ROWS-W;
  var total=(maxc+1)*(maxr+1);
  pos=(pos)%total;
  var cr=Math.floor(pos/(maxc+1)), cc=pos%(maxc+1);
  var cells=fg.querySelectorAll('.cell');
  for(var i=0;i&lt;cells.length;i++)cells[i].classList.remove('win');
  var nm=0,nb=0;
  for(var r=cr;r&lt;cr+W;r++)for(var c=cc;c&lt;cc+W;c++){
    var el=fg.querySelector('.cell[data-c=&quot;'+c+'&quot;][data-r=&quot;'+r+'&quot;]');
    if(el){el.classList.add('win'); if(maskSet[c+','+r])nm++;else nb++;}
  }
  /* position the outline box */
  var first=fg.querySelector('.cell[data-c=&quot;'+cc+'&quot;][data-r=&quot;'+cr+'&quot;]');
  if(first){
    var fr=first.getBoundingClientRect(), pr=fg.getBoundingClientRect(), cw=fr.width+3;
    $('wb').style.left=(fg.offsetLeft+(fr.left-pr.left)-2)+'px';
    $('wb').style.top=(fg.offsetTop+(fr.top-pr.top)-2)+'px';
    $('wb').style.width=(cw*3-3+4)+'px';
    $('wb').style.height=(cw*3-3+4)+'px';
  }
  var mmd, txt;
  if(nm===0){
    $('ro1').innerHTML='This window contains &lt;b&gt;no masked features&lt;/b&gt;, so it is skipped &amp;mdash; RC-S only averages over windows that intersect the mask.';
  }else{
    var base=[0.021,0.084,0.163][quality];
    mmd=(base*(0.75+0.5*(nm/9))).toFixed(3);
    txt=['features inside the mask are statistically indistinguishable from the surrounding background',
         'the restored patch is over-smoothed, so its feature distribution has drifted from the background',
         'residual foreground content survives inside the mask, producing a large local distribution gap'][quality];
    $('ro1').innerHTML='Window covers &lt;b&gt;'+nm+'&lt;/b&gt; masked and &lt;b&gt;'+nb+'&lt;/b&gt; background feature positions. Local &lt;span class=&quot;mono&quot;&gt;MMD&amp;sup2; = '+mmd+'&lt;/span&gt; &amp;mdash; '+txt+'. If a window falls entirely inside the mask, all background features in the crop are used as the reference instead.';
  }
}
function tick(){pos++;moveWin()}
$('play').addEventListener('click',function(){
  if(timer){clearInterval(timer);timer=null;this.innerHTML='&amp;#9658; Play sweep';this.classList.remove('on')}
  else{timer=setInterval(tick,620);this.innerHTML='&amp;#10074;&amp;#10074; Pause';this.classList.add('on')}
});
var qb=document.querySelectorAll('[data-q]');
for(var i=0;i&lt;qb.length;i++){(function(b){b.addEventListener('click',function(){
  for(var j=0;j&lt;qb.length;j++)qb[j].classList.remove('on');
  b.classList.add('on');quality=+b.getAttribute('data-q');moveWin();
})})(qb[i])}

/* ---------- pane 2 : RC-T ---------- */
function drawT(){
  var m=+$('mv').value; $('mvv').textContent=m+'%';
  var off=m*0.30;
  $('s1').style.cssText='left:16%;top:18%;width:40%;height:62%';
  $('s2').style.cssText='left:'+(16+off*0.45)+'%;top:18%;width:40%;height:62%';
  var uL=16, uR=16+off*0.45+40, iL=16+off*0.45, iR=56;
  var interW=Math.max(0,iR-iL);
  $('un').style.cssText='left:'+uL+'%;top:14%;width:'+(uR-uL)+'%;height:70%';
  $('it').style.cssText='left:'+iL+'%;top:18%;width:'+interW+'%;height:62%;'+(interW&lt;=0?'opacity:0':'');
  var pct=Math.round(interW/40*100);
  $('ro2').innerHTML= interW&lt;=0
    ? 'The two masks no longer overlap. There is &lt;b&gt;no shared restored region&lt;/b&gt;, so this frame pair contributes no valid window to RC-T.'
    : 'Union crop spans both masks (dashed). The intersection covers &lt;b&gt;'+pct+'%&lt;/b&gt; of the target and is the only area scored. Feature drift there means the model re-hallucinated the same background differently between frames &amp;mdash; visible flickering. Global metrics such as TC and TF average over the whole frame, so an artifact this small is diluted by unchanged background.';
  rz();
}
$('mv').addEventListener('input',drawT);

/* ---------- pane 3 : correlations ---------- */
var BM=['RORD','OBER-Wild','DAVIS','ROSE','PROVE-M','PROVE-H','AVG'];
var D={
 'PSNR':[0.01,null,null,0.36,0.38,null,0.25],
 'SSIM':[-0.22,null,null,0.11,0.43,null,0.11],
 'LPIPS':[-0.23,null,null,0.24,0.33,null,0.12],
 'm-LPIPS':[0.19,null,null,0.68,0.68,null,0.52],
 'ReMOVE':[0.06,0.54,0.15,0.21,0.33,0.23,0.26],
 'CFD':[-0.04,0.40,0.21,0.03,0.24,0.12,0.16],
 'RC-S':[0.31,0.57,0.60,0.61,0.70,0.76,0.59]
};
var cur=6;
var bsel=$('bsel');
for(var i=0;i&lt;BM.length;i++){(function(k){
  var b=document.createElement('button');
  b.className='btn'+(k===6?' on':'');b.textContent=BM[k];
  b.addEventListener('click',function(){
    var all=bsel.querySelectorAll('.btn');
    for(var j=0;j&lt;all.length;j++)all[j].classList.remove('on');
    b.classList.add('on');cur=k;drawBars();
  });
  bsel.appendChild(b);
})(i)}
function drawBars(){
  var h='';
  for(var k in D){
    var v=D[k][cur];
    var isRC=(k==='RC-S');
    if(v===null){
      h+='&lt;div class=&quot;mrow&quot;&gt;&lt;div class=&quot;mname&quot;&gt;'+k+'&lt;/div&gt;&lt;div class=&quot;bar&quot;&gt;&lt;/div&gt;&lt;div class=&quot;mval&quot; style=&quot;color:#5c5c68&quot;&gt;&amp;mdash;&lt;/div&gt;&lt;/div&gt;';
    }else{
      var w=Math.max(0,(v+0.3)/1.1*100);
      h+='&lt;div class=&quot;mrow&quot;&gt;&lt;div class=&quot;mname&quot; style=&quot;'+(isRC?'color:#FF9440':'')+'&quot;&gt;'+k+'&lt;/div&gt;&lt;div class=&quot;bar&quot;&gt;&lt;div class=&quot;fill&quot; style=&quot;width:'+w+'%;background:'+(isRC?'#FF6900':(v&lt;0?'#5a3a3a':'#4a4a58'))+'&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;mval&quot; style=&quot;'+(isRC?'color:#FF9440':(v&lt;0?'color:#a06a6a':''))+'&quot;&gt;'+v.toFixed(2)+'&lt;/div&gt;&lt;/div&gt;';
    }
  }
  $('bars').innerHTML=h;rz();
}

/* ---------- resize ---------- */
function rz(){
  try{
    var el=document.getElementById('pv');
    var hgt=el.offsetHeight+40;
    parent.postMessage({mtpProveHeight:hgt},'*');
  }catch(e){}
}
window.addEventListener('resize',function(){moveWin();rz()});

drawBlur();moveWin();drawT();drawBars();
setTimeout(function(){moveWin();rz()},260);
setTimeout(rz,900);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
</div>
<script>
(function(){
  var f=document.getElementById('mtp-prove-frame');
  window.addEventListener('message',function(e){
    if(e&&e.data&&e.data.mtpProveHeight&&f){ f.style.height=e.data.mtpProveHeight+'px'; }
  },false);
})();
</script>




<p class="wp-block-paragraph"></p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>RC-S and RC-T score removal locally via sliding-window MMD on DINOv2 features, no ground truth required.</li>



<li>RC-S hits 0.59 average Kendall&#8217;s τ against human rankings versus 0.26 for ReMOVE and 0.16 for CFD.</li>



<li>ReMOVE and CFD both reward blur; RC-S degrades monotonically and wins 100% of perturbation tests.</li>



<li>PROVE-Bench adds 80 motion-augmented paired videos plus 100 GT-free hard videos, all real-world 1080p.</li>



<li>Apache 2.0 code and Hugging Face dataset; RC-S runs at 134.6 ms/frame, 13.7× cheaper than CFD.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the <strong><a href="https://arxiv.org/abs/2605.14534" target="_blank" rel="noreferrer noopener">Paper</a>,</strong> <strong><a href="https://github.com/xiaomi-research/prove" target="_blank" rel="noreferrer noopener">GitHub Repo</a></strong>, <a href="https://xiaomi-research.github.io/prove/" target="_blank" rel="noreferrer noopener"><strong>Project Page</strong></a>, and <a href="https://huggingface.co/datasets/HigherHu/PROVE-Bench" target="_blank" rel="noreferrer noopener"><strong>Datase</strong>t</a>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://magic.beehiiv.com/v1/f5e63dd4-5653-4f09-83e2-321a8b1ba526?email={{email}}" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/08/11/xiaomis-milm-plus-releases-prove-perception-aligned-object-removal-metrics-rc-s-and-rc-t-with-a-real-world-video-benchmark/">Xiaomi&#8217;s MiLM Plus Releases PROVE: Perception-Aligned Object Removal Metrics RC-S and RC-T With a Real-World Video Benchmark</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81679</post-id>	</item>
		<item>
		<title>The Video Production Stack Now Fits on One Desk: LTX-2.5 Launches as NVIDIA-Accelerated Open Weights World Model</title>
		<link>https://www.marktechpost.com/2026/08/11/the-video-production-stack-now-fits-on-one-desk-ltx-2-5-launches-as-nvidia-accelerated-open-weights-world-model/</link>
		
		<dc:creator><![CDATA[Jean-marc Mommessin&nbsp;and&nbsp;Asif Razzaq]]></dc:creator>
		<pubDate>Tue, 11 Aug 2026 20:02:03 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Open Source]]></category>
		<category><![CDATA[Physical AI]]></category>
		<category><![CDATA[Promote]]></category>
		<category><![CDATA[Sponsored]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Uncategorized]]></category>
		<category><![CDATA[World Model]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81673</guid>

					<description><![CDATA[<p>LTX-2.5 brings frontier video generation to local NVIDIA hardware: 6.8-second clips, native multishot, day-one ComfyUI, open weights.</p>
<p>The post <a href="https://www.marktechpost.com/2026/08/11/the-video-production-stack-now-fits-on-one-desk-ltx-2-5-launches-as-nvidia-accelerated-open-weights-world-model/">The Video Production Stack Now Fits on One Desk: LTX-2.5 Launches as NVIDIA-Accelerated Open Weights World Model</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">Video production is shifting as social clips, ad creative and film pre-visualization move from cloud to local GPUs.<a href="https://ltx.io/newsroom/introducing-ltx-2-5"> LTX</a> today released <strong>LTX-2.5</strong>, an open weights world model for video generation, real-time applications, and physical AI, built for exactly that shift. LTX optimized the model for local inference on<a href="https://www.nvidia.com/en-us/geforce/graphics-cards/"> NVIDIA RTX GPUs</a> and<a href="https://www.nvidia.com/en-us/products/workstations/dgx-spark/"> NVIDIA DGX Spark</a>, cutting VRAM requirements so a frontier world model runs on hardware creators already own. The release anchors NVIDIA&#8217;s month-long<a href="https://blogs.nvidia.com/blog/local-ai-open-source-models-agents-nemotron/"> local AI series</a>, launched the same day as its open Nemotron 3.5 Lightning agent model. The signal from both: open models, accelerated locally, are becoming default production infrastructure.</p>



<h2 id="h-what-local-generation-changes-for-creators" class="wp-block-heading"><strong>What Local Generation Changes for Creators</strong></h2>



<p class="wp-block-paragraph">LTX-2.5 puts something in creators&#8217; hands that used to sit behind a studio door: real consistency. Native multishot generation renders a whole sequence as one coherent piece, holding a character&#8217;s look shot to shot, fixing the glitching that made earlier open models unusable for campaigns. Add a sharper<a href="https://deepmind.google/models/gemma/gemma-4/"> Gemma 4</a> language backbone and a new decoder that cuts artifacts in high-motion shots, and the output is close to post-ready. It all runs on a consumer<a href="https://www.nvidia.com/en-us/geforce/graphics-cards/"> NVIDIA RTX GPU</a>, straight inside<a href="https://comfy.org/"> ComfyUI</a>. One person at a desk can lock a branded character or signature style with a quick LoRA fine-tune. No studio. No cloud. No IP leaving the machine.</p>



<p class="wp-block-paragraph">That is the real shift: the entire production stack now fits on a single desktop. What used to take a crew, a shoot day, a render farm, and a cloud bill now happens on the RTX card already in the machine. Additional clips carry no per-generation fees or metered credits. That rewires how creators work: experiment widely, chase a dozen directions instead of betting on one safe idea, and let the GPU batch-generate a week of content overnight. You wake up to a folder full of options.</p>



<p class="wp-block-paragraph">For short-form creators and ad teams on constant refresh, that is transformational. Ad fatigue commonly sets in within 7 to 10 days, so the bottleneck was never ideas; it was the cost and time of producing enough of them. Local generation erases it: spin up variations on the same brief, test ten hooks, localize for five markets, and refresh creative before fatigue arrives. Solo creators and small teams can now match the output volume of a full studio with <strong>one RTX GPU on a desk.</strong></p>



<h2 id="h-speed-the-numbers-behind-the-story" class="wp-block-heading"><strong>Speed: The Numbers Behind the Story</strong></h2>



<p class="wp-block-paragraph">None of this matters unless generation is fast, and it is. In LTX&#8217;s published image-to-video benchmark, a 10-second clip takes <strong>6.8 seconds on-prem running on 2x NVIDIA GB200</strong> and <strong>23.7 seconds via the LTX API</strong>. The fastest closed alternatives listed, Omni Flash, Grok 1.5, and Veo 3.1, land at 52 to 70 seconds. Slower systems stretch far beyond:Seedance 2.0 at 196, FLUX 3 at 259, Seedance 2.5 at 317, and Kling 3.0 Pro at 398. On-prem, LTX-2.5 generates faster than the clip&#8217;s own runtime, 7.6x faster than the nearest closed alternative and roughly 58x faster than the slowest. That gap makes overnight batch generation and rapid A/B iteration practical, not theoretical.</p>



<!-- LTX-2.5 Speed Benchmark Chart (animated) | Marktechpost | Paste into a Custom HTML block -->
<div id="mtp-ltx25-speed-wrap" style="max-width:820px;margin:24px auto;">
<iframe id="mtp-ltx25-speed" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;style&gt;
  * { margin:0; padding:0; box-sizing:border-box; }
  body { background:#0A0A0A; font-family:'Segoe UI', -apple-system, Arial, sans-serif; color:#EAEAEA; }
  .wrap { max-width:760px; margin:0 auto; padding:22px 18px 14px; }
  .kicker { font-size:11px; letter-spacing:2.5px; text-transform:uppercase; color:#76B900; text-align:center; margin-bottom:6px; }
  h2.title { font-size:22px; text-align:center; color:#FFFFFF; font-weight:700; margin-bottom:4px; }
  .sub { font-size:12.5px; color:#8C8C8C; text-align:center; margin-bottom:18px; }
  .sub b { color:#BDBDBD; }
  .chart { background:#141414; border:1px solid #2A2A2A; border-radius:14px; padding:20px 18px 14px; }
  .rowc { display:flex; align-items:center; gap:10px; margin-bottom:11px; opacity:0; transform:translateY(8px); }
  .rowc.go { animation:rowIn .45s forwards; }
  @keyframes rowIn { to { opacity:1; transform:translateY(0);} }
  .name { width:150px; text-align:right; font-size:12.5px; font-weight:700; color:#EAEAEA; flex-shrink:0; line-height:1.25; }
  .name small { display:block; font-weight:600; font-size:10px; color:#707070; letter-spacing:.5px; }
  .track { flex:1; height:20px; background:#0D0D0D; border:1px solid #262626; border-radius:6px; position:relative; overflow:visible; }
  .bar { height:100%; width:0%; border-radius:5px; background:#3A3A3A; transition:width 1.2s cubic-bezier(.2,.7,.2,1); }
  .ltx .bar { background:linear-gradient(90deg,#5A8F00,#A8E063); box-shadow:0 0 12px rgba(118,185,0,.5); }
  .ltx .name { color:#9BD34A; }
  .val { position:absolute; left:calc(100% + 8px); top:50%; transform:translateY(-50%); font-size:12px; font-weight:700; color:#8C8C8C; white-space:nowrap; }
  .ltx .val { color:#C8F08F; }
  .valInner { position:absolute; top:50%; transform:translateY(-50%); font-size:12px; font-weight:700; white-space:nowrap; }
  .divider { border-top:1px dashed #2E2E2E; margin:4px 0 13px 160px; position:relative; }
  .divider span { position:absolute; top:-8px; left:0; background:#141414; padding:0 8px 0 0; font-size:10px; letter-spacing:1.5px; text-transform:uppercase; color:#707070; }
  .controls { display:flex; justify-content:center; gap:10px; margin-top:14px; }
  .btn { background:#1A1A1A; border:1px solid #2E2E2E; color:#BDBDBD; font-size:12px; font-weight:600; padding:8px 16px; border-radius:999px; cursor:pointer; transition:all .25s; }
  .btn:hover { border-color:#76B900; color:#fff; }
  .btn.on { background:linear-gradient(135deg,#5A8F00,#76B900); border-color:#9BD34A; color:#fff; box-shadow:0 0 14px rgba(118,185,0,.4); }
  .note { font-size:11px; color:#707070; text-align:center; margin-top:12px; line-height:1.55; }
  .note b { color:#9BD34A; }
  .foot { text-align:center; margin-top:14px; padding-top:12px; border-top:1px solid #262626; font-size:11px; color:#707070; }
  .foot b { color:#76B900; }
  @media (max-width:640px) {
    h2.title { font-size:18px; }
    .name { width:104px; font-size:11px; }
    .divider { margin-left:114px; }
    .val, .valInner { font-size:10.5px; }
    .btn { font-size:11px; padding:7px 12px; }
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot; id=&quot;mtpApp&quot;&gt;
  &lt;div class=&quot;kicker&quot;&gt;Benchmark &amp;#8226; Lower Is Better&lt;/div&gt;
  &lt;h2 class=&quot;title&quot;&gt;Speed: Time to Generate a 10-Second Clip&lt;/h2&gt;
  &lt;p class=&quot;sub&quot;&gt;Image-to-video generation time. &lt;b&gt;LTX-2.5 on-prem finishes in 6.8s &amp;#8212; faster than the clip itself plays.&lt;/b&gt;&lt;/p&gt;

  &lt;div class=&quot;chart&quot;&gt;
    &lt;div id=&quot;rows&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;controls&quot;&gt;
      &lt;button class=&quot;btn on&quot; id=&quot;bSec&quot;&gt;Seconds&lt;/button&gt;
      &lt;button class=&quot;btn&quot; id=&quot;bRel&quot;&gt;Relative to LTX-2.5&lt;/button&gt;
      &lt;button class=&quot;btn&quot; id=&quot;bReplay&quot;&gt;&amp;#9654; Replay&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;note&quot;&gt;On-prem measured on &lt;b&gt;2x NVIDIA GB200&lt;/b&gt;. Data: LTX-2.5 launch benchmark (LTX, Aug 11, 2026). Relative view divides each time by the 6.8s on-prem result.&lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;foot&quot;&gt;Chart by &lt;b&gt;Marktechpost&lt;/b&gt; &amp;#8226; Sources: LTX Newsroom &amp;amp; NVIDIA Blog (Aug 11, 2026)&lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var DATA = [
    { name:'LTX 2.5', tag:'ON-PREM', s:6.8,  ltx:true },
    { name:'LTX 2.5', tag:'API',     s:23.7, ltx:true },
    { name:'Omni Flash',   s:52  },
    { name:'Grok 1.5',     s:63  },
    { name:'Veo 3.1',      s:70  },
    { name:'MiniMax H3',   s:180 },
    { name:'Seedance 2.0', s:196 },
    { name:'FLUX 3',       s:259 },
    { name:'Seedance 2.5', s:317 },
    { name:'Kling 3.0 Pro',s:398 }
  ];
  var MAX = 398;
  var mode = 'sec';
  var rowsEl = document.getElementById('rows');

  function fmt(d){
    if (mode === 'sec') return d.s + ' s';
    var r = d.s / 6.8;
    return (r &lt; 1.05 &amp;&amp; d.s === 6.8) ? '1x (baseline)' : (Math.round(r*10)/10) + 'x slower';
  }

  function build(){
    rowsEl.innerHTML = '';
    for (var i=0;i&lt;DATA.length;i++){
      var d = DATA[i];
      if (i === 2){
        var dv = document.createElement('div');
        dv.className = 'divider';
        dv.innerHTML = '&lt;span&gt;Closed alternatives&lt;/span&gt;';
        rowsEl.appendChild(dv);
      }
      var row = document.createElement('div');
      row.className = 'rowc' + (d.ltx ? ' ltx' : '');
      row.style.animationDelay = (i*0.12) + 's';
      var tagHtml = d.tag ? '&lt;small&gt;' + d.tag + '&lt;/small&gt;' : '';
      row.innerHTML = '&lt;div class=&quot;name&quot;&gt;' + d.name + tagHtml + '&lt;/div&gt;' +
        '&lt;div class=&quot;track&quot;&gt;&lt;div class=&quot;bar&quot; data-s=&quot;' + d.s + '&quot;&gt;&lt;/div&gt;&lt;div class=&quot;val&quot;&gt;&lt;/div&gt;&lt;/div&gt;';
      rowsEl.appendChild(row);
    }
  }

  function animate(){
    var rows = rowsEl.querySelectorAll('.rowc');
    for (var i=0;i&lt;rows.length;i++){
      (function(row, idx){
        row.classList.remove('go');
        var bar = row.querySelector('.bar');
        var val = row.querySelector('.val');
        bar.style.transition = 'none';
        bar.style.width = '0%';
        void bar.offsetWidth;
        row.classList.add('go');
        var s = parseFloat(bar.getAttribute('data-s'));
        var pct = Math.max(1.2, (s / MAX) * 92);
        setTimeout(function(){
          bar.style.transition = 'width 1.2s cubic-bezier(.2,.7,.2,1)';
          bar.style.width = pct + '%';
          // count-up
          var t0 = null, dur = 1200;
          function step(ts){
            if (!t0) t0 = ts;
            var p = Math.min(1, (ts - t0)/dur);
            var eased = 1 - Math.pow(1 - p, 3);
            var d = { s: Math.round(s * eased * 10)/10 };
            if (mode === 'sec'){ val.textContent = d.s + ' s'; }
            else {
              var r = (s * eased) / 6.8;
              val.textContent = (s === 6.8) ? '1x' : (Math.round(r*10)/10) + 'x';
            }
            if (p &lt; 1) requestAnimationFrame(step);
            else {
              // final exact label
              var dd = { s:s };
              val.textContent = fmtFinal(s);
              positionVal(row, bar, val, pct);
            }
          }
          requestAnimationFrame(step);
          setTimeout(function(){ positionVal(row, bar, val, pct); sendH(); }, 1250);
        }, 150 + idx*120);
      })(rows[i], i);
    }
    setTimeout(sendH, 400);
  }

  function fmtFinal(s){
    if (mode === 'sec') return s + ' s';
    return (s === 6.8) ? '1x (baseline)' : (Math.round((s/6.8)*10)/10) + 'x slower';
  }

  function positionVal(row, bar, val, pct){
    // place label just past bar end; for long bars keep inside track
    val.style.left = 'calc(' + pct + '% + 8px)';
  }

  function setMode(m){
    mode = m;
    document.getElementById('bSec').classList.toggle('on', m==='sec');
    document.getElementById('bRel').classList.toggle('on', m==='rel');
    animate();
  }

  document.getElementById('bSec').addEventListener('click', function(){ setMode('sec'); });
  document.getElementById('bRel').addEventListener('click', function(){ setMode('rel'); });
  document.getElementById('bReplay').addEventListener('click', animate);

  function sendH(){
    try {
      var h = document.getElementById('mtpApp').offsetHeight + 40;
      parent.postMessage({ mtpEmbed:'ltx25-speed-chart', height:h }, '*');
    } catch(e){}
  }
  window.addEventListener('load', function(){ build(); animate(); sendH(); });
  window.addEventListener('resize', sendH);
  setInterval(sendH, 1200);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
" style="width:100%;border:0;display:block;border-radius:14px;overflow:hidden;" height="600" scrolling="no" title="LTX-2.5 Speed Benchmark Chart"></iframe>
</div>
<script>
(function(){
  window.addEventListener('message', function(e){
    if (e.data && e.data.mtpEmbed === 'ltx25-speed-chart' && e.data.height) {
      var f = document.getElementById('mtp-ltx25-speed');
      if (f) f.style.height = e.data.height + 'px';
    }
  });
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-nvidia-s-local-ai-momentum" class="wp-block-heading"><strong>NVIDIA&#8217;s Local AI Momentum</strong></h2>



<p class="wp-block-paragraph">Throughout August, NVIDIA is spotlighting models, applications, and tools across the local AI ecosystem.<a href="https://blogs.nvidia.com/blog/nemotron-lightning-switchyard-rtx-dgx"> Nemotron 3.5 Lightning</a>, also released today, is an open 30B mixture-of-experts model for always-on agents, joined by<a href="https://github.com/NVIDIA-NeMo/Switchyard"> NeMo Switchyard</a>, an open source library that routes each agent workflow step to the best-fit model. The common thread is hardware choice: NVIDIA-ecosystem open models scale from<a href="https://www.nvidia.com/en-us/ai-on-rtx/"> RTX PCs</a> to workstations, data centers, and cloud. LTX-2.5 slots directly into that story as an NVIDIA-accelerated world model for creators, developers, and robotics teams.</p>



<h2 id="h-what-is-ltx-2-5" class="wp-block-heading"><strong>What is LTX-2.5?</strong></h2>



<p class="wp-block-paragraph">Where large language models (LLMs) learn to predict the next word, world models learn to predict the next moment. They generate environments, simulate how they behave, and let users act inside them. That foundation supports film, advertising, gaming, simulation, and robots in warehouses and factories. LTX describes the LTX family as the most used open world model, with <strong>more than 33 million downloads</strong>, and positions LTX-2.5 as its most capable release yet. Open weights give teams full control of hardware, customization, and IP.</p>



<h2 id="h-what-s-new-in-the-architecture" class="wp-block-heading"><strong>What&#8217;s New in the Architecture</strong></h2>



<p class="wp-block-paragraph"><strong>LTX rebuilt nearly every stage of the generation pipeline rather than bolting features onto an older core:</strong></p>



<ul class="wp-block-list">
<li><strong>New diffusion video decoder:</strong> Reduces visual artifacts in high-motion scenes while preserving LTX&#8217;s high compression ratio and staying true to existing footage.</li>



<li><strong>Native multishot generation:</strong> Renders a full sequence as one output, holding character, scene, and voice consistent across cuts. A custom Gemma 4 language backbone and dedicated prompt enhancer improve comprehension of complex, multi-subject prompts.</li>



<li><strong>Diffusion Fidelity Rendering:</strong> Builds motion and structure in an <strong>8x temporally compressed latent space</strong>, then generates high-fidelity keyframes to anchor visual detail. Keyframe count adapts to scene complexity and compute budget.</li>



<li><strong>A physical AI checkpoint:</strong> A pretrained checkpoint tuned for robotics gives teams a base for fine-tuning on domain data unlike cinematic video.</li>



<li><strong>A stronger distilled model:</strong> Delivers the same quality at lower cost and faster inference for production-volume deployment.</li>
</ul>



<!-- LTX-2.5 Interactive Explainer (animated, NVIDIA theme) | Marktechpost | Paste into a Custom HTML block -->
<div id="mtp-ltx25-explainer-wrap" style="max-width:840px;margin:24px auto;">
<iframe id="mtp-ltx25-explainer" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;style&gt;
  * { margin:0; padding:0; box-sizing:border-box; }
  body { background:#0A0A0A; font-family:'Segoe UI', -apple-system, Arial, sans-serif; color:#EAEAEA; }
  .wrap { max-width:780px; margin:0 auto; padding:22px 18px 14px; }
  .kicker { font-size:11px; letter-spacing:2.5px; text-transform:uppercase; color:#76B900; text-align:center; margin-bottom:6px; }
  h2.title { font-size:22px; text-align:center; color:#fff; font-weight:700; margin-bottom:4px; line-height:1.25; }
  .sub { font-size:12.5px; color:#8C8C8C; text-align:center; margin-bottom:18px; }
  .tabs { display:flex; gap:8px; justify-content:center; flex-wrap:wrap; margin-bottom:16px; }
  .tab { background:#1A1A1A; border:1px solid #2E2E2E; color:#BDBDBD; font-size:12.5px; font-weight:600; padding:9px 14px; border-radius:999px; cursor:pointer; transition:all .25s; }
  .tab:hover { border-color:#76B900; color:#fff; }
  .tab.on { background:linear-gradient(135deg,#5A8F00,#76B900); border-color:#9BD34A; color:#fff; box-shadow:0 0 16px rgba(118,185,0,.4); }
  .panel { display:none; background:#141414; border:1px solid #2A2A2A; border-radius:14px; padding:20px 18px; }
  .panel.on { display:block; animation:fadeUp .45s ease; }
  @keyframes fadeUp { from{opacity:0; transform:translateY(10px);} to{opacity:1; transform:translateY(0);} }
  .pTitle { font-size:16.5px; font-weight:700; color:#9BD34A; margin-bottom:5px; }
  .pDesc { font-size:13px; color:#A6A6A6; line-height:1.6; margin-bottom:16px; }
  .stage { background:#0D0D0D; border:1px solid #262626; border-radius:10px; padding:18px 14px; position:relative; }
  .caption { font-size:11.5px; color:#8C8C8C; text-align:center; margin-top:13px; line-height:1.55; padding-top:11px; border-top:1px dashed #262626; }
  .caption b { color:#9BD34A; }
  .lbl { font-size:10.5px; letter-spacing:1.6px; text-transform:uppercase; color:#707070; text-align:center; margin-bottom:8px; }

  /* T1 world model */
  .row { display:flex; align-items:center; justify-content:center; gap:6px; flex-wrap:wrap; }
  .tok { background:#16210A; border:1px solid #2E3B12; color:#C8D6B0; font-size:12px; padding:5px 10px; border-radius:6px; opacity:0; }
  .on .tok { animation:pop .4s forwards; }
  .on .tok:nth-child(2){animation-delay:.15s}.on .tok:nth-child(3){animation-delay:.3s}.on .tok:nth-child(4){animation-delay:.45s}.on .tok:nth-child(5){animation-delay:.6s}
  @keyframes pop { from{opacity:0; transform:scale(.6);} to{opacity:1; transform:scale(1);} }
  .frames { display:flex; gap:5px; justify-content:center; flex-wrap:wrap; }
  .frame { width:56px; height:38px; border-radius:5px; background:linear-gradient(135deg,#3D6B00,#A8E063); opacity:0; position:relative; overflow:hidden; }
  .frame:after { content:''; position:absolute; left:6px; bottom:5px; width:9px; height:16px; border-radius:2px; background:rgba(10,10,10,.55); }
  .frame:nth-child(2):after{left:14px}.frame:nth-child(3):after{left:22px}.frame:nth-child(4):after{left:30px}.frame:nth-child(5):after{left:38px}
  .on .frame { animation:slideIn .5s forwards; }
  .on .frame:nth-child(2){animation-delay:.2s}.on .frame:nth-child(3){animation-delay:.4s}.on .frame:nth-child(4){animation-delay:.6s}.on .frame:nth-child(5){animation-delay:.8s}
  @keyframes slideIn { from{opacity:0; transform:translateX(-16px);} to{opacity:1; transform:translateX(0);} }
  .vs { text-align:center; color:#76B900; font-weight:700; font-size:11.5px; margin:14px 0 10px; letter-spacing:2.5px; }

  /* T2 fidelity */
  .latent { display:flex; gap:4px; justify-content:center; align-items:flex-end; height:76px; margin-bottom:12px; }
  .cell { width:22px; height:32px; border-radius:4px; background:#1E1E1E; border:1px solid #2E2E2E; transition:height .4s, background .4s; }
  .cell.key { background:linear-gradient(180deg,#A8E063,#5A8F00); border-color:#C8F08F; height:62px; box-shadow:0 0 14px rgba(118,185,0,.55); }
  .legend { display:flex; gap:16px; justify-content:center; font-size:11px; color:#8C8C8C; margin-bottom:14px; flex-wrap:wrap; }
  .dot { display:inline-block; width:9px; height:9px; border-radius:2px; margin-right:5px; vertical-align:middle; }
  .d1{background:#1E1E1E; border:1px solid #2E2E2E;} .d2{background:linear-gradient(180deg,#A8E063,#5A8F00);}
  .sliderRow { display:flex; align-items:center; gap:12px; justify-content:center; flex-wrap:wrap; }
  .sliderRow label { font-size:12px; color:#D6D6D6; font-weight:600; }
  input[type=range] { width:180px; accent-color:#76B900; cursor:pointer; }
  .kcount { font-size:12px; color:#9BD34A; font-weight:700; min-width:118px; }

  /* T3 multishot */
  .toggleRow { display:flex; gap:8px; justify-content:center; margin-bottom:14px; }
  .tg { background:#1A1A1A; border:1px solid #2E2E2E; color:#BDBDBD; font-size:11.5px; font-weight:600; padding:6px 13px; border-radius:8px; cursor:pointer; transition:all .25s; }
  .tg.on { background:#16210A; border-color:#76B900; color:#9BD34A; }
  .shots { display:flex; gap:10px; justify-content:center; flex-wrap:wrap; }
  .shot { width:124px; border-radius:8px; overflow:hidden; border:1px solid #2E2E2E; opacity:0; }
  .on .shot { animation:fadeUp .5s forwards; }
  .on .shot:nth-child(2){animation-delay:.25s} .on .shot:nth-child(3){animation-delay:.5s}
  .scene { height:70px; position:relative; }
  .s1{background:linear-gradient(160deg,#223311,#0A0A0A);} .s2{background:linear-gradient(160deg,#1A331F,#0A0A0A);} .s3{background:linear-gradient(160deg,#0F3328,#0A0A0A);}
  .char { position:absolute; bottom:9px; left:50%; transform:translateX(-50%); transition:all .4s; }
  .char .head { width:11px; height:11px; border-radius:50%; margin:0 auto 2px; background:#EAF7D0; transition:background .4s; }
  .char .bod { width:15px; height:19px; border-radius:4px; margin:0 auto; background:#76B900; transition:background .4s; }
  .drift .shot:nth-child(2) .head { background:#E0A0C0; } .drift .shot:nth-child(2) .bod { background:#B04070; }
  .drift .shot:nth-child(3) .head { background:#9CC4E4; } .drift .shot:nth-child(3) .bod { background:#3A6EA5; }
  .drift .shot:nth-child(3) .char { transform:translateX(-50%) scale(1.28); }
  .cap { font-size:10.5px; color:#8C8C8C; text-align:center; padding:6px 4px; background:#1A1A1A; }
  .verdict { text-align:center; font-size:12px; font-weight:700; margin-top:12px; }
  .verdict.good { color:#9BD34A; } .verdict.bad { color:#D98080; }

  /* T4 local */
  .flowRow { display:flex; align-items:center; justify-content:center; gap:8px; flex-wrap:wrap; margin-bottom:6px; }
  .node { border:1px solid #2E2E2E; background:#1A1A1A; border-radius:8px; padding:9px 12px; font-size:11.5px; font-weight:600; color:#BDBDBD; opacity:0; text-align:center; min-width:88px; }
  .node.grn { border-color:#2E3B12; background:#16210A; color:#A8E063; }
  .arw { color:#707070; font-size:14px; opacity:0; }
  .on .node, .on .arw { animation:pop .4s forwards; }
  .on .n2{animation-delay:.18s}.on .a1{animation-delay:.1s}.on .a2{animation-delay:.28s}.on .n3{animation-delay:.38s}.on .a3{animation-delay:.48s}.on .n4{animation-delay:.58s}
  .cost { display:flex; gap:10px; justify-content:center; flex-wrap:wrap; margin-top:16px; }
  .cbox { flex:1; min-width:150px; border-radius:9px; padding:12px 11px; border:1px solid #2E2E2E; background:#111; }
  .cbox.grn { border-color:#2E3B12; background:#101806; }
  .cH { font-size:11px; letter-spacing:1.2px; text-transform:uppercase; color:#707070; margin-bottom:6px; }
  .cbox.grn .cH { color:#76B900; }
  .cL { font-size:12px; color:#A6A6A6; line-height:1.6; }
  .cL b { color:#EAEAEA; }
  .cbox.grn .cL b { color:#C8F08F; }

  .foot { text-align:center; margin-top:16px; padding-top:12px; border-top:1px solid #262626; font-size:11px; color:#707070; }
  .foot b { color:#76B900; }
  @media (max-width:640px) {
    h2.title { font-size:18px; }
    .tab { font-size:11.5px; padding:8px 11px; }
    .shot { width:98px; }
    .cell { width:16px; }
    .cbox { min-width:100%; }
    .flowRow { flex-direction:column; gap:5px; }
    .node { min-width:150px; }
    .arw { transform:rotate(90deg); font-size:12px; }
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class=&quot;wrap&quot; id=&quot;mtpApp&quot;&gt;
  &lt;div class=&quot;kicker&quot;&gt;Interactive Explainer&lt;/div&gt;
  &lt;h2 class=&quot;title&quot;&gt;How LTX-2.5 Works, and Why Local Changes Everything&lt;/h2&gt;
  &lt;p class=&quot;sub&quot;&gt;Tap a concept. Each panel animates the idea and explains what it means in production.&lt;/p&gt;

  &lt;div class=&quot;tabs&quot;&gt;
    &lt;button class=&quot;tab on&quot; data-p=&quot;p1&quot;&gt;1 &amp;#8226; World Model&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-p=&quot;p2&quot;&gt;2 &amp;#8226; Fidelity Rendering&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-p=&quot;p3&quot;&gt;3 &amp;#8226; Multishot&lt;/button&gt;
    &lt;button class=&quot;tab&quot; data-p=&quot;p4&quot;&gt;4 &amp;#8226; Running Local&lt;/button&gt;
  &lt;/div&gt;

  &lt;div class=&quot;panel on&quot; id=&quot;p1&quot;&gt;
    &lt;div class=&quot;pTitle&quot;&gt;From predicting the next word to the next moment&lt;/div&gt;
    &lt;div class=&quot;pDesc&quot;&gt;An LLM continues a sentence. A world model continues a &lt;b&gt;world&lt;/b&gt;: it generates an environment, simulates how that environment behaves over time, and lets you act inside it. That is why the same model serves film, gaming, and robotics.&lt;/div&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;lbl&quot;&gt;Language model &amp;#8226; predicts the next word&lt;/div&gt;
      &lt;div class=&quot;row&quot;&gt;&lt;span class=&quot;tok&quot;&gt;The&lt;/span&gt;&lt;span class=&quot;tok&quot;&gt;robot&lt;/span&gt;&lt;span class=&quot;tok&quot;&gt;lifts&lt;/span&gt;&lt;span class=&quot;tok&quot;&gt;the&lt;/span&gt;&lt;span class=&quot;tok&quot;&gt;crate&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;vs&quot;&gt;&amp;#9660; &amp;nbsp; WORLD MODEL &amp;nbsp; &amp;#9660;&lt;/div&gt;
      &lt;div class=&quot;lbl&quot;&gt;Predicts the next moment &amp;#8226; motion, space, and sound over time&lt;/div&gt;
      &lt;div class=&quot;frames&quot;&gt;&lt;span class=&quot;frame&quot;&gt;&lt;/span&gt;&lt;span class=&quot;frame&quot;&gt;&lt;/span&gt;&lt;span class=&quot;frame&quot;&gt;&lt;/span&gt;&lt;span class=&quot;frame&quot;&gt;&lt;/span&gt;&lt;span class=&quot;frame&quot;&gt;&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;caption&quot;&gt;The output is not a description of the robot lifting a crate. It is &lt;b&gt;the footage of it happening&lt;/b&gt;, with physics, spatial layout, and audio held consistent frame to frame.&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;panel&quot; id=&quot;p2&quot;&gt;
    &lt;div class=&quot;pTitle&quot;&gt;Diffusion Fidelity Rendering: detail where it counts&lt;/div&gt;
    &lt;div class=&quot;pDesc&quot;&gt;Rendering every frame at full quality is wasteful. LTX-2.5 builds motion and structure inside an &lt;b&gt;8x temporally compressed latent space&lt;/b&gt;, then spends full detail only on high-fidelity keyframes that anchor the rest. Drag the slider to see the count adapt.&lt;/div&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;lbl&quot;&gt;Timeline &amp;#8226; tall bars are high-fidelity keyframes&lt;/div&gt;
      &lt;div class=&quot;latent&quot; id=&quot;latent&quot;&gt;&lt;/div&gt;
      &lt;div class=&quot;legend&quot;&gt;&lt;span&gt;&lt;span class=&quot;dot d1&quot;&gt;&lt;/span&gt;Compressed latent frame (8x)&lt;/span&gt;&lt;span&gt;&lt;span class=&quot;dot d2&quot;&gt;&lt;/span&gt;High-fidelity keyframe&lt;/span&gt;&lt;/div&gt;
      &lt;div class=&quot;sliderRow&quot;&gt;
        &lt;label for=&quot;cx&quot;&gt;Scene complexity&lt;/label&gt;
        &lt;input type=&quot;range&quot; id=&quot;cx&quot; min=&quot;1&quot; max=&quot;5&quot; value=&quot;2&quot;&gt;
        &lt;span class=&quot;kcount&quot; id=&quot;kc&quot;&gt;Keyframes: 3&lt;/span&gt;
      &lt;/div&gt;
      &lt;div class=&quot;caption&quot; id=&quot;fidCap&quot;&gt;A simple scene needs few anchors. As complexity rises, LTX-2.5 &lt;b&gt;adds keyframes&lt;/b&gt; instead of raising cost on every frame &amp;#8212; which is how quality stays high while render time stays low.&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;panel&quot; id=&quot;p3&quot;&gt;
    &lt;div class=&quot;pTitle&quot;&gt;Native multishot: one output, not three clips&lt;/div&gt;
    &lt;div class=&quot;pDesc&quot;&gt;Earlier open models generated each shot separately, so the character drifted between cuts &amp;#8212; unusable for a campaign. LTX-2.5 renders the full sequence as a single output, holding character, scene, and voice across cuts. Toggle below to see the difference.&lt;/div&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;toggleRow&quot;&gt;
        &lt;button class=&quot;tg&quot; id=&quot;tgOld&quot;&gt;Separate clips (old way)&lt;/button&gt;
        &lt;button class=&quot;tg on&quot; id=&quot;tgNew&quot;&gt;Native multishot&lt;/button&gt;
      &lt;/div&gt;
      &lt;div class=&quot;shots&quot; id=&quot;shots&quot;&gt;
        &lt;div class=&quot;shot&quot;&gt;&lt;div class=&quot;scene s1&quot;&gt;&lt;div class=&quot;char&quot;&gt;&lt;div class=&quot;head&quot;&gt;&lt;/div&gt;&lt;div class=&quot;bod&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;cap&quot;&gt;Shot 1 &amp;#8226; Wide&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;shot&quot;&gt;&lt;div class=&quot;scene s2&quot;&gt;&lt;div class=&quot;char&quot;&gt;&lt;div class=&quot;head&quot;&gt;&lt;/div&gt;&lt;div class=&quot;bod&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;cap&quot;&gt;Shot 2 &amp;#8226; Medium&lt;/div&gt;&lt;/div&gt;
        &lt;div class=&quot;shot&quot;&gt;&lt;div class=&quot;scene s3&quot;&gt;&lt;div class=&quot;char&quot;&gt;&lt;div class=&quot;head&quot;&gt;&lt;/div&gt;&lt;div class=&quot;bod&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;&lt;div class=&quot;cap&quot;&gt;Shot 3 &amp;#8226; Close&lt;/div&gt;&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;verdict good&quot; id=&quot;verdict&quot;&gt;Same character, same wardrobe, same voice across all three cuts&lt;/div&gt;
      &lt;div class=&quot;caption&quot;&gt;A custom &lt;b&gt;Gemma 4 language backbone&lt;/b&gt; and a dedicated prompt enhancer parse complex, multi-subject prompts, so &quot;the same courier, three angles&quot; stays the same courier.&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;panel&quot; id=&quot;p4&quot;&gt;
    &lt;div class=&quot;pTitle&quot;&gt;Why running it locally rewires the workflow&lt;/div&gt;
    &lt;div class=&quot;pDesc&quot;&gt;LTX and NVIDIA optimized LTX-2.5 for local inference on &lt;b&gt;NVIDIA RTX GPUs&lt;/b&gt; and &lt;b&gt;NVIDIA DGX Spark&lt;/b&gt;, cutting VRAM requirements so a frontier world model fits on hardware teams already own. The pipeline collapses onto one desk.&lt;/div&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;lbl&quot;&gt;The whole production stack, on one machine&lt;/div&gt;
      &lt;div class=&quot;flowRow&quot;&gt;
        &lt;div class=&quot;node n1&quot;&gt;Prompt&lt;br&gt;or image&lt;/div&gt;
        &lt;div class=&quot;arw a1&quot;&gt;&amp;#10230;&lt;/div&gt;
        &lt;div class=&quot;node grn n2&quot;&gt;ComfyUI&lt;br&gt;workflow&lt;/div&gt;
        &lt;div class=&quot;arw a2&quot;&gt;&amp;#10230;&lt;/div&gt;
        &lt;div class=&quot;node grn n3&quot;&gt;LTX-2.5 on&lt;br&gt;RTX / DGX Spark&lt;/div&gt;
        &lt;div class=&quot;arw a3&quot;&gt;&amp;#10230;&lt;/div&gt;
        &lt;div class=&quot;node n4&quot;&gt;Finished&lt;br&gt;sequence&lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;cost&quot;&gt;
        &lt;div class=&quot;cbox&quot;&gt;
          &lt;div class=&quot;cH&quot;&gt;Cloud generation&lt;/div&gt;
          &lt;div class=&quot;cL&quot;&gt;Per-clip fees &amp;#8226; metered credits&lt;br&gt;Assets leave your machine&lt;br&gt;Volume is a &lt;b&gt;budget decision&lt;/b&gt;&lt;/div&gt;
        &lt;/div&gt;
        &lt;div class=&quot;cbox grn&quot;&gt;
          &lt;div class=&quot;cH&quot;&gt;Local on NVIDIA&lt;/div&gt;
          &lt;div class=&quot;cL&quot;&gt;No per-generation fees&lt;br&gt;Data and IP stay &lt;b&gt;on-device&lt;/b&gt;&lt;br&gt;Volume is a &lt;b&gt;time decision&lt;/b&gt;&lt;/div&gt;
        &lt;/div&gt;
      &lt;/div&gt;
      &lt;div class=&quot;caption&quot;&gt;That shift is what makes batch-generating overnight and testing ten hooks per brief practical. NVIDIA cites &lt;b&gt;significantly less VRAM consumption&lt;/b&gt; for the optimized local path.&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;

  &lt;div class=&quot;foot&quot;&gt;Explainer by &lt;b&gt;Marktechpost&lt;/b&gt; &amp;#8226; Sources: LTX Newsroom &amp;amp; NVIDIA Blog (Aug 11, 2026)&lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var tabs = document.querySelectorAll('.tab');
  var panels = document.querySelectorAll('.panel');
  function show(id){
    for (var i=0;i&lt;panels.length;i++) panels[i].classList.remove('on');
    for (var j=0;j&lt;tabs.length;j++) tabs[j].classList.toggle('on', tabs[j].getAttribute('data-p')===id);
    var p = document.getElementById(id);
    void p.offsetWidth;
    p.classList.add('on');
    sendH();
  }
  for (var k=0;k&lt;tabs.length;k++){
    tabs[k].addEventListener('click', function(){ show(this.getAttribute('data-p')); });
  }

  // Fidelity slider
  var latent = document.getElementById('latent');
  var kc = document.getElementById('kc');
  var cx = document.getElementById('cx');
  var fidCap = document.getElementById('fidCap');
  var CELLS = 16;
  function drawLatent(c){
    var keys = c + 1;
    latent.innerHTML = '';
    var step = Math.max(2, Math.floor(CELLS/keys));
    var placed = 0;
    for (var i=0;i&lt;CELLS;i++){
      var el = document.createElement('div');
      el.className = 'cell';
      if ((i % step === Math.floor(step/2)) &amp;&amp; placed &lt; keys){ el.className += ' key'; placed++; }
      latent.appendChild(el);
    }
    kc.textContent = 'Keyframes: ' + placed;
    fidCap.innerHTML = (c &lt;= 2)
      ? 'A simple scene needs few anchors. As complexity rises, LTX-2.5 &lt;b&gt;adds keyframes&lt;/b&gt; instead of raising cost on every frame &amp;#8212; which is how quality stays high while render time stays low.'
      : 'A busier scene gets &lt;b&gt;more anchors&lt;/b&gt;, so fast motion and fine detail hold up. Cost scales with what the shot actually needs, not with frame count.';
    sendH();
  }
  cx.addEventListener('input', function(){ drawLatent(parseInt(this.value,10)); });
  drawLatent(2);

  // Multishot toggle
  var shots = document.getElementById('shots');
  var verdict = document.getElementById('verdict');
  var tgOld = document.getElementById('tgOld');
  var tgNew = document.getElementById('tgNew');
  function setShots(drift){
    shots.classList.toggle('drift', drift);
    tgOld.classList.toggle('on', drift);
    tgNew.classList.toggle('on', !drift);
    verdict.className = 'verdict ' + (drift ? 'bad' : 'good');
    verdict.textContent = drift
      ? 'Character drifts between cuts \u2014 wardrobe, colour, and scale all change'
      : 'Same character, same wardrobe, same voice across all three cuts';
  }
  tgOld.addEventListener('click', function(){ setShots(true); });
  tgNew.addEventListener('click', function(){ setShots(false); });

  function sendH(){
    try {
      var h = document.getElementById('mtpApp').offsetHeight + 40;
      parent.postMessage({ mtpEmbed:'ltx25-explainer', height:h }, '*');
    } catch(e){}
  }
  window.addEventListener('load', sendH);
  window.addEventListener('resize', sendH);
  setInterval(sendH, 1200);
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
" style="width:100%;border:0;display:block;border-radius:14px;overflow:hidden;" height="640" scrolling="no" title="LTX-2.5 Interactive Explainer"></iframe>
</div>
<script>
(function(){
  window.addEventListener('message', function(e){
    if (e.data && e.data.mtpEmbed === 'ltx25-explainer' && e.data.height) {
      var f = document.getElementById('mtp-ltx25-explainer');
      if (f) f.style.height = e.data.height + 'px';
    }
  });
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-who-ltx-2-5-is-for" class="wp-block-heading"><strong>Who LTX-2.5 is For</strong></h2>



<ul class="wp-block-list">
<li><strong>Film and video studios:</strong> Multishot consistency plus the cleaner decoder make sequences usable in real productions. Studios like<a href="https://www.asteriafilm.com/"> Asteria</a> already produce original film and video on LTX.</li>



<li><strong>Short-form creators and ad teams:</strong> Local generation with no per-clip fees turns A/B testing into a strategy: batch variations overnight, refresh creative weekly, and localize across markets without a production budget.</li>



<li><strong>Real-time application developers:</strong><a href="https://www.reactor.inc/"> Reactor</a> runs LTX-2.5 on its low-latency infrastructure to power interactive avatars, live worlds, and real-time robotics workloads.</li>



<li><strong>Robotics and physical AI teams:</strong> The physical AI checkpoint provides a fine-tuning base for non-cinematic domain data.<a href="https://www.markovrobotics.com/"> Markov Robotics</a> uses LTX to develop how physical systems perceive and move through the world.</li>
</ul>



<h2 id="h-availability-and-licensing" class="wp-block-heading"><strong>Availability and Licensing</strong></h2>



<p class="wp-block-paragraph">LTX-2.5 ships as open weights on<a href="https://huggingface.co/Lightricks"> Hugging Face</a>, natively in ComfyUI, and through the<a href="https://ltx.io/model/api"> LTX API</a> for managed generation. It runs on anything from data center GPUs to a Mac and is <strong>free for organizations under $10M in annual recurring revenue</strong>. Code is on<a href="https://github.com/Lightricks/LTX-2"> GitHub</a>, with<a href="https://docs.ltx.video/open-source-model/getting-started/overview"> documentation</a>.</p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>LTX-2.5 is an open weights world model for video, real-time apps, and robotics.</li>



<li>On-prem generation hits 6.8 seconds for a 10-second clip, versus 52 to 398 seconds for closed rivals.</li>



<li>NVIDIA optimization cuts VRAM requirements for local inference on RTX GPUs and DGX Spark.</li>



<li>Native multishot with a Gemma 4 backbone holds characters consistent, making campaign-grade output possible locally.</li>



<li>Weights are free on Hugging Face under $10M ARR, with day-one ComfyUI support.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph"><em><sub>Thanks to the NVIDIA team for the thought leadership / resources for this article. This article is sponsored by NVIDIA.</sub></em></p>
<p>The post <a href="https://www.marktechpost.com/2026/08/11/the-video-production-stack-now-fits-on-one-desk-ltx-2-5-launches-as-nvidia-accelerated-open-weights-world-model/">The Video Production Stack Now Fits on One Desk: LTX-2.5 Launches as NVIDIA-Accelerated Open Weights World Model</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81673</post-id>	</item>
		<item>
		<title>Building and Validating a Quantitative Trading Strategy with OctoBot, Walk-Forward Backtesting, Parameter Optimization, and Interactive Analysis</title>
		<link>https://www.marktechpost.com/2026/08/11/building-and-validating-a-quantitative-trading-strategy-with-octobot-walk-forward-backtesting-parameter-optimization-and-interactive-analysis/</link>
		
		<dc:creator><![CDATA[Sana Hassan]]></dc:creator>
		<pubDate>Tue, 11 Aug 2026 15:17:29 +0000</pubDate>
				<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Big Data]]></category>
		<category><![CDATA[Data Science]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Technology]]></category>
		<category><![CDATA[Tutorials]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81669</guid>

					<description><![CDATA[<p>In this tutorial, we build a complete quantitative backtesting workflow with OctoBot and OctoBot-Script while keeping the environment isolated from Colab’s preinstalled dependencies. We configure a rule-based trading strategy that combines RSI-based oversold signals, EMA trend confirmation, and ATR-driven adaptive stop-loss and take-profit levels, and we execute it through OctoBot’s native market-order and backtesting APIs. [&#8230;]</p>
<p>The post <a href="https://www.marktechpost.com/2026/08/11/building-and-validating-a-quantitative-trading-strategy-with-octobot-walk-forward-backtesting-parameter-optimization-and-interactive-analysis/">Building and Validating a Quantitative Trading Strategy with OctoBot, Walk-Forward Backtesting, Parameter Optimization, and Interactive Analysis</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">In this tutorial, we build a complete quantitative backtesting workflow with<a href="https://github.com/Drakkar-Software/OctoBot"> <strong>OctoBot</strong></a> and OctoBot-Script while keeping the environment isolated from Colab’s preinstalled dependencies. We configure a rule-based trading strategy that combines RSI-based oversold signals, EMA trend confirmation, and ATR-driven adaptive stop-loss and take-profit levels, and we execute it through OctoBot’s native market-order and backtesting APIs. We also retrieve historical OHLCV data through OctoBot’s data layer with automatic exchange fallback, perform a multi-parameter grid search over an in-sample period, and select the strongest configuration based on its excess return relative to buy-and-hold. We then validate the selected parameters on a completely separate out-of-sample period to assess generalization and identify potential overfitting. Finally, we extract OctoBot’s backtest report data and use Pandas and Plotly to analyze parameter sensitivity, portfolio performance, price action, indicators, and execution results in an interactive Colab environment.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">SYMBOL          = "BTC/USDT"
TIME_FRAME      = "1d"
EXCHANGES       = ["binance", "kucoin", "okx", "bybit", "mexc", "kraken"]
IN_SAMPLE       = ("2019-01-01", "2023-01-01")
OUT_OF_SAMPLE   = ("2023-01-01", "2025-06-01")
GRID = {
   "rsi_period":     [7, 14, 21],
   "rsi_threshold":  [25, 30, 35],
   "tp_atr_mult":    [3.0, 5.0],
}
FIXED = {
   "ema_fast":      50,
   "ema_slow":      200,
   "atr_period":    14,
   "sl_atr_mult":   2.0,
   "position_size": "20%",
   "min_offset_pct": 1.0,
   "max_offset_pct": 40.0,
}
VENV_DIR   = "/content/octobot_env"
WORK_DIR   = "/content/octobot_lab"
OCTOBOT_V  = "2.1.1"
PY_VERSION = "3.12"
import json, os, subprocess, sys, textwrap, time, itertools, shutil
os.makedirs(WORK_DIR, exist_ok=True)
PY     = os.path.join(VENV_DIR, "bin", "python")
MARKER = os.path.join(VENV_DIR, ".octobot_ready")
def sh(cmd, **kw):
   """Run a command, streaming its output live into the Colab cell."""
   print(f"$ {' '.join(cmd)}")
   p = subprocess.Popen(cmd, stdout=subprocess.PIPE, stderr=subprocess.STDOUT,
                        text=True, bufsize=1, **kw)
   for line in p.stdout:
       print("   " + line.rstrip())
   p.wait()
   if p.returncode != 0:
       raise RuntimeError(f"command failed ({p.returncode}): {' '.join(cmd)}")
if not os.path.exists(MARKER):
   print("=" * 90, "\n  BUILDING OCTOBOT ENVIRONMENT (one-off, ~2 min)\n", "=" * 90)
   subprocess.run([sys.executable, "-m", "pip", "install", "-q", "uv"], check=True)
   UV = [sys.executable, "-m", "uv"]
   sh(UV + ["venv", "--python", PY_VERSION, VENV_DIR])
   sh(UV + ["pip", "install", "--python", PY, "-q",
            f"OctoBot=={OCTOBOT_V}", "wheel", "setuptools", "appdirs==1.4.4"])
   sh(UV + ["pip", "install", "--python", PY, "-q", "--no-build-isolation", "octobot-script"])
   sh([PY, "-m", "octobot_script.cli", "install_tentacles", "--quite"])
   sh([PY, "-c", textwrap.dedent("""
       import os, shutil, octobot_script.resources as r
       base = r.get_report_resource_path("")
       src, dst_dir = os.path.join(base, "index.html"), os.path.join(base, "dist")
       os.makedirs(dst_dir, exist_ok=True)
       dst = os.path.join(dst_dir, "index.html")
       if os.path.exists(src) and not os.path.exists(dst):
           shutil.copy2(src, dst); print("patched report template ->", dst)
       else:
           print("report template already fine")
   """)])
   open(MARKER, "w").write("ok")
   print("\n<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2705.png" alt="✅" class="wp-smiley" style="height: 1em; max-height: 1em;" /> environment ready\n")
else:
   print("<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/2705.png" alt="✅" class="wp-smiley" style="height: 1em; max-height: 1em;" /> environment already built (delete", VENV_DIR, "to rebuild)\n")
</code></pre></div></div>



<p class="wp-block-paragraph">We define the core trading configuration, including the symbol, timeframe, exchange fallback list, backtesting windows, parameter grid, and fixed strategy settings. We then create an isolated Python environment with uv and install the pinned OctoBot and OctoBot-Script dependencies required for the workflow. We also install the OctoBot tentacles package and patch the report-template path so later backtest reporting works correctly inside the Colab environment.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">WORKER = os.path.join(WORK_DIR, "octobot_worker.py")
WORKER_SRC = r'''
import asyncio, itertools, json, os, sys, time, traceback
import numpy as np
import tulipy
import octobot_script as obs
CFG  = json.load(open(os.environ["OBS_CONFIG"]))
OUT  = os.environ["OBS_OUT"]
FIX  = CFG["fixed"]
for kw in ("Close", "High", "Low", "Time", "market", "current_live_time", "plot_indicator"):
   if not hasattr(obs, kw):
       raise RuntimeError(
           f"octobot_script.{kw} missing -> tentacles are not installed. "
           "Run: python -m octobot_script.cli install_tentacles"
       )
def tail(*arrays):
   """tulipy indicators return different lengths; right-align them all."""
   n = min(len(a) for a in arrays)
   return [np.asarray(a)[-n:] for a in arrays]
def clamp(v):
   return float(min(max(v, FIX["min_offset_pct"]), FIX["max_offset_pct"]))
def build_callbacks(params, run_data):
   """
   OctoBot-Script splits a strategy into:
     initialize(ctx) -> runs once on the first candle. Do vectorised work here.
     strategy(ctx)   -> runs on EVERY closed candle. Keep it cheap.
   """
   async def initialize(ctx):
       closes = await obs.Close(ctx, max_history=True)
       highs  = await obs.High(ctx,  max_history=True)
       lows   = await obs.Low(ctx,   max_history=True)
       times  = await obs.Time(ctx,  max_history=True, use_close_time=True)
       rsi  = tulipy.rsi(closes, period=params["rsi_period"])
       ema_f = tulipy.ema(closes, period=FIX["ema_fast"])
       ema_s = tulipy.ema(closes, period=FIX["ema_slow"])
       atr   = tulipy.atr(highs, lows, closes, period=FIX["atr_period"])
       t, c, rsi, ema_f, ema_s, atr = tail(times, closes, rsi, ema_f, ema_s, atr)
       atr_pct = np.where(c > 0, atr / c * 100.0, 0.0)
       entries, offsets = set(), {}
       for i in range(len(t)):
           oversold = rsi[i] &lt; params["rsi_threshold"]
           uptrend  = ema_f[i] > ema_s[i]
           if oversold and uptrend and atr_pct[i] > 0:
               ts = float(t[i])
               entries.add(ts)
               offsets[ts] = (
                   clamp(FIX["sl_atr_mult"]     * atr_pct[i]),
                   clamp(params["tp_atr_mult"]  * atr_pct[i]),
               )
       run_data["entries"] = entries
       run_data["offsets"] = offsets
       if run_data.get("plot"):
           await obs.plot_indicator(ctx, f"RSI({params['rsi_period']})", t, rsi, entries)
           await obs.plot_indicator(ctx, f"EMA{FIX['ema_fast']}",  t, ema_f)
           await obs.plot_indicator(ctx, f"EMA{FIX['ema_slow']}",  t, ema_s)
           await obs.plot_indicator(ctx, "ATR %", t, atr_pct)
   async def strategy(ctx):
       now = obs.current_live_time(ctx)
       if now not in run_data["entries"]:
           return
       sl, tp = run_data["offsets"][now]
       await obs.market(
           ctx, "buy",
           amount=FIX["position_size"],
           stop_loss_offset=f"-{sl:.2f}%",
           take_profit_offset=f"{tp:.2f}%",
       )
   return initialize, strategy
def metrics(res):
   br = res.report.get("bot_report", {})
   first = lambda d: float(list(d.values())[0]) if isinstance(d, dict) and d else float("nan")
   return {
       "profitability":  first(br.get("profitability", {})),
       "market":         first(br.get("market_average_profitability", {})),
       "reference":      br.get("reference_market"),
       "start_portfolio": str(br.get("starting_portfolio")),
       "end_portfolio":   str(br.get("end_portfolio")),
       "candles":        res.candles_count,
       "duration_s":     round(res.duration or 0, 2),
       "errors":         res.report.get("errors_count"),
   }
async def load_data(window):
   """Try each exchange until one serves data (Binance blocks many datacenter IPs)."""
   start, end = window
   last = None
   for ex in CFG["exchanges"]:
       try:
           print(f"  ↓ fetching {CFG['symbol']} {CFG['time_frame']} from {ex} "
                 f"[{time.strftime('%Y-%m-%d', time.gmtime(start))} → "
                 f"{time.strftime('%Y-%m-%d', time.gmtime(end))}]", flush=True)
           data = await obs.get_data(
               CFG["symbol"], CFG["time_frame"],
               exchange=ex, exchange_type="spot",
               start_timestamp=start, end_timestamp=end,
               social_services=[],
           )
           print(f"    ✓ {ex} ok -> {data.data_files}", flush=True)
           return data, ex
       except Exception as e:
           last = e
           print(f"    ✗ {ex}: {type(e).__name__}: {e}", flush=True)
   raise RuntimeError(f"no exchange served data; last error: {last}")
async def backtest(data, params, plot=False, storage=False):
   run_data = {"entries": None, "offsets": {}, "plot": plot}
   init_f, strat_f = build_callbacks(params, run_data)
   res = await obs.run(
       data, params,
       strategy_func=strat_f,
       initialize_func=init_f,
       enable_logs=False,
       enable_storage=storage,
   )
   return res, len(run_data["entries"] or ())
async def main():
   out = {"grid": [], "best": None, "oos": None, "errors": []}
   print("\n" + "=" * 78 + "\n  IN-SAMPLE GRID SEARCH\n" + "=" * 78, flush=True)
   is_data, ex_used = await load_data(CFG["in_sample"])
   out["exchange"] = ex_used
   keys  = list(CFG["grid"].keys())
   combos = [dict(zip(keys, v)) for v in itertools.product(*CFG["grid"].values())]
   print(f"  {len(combos)} configurations to evaluate\n", flush=True)
   for i, params in enumerate(combos, 1):
       try:
           res, n_sig = await backtest(is_data, params)
           m = metrics(res)
           m.update(params); m["signals"] = n_sig
           m["edge"] = m["profitability"] - m["market"]
           out["grid"].append(m)
           print(f"  [{i:>2}/{len(combos)}] {params}  "
                 f"P&amp;L {m['profitability']:+.2f}%  vs market {m['market']:+.2f}%  "
                 f"edge {m['edge']:+.2f}%  ({n_sig} signals, {m['duration_s']}s)", flush=True)
       except Exception as e:
           out["errors"].append(f"{params}: {e}")
           print(f"  [{i:>2}/{len(combos)}] {params} FAILED: {e}", flush=True)
           traceback.print_exc()
   await is_data.stop()
   if not out["grid"]:
       json.dump(out, open(OUT, "w")); raise SystemExit("no successful runs")
   best = max(out["grid"], key=lambda r: r["edge"])
   out["best"] = {k: best[k] for k in keys}
   print(f"\n  <img src="https://s.w.org/images/core/emoji/17.0.2/72x72/1f3c6.png" alt="🏆" class="wp-smiley" style="height: 1em; max-height: 1em;" /> best in-sample config: {out['best']}  (edge {best['edge']:+.2f}%)", flush=True)
   print("\n" + "=" * 78 + "\n  OUT-OF-SAMPLE VALIDATION (never optimised on)\n" + "=" * 78,
         flush=True)
   oos_data, _ = await load_data(CFG["out_of_sample"])
   res, n_sig = await backtest(oos_data, out["best"], plot=True, storage=True)
   m = metrics(res); m.update(out["best"])
   m["signals"] = n_sig; m["edge"] = m["profitability"] - m["market"]
   out["oos"] = m
   print(f"  OOS P&amp;L {m['profitability']:+.2f}%  vs market {m['market']:+.2f}%  "
         f"edge {m['edge']:+.2f}%  ({n_sig} signals)", flush=True)
   print("  " + res.describe(), flush=True)
   report_dir = os.path.join(os.getcwd(), "report")
   os.makedirs(report_dir, exist_ok=True)
   try:
       plot = await res.plot(report_file=os.path.join(report_dir, "report.html"), show=False)
       out["bundle"] = os.path.join(os.path.dirname(os.path.abspath(plot.report_file)),
                                    "report.json")
       print(f"  ✓ report bundle: {out['bundle']}", flush=True)
   except Exception as e:
       out["errors"].append(f"report: {e}")
       print(f"  ✗ report generation failed: {e}", flush=True)
   await oos_data.stop()
   json.dump(out, open(OUT, "w"), indent=2, default=str)
   print("\n✓ results written to", OUT, flush=True)
asyncio.run(main())
'''
with open(WORKER, "w") as f:
   f.write(WORKER_SRC)
</code></pre></div></div>



<p class="wp-block-paragraph">We build the standalone OctoBot worker that contains the strategy logic and executes inside the isolated virtual environment. We calculate RSI, fast and slow EMAs, and ATR values, generate entry signals when oversold conditions align with an upward trend, and derive volatility-adjusted stop-loss and take-profit offsets. We also define the historical data loader, backtest runner, grid-search loop, out-of-sample validation, performance metrics, and report generation process.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import datetime as _dt
def ts(d):
   return int(_dt.datetime.strptime(d, "%Y-%m-%d")
              .replace(tzinfo=_dt.timezone.utc).timestamp())
CONFIG_PATH  = os.path.join(WORK_DIR, "config.json")
RESULTS_PATH = os.path.join(WORK_DIR, "results.json")
json.dump({
   "symbol": SYMBOL, "time_frame": TIME_FRAME, "exchanges": EXCHANGES,
   "in_sample":     [ts(IN_SAMPLE[0]),     ts(IN_SAMPLE[1])],
   "out_of_sample": [ts(OUT_OF_SAMPLE[0]), ts(OUT_OF_SAMPLE[1])],
   "grid": GRID, "fixed": FIXED,
}, open(CONFIG_PATH, "w"), indent=2)
env = dict(os.environ, OBS_CONFIG=CONFIG_PATH, OBS_OUT=RESULTS_PATH,
          PYTHONUNBUFFERED="1")
t0 = time.time()
proc = subprocess.Popen([PY, WORKER], cwd=WORK_DIR, env=env, text=True,
                       stdout=subprocess.PIPE, stderr=subprocess.STDOUT, bufsize=1)
for line in proc.stdout:
   print(line.rstrip())
proc.wait()
print(f"\n<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/23f1.png" alt="⏱" class="wp-smiley" style="height: 1em; max-height: 1em;" />  total backtesting time: {time.time() - t0:.1f}s  (exit {proc.returncode})")
if not os.path.exists(RESULTS_PATH):
   raise SystemExit("No results produced — read the log above. "
                    "Most common cause: every exchange refused the data request.")
R = json.load(open(RESULTS_PATH))
</code></pre></div></div>



<p class="wp-block-paragraph">We convert the selected in-sample and out-of-sample dates into UTC timestamps and serialize the complete experiment configuration into a JSON file. We launch the OctoBot worker as a separate subprocess so its dependency environment remains isolated from the main Colab kernel while its logs stream directly into the notebook. We then verify that the run produces a results file and load the generated JSON output for downstream analysis.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">import pandas as pd
import plotly.graph_objects as go
from plotly.subplots import make_subplots
pd.set_option("display.width", 160)
grid = pd.DataFrame(R["grid"]).sort_values("edge", ascending=False)
cols = [c for c in ["rsi_period", "rsi_threshold", "tp_atr_mult", "signals",
                   "profitability", "market", "edge", "duration_s"] if c in grid.columns]
print("\n=== IN-SAMPLE GRID (ranked by edge over buy &amp; hold) ===")
print(grid[cols].to_string(index=False, float_format=lambda v: f"{v:,.2f}"))
if R.get("oos"):
   o = R["oos"]
   print("\n=== OUT-OF-SAMPLE ===")
   print(f"  config          : { {k: o[k] for k in GRID} }")
   print(f"  strategy return : {o['profitability']:+.2f}%")
   print(f"  buy &amp; hold      : {o['market']:+.2f}%")
   print(f"  edge            : {o['edge']:+.2f}%   ← the only number that matters")
   print(f"  entries taken   : {o['signals']}")
   print(f"  end portfolio   : {o['end_portfolio']}")
   is_edge = grid.iloc[0]["edge"]
   decay = o["edge"] - is_edge
   print(f"\n  edge decay IS→OOS: {decay:+.2f} pts "
         f"({'holds up' if decay > -5 else 'likely overfit — treat with suspicion'})")
</code></pre></div></div>



<p class="wp-block-paragraph">We move back into the Colab environment and organize the grid-search results with Pandas for easier comparison and interpretation. We rank every parameter configuration according to its excess return over the market and print the key performance metrics for both the in-sample search and out-of-sample validation. We also calculate the change in strategy edge between the two periods to obtain a simple indication of whether the optimized parameters generalize or show signs of overfitting.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">if {"rsi_period", "rsi_threshold"} &lt;= set(grid.columns):
   pivot = grid.pivot_table(index="rsi_threshold", columns="rsi_period",
                            values="edge", aggfunc="mean")
   fig = go.Figure(go.Heatmap(z=pivot.values, x=pivot.columns, y=pivot.index,
                              colorscale="RdYlGn", zmid=0,
                              colorbar=dict(title="edge %"),
                              text=pivot.round(1).values, texttemplate="%{text}"))
   fig.update_layout(title="In-sample edge vs buy &amp; hold — a broad plateau is trustworthy, "
                           "an isolated hot cell is noise",
                     xaxis_title="RSI period", yaxis_title="RSI buy threshold",
                     height=380, template="plotly_dark")
   fig.show()
def harvest(node, found):
   """The report bundle nests display elements arbitrarily; walk it and grab
   anything that looks like a plottable series."""
   if isinstance(node, dict):
       if isinstance(node.get("x"), list) and len(node["x"]) > 1:
           if all(k in node for k in ("open", "high", "low", "close")):
               found["candles"].append(node)
           elif isinstance(node.get("y"), list) and len(node["y"]) == len(node["x"]):
               found["series"].append(node)
       for v in node.values():
           harvest(v, found)
   elif isinstance(node, list):
       for v in node:
           harvest(v, found)
   return found
bundle_path = R.get("bundle")
</code></pre></div></div>



<p class="wp-block-paragraph">We visualize the parameter-search surface by plotting the average strategy edge across RSI periods and entry thresholds as an interactive Plotly heatmap. We use this surface to inspect whether strong performance appears across a broad parameter region or only around an isolated configuration that may represent noise. We also define a recursive report-harvesting function that searches OctoBot&#8217;s nested report structure for candle data and other plottable time-series elements.</p>



<div class="dm-code-snippet dark dm-normal-version default no-background-mobile" snippet-height="" style="background-color:#abb8c3"><div class="control-language"><div class="dm-buttons"><div class="dm-buttons-left"><div class="dm-button-snippet red-button"></div><div class="dm-button-snippet orange-button"></div><div class="dm-button-snippet green-button"></div></div><div class="dm-buttons-right"><a id="dm-copy-raw-code"><span class="dm-copy-text">Copy Code</span><span class="dm-copy-confirmed" style="display:none">Copied</span><span class="dm-error-message" style="display:none">Use a different Browser</span></a></div></div><pre class=" no-line-numbers"><code id="dm-code-raw" class=" no-wrap language-php">if bundle_path and os.path.exists(bundle_path):
   bundle = json.load(open(bundle_path))
   f = harvest(bundle, {"candles": [], "series": []})
   print(f"\n=== REPORT BUNDLE === {len(f['candles'])} candle set(s), "
         f"{len(f['series'])} series")
   def norm_x(xs):
       xs = [float(v) for v in xs]
       unit = "ms" if (xs and max(xs) > 1e11) else "s"
       return pd.to_datetime(xs, unit=unit)
   fig = make_subplots(rows=2, cols=1, shared_xaxes=True,
                       row_heights=[0.62, 0.38], vertical_spacing=0.06,
                       subplot_titles=("Price &amp; executed trades",
                                       "Portfolio value / indicators"))
   if f["candles"]:
       c = max(f["candles"], key=lambda d: len(d["x"]))
       fig.add_trace(go.Candlestick(x=norm_x(c["x"]), open=c["open"], high=c["high"],
                                    low=c["low"], close=c["close"], name=SYMBOL),
                     row=1, col=1)
   portfolio_kw = ("portfolio", "value", "wallet", "balance")
   for s in f["series"]:
       title = str(s.get("title") or s.get("name") or "series")
       n = len(s["x"])
       if n &lt; 3:
           continue
       mode = s.get("mode") or ("markers" if n &lt; 60 else "lines")
       row = 2 if any(k in title.lower() for k in portfolio_kw) or "rsi" in title.lower() \
               or "atr" in title.lower() else 1
       fig.add_trace(go.Scatter(x=norm_x(s["x"]), y=s["y"], name=title[:38],
                                mode=mode, opacity=0.9), row=row, col=1)
   fig.update_layout(height=760, template="plotly_dark", xaxis_rangeslider_visible=False,
                     title=f"OctoBot out-of-sample run — {SYMBOL} {TIME_FRAME} "
                           f"({R.get('exchange', '?')}) — {OUT_OF_SAMPLE[0]} → {OUT_OF_SAMPLE[1]}",
                     legend=dict(orientation="h", y=-0.08))
   fig.show()
else:
   print("\n(no report bundle — charts skipped; the numeric results above are still valid)")
if R.get("errors"):
   print("\n<img src="https://s.w.org/images/core/emoji/17.0.2/72x72/26a0.png" alt="⚠" class="wp-smiley" style="height: 1em; max-height: 1em;" /> non-fatal errors during the run:")
   for e in R["errors"]:
       print("  -", e)
print("""
──────────────────────────────────────────────────────────────────────────────
WHERE TO GO NEXT
 • Edit GRID / FIXED at the top and re-run — the env is cached, only backtests rerun.
 • obs.limit(ctx, "sell", amount="50%", offset="2%")  → limit orders
 • obs.set_leverage(ctx, 3) + exchange_type="future"  → futures / shorts
 • get_data() accepts LISTS for symbol and time_frame → multi-asset, multi-TF strategies
 • Swap tulipy for pandas-ta / your own ML model: initialize() just needs to fill a
   set of entry timestamps, so a trained classifier drops straight in.
 • Docs: https://www.octobot.cloud/en/guides/octobot-script
 • For live/paper trading use the full OctoBot app, not this scripting layer.
Reminder: past performance in a backtest tells you about the past. Slippage, fees
beyond the simulator's model, liquidity and regime change all bite in live markets.
──────────────────────────────────────────────────────────────────────────────
""")
</code></pre></div></div>



<p class="wp-block-paragraph">We load the generated OctoBot report bundle and reconstruct the out-of-sample trading results as interactive price and indicator charts. We normalize timestamps, display candlestick data, and dynamically add available portfolio, RSI, ATR, trade, and other report series to a multi-panel Plotly visualization. We finally surface any non-fatal execution errors and outline several directions for extending the workflow, including limit orders, futures, multi-asset strategies, and machine-learning-based signals.</p>



<p class="wp-block-paragraph">In conclusion, we implemented an end-to-end OctoBot quantitative research pipeline that moves beyond a simple single-run backtest and introduces a more disciplined strategy-development process. We isolated OctoBot’s dependency stack, retrieved exchange data through its native infrastructure, defined a volatility-aware RSI and EMA strategy, optimized its parameters on historical in-sample data, and evaluated the winning configuration on an untouched out-of-sample window. By comparing strategy profitability against buy-and-hold performance and examining parameter surfaces and out-of-sample edge decay, we gained a clearer view of whether our results represent a robust trading signal or merely an overfitted historical pattern. We also transformed OctoBot’s generated report bundle into interactive visualizations that make strategy behavior, market movements, indicators, and portfolio dynamics easier to inspect.</p>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the<strong> <a href="https://github.com/MARKTECHPOST-AI-MEDIA-INC/AI-Agents-Projects-Tutorials/blob/main/Data%20Science/OctoBot_Advanced_Quant_Strategy_Backtesting_Marktechpost.ipynb" target="_blank" rel="noreferrer noopener">Full Codes here</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://magic.beehiiv.com/v1/f5e63dd4-5653-4f09-83e2-321a8b1ba526?email={{email}}" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/08/11/building-and-validating-a-quantitative-trading-strategy-with-octobot-walk-forward-backtesting-parameter-optimization-and-interactive-analysis/">Building and Validating a Quantitative Trading Strategy with OctoBot, Walk-Forward Backtesting, Parameter Optimization, and Interactive Analysis</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81669</post-id>	</item>
		<item>
		<title>webAI Releases TwIL-LM: A 1.7B and 3B Formal-Logic Model Family for Autoformalization on Local Hardware</title>
		<link>https://www.marktechpost.com/2026/08/10/webai-releases-twil-lm-a-1-7b-and-3b-formal-logic-model-family-for-autoformalization-on-local-hardware/</link>
		
		<dc:creator><![CDATA[Asif Razzaq]]></dc:creator>
		<pubDate>Tue, 11 Aug 2026 06:20:09 +0000</pubDate>
				<category><![CDATA[Agentic AI]]></category>
		<category><![CDATA[AI Infrastructure]]></category>
		<category><![CDATA[AI Shorts]]></category>
		<category><![CDATA[Applications]]></category>
		<category><![CDATA[Artificial Intelligence]]></category>
		<category><![CDATA[Editors Pick]]></category>
		<category><![CDATA[Language Model]]></category>
		<category><![CDATA[Large Language Model]]></category>
		<category><![CDATA[Machine Learning]]></category>
		<category><![CDATA[New Releases]]></category>
		<category><![CDATA[Software Engineering]]></category>
		<category><![CDATA[Staff]]></category>
		<category><![CDATA[Tech News]]></category>
		<category><![CDATA[Technology]]></category>
		<guid isPermaLink="false">https://www.marktechpost.com/?p=81661</guid>

					<description><![CDATA[<p>webAI has released TwIL-LM, a family of formal-logic models at 1.7B and 3B parameters that translate English into first-order logic and check whether conclusions follow from premises. The 3B runs on CPU or 4GB of VRAM; the 1.7B downloads at 1.06GB. Both ship under a non-commercial license. The model card also shows that the headline benchmark scores belong to an unreleased checkpoint, not the weights on the Hub.</p>
<p>The post <a href="https://www.marktechpost.com/2026/08/10/webai-releases-twil-lm-a-1-7b-and-3b-formal-logic-model-family-for-autoformalization-on-local-hardware/">webAI Releases TwIL-LM: A 1.7B and 3B Formal-Logic Model Family for Autoformalization on Local Hardware</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></description>
										<content:encoded><![CDATA[
<p class="wp-block-paragraph">webAI has released <a href="https://huggingface.co/webAI-Official/TwIL-LM">TwIL-LM</a>, a <a href="https://www.webai.com/blog/webai-releases-twil-lm-a-family-of-formal-logic-models-that-outreason-a-120b-model-and-run-on-an-iphone" target="_blank" rel="noreferrer noopener">two-model family of formal-logic reasoners at 1.7B and 3B parameters</a>. The 3B member, <a href="https://huggingface.co/webAI-Official/TwIL-LM3">TwIL-LM3</a>, is a merged fine-tune of <a href="https://huggingface.co/HuggingFaceTB/SmolLM3-3B">SmolLM3-3B</a>; the 1.7B member is a PEFT LoRA adapter for <a href="https://huggingface.co/HuggingFaceTB/SmolLM2-1.7B-Instruct">SmolLM2-1.7B-Instruct</a>. Both target <a href="https://arxiv.org/abs/2205.12615">autoformalization</a>: translating English into first-order logic and checking whether a conclusion follows from its premises. Both run locally, with a 1.06 GB quantized build for the 1.7B and a 1.78 GiB <code>Q4_K_M</code> GGUF for the 3B. webAI&#8217;s <a href="https://www.webai.com/blog/webai-releases-twil-lm-a-family-of-formal-logic-models-that-outreason-a-120b-model-and-run-on-an-iphone">announcement</a> frames the release around beating gpt-oss-120b on four of five formal-reasoning lanes. </p>



<h2 id="h-is-it-deployable" class="wp-block-heading"><strong>Is it deployable?</strong></h2>



<p class="wp-block-paragraph"><strong>Partially</strong>. Non-commercial use only, as of now.</p>



<p class="wp-block-paragraph">Both checkpoints ship under the <a href="https://huggingface.co/webAI-Official/TwIL-LM3">webAI Non-Commercial License ver. 1.0</a>. Revenue-generating deployment requires a separate agreement with webAI.</p>



<ul class="wp-block-list">
<li><strong>Company level:</strong> any size. The 3B <code>Q4_K_M</code> GGUF is 1.78 GiB and runs on CPU or 4 GB of VRAM. The 1.7B <code>Q4_K_M</code> is 1.06 GB.</li>



<li><strong>Industries:</strong> compliance and RegTech, financial services, healthcare and pharma, legal and contract operations, formal-methods research. webAI positions local execution for environments where data cannot leave the device.</li>



<li><strong>Applications:</strong> first-order logic (FOL) translation, entailment classification over premise sets, natural language to structured query, <a href="https://lean-lang.org/">Lean</a> formalization drafting and critique, and a verifier layer that checks a larger model&#8217;s output.</li>
</ul>



<h2 id="h-how-twil-lm3-was-built" class="wp-block-heading"><strong>How TwIL-LM3 was built?</strong></h2>



<p class="wp-block-paragraph">Four stages sit on top of the base model. LoRA supervised fine-tuning on a synthetic formal-logic corpus. Checkpoint fusion, averaging intermediate SFT checkpoints in parameter space. WiSE-FT interpolation back toward the pretrained base at <strong>λ = 0.25</strong>. Then MGPO, an entropy-weighted <a href="https://arxiv.org/abs/2402.03300">GRPO</a> stage run against a programmatic verifier. The published checkpoint is step 2071.</p>



<p class="wp-block-paragraph">That λ is load-bearing: only a quarter of the fine-tuned delta is retained. A sibling arm that skipped the interpolation scored higher in-domain, at macro gate 0.515, but gave back roughly twelve points of held-out capability. webAI did not publish that arm.</p>



<iframe id="twil-explainer-frame" style="width:100%;border:0;overflow:hidden;display:block;" height="600" scrolling="no" title="webAI TwIL-LM interactive explainer" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;
&lt;title&gt;TwIL-LM Explainer&lt;/title&gt;
&lt;style&gt;
  :root{
    --ink:#0B0D10; --panel:#12151A; --panel2:#171B21; --line:#232A33;
    --text:#E6E9EE; --muted:#8B95A3; --dim:#5C6672;
    --blue:#4FC3F7; --violet:#8B7BFF; --amber:#E8A33D;
    --mono: ui-monospace, &quot;SF Mono&quot;, &quot;JetBrains Mono&quot;, &quot;Roboto Mono&quot;, Menlo, Consolas, monospace;
    --sans: -apple-system, BlinkMacSystemFont, &quot;Segoe UI&quot;, Helvetica, Arial, sans-serif;
  }
  *{box-sizing:border-box}
  html,body{margin:0;padding:0;background:transparent}
  #twil{
    background:var(--ink); color:var(--text); font-family:var(--sans);
    border:1px solid var(--line); border-radius:14px; overflow:hidden;
    max-width:860px; margin:0 auto; line-height:1.5;
  }
  #twil .hd{padding:22px 24px 18px;border-bottom:1px solid var(--line);
    background:linear-gradient(180deg,#101419 0%,var(--ink) 100%)}
  #twil .eyebrow{font-family:var(--mono);font-size:11px;letter-spacing:.16em;
    text-transform:uppercase;color:var(--blue);margin:0 0 8px}
  #twil h2{margin:0 0 6px;font-size:23px;line-height:1.2;letter-spacing:-.02em;font-weight:750;color:#fff}
  #twil .sub{margin:0;font-size:13.5px;color:var(--muted);max-width:62ch}

  #twil .tabs{display:flex;gap:0;border-bottom:1px solid var(--line);overflow-x:auto;
    -webkit-overflow-scrolling:touch;scrollbar-width:none}
  #twil .tabs::-webkit-scrollbar{display:none}
  #twil .tab{flex:0 0 auto;appearance:none;background:none;border:0;cursor:pointer;
    font-family:var(--mono);font-size:11.5px;letter-spacing:.08em;text-transform:uppercase;
    color:var(--dim);padding:14px 18px;border-bottom:2px solid transparent;white-space:nowrap}
  #twil .tab:hover{color:var(--text)}
  #twil .tab[aria-selected=&quot;true&quot;]{color:#fff;border-bottom-color:var(--blue)}
  #twil .tab:focus-visible{outline:2px solid var(--blue);outline-offset:-4px}

  #twil .pane{padding:22px 24px 24px;display:none}
  #twil .pane.on{display:block}
  #twil .lede{font-size:14px;color:var(--muted);margin:0 0 18px;max-width:66ch}
  #twil .lede b{color:var(--text);font-weight:600}

  /* ---- controls ---- */
  #twil .ctl{display:flex;flex-wrap:wrap;gap:8px;align-items:center;margin:0 0 16px}
  #twil .btn{appearance:none;font-family:var(--mono);font-size:11.5px;letter-spacing:.06em;
    background:var(--panel2);color:var(--text);border:1px solid var(--line);
    border-radius:6px;padding:7px 12px;cursor:pointer}
  #twil .btn:hover{border-color:var(--blue);color:#fff}
  #twil .btn[aria-pressed=&quot;true&quot;],#twil .btn.primary{background:var(--blue);color:#06212C;
    border-color:var(--blue);font-weight:700}
  #twil .btn[data-s=&quot;star&quot;][aria-pressed=&quot;true&quot;]{background:var(--violet);color:#0F0B24;border-color:var(--violet)}
  #twil .btn[data-s=&quot;oss&quot;][aria-pressed=&quot;true&quot;]{background:var(--amber);color:#2A1A02;border-color:var(--amber)}
  #twil .btn[data-s][aria-pressed=&quot;false&quot;]{opacity:.55}
  #twil .btn:focus-visible{outline:2px solid var(--blue);outline-offset:2px}
  #twil .lbl{font-family:var(--mono);font-size:10.5px;letter-spacing:.12em;
    text-transform:uppercase;color:var(--dim);margin-right:4px}

  /* ---- proof / autoformalize ---- */
  #twil .stage{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:18px}
  #twil .row{margin:0 0 14px;opacity:0;transform:translateY(6px);transition:opacity .45s,transform .45s}
  #twil .row.in{opacity:1;transform:none}
  #twil .cap{font-family:var(--mono);font-size:10px;letter-spacing:.14em;text-transform:uppercase;
    color:var(--dim);margin:0 0 6px}
  #twil .nl{font-size:15px;color:var(--text)}
  #twil .fol{font-family:var(--mono);font-size:13.5px;color:var(--blue);white-space:pre-wrap;word-break:break-word}
  #twil .bar{height:2px;background:var(--blue);width:0;transition:width .6s ease;margin:14px 0 10px;
    box-shadow:0 0 12px rgba(79,195,247,.55)}
  #twil .bar.in{width:100%}
  #twil .concl{font-family:var(--mono);font-size:15px;color:#fff}
  #twil .turn{color:var(--violet);font-weight:700}
  #twil .badge{display:inline-block;font-family:var(--mono);font-size:11px;letter-spacing:.1em;
    text-transform:uppercase;padding:5px 10px;border-radius:5px;
    background:rgba(79,195,247,.12);color:var(--blue);border:1px solid rgba(79,195,247,.4)}
  #twil .note{font-size:12.5px;color:var(--dim);margin:14px 0 0;border-left:2px solid var(--line);padding-left:12px}
  #twil .cursor{display:inline-block;width:8px;height:1.05em;background:var(--blue);
    vertical-align:-2px;animation:blink 1s steps(2) infinite}
  @keyframes blink{50%{opacity:0}}

  /* ---- bars ---- */
  #twil .lane{margin:0 0 12px}
  #twil .lane-h{display:flex;justify-content:space-between;align-items:baseline;margin:0 0 5px}
  #twil .lane-n{font-family:var(--mono);font-size:11.5px;color:var(--text)}
  #twil .lane-u{font-family:var(--mono);font-size:10px;color:var(--dim)}
  #twil .track{display:flex;flex-direction:column;gap:4px}
  #twil .b-wrap{display:flex;align-items:center;gap:8px}
  #twil .b-out{flex:1;height:12px;background:var(--panel2);border-radius:3px;overflow:hidden}
  #twil .b-in{height:100%;width:0;border-radius:3px;transition:width .8s cubic-bezier(.22,1,.36,1)}
  #twil .b-val{font-family:var(--mono);font-size:11px;width:44px;text-align:right;color:var(--muted)}
  #twil .s-ship .b-in{background:var(--blue)} #twil .s-ship .b-val{color:var(--blue)}
  #twil .s-star .b-in{background:var(--violet)} #twil .s-star .b-val{color:var(--violet)}
  #twil .s-oss .b-in{background:var(--amber)} #twil .s-oss .b-val{color:var(--amber)}
  #twil .lane.avg{border-top:1px solid var(--line);padding-top:14px;margin-top:16px}
  #twil .warn{margin:18px 0 0;background:rgba(139,123,255,.07);border:1px solid rgba(139,123,255,.3);
    border-radius:8px;padding:13px 15px;font-size:13px;color:var(--muted)}
  #twil .warn b{color:var(--violet)}

  /* ---- training ---- */
  #twil .steps{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:0 0 16px}
  #twil .step{background:var(--panel);border:1px solid var(--line);border-radius:8px;
    padding:11px 12px;cursor:pointer;text-align:left;color:var(--muted);font-family:var(--sans)}
  #twil .step:hover{border-color:var(--blue)}
  #twil .step[aria-pressed=&quot;true&quot;]{border-color:var(--blue);background:var(--panel2);color:var(--text)}
  #twil .step .k{font-family:var(--mono);font-size:10px;color:var(--blue);letter-spacing:.1em;display:block;margin-bottom:5px}
  #twil .step .t{font-size:12.5px;font-weight:600;line-height:1.3}
  #twil .detail{background:var(--panel);border:1px solid var(--line);border-radius:8px;
    padding:15px 16px;font-size:13.5px;color:var(--muted);min-height:74px}
  #twil .detail b{color:var(--text)}
  #twil .gauges{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin:16px 0 0}
  #twil .g{background:var(--panel);border:1px solid var(--line);border-radius:8px;padding:14px}
  #twil .g-l{font-family:var(--mono);font-size:10px;letter-spacing:.11em;text-transform:uppercase;color:var(--dim)}
  #twil .g-v{font-family:var(--mono);font-size:26px;color:#fff;margin:6px 0 9px;letter-spacing:-.01em}
  #twil .g-b{height:8px;background:var(--panel2);border-radius:3px;overflow:hidden}
  #twil .g-f{height:100%;width:0;border-radius:3px;transition:width .7s cubic-bezier(.22,1,.36,1)}

  /* ---- throughput ---- */
  #twil .rt{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:16px}
  #twil .rl{margin:0 0 15px}
  #twil .rl-h{display:flex;justify-content:space-between;align-items:baseline;margin:0 0 6px;gap:10px}
  #twil .rl-n{font-family:var(--mono);font-size:12px;color:var(--text)}
  #twil .rl-s{font-family:var(--mono);font-size:11px;color:var(--muted)}
  #twil .lane-t{position:relative;height:22px;background:var(--panel2);border-radius:4px;overflow:hidden}
  #twil .dot{position:absolute;top:7px;width:8px;height:8px;border-radius:50%;left:-10px}
  #twil .tbl{width:100%;border-collapse:collapse;margin:16px 0 0;font-family:var(--mono);font-size:11.5px}
  #twil .tbl th{text-align:left;color:var(--dim);font-weight:400;padding:6px 8px;border-bottom:1px solid var(--line);
    letter-spacing:.06em;text-transform:uppercase;font-size:10px}
  #twil .tbl td{padding:6px 8px;border-bottom:1px solid var(--line);color:var(--muted)}
  #twil .tbl td.hi{color:var(--blue)}

  #twil .ft{border-top:1px solid var(--line);padding:14px 24px;display:flex;
    justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap;
    font-family:var(--mono);font-size:10.5px;letter-spacing:.06em;color:var(--dim)}
  #twil .ft a{color:var(--blue);text-decoration:none}
  #twil .ft a:hover{text-decoration:underline}
  #twil .ft .mtp{color:var(--text)}
  #twil .ft .mtp span{color:var(--blue)}

  @media (max-width:640px){
    #twil .hd{padding:18px 16px 15px}
    #twil h2{font-size:19px}
    #twil .pane{padding:18px 16px 20px}
    #twil .tab{padding:12px 13px;font-size:10.5px}
    #twil .steps{grid-template-columns:1fr 1fr}
    #twil .gauges{grid-template-columns:1fr}
    #twil .stage{padding:14px}
    #twil .fol{font-size:12px}
    #twil .b-val{width:38px;font-size:10px}
    #twil .ft{padding:12px 16px;font-size:9.5px}
    #twil .dot{width:6px;height:6px;top:8px}
  }
  @media (prefers-reduced-motion:reduce){
    #twil *{transition-duration:.01ms !important;animation:none !important}
  }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id=&quot;twil&quot;&gt;
  &lt;div class=&quot;hd&quot;&gt;
    &lt;p class=&quot;eyebrow&quot;&gt;webAI &amp;middot; TwIL-LM &amp;middot; interactive explainer&lt;/p&gt;
    &lt;h2&gt;What a 3B formal-logic model actually does, and what it actually scores&lt;/h2&gt;
    &lt;p class=&quot;sub&quot;&gt;Four views built only from webAI's published model cards. Every number below is traceable to the TwIL-LM3 or TwIL-LM card on Hugging Face.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;tabs&quot; role=&quot;tablist&quot; aria-label=&quot;TwIL-LM explainer views&quot;&gt;
    &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;true&quot; data-p=&quot;p1&quot;&gt;1 &amp;middot; Autoformalize&lt;/button&gt;
    &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-p=&quot;p2&quot;&gt;2 &amp;middot; Benchmarks&lt;/button&gt;
    &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-p=&quot;p3&quot;&gt;3 &amp;middot; Training&lt;/button&gt;
    &lt;button class=&quot;tab&quot; role=&quot;tab&quot; aria-selected=&quot;false&quot; data-p=&quot;p4&quot;&gt;4 &amp;middot; Throughput&lt;/button&gt;
  &lt;/div&gt;

  &lt;!-- ============ 1 ============ --&gt;
  &lt;div class=&quot;pane on&quot; id=&quot;p1&quot; role=&quot;tabpanel&quot;&gt;
    &lt;p class=&quot;lede&quot;&gt;TwIL-LM does one job: turn English into formal logic, then check whether the conclusion follows. Pick a case and step through it.&lt;/p&gt;
    &lt;div class=&quot;ctl&quot;&gt;
      &lt;span class=&quot;lbl&quot;&gt;Case&lt;/span&gt;
      &lt;button class=&quot;btn&quot; data-ex=&quot;0&quot; aria-pressed=&quot;true&quot;&gt;Entailment&lt;/button&gt;
      &lt;button class=&quot;btn&quot; data-ex=&quot;1&quot; aria-pressed=&quot;false&quot;&gt;Contradiction&lt;/button&gt;
      &lt;button class=&quot;btn&quot; data-ex=&quot;2&quot; aria-pressed=&quot;false&quot;&gt;Neutral&lt;/button&gt;
      &lt;button class=&quot;btn primary&quot; id=&quot;run&quot;&gt;Run &amp;#9654;&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;stage&quot;&gt;
      &lt;div class=&quot;row&quot; id=&quot;r1&quot;&gt;&lt;p class=&quot;cap&quot;&gt;01 &amp;nbsp;Premises, natural language&lt;/p&gt;&lt;div class=&quot;nl&quot; id=&quot;nlp&quot;&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot; id=&quot;r2&quot;&gt;&lt;p class=&quot;cap&quot;&gt;02 &amp;nbsp;Autoformalization, first-order logic&lt;/p&gt;&lt;div class=&quot;fol&quot; id=&quot;folp&quot;&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot; id=&quot;r3&quot;&gt;&lt;p class=&quot;cap&quot;&gt;03 &amp;nbsp;Deduction&lt;/p&gt;&lt;div class=&quot;bar&quot; id=&quot;bar&quot;&gt;&lt;/div&gt;
        &lt;div class=&quot;concl&quot;&gt;&lt;span class=&quot;turn&quot;&gt;&amp;#8866;&lt;/span&gt; &lt;span id=&quot;concl&quot;&gt;&lt;/span&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;row&quot; id=&quot;r4&quot;&gt;&lt;p class=&quot;cap&quot;&gt;04 &amp;nbsp;Label&lt;/p&gt;&lt;span class=&quot;badge&quot; id=&quot;badge&quot;&gt;&lt;/span&gt;&lt;/div&gt;
      &lt;p class=&quot;note&quot; id=&quot;note&quot;&gt;&lt;/p&gt;
    &lt;/div&gt;
    &lt;p class=&quot;note&quot;&gt;The card is explicit that this is not a verifier. Plausible FOL can still be wrong, so webAI recommends pairing the model with a symbolic solver or Lean before anything downstream trusts the output.&lt;/p&gt;
  &lt;/div&gt;

  &lt;!-- ============ 2 ============ --&gt;
  &lt;div class=&quot;pane&quot; id=&quot;p2&quot; role=&quot;tabpanel&quot;&gt;
    &lt;p class=&quot;lede&quot;&gt;Track A, in-domain formal logic, scores &amp;times;100. Toggle the arms. &lt;b&gt;TwIL-LM3*&lt;/b&gt; is the version webAI's press numbers come from, and its weights are not released yet.&lt;/p&gt;
    &lt;div class=&quot;ctl&quot;&gt;
      &lt;span class=&quot;lbl&quot;&gt;Arms&lt;/span&gt;
      &lt;button class=&quot;btn&quot; data-s=&quot;ship&quot; aria-pressed=&quot;true&quot;&gt;TwIL-LM3 (shipping)&lt;/button&gt;
      &lt;button class=&quot;btn&quot; data-s=&quot;star&quot; aria-pressed=&quot;true&quot;&gt;TwIL-LM3* (unreleased)&lt;/button&gt;
      &lt;button class=&quot;btn&quot; data-s=&quot;oss&quot; aria-pressed=&quot;true&quot;&gt;gpt-oss-120b&lt;/button&gt;
    &lt;/div&gt;
    &lt;div id=&quot;lanes&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;warn&quot;&gt;&lt;b&gt;Read the column header.&lt;/b&gt; webAI's announcement cites 96.4 rule induction, 87.6 semantic parse, 64.6 Lean, 52.0 MCQ and 68.7 entailment. All five are TwIL-LM3* values. The shipping checkpoint scores 31.92, 44.16, 58.69, 11.00 and 57.50, and trails gpt-oss-120b on the six-lane average, 0.4488 to 0.5192.&lt;/div&gt;
  &lt;/div&gt;

  &lt;!-- ============ 3 ============ --&gt;
  &lt;div class=&quot;pane&quot; id=&quot;p3&quot; role=&quot;tabpanel&quot;&gt;
    &lt;p class=&quot;lede&quot;&gt;Four stages sit on top of SmolLM3-3B. Stage 3 is the one that decides whether general capability survives the fine-tune.&lt;/p&gt;
    &lt;div class=&quot;steps&quot; id=&quot;steps&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;detail&quot; id=&quot;sdet&quot;&gt;&lt;/div&gt;
    &lt;div class=&quot;ctl&quot; style=&quot;margin:18px 0 0&quot;&gt;
      &lt;span class=&quot;lbl&quot;&gt;WiSE-FT setting&lt;/span&gt;
      &lt;button class=&quot;btn&quot; data-l=&quot;0&quot; aria-pressed=&quot;false&quot;&gt;&amp;#955; = 0 (base)&lt;/button&gt;
      &lt;button class=&quot;btn&quot; data-l=&quot;1&quot; aria-pressed=&quot;true&quot;&gt;&amp;#955; = 0.25 (published)&lt;/button&gt;
      &lt;button class=&quot;btn&quot; data-l=&quot;2&quot; aria-pressed=&quot;false&quot;&gt;no interpolation&lt;/button&gt;
    &lt;/div&gt;
    &lt;div class=&quot;gauges&quot;&gt;
      &lt;div class=&quot;g&quot;&gt;&lt;div class=&quot;g-l&quot;&gt;In-domain &amp;middot; macro gate&lt;/div&gt;&lt;div class=&quot;g-v&quot; id=&quot;gv1&quot;&gt;0.4218&lt;/div&gt;
        &lt;div class=&quot;g-b&quot;&gt;&lt;div class=&quot;g-f&quot; id=&quot;gf1&quot; style=&quot;background:var(--blue)&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
      &lt;div class=&quot;g&quot;&gt;&lt;div class=&quot;g-l&quot;&gt;Held-out &amp;middot; macro, 10 CoT sets&lt;/div&gt;&lt;div class=&quot;g-v&quot; id=&quot;gv2&quot;&gt;0.7339&lt;/div&gt;
        &lt;div class=&quot;g-b&quot;&gt;&lt;div class=&quot;g-f&quot; id=&quot;gf2&quot; style=&quot;background:var(--amber)&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;p class=&quot;note&quot; id=&quot;lnote&quot;&gt;&lt;/p&gt;
  &lt;/div&gt;

  &lt;!-- ============ 4 ============ --&gt;
  &lt;div class=&quot;pane&quot; id=&quot;p4&quot; role=&quot;tabpanel&quot;&gt;
    &lt;p class=&quot;lede&quot;&gt;Track B, answers per second. Each dot is one completed answer, released in real time at the measured rate over a two-second window. This is the gap that survives every caveat.&lt;/p&gt;
    &lt;div class=&quot;ctl&quot;&gt;
      &lt;button class=&quot;btn primary&quot; id=&quot;race&quot;&gt;Start &amp;#9654;&lt;/button&gt;
      &lt;button class=&quot;btn&quot; id=&quot;reset&quot;&gt;Reset&lt;/button&gt;
      &lt;span class=&quot;lbl&quot; style=&quot;margin-left:6px&quot;&gt;Elapsed &lt;span id=&quot;clock&quot; style=&quot;color:var(--blue)&quot;&gt;0.0s&lt;/span&gt;&lt;/span&gt;
    &lt;/div&gt;
    &lt;div class=&quot;rt&quot; id=&quot;rt&quot;&gt;&lt;/div&gt;
    &lt;table class=&quot;tbl&quot;&gt;
      &lt;tr&gt;&lt;th&gt;Arm&lt;/th&gt;&lt;th&gt;ans/s&lt;/th&gt;&lt;th&gt;tok/s&lt;/th&gt;&lt;th&gt;mean gen length&lt;/th&gt;&lt;/tr&gt;
      &lt;tr&gt;&lt;td class=&quot;hi&quot;&gt;TwIL-LM3&lt;/td&gt;&lt;td class=&quot;hi&quot;&gt;32.9&lt;/td&gt;&lt;td&gt;15880&lt;/td&gt;&lt;td class=&quot;hi&quot;&gt;482&lt;/td&gt;&lt;/tr&gt;
      &lt;tr&gt;&lt;td&gt;Llama-3.2-3B&lt;/td&gt;&lt;td&gt;31.7&lt;/td&gt;&lt;td&gt;16160&lt;/td&gt;&lt;td&gt;510&lt;/td&gt;&lt;/tr&gt;
      &lt;tr&gt;&lt;td&gt;SmolLM3-3B base&lt;/td&gt;&lt;td&gt;24.9&lt;/td&gt;&lt;td&gt;15564&lt;/td&gt;&lt;td&gt;626&lt;/td&gt;&lt;/tr&gt;
      &lt;tr&gt;&lt;td&gt;Qwen3-8B&lt;/td&gt;&lt;td&gt;4.9&lt;/td&gt;&lt;td&gt;9420&lt;/td&gt;&lt;td&gt;&amp;#8776;1931&lt;/td&gt;&lt;/tr&gt;
      &lt;tr&gt;&lt;td&gt;gpt-oss-120b&lt;/td&gt;&lt;td&gt;4.2&lt;/td&gt;&lt;td&gt;3374&lt;/td&gt;&lt;td&gt;801&lt;/td&gt;&lt;/tr&gt;
    &lt;/table&gt;
    &lt;p class=&quot;note&quot;&gt;gpt-oss-120b was run at MXFP4 on tensor-parallel 2, so its throughput is not directly comparable to the single-GPU bf16 arms. webAI's blog frames this gap as 2.6&amp;times; (32.9 vs 12.6); the Track B table implies roughly 8&amp;times;.&lt;/p&gt;
  &lt;/div&gt;

  &lt;div class=&quot;ft&quot;&gt;
    &lt;span&gt;Source: webAI model cards on Hugging Face, retrieved 10 Aug 2026&lt;/span&gt;
    &lt;span class=&quot;mtp&quot;&gt;Marktechpost&lt;span&gt;.&lt;/span&gt;com&lt;/span&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;script&gt;
(function(){
  var root = document.getElementById('twil');
  var RM = window.matchMedia &amp;&amp; window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  /* ---------- tabs ---------- */
  var tabs = root.querySelectorAll('.tab');
  for (var i=0;i&lt;tabs.length;i++){
    tabs[i].addEventListener('click', function(){
      for (var j=0;j&lt;tabs.length;j++){ tabs[j].setAttribute('aria-selected','false'); }
      this.setAttribute('aria-selected','true');
      var panes = root.querySelectorAll('.pane');
      for (var k=0;k&lt;panes.length;k++){ panes[k].classList.remove('on'); }
      document.getElementById(this.getAttribute('data-p')).classList.add('on');
      if (this.getAttribute('data-p')==='p2'){ drawLanes(); }
      sizeUp();
    });
  }

  /* ---------- 1. autoformalize ---------- */
  var EX = [
    { nl:'All dogs are mammals. Rex is a dog.',
      fol:'\u2200x (Dog(x) \u2192 Mammal(x))\nDog(rex)',
      cc:'Mammal(rex)', lab:'entailment',
      note:'Universal instantiation, then modus ponens. This is the entailment_label lane, where the shipping model scores 0.5750 and gpt-oss-120b scores 0.7750.' },
    { nl:'No reptile is warm-blooded. Every snake is a reptile.',
      fol:'\u2200x (Reptile(x) \u2192 \u00ACWarmBlooded(x))\n\u2200x (Snake(x) \u2192 Reptile(x))',
      cc:'\u2203x (Snake(x) \u2227 WarmBlooded(x))', lab:'contradiction',
      note:'The candidate conclusion is refuted by the premise set, not merely unsupported. Distinguishing these two failure modes is the whole task.' },
    { nl:'Some students passed the exam. Ana is a student.',
      fol:'\u2203x (Student(x) \u2227 Passed(x))\nStudent(ana)',
      cc:'Passed(ana)', lab:'neutral',
      note:'An existential premise does not license a claim about a named individual. Small models fail this lane by defaulting to entailment.' }
  ];
  var cur = 0;
  var exBtns = document.querySelectorAll('#p1 .btn[data-ex]');
  for (var e=0;e&lt;exBtns.length;e++){
    exBtns[e].addEventListener('click', function(){
      for (var m=0;m&lt;exBtns.length;m++){ exBtns[m].setAttribute('aria-pressed','false'); }
      this.setAttribute('aria-pressed','true');
      cur = parseInt(this.getAttribute('data-ex'),10);
      play();
    });
  }
  document.getElementById('run').addEventListener('click', play);

  var timers = [];
  function clearT(){ for (var t=0;t&lt;timers.length;t++){ clearTimeout(timers[t]); } timers=[]; }
  function at(ms,fn){ timers.push(setTimeout(fn, RM ? 0 : ms)); }

  function play(){
    clearT();
    var d = EX[cur];
    var rows = ['r1','r2','r3','r4'];
    for (var r=0;r&lt;rows.length;r++){ document.getElementById(rows[r]).classList.remove('in'); }
    document.getElementById('bar').classList.remove('in');
    document.getElementById('nlp').innerHTML='';
    document.getElementById('folp').textContent='';
    document.getElementById('concl').textContent='';
    document.getElementById('badge').textContent='';
    document.getElementById('note').textContent='';

    at(60, function(){ document.getElementById('r1').classList.add('in'); typeIn('nlp', d.nl, 16); });
    at(60 + d.nl.length*16 + 260, function(){
      document.getElementById('r2').classList.add('in'); typeIn('folp', d.fol, 13);
    });
    var t2 = 60 + d.nl.length*16 + 260 + d.fol.length*13 + 320;
    at(t2, function(){ document.getElementById('r3').classList.add('in');
      document.getElementById('bar').classList.add('in'); });
    at(t2+500, function(){ typeIn('concl', d.cc, 16); });
    at(t2+500+d.cc.length*16+280, function(){
      document.getElementById('r4').classList.add('in');
      document.getElementById('badge').textContent = d.lab;
      document.getElementById('note').textContent = d.note;
      sizeUp();
    });
    if (RM){
      document.getElementById('nlp').textContent=d.nl;
      document.getElementById('folp').textContent=d.fol;
      document.getElementById('concl').textContent=d.cc;
    }
  }
  function typeIn(id, txt, sp){
    var el = document.getElementById(id);
    if (RM){ el.textContent = txt; return; }
    el.textContent=''; var n=0;
    var iv = setInterval(function(){
      n++; el.textContent = txt.slice(0,n);
      if (n&gt;=txt.length){ clearInterval(iv); sizeUp(); }
    }, sp);
    timers.push(iv);
  }
  play();

  /* ---------- 2. benchmarks ---------- */
  var LANES = [
    {n:'lean_formalize', u:'token F1', ship:58.69, star:64.56, oss:63.06},
    {n:'rule_induction', u:'derivation', ship:31.92, star:96.44, oss:65.18},
    {n:'entailment_label', u:'accuracy', ship:57.50, star:68.67, oss:77.50},
    {n:'mcq_answer', u:'accuracy', ship:11.00, star:52.00, oss:7.00},
    {n:'semantic_parse', u:'token F1', ship:44.16, star:87.62, oss:43.31},
    {n:'lean_critic', u:'accuracy', ship:66.00, star:52.00, oss:55.50}
  ];
  var AVG = {n:'average, 6 lanes', u:'summary', ship:44.88, star:70.21, oss:51.92};
  var SHOW = {ship:true, star:true, oss:true};
  var NAMES = {ship:'TwIL-LM3', star:'TwIL-LM3*', oss:'gpt-oss-120b'};

  var sBtns = document.querySelectorAll('#p2 .btn[data-s]');
  for (var s=0;s&lt;sBtns.length;s++){
    sBtns[s].addEventListener('click', function(){
      var k = this.getAttribute('data-s');
      SHOW[k] = !SHOW[k];
      this.setAttribute('aria-pressed', SHOW[k] ? 'true':'false');
      drawLanes();
    });
  }
  function laneHTML(d, isAvg){
    var h = '&lt;div class=&quot;lane'+(isAvg?' avg':'')+'&quot;&gt;&lt;div class=&quot;lane-h&quot;&gt;&lt;span class=&quot;lane-n&quot;&gt;'+d.n+
            '&lt;/span&gt;&lt;span class=&quot;lane-u&quot;&gt;'+d.u+'&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;track&quot;&gt;';
    var keys=['ship','star','oss'];
    for (var q=0;q&lt;keys.length;q++){
      var k=keys[q];
      if(!SHOW[k]) continue;
      h += '&lt;div class=&quot;b-wrap s-'+k+'&quot; title=&quot;'+NAMES[k]+'&quot;&gt;&lt;div class=&quot;b-out&quot;&gt;'+
           '&lt;div class=&quot;b-in&quot; data-w=&quot;'+d[k]+'&quot;&gt;&lt;/div&gt;&lt;/div&gt;&lt;span class=&quot;b-val&quot;&gt;'+d[k].toFixed(2)+'&lt;/span&gt;&lt;/div&gt;';
    }
    return h+'&lt;/div&gt;&lt;/div&gt;';
  }
  function drawLanes(){
    var box = document.getElementById('lanes'); var h='';
    for (var q=0;q&lt;LANES.length;q++){ h += laneHTML(LANES[q], false); }
    h += laneHTML(AVG, true);
    box.innerHTML = h;
    var bars = box.querySelectorAll('.b-in');
    setTimeout(function(){
      for (var b=0;b&lt;bars.length;b++){ bars[b].style.width = bars[b].getAttribute('data-w')+'%'; }
      sizeUp();
    }, RM ? 0 : 40);
  }
  drawLanes();

  /* ---------- 3. training ---------- */
  var STEPS = [
    {k:'STAGE 01', t:'LoRA SFT', d:'&lt;b&gt;LoRA supervised fine-tuning&lt;/b&gt; on a synthetic formal-logic corpus covering the Track A objectives: FOL translation, entailment labelling, semantic parsing, Lean formalisation and critique, procedural reasoning, rule induction.'},
    {k:'STAGE 02', t:'Checkpoint fusion', d:'&lt;b&gt;Parameter-space averaging&lt;/b&gt; of intermediate SFT checkpoints, selected by a diversity probe, rather than simply taking the final checkpoint.'},
    {k:'STAGE 03', t:'WiSE-FT, &amp;#955; = 0.25', d:'&lt;b&gt;Interpolation back toward the pretrained base:&lt;/b&gt; W = (1 &amp;minus; &amp;#955;)&amp;middot;W_base + &amp;#955;&amp;middot;W_finetuned. Only a quarter of the fine-tuned delta is kept. &amp;#955; was chosen by constrained optimisation: maximise in-domain score subject to minimal held-out degradation.'},
    {k:'STAGE 04', t:'MGPO', d:'&lt;b&gt;Entropy-weighted GRPO&lt;/b&gt; reinforcement learning against a programmatic verifier, with partial credit for loose matches and token-F1 so all-fail prompt groups still produce gradient. Published checkpoint is step 2071.'}
  ];
  var sh='';
  for (var st=0;st&lt;STEPS.length;st++){
    sh += '&lt;button class=&quot;step&quot; data-i=&quot;'+st+'&quot; aria-pressed=&quot;'+(st===2?'true':'false')+'&quot;&gt;'+
          '&lt;span class=&quot;k&quot;&gt;'+STEPS[st].k+'&lt;/span&gt;&lt;span class=&quot;t&quot;&gt;'+STEPS[st].t+'&lt;/span&gt;&lt;/button&gt;';
  }
  document.getElementById('steps').innerHTML = sh;
  document.getElementById('sdet').innerHTML = STEPS[2].d;
  var stBtns = document.querySelectorAll('#steps .step');
  for (var y=0;y&lt;stBtns.length;y++){
    stBtns[y].addEventListener('click', function(){
      for (var z=0;z&lt;stBtns.length;z++){ stBtns[z].setAttribute('aria-pressed','false'); }
      this.setAttribute('aria-pressed','true');
      document.getElementById('sdet').innerHTML = STEPS[parseInt(this.getAttribute('data-i'),10)].d;
      sizeUp();
    });
  }

  var LAM = [
    {g:0.3466, h:0.7193, n:'&amp;#955; = 0 keeps none of the fine-tune. This is the SmolLM3-3B base column, shown for reference.'},
    {g:0.4218, h:0.7339, n:'The published release. It is the only arm in the project that gains on both tracks at once: +26% relative in-domain, and +0.022 on the held-out core average.'},
    {g:0.5150, h:0.6139, n:'A sibling arm that skipped the conservative interpolation scored macro gate 0.515 but gave back roughly twelve points of held-out capability. webAI did not publish it; the held-out bar here is derived from that stated twelve-point drop, not a printed figure.'}
  ];
  var lBtns = document.querySelectorAll('#p3 .btn[data-l]');
  for (var l=0;l&lt;lBtns.length;l++){
    lBtns[l].addEventListener('click', function(){
      for (var n=0;n&lt;lBtns.length;n++){ lBtns[n].setAttribute('aria-pressed','false'); }
      this.setAttribute('aria-pressed','true');
      setLam(parseInt(this.getAttribute('data-l'),10));
    });
  }
  function setLam(i){
    var d = LAM[i];
    document.getElementById('gv1').textContent = d.g.toFixed(4);
    document.getElementById('gv2').textContent = d.h.toFixed(4) + (i===2?' *':'');
    document.getElementById('gf1').style.width = (d.g*100)+'%';
    document.getElementById('gf2').style.width = (d.h*100)+'%';
    document.getElementById('lnote').innerHTML = d.n;
    sizeUp();
  }
  setTimeout(function(){ setLam(1); }, RM?0:60);

  /* ---------- 4. throughput ---------- */
  var ARMS = [
    {n:'TwIL-LM3', r:32.9, c:'var(--blue)'},
    {n:'Llama-3.2-3B', r:31.7, c:'#6E7B8A'},
    {n:'SmolLM3-3B base', r:24.9, c:'#6E7B8A'},
    {n:'Qwen3-8B', r:4.9, c:'#6E7B8A'},
    {n:'gpt-oss-120b', r:4.2, c:'var(--amber)'}
  ];
  var rh='';
  for (var a=0;a&lt;ARMS.length;a++){
    rh += '&lt;div class=&quot;rl&quot;&gt;&lt;div class=&quot;rl-h&quot;&gt;&lt;span class=&quot;rl-n&quot;&gt;'+ARMS[a].n+
          '&lt;/span&gt;&lt;span class=&quot;rl-s&quot;&gt;&lt;span id=&quot;cnt'+a+'&quot;&gt;0&lt;/span&gt; answers &amp;middot; '+ARMS[a].r.toFixed(1)+
          '/s&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;lane-t&quot; id=&quot;tk'+a+'&quot;&gt;&lt;/div&gt;&lt;/div&gt;';
  }
  document.getElementById('rt').innerHTML = rh;

  var raf=null, t0=0, acc=[], counts=[], running=false;
  function resetRace(){
    if(raf) cancelAnimationFrame(raf); raf=null; running=false;
    acc=[]; counts=[];
    for (var a2=0;a2&lt;ARMS.length;a2++){
      acc.push(0); counts.push(0);
      document.getElementById('tk'+a2).innerHTML='';
      document.getElementById('cnt'+a2).textContent='0';
    }
    document.getElementById('clock').textContent='0.0s';
    document.getElementById('race').textContent='Start \u25B6';
  }
  var WINDOW = 2.0;                 // seconds of real-time playback
  var MAXDOTS = Math.ceil(32.9*WINDOW) + 1;
  function emit(i){
    var tk = document.getElementById('tk'+i);
    var w = tk.clientWidth || 300;
    var gap = (w - 10) / MAXDOTS;
    var dot = document.createElement('div');
    dot.className='dot';
    dot.style.background = ARMS[i].c;
    dot.style.left = (counts[i]*gap) + 'px';
    dot.style.opacity = '0';
    tk.appendChild(dot);
    setTimeout(function(){ dot.style.transition='opacity .2s'; dot.style.opacity='1'; },8);
    counts[i]++;
    document.getElementById('cnt'+i).textContent = counts[i];
  }
  function step(ts){
    if(!t0) t0=ts;
    var el=(ts-t0)/1000;
    document.getElementById('clock').textContent = el.toFixed(1)+'s';
    for (var i=0;i&lt;ARMS.length;i++){
      var want = Math.floor(el*ARMS[i].r);
      var need = Math.min(want-counts[i], 4);
      for (var q=0;q&lt;need;q++){ emit(i); }
    }
    if (el &lt; WINDOW){ raf=requestAnimationFrame(step); }
    else { running=false; document.getElementById('clock').textContent=WINDOW.toFixed(1)+'s';
           document.getElementById('race').textContent='Replay \u21BA'; }
  }
  document.getElementById('race').addEventListener('click', function(){
    resetRace(); t0=0; running=true;
    this.textContent='Running...';
    raf=requestAnimationFrame(step);
  });
  document.getElementById('reset').addEventListener('click', resetRace);
  resetRace();

  /* ---------- auto-resize ---------- */
  function sizeUp(){
    try{
      var h = root.offsetHeight + 40;
      if (window.parent &amp;&amp; window.parent !== window){
        window.parent.postMessage({twilHeight:h}, '*');
      }
    }catch(err){}
  }
  window.addEventListener('load', sizeUp);
  setInterval(sizeUp, 1200);
  sizeUp();
})();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
"></iframe>
<script>
(function(){
  window.addEventListener("message", function(e){
    if(!e.data || typeof e.data.twilHeight !== "number") return;
    var f = document.getElementById("twil-explainer-frame");
    if(f && e.data.twilHeight > 200 && e.data.twilHeight < 6000){ f.style.height = e.data.twilHeight + "px"; }
  }, false);
})();
</script>



<p class="wp-block-paragraph"></p>



<h2 id="h-performance" class="wp-block-heading"><strong>Performance</strong></h2>



<p class="wp-block-paragraph">webAI's announcement lists 96.4 on rule induction, 87.6 on semantic parsing, 64.6 on Lean formalization, 52.0 on exact-format answering, and 68.7 on entailment labeling. </p>



<p class="wp-block-paragraph">It reports two tracks. On Track A, in-domain formal logic, TwIL-LM3 scores 0.4488 on the six-lane average and 0.4218 on the macro gate, the metric the training pipeline gates on. It leads every arm up to and including LFM2.5-8B-A1B on all six objective lanes, at 0.4218 against 0.3757 with a third of the parameters. It does not lead the two largest arms. <a href="https://huggingface.co/Qwen/Qwen3-8B">Qwen3-8B</a> takes the gate 0.5336 to 0.4218, but most of that is loose-match credit; under strict-7 the two sit at 0.2093 and 0.1971. <a href="https://huggingface.co/openai/gpt-oss-120b">gpt-oss-120b</a> takes the six-lane average 0.5192 to 0.4488.</p>



<p class="wp-block-paragraph">Efficiency is where the model card is unambiguous. TwIL-LM3 produces the shortest generations of any arm, 482 tokens on Track B, and consequently the most answers per second at 32.9 against the 120B's 4.2.</p>


<div class="wp-block-image">
<figure class="aligncenter size-full is-resized"><img fetchpriority="high" decoding="async" width="1992" height="1308" data-attachment-id="81663" data-permalink="https://www.marktechpost.com/2026/08/10/webai-releases-twil-lm-a-1-7b-and-3b-formal-logic-model-family-for-autoformalization-on-local-hardware/screenshot-2026-08-10-at-11-11-44-pm-2/" data-orig-file="https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1.png" data-orig-size="1992,1308" data-comments-opened="0" data-image-meta="{&quot;aperture&quot;:&quot;0&quot;,&quot;credit&quot;:&quot;&quot;,&quot;camera&quot;:&quot;&quot;,&quot;caption&quot;:&quot;&quot;,&quot;created_timestamp&quot;:&quot;0&quot;,&quot;copyright&quot;:&quot;&quot;,&quot;focal_length&quot;:&quot;0&quot;,&quot;iso&quot;:&quot;0&quot;,&quot;shutter_speed&quot;:&quot;0&quot;,&quot;title&quot;:&quot;&quot;,&quot;orientation&quot;:&quot;0&quot;,&quot;alt&quot;:&quot;&quot;}" data-image-title="Screenshot 2026-08-10 at 11.11.44 PM" data-image-description="" data-image-caption="" data-large-file="https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-1024x672.png" src="https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1.png" alt="" class="wp-image-81663" style="aspect-ratio:1.5229607183440654;width:732px;height:auto" srcset="https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1.png 1992w, https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-300x197.png 300w, https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-1024x672.png 1024w, https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-768x504.png 768w, https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-1536x1009.png 1536w, https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-640x420.png 640w, https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-150x98.png 150w, https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-696x457.png 696w, https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-1068x701.png 1068w, https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-1920x1261.png 1920w, https://www.marktechpost.com/wp-content/uploads/2026/08/Screenshot-2026-08-10-at-11.11.44-PM-1-741x486.png 741w" sizes="(max-width: 1992px) 100vw, 1992px" /><figcaption class="wp-element-caption">https://www.webai.com/blog/webai-releases-twil-lm-a-family-of-formal-logic-models-that-outreason-a-120b-model-and-run-on-an-iphone</figcaption></figure>
</div>


<h2 id="h-held-out-transfer" class="wp-block-heading"><strong>Held-out transfer</strong></h2>



<p class="wp-block-paragraph">TwIL-LM3 improves in-domain by +26% relative, macro gate 0.336 to 0.422, while also gaining +0.022 on the held-out core average. The model card calls it the only arm in the project that gains on both tracks. LogicBench moves to 0.7167 from 0.6467. GSM8K slips slightly to 0.8733 from 0.8833, and IFEval regresses to 0.6433 from 0.6767.</p>



<p class="wp-block-paragraph">The 1.7B is a different trade. Its macro-primary score is 0.361 against 0.185 for the unadapted base. Out-of-distribution results are mixed: LogicBench BQA improves to 0.590 from 0.563, while GSM8K falls to 0.380 from 0.413 and ARC-C chain-of-thought falls to 0.463 from 0.587.</p>



<h2 id="h-key-takeaways" class="wp-block-heading"><strong>Key Takeaways</strong></h2>



<ul class="wp-block-list">
<li>TwIL-LM3 (3B) and TwIL-LM (1.7B) target formal logic, both under a non-commercial license.</li>



<li>Shipping TwIL-LM3 trails gpt-oss-120b on the six-lane average, 0.4488 to 0.5192.</li>



<li>Its real edge is efficiency: 32.9 answers/sec from 482-token generations.</li>



<li>WiSE-FT at λ = 0.25 is why in-domain gains do not collapse held-out performance.</li>
</ul>



<p class="wp-block-paragraph"></p>



<hr class="wp-block-separator has-alpha-channel-opacity"/>



<p class="wp-block-paragraph"></p>



<p class="wp-block-paragraph">Check out the<strong> <a href="https://huggingface.co/webAI-Official/TwIL-LM3" target="_blank" rel="noreferrer noopener">Model weights</a> </strong>and<strong> <a href="https://www.webai.com/blog/webai-releases-twil-lm-a-family-of-formal-logic-models-that-outreason-a-120b-model-and-run-on-an-iphone" target="_blank" rel="noreferrer noopener">Technical details</a></strong>.<strong> </strong>Also, feel free to follow us on <strong><a href="https://x.com/intent/follow?screen_name=marktechpost" target="_blank" rel="noreferrer noopener"><mark>Twitter</mark></a></strong> and don’t forget to join our <strong><a href="https://www.reddit.com/r/machinelearningnews/" target="_blank" rel="noreferrer noopener">150k+ML SubReddit</a></strong> and Subscribe to <strong><a href="https://magic.beehiiv.com/v1/f5e63dd4-5653-4f09-83e2-321a8b1ba526?email={{email}}" target="_blank" rel="noreferrer noopener">our Newsletter</a></strong>. Wait! are you on telegram? <strong><a href="https://t.me/machinelearningresearchnews" target="_blank" rel="noreferrer noopener">now you can join us on telegram as well.</a></strong></p>



<p class="wp-block-paragraph">Need to partner with us for promoting your GitHub Repo OR Hugging Face Page OR Product Release OR Webinar etc.?&nbsp;<strong><a href="https://forms.gle/wbash1wF6efRj8G58" target="_blank" rel="noreferrer noopener"><mark>Connect with us</mark></a></strong></p>
<p>The post <a href="https://www.marktechpost.com/2026/08/10/webai-releases-twil-lm-a-1-7b-and-3b-formal-logic-model-family-for-autoformalization-on-local-hardware/">webAI Releases TwIL-LM: A 1.7B and 3B Formal-Logic Model Family for Autoformalization on Local Hardware</a> appeared first on <a href="https://www.marktechpost.com">MarkTechPost</a>.</p>
]]></content:encoded>
					
		
		
		<post-id xmlns="com-wordpress:feed-additions:1">81661</post-id>	</item>
	</channel>
</rss>
