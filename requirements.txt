# 阿卡夏圖書館・每日館報系統
# 對應規格 §16.2

# ---------- RSS 抓取與解析 ----------
feedparser>=6.0,<7.0
requests>=2.31,<3.0
beautifulsoup4>=4.12,<5.0

# ---------- 時間與日期 ----------
python-dateutil>=2.9,<3.0

# ---------- 資料驗證 ----------
pydantic>=2.5,<3.0
jsonschema>=4.21,<5.0
PyYAML>=6.0,<7.0

# ---------- 字串相似度（dedup / event cluster MVP）----------
rapidfuzz>=3.6,<4.0

# ---------- Claude API ----------
anthropic>=0.40,<1.0

# ---------- 測試 ----------
pytest>=8.0,<9.0
pytest-mock>=3.12,<4.0

# 注意：Embedding 套件（sentence-transformers / openai 等）不列入 MVP，
# 待事件聚合需要語意相似度時再加。
